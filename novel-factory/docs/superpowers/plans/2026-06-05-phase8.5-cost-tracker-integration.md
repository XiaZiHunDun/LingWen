# Phase 8.5 Implementation Plan — CostTracker 接入 AgentComputeFn (+ SQLite 持久化)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `CostTracker` 接入 `AgentComputeFn` (GoT compute 注入点) + SQLite 持久化 (`CostTrackerDB`) + Dashboard `WorkflowStatusResponse` 加 `total_cost_usd` 字段, 让成本数据真有用. 0 改旧测试 (cost_tracker default None 兜底), 1 feat commit.

**Architecture:**
- 1 新模块 `infra/agent_system/cost_persistence.py` (~80L) — `CostTrackerDB` mirror `ReadingPowerDB` 模式
- 1 新测试 `tests/agent_system/test_cost_persistence.py` (~120L) — 8 tests (TDD)
- 1 注入点 `infra/agent_system/got_bridge.py:AgentComputeFn` — `cost_tracker` kwarg, handler 成功估 token + record
- 1 透传 `infra/agent_system/master_controller.py:__init__` — `cost_tracker` kwarg, 透传给 `AgentComputeFn`
- 1 透传 `tests/agent_system/_e2e_helpers.py:make_master_with_router` — `cost_tracker` kwarg (default None)
- 1 字段 `dashboard/protocols.py:WorkflowStatusResponse` — `total_cost_usd: float = 0.0`
- 1 填充 `dashboard/app.py:get_active_workflow_status` — 拿 `master.cost_tracker.total_cost()` 填字段
- 3 + 1 + 1 = 5 new tests (got_bridge 3 + dashboard 1 + e2e 1)

**Tech Stack:** sqlite3 (stdlib), pytest, ruff (跟 Phase 8/8.1/8.2/8.3/8.4 一致, 无新依赖)

---

## Context: 为什么本期要做

`CostTracker` 自 Phase 2.13 (2026-06-03) 引入 4 月, 0 接入点. `git grep "CostTracker\|cost_tracker" infra/` 只在 `cost_tracker.py` 自身引用 + `tiered_router.py:55-66` (Optional 注入) + `llm_compute.py:71-76` (Optional 注入, 但无人调). 生产路径从不写记录, "成本追踪" 实际是空架子.

**当前瓶颈**:
- `agent.chat()` → `router.generate()` → `provider.generate()` 返回 `str`, 用量被丢
- `AgentComputeFn.__call__` (`got_bridge.py:188-243`) 是 perfect 注入点: 知道 `node.prompt_scenario`, handler 返回后能估 token + 写 `cost_tracker.record()`
- `ComputeResult.cost_tokens=0` 硬编码 (`got_bridge.py:219, 243`), GoTScheduler 看到永远是 0
- `SCENARIO_TIER_MAP` (`scenarios.py:108-131`) 已就位 — 12 scenario → 3 tier 静态映射
- `infra/ai_service/model_tiers.compute_cost(in, out, tier)` 已就位

**主公决策** (2026-06-05): 选 Option B (Mid — + SQLite 持久化, 3 选项中 B 居中, 范围不画 UI 保持 phase 边界).

**当前 baseline**: 2090 passed, 0 failed, 21 skipped
**目标 baseline**: 2103 passed, 0 failed, 21 skipped (2090 + 13 new)

**TDD 风格**: Task 2 严格 RED → GREEN → refactor; Task 3/4 先写 RED 测试后改 impl.

---

## 实施原则

- **Token 估算公式**: `len(prompt + response) // 4` (跟 `infra/ai_service/tiered_router.py:148-149` 公式一致, 4 char/token 经验值)
- **Tier 来源**: `SCENARIO_TIER_MAP[node.prompt_scenario]` (已存在, 12 scenario 静态映射)
- **成本计算**: 复用 `compute_cost(in_tok, out_tok, tier)` (不重新实现)
- **Backward compat**: `cost_tracker: Optional[CostTracker] = None`, None 时跳过 record (旧测试零修改)
- **DB persistence**: mirror `ReadingPowerDB` (sqlite3 + Row factory + `_connect` context manager + `_init_db` lazy)
- **Dashboard**: 仅加 `total_cost_usd: float = 0.0` 字段, **不画 UI** (YAGNI, 留 followup)
- **gitignored**: `infra/.state/cost_tracker.db` 跟现有 `.state/workflow.db` / `decisions.json` 一致, 永不 commit

---

## File Map

| 类别 | 路径 | 操作 | 行数 |
|------|------|------|------|
| 新 spec | `docs/superpowers/specs/2026-06-05-phase8.5-cost-tracker-integration-design.md` (NEW) | 创建 | ~120L |
| 新 plan | `docs/superpowers/plans/2026-06-05-phase8.5-cost-tracker-integration.md` (NEW) | 创建 | ~250L (本文件) |
| 新模块 | `infra/agent_system/cost_persistence.py` (NEW) | 创建 | ~80L |
| 新测试 | `tests/agent_system/test_cost_persistence.py` (NEW) | 创建 | ~120L |
| 改 wiring | `infra/agent_system/got_bridge.py:188-243` | 加 `cost_tracker` 注入 | +15L |
| 改 wiring | `infra/agent_system/master_controller.py:__init__` | 加 `cost_tracker` kwarg + 透传 | +5L |
| 改 wiring | `tests/agent_system/_e2e_helpers.py:make_master_with_router` | 加 `cost_tracker` 透传 (default None) | +3L |
| 改 dashboard | `dashboard/protocols.py:WorkflowStatusResponse` | 加 `total_cost_usd: float = 0.0` | +1L |
| 改 dashboard | `dashboard/app.py:get_active_workflow_status` | 拿 cost_tracker 累加填字段 | +5L |
| 同步 stub | `tests/agent_system/test_got_bridge.py:TestComputeFn` | 3 cost_tracker 注入测试 | +30L |
| 同步 e2e | `tests/agent_system/test_master_controller_stub_router_e2e.py` | 1 test 验证 cost_tokens > 0 | +5L |
| 同步 dashboard | `tests/dashboard/test_decision_api.py` | 1 test 验证 total_cost_usd 字段 | +30L |

**估计**: 2 new docs + 2 new modules + 1 commit. 总改 12 文件, +295L 新, 0 改 production code 行为 (仅加注入点).

---

## Task 1: 写 spec + plan docs (per 项目 pattern)

- [x] **Step 1.1**: 写 spec doc `docs/superpowers/specs/2026-06-05-phase8.5-cost-tracker-integration-design.md` (NEW, ~120L)
- [x] **Step 1.2**: 写 plan doc (本文件, NEW, ~250L)

---

## Task 2: TDD `cost_persistence.py` + `test_cost_persistence.py`

### Step 2.1: 写 RED tests (8 tests)

**File**: `tests/agent_system/test_cost_persistence.py` (NEW, ~120L)

**Tests** (1 class `TestCostTrackerDB`):
1. `test_init_db_creates_table` — DB 不存在 → 调 `init_db` → table 存在
2. `test_record_persists_to_db` — record 1 条 → `records()` 返回 1 条 + 字段对齐
3. `test_total_cost_aggregates_correctly` — record 3 条 → `total_cost()` 等于 3 条 sum
4. `test_cost_by_scenario_groups_correctly` — 2 scenario 各 2 条 → `cost_by_scenario()` 返 2 keys
5. `test_cost_by_tier_groups_correctly` — HAIKU 1 + SONNET 2 → `cost_by_tier()` 返 2 keys
6. `test_records_survives_reopen` — record → close conn → 重新 open DB → records 还在
7. `test_init_db_idempotent` — 调 2 次 `init_db` 不报错
8. `test_db_path_under_state_dir` — `cost_tracker.db` 路径在 `infra/.state/`

### Step 2.2: 跑 RED 验证

```bash
pytest tests/agent_system/test_cost_persistence.py -v
# 期望: ModuleNotFoundError (cost_persistence 不存在) — RED 确认
```

### Step 2.3: 写 `cost_persistence.py` (GREEN)

**File**: `infra/agent_system/cost_persistence.py` (NEW, ~80L)

**Code skeleton** (mirror `ReadingPowerDB` pattern):
```python
"""Cost 持久化 — Phase 8.5

SQLite 持久化 CostRecord 列表 (mirror ReadingPowerDB pattern).
DB 路径: infra/.state/cost_tracker.db (gitignored).
"""
from __future__ import annotations

import sqlite3
from contextlib import contextmanager
from datetime import datetime
from pathlib import Path
from typing import Iterator, Optional

from infra.ai_service.cost_tracker import CostRecord
from infra.ai_service.model_tiers import ModelTier, compute_cost


_DB_PATH = Path(__file__).parent.parent / ".state" / "cost_tracker.db"


class CostTrackerDB:
    """SQLite 持久化 CostRecord (Phase 8.5)

    单表 cost_records (id PK, scenario, tier, input_tokens, output_tokens,
    cost_usd, timestamp). 镜像 ReadingPowerDB 模式.
    """

    def __init__(self, db_path: Optional[Path] = None, init_if_missing: bool = True) -> None:
        self.db_path = db_path or _DB_PATH
        if init_if_missing:
            self._ensure_db_path()

    def _ensure_db_path(self) -> None:
        if not self.db_path.parent.exists():
            self.db_path.parent.mkdir(parents=True, exist_ok=True)

    @contextmanager
    def _connect(self) -> Iterator[sqlite3.Connection]:
        conn = sqlite3.connect(str(self.db_path), timeout=5)
        conn.row_factory = sqlite3.Row
        try:
            yield conn
            conn.commit()
        finally:
            conn.close()

    def init_db(self) -> None:
        with self._connect() as conn:
            conn.executescript("""
                CREATE TABLE IF NOT EXISTS cost_records (
                    id INTEGER PRIMARY KEY AUTOINCREMENT,
                    scenario TEXT NOT NULL,
                    tier TEXT NOT NULL,
                    input_tokens INTEGER NOT NULL,
                    output_tokens INTEGER NOT NULL,
                    cost_usd REAL NOT NULL,
                    timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP
                );
                CREATE INDEX IF NOT EXISTS idx_cost_records_scenario ON cost_records(scenario);
                CREATE INDEX IF NOT EXISTS idx_cost_records_tier ON cost_records(tier);
            """)

    def record(self, scenario: str, tier: ModelTier, input_tokens: int, output_tokens: int) -> CostRecord:
        cost = compute_cost(input_tokens, output_tokens, tier)
        rec = CostRecord(
            scenario=scenario, tier=tier,
            input_tokens=input_tokens, output_tokens=output_tokens,
            cost_usd=cost,
        )
        with self._connect() as conn:
            conn.execute(
                """INSERT INTO cost_records
                   (scenario, tier, input_tokens, output_tokens, cost_usd, timestamp)
                   VALUES (?, ?, ?, ?, ?, ?)""",
                (rec.scenario, rec.tier.value, rec.input_tokens, rec.output_tokens,
                 rec.cost_usd, rec.timestamp.isoformat()),
            )
        return rec

    def records(self) -> list[CostRecord]:
        with self._connect() as conn:
            rows = conn.execute(
                """SELECT scenario, tier, input_tokens, output_tokens, cost_usd, timestamp
                   FROM cost_records ORDER BY id"""
            ).fetchall()
        return [
            CostRecord(
                scenario=r["scenario"],
                tier=ModelTier(r["tier"]),
                input_tokens=r["input_tokens"],
                output_tokens=r["output_tokens"],
                cost_usd=r["cost_usd"],
                timestamp=datetime.fromisoformat(r["timestamp"]),
            )
            for r in rows
        ]

    def total_cost(self) -> float:
        with self._connect() as conn:
            row = conn.execute("SELECT COALESCE(SUM(cost_usd), 0.0) as total FROM cost_records").fetchone()
        return float(row["total"])

    def cost_by_scenario(self) -> dict[str, float]:
        with self._connect() as conn:
            rows = conn.execute(
                """SELECT scenario, SUM(cost_usd) as total
                   FROM cost_records GROUP BY scenario"""
            ).fetchall()
        return {r["scenario"]: float(r["total"]) for r in rows}

    def cost_by_tier(self) -> dict[ModelTier, float]:
        with self._connect() as conn:
            rows = conn.execute(
                """SELECT tier, SUM(cost_usd) as total
                   FROM cost_records GROUP BY tier"""
            ).fetchall()
        return {ModelTier(r["tier"]): float(r["total"]) for r in rows}
```

### Step 2.4: 跑 GREEN 验证

```bash
pytest tests/agent_system/test_cost_persistence.py -v
# 期望: 8 passed
```

---

## Task 3: Wire `CostTracker` into `AgentComputeFn` + `MasterController`

### Step 3.1: 写 RED tests in `test_got_bridge.py` (3 tests)

**File**: `tests/agent_system/test_got_bridge.py` (+30L)

**Tests** (3 new in `TestComputeFn` class):
1. `test_compute_fn_records_cost_when_tracker_provided` — AgentComputeFn 注入 CostTracker → handler 调 → `cost_tracker.total_cost() > 0`
2. `test_compute_fn_no_tracker_is_noop` — AgentComputeFn 不传 cost_tracker → handler 调 → 0 AttributeError (旧测试不破)
3. `test_compute_result_cost_tokens_filled` — AgentComputeFn 注入 CostTracker → `ComputeResult.cost_tokens > 0` (替代硬编码 0)

**Stub pattern** (跟 Phase 7.1 模式):
```python
from infra.ai_service.cost_tracker import CostTracker
from infra.ai_service.model_tiers import ModelTier


def test_compute_fn_records_cost_when_tracker_provided(monkeypatch):
    master = _StubMaster()
    cost_tracker = CostTracker()
    compute = AgentComputeFn(master, cost_tracker=cost_tracker)

    node = _node("n1", "chapter_writing")
    result = compute(node, {"chapter_num": 1, "use_llm": False})

    assert result.fail is False
    assert cost_tracker.total_cost() > 0
    assert result.cost_tokens > 0
```

### Step 3.2: 跑 RED 验证

```bash
pytest tests/agent_system/test_got_bridge.py -k "records_cost or no_tracker_is_noop or cost_tokens_filled" -v
# 期望: TypeError (AgentComputeFn 不接受 cost_tracker kwarg) — RED 确认
```

### Step 3.3: 改 `got_bridge.py` (GREEN)

**File**: `infra/agent_system/got_bridge.py:188-243` (+15L)

**Changes**:
1. Import: `from infra.ai_service.cost_tracker import CostTracker`
2. Import: `from infra.ai_service.model_tiers import ModelTier`
3. Import: `from infra.prompt_engineering.scenarios import SCENARIO_TIER_MAP`
4. `AgentComputeFn.__init__` 加 `cost_tracker: Optional[CostTracker] = None` 参数
5. `AgentComputeFn.__call__` 在 handler 成功返回后:
   - 估 `in_tok = len(str(inputs)) // 4`, `out_tok = len(str(output)) // 4`
   - `tier = SCENARIO_TIER_MAP.get(scenario, ModelTier.SONNET)` (默认 SONNET)
   - `if self._cost_tracker: cost_tracker.record(scenario, tier, in_tok, out_tok)`
   - `cost_tokens = in_tok + out_tok` (替换硬编码 0)

### Step 3.4: 改 `master_controller.py` (1-line propagation)

**File**: `infra/agent_system/master_controller.py:__init__` (+5L)

**Change**:
```python
def __init__(self, ..., cost_tracker: Optional["CostTracker"] = None) -> None:
    ...
    self.cost_tracker = cost_tracker  # 透传给 AgentComputeFn
```

In `build_got_scheduler` call (got_bridge.py:248-279):
```python
compute = AgentComputeFn(master, cost_tracker=master.cost_tracker)
```

### Step 3.5: 改 `_e2e_helpers.py` (default None)

**File**: `tests/agent_system/_e2e_helpers.py:make_master_with_router` (+3L)

**Change**:
```python
def make_master_with_router(
    state_dir: Path,
    router: AIRouter,
    cost_tracker: Optional[CostTracker] = None,
) -> MasterController:
    return MasterController(
        state_dir=str(state_dir),
        router=router,
        config=_minimal_config(str(state_dir)),
        cost_tracker=cost_tracker,
    )
```

(默认 None → 旧 14 e2e tests 零修改)

### Step 3.6: 跑 GREEN 验证

```bash
pytest tests/agent_system/test_got_bridge.py tests/agent_system/test_master_controller.py -v
# 期望: 全部 passed (含 3 new)

pytest tests/agent_system/test_master_controller_stub_router_e2e.py -v
# 期望: 14 passed (cost_tracker=None 兜底, 旧测试零修改)
```

---

## Task 4: Dashboard `total_cost_usd` 字段

### Step 4.1: 改 `dashboard/protocols.py`

**File**: `dashboard/protocols.py:MasterControllerAdapter.get_active_workflow_status` (+5L)

**Change** in the returned dict (add `total_cost_usd`):
```python
# Phase 8.5: pull total cost from master's cost_tracker (0.0 if not wired)
total_cost = 0.0
cost_tracker = getattr(self._controller, "cost_tracker", None)
if cost_tracker is not None:
    total_cost = cost_tracker.total_cost()
return {
    ...,
    "total_cost_usd": total_cost,
}
```

### Step 4.2: 改 `dashboard/app.py:WorkflowStatusResponse`

**File**: `dashboard/app.py:WorkflowStatusResponse` (+1L)

**Change**:
```python
class WorkflowStatusResponse(BaseModel):
    """工作流状态响应"""
    workflow_name: Optional[str] = None
    is_active: bool = False
    completed: int = 0
    failed: int = 0
    paused: bool = False
    paused_nodes: list[str] = Field(default_factory=list)
    node_count: int = 0
    steps: int = 0
    total_cost_usd: float = 0.0  # Phase 8.5: 0.0 if no cost_tracker wired
    pending_decisions: list[dict[str, Any]] = Field(default_factory=list)
    executions: dict[str, str] = Field(default_factory=dict)
    score_data: dict[str, dict[str, Any]] = Field(default_factory=dict)
```

### Step 4.3: 写 RED test in `test_decision_api.py`

**File**: `tests/dashboard/test_decision_api.py` (+30L)

**Test**:
```python
def test_active_workflow_status_includes_total_cost_usd(self, tmp_path: Path):
    """Phase 8.5: total_cost_usd reflects cost_tracker.total_cost()"""
    from infra.agent_system import master_controller as mc_mod
    from infra.ai_service.cost_tracker import CostTracker
    from infra.ai_service.model_tiers import ModelTier
    from infra.got.data_structures import NodeExecution, NodeStatus, NodeType, ThoughtNode
    from datetime import datetime, timezone

    # 构造 fake master, _last_* 缓存指向有 polish_merge 节点
    master = mc_mod.MasterController.__new__(mc_mod.MasterController)
    master.cost_tracker = CostTracker()
    master.cost_tracker.record("chapter_writing", ModelTier.SONNET, 1000, 500)

    class _StubGraph:
        def node_ids(self): return []
        def has_execution(self, nid): return False
        def get_execution(self, nid): return None
        def get_node(self, nid): return None
    class _StubSummary: steps = 0
    class _StubScheduler: _summary = _StubSummary()
    master._last_scheduler = _StubScheduler()
    master._last_graph = _StubGraph()
    master._last_workflow_name = "novel_writing"

    app = create_app(db_path=tmp_path / "rp.db", master_controller=master)
    client = TestClient(app)
    response = client.get("/api/workflows/active")
    assert response.status_code == 200
    assert response.json()["total_cost_usd"] == pytest.approx(0.0105, abs=1e-6)
```

### Step 4.4: 跑 GREEN 验证

```bash
pytest tests/dashboard/test_decision_api.py -v
# 期望: 全部 passed (含 1 new)
```

---

## Task 5: 同步 hardcode (1 e2e test 验证 cost > 0)

### Step 5.1: 写 RED test in `test_master_controller_stub_router_e2e.py`

**File**: `tests/agent_system/test_master_controller_stub_router_e2e.py` (+5L)

**Add to `TestNovelWritingE2E`** (1 new test):
```python
def test_novel_writing_records_cost_to_tracker(self, tmp_path: Path) -> None:
    """Phase 8.5: 跑生产 path 后, cost_tracker 至少记录 1 笔 (写+审核)"""
    from infra.ai_service.cost_tracker import CostTracker
    cost_tracker = CostTracker()
    router, _ = make_stub_router()
    master = make_master_with_router(tmp_path, router, cost_tracker=cost_tracker)
    master.run_workflow(
        workflow_name="novel_writing",
        initial_inputs={
            "chapter_num": 1,
            "outline": self.MINIMAL_OUTLINE,
            "characters": [],
            "memory_context": {},
            "style_guide": {},
            "timeline": [],
            "use_llm": True,
        },
    )
    assert cost_tracker.total_cost() > 0  # 至少 1 笔
    assert cost_tracker.count_by_scenario()  # 至少 1 scenario
```

---

## Task 6: 验证 2090 baseline + 13 new tests

```bash
pytest -q 2>&1 | tail -5
# 期望: 2103 passed, 0 failed, 21 skipped (2090 + 13 new)

pytest tests/agent_system/test_cost_persistence.py -v
# 期望: 8 passed

pytest tests/agent_system/test_got_bridge.py -k "cost" -v
# 期望: 3 passed

pytest tests/dashboard/test_decision_api.py -k "total_cost" -v
# 期望: 1 passed

pytest tests/agent_system/test_master_controller_stub_router_e2e.py -v
# 期望: 15 passed (14 + 1 new)

# ruff
ruff check infra/agent_system/cost_persistence.py infra/agent_system/got_bridge.py infra/agent_system/master_controller.py tests/agent_system/test_cost_persistence.py tests/agent_system/test_got_bridge.py
# 期望: 0 issues

# grep 验证 (no false references)
grep -rn "cost_tracker" infra/ --include="*.py" | grep -v test_cost_persistence | grep -v cost_tracker.py | grep -v cost_persistence.py
# 期望: 只 got_bridge.py + master_controller.py (2 注入点) + _e2e_helpers.py (1 透传) + tiered_router.py (既有) + llm_compute.py (既有)
```

---

## Task 7: 1 feat commit

```bash
git add docs/superpowers/specs/2026-06-05-phase8.5-cost-tracker-integration-design.md \
        docs/superpowers/plans/2026-06-05-phase8.5-cost-tracker-integration.md \
        infra/agent_system/cost_persistence.py \
        infra/agent_system/got_bridge.py \
        infra/agent_system/master_controller.py \
        tests/agent_system/_e2e_helpers.py \
        tests/agent_system/test_cost_persistence.py \
        tests/agent_system/test_got_bridge.py \
        tests/agent_system/test_master_controller_stub_router_e2e.py \
        dashboard/protocols.py \
        dashboard/app.py \
        tests/dashboard/test_decision_api.py

git status  # 确认 12 改文件

git commit -m "feat(cost): phase 8.5 — wire CostTracker into AgentComputeFn + SQLite persistence
...
"
```

**严禁**: 0 force-push, 0 --no-verify, 0 Co-Authored-By footer
**严禁**: 0 push (留待主公 review commit 后手动 push)

---

## Critical files

### Created
- `docs/superpowers/specs/2026-06-05-phase8.5-cost-tracker-integration-design.md` (~120L)
- `docs/superpowers/plans/2026-06-05-phase8.5-cost-tracker-integration.md` (~250L, 本文件)
- `infra/agent_system/cost_persistence.py` (~80L) — SQLite 持久化 CostRecord
- `tests/agent_system/test_cost_persistence.py` (~120L) — 8 tests

### Modified (注入点 + 透传 + 字段)
- `infra/agent_system/got_bridge.py:188-243` (AgentComputeFn 加 cost_tracker, 估 token, 写 record)
- `infra/agent_system/master_controller.py:__init__` (加 cost_tracker kwarg, 透传 AgentComputeFn)
- `tests/agent_system/_e2e_helpers.py:make_master_with_router` (加 cost_tracker 透传, default None)
- `dashboard/protocols.py:MasterControllerAdapter.get_active_workflow_status` (加 total_cost_usd)
- `dashboard/app.py:WorkflowStatusResponse` (加 total_cost_usd 字段)
- `tests/agent_system/test_got_bridge.py:TestComputeFn` (3 new cost tests)
- `tests/agent_system/test_master_controller_stub_router_e2e.py:TestNovelWritingE2E` (1 new test)
- `tests/dashboard/test_decision_api.py` (1 new test)

### Referenced / 复用 (EXISTING, 0 改)
- `infra/ai_service/cost_tracker.py:CostRecord/CostTracker`
- `infra/ai_service/model_tiers.py:compute_cost/MODEL_TIERS`
- `infra/prompt_engineering/scenarios.py:SCENARIO_TIER_MAP`
- `dashboard/app.py:ReadingPowerDB:154-345` (pattern template)
- `infra/agent_system/got_bridge.py:SCENARIO_HANDLERS`

### NOT modified
- `infra/ai_service/cost_tracker.py` (CostTracker 自身)
- `infra/ai_service/model_tiers.py`
- `infra/ai_service/tiered_router.py`
- 任何 provider (`anthropic_provider.py` / `openai_provider.py` / `minimax_provider.py`)
- `infra/prompt_engineering/scenarios.py`
- `dashboard/*.vue`
- `pyproject.toml` / `pytest.ini` / `CLAUDE.md`
- `infra/.state/cost_tracker.db` (NOT committed, gitignored)
- 18 历史 spec/plan docs (0 改, 跟 Phase 8.4 一样不动)

---

## 完成定义 (DoD)

- [ ] `docs/superpowers/specs/2026-06-05-phase8.5-cost-tracker-integration-design.md` 创建 (~120L)
- [ ] `docs/superpowers/plans/2026-06-05-phase8.5-cost-tracker-integration.md` 创建 (~250L)
- [ ] `infra/agent_system/cost_persistence.py` 创建 (~80L, 0 lint)
- [ ] `tests/agent_system/test_cost_persistence.py` 创建 (8 tests passed)
- [ ] `infra/agent_system/got_bridge.py` AgentComputeFn 加 cost_tracker (default None)
- [ ] `infra/agent_system/master_controller.py` __init__ 加 cost_tracker kwarg (default None)
- [ ] `tests/agent_system/_e2e_helpers.py:make_master_with_router` 加 cost_tracker 透传 (default None)
- [ ] `dashboard/protocols.py:MasterControllerAdapter.get_active_workflow_status` 加 `total_cost_usd` 字段
- [ ] `dashboard/app.py:WorkflowStatusResponse` 加 `total_cost_usd: float = 0.0` 字段
- [ ] `tests/agent_system/test_got_bridge.py:TestComputeFn` 3 new cost tests passed
- [ ] `tests/dashboard/test_decision_api.py` 1 new total_cost_usd test passed
- [ ] `tests/agent_system/test_master_controller_stub_router_e2e.py` 1 new cost e2e test passed
- [ ] 1 feat commit
- [ ] pytest 2103 passed, 0 failed, 21 skipped (2090 + 13 new)
- [ ] ruff 0
- [ ] 0 旧测试代码改动 (2090 baseline 不变)
- [ ] 0 production code 行为破坏
- [ ] 0 CLAUDE.md / pyproject.toml / pytest.ini 改动
- [ ] 0 误改 .env / 0 API key 泄漏
- [ ] 0 infra/.state/* 提交 (gitignored)
- [ ] 0 force-push, 0 --no-verify, 0 Co-Authored-By footer
- [ ] 0 provider 改 (YAGNI, 留 Phase 8.6)
- [ ] 0 dashboard UI 改 (YAGNI, 留 Phase 8.7)
- [ ] 0 tiered_router 改 (YAGNI, 留 Phase 8.8+)
