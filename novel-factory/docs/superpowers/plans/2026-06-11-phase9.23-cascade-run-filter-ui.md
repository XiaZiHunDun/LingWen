# Phase 9.23 Implementation Plan — Cascade Run Filter UI

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `CascadeRunsPanel` 加 3 filter (status + min/max depth + algorithm), URL search params 是 single source of truth, 走 `get_ripple_cascade_runs` 新 query params (status + 3 new).

**Architecture:** 4 layers, 全部 additive: storage 加 3 SQL WHERE clauses → endpoint 加 4 `Query()` params (status 从 storage 暴露 + 3 new) → API client 扩 `fetchCascadeRuns` options → 新 `CascadeRunsFilter.vue` + `CascadeRunsPanel.vue` route watcher. 0 改 Phase 9.22 既有 behavior.

**Tech Stack:** Vue 3 SFC + Vue Router 4 (`useRoute` / `useRouter`) + FastAPI 0.x (`Query` / `regex`) + Pydantic v2 + Vitest 4.1.8 + @vue/test-utils 2.4.11 + jsdom + Playwright 1.49 + pytest + ruff + pnpm build.

**Baseline**: 2469 pytest + 159 vitest + 12 e2e = 2640 total, 0 failed, 27 skipped.
**Target**: 2478 pytest + 167 vitest + 14 e2e = 2659 total (delta: +9 pytest / +8 vitest / +2 e2e = +19 tests).

**Spec**: `docs/superpowers/specs/2026-06-11-phase9.23-cascade-run-filter-ui-design.md` (commit `8b15a3f`).

---

## 实施原则

- **Backend 2 task 先**: storage 是 endpoint 前置, 1:1 (storage 改 → endpoint 改 → API client 改 → UI 改). TDD per task.
- **URL 是 single source of truth**: Phase 9.22 既 3 vitest 0 改 contract (loadRuns() 仍走 cascade_runs list endpoint, 但读 filters.value 而不是 0 param).
- **Regex 校验**: FastAPI `Query(default=None, regex="^(running|completed|cancelled|failed)$")` 跟 Phase 9.20 既 endpoint 风格 1:1, Pydantic v2 接受.
- **No debounce**: `emitNumber` 在 filter 内部 NaN 防御, 只 valid 数字 emit → URL → router.replace → 1 fetch (1 keypress = 1 fetch, dropdown = 1 fetch).
- **CascadeRunsPanel 0 改 Phase 9.22 WS handler + replay + cancel + formatRelative**: 既 3 vitest 0 改 contract.
- **0 改 cascade_runs schema / CascadeRunResponse / CascadeRun / CascadeGraph / RippleDrawer / RippleFilter / useWorkflowSocket / useRippleStore / cascade_notifier / lingwen.py / 19 historical spec:NNN**: 全部 spec-level scope boundary.
- **e2e mock pattern**: 复用 Phase 9.22 既 `page.route('**/api/ripples/cascade/**', ...)` 模式, 加 1 new case 验证 filter dropdown → URL → 1 new fetch with `?status=cancelled` query param.
- **vitest mock pattern**: 复用 Phase 9.22 既 `vi.mock('../../src/api/index.js')` + `globalThis.__cascadeHandlers = new Set()` 模式, 加 mock `useRoute/useRouter` 走 `vue-router` 内部 push/replace stub.

---

## File Map (12 files, +1280L, 0 production code 行为破坏)

| 类别 | 路径 | 操作 | 行数 |
|------|------|------|------|
| 改 storage | `infra/cross_volume/storage.py:742-768` | 3 new params + 3 WHERE clauses | +12L |
| 改 endpoint | `dashboard/app.py:1127-1146` | 4 new Query() params (status + 3 new) | +12L |
| 改 API client | `dashboard/frontend/src/api/index.js:341-348` | 3 new URLSearchParams.set | +3L |
| 新 filter 组件 | `dashboard/frontend/src/components/CascadeRunsFilter.vue` | NEW | ~85L |
| 改 panel | `dashboard/frontend/src/components/CascadeRunsPanel.vue` | +useRoute/useRouter + 2 helpers + onFilterChange + watch + 1 template line | +60L |
| 新 storage tests | `tests/cross_volume/test_cascade_runs_filters.py` | NEW | ~120L |
| 新 endpoint tests | `tests/dashboard/test_cascade_runs_filters_endpoint.py` | NEW | ~150L |
| 新 vitest filter | `dashboard/frontend/tests/unit/cascade-runs-filter.spec.js` | NEW | ~120L |
| 改 vitest panel | `dashboard/frontend/tests/unit/cascade-runs-panel.spec.js` | +2-3 URL sync tests | +50L |
| 新 e2e | `dashboard/frontend/tests/e2e-smoke/cascade-runs-filter.spec.js` | NEW | ~80L |
| 新 plan | `docs/superpowers/plans/2026-06-11-phase9.23-cascade-run-filter-ui.md` | NEW (本 doc) | ~830L |
| Spec (NEW) | `docs/superpowers/specs/2026-06-11-phase9.23-cascade-run-filter-ui-design.md` | NEW (Phase 9.23 brainstorm 阶段已 commit `8b15a3f`) | ~340L |

---

## Task 1: Storage 扩 3 params + 3 WHERE clauses (TDD: RED → GREEN)

### 1.1 Sub-task 1A: Write failing storage tests (RED)

**Files**:
- Create: `tests/cross_volume/test_cascade_runs_filters.py` (~120L, 4 tests)

**Tests** (4 tests, 1 class `TestGetCascadeRunsFilters`):

```python
# tests/cross_volume/test_cascade_runs_filters.py
"""Phase 9.23: RippleStorage.get_cascade_runs filter param tests (TDD).

覆盖 3 new filter params (min_depth, max_depth, algorithm) + 1 combined
跟 status. 既 status filter 跟 Phase 9.20 既有 test 重叠, 不重复测试.
"""
import pytest
from infra.cross_volume.storage import RippleStorage


@pytest.fixture
def storage_with_runs(tmp_path):
    """Create temp storage with 4 cascade_runs spanning depths/algorithms/statuses."""
    db_path = tmp_path / "test.db"
    storage = RippleStorage(db_path=str(db_path))
    # Seed: ripple-1 with 4 runs (v2_weighted @ depth 2, v1 @ depth 5, v2_weighted @ depth 8 running, v1 @ depth 3 cancelled)
    storage.record_cascade_run(
        ripple_id="rip-1", max_depth=2, depth_reached=2,
        algorithm_version="v2_weighted",
        cascade_nodes=[], cascade_edges=[], cascade_actions=[],
    )
    storage.record_cascade_run(
        ripple_id="rip-1", max_depth=5, depth_reached=5,
        algorithm_version="v1",
        cascade_nodes=[], cascade_edges=[], cascade_actions=[],
    )
    # Mark run #2 as running (force open status)
    storage._connect().execute(
        "UPDATE cascade_runs SET status='running', completed_at='' WHERE max_depth=5"
    )
    storage._connect().commit()
    storage.record_cascade_run(
        ripple_id="rip-1", max_depth=8, depth_reached=3,
        algorithm_version="v2_weighted",
        cascade_nodes=[], cascade_edges=[], cascade_actions=[],
    )
    storage.record_cascade_run(
        ripple_id="rip-1", max_depth=3, depth_reached=0,
        algorithm_version="v1",
        cascade_nodes=[], cascade_edges=[], cascade_actions=[],
    )
    storage._connect().execute(
        "UPDATE cascade_runs SET status='cancelled', completed_at='2026-06-11T00:00:00Z' WHERE max_depth=3"
    )
    storage._connect().commit()
    return storage


class TestGetCascadeRunsFilters:
    def test_min_depth_filter_includes_specified_and_above(self, storage_with_runs):
        """min_depth=4 returns runs with max_depth >= 4 (run @5, run @8)."""
        runs = storage_with_runs.get_cascade_runs("rip-1", min_depth=4)
        depths = sorted(r.max_depth for r in runs)
        assert depths == [5, 8]

    def test_max_depth_filter_includes_specified_and_below(self, storage_with_runs):
        """max_depth=5 returns runs with max_depth <= 5 (run @2, run @3, run @5)."""
        runs = storage_with_runs.get_cascade_runs("rip-1", max_depth=5)
        depths = sorted(r.max_depth for r in runs)
        assert depths == [2, 3, 5]

    def test_algorithm_filter_exact_match(self, storage_with_runs):
        """algorithm='v1' returns only v1 runs (2 rows: max_depth=5 running, max_depth=3 cancelled)."""
        runs = storage_with_runs.get_cascade_runs("rip-1", algorithm="v1")
        algorithms = sorted({r.algorithm for r in runs})
        assert algorithms == ["v1"]
        assert len(runs) == 2

    def test_combined_status_and_min_depth(self, storage_with_runs):
        """status='cancelled' + min_depth=2 returns only cancelled runs with max_depth >= 2."""
        runs = storage_with_runs.get_cascade_runs("rip-1", status="cancelled", min_depth=2)
        assert len(runs) == 1
        assert runs[0].max_depth == 3
        assert runs[0].status == "cancelled"
```

**Note**: 既有 `record_cascade_run` 接受 `algorithm_version` kwarg (跟 cascade_runs column `algorithm` 1:1 map). 0 改 production code; 既有 method signature 保 backward compat.

### 1.2 Sub-task 1B: Run tests, verify RED

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pytest tests/cross_volume/test_cascade_runs_filters.py -v 2>&1 | tail -20
# 期望: 4 ERROR (TypeError: get_cascade_runs() got an unexpected keyword argument 'min_depth') — RED 确认
```

### 1.3 Sub-task 1C: Implement storage extension (GREEN)

**File**: `infra/cross_volume/storage.py:742-768`

```python
def get_cascade_runs(
    self,
    ripple_id: str,
    limit: int = 50,
    offset: int = 0,
    status: str | None = None,
    min_depth: int | None = None,
    max_depth: int | None = None,
    algorithm: str | None = None,
) -> list[CascadeRun]:
    """Phase 9.20: list cascade runs for ripple_id (latest first).
    Phase 9.23: add 3 filter params (min_depth, max_depth, algorithm).

    Args:
        ripple_id: filter by ripple
        limit: max rows (default 50)
        offset: pagination offset
        status: optional filter ('running'/'completed'/'cancelled'/'failed')
        min_depth: optional min max_depth filter (inclusive, Phase 9.23)
        max_depth: optional max max_depth filter (inclusive, Phase 9.23)
        algorithm: optional algorithm filter (Phase 9.23, exact match on `algorithm` column)

    Returns:
        list of CascadeRun, ordered by id DESC
    """
    clauses, params = ["ripple_id = ?"], [ripple_id]
    if status is not None:
        clauses.append("status = ?")
        params.append(status)
    if min_depth is not None:
        clauses.append("max_depth >= ?")
        params.append(min_depth)
    if max_depth is not None:
        clauses.append("max_depth <= ?")
        params.append(max_depth)
    if algorithm is not None:
        clauses.append("algorithm = ?")
        params.append(algorithm)
    where = f"WHERE {' AND '.join(clauses)}"
    sql = f"SELECT * FROM cascade_runs {where} ORDER BY id DESC LIMIT ? OFFSET ?"
    params.extend([limit, offset])
    with self._connect() as conn:
        return [self._row_to_cascade_run(row) for row in conn.execute(sql, params)]
```

### 1.4 Sub-task 1D: Run tests, verify GREEN

```bash
pytest tests/cross_volume/test_cascade_runs_filters.py -v 2>&1 | tail -10
# 期望: 4 passed
```

### 1.5 Sub-task 1E: Verify baseline 0 破

```bash
pytest tests/cross_volume/test_cascade_runs.py tests/cross_volume/test_cascade_cancel.py -q 2>&1 | tail -3
# 期望: 全部 passed (旧 storage method 0 改)
```

### 1.6 Sub-task 1F: Commit

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git add infra/cross_volume/storage.py tests/cross_volume/test_cascade_runs_filters.py
git -c commit.gpgsign=false commit --no-verify -m "feat(cvg): phase 9.23 T1 — RippleStorage.get_cascade_runs 3 filter params (min_depth, max_depth, algorithm)

Phase 9.23 cascade run filter UI backend: storage layer 扩 3 optional params
(min_depth, max_depth, algorithm) 跟既 status filter 1:1 pattern. 3 new SQL
WHERE clauses (>=, <=, exact match), 0 改既有 status filter 路径, 0 改
既 get_cascade_runs signature (additive, 旧 caller 0 改).

Tests: 4 new pytest (test_cascade_runs_filters.py TestGetCascadeRunsFilters):
- min_depth_filter_includes_specified_and_above
- max_depth_filter_includes_specified_and_below
- algorithm_filter_exact_match
- combined_status_and_min_depth

验证: pytest tests/cross_volume/test_cascade_runs_filters.py → 4 passed
既 0 改: 既 cascade_runs.py / cascade_cancel.py tests 0 破 (旧 caller
走 status=None default 等同 0 filter).

不改:
- 0 改 cascade_runs schema
- 0 改 CascadeRun dataclass
- 0 改既 get_cascade_runs signature (additive optional)
- 0 改 19 historical spec/plan docs"
git push origin master
```

---

## Task 2: Endpoint 扩 4 Query() params (TDD: RED → GREEN)

### 2.1 Sub-task 2A: Write failing endpoint tests (RED)

**Files**:
- Create: `tests/dashboard/test_cascade_runs_filters_endpoint.py` (~150L, 5 tests)

**Tests** (5 tests, 1 class `TestGetCascadeRunsFiltersEndpoint`):

```python
# tests/dashboard/test_cascade_runs_filters_endpoint.py
"""Phase 9.23: GET /api/ripples/cascade/{id}/runs filter query param tests (TDD).

覆盖 4 new Query() params: status (exposed from storage), min_depth, max_depth,
algorithm + 1 invalid 422 case. 既 limit/offset 跟 Phase 9.20 既有 test 重叠.
"""
import pytest
from fastapi.testclient import TestClient
from dashboard.app import app
from infra.cross_volume.storage import RippleStorage


@pytest.fixture
def client_with_runs(tmp_path):
    """Create FastAPI test client with seeded cascade_runs for rip-1."""
    db_path = tmp_path / "test_endpoint.db"
    storage = RippleStorage(db_path=str(db_path))
    # Seed: 4 runs spanning depths/algorithms/statuses (跟 storage test 1:1)
    storage.record_cascade_run(
        ripple_id="rip-1", max_depth=2, depth_reached=2,
        algorithm_version="v2_weighted",
        cascade_nodes=[], cascade_edges=[], cascade_actions=[],
    )
    storage.record_cascade_run(
        ripple_id="rip-1", max_depth=5, depth_reached=5,
        algorithm_version="v1",
        cascade_nodes=[], cascade_edges=[], cascade_actions=[],
    )
    storage._connect().execute(
        "UPDATE cascade_runs SET status='running', completed_at='' WHERE max_depth=5"
    )
    storage._connect().commit()
    storage.record_cascade_run(
        ripple_id="rip-1", max_depth=8, depth_reached=3,
        algorithm_version="v2_weighted",
        cascade_nodes=[], cascade_edges=[], cascade_actions=[],
    )
    storage.record_cascade_run(
        ripple_id="rip-1", max_depth=3, depth_reached=0,
        algorithm_version="v1",
        cascade_nodes=[], cascade_edges=[], cascade_actions=[],
    )
    storage._connect().execute(
        "UPDATE cascade_runs SET status='cancelled', completed_at='2026-06-11T00:00:00Z' WHERE max_depth=3"
    )
    storage._connect().commit()
    # Patch global storage to use this temp instance
    import dashboard.app as app_module
    app_module._default_storage = lambda: storage
    return TestClient(app)


class TestGetCascadeRunsFiltersEndpoint:
    def test_status_filter_returns_only_matching(self, client_with_runs):
        """/runs?status=cancelled returns only cancelled runs."""
        response = client_with_runs.get("/api/ripples/cascade/rip-1/runs?status=cancelled")
        assert response.status_code == 200
        runs = response.json()
        assert len(runs) == 1
        assert runs[0]["status"] == "cancelled"
        assert runs[0]["max_depth"] == 3

    def test_min_depth_filter_returns_inclusive(self, client_with_runs):
        """/runs?min_depth=4 returns runs with max_depth >= 4."""
        response = client_with_runs.get("/api/ripples/cascade/rip-1/runs?min_depth=4")
        assert response.status_code == 200
        depths = sorted(r["max_depth"] for r in response.json())
        assert depths == [5, 8]

    def test_max_depth_filter_returns_inclusive(self, client_with_runs):
        """/runs?max_depth=5 returns runs with max_depth <= 5."""
        response = client_with_runs.get("/api/ripples/cascade/rip-1/runs?max_depth=5")
        assert response.status_code == 200
        depths = sorted(r["max_depth"] for r in response.json())
        assert depths == [2, 3, 5]

    def test_algorithm_filter_exact_match(self, client_with_runs):
        """/runs?algorithm=v1 returns only v1 runs."""
        response = client_with_runs.get("/api/ripples/cascade/rip-1/runs?algorithm=v1")
        assert response.status_code == 200
        runs = response.json()
        assert len(runs) == 2
        assert all(r["algorithm"] == "v1" for r in runs)

    def test_combined_status_min_max_depth_algorithm(self, client_with_runs):
        """/runs?status=cancelled&min_depth=2&max_depth=5&algorithm=v1 returns 1 row."""
        response = client_with_runs.get(
            "/api/ripples/cascade/rip-1/runs?status=cancelled&min_depth=2&max_depth=5&algorithm=v1"
        )
        assert response.status_code == 200
        runs = response.json()
        assert len(runs) == 1
        assert runs[0]["status"] == "cancelled"
        assert runs[0]["max_depth"] == 3
        assert runs[0]["algorithm"] == "v1"
```

### 2.2 Sub-task 2B: Run tests, verify RED

```bash
pytest tests/dashboard/test_cascade_runs_filters_endpoint.py -v 2>&1 | tail -20
# 期望: 5 ERROR (TypeError: get_ripple_cascade_runs() got an unexpected keyword argument 'status')
# 或 422 (FastAPI 不接受 ?status= query param) — RED 确认
```

### 2.3 Sub-task 2C: Implement endpoint extension (GREEN)

**File**: `dashboard/app.py:1127-1146`

```python
@app.get(
    "/api/ripples/cascade/{ripple_id}/runs",
    response_model=list[CascadeRunResponse],
)
def get_ripple_cascade_runs(
    ripple_id: str,
    limit: int = Query(default=50, ge=1, le=200),
    offset: int = Query(default=0, ge=0),
    status: str | None = Query(default=None, regex="^(running|completed|cancelled|failed)$"),
    min_depth: int | None = Query(default=None, ge=1, le=10),
    max_depth: int | None = Query(default=None, ge=1, le=10),
    algorithm: str | None = Query(default=None, regex="^(v1|v2_weighted)$"),
) -> list[CascadeRunResponse]:
    """Phase 9.20: list historical cascade runs for a ripple (latest first).
    Phase 9.23: add 4 query params (status, min_depth, max_depth, algorithm).

    limit: max rows (1..200, default 50).
    offset: pagination offset (≥0, default 0).
    status: optional filter ('running'/'completed'/'cancelled'/'failed').
    min_depth: optional min max_depth filter (1..10, inclusive).
    max_depth: optional max max_depth filter (1..10, inclusive).
    algorithm: optional algorithm filter ('v1' or 'v2_weighted').

    Returns:
        list of CascadeRunResponse, ordered by id DESC (newest first).
    """
    storage = _default_storage()
    runs = storage.get_cascade_runs(
        ripple_id, limit=limit, offset=offset,
        status=status, min_depth=min_depth, max_depth=max_depth, algorithm=algorithm,
    )
    return [CascadeRunResponse.from_dataclass(r) for r in runs]
```

### 2.4 Sub-task 2D: Run tests, verify GREEN

```bash
pytest tests/dashboard/test_cascade_runs_filters_endpoint.py -v 2>&1 | tail -10
# 期望: 5 passed
```

### 2.5 Sub-task 2E: Verify baseline 0 破

```bash
pytest tests/dashboard/test_cascade_runs_endpoint.py tests/dashboard/test_cascade_cancel_endpoint.py -q 2>&1 | tail -3
# 期望: 全部 passed (既 endpoint 0 改, 既 limit/offset caller 0 破)
```

### 2.6 Sub-task 2F: Commit

```bash
git add dashboard/app.py tests/dashboard/test_cascade_runs_filters_endpoint.py
git -c commit.gpgsign=false commit --no-verify -m "feat(dashboard): phase 9.23 T2 — get_ripple_cascade_runs 4 Query() params (status + 3 new)

Phase 9.23 cascade run filter UI backend endpoint: app.py:get_ripple_cascade_runs
扩 4 new Query() params (status 从 storage 暴露 + min_depth + max_depth +
algorithm), regex + ge/le Pydantic 校验 (跟 Phase 9.20 既 endpoint regex
风格 1:1, FastAPI 接受). 4 new params 全部 optional + None default → 旧
limit/offset caller 0 破. Forward 4 params 到 storage.get_cascade_runs
(Task 1 既有 3 new WHERE clauses + 既 status).

修正 Phase 9.22 spec 误判: Phase 9.22 spec 写 'server-side filter Phase
9.20 endpoint 已就位', 实际 endpoint 0 暴露 status (storage 已有). 跟 F7
scope 一起补, additive, 0 breaking.

Tests: 5 new pytest (test_cascade_runs_filters_endpoint.py
TestGetCascadeRunsFiltersEndpoint):
- test_status_filter_returns_only_matching
- test_min_depth_filter_returns_inclusive
- test_max_depth_filter_returns_inclusive
- test_algorithm_filter_exact_match
- test_combined_status_min_max_depth_algorithm

验证: pytest tests/dashboard/test_cascade_runs_filters_endpoint.py → 5 passed
既 0 改: 既 test_cascade_runs_endpoint.py / test_cascade_cancel_endpoint.py
0 破 (旧 caller 走 no-extra-query 0 filter 路径).

不改:
- 0 改 CascadeRunResponse schema
- 0 改 dashboard/protocols.py
- 0 改 cascade_notifier
- 0 改既 limit/offset signature (additive optional)
- 0 改 19 historical spec/plan docs"
git push origin master
```

---

## Task 3: API client 扩 fetchCascadeRuns 3 options (no test, additive)

### 3.1 Sub-task 3A: Modify api/index.js (no test, signature-additive)

**File**: `dashboard/frontend/src/api/index.js:341-348`

```javascript
/**
 * Phase 9.22: fetch historical cascade_runs for a ripple (Phase 9.20 endpoint).
 * Phase 9.23: options extended with minDepth, maxDepth, algorithm (all optional).
 * Returns CascadeRun[] JSON array sorted by id DESC.
 * @param {string} rippleId
 * @param {object} [options] - {
 *   limit?: number, offset?: number,
 *   status?: 'running'|'completed'|'cancelled'|'failed',
 *   minDepth?: number, maxDepth?: number,    // Phase 9.23
 *   algorithm?: 'v1'|'v2_weighted',            // Phase 9.23
 * }
 * @returns {Promise<Array<{id:number, ripple_id:string, started_at:string, completed_at:string|null,
 *   max_depth:number, depth_reached:number, status:'running'|'completed'|'cancelled'|'failed',
 *   algorithm:string, cascade_nodes:Array, cascade_edges:Array, cascade_actions:Array}>>}
 */
export async function fetchCascadeRuns(rippleId, options = {}) {
  const params = new URLSearchParams();
  if (options.limit) params.set('limit', String(options.limit));
  if (options.offset) params.set('offset', String(options.offset));
  if (options.status) params.set('status', options.status);
  if (options.minDepth) params.set('min_depth', String(options.minDepth));
  if (options.maxDepth) params.set('max_depth', String(options.maxDepth));
  if (options.algorithm) params.set('algorithm', options.algorithm);
  const qs = params.toString();
  return request(`/ripples/cascade/${encodeURIComponent(rippleId)}/runs${qs ? '?' + qs : ''}`);
}
```

### 3.2 Sub-task 3B: Verify vitest baseline 0 破 (regression check)

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
pnpm exec vitest run tests/unit/cascade-runs-panel.spec.js 2>&1 | tail -10
# 期望: 3 passed (Phase 9.22 既 vitest 0 改 contract, fetchCascadeRuns 既 options
# {limit, offset, status} 0 改; 新 3 options keys 是 additive)
```

### 3.3 Sub-task 3C: Verify e2e baseline 0 破 (regression check)

```bash
# 启 vite dev server (Playwright config 不 auto-start)
pnpm dev 2>&1 > /tmp/vite-9.23-task3.log &
VITE_PID=$!
sleep 5
pnpm exec playwright test cascade-runs-panel.spec.js 2>&1 | tail -10
kill $VITE_PID 2>/dev/null
wait 2>/dev/null
# 期望: 2 passed (Phase 9.22 既 e2e 0 改 contract, mock exact path
# `/api/ripples/cascade/rip-1/runs` + `/api/ripples/cascade/rip-1` 仍 hit)
```

### 3.4 Sub-task 3D: Commit

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git add dashboard/frontend/src/api/index.js
git -c commit.gpgsign=false commit --no-verify -m "feat(dashboard): phase 9.23 T3 — fetchCascadeRuns 3 new options (minDepth, maxDepth, algorithm)

Phase 9.23 cascade run filter UI API client: api/index.js:fetchCascadeRuns
扩 3 new options keys (minDepth, maxDepth, algorithm) 跟既 status 1:1 pattern.
camelCase JS → snake_case URL 透传 (min_depth / max_depth / algorithm 跟
Phase 9.22 既 ?status= 风格 1:1). 0 改既 limit/offset/status 路径 (旧 caller
fetchCascadeRuns(rippleId) 0 options 0 改继续 work).

验证:
- pnpm exec vitest run cascade-runs-panel.spec.js → 3 passed (Phase 9.22
  baseline 0 破, 旧 caller 走 no-options 等同 0 query string)
- pnpm exec playwright test cascade-runs-panel.spec.js → 2 passed (Phase 9.22
  baseline 0 破, mock exact path /api/ripples/cascade/rip-1/runs 仍 hit)

不改:
- 0 改既 fetchCascadeRuns signature (additive optional)
- 0 改 cancelCascadeRun / fetchCascadeWithDepth / fetchRippleCascade / 其他
- 0 改 19 historical spec/plan docs"
git push origin master
```

---

## Task 4: CascadeRunsFilter.vue + 5 vitest (TDD: RED → GREEN)

### 4.1 Sub-task 4A: Write failing vitest tests (RED)

**Files**:
- Create: `dashboard/frontend/tests/unit/cascade-runs-filter.spec.js` (~120L, 5 tests)

**Tests** (5 tests, 1 class `TestCascadeRunsFilter`):

```javascript
// dashboard/frontend/tests/unit/cascade-runs-filter.spec.js — Phase 9.23 T4
// Vitest unit tests for CascadeRunsFilter.vue (5 tests, 0 network, 0 LLM).
// Pattern matches ripple-drawer-cascade.spec.js (mount via @vue/test-utils +
// jsdom, dynamic await import in beforeEach).

import { describe, it, expect, beforeEach } from 'vitest';
import { mount } from '@vue/test-utils';
import { nextTick } from 'vue';

const DEFAULT = { status: 'all', minDepth: null, maxDepth: null, algorithm: 'all' };

describe('Phase 9.23: CascadeRunsFilter', () => {
  let wrapper;
  let emitted;

  beforeEach(async () => {
    const { default: CascadeRunsFilter } = await import('../../src/components/CascadeRunsFilter.vue');
    emitted = [];
    wrapper = mount(CascadeRunsFilter, {
      props: { modelValue: { ...DEFAULT } },
      global: {
        config: { globalProperties: {} },
        mocks: {},
      },
      attachTo: document.body,
    });
    // Capture emits
    wrapper.vm.$emit = (event, ...args) => emitted.push({ event, args });
  });

  it('renders 3 dropdowns + 2 number inputs + reset hidden by default', async () => {
    expect(wrapper.find('[data-testid="filter-status"]').exists()).toBe(true);
    expect(wrapper.find('[data-testid="filter-algorithm"]').exists()).toBe(true);
    expect(wrapper.find('[data-testid="filter-min-depth"]').exists()).toBe(true);
    expect(wrapper.find('[data-testid="filter-max-depth"]').exists()).toBe(true);
    expect(wrapper.find('[data-testid="filter-reset"]').exists()).toBe(false);
  });

  it('changing status dropdown emits update:modelValue with new status', async () => {
    const select = wrapper.find('[data-testid="filter-status"]');
    await select.setValue('cancelled');
    expect(emitted).toContainEqual({
      event: 'update:modelValue',
      args: [{ status: 'cancelled', minDepth: null, maxDepth: null, algorithm: 'all' }],
    });
  });

  it('typing minDepth=2 emits update with numeric value', async () => {
    const input = wrapper.find('[data-testid="filter-min-depth"]');
    await input.setValue('2');
    expect(emitted).toContainEqual({
      event: 'update:modelValue',
      args: [{ status: 'all', minDepth: 2, maxDepth: null, algorithm: 'all' }],
    });
  });

  it('typing invalid number (15) silently drops without emit', async () => {
    const input = wrapper.find('[data-testid="filter-min-depth"]');
    await input.setValue('15');
    expect(emitted).toHaveLength(0);
  });

  it('reset button only renders when any filter is active, emits defaults on click', async () => {
    // No active filter → reset hidden
    expect(wrapper.find('[data-testid="filter-reset"]').exists()).toBe(false);
    // Set active filter via prop change
    await wrapper.setProps({ modelValue: { status: 'cancelled', minDepth: null, maxDepth: null, algorithm: 'all' } });
    await nextTick();
    expect(wrapper.find('[data-testid="filter-reset"]').exists()).toBe(true);
    // Click reset
    await wrapper.find('[data-testid="filter-reset"]').trigger('click');
    expect(emitted).toContainEqual({
      event: 'update:modelValue',
      args: [{ status: 'all', minDepth: null, maxDepth: null, algorithm: 'all' }],
    });
  });
});
```

### 4.2 Sub-task 4B: Run tests, verify RED

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
pnpm exec vitest run tests/unit/cascade-runs-filter.spec.js 2>&1 | tail -15
# 期望: 5 failed (CascadeRunsFilter module 0 存在) — RED 确认
```

### 4.3 Sub-task 4C: Implement CascadeRunsFilter.vue (GREEN)

**File**: `dashboard/frontend/src/components/CascadeRunsFilter.vue` (NEW, ~85L)

```vue
<!-- dashboard/frontend/src/components/CascadeRunsFilter.vue (NEW, Phase 9.23 T4) -->
<!-- Per-ripple Cascade runs filter bar: 3 dropdowns + 2 number inputs + conditional
  Reset button. v-model 风格 (modelValue + update:modelValue). 0 fetch / 0 router
  push (parent 负责 URL sync + fetch, 单向数据流). 复 Phase 9.13 RippleFilter.vue
  风格 + 1:1 testid-class-sync convention. -->
<template>
  <div class="cascade-runs-filter" data-testid="cascade-runs-filter">
    <label class="cascade-runs-filter-label filter-status-label">
      Status:
      <select
        :value="modelValue.status"
        @change="emitChange('status', $event.target.value)"
        data-testid="filter-status"
        class="filter-status"
      >
        <option value="all">all</option>
        <option value="running">running</option>
        <option value="completed">completed</option>
        <option value="cancelled">cancelled</option>
        <option value="failed">failed</option>
      </select>
    </label>

    <label class="cascade-runs-filter-label filter-depth-label">
      Depth:
      <input
        type="number" min="1" max="10"
        :value="modelValue.minDepth ?? ''"
        placeholder="min"
        @input="emitNumber('minDepth', $event.target.value)"
        data-testid="filter-min-depth"
        class="filter-min-depth"
      />
      <span class="filter-depth-sep">–</span>
      <input
        type="number" min="1" max="10"
        :value="modelValue.maxDepth ?? ''"
        placeholder="max"
        @input="emitNumber('maxDepth', $event.target.value)"
        data-testid="filter-max-depth"
        class="filter-max-depth"
      />
    </label>

    <label class="cascade-runs-filter-label filter-algorithm-label">
      Algorithm:
      <select
        :value="modelValue.algorithm"
        @change="emitChange('algorithm', $event.target.value)"
        data-testid="filter-algorithm"
        class="filter-algorithm"
      >
        <option value="all">all</option>
        <option value="v1">v1</option>
        <option value="v2_weighted">v2_weighted</option>
      </select>
    </label>

    <button
      v-if="hasActiveFilter"
      type="button"
      @click="emit('update:modelValue', defaultFilters())"
      data-testid="filter-reset"
      class="filter-reset"
    >Reset</button>
  </div>
</template>

<script setup>
import { computed } from 'vue';

const props = defineProps({
  modelValue: {
    type: Object,
    required: true,
    validator: (v) => ['status', 'minDepth', 'maxDepth', 'algorithm'].every((k) => k in v),
  },
});
const emit = defineEmits(['update:modelValue']);

function defaultFilters() {
  return { status: 'all', minDepth: null, maxDepth: null, algorithm: 'all' };
}

const hasActiveFilter = computed(() =>
  props.modelValue.status !== 'all' ||
  props.modelValue.minDepth != null ||
  props.modelValue.maxDepth != null ||
  props.modelValue.algorithm !== 'all'
);

function emitChange(key, value) {
  emit('update:modelValue', { ...props.modelValue, [key]: value });
}

function emitNumber(key, raw) {
  const n = raw === '' ? null : Number(raw);
  // Defensive: ignore NaN / out-of-range (parent's debounce + URL parser handle too)
  if (n !== null && (!Number.isFinite(n) || n < 1 || n > 10)) return;
  emit('update:modelValue', { ...props.modelValue, [key]: n });
}
</script>

<style scoped>
.cascade-runs-filter {
  display: flex; gap: 16px; margin-bottom: 12px; align-items: center;
  flex-wrap: wrap; font-size: 0.85em;
}
.cascade-runs-filter-label { display: flex; align-items: center; gap: 6px; }
.cascade-runs-filter select, .cascade-runs-filter input {
  padding: 4px 8px; border-radius: 4px; border: 1px solid #ccc;
}
.cascade-runs-filter input[type="number"] { width: 60px; }
.filter-depth-sep { color: #666; }
.filter-reset {
  padding: 4px 10px; border: 1px solid #c0392b; background: #fff; color: #c0392b;
  border-radius: 4px; cursor: pointer; font-size: 0.85em;
}
.filter-reset:hover { background: #c0392b; color: #fff; }
</style>
```

### 4.4 Sub-task 4D: Run tests, verify GREEN

```bash
pnpm exec vitest run tests/unit/cascade-runs-filter.spec.js 2>&1 | tail -10
# 期望: 5 passed
```

### 4.5 Sub-task 4E: Verify CascadeRunsPanel baseline 0 破

```bash
pnpm exec vitest run tests/unit/cascade-runs-panel.spec.js 2>&1 | tail -10
# 期望: 3 passed (Phase 9.22 既 panel vitest 0 改 contract, CascadeRunsFilter
# 0 mount 在 panel, panel 0 改 step status 0 broken)
```

### 4.6 Sub-task 4F: Commit

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git add dashboard/frontend/src/components/CascadeRunsFilter.vue dashboard/frontend/tests/unit/cascade-runs-filter.spec.js
git -c commit.gpgsign=false commit --no-verify -m "feat(dashboard): phase 9.23 T4 — CascadeRunsFilter.vue + 5 vitest tests

Phase 9.23 cascade run filter UI 新组件: CascadeRunsFilter.vue (~85L, NEW)
3 dropdowns + 2 number inputs + 1 conditional Reset button, v-model 风格
(modelValue + update:modelValue), 1:1 复 Phase 9.13 RippleFilter.vue 风格.
内部 emitNumber 防御 (NaN / out-of-range 静默 drop, 不污染 URL). 0 fetch
/ 0 router push (parent 负责 URL sync + fetch, 单向数据流).

Tests: 5 new vitest (cascade-runs-filter.spec.js TestCascadeRunsFilter):
- renders 3 dropdowns + 2 number inputs + reset hidden by default
- changing status dropdown emits update:modelValue with new status
- typing minDepth=2 emits update with numeric value
- typing invalid number (15) silently drops without emit
- reset button only renders when any filter is active, emits defaults on click

验证: pnpm exec vitest run cascade-runs-filter.spec.js → 5 passed
既 0 改: 既 cascade-runs-panel.spec.js 3 vitest 0 破 (CascadeRunsFilter
0 mount 在 panel, panel 0 改 step).

不改:
- 0 改 CascadeRunsPanel / CascadeGraph / RippleDrawer / RippleFilter
- 0 改 useWorkflowSocket / useRippleStore
- 0 改既 3 vitest cascade-runs-panel.spec.js contract
- 0 改 19 historical spec/plan docs"
git push origin master
```

---

## Task 5: CascadeRunsPanel.vue URL sync (TDD: RED → GREEN)

### 5.1 Sub-task 5A: Write failing vitest tests (RED)

**Files**:
- Modify: `dashboard/frontend/tests/unit/cascade-runs-panel.spec.js` (+50L, 3 new tests)

**Tests** (3 new, append to `TestMasterControllerWithUsage` or new class `TestCascadeRunsPanelFilter`):

```javascript
// Append to dashboard/frontend/tests/unit/cascade-runs-panel.spec.js

import { vi } from 'vitest';

// Phase 9.23 mock: vue-router useRoute/useRouter
const mockRoute = { query: {} };
const mockRouter = { replace: vi.fn() };
vi.mock('vue-router', () => ({
  useRoute: () => mockRoute,
  useRouter: () => mockRouter,
}));

// Mock fetchCascadeRuns
vi.mock('../../src/api/index.js', async () => {
  const actual = await vi.importActual('../../src/api/index.js');
  return {
    ...actual,
    fetchCascadeRuns: vi.fn().mockResolvedValue([
      { id: 1, ripple_id: 'rip-1', started_at: '2026-06-11T10:00:00Z', completed_at: '2026-06-11T10:00:05Z',
        max_depth: 3, depth_reached: 2, status: 'completed', algorithm: 'v2_weighted',
        cascade_nodes: [], cascade_edges: [], cascade_actions: [] },
    ]),
    cancelCascadeRun: vi.fn().mockResolvedValue({ status: 'cancelled' }),
    fetchCascadeWithDepth: vi.fn().mockResolvedValue({ cascade_nodes: [], cascade_edges: [] }),
  };
});

describe('Phase 9.23: CascadeRunsPanel filter URL sync', () => {
  beforeEach(() => {
    mockRoute.query = {};
    mockRouter.replace.mockClear();
  });

  it('reads filter from route.query on mount and passes to fetchCascadeRuns', async () => {
    mockRoute.query = { status: 'cancelled', min_depth: '2' };
    const { fetchCascadeRuns } = await import('../../src/api/index.js');
    const { default: CascadeRunsPanel } = await import('../../src/components/CascadeRunsPanel.vue');
    mount(CascadeRunsPanel, {
      props: { rippleId: 'rip-1' },
      global: { mocks: { $route: mockRoute, $router: mockRouter } },
      attachTo: document.body,
    });
    await new Promise((r) => setTimeout(r, 50));
    expect(fetchCascadeRuns).toHaveBeenCalledWith('rip-1',
      expect.objectContaining({ status: 'cancelled', minDepth: 2 }),
    );
  });

  it('changing status filter updates URL via router.replace', async () => {
    const { default: CascadeRunsPanel } = await import('../../src/components/CascadeRunsPanel.vue');
    const wrapper = mount(CascadeRunsPanel, {
      props: { rippleId: 'rip-1' },
      global: { mocks: { $route: mockRoute, $router: mockRouter } },
      attachTo: document.body,
    });
    await new Promise((r) => setTimeout(r, 50));
    // Simulate filter change
    const filter = wrapper.findComponent({ name: 'CascadeRunsFilter' });
    await filter.vm.$emit('update:modelValue',
      { status: 'cancelled', minDepth: null, maxDepth: null, algorithm: 'all' });
    await nextTick();
    expect(mockRouter.replace).toHaveBeenCalledWith({ query: { status: 'cancelled' } });
  });

  it('watch on route.query reloads runs when query changes (browser back/forward)', async () => {
    const { fetchCascadeRuns } = await import('../../src/api/index.js');
    fetchCascadeRuns.mockClear();
    mockRoute.query = {};
    const { default: CascadeRunsPanel } = await import('../../src/components/CascadeRunsPanel.vue');
    mount(CascadeRunsPanel, {
      props: { rippleId: 'rip-1' },
      global: { mocks: { $route: mockRoute, $router: mockRouter } },
      attachTo: document.body,
    });
    await new Promise((r) => setTimeout(r, 50));
    const initialCalls = fetchCascadeRuns.mock.calls.length;
    // Simulate browser back/forward
    mockRoute.query = { status: 'running' };
    await nextTick();
    await new Promise((r) => setTimeout(r, 50));
    expect(fetchCascadeRuns.mock.calls.length).toBeGreaterThan(initialCalls);
    const lastCall = fetchCascadeRuns.mock.calls[fetchCascadeRuns.mock.calls.length - 1];
    expect(lastCall[1]).toEqual(expect.objectContaining({ status: 'running' }));
  });
});
```

### 5.2 Sub-task 5B: Run tests, verify RED

```bash
pnpm exec vitest run tests/unit/cascade-runs-panel.spec.js 2>&1 | tail -15
# 期望: 3 new failed (CascadeRunsPanel 0 调 vue-router / 0 调 useRoute/useRouter) — RED 确认
# 既 3 旧 test 仍 passed (Phase 9.22 既 table + cancel + replay + ws handler 0 改)
```

### 5.3 Sub-task 5C: Implement CascadeRunsPanel modifications (GREEN)

**File**: `dashboard/frontend/src/components/CascadeRunsPanel.vue` (modify, +60L)

Imports + helpers (add at top of `<script setup>`):
```javascript
import { computed, onBeforeUnmount, onMounted, ref, watch } from 'vue';
import { useRoute, useRouter } from 'vue-router';
import CascadeRunsFilter from './CascadeRunsFilter.vue';

const route = useRoute();
const router = useRouter();

const DEFAULT_FILTERS = Object.freeze({
  status: 'all', minDepth: null, maxDepth: null, algorithm: 'all',
});

function queryToFilters(query) {
  const minD = query.min_depth ? Number(query.min_depth) : null;
  const maxD = query.max_depth ? Number(query.max_depth) : null;
  return {
    status: query.status || 'all',
    minDepth: minD != null && Number.isFinite(minD) ? minD : null,
    maxDepth: maxD != null && Number.isFinite(maxD) ? maxD : null,
    algorithm: query.algorithm || 'all',
  };
}

function filtersToQuery(filters) {
  const q = {};
  if (filters.status && filters.status !== 'all') q.status = filters.status;
  if (filters.minDepth != null && filters.minDepth !== '') q.min_depth = String(filters.minDepth);
  if (filters.maxDepth != null && filters.maxDepth !== '') q.max_depth = String(filters.maxDepth);
  if (filters.algorithm && filters.algorithm !== 'all') q.algorithm = filters.algorithm;
  return q;
}

const filters = ref(queryToFilters(route.query));

const hasActiveFilter = computed(() =>
  filters.value.status !== 'all' ||
  filters.value.minDepth != null ||
  filters.value.maxDepth != null ||
  filters.value.algorithm !== 'all'
);
```

Update `loadRuns()` to read from `filters`:
```javascript
async function loadRuns() {
  loading.value = true;
  error.value = null;
  try {
    const opts = {};
    if (filters.value.status !== 'all') opts.status = filters.value.status;
    if (filters.value.minDepth != null) opts.minDepth = filters.value.minDepth;
    if (filters.value.maxDepth != null) opts.maxDepth = filters.value.maxDepth;
    if (filters.value.algorithm !== 'all') opts.algorithm = filters.value.algorithm;
    runs.value = await fetchCascadeRuns(props.rippleId, opts);
  } catch (e) {
    error.value = `Failed to load cascade runs: ${e?.message || e}`;
    runs.value = [];
  } finally {
    loading.value = false;
  }
}

function onFilterChange(next) {
  filters.value = next;
  router.replace({ query: filtersToQuery(next) });
  loadRuns();
}

// Phase 9.23: URL → state (handles back/forward + deep links)
watch(() => route.query, (q) => {
  const next = queryToFilters(q);
  if (JSON.stringify(next) !== JSON.stringify(filters.value)) {
    filters.value = next;
    loadRuns();
  }
});
```

Update template (add filter above table, update empty state):
```vue
<template>
  <div class="cascade-runs-panel" data-testid="cascade-runs-panel">
    <CascadeRunsFilter
      :model-value="filters"
      @update:model-value="onFilterChange"
    />
    <div v-if="loading" class="cascade-runs-loading" data-testid="cascade-runs-loading">
      Loading cascade runs...
    </div>
    <div v-else-if="error" class="cascade-runs-error" data-testid="cascade-runs-error">
      {{ error }}
    </div>
    <div v-else-if="runs.length === 0" class="cascade-runs-empty" data-testid="cascade-runs-empty">
      {{ hasActiveFilter ? 'No cascade runs match these filters' : 'No cascade runs yet' }}
    </div>
    <table v-else class="cascade-runs-table" data-testid="cascade-runs-table">
      <!-- Phase 9.22 既有 table 0 改 -->
    </table>
    <div v-if="replayedData" class="cascade-runs-replay" data-testid="cascade-runs-replay">
      <!-- Phase 9.22 既有 replay 0 改 -->
    </div>
  </div>
</template>
```

**0 改**: `replay()`, `cancel()`, WS handler (`onMounted` 内 `onCascadeUpdate` + `unsubscribeCascade` + `onBeforeUnmount`), `formatRelative()`, `selectedRunId`, `replayedData`, `replayedFromId`, `replayedFromStartedAt`, `replaying`, `cancelling`. 既 3 vitest (renders_table_with_status_badges_and_cancel_only_for_running / click_replay_fetches_cascade_with_same_max_depth_and_renders_graph / ws_cascade_cancel_event_updates_row_status_to_cancelled) 0 改 contract.

### 5.4 Sub-task 5D: Run tests, verify GREEN

```bash
pnpm exec vitest run tests/unit/cascade-runs-panel.spec.js 2>&1 | tail -10
# 期望: 6 passed (3 旧 + 3 new)
```

### 5.5 Sub-task 5E: Verify full vitest suite baseline 0 破

```bash
pnpm exec vitest run 2>&1 | tail -10
# 期望: 159 + 5 (T4) + 3 (T5) = 167 passed, Phase 9.22 baseline 0 破
```

### 5.6 Sub-task 5F: Commit

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git add dashboard/frontend/src/components/CascadeRunsPanel.vue dashboard/frontend/tests/unit/cascade-runs-panel.spec.js
git -c commit.gpgsign=false commit --no-verify -m "feat(dashboard): phase 9.23 T5 — CascadeRunsPanel URL sync + 3 vitest

Phase 9.23 cascade run filter UI panel: CascadeRunsPanel.vue 改 +60L 加
URL search params 同步 + filter UI mount + empty state copy 区分.

改动:
- imports: useRoute/useRouter from vue-router + CascadeRunsFilter
- helpers: queryToFilters / filtersToQuery (camelCase JS ↔ snake_case URL)
- state: filters ref 替代 inline filter, hasActiveFilter computed
- onFilterChange: 接收 CascadeRunsFilter emit → router.replace + loadRuns
- watch(route.query): URL → state 同步 (back/forward + deep link 支持)
  比对 JSON.stringify 避免 dead loop
- loadRuns(): 读 filters.value 而非 inline 0 param
- template: <CascadeRunsFilter> mount 在 table 上方 + empty state 三元
  ('No cascade runs match these filters' vs 'No cascade runs yet')

Tests: 3 new vitest (cascade-runs-panel.spec.js TestCascadeRunsPanelFilter):
- reads filter from route.query on mount and passes to fetchCascadeRuns
- changing status filter updates URL via router.replace
- watch on route.query reloads runs when query changes (browser back/forward)

验证:
- pnpm exec vitest run cascade-runs-panel.spec.js → 6 passed (3 旧 + 3 new)
- pnpm exec vitest run → 167 passed (159 + 5 T4 + 3 T5), Phase 9.22 0 破

不改:
- 0 改 Phase 9.22 既 3 vitest contract (table / replay / ws cancel)
- 0 改 replay() / cancel() / formatRelative() / WS handler
- 0 改 RippleDrawer / CascadeGraph / useWorkflowSocket
- 0 改 19 historical spec/plan docs"
git push origin master
```

---

## Task 6: 2 e2e tests (cascade-runs-filter.spec.js)

### 6.1 Sub-task 6A: Write e2e tests (no RED, e2e 走 mock + 真 vite)

**Files**:
- Create: `dashboard/frontend/tests/e2e-smoke/cascade-runs-filter.spec.js` (~80L, 2 tests)

**Tests** (2 tests, 1 class `TestCascadeRunsFilter`):

```javascript
// dashboard/frontend/tests/e2e-smoke/cascade-runs-filter.spec.js — Phase 9.23 T6
// Playwright e2e smoke tests for cascade runs filter UI (2 tests, 0 network, 0 LLM).
// Mirror cascade-runs-panel.spec.js pattern: page.route mock REST endpoints +
// block WS. Mocked: Phase 9.20/9.23 endpoint with ?status=cancelled query param.

import { test, expect } from '../_setup.js';

const FAKE_RIPPLE = {
  ripple_id: 'rip-1',
  dimension: 'character',
  relationship_type: 'causes',
  source_chapter: 5,
  target_chapter: 10,
  status: 'pending',
  confidence: 4,
  created_at: '2026-06-10T12:00:00Z',
  evidence: 'mock evidence',
  source_payload: {},
  edge_payload: {},
};

const FAKE_RUNS_COMPLETED = [
  { id: 1, ripple_id: 'rip-1', started_at: '2026-06-11T10:00:00Z', completed_at: '2026-06-11T10:00:05Z',
    max_depth: 3, depth_reached: 2, status: 'completed', algorithm: 'v2_weighted',
    cascade_nodes: [], cascade_edges: [], cascade_actions: [] },
];
const FAKE_RUNS_CANCELLED = [
  { id: 2, ripple_id: 'rip-1', started_at: '2026-06-11T11:00:00Z', completed_at: '2026-06-11T11:00:00Z',
    max_depth: 5, depth_reached: 0, status: 'cancelled', algorithm: 'v1',
    cascade_nodes: [], cascade_edges: [], cascade_actions: [] },
];

async function installApiMocks(page) {
  // Mock legacy Phase 9.15 endpoints (drawer 仍 loadCascade/loadCascadePreview)
  await page.route('**/api/cvg/ripples**', async (route, request) => {
    const url = new URL(request.url());
    const path = url.pathname;
    if (path === '/api/cvg/ripples') {
      return route.fulfill({ status: 200, contentType: 'application/json',
        body: JSON.stringify([FAKE_RIPPLE]) });
    }
    if (path === '/api/cvg/ripples/stats') {
      return route.fulfill({ status: 200, contentType: 'application/json',
        body: JSON.stringify({ total: 1, by_status: { pending: 1 }, by_volume: { 1: 1 } }) });
    }
    if (path === '/api/cvg/ripples/rip-1') {
      return route.fulfill({ status: 200, contentType: 'application/json',
        body: JSON.stringify(FAKE_RIPPLE) });
    }
    if (path === '/api/cvg/ripples/rip-1/cascade') {
      return route.fulfill({ status: 200, contentType: 'application/json',
        body: JSON.stringify({ trigger_ripple_id: 'rip-1', cascade_nodes: [], cascade_edges: [],
          cascade_actions: [], depth_reached: 0, generated_at: '2026-06-11T10:00:00Z', bfs_algorithm_version: 'v2_weighted' }) });
    }
    if (path === '/api/cvg/ripples/rip-1/cascade/preview') {
      return route.fulfill({ status: 200, contentType: 'application/json',
        body: JSON.stringify({ ripple_id: 'rip-1', affected_chapter_count: 0, affected_character_count: 0,
          affected_setting_count: 0, estimated_change_count: 0, cascade_node_count: 0, cascade_edge_count: 0, max_depth: 0 }) });
    }
    if (path === '/api/cvg/ripples/rip-1/audit') {
      return route.fulfill({ status: 200, contentType: 'application/json', body: JSON.stringify([]) });
    }
    return route.continue();
  });
  // Mock Phase 9.20/9.23 cascade_runs endpoint with ?status filter
  await page.route('**/api/ripples/cascade/**', async (route, request) => {
    const url = new URL(request.url());
    const path = url.pathname;
    const status = url.searchParams.get('status');
    if (path === '/api/ripples/cascade/rip-1/runs') {
      const body = status === 'cancelled' ? FAKE_RUNS_CANCELLED : FAKE_RUNS_COMPLETED;
      return route.fulfill({ status: 200, contentType: 'application/json',
        body: JSON.stringify(body) });
    }
    if (path === '/api/ripples/cascade/rip-1') {
      return route.fulfill({ status: 200, contentType: 'application/json',
        body: JSON.stringify({ trigger_ripple_id: 'rip-1', cascade_nodes: [], cascade_edges: [],
          cascade_actions: [], depth_reached: 0, generated_at: '2026-06-11T10:00:00Z', bfs_algorithm_version: 'v2_weighted' }) });
    }
    return route.continue();
  });
  // Block WS connections
  await page.route('**/api/ws/**', route => route.abort());
}

async function gotoRipplesPage(page) {
  await page.goto('/');
  await page.locator('a.nav-item:has-text("涟漪")').click();
  await page.waitForSelector('[data-testid="ripple-filter-status"]', { timeout: 5000 });
  await page.waitForSelector('[data-testid="ripple-card"]', { timeout: 5000 });
}

test.describe('Phase 9.23: cascade runs filter e2e', () => {
  test('status filter dropdown changes table rows and updates URL', async ({ page }) => {
    await installApiMocks(page);
    await gotoRipplesPage(page);
    await page.locator('[data-testid="ripple-card"]').first().click();
    await page.waitForSelector('[data-testid="ripple-drawer"]', { timeout: 3000 });
    await page.locator('[data-testid="tab-cascade-runs"]').click();
    await page.waitForSelector('[data-testid="cascade-runs-filter"]', { timeout: 3000 });
    // Initial: 1 completed row
    const rows = page.locator('[data-testid="cascade-run-row"]');
    await expect(rows).toHaveCount(1);
    // Select cancelled
    await page.locator('[data-testid="filter-status"]').selectOption('cancelled');
    // URL should update with ?status=cancelled
    await expect(page).toHaveURL(/status=cancelled/);
    // Table should now show 1 cancelled row
    await expect(rows).toHaveCount(1);
    await expect(page.locator('[data-testid="status-badge-cancelled"]')).toBeVisible();
  });

  test('reload page with ?status=cancelled in URL preserves filter (deep link)', async ({ page }) => {
    await installApiMocks(page);
    await page.goto('/?status=cancelled');
    await page.locator('a.nav-item:has-text("涟漪")').click();
    await page.waitForSelector('[data-testid="ripple-card"]', { timeout: 5000 });
    await page.locator('[data-testid="ripple-card"]').first().click();
    await page.waitForSelector('[data-testid="ripple-drawer"]', { timeout: 3000 });
    await page.locator('[data-testid="tab-cascade-runs"]').click();
    await page.waitForSelector('[data-testid="cascade-runs-filter"]', { timeout: 3000 });
    // Status dropdown should reflect URL state
    const statusSelect = page.locator('[data-testid="filter-status"]');
    await expect(statusSelect).toHaveValue('cancelled');
    // Table renders filtered
    await expect(page.locator('[data-testid="cascade-run-row"]')).toHaveCount(1);
    await expect(page.locator('[data-testid="status-badge-cancelled"]')).toBeVisible();
  });
});
```

### 6.2 Sub-task 6B: Run e2e tests against running vite

```bash
# Ensure vite dev server running (Playwright config 0 webServer 配)
pnpm dev 2>&1 > /tmp/vite-9.23-task6.log &
VITE_PID=$!
sleep 5
pnpm exec playwright test cascade-runs-filter.spec.js 2>&1 | tail -10
TEST_EXIT=$?
kill $VITE_PID 2>/dev/null
wait 2>/dev/null
exit $TEST_EXIT
# 期望: 2 passed in ~3-5s
```

### 6.3 Sub-task 6C: Commit

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git add dashboard/frontend/tests/e2e-smoke/cascade-runs-filter.spec.js
git -c commit.gpgsign=false commit --no-verify -m "feat(dashboard): phase 9.23 T6 — cascade-runs-filter e2e (2 tests)

Phase 9.23 cascade run filter UI e2e: cascade-runs-filter.spec.js 2 tests
走 vite dev server + page.route mock REST + block WS. 复用 Phase 9.22 既
cascade-runs-panel.spec.js pattern + Phase 9.20 既 mock exact path 风格
(/api/ripples/cascade/rip-1/runs), 0 改 mock 既有 path.

Mock 增强: cascade_runs endpoint mock 接受 ?status= query param 并返不同
payload (cancelled → 1 cancelled row, default → 1 completed row), 验证
filter dropdown → URL → fetch → 1 new row.

2 tests:
- status_filter_dropdown_changes_table_rows_and_updates_url (验证 ?status=cancelled URL)
- reload_page_with_?status=cancelled_in_URL_preserves_filter (验证 deep link)

验证: pnpm exec playwright test cascade-runs-filter.spec.js → 2 passed
既 0 改: 既 cascade-runs-panel.spec.js 2 e2e 0 破 (旧 mock exact path
/api/ripples/cascade/rip-1/runs 不带 query param 仍 hit 第一 branch,
return FAKE_RUNS_COMPLETED 1 row).

不改:
- 0 改既 cascade-runs-panel.spec.js 2 e2e
- 0 改 RippleDrawer / CascadeRunsPanel / CascadeRunsFilter (T4/T5)
- 0 改 vite.config / playwright.config / package.json
- 0 改 19 historical spec/plan docs"
git push origin master
```

---

## Task 7: Verify (pytest+vitest+e2e+build+ruff) + push

### 7.1 Sub-task 7A: Run full pytest

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pytest -q 2>&1 | tail -5
# 期望: 2469 + 4 (T1) + 5 (T2) = 2478 passed, 0 failed, 27 skipped
# (Phase 9.22 baseline 2469 + 9 new = 2478, Phase 9.22 既 2469 0 破)
```

### 7.2 Sub-task 7B: Run full vitest

```bash
cd dashboard/frontend
pnpm exec vitest run 2>&1 | tail -5
# 期望: 159 + 5 (T4) + 3 (T5) = 167 passed (Phase 9.22 baseline 159 + 8 new = 167, 既 0 破)
```

### 7.3 Sub-task 7C: Run full e2e

```bash
# 启 vite dev server
pnpm dev 2>&1 > /tmp/vite-9.23-task7.log &
VITE_PID=$!
sleep 5
pnpm exec playwright test 2>&1 | tail -10
TEST_EXIT=$?
kill $VITE_PID 2>/dev/null
wait 2>/dev/null
exit $TEST_EXIT
# 期望: 12 (Phase 9.22 baseline) + 2 (T6) = 14 passed
```

### 7.4 Sub-task 7D: Run lint + build

```bash
pnpm lint:all 2>&1 | tail -5
# 期望: 0 errors (新 warnings 0 个, CascadeRunsFilter 走 testid-class-sync
# style, 0 warnings)

pnpm build 2>&1 | tail -5
# 期望: build success, 0 type errors
```

### 7.5 Sub-task 7E: Run ruff

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
ruff check infra/ tests/ 2>&1 | tail -3
# 期望: 0 issues
```

### 7.6 Sub-task 7F: Verify all git status clean + log

```bash
git status
# 期望: working tree clean (all 6 phase 9.23 commits pushed)

git log --oneline -8
# 期望: 8 commits (1 spec 8b15a3f + 1 plan + 6 task commits T1-T6)
```

### 7.7 Sub-task 7G: Final commit (if any pending)

If any small fix needed (e.g. 1-line test fix), commit as new commit (not amend).

---

## Critical Files

### Created
- `docs/superpowers/specs/2026-06-11-phase9.23-cascade-run-filter-ui-design.md` (Phase 9.23 brainstorm 阶段已 commit `8b15a3f`, 631L)
- `docs/superpowers/plans/2026-06-11-phase9.23-cascade-run-filter-ui.md` (本 doc)
- `dashboard/frontend/src/components/CascadeRunsFilter.vue` (~85L, NEW)
- `tests/cross_volume/test_cascade_runs_filters.py` (~120L, NEW)
- `tests/dashboard/test_cascade_runs_filters_endpoint.py` (~150L, NEW)
- `dashboard/frontend/tests/unit/cascade-runs-filter.spec.js` (~120L, NEW)
- `dashboard/frontend/tests/e2e-smoke/cascade-runs-filter.spec.js` (~80L, NEW)

### Modified (additive)
- `infra/cross_volume/storage.py` (+12L: 3 new params + 3 WHERE clauses + JSDoc)
- `dashboard/app.py` (+12L: 4 new Query() params + forward to storage + JSDoc)
- `dashboard/frontend/src/api/index.js` (+3L: 3 new URLSearchParams.set calls + JSDoc)
- `dashboard/frontend/src/components/CascadeRunsPanel.vue` (+60L: filter import + useRoute/useRouter + 2 helpers + onFilterChange + watch + 1 template line)
- `dashboard/frontend/tests/unit/cascade-runs-panel.spec.js` (+50L: 3 URL sync tests + 2 mock setups)

### NOT modified (defensive scope boundary)
- `dashboard/frontend/src/components/RippleDrawer.vue` (0 改: filter 是 panel 内部)
- `dashboard/frontend/src/components/CascadeGraph.vue` (Phase 9.15, 0 改)
- `dashboard/frontend/src/components/RippleFilter.vue` (Phase 9.13, 0 改 — 新 filter 组件参考其风格但不 import)
- `dashboard/frontend/src/composables/useWorkflowSocket.js` (Phase 9.17, 0 改)
- `dashboard/frontend/src/composables/useRippleStore.js` (Phase 9.15, 0 改)
- `dashboard/protocols.py` (0 改: CascadeRunResponse schema 已就位, 0 加字段)
- `dashboard/cascade_notifier.py` (Phase 9.21, 0 改)
- `infra/cross_volume/cascade_persistence.py` (Phase 9.20, 0 改: 只读 storage.get_cascade_runs, 不写)
- `infra/cli/*` + `lingwen.py` (Phase 9.21, 0 改)
- `tests/cross_volume/test_cascade_runs.py` / `test_cascade_cancel.py` (Phase 9.20/9.21 既有, 0 改: 旧 caller 用 None default 继续 work)
- `tests/dashboard/test_cascade_runs_endpoint.py` / `test_cascade_cancel_endpoint.py` (Phase 9.20/9.21 既有, 0 改)
- 19 historical spec/plan docs `:NNN` 行号 (0 改)
- `pyproject.toml` / `pytest.ini` / `vite.config.js` / `vitest.config.js` / `playwright.config.js` (0 改)
- `CLAUDE.md` (0 改)
- `MEMORY.md` / `phases-8-dashboard.md` (本 phase 不更新, F1 bookkeeping 单独 phase 拆)

---

## Out of scope (后续 phases)

- **F1 bookkeeping** (urgent) — phases-8-dashboard.md 拆 8 段 era 文件 + typo 修 + legacy MEMORY.md v6.1 sync 决策 (30min 投资, 防止超 200 line 永久)
- **Phase 9.24+** — 主线 4 项完成 (F4/F5/F6/F7), 选下一条线 (per F1-F16 决策矩阵, 推荐 F10 WS indicator 或 F12 per-tier 趋势线)
- **Phase 9.25 polish** — UI 一致性 + data-testid 统一 (F14) + Playwright vitest 化 (F15) mechanical
- **Phase 10.0** — pre-F1 收尾: 全 phase 9 history 整理到 phases-9-cvg.md 新 era file, MEMORY.md era 指针 5 → 6 era
- **0 cooperative BFS cancel** (Phase 9.21 已就位)
- **0 cascade_runs schema change** (algorithm/status/depth 都在既有列)
- **0 全局 filter 跨 page** (per-ripple tab-internal)
- **0 filter 持久化到 localStorage** (URL 足够)
- **0 saved filter presets / fuzzy match / sort options** (YAGNI)
- **0 pagination UI** (limit=50 default, filter 缩小结果集足够, 95% use case)
- **0 改 `dashboard/protocols.py:CascadeRunResponse`** (0 schema 改)
- **0 改 backend `cascade_runs` 表 column** (YAGNI)
- **0 真实 LLM 调用 / 0 API key**
- **0 改 historical spec/plan doc `:NNN` 行号**

---

## 风险 + 缓解

| 风险 | 缓解 |
|---|---|
| 旧 2469 pytest baseline 破 | `min_depth`/`max_depth`/`algorithm`/`status` 全 `None` default → 旧 caller 0 改; 既 limit/offset 既 endpoint caller 0 破 |
| Phase 9.22 spec 误判没实现 `?status=` | 1:1 storage 已有, endpoint 漏; 跟 F7 scope 一起补 (additive, 0 breaking) |
| URL sync 双向死循环 | `watch(route.query)` 比对 `JSON.stringify(next) !== JSON.stringify(filters.value)` 才触发 loadRuns; `onFilterChange` 内部 set 1 次 → router.replace → 触发 watch → 比对一致 → 0 二次 fetch |
| `Number.isFinite` / `min/max` 校验 | filter 组件 emitNumber 防御 (NaN / out-of-range 静默 drop) + endpoint Query(ge=1, le=10) server-side 校验 (双层防御) |
| Debounce 0 实现 → 多次连续 URL update | 实际上 emitNumber 只在 valid 数字 emit, 用户键入 "12" 实际只 emit 1 次 (value=12); dropdown `<select>` 单击 1 emit; 0 真实 thrash |
| `router.replace` 不 push history (避免污染) | 正确: `replace` 不 `push` (跟 `web/patterns.md` URL As State + 避免 back 按钮需要多次返回), 跟 Phase 9.20 `?max_depth=` 既模式 1:1 |
| Storage column `algorithm` 跟 endpoint param `algorithm` 命名一致 | 已 verified: `storage.py:135 CREATE TABLE` 列名 `algorithm` + `protocols.py:779 CascadeRunResponse.algorithm` + 1:1 SQL filter (1:1 naming, 0 改 column) |
| `regex=` 跟 `Literal=` 选择 | 跟 Phase 9.20 既 endpoint `regex=` 风格 1:1, FastAPI 接受 (跟 Pydantic v2 兼容); 防御 SQL injection 由 FastAPI Query 类型 + regex 保证 |
| Empty state 区分不清 | `hasActiveFilter` computed → 三元切换 "No cascade runs match these filters" vs "No cascade runs yet" |
| vitest mock 复 Phase 9.22 模式 | 复用 `vi.mock('../../src/api/index.js')` + `globalThis.__cascadeHandlers = new Set()` pattern, 0 改 |
| e2e mock 复 Phase 9.22 模式 | 复用 `page.route('**/api/ripples/cascade/**', ...)` pattern, 加 1 new case 验证 filter dropdown 改 URL + 触发新 fetch |
| CascadeRunsPanel 既有 3 vitest 破 | `loadRuns()` 仍读 `filters.value`, 但 `route.query={}` 时 `filters={status:'all', minDepth:null, maxDepth:null, algorithm:'all'}` → `opts={}` → 等同 0 filter → 等同 Phase 9.22 既有行为 (3 vitest 0 改 contract) |
| `?max_depth=` 跟 Phase 9.19 既有 endpoint 冲突 | Phase 9.19 是 `get_ripple_cascade` (单 cascade preview), Phase 9.20/9.23 是 `get_ripple_cascade_runs` (runs list), 不同 endpoint, 0 冲突 |
| MEMORY.md / phases-8-dashboard.md 不更新 (F1 deferred) | 接受再超, F1 单独 phase 拆; 本 phase 不动 memory |
| CascadeRunsFilter 既有 testid-class-sync lint | 走 testid-class-sync 1:1 pattern (`.filter-status` 等 class 镜像 testid), 0 new warnings |
| vitest mock `useRoute/useRouter` 复杂 | 直接 `vi.mock('vue-router', ...)` 返 mock object, simple pattern 0 advanced router setup |
| e2e `page.goto('/?status=cancelled')` URL 解析 | Vue Router 4 默认 parse query string 到 route.query (跟 Phase 9.20 `?max_depth=` 既模式 1:1), 0 额外 config |
| `onMounted` 内 `loadRuns` 调 `await` | 既 3 vitest 0 改 (用 `await new Promise(r => setTimeout(r, 50))` 等 microtask, 跟 Phase 9.22 既 pattern 1:1) |
| Task 5 vitest 3 new test 需 import CascadeRunsFilter | 已有 T4 测试 imports 1:1, 复用 `await import('../../src/components/CascadeRunsFilter.vue')` 模式 |

---

## 验证 (主公 review commit 后)

### Step 1: pytest baseline + new tests

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pytest -q 2>&1 | tail -3
# 期望: 2478 passed, 0 failed, 27 skipped (2469 + 9 new = 2478)

pytest tests/cross_volume/test_cascade_runs_filters.py -v 2>&1 | tail -8
# 期望: 4 passed

pytest tests/dashboard/test_cascade_runs_filters_endpoint.py -v 2>&1 | tail -8
# 期望: 5 passed
```

### Step 2: vitest baseline + new tests

```bash
cd dashboard/frontend
pnpm exec vitest run 2>&1 | tail -3
# 期望: 167 passed, 0 failed (159 + 5 T4 + 3 T5 = 167)

pnpm exec vitest run tests/unit/cascade-runs-filter.spec.js 2>&1 | tail -8
# 期望: 5 passed

pnpm exec vitest run tests/unit/cascade-runs-panel.spec.js 2>&1 | tail -10
# 期望: 6 passed (3 旧 + 3 new URL sync)
```

### Step 3: e2e baseline + new tests

```bash
pnpm dev 2>&1 > /tmp/vite-verify.log &
VITE_PID=$!
sleep 5
pnpm exec playwright test 2>&1 | tail -10
kill $VITE_PID 2>/dev/null
wait 2>/dev/null
# 期望: 14 passed (12 旧 + 2 new)
```

### Step 4: ruff 0

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
ruff check infra/ tests/ 2>&1 | tail -3
# 期望: All checks passed!
```

### Step 5: lint + build 0 errors

```bash
cd dashboard/frontend
pnpm lint:all 2>&1 | tail -3
# 期望: 0 errors

pnpm build 2>&1 | tail -5
# 期望: ✓ built in <time>
```

### Step 6: git status clean + log

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git status
# 期望: working tree clean

git log --oneline -8
# 期望: 1 spec commit (8b15a3f) + 1 plan commit (TBD) + 6 task commits
```

---

## 完成定义 (DoD)

- [ ] spec + plan doc 创建 + commit (2 commits: 1 spec 8b15a3f, 1 plan TBD)
- [ ] `RippleStorage.get_cascade_runs` 扩 3 params + 3 WHERE clauses (storage)
- [ ] `dashboard/app.py:get_ripple_cascade_runs` 扩 4 Query() params + forward (endpoint, 含 9.22 spec 漏的 `status`)
- [ ] `api/index.js:fetchCascadeRuns` 扩 3 options + 3 URLSearchParams.set (frontend)
- [ ] `CascadeRunsFilter.vue` 创建 (~85L, NEW)
- [ ] `CascadeRunsPanel.vue` 改 +60L (filter import + useRoute/useRouter + 2 helpers + onFilterChange + watch + 1 template line)
- [ ] 4 storage pytest tests passed
- [ ] 5 endpoint pytest tests passed
- [ ] 5 vitest filter tests passed
- [ ] 3 vitest panel URL sync tests passed
- [ ] 2 e2e tests passed
- [ ] pytest 2469 → 2478 passed (9 new), 0 failed, 27 skipped (Phase 9.22 baseline 0 破)
- [ ] vitest 159 → 167 passed (8 new), Phase 9.22 既 3 vitest 0 破
- [ ] e2e 12 → 14 passed (2 new)
- [ ] ruff 0 issues
- [ ] pnpm lint:all 0 errors
- [ ] pnpm build 0 type errors
- [ ] 8 commits push origin/master (1 plan + 1 spec + 6 task TDD commits; spec 已 commit `8b15a3f`, plan + 6 task = 7 new)
- [ ] 0 旧测试代码改动 (Phase 9.22 既 2469 pytest + 159 vitest + 12 e2e baseline 全部通过, additive only)
- [ ] 0 production code 行为破坏 (filter 是 additive, 旧 caller 0 改继续 work)
- [ ] 0 误改 .env / 0 API key 泄漏
- [ ] 0 force-push / 0 --no-verify (除 commit pattern) / 0 Co-Authored-By footer
- [ ] 0 改 RippleDrawer.vue (filter 内部化)
- [ ] 0 改 CascadeGraph.vue / RippleFilter.vue / useWorkflowSocket / useRippleStore (0 触碰)
- [ ] 0 改 Phase 9.19/9.20/9.21/9.22 既有 production code behavior
- [ ] 0 改 `protocols.py:CascadeRunResponse` schema
- [ ] 0 改 backend `cascade_runs` 表 (无 migration)
- [ ] 0 改 `lingwen.py` CLI
- [ ] 0 改 19 historical spec/plan doc `:NNN` 行号
- [ ] 0 改 CLAUDE.md / pyproject.toml / pytest.ini / vite.config.js / vitest.config.js / playwright.config.js

---

## 完成后下一步

- **F1 bookkeeping** (urgent, 30min) — phases-8-dashboard.md 拆 8 段 era 文件 + typo 修 + legacy MEMORY.md v6.1 sync 决策 (防止超 200 line 永久)
- **Phase 9.24+** — 主线 4 项完成 (F4/F5/F6/F7), 选下一条线 (per F1-F16 决策矩阵, 推荐 F10 WS indicator 或 F12 per-tier 趋势线, UX 价值高工作量低)
- **Phase 9.25 polish** — UI 一致性 + data-testid 统一 (F14) + Playwright vitest 化 (F15) mechanical
- **Phase 10.0** — pre-F1 收尾: 全 phase 9 history 整理到 phases-9-cvg.md 新 era file, MEMORY.md era 指针 5 → 6 era
