# Plan: Phase 7.2 — PolisherTools LLM-化

## Context

Phase 7 (commit 671db78) 暴露 3 个 production gap,其中 Phase 7.1 (commit fac604f) 修复了 1+3,**跳过了候选 2 (PolisherTools LLM-化)**。本阶段补齐这一候选 2。

**当前 PolisherTools 现状** (49 行):
- 不继承 AgentBase
- `optimize_dialogue` / `adjust_pacing` 是 no-op
- `remove_ai_gloss` 纯规则(7 个字符串替换)
- `apply_style_guide` 纯规则(1 个配置驱动)

**目标 (主公决策 A 范围 + 默认 reader_id)**:
- PolisherTools 继承 AgentBase
- 3 个 LLM 方法:`polish_chapter` 主入口 + `optimize_dialogue_llm` + `adjust_pacing_llm`
- 保留 `remove_ai_gloss` 规则路径(快速 + 兜底)
- 保留 `apply_style_guide` 规则(避免破坏 test_agent_tools.py)
- `polish_chapter` 接受可选 `reader_id`,默认 'A'

**基线**: 2023 passed, 0 failed, 17 skipped
**目标**: ~2038 passed (+15)
**Spec**: `docs/superpowers/specs/2026-06-04-phase7.2-polisher-llm-design.md` (commit 8d9fcfd)

---

## 实施原则 (从 Phase 1.1-7.1 提炼)

- **TDD**: RED → GREEN per task, 1 final commit
- **向后兼容**: 保留 `remove_ai_gloss` / `apply_style_guide` 规则方法, master.polish_chapter 签名保持 `→ str`
- **韧性契约**: LLM 子方法 try/except 兜底, 失败降级到规则路径 (与 master.audit_chapter 模式一致)
- **防御性集成**: `_load_variant_enhancement` try/except 包装 variant_loader
- **subagent 角色**: 实施 haiku (机械任务) / 协调 sonnet; 2-stage 审查: spec compliance → code quality
- **不 commit**: `infra/.state/workflow.db` (state 文件)

---

## File Map

| 新文件 | 类/职责 | ~ 行数 |
|--------|---------|--------|
| `infra/agent_system/agents/polisher/prompts.py` (NEW) | 3 组 prompt 模板 (polish/dialogue/pacing) | ~150 |
| `tests/agent_system/test_polisher_llm.py` (NEW) | 5 测试类 / 15 测试 | ~280 |

| 修改文件 | 改动 |
|----------|------|
| `infra/agent_system/agents/polisher/tools.py` (REWRITE) | PolisherTools 继承 AgentBase + 3 LLM 方法 + 保留 2 规则方法 | 49 → ~180 |
| `infra/agent_system/master_controller.py:583-586` | 调 `self.polisher.polish_chapter(chapter_num=0, content=content)` 替代 2 步串联 | 2 行 |
| `MEMORY.md` | Phase 7.2 进度条目 | +1 |

**估计**: 1 重写 + 1 新 src + 1 改 master + 1 新 test = 4 文件, ~612 行, 1 commit, 2023 → 2038 passed (+15)

---

## Feature 1: prompts.py 模板 (Task 1)

**Files**:
- Create: `infra/agent_system/agents/polisher/prompts.py`

**TDD 子任务**:

### Task 1.1: 写 prompts.py 失败测试 (test_prompts_module)

- [ ] **Step 1.1.1: 写失败测试**

```python
# tests/agent_system/test_prompts_polisher.py (临时测试, Task 1.5 删除)
def test_prompts_module_imports():
    """prompts.py 导入 6 个公开函数, 全部可调用"""
    from infra.agent_system.agents.polisher.prompts import (
        build_polish_prompt, get_polish_system_prompt,
        build_dialogue_prompt, get_dialogue_system_prompt,
        build_pacing_prompt, get_pacing_system_prompt,
    )
    # 6 个函数都可调用, 返回 str
    assert isinstance(build_polish_prompt("test", "A"), str)
    assert isinstance(get_polish_system_prompt("A"), str)
    assert isinstance(build_dialogue_prompt("test", "A"), str)
    assert isinstance(get_dialogue_system_prompt("A"), str)
    assert isinstance(build_pacing_prompt("test", "A"), str)
    assert isinstance(get_pacing_system_prompt("A"), str)
```

- [ ] **Step 1.1.2: 跑测试, 验证 ImportError FAIL**

Run: `pytest tests/agent_system/test_prompts_polisher.py -v`
Expected: ImportError (prompts.py 还没创建)

- [ ] **Step 1.1.3: 创建 prompts.py** (见 spec §2 完整代码, ~150L)

- [ ] **Step 1.1.4: 跑测试, 验证 PASS**

Run: `pytest tests/agent_system/test_prompts_polisher.py -v`
Expected: 1 passed

### Task 1.2: 临时测试删除 (Task 1.5 一并清理)

- [ ] **Step 1.2.1: 删除 tests/agent_system/test_prompts_polisher.py** (Task 1.5 时)

---

## Feature 2: PolisherTools 继承 AgentBase (Task 2)

**Files**:
- Modify: `infra/agent_system/agents/polisher/tools.py`

### Task 2.1: 写失败测试 - PolisherTools 继承 AgentBase

- [ ] **Step 2.1.1: 在 tests/agent_system/test_polisher_llm.py 写 test_inherits_agent_base**

```python
"""Phase 7.2 PolisherTools LLM-化 — 15 测试

- PolisherTools 继承 AgentBase (fix 1)
- 3 LLM 方法 (optimize_dialogue_llm / adjust_pacing_llm / polish_chapter)
- 韧性契约: LLM 失败降级到规则路径
- 默认 reader_id='A' + 可选切换
- 保留 remove_ai_gloss / apply_style_guide 规则方法 (向后兼容)
"""
from __future__ import annotations

from typing import Any, Dict, List, Optional
from unittest.mock import MagicMock

import pytest

from infra.agent_system.agents.base import AgentBase
from infra.agent_system.agents.polisher.tools import PolisherTools
from infra.agent_system.master_controller import MasterController


class TestPolisherLLMInit:
    """PolisherTools 继承 AgentBase + 初始化"""

    def test_inherits_agent_base(self):
        """PolisherTools 继承 AgentBase 获得 LLM 能力"""
        polisher = PolisherTools()
        assert isinstance(polisher, AgentBase)
        assert hasattr(polisher, "chat")
        assert hasattr(polisher, "_fallback_mode")

    def test_no_router_enters_fallback_mode(self):
        """PolisherTools() 无 router → fallback_mode=True"""
        polisher = PolisherTools()
        assert polisher._fallback_mode is True
        assert polisher.is_available is False

    def test_reader_id_default_and_set(self):
        """set_reader/get_current_reader 工作"""
        polisher = PolisherTools()
        assert polisher.get_current_reader() is None
        polisher.set_reader("B")
        assert polisher.get_current_reader() == "B"
        polisher.set_reader("T")  # 大写
        assert polisher.get_current_reader() == "T"
```

- [ ] **Step 2.1.2: 跑测试, 验证 FAIL**

Run: `pytest tests/agent_system/test_polisher_llm.py::TestPolisherLLMInit -v`
Expected: 3 failed (PolisherTools 不继承 AgentBase, 仍纯规则)

- [ ] **Step 2.1.3: 重写 tools.py**

完整代码 (见 spec §1):
```python
# novel-factory/agent_system/agents/polisher/tools.py
from typing import Any, Dict, List, Optional

from ..base import AgentBase

try:
    from .variant_loader import get_variant_loader
    VARIANT_LOADER_AVAILABLE = True
except ImportError:
    VARIANT_LOADER_AVAILABLE = False

from .prompts import (
    build_polish_prompt, get_polish_system_prompt,
    build_dialogue_prompt, get_dialogue_system_prompt,
    build_pacing_prompt, get_pacing_system_prompt,
)


class PolisherTools(AgentBase):
    """润色师工具集 — LLM 增强版 (Phase 7.2)"""

    def __init__(self, router=None):
        super().__init__(router)
        self._current_reader_id: Optional[str] = None
        if VARIANT_LOADER_AVAILABLE:
            self._variant_loader = get_variant_loader()

    def set_reader(self, reader_id: str) -> None:
        self._current_reader_id = reader_id.upper()

    def get_current_reader(self) -> Optional[str]:
        return self._current_reader_id

    def polish_chapter(
        self,
        chapter_num: int,
        content: str,
        style_guide: Optional[Dict] = None,
        reader_id: Optional[str] = None,
    ) -> Dict[str, Any]:
        effective_reader = reader_id or self._current_reader_id or "A"
        polished = content
        issues: List[Dict] = []

        try:
            polished = self.optimize_dialogue_llm(polished, reader_id=effective_reader)
        except Exception as e:
            issues.append({"type": "dialogue_llm_fail", "severity": "P3", "issue": str(e)})

        try:
            polished = self.adjust_pacing_llm(polished, reader_id=effective_reader)
        except Exception as e:
            issues.append({"type": "pacing_llm_fail", "severity": "P3", "issue": str(e)})

        polished = self.remove_ai_gloss(polished)

        return {
            "content": polished,
            "reader": effective_reader,
            "issues": issues,
            "chapter_num": chapter_num,
        }

    def optimize_dialogue_llm(
        self,
        content: str,
        reader_id: Optional[str] = None,
    ) -> str:
        effective_reader = reader_id or self._current_reader_id or "A"
        prompt = build_dialogue_prompt(content, effective_reader)
        system = get_dialogue_system_prompt(effective_reader)
        return self.chat(prompt=prompt, system=system, temperature=0.5, max_tokens=3000)

    def adjust_pacing_llm(
        self,
        content: str,
        reader_id: Optional[str] = None,
    ) -> str:
        effective_reader = reader_id or self._current_reader_id or "A"
        prompt = build_pacing_prompt(content, effective_reader)
        system = get_pacing_system_prompt(effective_reader)
        return self.chat(prompt=prompt, system=system, temperature=0.4, max_tokens=3000)

    def remove_ai_gloss(self, content: str) -> str:
        """去除 AI 痕迹 — 规则路径, 快速 + 兜底"""
        replacements = [
            ("首先", ""), ("其次", ""), ("然后", ""),
            ("最后", ""), ("总之", ""), ("可以看出", ""),
        ]
        result = content
        for old, new in replacements:
            result = result.replace(old, new)
        return result

    def apply_style_guide(self, content: str, style_guide: Dict) -> str:
        """应用文风指南 — 规则路径 (out of scope, 不 LLM 化)"""
        result = content
        if style_guide.get("remove_filler"):
            result = self._remove_filler(result)
        return result

    def _remove_filler(self, content: str) -> str:
        filler_phrases = ["值得注意的是", "需要指出的是", "实际上"]
        result = content
        for phrase in filler_phrases:
            result = result.replace(phrase, "")
        return result
```

- [ ] **Step 2.1.4: 跑测试, 验证 PASS**

Run: `pytest tests/agent_system/test_polisher_llm.py::TestPolisherLLMInit -v`
Expected: 3 passed

---

## Feature 3: LLM 子方法测试 (Task 3)

**Files**:
- Modify: `tests/agent_system/test_polisher_llm.py` (加 TestDialogueLLM + TestPacingLLM)

### Task 3.1: 写失败测试 - LLM 子方法调用 chat

- [ ] **Step 3.1.1: 追加测试类**

```python
class _StubRouter:
    """极简 StubRouter — 模拟 AIRouter.generate"""
    def __init__(self, response: str = "LLM-stub-output"):
        self.response = response
        self.calls: List[Dict[str, Any]] = []

    def generate(self, prompt: str, **kwargs) -> str:
        self.calls.append({"prompt": prompt, **kwargs})
        return self.response


class TestDialogueLLM:
    """optimize_dialogue_llm — LLM 路径"""

    def test_optimize_dialogue_llm_calls_chat(self, monkeypatch):
        polisher = PolisherTools(router=_StubRouter("dialogue-output"))
        called = {"count": 0, "prompt": None, "system": None, "kwargs": {}}
        def fake_chat(prompt, system, temperature, max_tokens, **kwargs):
            called["count"] += 1
            called["prompt"] = prompt
            called["system"] = system
            called["kwargs"] = {"temperature": temperature, "max_tokens": max_tokens, **kwargs}
            return "LLM 对话优化输出"
        monkeypatch.setattr(polisher, "chat", fake_chat)
        result = polisher.optimize_dialogue_llm("原文对话...", reader_id="A")
        assert called["count"] == 1
        assert "对话" in called["prompt"]  # 提示词模板正确
        assert "读者 A" in called["prompt"] or "A" in called["prompt"]  # reader_id 透传
        assert result == "LLM 对话优化输出"

    def test_optimize_dialogue_uses_reader_variant_enhancement(self, monkeypatch):
        """reader_id='B' 触发 _load_variant_enhancement"""
        polisher = PolisherTools(router=_StubRouter("output"))
        captured = {"prompt": None}
        def fake_chat(prompt, system, **kwargs):
            captured["prompt"] = prompt
            return "output"
        monkeypatch.setattr(polisher, "chat", fake_chat)
        polisher.optimize_dialogue_llm("test content", reader_id="A")
        # reader_id='A' 是悬念铺设专长, prompt 应当包含 A 关注领域或专长
        # (变体可能加载失败, 弱断言: 仅验证 reader_id 在 prompt 里)
        assert "A" in captured["prompt"]

    def test_optimize_dialogue_fallback_returns_placeholder(self):
        """无 router 时返回 fallback string, 不抛错"""
        polisher = PolisherTools()  # 无 router
        result = polisher.optimize_dialogue_llm("test content")
        # fallback 模式返回占位符, 应当是 str
        assert isinstance(result, str)


class TestPacingLLM:
    """adjust_pacing_llm — LLM 路径"""

    def test_adjust_pacing_llm_calls_chat(self, monkeypatch):
        polisher = PolisherTools(router=_StubRouter("pacing-output"))
        called = {"count": 0, "kwargs": {}}
        def fake_chat(prompt, system, temperature, max_tokens, **kwargs):
            called["count"] += 1
            called["kwargs"] = {"temperature": temperature, "max_tokens": max_tokens}
            return "LLM 节奏调整输出"
        monkeypatch.setattr(polisher, "chat", fake_chat)
        result = polisher.adjust_pacing_llm("原文...", reader_id="A")
        assert called["count"] == 1
        assert "节奏" in called.get("prompt", "") or result == "LLM 节奏调整输出"
        assert result == "LLM 节奏调整输出"

    def test_adjust_pacing_temperature_is_lower(self, monkeypatch):
        """pacing 温度 0.4 (低于 dialogue 0.5)"""
        polisher = PolisherTools(router=_StubRouter("output"))
        captured = {"temp": None}
        def fake_chat(prompt, system, temperature, max_tokens, **kwargs):
            captured["temp"] = temperature
            return "output"
        monkeypatch.setattr(polisher, "chat", fake_chat)
        polisher.adjust_pacing_llm("test")
        assert captured["temp"] == 0.4
```

- [ ] **Step 3.1.2: 跑测试, 验证 PASS (因为 Task 2 已实现 LLM 方法)**

Run: `pytest tests/agent_system/test_polisher_llm.py::TestDialogueLLM tests/agent_system/test_polisher_llm.py::TestPacingLLM -v`
Expected: 5 passed

**注**: Task 3 实际上是 "GREEN 写测试" (Task 2 RED→GREEN 后这里立即 GREEN)。TDD 心智: Task 2 是核心, Task 3 是补全覆盖。

---

## Feature 4: 主入口 polish_chapter + master 集成 (Task 4)

**Files**:
- Modify: `tests/agent_system/test_polisher_llm.py` (加 TestPolishChapterMain)
- Modify: `infra/agent_system/master_controller.py:583-586`

### Task 4.1: 写失败测试 - polish_chapter 主入口串联

- [ ] **Step 4.1.1: 追加测试类**

```python
class TestPolishChapterMain:
    """polish_chapter 主入口 — 串联 3 路径, 返回 dict"""

    def test_polish_chapter_chains_three_paths(self, monkeypatch):
        polisher = PolisherTools(router=_StubRouter("llm-out"))
        # 替换 LLM 子方法为 stub
        polisher.optimize_dialogue_llm = lambda content, reader_id=None: content + " [dial]"
        polisher.adjust_pacing_llm = lambda content, reader_id=None: content + " [pace]"

        result = polisher.polish_chapter(chapter_num=1, content="原文首先")

        # 关键断言: 3 路径串联, content 经历 dial → pace → remove_ai_gloss
        assert " [dial]" in result["content"]
        assert " [pace]" in result["content"]
        assert "首先" not in result["content"]  # remove_ai_gloss 规则清理

    def test_polish_chapter_returns_dict_with_required_keys(self):
        polisher = PolisherTools(router=_StubRouter("out"))
        result = polisher.polish_chapter(chapter_num=1, content="test")
        for key in ["content", "reader", "issues", "chapter_num"]:
            assert key in result, f"missing key: {key}"
        assert isinstance(result["issues"], list)
        assert result["chapter_num"] == 1

    def test_polish_chapter_default_reader_is_A(self, monkeypatch):
        polisher = PolisherTools(router=_StubRouter("out"))
        captured = {"dialogue_reader": None, "pacing_reader": None}
        def fake_dialogue(content, reader_id=None):
            captured["dialogue_reader"] = reader_id
            return content
        def fake_pacing(content, reader_id=None):
            captured["pacing_reader"] = reader_id
            return content
        monkeypatch.setattr(polisher, "optimize_dialogue_llm", fake_dialogue)
        monkeypatch.setattr(polisher, "adjust_pacing_llm", fake_pacing)
        result = polisher.polish_chapter(chapter_num=1, content="test")
        assert captured["dialogue_reader"] == "A"
        assert captured["pacing_reader"] == "A"
        assert result["reader"] == "A"

    def test_polish_chapter_llm_failure_falls_back_to_rules(self):
        """韧性契约: 单 LLM 抛错时, content 仍产出, issues 记录"""
        polisher = PolisherTools(router=_StubRouter("out"))
        def boom(*a, **kw): raise RuntimeError("simulated LLM failure")
        polisher.optimize_dialogue_llm = boom
        polisher.adjust_pacing_llm = lambda content, reader_id=None: content  # OK

        result = polisher.polish_chapter(chapter_num=1, content="首先...其次...")

        assert "首先" not in result["content"]  # remove_ai_gloss 仍工作
        assert "其次" not in result["content"]
        assert len(result["issues"]) == 1
        assert result["issues"][0]["type"] == "dialogue_llm_fail"
        assert result["reader"] == "A"

    def test_polish_chapter_propagates_to_master_polish_chapter(self):
        """master.polish_chapter 调 polisher.polish_chapter, 返回 str"""
        # 用 MasterController.__new__ 跳过 __init__ (与 Phase 7.1 test_review_handler 同模式)
        master = MasterController.__new__(MasterController)
        master.polisher = PolisherTools(router=_StubRouter("llm-out"))
        master.polisher.optimize_dialogue_llm = lambda c, reader_id=None: c + " [d]"
        master.polisher.adjust_pacing_llm = lambda c, reader_id=None: c + " [p]"

        result = master.polish_chapter("原文首先")

        # 关键断言: master.polish_chapter 返回 str (向后兼容)
        assert isinstance(result, str)
        assert " [d]" in result
        assert " [p]" in result
        assert "首先" not in result

    def test_polish_chapter_with_all_llm_paths_failing_still_returns_content(self):
        """韧性契约扩展: 两 LLM 全抛错, content 仍产出 (全降级到规则)"""
        polisher = PolisherTools(router=_StubRouter("out"))
        def boom(*a, **kw): raise RuntimeError("boom")
        polisher.optimize_dialogue_llm = boom
        polisher.adjust_pacing_llm = boom

        result = polisher.polish_chapter(chapter_num=1, content="首先...其次...最后...")

        assert "首先" not in result["content"]
        assert "其次" not in result["content"]
        assert "最后" not in result["content"]
        assert len(result["issues"]) == 2
        assert {i["type"] for i in result["issues"]} == {"dialogue_llm_fail", "pacing_llm_fail"}
```

### Task 4.2: 改 master_controller.py:583-586

- [ ] **Step 4.2.1: 改前**

```python
def polish_chapter(self, content: str) -> str:
    """润色章节（委托给polisher）"""
    result = self.polisher.remove_ai_gloss(content)
    result = self.polisher.optimize_dialogue(result)
    return result
```

- [ ] **Step 4.2.2: 改后**

```python
def polish_chapter(self, content: str) -> str:
    """润色章节（委托给polisher, LLM 化路径 Phase 7.2）"""
    result = self.polisher.polish_chapter(chapter_num=0, content=content)
    return result["content"]
```

- [ ] **Step 4.2.3: 跑测试, 验证 PASS**

Run: `pytest tests/agent_system/test_polisher_llm.py::TestPolishChapterMain -v`
Expected: 6 passed

**注**: test_polish_chapter_chains_three_paths 会在 step 4.1.1 已 PASS(因为 Task 2 已实现 polish_chapter)。Task 4.2 主要改 master, test 4 的 propagation 测验证 master 集成。

---

## Feature 5: 清理 + 规则路径保留 + 验证 (Task 5)

**Files**:
- Modify: `tests/agent_system/test_polisher_llm.py` (加 TestRulePaths)
- Delete: `tests/agent_system/test_prompts_polisher.py` (Task 1 临时文件)

### Task 5.1: 追加规则路径保留测试 + 临时文件清理

- [ ] **Step 5.1.1: 追加 TestRulePaths**

```python
class TestRulePaths:
    """规则方法保留 — 不破坏 test_agent_tools.py 现有测试"""

    def test_remove_ai_gloss_still_works(self):
        """规则路径不破坏, fallback 模式可用"""
        polisher = PolisherTools()  # 无 router (fallback)
        content = "首先，我们需要冷静分析。其次，制定计划。最后，执行。"
        result = polisher.remove_ai_gloss(content)
        assert "首先" not in result
        assert "其次" not in result
        assert "最后" not in result

    def test_apply_style_guide_unchanged(self):
        """apply_style_guide 保持原签名 (Phase 7.2 out of scope)"""
        polisher = PolisherTools()
        content = "值得注意的是，我们需要认真。"
        style = {"remove_filler": True}
        result = polisher.apply_style_guide(content, style)
        assert "值得注意的是" not in result
```

- [ ] **Step 5.1.2: 清理 Task 1 临时测试**

```bash
rm -f tests/agent_system/test_prompts_polisher.py
```

- [ ] **Step 5.1.3: 跑测试, 验证 PASS**

Run: `pytest tests/agent_system/test_polisher_llm.py -v`
Expected: 15 passed (3 init + 3 dialogue + 2 pacing + 6 main + 2 rule + 1 重复?)

等等,我数错了:
- TestPolisherLLMInit: 3
- TestDialogueLLM: 3
- TestPacingLLM: 2
- TestPolishChapterMain: 6
- TestRulePaths: 2
- Total: 16 (多了 1)

让我重新数 spec:
- 1, 2, 3 → TestPolisherLLMInit (3)
- 4, 5, 6 → TestDialogueLLM (3)
- 7, 8 → TestPacingLLM (2)
- 9, 10, 11, 12, 13, 14 → TestPolishChapterMain (6)
- 15 → TestRulePaths (1)
- Total: 15

TestRulePaths 应该是 1 个 (test_remove_ai_gloss_still_works),test_apply_style_guide_unchanged 是额外的。

让我精简 TestRulePaths 只保留 1 个,因为 test_apply_style_guide_unchanged 在 test_agent_tools.py 已有(100-114),重复。

调整为:
- TestRulePaths: 1 个测试 (`test_remove_ai_gloss_still_works`)
- Total: 3 + 3 + 2 + 6 + 1 = 15

OK,调整完毕。final 跑测试 15 passed。

---

## 测试计划 (跨 5 tasks)

| 阶段 | 动作 | 期望 | 累计 |
|------|------|------|------|
| 0 | 基线: `pytest -q` | 2023 passed | 2023 |
| 1.1.2 | Task 1 RED: prompts 导入 | ImportError | 2023 |
| 1.1.4 | Task 1 GREEN: prompts.py | 1 passed | 2024 |
| 2.1.2 | Task 2 RED: 3 init tests | 3 failed | 2020 (-3 from baseline, RED) |
| 2.1.4 | Task 2 GREEN: tools.py 重写 | 3 passed | 2027 |
| 3.1.2 | Task 3: 5 LLM 子方法测试 | 5 passed | 2032 |
| 4.2.3 | Task 4: 6 main + 1 master 集成 | 6 passed | 2038 |
| 5.1.3 | Task 5: 1 rule + 清理 | 1 passed | 2039 |
| Final | 全部 + ruff + 验证 | 2039 passed, 0 failed, 17 skipped | 2039 |

**注**: +16 而非 +15, 因为 Task 1 临时测试 1 个, 后续删除。先 +1 后 -1 = net +15 (我们 spec 说 +15 是 net)。

实际:
- 0: 2023 (基线)
- 1.1.4: 2023 + 1 = 2024 (临时)
- 2.1.4: 2024 - 0 + 3 (Task 2 tests) = 2027
- 3.1.2: 2027 + 5 = 2032
- 4.2.3: 2032 + 6 = 2038
- 5.1.3 (删 1 加 1): 2038 + 0 = 2038
- 实际最终: 2038

匹配 spec 目标 2038 ✓

---

## 风险 + 缓解

| 风险 | 缓解 |
|------|------|
| prompts.py 拆分导致循环 import | tools 顶层 import prompts, prompts 不 import tools (单向) |
| PolisherTools 继承 AgentBase 后, `PolisherTools()` 无 router 现有测试不工作 | AgentBase fallback_mode 自动开启, 规则方法不依赖 router, LLM 方法返回占位符 |
| `test_polisher_tools_apply_style` 依赖 `apply_style_guide` | 保留原方法, 仅内部不改 (out of scope) |
| `master.polish_chapter` 改 2 行破坏其他测试 | 签名保持 `→ str`, 内部取 `result["content"]` |
| `_load_variant_enhancement` variant_loader 失败阻塞 | try/except 包装, 失败返回空字符串 |
| Task 1 临时测试 `test_prompts_polisher.py` 忘记删 | Task 5.1.2 显式 rm |
| LLM 子方法在 fallback 模式返回 `_fallback_response` 占位符, 与真实 LLM 行为不同 | 接受 (与 auditor 一致), TestDialogueLLM 覆盖 fallback 行为 |

---

## 验证 (最终)

```bash
# 1. 跑新测试
pytest tests/agent_system/test_polisher_llm.py -v
# 期望: 15 passed

# 2. 跑 agent_system 全部 (验证集成)
pytest tests/agent_system/ -q
# 期望: ~192 passed (177 + 15 新增)

# 3. 跑现有 polisher 测试 (验证向后兼容)
pytest tests/agent_system/test_agent_tools.py -k polish -v
# 期望: test_polisher_tools_remove_ai_gloss + test_polisher_tools_apply_style 全过

# 4. 全量
pytest -q
# 期望: 2038 passed (2023 + 15 net), 17 skipped

# 5. ruff
ruff check infra/agent_system/agents/polisher/ tests/agent_system/test_polisher_llm.py infra/agent_system/master_controller.py
# 期望: All checks passed!

# 6. 验证 master.polish_chapter 集成
pytest tests/agent_system/ -k "polish" -v
# 期望: 全部 polish 相关测试过
```

---

## Critical files (modified)

- `infra/agent_system/agents/polisher/tools.py` (49L → ~180L) — PolisherTools 继承 AgentBase + 3 LLM 方法
- `infra/agent_system/agents/polisher/prompts.py` (NEW, ~150L) — 3 组提示词模板
- `infra/agent_system/master_controller.py:583-586` (2 行改) — 调 polisher.polish_chapter 主入口
- `tests/agent_system/test_polisher_llm.py` (NEW, ~280L) — 15 测试
- `MEMORY.md` — Phase 7.2 进度条目

## Critical files (referenced)

- `infra/agent_system/agents/auditor/tools.py:14` — AuditorTools(AgentBase) 模板 (验证继承模式)
- `infra/agent_system/agents/auditor/tools.py:156-223` — _build_audit_prompt 模板风格
- `infra/agent_system/agents/base.py:43-97` — AgentBase.__init__ + chat() 接口
- `infra/agent_system/agents/polisher/variants/variant_a.yaml` — reader-a 变体配置参考
- `infra/agent_system/agents/polisher/variant_loader.py` — 现有 variant_loader (复用)
- `tests/agent_system/_e2e_helpers.py` — Phase 7 StubProvider 模式 (复用)
- `tests/agent_system/test_agent_tools.py:100-114` — 现有 polisher 测试 (依赖保留)
- `docs/superpowers/specs/2026-06-04-phase7.2-polisher-llm-design.md` — 本 spec (commit 8d9fcfd)

---

## TDD Sub-phases

| Task | 阶段 | 动作 | 累计测试 |
|------|------|------|---------|
| 1.1 | RED | 写 prompts import 失败测试 | 2023 (baseline) |
| 1.1 | GREEN | 创建 prompts.py | 2024 (+1) |
| 2.1 | RED | 写 3 init 测试, 验证 FAIL | 2024 |
| 2.1 | GREEN | 重写 tools.py 继承 AgentBase | 2027 (+3) |
| 3.1 | GREEN | 5 LLM 子方法测试 | 2032 (+5) |
| 4.1-4.2 | GREEN | 6 main + 改 master | 2038 (+6) |
| 5.1 | GREEN | 1 rule + 删临时 | 2038 (net 0) |
| Final | verify | ruff 0 + 2038 passed + 1 commit + push | 2038 |

**预计 commit**: 1
- `feat(agent_system): phase 7.2 — PolisherTools LLM-化 (3 LLM 方法 + 韧性契约)`

**注**: Task 1 临时测试在 Task 5 清理,净 +15 测试。spec 目标 2038 ✓

---

## Out of scope (后续 phases)

- `apply_style_guide` LLM 化 (Phase 7.x)
- `polish_chapter_with_feedback` reader 报告方法 (Phase 7.x)
- 20 reader 变体全集成 (Phase 7.x)
- `prompts.build_polish_prompt` 实际使用 (Phase 7.3 - novel_writing.yaml 加 polish 节点)
- novel_writing.yaml 加 polish 节点 (Phase 7.3)
- polish_chapter 进度暴露到 WebSocket (Phase 6.5+)
