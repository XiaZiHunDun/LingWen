# Phase 8.12 — Budget DB 持久化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 budget 从内存 in-memory (Phase 8.8) 升级为 SQLite 持久化,新增 per-day / per-week 两档,AgentComputeFn 一次性检查 3 档超阈即 raise,Sidebar 优先显示最紧迫的 budget。

**Architecture:** 新增 `infra/agent_system/budget_persistence.py` (`BudgetService` 类,append-only 模式,单表 `budgets` + 2 索引),`CostBudgetExceeded` 扩 `scope` 字段 (backward compat `scope='run'`),`AgentComputeFn.__call__` 加 window check (`scope='day'/'week'`),dashboard `protocols.py` 加 2 字段 (`budget_per_day`/`budget_per_week`) + 2 端点 (`GET /api/budgets`, `PUT /api/budgets/{day,week}`),`SidebarCostBanner.vue` 改 3 档 priority cascade。Phase 8.5 `cost_records` 表 0 改。

**Tech Stack:** SQLite 3 (stdlib `sqlite3`) + Python 3.13 (Phase 8.5/8.8 模式) + FastAPI (dashboard) + Vue 3 Composition API (front-end) + Playwright (e2e)。

---

## 实施原则

- **TDD RED → GREEN → commit**: 每 task 5 steps (写失败 test → 跑确认 RED → 写最小 impl → 跑确认 GREEN → commit)
- **Append-only budget 模式**: 每次 set 插新行 (含 run_id),"current" = `ORDER BY id DESC LIMIT 1 WHERE scope=?`。"无" = 0 行。"修改" = 插新行,旧行变历史
- **Window semantics**: per-day = UTC 00:00-23:59, per-week = Mon-Sun (`date('now', 'weekday 0', '-7 days')`)
- **Backward compat**: `CostBudgetExceeded.scope: str = "run"` default 保 Phase 8.8/8.9/8.10 tests 0 改; `budget_service: Optional[BudgetService] = None` default 保 master_controller / got_bridge 双路径 baseline
- **Single DB file**: 复用 `infra/.state/cost_tracker.db` (Phase 8.5 路径已就位,gitignored),`budgets` 表并存 (避免双 db 文件)
- **Frequent commits**: 6 tasks = 6 commits (1 task 1 commit),1 final feat commit 总览

---

## Critical files

### Created
- `infra/agent_system/budget_persistence.py` (~150L) — `BudgetService` 类
- `tests/agent_system/test_budget_persistence.py` (~250L) — 12 unit tests
- `tests/agent_system/test_master_controller_budget.py` (~150L) — 5 tests
- `tests/agent_system/test_got_bridge_budget.py` (~120L) — 6 tests
- `tests/agent_system/test_dashboard_budget_endpoints.py` (~150L) — 5 tests
- `tests/dashboard/test_sidebar_banner_priority_cascade.py` (~80L) — 1 ceremonial e2e

### Modified
- `infra/ai_service/cost_tracker.py` (+10L) — `CostBudgetExceeded.scope: str = "run"` field
- `infra/agent_system/master_controller.py` (+20L) — `budget_service` kwarg + `run_id` uuid4 + `set(scope='run')` call
- `infra/agent_system/got_bridge.py` (+15L) — `AgentComputeFn.__call__` 加 `check_all_scopes(total_cost)` call after Phase 8.8 `check_budget`
- `dashboard/protocols.py` (+30L) — `_extract_budget_per_window` helper + 2 字段
- `dashboard/app.py` (+50L) — `GET /api/budgets` + `PUT /api/budgets/{day,week}` + `BudgetSetRequest`
- `dashboard/frontend/src/components/SidebarCostBanner.vue` (+30L, 0 删) — 3 档 priority cascade

### NOT modified
- `infra/agent_system/cost_persistence.py` (Phase 8.5 `cost_records` 0 改)
- `infra/agent_system/cost_tracker.py` 内存逻辑 (`record()` / `total_cost` 0 改)
- `dashboard/ws.py` (Phase 6.4 polling+broadcast 0 改)
- `dashboard/frontend/src/composables/useWorkflowSocket.js` (0 改, 透传 status)
- `dashboard/frontend/src/components/CostBarChart.vue` (Phase 8.7 0 改)
- `dashboard/frontend/src/components/WorkflowStatus.vue` (Phase 8.11 0 改)
- `dashboard/frontend/src/App.vue` (Phase 8.11 0 改)
- 12 SCENARIOS 路由 / 8 historical spec/plan docs (`:NNN` 行号不动)
- `pyproject.toml` / `pytest.ini` / `CLAUDE.md` / `MEMORY.md`

---

## Task 1: BudgetService 类 (TDD, 12 unit tests)

**Files:**
- Create: `infra/agent_system/budget_persistence.py`
- Test: `tests/agent_system/test_budget_persistence.py`

- [ ] **Step 1: Write failing tests**

```python
# tests/agent_system/test_budget_persistence.py
"""Tests for BudgetService (Phase 8.12)

Per-run / per-day / per-week budget persistence. Append-only `budgets` table.
Per-day = UTC 00:00-23:59, per-week = Mon-Sun.
"""
from __future__ import annotations

import sqlite3
from datetime import datetime, timedelta, timezone
from pathlib import Path
from typing import Optional

import pytest

from infra.agent_system.budget_persistence import BudgetService


class TestBudgetService:
    """BudgetService 行为 — mirror CostTrackerDB patterns"""

    def test_init_db_creates_table_and_indices(self, tmp_path: Path) -> None:
        """init_db → budgets table + 2 indices 存在"""
        db = tmp_path / "test.db"
        service = BudgetService(db_path=db)
        service.init_db()
        with sqlite3.connect(str(db)) as conn:
            tables = conn.execute(
                "SELECT name FROM sqlite_master WHERE type='table' AND name='budgets'"
            ).fetchall()
            assert len(tables) == 1
            indices = conn.execute(
                "SELECT name FROM sqlite_master WHERE type='index' AND tbl_name='budgets'"
            ).fetchall()
            index_names = {row[0] for row in indices}
            assert any("scope" in n for n in index_names)
            assert any("run_id" in n for n in index_names)

    def test_set_scope_run_inserts_with_run_id(self, tmp_path: Path) -> None:
        """set('run', 0.1, 'abc') → 1 行, scope/run_id/usd 正确"""
        service = BudgetService(db_path=tmp_path / "test.db")
        service.init_db()
        service.set("run", 0.1, run_id="abc123")
        with sqlite3.connect(str(tmp_path / "test.db")) as conn:
            row = conn.execute(
                "SELECT scope, usd, run_id FROM budgets"
            ).fetchone()
        assert row[0] == "run"
        assert row[1] == 0.1
        assert row[2] == "abc123"

    def test_set_scope_day_week_run_id_none(self, tmp_path: Path) -> None:
        """set('day', 0.5) / set('week', 2.0) → run_id NULL"""
        service = BudgetService(db_path=tmp_path / "test.db")
        service.init_db()
        service.set("day", 0.5)
        service.set("week", 2.0)
        with sqlite3.connect(str(tmp_path / "test.db")) as conn:
            rows = conn.execute(
                "SELECT scope, usd, run_id FROM budgets ORDER BY id"
            ).fetchall()
        assert rows[0] == ("day", 0.5, None)
        assert rows[1] == ("week", 2.0, None)

    def test_get_current_returns_latest_for_scope(self, tmp_path: Path) -> None:
        """set 2 次同 scope → get_current 返最后一次"""
        service = BudgetService(db_path=tmp_path / "test.db")
        service.init_db()
        service.set("day", 0.5)
        service.set("day", 1.0)
        current = service.get_current("day")
        assert current is not None
        assert current.usd == 1.0

    def test_get_current_returns_none_when_no_row(self, tmp_path: Path) -> None:
        """0 行 → get_current 返 None"""
        service = BudgetService(db_path=tmp_path / "test.db")
        service.init_db()
        assert service.get_current("day") is None
        assert service.get_current("week") is None
        assert service.get_current("run", run_id="missing") is None

    def test_get_current_run_filters_by_run_id(self, tmp_path: Path) -> None:
        """2 run 各 set → get_current(scope='run', run_id='r1') 只返 r1"""
        service = BudgetService(db_path=tmp_path / "test.db")
        service.init_db()
        service.set("run", 0.1, run_id="r1")
        service.set("run", 0.2, run_id="r2")
        r1 = service.get_current("run", run_id="r1")
        r2 = service.get_current("run", run_id="r2")
        assert r1 is not None and r1.usd == 0.1
        assert r2 is not None and r2.usd == 0.2

    def test_check_all_scopes_per_run_exceeded(self, tmp_path: Path) -> None:
        """per-run set 0.1, total_cost 0.15 → raise CostBudgetExceeded(scope='run')"""
        from infra.ai_service.cost_tracker import CostBudgetExceeded
        service = BudgetService(db_path=tmp_path / "test.db")
        service.init_db()
        service.set("run", 0.1, run_id="r1")
        with pytest.raises(CostBudgetExceeded) as exc:
            service.check_all_scopes(total_cost_usd=0.15, current_run_id="r1")
        assert exc.value.scope == "run"
        assert exc.value.used_usd == 0.15
        assert exc.value.budget_usd == 0.1

    def test_check_all_scopes_per_day_within_window(self, tmp_path: Path) -> None:
        """set 0.5 今日, total_cost 0.3 → pass (no raise)"""
        service = BudgetService(db_path=tmp_path / "test.db")
        service.init_db()
        service.set("day", 0.5)
        # 不 raise = pass
        service.check_all_scopes(total_cost_usd=0.3, current_run_id="r1")

    def test_check_all_scopes_per_day_outside_window(self, tmp_path: Path) -> None:
        """set 0.5 昨日, total_cost 0.3 → pass (window 失效)"""
        service = BudgetService(db_path=tmp_path / "test.db")
        service.init_db()
        # 直接插一行 set_at = 昨天
        with sqlite3.connect(str(tmp_path / "test.db")) as conn:
            yesterday = (datetime.now(timezone.utc) - timedelta(days=1)).isoformat()
            conn.execute(
                "INSERT INTO budgets (scope, usd, run_id, set_at) VALUES (?, ?, ?, ?)",
                ("day", 0.5, None, yesterday),
            )
            conn.commit()
        # 不 raise = pass (window 失效, 不 check)
        service.check_all_scopes(total_cost_usd=0.3, current_run_id="r1")

    def test_check_all_scopes_per_week_within_window(self, tmp_path: Path) -> None:
        """set 2.0 本周, total_cost 1.5 → pass"""
        service = BudgetService(db_path=tmp_path / "test.db")
        service.init_db()
        service.set("week", 2.0)
        service.check_all_scopes(total_cost_usd=1.5, current_run_id="r1")

    def test_check_all_scopes_priority_run_over_day_over_week(
        self, tmp_path: Path
    ) -> None:
        """3 档都设, total_cost 超 per-run → raise(scope='run') 不管 day/week"""
        from infra.ai_service.cost_tracker import CostBudgetExceeded
        service = BudgetService(db_path=tmp_path / "test.db")
        service.init_db()
        service.set("run", 0.1, run_id="r1")
        service.set("day", 10.0)
        service.set("week", 20.0)
        with pytest.raises(CostBudgetExceeded) as exc:
            service.check_all_scopes(total_cost_usd=0.15, current_run_id="r1")
        assert exc.value.scope == "run"

    def test_list_runs_returns_recent_distinct(self, tmp_path: Path) -> None:
        """set 3 run (r1/r2/r3) → list_runs(limit=2) 返 2 最新"""
        service = BudgetService(db_path=tmp_path / "test.db")
        service.init_db()
        service.set("run", 0.1, run_id="r1")
        service.set("run", 0.2, run_id="r2")
        service.set("run", 0.3, run_id="r3")
        runs = service.list_runs(limit=2)
        assert len(runs) == 2
        assert "r3" in runs
        assert "r2" in runs
        assert "r1" not in runs
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
pytest tests/agent_system/test_budget_persistence.py -v 2>&1 | tail -20
# 期望: 全部 FAIL with "ModuleNotFoundError: No module named 'infra.agent_system.budget_persistence'" (RED 确认)
```

- [ ] **Step 3: Write minimal implementation**

```python
# infra/agent_system/budget_persistence.py
"""Budget 持久化 — Phase 8.12

Per-run / per-day / per-week budget persistence (append-only `budgets` table).
Reuses infra/.state/cost_tracker.db (跟 cost_records 共存,避免双 db 文件).

设计:
- 单表 budgets (id PK, scope, usd, run_id, set_at) + 2 索引 (scope+set_at, run_id)
- Append-only: set 插新行, "current" = `ORDER BY id DESC LIMIT 1 WHERE scope=?`
- Window semantics: 'day' = UTC 00:00-23:59, 'week' = Mon-Sun
- 'run' scope 不需 window, 用 run_id 隔离

Why single DB 跟 cost_records 共存:
- 跟 Phase 8.5 cost_records 表并存, 单一 file 简化 ops
- gitignored infra/.state/cost_tracker.db 路径已就位

Backward compat: scope='run' 跟 Phase 8.8 _current_budget_usd 等价 (in-memory)
"""
from __future__ import annotations

import sqlite3
from contextlib import contextmanager
from dataclasses import dataclass
from datetime import datetime, timedelta, timezone
from pathlib import Path
from typing import Iterator, Optional

from infra.ai_service.cost_tracker import CostBudgetExceeded

# 默认 DB 路径: 复用 cost_tracker.db (gitignored)
_DB_PATH = Path(__file__).parent.parent / ".state" / "cost_tracker.db"


@dataclass(frozen=True)
class BudgetEntry:
    """Budget 持久化条目 (append-only)"""
    id: int
    scope: str  # 'run' | 'day' | 'week'
    usd: float
    run_id: Optional[str]
    set_at: datetime  # UTC


def _is_within_window(set_at: datetime, scope: str, now: datetime) -> bool:
    """检查 set_at 是否在 scope 对应 window 内 (UTC)."""
    if scope == "day":
        return set_at.date() == now.date()
    elif scope == "week":
        # Mon-Sun: 推到本周一 00:00
        days_since_monday = now.weekday()
        week_start = (now - timedelta(days=days_since_monday)).replace(
            hour=0, minute=0, second=0, microsecond=0
        )
        return set_at >= week_start
    return True  # 'run' 不需 window check


class BudgetService:
    """SQLite 持久化 Budget (Phase 8.12)

    3 档 scope (run / day / week), append-only 历史,
    per-run 用 run_id 隔离, per-day/per-week 用 UTC Calendar window.
    """

    def __init__(
        self,
        db_path: Optional[Path] = None,
        init_if_missing: bool = True,
    ) -> None:
        self.db_path = db_path or _DB_PATH
        if init_if_missing:
            self._ensure_db_path()

    def _ensure_db_path(self) -> None:
        if not self.db_path.parent.exists():
            self.db_path.parent.mkdir(parents=True, exist_ok=True)

    @contextmanager
    def _connect(self) -> Iterator[sqlite3.Connection]:
        conn = sqlite3.connect(str(self.db_path), timeout=5)
        conn.row_factory = sqlite3.Row
        try:
            yield conn
            conn.commit()
        finally:
            conn.close()

    def init_db(self) -> None:
        """初始化 budgets 表 + 2 索引 (幂等 CREATE IF NOT EXISTS)"""
        with self._connect() as conn:
            conn.executescript("""
                CREATE TABLE IF NOT EXISTS budgets (
                    id INTEGER PRIMARY KEY AUTOINCREMENT,
                    scope TEXT NOT NULL CHECK(scope IN ('run', 'day', 'week')),
                    usd REAL NOT NULL CHECK(usd >= 0),
                    run_id TEXT,
                    set_at TEXT NOT NULL
                );
                CREATE INDEX IF NOT EXISTS idx_budgets_scope_set_at
                    ON budgets(scope, set_at DESC);
                CREATE INDEX IF NOT EXISTS idx_budgets_run_id
                    ON budgets(run_id);
            """)

    def set(
        self,
        scope: str,
        usd: float,
        run_id: Optional[str] = None,
    ) -> BudgetEntry:
        """插一条新 budget (append-only)"""
        if scope not in ("run", "day", "week"):
            raise ValueError(f"scope must be 'run'/'day'/'week', got {scope!r}")
        if usd < 0:
            raise ValueError(f"usd must be non-negative, got {usd}")
        self.init_db()
        set_at = datetime.now(timezone.utc).isoformat()
        with self._connect() as conn:
            cursor = conn.execute(
                "INSERT INTO budgets (scope, usd, run_id, set_at) VALUES (?, ?, ?, ?)",
                (scope, usd, run_id, set_at),
            )
            new_id = cursor.lastrowid
        return BudgetEntry(
            id=new_id,
            scope=scope,
            usd=usd,
            run_id=run_id,
            set_at=datetime.fromisoformat(set_at),
        )

    def get_current(
        self,
        scope: str,
        run_id: Optional[str] = None,
    ) -> Optional[BudgetEntry]:
        """返该 scope 最新一条 (None if 0 行)"""
        self.init_db()
        if scope == "run" and run_id is not None:
            sql = (
                "SELECT id, scope, usd, run_id, set_at FROM budgets "
                "WHERE scope = ? AND run_id = ? ORDER BY id DESC LIMIT 1"
            )
            params: tuple = (scope, run_id)
        else:
            sql = (
                "SELECT id, scope, usd, run_id, set_at FROM budgets "
                "WHERE scope = ? ORDER BY id DESC LIMIT 1"
            )
            params = (scope,)
        with self._connect() as conn:
            row = conn.execute(sql, params).fetchone()
        if row is None:
            return None
        return BudgetEntry(
            id=row["id"],
            scope=row["scope"],
            usd=row["usd"],
            run_id=row["run_id"],
            set_at=datetime.fromisoformat(row["set_at"]),
        )

    def check_all_scopes(
        self,
        total_cost_usd: float,
        current_run_id: Optional[str] = None,
    ) -> None:
        """检查 3 档 budget, 任一超阈 raise CostBudgetExceeded(scope=...)

        Priority: per-run > per-day > per-week (run 优先因为最严苛)
        Window: per-day/per-week 用 UTC Calendar (set_at 必须在 window 内)
        """
        now = datetime.now(timezone.utc)
        # 1. Check per-run (current_run_id 必传)
        if current_run_id is not None:
            run_budget = self.get_current("run", run_id=current_run_id)
            if run_budget is not None and total_cost_usd > run_budget.usd:
                raise CostBudgetExceeded(
                    used_usd=total_cost_usd,
                    budget_usd=run_budget.usd,
                    scope="run",
                )
        # 2. Check per-day (window 内)
        day_budget = self.get_current("day")
        if (
            day_budget is not None
            and _is_within_window(day_budget.set_at, "day", now)
            and total_cost_usd > day_budget.usd
        ):
            raise CostBudgetExceeded(
                used_usd=total_cost_usd,
                budget_usd=day_budget.usd,
                scope="day",
            )
        # 3. Check per-week (window 内)
        week_budget = self.get_current("week")
        if (
            week_budget is not None
            and _is_within_window(week_budget.set_at, "week", now)
            and total_cost_usd > week_budget.usd
        ):
            raise CostBudgetExceeded(
                used_usd=total_cost_usd,
                budget_usd=week_budget.usd,
                scope="week",
            )

    def list_runs(self, limit: int = 20) -> list[str]:
        """返最近 N 个 distinct run_id (按最近 set_at 倒序)"""
        self.init_db()
        with self._connect() as conn:
            rows = conn.execute(
                """
                SELECT DISTINCT run_id FROM budgets
                WHERE scope = 'run' AND run_id IS NOT NULL
                ORDER BY MAX(id) DESC LIMIT ?
                """,
                (limit,),
            ).fetchall()
        return [r["run_id"] for r in rows]


__all__ = ["BudgetService", "BudgetEntry"]
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
pytest tests/agent_system/test_budget_persistence.py -v 2>&1 | tail -25
# 期望: 12 passed
```

- [ ] **Step 5: Commit**

```bash
git add infra/agent_system/budget_persistence.py tests/agent_system/test_budget_persistence.py
git commit -m "feat(budget): phase 8.12 task 1 — BudgetService class (append-only budgets table)"
```

---

## Task 2: CostBudgetExceeded 扩 `scope` 字段

**Files:**
- Modify: `infra/ai_service/cost_tracker.py:51-65`

- [ ] **Step 1: Modify `CostBudgetExceeded` to add `scope` field with backward compat default**

```python
# infra/ai_service/cost_tracker.py:51-65
class CostBudgetExceeded(Exception):
    """Phase 8.8: raised by CostTracker.check_budget when total_cost() > budget_usd.
    Phase 8.12: 扩 scope field (default 'run' 保 backward compat, Phase 8.8/8.9/8.10
                tests 0 改; 新 BudgetService.check_all_scopes 显式传 'run'/'day'/'week').

    Attributes are exposed for caller introspection (no re-parsing of message).
    Strictly greater than triggers (equal is OK).
    """

    def __init__(
        self,
        used_usd: float,
        budget_usd: float,
        scenario: str | None = None,
        scope: str = "run",  # NEW Phase 8.12 (default 'run' backward compat)
    ) -> None:
        self.used_usd = used_usd
        self.budget_usd = budget_usd
        self.scenario = scenario
        self.scope = scope
        scenario_msg = f" (last scenario: {scenario})" if scenario else ""
        scope_msg = f" [scope={scope}]" if scope != "run" else ""
        super().__init__(
            f"Cost budget exceeded: ${used_usd:.4f} / ${budget_usd:.4f}{scope_msg}{scenario_msg}"
        )
```

- [ ] **Step 2: Run existing tests to verify backward compat**

```bash
pytest tests/ai_service/test_cost_tracker.py -v 2>&1 | tail -20
# 期望: 全部 passed (Phase 8.8 tests 不破, default scope='run' 兜底)

pytest tests/agent_system/test_cost_persistence.py -v
# 期望: 12 passed (Task 1 tests 不破 — BudgetService.check_all_scopes 显式传 scope)
```

- [ ] **Step 3: Commit**

```bash
git add infra/ai_service/cost_tracker.py
git commit -m "feat(budget): phase 8.12 task 2 — CostBudgetExceeded.scope field (backward compat default 'run')"
```

---

## Task 3: master_controller 注入 budget_service + run_id uuid4

**Files:**
- Modify: `infra/agent_system/master_controller.py:97-127, 280-282`
- Test: `tests/agent_system/test_master_controller_budget.py` (NEW)

- [ ] **Step 1: Write failing tests**

```python
# tests/agent_system/test_master_controller_budget.py
"""Tests for MasterController budget_service injection + run_id (Phase 8.12)"""
from __future__ import annotations

import re
from pathlib import Path
from typing import Optional
from unittest.mock import MagicMock

import pytest

from infra.agent_system.master_controller import MasterController


@pytest.fixture
def tmp_state_dir(tmp_path: Path) -> Path:
    return tmp_path


def _make_stub_master(state_dir: Path) -> MasterController:
    """构造 MasterController 用 stub router/state 避免真实依赖"""
    from infra.agent_system.agent_config import MasterControllerConfig
    config = MasterControllerConfig(
        state_dir=state_dir,
        router_config=None,
        social_config=None,
        skill_registry_config=None,
    )
    master = MasterController.__new__(MasterController)
    master._config = config
    master._router = MagicMock()
    master.cost_tracker = MagicMock()
    master._current_budget_usd = None
    return master


class TestMasterControllerBudget:
    """MasterController budget_service 注入 + run_id uuid4 行为"""

    def test_run_workflow_generates_run_id_uuid4_hex(
        self, tmp_state_dir: Path
    ) -> None:
        """run_workflow 后, internal run_id 是 32 char hex (uuid4().hex)"""
        master = _make_stub_master(tmp_state_dir)
        # Stub budget_service
        master.budget_service = MagicMock()
        # Stub run_workflow 内部依赖 (避免真实 GoT 调度)
        master._last_scheduler = None
        master._last_graph = None
        # Patch _harvest_decision_specs + build_got_scheduler
        master._harvest_decision_specs = MagicMock(return_value=[])
        from infra.agent_system import got_bridge
        original_build = got_bridge.build_got_scheduler
        got_bridge.build_got_scheduler = MagicMock(
            return_value=(MagicMock(), MagicMock(node_ids=lambda: [], get_node=MagicMock(return_value=MagicMock(depends_on=[])), has_execution=lambda _: False, get_execution=lambda _: None))
        )
        try:
            result = master.run_workflow(
                workflow_name="test", cost_budget_usd=0.1
            )
            # Verify call to budget_service.set included run_id (32 char hex)
            call_kwargs = master.budget_service.set.call_args.kwargs
            assert "run_id" in call_kwargs
            assert re.match(r"^[0-9a-f]{32}$", call_kwargs["run_id"]), (
                f"run_id should be 32 char hex (uuid4().hex), got {call_kwargs['run_id']!r}"
            )
        finally:
            got_bridge.build_got_scheduler = original_build

    def test_run_workflow_calls_budget_service_set_run(
        self, tmp_state_dir: Path
    ) -> None:
        """run_workflow 调 budget_service.set(scope='run', usd=X, run_id=...)"""
        master = _make_stub_master(tmp_state_dir)
        master.budget_service = MagicMock()
        master._harvest_decision_specs = MagicMock(return_value=[])
        from infra.agent_system import got_bridge
        original_build = got_bridge.build_got_scheduler
        got_bridge.build_got_scheduler = MagicMock(
            return_value=(MagicMock(), MagicMock(node_ids=lambda: [], get_execution=lambda _: None, has_execution=lambda _: False, get_node=MagicMock(return_value=MagicMock(depends_on=[]))))
        )
        try:
            master.run_workflow(workflow_name="test", cost_budget_usd=0.1)
            master.budget_service.set.assert_called_once()
            call_kwargs = master.budget_service.set.call_args.kwargs
            assert call_kwargs["scope"] == "run"
            assert call_kwargs["usd"] == 0.1
            assert "run_id" in call_kwargs
        finally:
            got_bridge.build_got_scheduler = original_build

    def test_run_workflow_no_budget_service_does_not_call_set(
        self, tmp_state_dir: Path
    ) -> None:
        """budget_service=None → 不 set (backward compat)"""
        master = _make_stub_master(tmp_state_dir)
        master.budget_service = None
        master._harvest_decision_specs = MagicMock(return_value=[])
        from infra.agent_system import got_bridge
        original_build = got_bridge.build_got_scheduler
        got_bridge.build_got_scheduler = MagicMock(
            return_value=(MagicMock(), MagicMock(node_ids=lambda: [], get_execution=lambda _: None, has_execution=lambda _: False, get_node=MagicMock(return_value=MagicMock(depends_on=[]))))
        )
        try:
            # 不 raise 即 pass
            master.run_workflow(workflow_name="test", cost_budget_usd=0.1)
        finally:
            got_bridge.build_got_scheduler = original_build

    def test_run_workflow_finally_resets_budget_state(
        self, tmp_state_dir: Path
    ) -> None:
        """run 结束后 _current_budget_usd = None (防 leak 跨 run)"""
        master = _make_stub_master(tmp_state_dir)
        master.budget_service = MagicMock()
        master._harvest_decision_specs = MagicMock(return_value=[])
        from infra.agent_system import got_bridge
        original_build = got_bridge.build_got_scheduler
        got_bridge.build_got_scheduler = MagicMock(
            return_value=(MagicMock(), MagicMock(node_ids=lambda: [], get_execution=lambda _: None, has_execution=lambda _: False, get_node=MagicMock(return_value=MagicMock(depends_on=[]))))
        )
        try:
            master.run_workflow(workflow_name="test", cost_budget_usd=0.1)
            assert master._current_budget_usd is None  # finally reset
        finally:
            got_bridge.build_got_scheduler = original_build

    def test_old_run_workflow_signature_still_works(
        self, tmp_state_dir: Path
    ) -> None:
        """旧 signature (cost_budget_usd=None) 0 budget_service 不破"""
        master = _make_stub_master(tmp_state_dir)
        master.budget_service = None
        master._harvest_decision_specs = MagicMock(return_value=[])
        from infra.agent_system import got_bridge
        original_build = got_bridge.build_got_scheduler
        got_bridge.build_got_scheduler = MagicMock(
            return_value=(MagicMock(), MagicMock(node_ids=lambda: [], get_execution=lambda _: None, has_execution=lambda _: False, get_node=MagicMock(return_value=MagicMock(depends_on=[]))))
        )
        try:
            master.run_workflow(workflow_name="test", cost_budget_usd=None)
            assert master._current_budget_usd is None
        finally:
            got_bridge.build_got_scheduler = original_build
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
pytest tests/agent_system/test_master_controller_budget.py -v 2>&1 | tail -10
# 期望: 5 FAIL with "AttributeError: 'MasterController' object has no attribute 'budget_service'" (RED 确认)
```

- [ ] **Step 3: Modify `MasterController.__init__` to add `budget_service` kwarg**

```python
# infra/agent_system/master_controller.py:97-127 (modify __init__)
def __init__(
    self,
    state_dir: Optional[str] = None,
    router: Optional[AIRouter] = None,
    config: Optional[MasterControllerConfig] = None,
    cost_tracker: Optional["CostTracker"] = None,
    budget_service: Optional["BudgetService"] = None,  # NEW Phase 8.12
):
    """初始化主控调度器

    Args:
        state_dir: 状态目录 (None = 使用 agent_config 中的默认值)
        router: 显式传入的 AIRouter (None = 从 config 构造)
        config: 显式传入的配置 (None = 从 env vars 加载)
        cost_tracker: 显式传入的 CostTracker (None = 不追踪成本)
        budget_service: Phase 8.12 budget 持久化 (None = 不持久化, 旧 path 0 改)
    """
    # 配置层
    self._config = config or load_default_config(state_dir=state_dir)
    # AI Router
    self._router = router if router is not None else build_router(self._config)
    # 成本追踪 (Phase 8.5)
    self.cost_tracker = cost_tracker
    # Phase 8.12: budget 持久化 (Optional, default None 兜底 旧测试零修改)
    self.budget_service = budget_service
    # Phase 8.8: budget alarm state (跨 run 累积, finally reset)
    self._current_budget_usd: Optional[float] = None
    # ... rest of __init__ unchanged
```

Add import (top of file, near other type imports):
```python
# infra/agent_system/master_controller.py (top imports, after existing TYPE_CHECKING block)
if TYPE_CHECKING:
    from ..ai_service.cost_tracker import CostTracker
    from .budget_persistence import BudgetService  # NEW Phase 8.12
```

- [ ] **Step 4: Modify `run_workflow` to generate run_id + call budget_service.set**

```python
# infra/agent_system/master_controller.py:280-339 (modify run_workflow try/finally)
        # Phase 8.8: store budget before try, so AgentComputeFn 读得到
        self._current_budget_usd = cost_budget_usd
        # Phase 8.12: generate run_id + persist budget (if service wired)
        import uuid
        run_id = uuid.uuid4().hex
        if self.budget_service is not None and cost_budget_usd is not None:
            self.budget_service.set(
                scope="run", usd=cost_budget_usd, run_id=run_id
            )
        try:
            # ... rest of try block unchanged ...
        finally:
            # Phase 8.8: reset for next run
            self._current_budget_usd = None
```

Note: the `import uuid` is local to avoid module-level dependency; consider hoisting to top imports if used elsewhere.

Hoist import to top:
```python
# infra/agent_system/master_controller.py (top imports)
import uuid
```

And remove the local import.

- [ ] **Step 5: Run tests to verify they pass**

```bash
pytest tests/agent_system/test_master_controller_budget.py -v 2>&1 | tail -10
# 期望: 5 passed

pytest tests/agent_system/ -q 2>&1 | tail -5
# 期望: 旧 tests 不破 (budget_service default None 保 backward compat)
```

- [ ] **Step 6: Commit**

```bash
git add infra/agent_system/master_controller.py tests/agent_system/test_master_controller_budget.py
git commit -m "feat(budget): phase 8.12 task 3 — master_controller budget_service injection + run_id uuid4"
```

---

## Task 4: AgentComputeFn 3 档 check + cost_tracker 0 改

**Files:**
- Modify: `infra/agent_system/got_bridge.py:251-329` (`AgentComputeFn.__init__` + `__call__`)
- Test: `tests/agent_system/test_got_bridge_budget.py` (NEW)

- [ ] **Step 1: Write failing tests**

```python
# tests/agent_system/test_got_bridge_budget.py
"""Tests for AgentComputeFn 3-tier budget check (Phase 8.12)"""
from __future__ import annotations

from pathlib import Path
from typing import Any
from unittest.mock import MagicMock

import pytest

from infra.ai_service.cost_tracker import CostBudgetExceeded, CostTracker
from infra.ai_service.model_tiers import ModelTier
from infra.agent_system.got_bridge import AgentComputeFn
from infra.got.data_structures import ThoughtNode, NodeType
from infra.got.scheduler import ComputeResult


def _make_node(scenario: str) -> ThoughtNode:
    return ThoughtNode(
        id="n1", prompt_scenario=scenario, node_type=NodeType.GENERATION
    )


def _make_compute(master: Any, cost_tracker: CostTracker, budget_service: Any) -> AgentComputeFn:
    return AgentComputeFn(
        master=master,
        cost_tracker=cost_tracker,
        budget_service=budget_service,
    )


class TestAgentComputeFnBudget:
    """AgentComputeFn 3-tier budget enforcement"""

    def test_compute_fn_checks_per_run_scope(self, tmp_path: Path) -> None:
        """set per-run budget 0.1, total_cost 0.15 → raise(scope='run')"""
        from infra.agent_system.budget_persistence import BudgetService
        service = BudgetService(db_path=tmp_path / "test.db")
        service.init_db()
        service.set("run", 0.1, run_id="r1")

        master = MagicMock()
        master._current_budget_usd = 0.1
        master.write_chapter_with_usage = MagicMock(
            return_value=({"content": "x"}, {"input_tokens": 100, "output_tokens": 50})
        )

        tracker = CostTracker()
        compute = _make_compute(master, tracker, service)

        with pytest.raises(CostBudgetExceeded) as exc:
            compute(_make_node("chapter_writing"), {"chapter_num": 1})
        assert exc.value.scope == "run"

    def test_compute_fn_checks_per_day_scope(self, tmp_path: Path) -> None:
        """set per-day 0.5, total_cost 0.6 → raise(scope='day')"""
        from infra.agent_system.budget_persistence import BudgetService
        service = BudgetService(db_path=tmp_path / "test.db")
        service.init_db()
        service.set("day", 0.5)

        master = MagicMock()
        master._current_budget_usd = None
        master.write_chapter_with_usage = MagicMock(
            return_value=({"content": "x"}, {"input_tokens": 100, "output_tokens": 50})
        )

        tracker = CostTracker()
        # 喂 cost_tracker record 到 total > 0.6
        tracker.record("chapter_writing", ModelTier.SONNET, 10_000_000, 5_000_000)
        compute = _make_compute(master, tracker, service)

        with pytest.raises(CostBudgetExceeded) as exc:
            compute(_make_node("chapter_writing"), {"chapter_num": 1})
        assert exc.value.scope == "day"

    def test_compute_fn_checks_per_week_scope(self, tmp_path: Path) -> None:
        """set per-week 2.0, total_cost 2.1 → raise(scope='week')"""
        from infra.agent_system.budget_persistence import BudgetService
        service = BudgetService(db_path=tmp_path / "test.db")
        service.init_db()
        service.set("week", 2.0)

        master = MagicMock()
        master._current_budget_usd = None
        master.write_chapter_with_usage = MagicMock(
            return_value=({"content": "x"}, {"input_tokens": 100, "output_tokens": 50})
        )

        tracker = CostTracker()
        # 喂 cost_tracker record 到 total > 2.0
        tracker.record("chapter_writing", ModelTier.SONNET, 100_000_000, 50_000_000)
        compute = _make_compute(master, tracker, service)

        with pytest.raises(CostBudgetExceeded) as exc:
            compute(_make_node("chapter_writing"), {"chapter_num": 1})
        assert exc.value.scope == "week"

    def test_compute_fn_no_budget_service_unchanged(
        self, tmp_path: Path
    ) -> None:
        """budget_service=None, 旧 Phase 8.8 path 0 改 (check_budget 仍工作)"""
        master = MagicMock()
        master._current_budget_usd = 0.0001  # 极小, cost_tracker > 立即超
        master.write_chapter_with_usage = MagicMock(
            return_value=({"content": "x"}, {"input_tokens": 100, "output_tokens": 50})
        )

        tracker = CostTracker()
        tracker.record("chapter_writing", ModelTier.SONNET, 1000, 500)  # > 0.0001
        compute = _make_compute(master, tracker, budget_service=None)

        with pytest.raises(CostBudgetExceeded) as exc:
            compute(_make_node("chapter_writing"), {"chapter_num": 1})
        # Phase 8.8 raise (默认 scope='run')
        assert exc.value.scope == "run"

    def test_compute_fn_default_scope_run_backward_compat(self) -> None:
        """Phase 8.8 raise 不带 scope → 等价 scope='run' (default)"""
        from infra.ai_service.cost_tracker import CostBudgetExceeded
        # 验证 CostBudgetExceeded 不传 scope 时 default = 'run'
        exc = CostBudgetExceeded(used_usd=0.2, budget_usd=0.1, scenario="test")
        assert exc.scope == "run"

    def test_compute_fn_window_expired_does_not_raise(
        self, tmp_path: Path
    ) -> None:
        """set day 昨天 → window 失效 → 不 raise (即使 cost > budget)"""
        from infra.agent_system.budget_persistence import BudgetService
        import sqlite3
        from datetime import datetime, timedelta, timezone

        service = BudgetService(db_path=tmp_path / "test.db")
        service.init_db()
        # 直接插一行 set_at = 昨天
        with sqlite3.connect(str(tmp_path / "test.db")) as conn:
            yesterday = (datetime.now(timezone.utc) - timedelta(days=1)).isoformat()
            conn.execute(
                "INSERT INTO budgets (scope, usd, run_id, set_at) VALUES (?, ?, ?, ?)",
                ("day", 0.0001, None, yesterday),
            )
            conn.commit()

        master = MagicMock()
        master._current_budget_usd = None
        master.write_chapter_with_usage = MagicMock(
            return_value=({"content": "x"}, {"input_tokens": 100, "output_tokens": 50})
        )

        tracker = CostTracker()
        tracker.record("chapter_writing", ModelTier.SONNET, 1000, 500)
        compute = _make_compute(master, tracker, service)

        # 不 raise 即 pass (window 失效)
        result = compute(_make_node("chapter_writing"), {"chapter_num": 1})
        assert result.fail is False
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
pytest tests/agent_system/test_got_bridge_budget.py -v 2>&1 | tail -10
# 期望: 5 FAIL with "TypeError: __init__() got an unexpected keyword argument 'budget_service'" (RED 确认)
```

- [ ] **Step 3: Modify `AgentComputeFn.__init__` to accept `budget_service`**

```python
# infra/agent_system/got_bridge.py:251-257
def __init__(
    self,
    master: MasterController,
    cost_tracker: Optional[CostTracker] = None,
    budget_service: Optional["BudgetService"] = None,  # NEW Phase 8.12
) -> None:
    self._master = master
    self._cost_tracker = cost_tracker
    self._budget_service = budget_service
```

Add type import (top of got_bridge.py):
```python
# infra/agent_system/got_bridge.py (top imports)
from typing import TYPE_CHECKING
if TYPE_CHECKING:
    from .budget_persistence import BudgetService
```

- [ ] **Step 4: Modify `AgentComputeFn.__call__` to call `check_all_scopes` after Phase 8.8 `check_budget`**

```python
# infra/agent_system/got_bridge.py:320-326 (after Phase 8.8 check_budget, before return)
        # === Phase 8.8 NEW: 集中 budget enforcement (1 次,tuple+dict 双路径均覆盖) ===
        if self._cost_tracker is not None:
            self._cost_tracker.check_budget(
                getattr(self._master, "_current_budget_usd", None)
            )
        # === END Phase 8.8 NEW ===

        # === Phase 8.12 NEW: 3-tier budget enforcement (per-run/per-day/per-week) ===
        # 跟 Phase 8.8 同模式: 超 budget 时 raise 让 scheduler 兜底 (node FAILED)
        if self._budget_service is not None:
            current_run_id = getattr(self._master, "_current_run_id", None)
            self._budget_service.check_all_scopes(
                total_cost_usd=self._cost_tracker.total_cost() if self._cost_tracker else 0.0,
                current_run_id=current_run_id,
            )
        # === END Phase 8.12 NEW ===

        return ComputeResult(output=output, cost_tokens=cost_tokens, fail=False)
```

Also update `build_got_scheduler` (line 363-366) to pass `budget_service`:
```python
# infra/agent_system/got_bridge.py:362-366 (modify build_got_scheduler)
    # Phase 8.5: 透传 master.cost_tracker (None 兜底) → AgentComputeFn 写 record
    # Phase 8.12: 透传 master.budget_service (None 兜底) → 3-tier budget check
    cost_tracker = getattr(master, "cost_tracker", None)
    budget_service = getattr(master, "budget_service", None)
    compute = AgentComputeFn(master, cost_tracker=cost_tracker, budget_service=budget_service)
    scheduler = GoTScheduler(graph, compute_fn=compute, max_backtracks=max_backtracks)
    return scheduler, graph
```

Also need to expose `_current_run_id` on master. Add to `run_workflow`:

In Task 3's run_workflow modification, also set `self._current_run_id = run_id` (after the budget_service.set call) and reset in finally:
```python
# infra/agent_system/master_controller.py:280-339 (in run_workflow)
        # Phase 8.12: generate run_id + persist budget + expose to AgentComputeFn
        import uuid
        run_id = uuid.uuid4().hex
        self._current_run_id = run_id  # NEW: AgentComputeFn 读得到
        if self.budget_service is not None and cost_budget_usd is not None:
            self.budget_service.set(
                scope="run", usd=cost_budget_usd, run_id=run_id
            )
        try:
            # ... rest unchanged ...
        finally:
            self._current_budget_usd = None
            self._current_run_id = None  # NEW: 防 leak
```

And initialize in `__init__`:
```python
# infra/agent_system/master_controller.py:127 (after _current_budget_usd)
        self._current_budget_usd: Optional[float] = None
        self._current_run_id: Optional[str] = None  # NEW Phase 8.12
```

- [ ] **Step 5: Run tests to verify they pass**

```bash
pytest tests/agent_system/test_got_bridge_budget.py -v 2>&1 | tail -15
# 期望: 6 passed

pytest tests/agent_system/ -q 2>&1 | tail -5
# 期望: 旧 tests 不破
```

- [ ] **Step 6: Commit**

```bash
git add infra/agent_system/got_bridge.py infra/agent_system/master_controller.py tests/agent_system/test_got_bridge_budget.py
git commit -m "feat(budget): phase 8.12 task 4 — AgentComputeFn 3-tier check + build_got_scheduler passthrough"
```

---

## Task 5: dashboard protocols + app endpoints

**Files:**
- Modify: `dashboard/protocols.py:280-359` (add `_extract_budget_per_window` + 2 字段)
- Modify: `dashboard/app.py:134-149` (add 2 字段 to `WorkflowStatusResponse`) + add 2 endpoints
- Test: `tests/agent_system/test_dashboard_budget_endpoints.py` (NEW)

- [ ] **Step 1: Write failing tests**

```python
# tests/agent_system/test_dashboard_budget_endpoints.py
"""Tests for dashboard budget endpoints (Phase 8.12)

GET /api/budgets → 3 tier dict
PUT /api/budgets/{day,week} → 接受 usd 持久化
"""
from __future__ import annotations

import sqlite3
from pathlib import Path
from typing import Any
from unittest.mock import MagicMock

import pytest
from fastapi.testclient import TestClient


def _make_test_client(tmp_path: Path) -> tuple[TestClient, Any]:
    """构造 TestClient + budget_service 注入"""
    from infra.agent_system.budget_persistence import BudgetService
    from dashboard.app import app
    from dashboard.protocols import MasterControllerAdapter

    service = BudgetService(db_path=tmp_path / "test.db")
    service.init_db()

    controller = MagicMock()
    controller.budget_service = service
    controller.cost_tracker = None
    controller._current_budget_usd = None

    # Inject into adapter singleton
    MasterControllerAdapter._controller = controller

    return TestClient(app), service


class TestDashboardBudgetEndpoints:
    """GET /api/budgets + PUT /api/budgets/{day,week}"""

    def test_get_budgets_returns_3_tiers(self, tmp_path: Path) -> None:
        """GET /api/budgets 返 per_run/per_day/per_week 3 dict"""
        client, service = _make_test_client(tmp_path)
        service.set("day", 0.5)
        service.set("week", 2.0)
        # No per-run set (controller has no current_run_id wired)
        response = client.get("/api/budgets")
        assert response.status_code == 200
        data = response.json()
        assert "per_run" in data
        assert "per_day" in data
        assert "per_week" in data
        # day/week 应有 status
        assert data["per_day"]["budget_usd"] == 0.5
        assert data["per_week"]["budget_usd"] == 2.0

    def test_put_budgets_day_persists(self, tmp_path: Path) -> None:
        """PUT /api/budgets/day usd=0.5 → 后续 GET 返 0.5"""
        client, service = _make_test_client(tmp_path)
        response = client.put("/api/budgets/day", json={"usd": 0.5})
        assert response.status_code == 200
        assert response.json()["ok"] is True
        # Verify persisted in DB
        current = service.get_current("day")
        assert current is not None
        assert current.usd == 0.5

    def test_put_budgets_week_persists(self, tmp_path: Path) -> None:
        """PUT /api/budgets/week usd=2.0 → 后续 GET 返 2.0"""
        client, service = _make_test_client(tmp_path)
        response = client.put("/api/budgets/week", json={"usd": 2.0})
        assert response.status_code == 200
        current = service.get_current("week")
        assert current is not None
        assert current.usd == 2.0

    def test_put_budgets_validates_scope(self, tmp_path: Path) -> None:
        """PUT /api/budgets/month → 400 (invalid scope)"""
        client, _ = _make_test_client(tmp_path)
        response = client.put("/api/budgets/month", json={"usd": 1.0})
        assert response.status_code == 400

    def test_put_budgets_validates_usd_negative(self, tmp_path: Path) -> None:
        """PUT day usd=-1 → 422 (Pydantic ge=0)"""
        client, _ = _make_test_client(tmp_path)
        response = client.put("/api/budgets/day", json={"usd": -1})
        assert response.status_code == 422

    def test_get_budgets_when_none_set_returns_null_per_tier(
        self, tmp_path: Path
    ) -> None:
        """0 budget 全 null/empty, 0 错"""
        client, _ = _make_test_client(tmp_path)
        response = client.get("/api/budgets")
        assert response.status_code == 200
        data = response.json()
        # 3 tier 都 empty/null (无 budget set)
        assert data["per_run"] in ({}, None)
        assert data["per_day"] in ({}, None)
        assert data["per_week"] in ({}, None)
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
pytest tests/agent_system/test_dashboard_budget_endpoints.py -v 2>&1 | tail -10
# 期望: 6 FAIL with "404 Not Found" (endpoints 不存在) (RED 确认)
```

- [ ] **Step 3: Modify `dashboard/protocols.py` to add `_extract_budget_per_window` + 2 字段**

```python
# dashboard/protocols.py (modify WorkflowStatusResponse construction, ~line 280-287)
        return {
            # ... existing fields ...
            "cost_budget_status": _extract_budget_status(self._controller),  # Phase 8.8
            "budget_per_day": _extract_budget_per_window(self._controller, "day"),  # NEW Phase 8.12
            "budget_per_week": _extract_budget_per_window(self._controller, "week"),  # NEW Phase 8.12
            "total_cost_usd": _extract_total_cost(self._controller),
        }


def _extract_budget_per_window(controller: Any, scope: str) -> dict[str, Any]:
    """Phase 8.12: 拿 controller.budget_service 查 current scope + window check,
    算 status (跟 _extract_budget_status 同模式, silent degrade 返 {}).

    Args:
        controller: MasterController 实例
        scope: 'day' or 'week' (per-run 走 _extract_budget_status 旧 path)

    Returns:
        dict with keys (status, budget_usd, used_usd, used_pct) or {} if no budget or budget_service missing
    """
    from datetime import datetime, timedelta, timezone
    try:
        service = getattr(controller, "budget_service", None)
        if service is None:
            return {}
        entry = service.get_current(scope)
        if entry is None:
            return {}
        # Window check (per-day = UTC same date, per-week = Mon-Sun)
        now = datetime.now(timezone.utc)
        if scope == "day":
            in_window = entry.set_at.date() == now.date()
        elif scope == "week":
            days_since_monday = now.weekday()
            week_start = (now - timedelta(days=days_since_monday)).replace(
                hour=0, minute=0, second=0, microsecond=0
            )
            in_window = entry.set_at >= week_start
        else:
            in_window = True
        if not in_window:
            return {}  # window 失效, 视为无 budget
        # 算 used
        cost_tracker = getattr(controller, "cost_tracker", None)
        used = float(cost_tracker.total_cost()) if cost_tracker is not None else 0.0
        used_pct = (used / entry.usd * 100) if entry.usd > 0 else 0.0
        status = "exceeded" if used > entry.usd else "ok"
        return {
            "status": status,
            "budget_usd": entry.usd,
            "used_usd": used,
            "used_pct": used_pct,
        }
    except Exception as exc:
        logger.warning("_extract_budget_per_window failed for scope=%s: %s", scope, exc)
        return {}
```

Also update `WorkflowStatusResponse` in `dashboard/app.py` to add 2 fields (next step).

- [ ] **Step 4: Modify `dashboard/app.py` to add 2 fields + 2 endpoints + 1 Pydantic model**

```python
# dashboard/app.py:134-149 (modify WorkflowStatusResponse)
class WorkflowStatusResponse(BaseModel):
    """工作流状态响应"""
    workflow_name: Optional[str] = None
    is_active: bool = False
    completed: int = 0
    failed: int = 0
    paused: bool = False
    paused_nodes: list[str] = Field(default_factory=list)
    node_count: int = 0
    steps: int = 0
    total_cost_usd: float = 0.0
    pending_decisions: list[dict[str, Any]] = Field(default_factory=list)
    executions: dict[str, str] = Field(default_factory=dict)
    score_data: dict[str, dict[str, Any]] = Field(default_factory=dict)
    cost_by_scenario: dict[str, float] = Field(default_factory=dict)
    cost_budget_status: dict[str, Any] = Field(default_factory=dict)
    # Phase 8.12 NEW: per-day / per-week budget status (per-run 仍走 cost_budget_status 旧 path)
    budget_per_day: dict[str, Any] = Field(default_factory=dict)
    budget_per_week: dict[str, Any] = Field(default_factory=dict)
```

Add `BudgetSetRequest` Pydantic model (after `ResumeWorkflowRequest`, around line 132):
```python
# dashboard/app.py (NEW Pydantic model, after ResumeWorkflowRequest)
class BudgetSetRequest(BaseModel):
    """Phase 8.12: 设置 day/week budget (per-run 不暴露, run 启动时传)"""
    usd: float = Field(ge=0, le=10000)  # 0 表示"无限但仍写行 0"
```

Add 2 endpoints (after existing `/api/workflows/active`, around line 737):
```python
# dashboard/app.py (NEW endpoints after existing @app.get("/api/workflows/active"))
    @app.get("/api/budgets")
    async def get_budgets() -> dict[str, Any]:
        """Phase 8.12: 返 3 档 current budget + status"""
        from dashboard.protocols import (
            _extract_budget_status,
            _extract_budget_per_window,
        )
        controller = MasterControllerAdapter._controller
        return {
            "per_run": _extract_budget_status(controller),
            "per_day": _extract_budget_per_window(controller, "day"),
            "per_week": _extract_budget_per_window(controller, "week"),
        }

    @app.put("/api/budgets/{scope}")
    async def set_budget(scope: str, req: BudgetSetRequest) -> dict[str, Any]:
        """Phase 8.12: 设置 day/week budget (per-run 不暴露)"""
        if scope not in ("day", "week"):
            raise HTTPException(400, "scope must be 'day' or 'week'")
        from infra.agent_system.budget_persistence import BudgetService
        controller = MasterControllerAdapter._controller
        service = getattr(controller, "budget_service", None)
        if service is None:
            raise HTTPException(503, "budget service not initialized")
        service.set(scope, req.usd, run_id=None)
        return {"ok": True, "scope": scope, "usd": req.usd}
```

- [ ] **Step 5: Run tests to verify they pass**

```bash
pytest tests/agent_system/test_dashboard_budget_endpoints.py -v 2>&1 | tail -15
# 期望: 6 passed
```

- [ ] **Step 6: Commit**

```bash
git add dashboard/protocols.py dashboard/app.py tests/agent_system/test_dashboard_budget_endpoints.py
git commit -m "feat(budget): phase 8.12 task 5 — dashboard protocols + 2 endpoints (GET/PUT budgets)"
```

---

## Task 6: SidebarCostBanner priority cascade + 1 ceremonial e2e

**Files:**
- Modify: `dashboard/frontend/src/components/SidebarCostBanner.vue` (+30L, 0 删)
- Test: `tests/dashboard/test_sidebar_banner_priority_cascade.py` (NEW)

- [ ] **Step 1: Write failing ceremonial e2e test**

```javascript
// tests/dashboard/test_sidebar_banner_priority_cascade.js
// Phase 8.12: 验证 Sidebar banner 在 3 档 budget 都设时, 优先显示 exceeded 档 (per-day)
import { test, expect } from '@playwright/test';

test('sidebar banner shows most urgent budget (exceeded priority)', async ({ page }) => {
  // Stub the active workflow status endpoint with 3 budget tiers
  await page.route('**/api/workflows/active/status', async (route) => {
    await route.fulfill({
      status: 200,
      contentType: 'application/json',
      body: JSON.stringify({
        is_active: true,
        completed: 0,
        failed: 0,
        paused: false,
        total_cost_usd: 0.06,
        cost_by_scenario: { chapter_writing: 0.06 },
        cost_budget_status: {
          status: 'ok',
          budget_usd: 0.1,
          used_usd: 0.05,
          used_pct: 50.0,
        },
        budget_per_day: {
          status: 'exceeded',
          budget_usd: 0.05,
          used_usd: 0.06,
          used_pct: 120.0,
        },
        budget_per_week: {
          status: 'ok',
          budget_usd: 0.2,
          used_usd: 0.16,
          used_pct: 80.0,
        },
      }),
    });
  });

  await page.goto('/');  // OverviewPage

  // Sidebar banner 可见
  const banner = page.locator('.sidebar-cost-banner');
  await expect(banner).toBeVisible();

  // Banner 文本含 "今日" (per-day, exceeded 优先 over per-run/per-week)
  await expect(banner).toContainText('今日');

  // Per-run "本次" / per-week "本周" label 不显示 (active 只 1 个)
  await expect(banner).not.toContainText('本次');
  await expect(banner).not.toContainText('本周');

  // Progress bar 100% (exceeded 红色 fill, clip 在 100%)
  const fill = banner.locator('.progress-bar-fill');
  const widthStyle = await fill.getAttribute('style');
  expect(widthStyle).toContain('width: 100%');

  // Fill class 是 exceeded
  const fillClass = await fill.getAttribute('class');
  expect(fillClass).toContain('exceeded');
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cd dashboard/frontend && pnpm playwright test tests/dashboard/test_sidebar_banner_priority_cascade.spec.js
# 期望: FAIL — banner text 不含 "今日" (current impl 只显 per-run) (RED 确认)
```

- [ ] **Step 3: Modify `SidebarCostBanner.vue` to add priority cascade + 3 labels**

```vue
<!-- dashboard/frontend/src/components/SidebarCostBanner.vue -->
<!-- Phase 8.12: 改 priority cascade (3 档 budget: per-run/per-day/per-week) -->
<template>
  <div
    v-if="hasCost"
    class="sidebar-cost-banner"
    role="region"
    aria-label="成本追踪"
  >
    <div class="sidebar-cost-row" role="status" aria-live="polite">
      <span class="sidebar-cost-total-text">
        <span class="sidebar-cost-icon">💰</span>${{ totalText }}
      </span>
    </div>
    <div
      v-if="activeBudget"
      class="sidebar-cost-row"
      role="status"
      aria-live="polite"
    >
      <span class="sidebar-cost-budget-text">
        {{ activeBudget.label }}预算: ${{ activeUsedText }} / ${{ activeBudgetText }} ({{ activePctText }}%)
      </span>
    </div>
    <div
      v-if="activeBudget"
      class="progress-bar"
      role="progressbar"
      :aria-valuenow="Math.round(activePct)"
      aria-valuemin="0"
      aria-valuemax="100"
      :aria-label="`${activeBudget.label}预算状态: 已用 ${activeUsedText} 美元 / 预算 ${activeBudgetText} 美元 (${activePctText}%)`"
    >
      <div
        class="progress-bar-fill"
        :class="activeBudget.status === 'exceeded' ? 'exceeded' : 'ok'"
        :style="{ width: activePct + '%' }"
      ></div>
    </div>
  </div>
</template>

<script setup>
import { computed } from 'vue';

const props = defineProps({
  status: {
    type: Object,
    required: true,
  },
});

const costByScenario = computed(() => props.status?.cost_by_scenario || {});
const hasCost = computed(() => {
  const data = costByScenario.value;
  return Object.keys(data).length > 0 && Object.values(data).some((v) => v > 0);
});

const totalUsd = computed(() => Number(props.status?.total_cost_usd ?? 0));
const totalText = computed(() => totalUsd.value.toFixed(4));

// Phase 8.12 NEW: 3 档 budget 收集 + label
const allBudgets = computed(() => {
  const list = [];
  const perRun = props.status?.cost_budget_status;
  if (perRun && perRun.budget_usd != null) {
    list.push({ ...perRun, scope: 'run', label: '本次' });
  }
  const perDay = props.status?.budget_per_day;
  if (perDay && perDay.budget_usd != null) {
    list.push({ ...perDay, scope: 'day', label: '今日' });
  }
  const perWeek = props.status?.budget_per_week;
  if (perWeek && perWeek.budget_usd != null) {
    list.push({ ...perWeek, scope: 'week', label: '本周' });
  }
  return list;
});

// Phase 8.12 NEW: priority cascade — exceeded 优先, 同状态按 used_pct desc
const activeBudget = computed(() => {
  if (allBudgets.value.length === 0) return null;
  return [...allBudgets.value].sort((a, b) => {
    if (a.status === 'exceeded' && b.status !== 'exceeded') return -1;
    if (b.status === 'exceeded' && a.status !== 'exceeded') return 1;
    return b.used_pct - a.used_pct;
  })[0];
});

const activePct = computed(() => {
  if (!activeBudget.value) return 0;
  return Math.max(0, Math.min(100, Number(activeBudget.value.used_pct ?? 0)));
});
const activeUsedText = computed(() =>
  activeBudget.value ? Number(activeBudget.value.used_usd ?? 0).toFixed(4) : '0.0000'
);
const activeBudgetText = computed(() =>
  activeBudget.value ? Number(activeBudget.value.budget_usd ?? 0).toFixed(4) : '0.0000'
);
const activePctText = computed(() => activePct.value.toFixed(1));
</script>

<!-- styles unchanged (no CSS modifications) -->
```

- [ ] **Step 4: Run e2e test to verify it passes**

```bash
cd dashboard/frontend && pnpm playwright test tests/dashboard/test_sidebar_banner_priority_cascade.spec.js
# 期望: 1 passed
```

- [ ] **Step 5: Verify build + ruff**

```bash
cd dashboard/frontend && pnpm build
# 期望: 0 error

ruff check dashboard/ tests/agent_system/ infra/agent_system/ 2>&1 | tail -3
# 期望: 0 issues
```

- [ ] **Step 6: Commit**

```bash
git add dashboard/frontend/src/components/SidebarCostBanner.vue tests/dashboard/test_sidebar_banner_priority_cascade.spec.js
git commit -m "feat(budget): phase 8.12 task 6 — SidebarCostBanner priority cascade + 1 e2e"
```

---

## Task 7: Final verification + 1 feat commit

- [ ] **Step 1: Run full pytest suite**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pytest -q 2>&1 | tail -5
# 期望: 2203 passed, 0 failed, 27 skipped (2168 baseline + 35 new tests)
```

Breakdown: 12 (test_budget_persistence) + 5 (test_master_controller_budget) + 6 (test_got_bridge_budget) + 6 (test_dashboard_budget_endpoints) + 6 (test_dashboard_budget_endpoints include 2 PUT + GET validation) = 35 new tests.

Wait: test_dashboard_budget_endpoints has 6 tests in spec (test_get_budgets_returns_3_tiers + test_put_budgets_day_persists + test_put_budgets_week_persists + test_put_budgets_validates_scope + test_put_budgets_validates_usd_negative + test_get_budgets_when_none_set_returns_null_per_tier). Recount: 12+5+6+6 = 29 new pytest tests. Plus 1 e2e = 30 total new.

Recompute baseline: 2168 + 29 = 2197 (not 2203). Let me correct the plan.

Actually the spec said 2203 which would be 35 new. Let me check: 
- 12 budget_persistence
- 5 master_controller_budget
- 6 got_bridge_budget
- 5 dashboard_budget_endpoints
- 1 e2e
= 29 total

But spec says 35. The spec listed 5 dashboard tests but the implementation here has 6 (split day/week PUT into 2 tests). Let me count what spec says:
- test_get_budgets_returns_3_tiers
- test_put_budgets_day_persists
- test_put_budgets_week_validates_scope
- test_put_budgets_validates_usd_negative
- test_get_budgets_when_none_set_returns_null_per_tier
= 5 in spec, but in my plan I have 6 (added test_put_budgets_week_persists as well). So either 5 or 6 dashboard tests is fine.

Total: 12 + 5 + 6 + 6 = 29 new pytest tests, + 1 e2e = 30 total new. Baseline: 2168 + 29 = 2197.

Or: 12 + 5 + 6 + 5 = 28 new pytest tests, + 1 e2e = 29 total. Baseline: 2168 + 28 = 2196.

Spec is slightly off (35 vs 29 actual). I'll target 28-29 new tests and note the actual count in commit. Update the DoD and verification commands.

```bash
# 期望: 2196-2197 passed, 0 failed, 27 skipped (2168 baseline + 28-29 new)
```

- [ ] **Step 2: Verify no regressions in old tests**

```bash
pytest tests/agent_system/test_cost_persistence.py \
       tests/agent_system/test_cost_persistence.py \
       tests/agent_system/test_got_bridge.py \
       tests/agent_system/test_master_controller_workflow.py \
       tests/agent_system/test_decision_integration.py -q
# 期望: 全部 passed (Phase 8.5/8.6/8.6.1/8.6.2/8.7/8.8/8.10/8.11 baseline 0 改)
```

- [ ] **Step 3: ruff**

```bash
ruff check infra/agent_system/ dashboard/ tests/agent_system/ 2>&1 | tail -3
# 期望: 0 issues
```

- [ ] **Step 4: front-end build**

```bash
cd dashboard/frontend && pnpm build
# 期望: 0 error
```

- [ ] **Step 5: grep verify**

```bash
grep -rn "BudgetService\|budget_service" infra/agent_system/ --include="*.py" | grep -v test_
# 期望: 
#   infra/agent_system/budget_persistence.py (def)
#   infra/agent_system/master_controller.py (kwarg + set call)
#   infra/agent_system/got_bridge.py (build_got_scheduler passthrough)
```

- [ ] **Step 6: git status clean (只 + 8 files tracked)**

```bash
git status
# 期望:
#   modified:   infra/ai_service/cost_tracker.py
#   modified:   infra/agent_system/master_controller.py
#   modified:   infra/agent_system/got_bridge.py
#   modified:   dashboard/protocols.py
#   modified:   dashboard/app.py
#   modified:   dashboard/frontend/src/components/SidebarCostBanner.vue
#   new file:   infra/agent_system/budget_persistence.py
#   new file:   tests/agent_system/test_budget_persistence.py
#   new file:   tests/agent_system/test_master_controller_budget.py
#   new file:   tests/agent_system/test_got_bridge_budget.py
#   new file:   tests/agent_system/test_dashboard_budget_endpoints.py
#   new file:   tests/dashboard/test_sidebar_banner_priority_cascade.spec.js
```

- [ ] **Step 7: 1 feat commit summarizing all 6 tasks**

```bash
git add infra/ai_service/cost_tracker.py \
        infra/agent_system/master_controller.py \
        infra/agent_system/got_bridge.py \
        infra/agent_system/budget_persistence.py \
        dashboard/protocols.py \
        dashboard/app.py \
        dashboard/frontend/src/components/SidebarCostBanner.vue \
        tests/agent_system/test_budget_persistence.py \
        tests/agent_system/test_master_controller_budget.py \
        tests/agent_system/test_got_bridge_budget.py \
        tests/agent_system/test_dashboard_budget_endpoints.py \
        tests/dashboard/test_sidebar_banner_priority_cascade.spec.js

git commit -m "feat(budget): phase 8.12 — budget DB persistence (per-run/day/week) + sidebar cascade

Phase 8.8 budget 在内存 (per-run only),server restart 即丢,无 per-day/
per-week 跨 run 累积预算. 本期收尾: BudgetService 新类 (append-only budgets
表, 跟 cost_records 共存 cost_tracker.db, gitignored) + CostBudgetExceeded
.scope field (default 'run' 保 backward compat) + AgentComputeFn 3 档
check_all_scopes + master_controller budget_service kwarg + run_id uuid4
hex + SidebarCostBanner priority cascade (exceeded 优先) + 2 dashboard
endpoints (GET/PUT /api/budgets).

改动:
- infra/agent_system/budget_persistence.py (NEW, 150L): BudgetService 类
  (init_db / set / get_current / check_all_scopes / list_runs, 2 索引
  scope+set_at / run_id, _is_within_window Python-side UTC Calendar check)
- infra/ai_service/cost_tracker.py (+10L): CostBudgetExceeded.scope: str = 'run'
  field (default 'run' 保 Phase 8.8/8.9/8.10 tests 0 改)
- infra/agent_system/master_controller.py (+20L): budget_service: Optional
  kwarg + uuid.uuid4().hex run_id gen + budget_service.set(scope='run', usd=...,
  run_id=...) call after _current_budget_usd set + _current_run_id 暴露
  给 AgentComputeFn + finally reset 防 leak 跨 run
- infra/agent_system/got_bridge.py (+15L): AgentComputeFn.__init__ 加
  budget_service kwarg + __call__ 加 check_all_scopes(total_cost_usd=...,
  current_run_id=...) call after Phase 8.8 check_budget (双路径) +
  build_got_scheduler 透传 budget_service (None 兜底保 baseline)
- dashboard/protocols.py (+30L): _extract_budget_per_window(scope) helper
  (mirror _extract_budget_status silent degrade 返 {}) + 2 字段
  budget_per_day / budget_per_week in WorkflowStatusResponse
- dashboard/app.py (+50L): WorkflowStatusResponse 加 2 字段 +
  BudgetSetRequest Pydantic (usd ge=0 le=10000) + GET /api/budgets
  endpoint (返 3 tier) + PUT /api/budgets/{day,week} endpoint (校验 scope
  in {day,week} + 503 if budget_service None)
- dashboard/frontend/src/components/SidebarCostBanner.vue (+30L, 0 删):
  allBudgets computed (3 档收集 + label 本次/今日/本周) + activeBudget
  sort (exceeded 优先, 同状态按 used_pct desc) + 6 computed 替换 4
  旧 computed (activePct / activeUsedText / activeBudgetText / activePctText)

- 28-29 new pytest tests (12 BudgetService unit + 5 master_controller
  budget_service inject + 6 got_bridge 3-tier check + 6 dashboard
  endpoints) + 1 ceremonial Playwright e2e (priority cascade with
  exceeded priority over ok)

不改:
- 0 production code 行为破 (CostBudgetExceeded.scope default 'run' 保
  Phase 8.8/8.9/8.10 tests; budget_service Optional default None 保
  master_controller / got_bridge 双路径 baseline)
- 0 改 cost_records 表 (Phase 8.5 路径 0 动, 新 budgets 表并存)
- 0 改 cost_tracker.py 内存逻辑 (record/total_cost 0 动)
- 0 改 ws.py / useWorkflowSocket / CostBarChart / WorkflowStatus / App.vue
  (Phase 6.4/8.7/8.11 路径 0 动)
- 0 改 12 SCENARIOS 路由
- 0 改 8 historical spec/plan docs (:NNN 行号不动)
- 0 改 CLAUDE.md / pyproject.toml / pytest.ini

Baseline:
- 2168 → 2196-2197 passed (28-29 new tests, 0 production code 行为破)
- ruff 0, vite build 0 error, e2e 1 passed (验)
- MEMORY.md 173 lines (under 200 soft limit, 0 动)

后续:
- Phase 8.13: per-tier budget (cost_by_tier 字段)
- Phase 8.14: soft warning 三态 (80/95/100 三色 banner)
- Phase 8.15: CI integration for opt-in tests
- Phase 9: 跨卷涟漪
- Future: DELETE endpoint / timestamp index / run_id↔cost_records /
  WebSocket 推送 / 历史 cost 时间序列图 / Dashboard 编辑 UI /
  workflow.db 漏 .gitignore (独立 followup)"

git push origin master
```

---

## Verification (主公 review commit 后)

```bash
# 1. pytest baseline + new tests
pytest -q 2>&1 | tail -3
# 期望: 2196-2197 passed, 0 failed, 27 skipped

# 2. ruff 0
ruff check infra/agent_system/ dashboard/ tests/agent_system/ 2>&1 | tail -3
# 期望: 0 issues

# 3. front-end build
cd dashboard/frontend && pnpm build
# 期望: 0 error

# 4. ceremonial e2e
cd dashboard/frontend && pnpm playwright test tests/dashboard/test_sidebar_banner_priority_cascade.spec.js
# 期望: 1 passed

# 5. 旧 tests 不破
pytest tests/agent_system/test_cost_persistence.py \
       tests/agent_system/test_got_bridge.py \
       tests/agent_system/test_master_controller_workflow.py \
       tests/agent_system/test_decision_integration.py -q
# 期望: 全部 passed

# 6. git log verify
git log --oneline -1
# 期望: feat(budget): phase 8.12 — budget DB persistence (per-run/day/week) + sidebar cascade

# 7. git status clean
git status
# 期望: 12 files (6 modified + 6 new)
```

---

## 风险 + 缓解

| 风险 | 缓解 |
|------|------|
| 旧 2168 测试破 (Phase 8.8/8.9/8.10 CostBudgetExceeded) | scope default 'run' 兜底, 旧 test 用 `assertRaises` 不依赖 str match |
| `_is_within_window` UTC 时区错 | Python-side `datetime.now(timezone.utc)` 跟 Phase 8.5 cost_records 一致 |
| `budgets` 表跟 `cost_records` 同一 DB 文件 | 复用 infra/.state/cost_tracker.db (Phase 8.5 路径, gitignored), 单一 file ops 简化 |
| run_id uuid4 hex 格式 | `re.match(r"^[0-9a-f]{32}$", ...)` test 1 验证 |
| AgentComputeFn 调用 budget_service 时 _current_run_id 未设 | master_controller 在 set(scope='run', ...) 之前 `self._current_run_id = run_id` |
| Sidebar 3 档挤 200px | activeBudget 单 label + 1 progress bar, 跟 Phase 8.11 3 行 layout 一致 |
| Priority cascade 测试用 mock 而非真实 service | e2e stub `/api/workflows/active/status` 返 3 tier, 跟 Phase 8.11 ceremonial 模式一致 |
| `_extract_budget_per_window` 跟 _extract_budget_status 重复 | helper 抽 is_within_window 逻辑, 但 status 计算共用 (DRY 通过 refactor not extraction, 避免 over-abstraction) |
| `_harvest_decision_specs` mock 在 test_got_bridge_budget | master mock 直接 bypass run_workflow, 走 AgentComputeFn 直接 |
| dashboard endpoints 测试 注入 MasterControllerAdapter._controller | 测试 fixture 用 monkeypatch + getfixture, 跟 Phase 8.7/8.8 模式一致 |
| 28-29 new tests + 6 new module/test | 跟 Phase 8.6/8.6.1/8.6.2 比略大但合理 (3 档 enforcement + API + e2e 3 区域) |
| 1 旧 handler (test_got_bridge.py) 用 MasterController.__new__ stub | Phase 8.8/8.10 已用同样 stub, 0 改 |

---

## 完成定义 (DoD)

- [ ] `infra/agent_system/budget_persistence.py` 创建 (~150L)
- [ ] `infra/ai_service/cost_tracker.py` 改 `CostBudgetExceeded.scope: str = "run"` (+10L)
- [ ] `infra/agent_system/master_controller.py` 改 `budget_service` kwarg + `run_id = uuid.uuid4().hex` + `set(scope='run', usd=..., run_id=...)` call + `_current_run_id` 暴露 (+20L)
- [ ] `infra/agent_system/got_bridge.py` 改 `AgentComputeFn.__init__/.__call__` + `build_got_scheduler` passthrough (+15L)
- [ ] `dashboard/protocols.py` 改 `_extract_budget_per_window` + 2 字段 (+30L)
- [ ] `dashboard/app.py` 改 `WorkflowStatusResponse` + `BudgetSetRequest` + 2 endpoints (+50L)
- [ ] `dashboard/frontend/src/components/SidebarCostBanner.vue` 改 priority cascade + 3 label (+30L)
- [ ] `tests/agent_system/test_budget_persistence.py` 创建 (12 tests, ~250L)
- [ ] `tests/agent_system/test_master_controller_budget.py` 创建 (5 tests, ~150L)
- [ ] `tests/agent_system/test_got_bridge_budget.py` 创建 (6 tests, ~120L)
- [ ] `tests/agent_system/test_dashboard_budget_endpoints.py` 创建 (6 tests, ~150L)
- [ ] `tests/dashboard/test_sidebar_banner_priority_cascade.spec.js` 创建 (1 test, ~80L)
- [ ] 1 feat commit (含 6 task commits) + 1 squash 总览 commit pushed to origin/master
- [ ] pytest 2196-2197 passed, 0 failed, 27 skipped (2168 + 28-29 new)
- [ ] ruff 0 / vite build 0 error / e2e 1 passed
- [ ] 0 改 `cost_records` 表 / 0 改 `cost_tracker.py` 内存逻辑 / 0 改 `useWorkflowSocket.js` / 0 改 Phase 8.5/8.7/8.8/8.10/8.11 既有 components
- [ ] 0 改 8 historical spec/plan docs (`:NNN` 行号不动)
- [ ] 0 改 CLAUDE.md / pyproject.toml / pytest.ini
- [ ] 0 误改 .env / 0 API key 泄漏
- [ ] 0 infra/.state/* 提交 (gitignored)
- [ ] 0 force-push, 0 --no-verify, 0 Co-Authored-By footer

---

## 后续 phases (out of scope)

- **Phase 8.13**: Per-tier budget (cost_by_tier 字段暴露, HAIKU/SONNET/OPUS 各档)
- **Phase 8.14**: Soft warning 三态 (80% 黄 / 95% 橙 / 100% 红, banner UI 渐变)
- **Phase 8.15**: CI integration for opt-in tests (`pytest -m budget_db`)
- **Phase 9**: 跨卷涟漪 (独立大方向)
- **Future**: DELETE /api/budgets/{day,week} 端点 (YAGNI, 主公提需求时)
- **Future**: cost_records.timestamp 索引 (prod 数据 > 100K 行时)
- **Future**: run_id 关联到 cost_records 表 (audit log 深度分析)
- **Future**: WebSocket 推送 budget 状态 (实时 banner, 留 Phase 9)
- **Future**: 历史 cost 时间序列图 (Phase 8.13+)
- **Future**: Dashboard 编辑 budget 弹窗 (Vue form, API-only 先就位)
- **Future**: pre-existing `infra/.state/workflow.db` 漏 `.gitignore` (独立 followup)
