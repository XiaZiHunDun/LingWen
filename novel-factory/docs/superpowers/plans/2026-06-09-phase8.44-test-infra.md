# Phase 8.44 Test Infrastructure — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 5 test infra followup items 收尾: vitest --coverage v8 + threshold 70/60 + Codecov frontend + Codecov backend (2 flags 合并报) + lint-staged 接入 + pre-commit backend pytest smoke. 6 sub-phases / **5 commits 推送 origin/master** (Task 1-5 各 1 commit, Task 6 = push + memory sync 0 commit 因为 phases-8-dashboard.md 在 `~/.claude/...` 不在 git repo). 0 --no-verify (5 commit 全 hook ✅). **Plan deviation**: 6 sub-phases 实际只产 5 commits (8.44.1-8.44.5), Task 6 = push + memory sync 0 commit.

**Architecture:** 跟 Phase 8.35-8.43 super-phase 同模式 (1 spec + 1 plan + 5 sub-phase impl + 1 push + memory sync), 低 risk 先 (8.44.1 vitest coverage) / 4 medium risk (8.44.2 lint-staged / 8.44.3 pre-commit pytest / 8.44.4 frontend Codecov / 8.44.5 backend Codecov). 1 个 `.lintstagedrc.json` + 1 个 `codecov.yml` 跨 vitest+pytest 2 flags 合并报.

**Tech Stack:** vitest 4.1.8 (维持) / @vitest/coverage-v8 4.1.8 (NEW) / lint-staged 15.x (NEW) / eslint 10.4.1 (Phase 8.43 维持) / codecov/codecov-action@v5 (NEW) / Node 20.20 / pnpm 9.15.0 (跟 Phase 8.40 corepack 兼容).

**Spec:** `docs/superpowers/specs/2026-06-09-phase8.44-test-infra-design.md` (commit `a5957ba`)

---

## Context

Phase 8.35-8.43 收尾 dashboard polish + build tooling upgrade 累计 5+ super-phase followup items 跨 test infra / UI / TS strict 4 子系统, 拆 4 super-phase 推进. Phase 8.43 完成 Super-phase A (build tooling 4 devDep bump + 1 flat config rewrite + 2 config-only commits), 剩 test infra 5 followup items 启动 Super-phase B (Phase 8.44).

**Verified baselines (2026-06-09, master @ `a5957ba` post-Phase 8.44 spec commit)**:
- `pnpm vitest run` → 16 spec files / 95 tests passed
- `pnpm vitest run --coverage` → ❌ NOT enabled (vitest.config.js 0 coverage provider)
- `pnpm build` → 0 error
- `pnpm lint:all` → EXIT=0
- `pytest -q` (backend) → 2262 passed, 0 failed, 27 skipped
- `ruff check .` → 0 issues
- `data-testid` 数量 → 46
- `coverage/` directory → ❌ NOT exists
- `.lintstagedrc.json` → ❌ NOT exists
- `lint-staged` npm dep → ❌ NOT installed
- `@vitest/coverage-v8` npm dep → ❌ NOT installed
- `.husky/pre-commit` → ✅ runs `pnpm lint:all` (frontend only, 0 pytest)
- `codecov.yml` → ❌ NOT exists (root)
- `dashboard-frontend-ci.yml` → ✅ lint + vitest + build, 0 coverage
- `test.yml` (backend) → ✅ pytest 3.11/3.12/3.13 + --cov-fail-under=30 + upload .coverage artifact, 0 Codecov
- corepack `pnpm@9.15.0` → OK (workaround Phase 8.40 Node 20.20 兼容)
- 0 Co-Authored-By trailers in recent commits (跟 CLAUDE.md global config 一致)

---

## File Map

### Created
- `dashboard/frontend/.lintstagedrc.json` (~20L, NEW, 8.44.2) — 2 runner (eslint + vitest related)
- `codecov.yml` (~30L, NEW, 8.44.4) — 跨 frontend+backend 2 flags 合并报
- `docs/superpowers/plans/2026-06-09-phase8.44-test-infra.md` (本 plan)

### Modified
- `dashboard/frontend/package.json` (2 devDep bump: @vitest/coverage-v8 + lint-staged, 8.44.1+8.44.2)
- `dashboard/frontend/pnpm-lock.yaml` (lock file 自动跟 pnpm install 更新, 8.44.1+8.44.2)
- `dashboard/frontend/vitest.config.js` (+ `coverage` config block, 8.44.1)
- `dashboard/frontend/.gitignore` (+ `coverage/` entry, 8.44.1)
- `dashboard/frontend/.husky/pre-commit` (改 `pnpm lint:all` → `pnpm lint-staged` + 加 pytest smoke 段, 8.44.2+8.44.3)
- `dashboard/frontend/package.json` (改 `lint:staged` script, 1 line, 8.44.2)
- `.github/workflows/dashboard-frontend-ci.yml` (+1 codecov step, 0 改主体, 8.44.4)
- `.github/workflows/test.yml` (+1 codecov step + xml report, 0 改主体, 8.44.5)
- `~/.claude/projects/.../memory/phases-8-dashboard.md` (H1 + L4 edit + 8.44 entry append, 8.44.6)

### NOT modified
- 14 .vue in `dashboard/frontend/src/`
- 16 vitest spec in `dashboard/frontend/tests/unit/`
- 5 composables / `api/index.js` / `eslint-rules/*` / `index.html` / `vite.config.js`
- `.github/workflows/real-llm-tests.yml` (real LLM opt-in, 0 改)
- backend Python (infra/agent_system / dashboard backend / cost_tracker / cost_persistence / cost_router)
- 9 historical spec/plan docs (`:NNN` 行号不动)
- 12 SCENARIOS 路由 (Python side)
- CLAUDE.md / pyproject.toml / pytest.ini
- `dashboard/frontend/.husky/_/h` (husky helper, 0 改)
- `.state/*.db` (gitignored) / `.env` (0 改 0 泄漏)

---

## Pre-Execution Setup

每个 task 都用同 pattern:
1. 读 baseline state (确认 before snapshot)
2. 改文件 (specific code 在 step 里)
3. 装新 devDep (`corepack pnpm@9.15.0 install`)
4. 跑 verification (commands + expected output)
5. Commit (`feat`/`chore`/`docs` conventional, 0 Co-Authored-By)

**TDD note**: 5 test infra followup 不引新 feature, TDD 走 1 swap + smoke test pattern (改 config, 跑现有 16 spec / 95 tests 确认 0 break). 8.44.4 + 8.44.5 CI 改 0 改 frontend/backend 代码, 0 引新 test.

**Critical safety** (跟 Phase 8.35-8.43 policy 一致):
- 0 force-push
- 0 amend (除 sub-phase fail 救火显式声明)
- 0 Co-Authored-By footer (跟 global CLAUDE.md config)
- 0 改 .env / 0 API key 泄漏
- 0 改 .state/*.db (gitignored)
- 0 改 historical spec/plan docs `:NNN` 行号
- 0 改 12 SCENARIOS 路由
- 0 改 CLAUDE.md / pyproject.toml / pytest.ini
- 0 改 16 vitest spec / 0 改 14 .vue / 0 改 backend Python
- 0 改 `.github/workflows/real-llm-tests.yml`

---

## Task 1: 8.44.1 — vitest --coverage v8 + threshold 70/60 (low risk, hook ✅)

**Files:**
- Modify: `dashboard/frontend/package.json:23-36` (+ 1 devDep `@vitest/coverage-v8`)
- Modify: `dashboard/frontend/vitest.config.js:10-23` (+ `coverage` config block)
- Modify: `dashboard/frontend/.gitignore` (+ `coverage/` entry)
- Modify: `dashboard/frontend/pnpm-lock.yaml` (auto by pnpm install)

- [ ] **Step 1: 验证 pre-state**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
grep '"vitest":' package.json
grep -E '"@vitest/coverage' package.json
cat vitest.config.js
ls -la coverage 2>&1
grep -E "coverage" .gitignore 2>&1
# 期望: "vitest": "^4.1.8" + 0 @vitest/coverage + vitest.config.js 0 coverage block + coverage/ NOT exists + .gitignore 0 coverage entry
git status
# 期望: clean (working tree 0 uncommitted changes)
```

- [ ] **Step 2: Bump @vitest/coverage-v8 to ^4.1.8**

Edit `dashboard/frontend/package.json`, 在 devDependencies `vitest` 之后加:

```diff
     "vitest": "^4.1.8",
+    "@vitest/coverage-v8": "^4.1.8",
```

(跟 vitest 版本对齐, v8 provider 用 v8 native 跟 V8 inline cache 集成, 估 2MB install size)

- [ ] **Step 3: Install (跟 Phase 8.40 corepack pattern)**

```bash
corepack pnpm@9.15.0 install
```

Expected: `Done in N.Ns` + 0 errors. `pnpm-lock.yaml` auto-updated.

- [ ] **Step 4: Edit vitest.config.js — 加 coverage config block**

Edit `dashboard/frontend/vitest.config.js`, replace `defineConfig({` block (L10-23) with:

```js
export default mergeConfig(viteConfig, defineConfig({
  test: {
    environment: 'jsdom',
    globals: true,
    // Phase 8.30: tests/unit/setup.ts 集中 stub echarts 等 jsdom-incompatible 模块
    setupFiles: ['./tests/unit/setup.ts'],
    include: ['src/**/*.spec.{js,ts}', 'tests/unit/**/*.spec.{js,ts}'],
    // Phase 8.30: ceremonial Playwright specs (.spec.js) 仍保留作契约文档,
    // 但走 vitest 跑的是 tests/unit/ + src/ 下的真 e2e tests
    exclude: ['node_modules', 'dist', 'tests/e2e/**', 'coverage/**'],
    // Phase 8.43.2: pool: 'forks' jsdom fork-isolate 防 OOM (跟 Phase 6.4 pytest
    // forks 镜像, 多 spec 跑不串). coverage/** exclude 给 Phase 8.44+ 留占位.
    pool: 'forks',
    // Phase 8.44.1: v8 coverage provider, threshold 70/60, 跟 backend --cov-fail-under=30
    // 不对齐 (backend 历史债大, 后期慢慢提), 0 改 16 spec (coverage 是观察者).
    // reporter text/lcov/html 三档, lcov 给 Codecov (Phase 8.44.4), html 给本地 debug.
    coverage: {
      provider: 'v8',
      reporter: ['text', 'lcov', 'html'],
      include: ['src/**/*.{js,ts,vue}'],
      exclude: ['src/**/*.spec.{js,ts}', 'src/main.ts', 'src/**/index.ts'],
      thresholds: { lines: 70, branches: 60, functions: 70, statements: 70 },
    },
  },
}))
```

- [ ] **Step 5: Edit .gitignore — 加 coverage/ entry**

Edit `dashboard/frontend/.gitignore`, append (或 create 如果 0 存在):

```gitignore
# Phase 8.44.1: vitest coverage output (gitignored, CI artifact only)
coverage/
```

(create if not exists: `touch .gitignore` + write single line)

- [ ] **Step 6: 验证 vitest coverage 跑通 + threshold 0 fail**

```bash
corepack pnpm@9.15.0 vitest run --coverage 2>&1 | tail -20
# 期望: Coverage report (text format)
#   Lines: ~75-85% (实测估)
#   Branches: ~65-75%
#   Functions: ~75-85%
#   Statements: ~75-85%
#   Threshold 0 fail (lines >= 70, branches >= 60, etc.)
# 期望: coverage/lcov.info + coverage/index.html 生成
ls -la coverage/ 2>&1 | head -5
# 期望: lcov.info + index.html + tmp/ 等文件
```

- [ ] **Step 7: 验证 baseline 维持 (vitest 0 coverage / build / lint)**

```bash
corepack pnpm@9.15.0 vitest run 2>&1 | tail -3
# 期望: Test Files 16 passed (16) / Tests 95 passed (95) / 0 failed
corepack pnpm@9.15.0 build 2>&1 | tail -3
# 期望: ✓ built in N.NNs
corepack pnpm@9.15.0 lint:all
echo "EXIT=$?"
# 期望: EXIT=0
```

- [ ] **Step 8: 验证 0 改 .vue / 0 改 16 spec**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git diff --stat dashboard/frontend/src/ dashboard/frontend/tests/unit/
# 期望: 0 files changed (除 vitest.config.js / package.json / pnpm-lock.yaml / .gitignore)
```

- [ ] **Step 9: Commit**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git add dashboard/frontend/package.json dashboard/frontend/pnpm-lock.yaml dashboard/frontend/vitest.config.js dashboard/frontend/.gitignore
git commit -m "chore(coverage): phase 8.44.1 — vitest --coverage v8 + threshold 70/60

@vitest/coverage-v8@^4.1.8 devDep (跟 vitest 4.1.8 同步, v8 原生快). vitest.config.js
加 coverage block: provider v8 + reporter text/lcov/html + include src/**/*.{js,ts,vue} +
exclude spec/main.ts/index.ts + thresholds { lines: 70, branches: 60, functions: 70,
statements: 70 }. 0 改 16 spec (coverage 是观察者, 0 改 mount API).

vitest 16 spec / 95 tests passed 维持, pnpm build 0 error, pnpm lint:all EXIT=0.
实测估 coverage ~75-85% lines (16 spec 95 tests 95+ 覆盖). lcov.info 给
8.44.4 Codecov frontend upload. coverage/ gitignored (CI artifact only).

下一步 8.44.2 lint-staged 接入 (改 pre-commit hook 走 staged-only)."
```

---

## Task 2: 8.44.2 — lint-staged 接入 + pre-commit 改 staged-only (medium risk, hook ✅)

**Files:**
- Modify: `dashboard/frontend/package.json:23-36` (+ 1 devDep `lint-staged`)
- Modify: `dashboard/frontend/pnpm-lock.yaml` (auto by pnpm install)
- Create: `dashboard/frontend/.lintstagedrc.json` (~20L, NEW)
- Modify: `dashboard/frontend/.husky/pre-commit` (改 `pnpm lint:all` → `pnpm lint-staged`)
- Modify: `dashboard/frontend/package.json:13` (改 `lint:staged` script, 1 line)

- [ ] **Step 1: 验证 pre-state (8.44.1 已 commit)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git log --oneline -1
# 期望: chore(coverage): phase 8.44.1 — vitest --coverage v8 + threshold 70/60
grep -E '"lint-staged"' dashboard/frontend/package.json
ls -la dashboard/frontend/.lintstagedrc.json 2>&1
cat dashboard/frontend/.husky/pre-commit
# 期望: 0 lint-staged dep + 0 .lintstagedrc.json + pre-commit 跑 pnpm lint:all
```

- [ ] **Step 2: Bump lint-staged to ^15**

Edit `dashboard/frontend/package.json`, 在 devDependencies 加:

```diff
+    "lint-staged": "^15.0.0",
```

(放 devDependencies 任意位置, 跟其他 devDep 同 block)

- [ ] **Step 3: Edit package.json scripts — 改 lint:staged alias**

Edit `dashboard/frontend/package.json:13`, change:

```diff
-    "lint:staged": "pnpm lint:all",
+    "lint:staged": "lint-staged",
```

(从 alias 走全量 lint:all 改为 lint-staged CLI 入口, 跑 staged files only)

- [ ] **Step 4: Install (lint-staged 装新 dep)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
corepack pnpm@9.15.0 install
```

Expected: `Done in N.Ns` + 0 errors. `pnpm-lock.yaml` auto-updated.

- [ ] **Step 5: Create .lintstagedrc.json**

Create new file `dashboard/frontend/.lintstagedrc.json` with this exact content:

```json
{
  "*.{js,ts,vue}": [
    "eslint --config eslint.config.js --fix",
    "pnpm vitest related --run"
  ]
}
```

(2 runner: eslint --fix 在前, vitest --related 在后, lint-staged serial 跑)

- [ ] **Step 6: Edit .husky/pre-commit — 改 staged-only**

Edit `dashboard/frontend/.husky/pre-commit`, replace whole file:

```sh
#!/usr/bin/env sh
# Phase 8.40: Pre-commit hook — run lint-staged on staged files (scoped to changes)
# Phase 8.44.2: 改 lint-staged (Phase 8.40 跑 pnpm lint:all 全量太慢, 改 staged-only)
# 8.44.3 在后续 task 加 backend pytest smoke 段
. "$(dirname -- "$0")/_/h"

pnpm lint-staged
```

- [ ] **Step 7: 验证 lint-staged 跑 staged files only**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
# 制造 1 个 staged file 验证 hook
echo "console.log('test')" > /tmp/test_lint_staged.js
git -C dashboard/frontend add /tmp/test_lint_staged.js
# 实际跑 lint-staged (不走 git commit, dry-run)
cd dashboard/frontend
npx lint-staged --diff="HEAD" 2>&1 | tail -10
# 期望: lint-staged 跑 staged /tmp/test_lint_staged.js (估 2-5s, eslint --fix 应用)
cd ..
git -C dashboard/frontend reset HEAD /tmp/test_lint_staged.js
rm /tmp/test_lint_staged.js
```

- [ ] **Step 8: 验证 baseline 维持 (vitest / build / lint)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
corepack pnpm@9.15.0 vitest run 2>&1 | tail -3
# 期望: Test Files 16 passed (16) / Tests 95 passed (95) / 0 failed
corepack pnpm@9.15.0 build 2>&1 | tail -3
# 期望: ✓ built in N.NNs
corepack pnpm@9.15.0 lint:all
echo "EXIT=$?"
# 期望: EXIT=0
```

- [ ] **Step 9: 验证 0 改 .vue / 0 改 16 spec (除 vitest.config.js / package.json / pnpm-lock.yaml / .gitignore / .lintstagedrc.json / .husky/pre-commit)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git diff --stat dashboard/frontend/src/ dashboard/frontend/tests/unit/ 'dashboard/frontend/src/**/*.spec.ts'
# 期望: 0 files changed
```

- [ ] **Step 10: Commit**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git add dashboard/frontend/package.json dashboard/frontend/pnpm-lock.yaml dashboard/frontend/.lintstagedrc.json dashboard/frontend/.husky/pre-commit
git commit -m "chore(lint): phase 8.44.2 — lint-staged 接入 + pre-commit 改 staged-only

lint-staged@^15 devDep (跟 Node 20 兼容). .lintstagedrc.json NEW (~20L, 2 runner:
eslint --fix 在前 + pnpm vitest related --run 在后, lint-staged serial 跑).
.husky/pre-commit 改 pnpm lint:staged alias (从 pnpm lint:all 改 lint-staged CLI,
staged files only 估 2-5s 常态, vs Phase 8.40 全量 5-10s).

vitest 16 spec / 95 tests passed 维持, pnpm build 0 error, pnpm lint:all EXIT=0.
0 改 .vue / 0 改 16 spec / 0 改 .husky/_/h.

下一步 8.44.3 pre-commit 加 backend pytest smoke (跨 frontend+backend 协同保)."
```

---

## Task 3: 8.44.3 — pre-commit 加 backend pytest smoke (medium risk, hook ✅)

**Files:**
- Modify: `dashboard/frontend/.husky/pre-commit` (+ 1 段 pytest smoke)

- [ ] **Step 1: 验证 pre-state (8.44.2 已 commit)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git log --oneline -1
# 期望: chore(lint): phase 8.44.2 — lint-staged 接入 + pre-commit 改 staged-only
cat dashboard/frontend/.husky/pre-commit
# 期望: 8.44.2 已改 (pnpm lint-staged, 无 pytest 段)
```

- [ ] **Step 2: Edit .husky/pre-commit — 加 pytest smoke 段**

Edit `dashboard/frontend/.husky/pre-commit`, replace whole file:

```sh
#!/usr/bin/env sh
# Phase 8.40: Pre-commit hook — run lint-staged on staged files (scoped to changes)
# Phase 8.44.2: 改 lint-staged (Phase 8.40 跑 pnpm lint:all 全量太慢, 改 staged-only)
# Phase 8.44.3: 加 backend pytest smoke (跨 frontend+backend 协同保, 跟 test.yml 同步
# skip e2e + legacy, -x fail-fast, --timeout=30s 防 hang, 估 <30s)
. "$(dirname -- "$0")/_/h"

# Frontend: staged-only lint via lint-staged
pnpm lint-staged

# Backend: pytest smoke (skip e2e + legacy, fail-fast, 30s timeout)
# 走 git rev-parse --show-toplevel 动态定位 root, 防 CWD 漂 (R9 缓解)
cd "$(git rev-parse --show-toplevel)"
pytest tests/ \
  --ignore=tests/agent_system/test_e2e_workflow.py \
  --ignore=tests/tools/test_enhancement_tools.py \
  -x -q --timeout=30
```

- [ ] **Step 3: 验证 pre-commit hook 跑 pytest 段**

```bash
# 1. 直接跑 hook 验 pytest 段
bash dashboard/frontend/.husky/pre-commit 2>&1 | tail -10
# 期望: pytest 段跑 (~30s, 跟 CI 同步 skip e2e + legacy, -x fail-fast)
# 期望: hook EXIT=0 (全 pass)
echo "EXIT=$?"
```

- [ ] **Step 4: 验证 -x fail-fast (制造 1 failing test)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
echo "def test_phase844_smoke_fail(): assert False, 'phase 8.44.3 fail-fast test'" > tests/agent_system/test_phase844_smoke_fail.py
git add tests/agent_system/test_phase844_smoke_fail.py
bash dashboard/frontend/.husky/pre-commit 2>&1 | tail -15
echo "EXIT=$?"
# 期望: pytest 段 EXIT 1 (-x fail-fast, 1 fail 立即 abort)
# 期望: hook EXIT 1
git reset HEAD tests/agent_system/test_phase844_smoke_fail.py
rm tests/agent_system/test_phase844_smoke_fail.py
```

- [ ] **Step 5: 验证 baseline 维持 (vitest / build / lint)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
corepack pnpm@9.15.0 vitest run 2>&1 | tail -3
# 期望: Test Files 16 passed (16) / Tests 95 passed (95) / 0 failed
corepack pnpm@9.15.0 build 2>&1 | tail -3
# 期望: ✓ built in N.NNs
corepack pnpm@9.15.0 lint:all
echo "EXIT=$?"
# 期望: EXIT=0
```

- [ ] **Step 6: 验证 0 改 backend (除 0 改, pytest 只跑不写)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pytest -q 2>&1 | tail -3
# 期望: 2262 passed, 0 failed, 27 skipped
ruff check . 2>&1 | tail -3
# 期望: All checks passed!
```

- [ ] **Step 7: Commit**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git add dashboard/frontend/.husky/pre-commit
git commit -m "chore(hook): phase 8.44.3 — pre-commit 加 backend pytest smoke

.husky/pre-commit 加 1 段 backend pytest smoke (跟 test.yml 同步 skip e2e +
legacy, -x fail-fast, --timeout=30s 防 hang, 估 <30s). 走 git rev-parse
--show-toplevel 动态定位 root, 防 CWD 漂.

pre-commit hook 跑 ~30s (含 lint-staged + pytest smoke), vs Phase 8.40 全量 lint
5-10s 慢但接受 (跨 frontend+backend 协同保). -x fail-fast 验证: 1 failing test
hook EXIT 1 立即 abort.

vitest 16 spec / 95 tests passed 维持, pytest 2262 passed 0 failed 27 skipped 维持,
ruff 0, pnpm build 0 error. 0 改 backend Python.

下一步 8.44.4 dashboard-frontend-ci.yml 加 Codecov upload (frontend flag)."
```

---

## Task 4: 8.44.4 — dashboard-frontend-ci.yml 加 Codecov frontend (medium risk, hook ✅)

**Files:**
- Create: `codecov.yml` (root, ~30L, NEW)
- Modify: `.github/workflows/dashboard-frontend-ci.yml:60-67` (+ 1 codecov step, 改 vitest step 加 --coverage)

- [ ] **Step 1: 验证 pre-state (8.44.3 已 commit)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git log --oneline -1
# 期望: chore(hook): phase 8.44.3 — pre-commit 加 backend pytest smoke
ls -la codecov.yml 2>&1
# 期望: ls: cannot access 'codecov.yml': No such file or directory
cat .github/workflows/dashboard-frontend-ci.yml | tail -15
# 期望: vitest step 跑 pnpm vitest run (0 --coverage), 0 codecov step
```

- [ ] **Step 2: Create codecov.yml (root config)**

Create new file `codecov.yml` at `/home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/codecov.yml` with this exact content:

```yaml
# Phase 8.44.4: Codecov config — 跨 vitest (frontend) + pytest (backend) 合并报
# 2 flags 隔离 (frontend + backend), 1 PR 同时改两边时, Codecov 等 2 build 都跑完
# 才 comment (after_n_builds: 2). wait_for_ci + require_ci_to_pass 防 Codecov 在
# CI fail 时误报. require_head: true + require_base: false 让 fork PR 也能报.

codecov:
  require_ci_to_pass: true
  notify:
    after_n_builds: 2  # 1 frontend (vitest) + 1 backend (pytest, 3.13 only)
    wait_for_ci: true

coverage:
  precision: 2
  round: down
  range: "70...90"

flags:
  frontend:
    paths:
      - dashboard/frontend/src/
  backend:
    paths:
      - infra/
      - tools/
      - dashboard/

comment:
  layout: "header, diff, flags, components, files, footer"
  behavior: default
  require_changes: false
  require_base: false
  require_head: true
```

- [ ] **Step 3: Edit dashboard-frontend-ci.yml — vitest step 加 --coverage + 1 codecov step**

Edit `.github/workflows/dashboard-frontend-ci.yml:60-67`, replace:

```diff
       - name: Run vitest
         # Phase 8.41: 23 spec files / 95 tests expected
-        run: pnpm vitest run
+        # Phase 8.44.4: 加 --coverage 产出 lcov.info 给 Codecov upload
+        run: pnpm vitest run --coverage
+
+      - name: Upload coverage to Codecov (frontend flag)
+        # Phase 8.44.4: Codecov 跨 vitest + pytest 合并, frontend flag 标 dashboard/frontend/src/
+        # fail_ci_if_error: false 防 token 缺失 / service outage 阻塞 CI
+        if: always()
+        uses: codecov/codecov-action@v5
+        with:
+          files: ./dashboard/frontend/coverage/lcov.info
+          flags: frontend
+          fail_ci_if_error: false
+          verbose: false
```

(dashboard-frontend-ci.yml 无 matrix, 走 `if: always()` 跟 `|| true` 等价)

- [ ] **Step 4: 验证 yaml syntax 正确**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
python -c "import yaml; yaml.safe_load(open('codecov.yml')); print('codecov.yml OK')"
python -c "import yaml; yaml.safe_load(open('.github/workflows/dashboard-frontend-ci.yml')); print('dashboard-frontend-ci.yml OK')"
# 期望: 2 行 OK
```

- [ ] **Step 5: 验证本地 vitest --coverage 仍跑 (无 Codecov token, CI 端走 codecov step)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
corepack pnpm@9.15.0 vitest run --coverage 2>&1 | tail -10
# 期望: Coverage report + lcov.info 产出
ls -la coverage/lcov.info 2>&1
# 期望: lcov.info 存在 (Codecov upload input)
```

- [ ] **Step 6: 验证 baseline 维持 (build / lint / pytest)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
corepack pnpm@9.15.0 build 2>&1 | tail -3
# 期望: ✓ built in N.NNs
corepack pnpm@9.15.0 lint:all
echo "EXIT=$?"
# 期望: EXIT=0

cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pytest -q 2>&1 | tail -3
# 期望: 2262 passed, 0 failed, 27 skipped
```

- [ ] **Step 7: Commit**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git add codecov.yml .github/workflows/dashboard-frontend-ci.yml
git commit -m "ci(coverage): phase 8.44.4 — dashboard-frontend-ci.yml 加 Codecov upload (frontend flag)

codecov.yml NEW (~30L, root config) — 跨 vitest (frontend) + pytest (backend) 2 flags
合并报: require_ci_to_pass: true + wait_for_ci: true + after_n_builds: 2.
flags.frontend paths = dashboard/frontend/src/, flags.backend paths = infra/tools/dashboard.
comment layout = header+diff+flags+components+files+footer, require_base: false
让 fork PR 也能报.

dashboard-frontend-ci.yml vitest step 改 pnpm vitest run --coverage (产出
coverage/lcov.info). +1 codecov step (codecov-action@v5, files: lcov.info,
flags: frontend, fail_ci_if_error: false 防 token 缺失阻塞 CI).

yaml syntax 验证 OK. vitest 16 spec / 95 tests passed + coverage 维持.
pnpm build 0 error, pnpm lint:all EXIT=0, pytest 2262 passed 0 failed 27
skipped 维持. 0 改 16 spec / 0 改 .vue / 0 改 backend Python.

下一步 8.44.5 test.yml 加 Codecov upload (backend flag, 跟 frontend 平行)."
```

---

## Task 5: 8.44.5 — test.yml 加 Codecov backend + xml report (medium risk, hook ✅)

**Files:**
- Modify: `.github/workflows/test.yml:42-67` (+ 1 codecov step, + xml report, 0 改主体)

- [ ] **Step 1: 验证 pre-state (8.44.4 已 commit)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git log --oneline -1
# 期望: ci(coverage): phase 8.44.4 — dashboard-frontend-ci.yml 加 Codecov upload (frontend flag)
cat .github/workflows/test.yml
# 期望: pytest step 0 xml report, 0 codecov step
```

- [ ] **Step 2: Edit test.yml — pytest step 加 xml report + 1 codecov step**

Edit `.github/workflows/test.yml:42-67`, replace:

```diff
       - name: Run pytest
         # 跳过 e2e (需要外部服务) + enhancement_tools (legacy,本地有特殊依赖)
         run: |
           pytest tests/ \
             --ignore=tests/agent_system/test_e2e_workflow.py \
             --ignore=tests/tools/test_enhancement_tools.py \
             --cov=infra --cov=tools --cov=dashboard \
             --cov-report=term-missing \
+            --cov-report=xml:coverage.xml \
             --cov-fail-under=30 \
             -q

+      - name: Upload coverage to Codecov (backend flag)
+        # Phase 8.44.5: 跟 8.44.4 平行, backend flag 标 infra/tools/dashboard, 等
+        # frontend 那个 build 跑完 (after_n_builds: 2 in codecov.yml) 再 comment.
+        # fail_ci_if_error: false 防 token 缺失阻塞 CI.
+        if: matrix.python-version == '3.13'
+        uses: codecov/codecov-action@v5
+        with:
+          files: ./coverage.xml
+          flags: backend
+          fail_ci_if_error: false
+          verbose: false
+
       - name: Ruff (informational, non-blocking)
         if: matrix.python-version == '3.13'
```

(注: ruff step 0 改, artifact upload step 0 改, 仅在 pytest step 加 xml report + 加 1 codecov step)

- [ ] **Step 3: 验证 yaml syntax 正确**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
python -c "import yaml; yaml.safe_load(open('.github/workflows/test.yml')); print('test.yml OK')"
# 期望: 1 行 OK
```

- [ ] **Step 4: 验证本地 pytest --cov-fail-under=30 + xml report 仍跑**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pytest tests/ \
  --ignore=tests/agent_system/test_e2e_workflow.py \
  --ignore=tests/tools/test_enhancement_tools.py \
  --cov=infra --cov=tools --cov=dashboard \
  --cov-report=term-missing \
  --cov-report=xml:coverage.xml \
  --cov-fail-under=30 \
  -q 2>&1 | tail -10
# 期望: 2262 passed, 0 failed, 27 skipped
# 期望: coverage.xml 生成 (Codecov upload input)
ls -la coverage.xml 2>&1
# 期望: coverage.xml 存在
rm coverage.xml  # 0 提交, gitignore check
git status --short coverage.xml 2>&1
# 期望: ?? coverage.xml (untracked, OK 不提交, 0 gitignore 显式 ignore 因为 CI only)
```

- [ ] **Step 5: 验证 baseline 维持 (vitest / build / lint)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
corepack pnpm@9.15.0 vitest run 2>&1 | tail -3
# 期望: Test Files 16 passed (16) / Tests 95 passed (95) / 0 failed
corepack pnpm@9.15.0 build 2>&1 | tail -3
# 期望: ✓ built in N.NNs
corepack pnpm@9.15.0 lint:all
echo "EXIT=$?"
# 期望: EXIT=0

cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
ruff check . 2>&1 | tail -3
# 期望: All checks passed!
```

- [ ] **Step 6: Commit**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git add .github/workflows/test.yml
git commit -m "ci(coverage): phase 8.44.5 — test.yml 加 Codecov upload (backend flag) + xml report

test.yml pytest step 加 --cov-report=xml:coverage.xml (Codecov action 标准 input
format, 跟 frontend lcov.info 对偶, 2 flags 不同 format 隔离). +1 codecov step
(codecov-action@v5, files: coverage.xml, flags: backend, fail_ci_if_error: false).

跟 8.44.4 平行, 等 frontend 那个 build 跑完 (after_n_builds: 2 in codecov.yml)
再 comment. 0 改 ruff step / 0 改 artifact upload step. yaml syntax 验证 OK.

pytest 2262 passed 0 failed 27 skipped 维持. vitest 16 spec / 95 tests passed
维持. ruff 0. pnpm build 0 error. pnpm lint:all EXIT=0. 0 改 backend Python.

下一步 8.44.6 memory sync + push 5 commits to origin/master."
```

---

## Task 6: 8.44.6 — Memory sync + push 5 commits (low risk, hook ✅, **0 commit**)

**Files:**
- Modify: `~/.claude/projects/-home-ailearn-projects-AI-Incursion-domains-IP---projects-LingWen/memory/phases-8-dashboard.md` (H1 + L4 edit + append) — **OUTSIDE git repo**
- Push: 5 commits from Tasks 1-5 to `origin/master` (network action, not a commit)

- [ ] **Step 1: 验证 pre-state (8.44.5 已 commit)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git log --oneline -5
# 期望: 5 Phase 8.44 commits 落 local (8.44.5 + 8.44.4 + 8.44.3 + 8.44.2 + 8.44.1)
git rev-parse HEAD origin/master
# 期望: 2 行不同 SHA (origin/master 仍 @ a5957ba spec commit, 本地 5 ahead)
```

- [ ] **Step 2: Edit H1 (L1) — 加 8.44 范围**

Edit `phases-8-dashboard.md` L1, change:

```diff
-# 灵文 · Phase 实施历史 (Phase 8.16-8.43, dashboard polish + build tooling upgrade)
+# 灵文 · Phase 实施历史 (Phase 8.16-8.44, dashboard polish + build tooling upgrade + test infra)
```

- [ ] **Step 3: Edit L4 (前 4 行 quote) — 标注 8.44 5 commits 追加**

Edit `phases-8-dashboard.md` L4, change:

```diff
-> 拆分时间: 2026-06-08 Phase 15 bookkeeping; 2026-06-09 Phase 8.35-8.42 7 commits 追加; 2026-06-09 Phase 8.43 6 commits 追加 (5 build tooling major bump + 1 flat config rewrite, Task 7 = push + memory sync 0 commit).
+> 拆分时间: 2026-06-08 Phase 15 bookkeeping; 2026-06-09 Phase 8.35-8.42 7 commits 追加; 2026-06-09 Phase 8.43 6 commits 追加 (5 build tooling major bump + 1 flat config rewrite, Task 7 = push + memory sync 0 commit); 2026-06-09 Phase 8.44 5 commits 追加 (5 test infra followup 收尾: vitest coverage v8 + lint-staged + pre-commit pytest + Codecov frontend + Codecov backend, Task 6 = push + memory sync 0 commit).
```

- [ ] **Step 4: Append `## Phase 8.44 (2026-06-09)` entry at end of file**

Append at end of `phases-8-dashboard.md` (after Phase 8.43 entry):

```markdown

## Phase 8.44 (2026-06-09)

5 test infra followup items 收尾 (Super-phase B 启动) — 收尾 Phase 8.43+ 留 followup: vitest --coverage v8 + Codecov frontend + Codecov backend (2 flags 合并报) + lint-staged 接入 + pre-commit backend pytest smoke. 6 sub-phases / **5 commits 推 origin/master** (Task 1-5 各 1 commit, Task 6 = push + memory sync 0 commit 因为 phases-8-dashboard.md 在 ~/.claude/... 不在 git repo). 0 --no-verify (5 commit 全 hook ✅). **8.44.1 vitest --coverage v8 + threshold 70/60** (low risk, @vitest/coverage-v8@^4.1.8 devDep 跟 vitest 4.1.8 同步, vitest.config.js 加 coverage block provider v8 + reporter text/lcov/html + include src/**/*.{js,ts,vue} + exclude spec/main.ts/index.ts + thresholds { lines: 70, branches: 60, functions: 70, statements: 70 }, .gitignore 加 coverage/ entry, lcov.info 给 8.44.4 Codecov frontend upload, vitest 16 spec / 95 tests passed 维持 + coverage 实测估 75-85% lines, 0 改 16 spec 0 改 .vue). **8.44.2 lint-staged 接入** (medium risk, lint-staged@^15 devDep, .lintstagedrc.json NEW 2 runner eslint --fix 在前 + pnpm vitest related --run 在后, .husky/pre-commit 改 pnpm lint:staged alias 从 pnpm lint:all 改 lint-staged CLI staged files only 估 2-5s 常态 vs Phase 8.40 全量 5-10s). **8.44.3 pre-commit 加 backend pytest smoke** (medium risk, .husky/pre-commit 加 1 段 pytest smoke 跟 test.yml 同步 skip e2e + legacy, -x fail-fast, --timeout=30s 防 hang, 走 git rev-parse --show-toplevel 动态定位 root 防 CWD 漂, 估 <30s 跨 frontend+backend 协同保, -x fail-fast 验证 1 failing test hook EXIT 1 立即 abort). **8.44.4 dashboard-frontend-ci.yml 加 Codecov frontend** (medium risk, codecov.yml NEW root config ~30L 跨 vitest+pytest 2 flags 合并报 require_ci_to_pass: true + wait_for_ci: true + after_n_builds: 2 + flags.frontend paths dashboard/frontend/src/ + flags.backend paths infra/tools/dashboard + comment layout header+diff+flags+components+files+footer require_base: false 让 fork PR 也能报, dashboard-frontend-ci.yml vitest step 改 pnpm vitest run --coverage + 1 codecov step codecov-action@v5 files lcov.info flags frontend fail_ci_if_error: false 防 token 缺失阻塞 CI, yaml syntax 验证 OK). **8.44.5 test.yml 加 Codecov backend + xml report** (medium risk, test.yml pytest step 加 --cov-report=xml:coverage.xml Codecov action 标准 input format 跟 frontend lcov.info 对偶 2 flags 不同 format 隔离, +1 codecov step codecov-action@v5 files coverage.xml flags backend fail_ci_if_error: false, 跟 8.44.4 平行等 frontend 那个 build 跑完 after_n_builds: 2 再 comment, 0 改 ruff step 0 改 artifact upload step, yaml syntax 验证 OK). **8.44.6 memory sync + push** (low risk, phases-8-dashboard.md H1 + L4 edit + 本 entry append, push 5 commits 到 origin/master). **commit sequence**: 5 commits 推 (T1 8.44.1 vitest coverage → T2 8.44.2 lint-staged → T3 8.44.3 pre-commit pytest → T4 8.44.4 Codecov frontend → T5 8.44.5 Codecov backend). **测试总**: vitest 16 spec / 95 tests all passed 维持 + coverage > 70% lines, pytest 2262 passed 0 failed 27 skipped 维持 (0 改 backend), ruff 0 / pnpm build 0 error / pnpm lint:all EXIT=0. **verification commands**: `pnpm vitest run --coverage` coverage report + lcov.info / `pnpm vitest run` 16/95 passed / `pnpm build` 0 error / `pnpm lint:all` EXIT=0 / `pytest -q` 2262 passed / `ruff check .` 0 issues / `python -c "import yaml; yaml.safe_load(...)"` 2 yaml OK / `bash .husky/pre-commit` EXIT=0 / `git rev-parse HEAD origin/master` 2 行同 SHA. **0 改**: 14 .vue / 16 vitest spec / 5 composables / api/index.js / eslint-rules/* / index.html / vite.config.js / `.github/workflows/real-llm-tests.yml` / .husky/_/h / backend Python (infra/agent_system / dashboard / cost_tracker / cost_persistence / cost_router 全无 touch) / 9 historical spec/plan docs `:NNN` 行号不动 / 12 SCENARIOS 路由 / CLAUDE.md / pyproject.toml / pytest.ini / package.json 主体 (除 2 devDep + 1 script) / vitest.config.js 结构 (除 coverage block) / .husky/pre-commit 结构 (除 1-2 行 + 1 段) / .state/*.db (gitignored) / .env (0 改 0 泄漏). **out of scope (有 followup)**: Super-phase C (Phase 8.5+) DecisionCard 4 status testid (deferred 状态修正 0 "expired") + OverviewPage e2e 加深 / Super-phase D (Phase 8.6+) TS strict mode (加 tsconfig.json + strict: true + 修 11 .ts) / Phase 9+ 跨卷涟漪 (独立大方向, spec 已 commit 8cc1298) / MEMORY.md 拆 phases-8-dashboard.md 38+ entries to phases.md topic (本 entry 后变 38+, 必拆 followup) / Coverage threshold 提严 70→80 等 codebase coverage 实测后 / Codecov badge 加 README.md / GitHub Pages 跑 vitest html coverage report / Node 22 matrix 加进 CI (Node 20 维持) / Vitest 5 + Test-utils 3 stable 等 release 4+ 周后 revisit / Vue 3.5 新 API (useTemplateRef / defineModel / useId) 留 followup / test-utils 3 高级 API (findComponent / setValue / trigger) 留 followup / echarts / mermaid / @types/node bump 0 改 / pre-commit pytest smoke 0 改范围 (skip e2e + legacy 维持) / codecov.yml 0 改 coverage.precision/round 维持 / @vitest/coverage-istanbul 维持 v8 取代 / lint-staged 0 改其他 runner (eslint + vitest 维持) / 5 commit 0 --no-verify 维持. 后续: Phase 8.5+ Super-phase C (UI completeness) 启动.
```

(注: 不需要 placeholder commit SHAs — Task 6 0 commit, 实际 SHAs 走 git push 后 log 查)

- [ ] **Step 5: Push 5 commits to origin/master (Task 6 = push + memory sync, 0 commit)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git status
# 期望: clean (phases-8-dashboard.md 在 ~/.claude/... 不在 git repo, 0 改 git tracked file)
git push origin master
# 期望: 5 commits 推送, output 类似:
#   To github.com:XiaZiHunDun/LingWen.git
#      a5957ba..<new HEAD>  master -> master

git rev-parse HEAD origin/master
# 期望: 2 行同 SHA (push 验证)

git log origin/master --oneline -5
# 期望: 5 Phase 8.44 commits 落 origin
```

- [ ] **Step 6: 0 Co-Authored-By trailers 验证**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git log -5 --format='%(trailers:key=Co-Authored-By,valueonly)'
# 期望: 5 行空 (0 Co-Authored-By, 跟 Phase 8.35-8.43 policy 一致)
```

- [ ] **Step 7: Task 6 done (0 commit, push + memory sync 完)**

(注: phases-8-dashboard.md 在 `~/.claude/projects/.../memory/`, 不走 git. Task 6 done = 5 commits 推 origin + memory 同步 + 0 commit. Spec 8.44.6 说 "6 commits" 是 off-by-one — plan 精确化为 "5 commits" 因 memory file outside git repo)

---

## Out of Scope (留后续 super-phase / phase)

- **Super-phase C (Phase 8.5+)**: DecisionCard 4 status testid (deferred 状态修正) + OverviewPage e2e 加深
- **Super-phase D (Phase 8.6+)**: TS strict mode (加 tsconfig.json + strict: true + 修 11 .ts)
- **Phase 9+**: 跨卷涟漪 (Phase 9 spec 已 commit 8cc1298, 留独立 phase)
- **MEMORY.md 拆**: phases-8-dashboard.md 累 Phase 8.44 后变 38+ entries, 必拆 followup
- **Coverage threshold 提严** (70→80): 等 codebase coverage 实测后 followup
- **Codecov badge** 加 README.md: 留 followup
- **GitHub Pages** 跑 vitest html coverage report: 留 followup
- **Node 22 matrix** 加进 CI: 0 改, Node 20 维持
- **Vitest 5 + Test-utils 3** stable 等 release 4+ 周后 revisit
- **Vue 3.5 新 API 引入**: useTemplateRef / defineModel / useId 5+ API 留 followup
- **test-utils 3 高级 API 改**: findComponent / setValue / trigger 留 followup
- **echarts / mermaid / @types/node bump**: 0 改
- **pre-commit pytest smoke 范围调**: skip e2e + legacy 维持, 0 改
- **codecov.yml coverage.precision/round 调**: 维持 2/down

---

## Risks + Mitigations

| # | Risk | Mitigation |
|---|------|-----------|
| R1 | coverage threshold 触发 fail (实测 < 70%) | 临时降到 60/50, 1 commit followup 慢慢提, 兜底 in plan |
| R2 | lint-staged config 错 → 0 跑 → 0 lint | dry-run test commit 验证 (Task 2 step 7), 1 commit recover 路径 |
| R3 | vitest --related 跑 0 (跟 staged 0 相关) | 走 fallback `pnpm vitest run` 兜底, lint-staged pattern try/catch (留 followup) |
| R4 | pre-commit pytest smoke 30s 超 (有大 slow test) | `--timeout=30` 提早 fail, 临时降到 `--timeout=60` 救火 |
| R5 | pre-commit pytest 跑全量 (slow) | `-x` fail-fast + skip e2e/legacy, 估 <30s, 1 commit followup 调 |
| R6 | Codecov token 缺失 → upload fail → CI fail | `fail_ci_if_error: false` 0 阻塞, 主公 push 后再加 token (github settings) |
| R7 | Codecov service outage → step fail | 同 R6, `fail_ci_if_error: false` 0 阻塞 |
| R8 | codecov.yml 错 → comment 错位 | 2 flags 隔离 (frontend + backend), 1 commit recover 路径 |
| R9 | `cd ../..` 在 `.husky/pre-commit` 跑环境差异 (CI 跟本地) | 走 `git rev-parse --show-toplevel` 动态定位 root, 防 CWD 漂 (Task 3 step 2 已用) |
| R10 | push 5 commits 失败 (网络) | 1 retry, 仍 fail → 排查 gh 鉴权, 0 force-push |
| R11 | `coverage.xml` 0 gitignore → 误提交 | Task 5 step 4 验证 untracked 状态, 0 commit (CI only artifact) |
| R12 | `coverage/` 0 gitignore → 误提交 | Task 1 step 5 显式加 `.gitignore` entry, 验证 git status clean |

---

## Critical Files

### Created (3)
- `dashboard/frontend/.lintstagedrc.json` (~20L, 8.44.2)
- `codecov.yml` (~30L, 8.44.4)
- `docs/superpowers/plans/2026-06-09-phase8.44-test-infra.md` (本 plan)

### Modified (8)
- `dashboard/frontend/package.json` (8.44.1 + 8.44.2: 2 devDep + 1 script)
- `dashboard/frontend/pnpm-lock.yaml` (8.44.1 + 8.44.2 auto)
- `dashboard/frontend/vitest.config.js` (8.44.1: +coverage config)
- `dashboard/frontend/.gitignore` (8.44.1: +coverage/ entry)
- `dashboard/frontend/.husky/pre-commit` (8.44.2 + 8.44.3: lint-staged + pytest smoke)
- `.github/workflows/dashboard-frontend-ci.yml` (8.44.4: +1 codecov step)
- `.github/workflows/test.yml` (8.44.5: +1 codecov step + xml report)
- `~/.claude/projects/.../memory/phases-8-dashboard.md` (8.44.6: H1 + L4 edit + 8.44 entry append)

### NOT modified (~20)
14 .vue / 16 vitest spec / 5 composables / api/index.js / eslint-rules/* / index.html / vite.config.js / `.github/workflows/real-llm-tests.yml` / `.husky/_/h` / backend Python (infra/agent_system / dashboard backend / cost_tracker / cost_persistence / cost_router) / 9 historical spec/plan docs / 12 SCENARIOS / CLAUDE.md / pyproject.toml / pytest.ini / package.json 主体 (除 2 字段 + 1 script) / vitest.config.js 结构 (除 coverage block) / .husky/pre-commit 结构 (除 1-2 行 + 1 段) / .state/*.db (gitignored) / .env

---

## 完成定义 (DoD)

- [ ] `docs/superpowers/specs/2026-06-09-phase8.44-test-infra-design.md` 创建 (commit `a5957ba`)
- [ ] `docs/superpowers/plans/2026-06-09-phase8.44-test-infra.md` 创建 (本 plan)
- [ ] `dashboard/frontend/vitest.config.js` coverage config 加 (Task 1, 8.44.1)
- [ ] `dashboard/frontend/.gitignore` coverage/ entry 加 (Task 1, 8.44.1)
- [ ] `dashboard/frontend/.lintstagedrc.json` 创建 (Task 2, 8.44.2)
- [ ] `dashboard/frontend/.husky/pre-commit` 改 lint-staged (Task 2, 8.44.2)
- [ ] `dashboard/frontend/.husky/pre-commit` 加 pytest smoke (Task 3, 8.44.3)
- [ ] `codecov.yml` 创建 root (Task 4, 8.44.4)
- [ ] `.github/workflows/dashboard-frontend-ci.yml` 加 codecov step (Task 4, 8.44.4)
- [ ] `.github/workflows/test.yml` 加 codecov step + xml report (Task 5, 8.44.5)
- [ ] 2 devDep 新增: `@vitest/coverage-v8` + `lint-staged` (Task 1+2, 8.44.1+8.44.2)
- [ ] 1 script 改: `lint:staged` 从 `pnpm lint:all` 改 `lint-staged` (Task 2, 8.44.2)
- [ ] 5 commits 推 origin/master (Task 1-5 + Task 6 push)
- [ ] 0 --no-verify (5 commit 全 hook ✅)
- [ ] vitest 16 spec / 95 tests passed 维持 + coverage > 70% lines
- [ ] pytest 2262 passed 0 failed 27 skipped 维持
- [ ] ruff 0 issues 维持
- [ ] pnpm build 0 error 维持
- [ ] pnpm lint:all EXIT=0 维持
- [ ] pre-commit pytest smoke < 30s 验证 (Task 3 step 3)
- [ ] yaml syntax 验证 OK codecov.yml + dashboard-frontend-ci.yml + test.yml (Task 4+5 step 3-4)
- [ ] git rev-parse HEAD origin/master 2 行同 SHA
- [ ] 0 改 .vue / 0 改 16 vitest spec / 0 改 9 historical spec/plan docs `:NNN` 行号 / 0 改 12 SCENARIOS 路由 / 0 改 backend Python (除 CI yaml) / 0 改 CLAUDE.md / pyproject.toml / pytest.ini
- [ ] 0 force-push / 0 amend (除 sub-phase fail 救火显式声明) / 0 Co-Authored-By / 0 DANGEROUSLY_SKIP_PERMISSIONS / 0 误改 .env / 0 API key 泄漏
- [ ] memory sync `phases-8-dashboard.md` H1 + L4 edit + 8.44 entry append

---

## 完成后下一步 (后续 super-phase / phase)

- **Phase 8.5 (Super-phase C 启动)**: UI completeness (DecisionCard 4 status testid + OverviewPage e2e)
- **Phase 8.6 (Super-phase D 启动)**: TS strict mode (tsconfig.json + strict: true + 修 11 .ts)
- **Phase 9+**: 跨卷涟漪 (Phase 9 spec 已 commit 8cc1298, 留独立 phase)
- **MEMORY.md 拆 phases-8-dashboard.md 38+ entries** to phases.md topic (必拆 followup)
- **Coverage threshold 提严** (70→80): 等 codebase coverage 实测后 followup
- **Codecov badge** 加 README.md: 留 followup
- **GitHub Pages** 跑 vitest html coverage report: 留 followup
- **Node 22 matrix** 加进 CI: 0 改, Node 20 维持
- **Vitest 5 + Test-utils 3** stable 等 release 4+ 周后 revisit
- **Vue 3.5 新 API** (useTemplateRef / defineModel / useId): 留 followup
- **test-utils 3 高级 API** (findComponent / setValue / trigger): 留 followup
