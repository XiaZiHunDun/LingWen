# Phase 9.14 — Ripple Audit Log + Real Rollback Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 Phase 9.13 (CVG UI + WS 推送) 基础上,加 `ripple_audit` append-only 表 + 软回滚 (status → pending) + 严格 fsync 原子化 + drawer timeline section,让 cross-volume ripple 操作全可审计可回滚。0 改 Phase 9.10/9.11/9.12/9.13 既有 source API 行为,仅 additive storage methods + additive endpoints + additive drawer section。

**Architecture:** 3-layer additive: (Layer1) `RippleStorage` 4 additive methods (`record_audit` / `get_audit_history` / `rollback_ripple` / 既有 2 method 内部 audit hook) + 新 `ripple_audit` table + 2 indexes + (Layer2) FastAPI 2 new endpoints (`GET /api/cvg/ripples/{id}/audit` / `POST /api/cvg/ripples/{id}/rollback`) + 2 Pydantic schemas + 2 既有 endpoint 接受可选 actor/origin/reason + (Layer3) `useRippleStore` 2 new actions + RippleDrawer 新增 timeline section + Rollback button + 2 api/index.js methods + `lingwen.py ripple audit + ripple rollback` 2 new CLI 子命令。

**Tech Stack:** Python 3.12 + sqlite3 (append-only audit + WAL + synchronous=FULL) + FastAPI + Pydantic + Click + Vue 3 + Vite + vitest + Playwright (smoke) + pytest 8 + ruff。

---

## Pre-Execution Setup

### Context
- Phase 9.10 (commit `c421da1`): CVG data model — 3 frozen dataclass (ReferenceNode 9 字段 / ReferenceEdge 8 字段 / CrossVolumeRipple 11 字段) + RippleStorage (sqlite3, 3 PRAGMAs, 8 public methods) + 16 tests
- Phase 9.11 (commit `149864c`, 9 commits): rule-based CVG backfill — 4 Extractor + Backfiller + `lingwen.py ripple backfill` CLI + 16 tests
- Phase 9.12 (commit `9406936`, 11 commits): LLM scanner 4 维 + Edge 推断 8 边类型
- Phase 9.13 (commit `9e996fc`, 14 commits): dashboard `/ripples` 页面 + 5 REST + 1 WebSocket `/api/ws/cvg` + 4 components + 2 composables + sidebar nav。Storage 3 additive methods (get_ripples / get_ripple_by_id / update_ripple_status) + ConflictError
- **Phase 9.14** (commit `5728ac7` spec, 790L): ripple audit log + real rollback + 严格 fsync 原子化

### Baseline State
- 测试: 2335 passed, 0 failed, 27 skipped
- ruff: 0 issues on new files (3 pre-existing I001 errors 在 Phase 9.14 范围外,留 followup)
- vitest: 128 tests passed (Phase 9.13 末状态)
- Playwright: 7 e2e smoke (Phase 9.13 末状态)
- 主公 (用户) hard rules (MUST apply verbatim 全程):
  - **0 force-push, 0 amend, 0 --no-verify, 0 Co-Authored-By footer** (Attribution disabled globally via `~/.claude/settings.json`)
  - **0 DANGEROUSLY_SKIP_PERMISSIONS, 0 API key 泄漏, 0 .env 改**
  - **0 改 CLAUDE.md / pyproject.toml / pytest.ini / vite.config.js / vitest.config.js / playwright.config.js / 12 SCENARIOS 路由 / historical spec/plan doc `:NNN` 行号**
  - **0 改 Phase 9.10/9.11/9.12/9.13 现有 source API 行为** (仅 additive RippleStorage 4 methods + 2 既有 method 内部 record_audit call + 1-line defensive WS broadcast hook 已在 Phase 9.13)
  - **0 改 dashboard 既有 endpoint handler / composable / store / router / sidebar nav / api method** (新增 2 endpoint + 2 schemas + 2 store actions + 1 drawer section + 2 api method + 2 CLI subcommand, 全部 additive)
  - **0 production code 改** (MasterController / got_bridge / workflow.db / cost_tracker.db 0 行)
  - File size 200-400L typical / 800L hard cap
  - TDD methodology (RED → GREEN → commit per task)
  - Conventional commits format
  - 沟通: 叙述中文 / 代码英文 / commit body 中文注释
  - Git root: `/home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/`
  - 当前 working dir: `novel-factory/`
  - MEMORY.md < 200 lines (current ~35, Phase 9.14 后 ~37, 留 followup 拆)

### Target State
- 测试: **2364 passed, 0 failed, 27 skipped** (2335 baseline + 29 new pytest)
- vitest: **+12 new** (128 baseline + 12 new = 140+)
- Playwright: **+3 new** (7 baseline + 3 new = 10)
- ruff: 0 issues on new files
- pnpm lint:all: 0 errors / ≤8 warnings (跟 Phase 9.13 baseline 一致)
- pnpm build: 0 error
- pnpm e2e:smoke --list: 10 tests (7 Phase 9.13 + 3 Phase 9.14)
- 0 改 Phase 9.10/9.11/9.12/9.13 现有 source API 行为
- 8 feat commits + 1 spec commit (5728ac7) + 1 plan commit (本文件) (10 total)
- 0 force-push / 0 amend / 0 Co-Authored-By

### File map summary
- **7 modified**: `infra/cross_volume/storage.py` / `dashboard/app.py` / `dashboard/protocols.py` / `dashboard/frontend/src/composables/useRippleStore.js` / `dashboard/frontend/src/components/RippleDrawer.vue` / `dashboard/frontend/src/api/index.js` / `lingwen.py`
- **1 spec doc** (已 commit 5728ac7): `docs/superpowers/specs/2026-06-10-phase9.14-audit-rollback-design.md`
- **1 plan doc** (本文件, 末尾 commit): `docs/superpowers/plans/2026-06-10-phase9.14-audit-rollback.md`
- **3 new pytest**: `tests/cross_volume/test_storage_audit.py` (15 tests) / `tests/dashboard/test_cvg_audit_api.py` (8 tests) / `tests/cvg_cli/test_ripple_audit.py` (6 tests)
- **2 new vitest**: `use-ripple-store-audit.spec.js` (6 tests) / `ripple-drawer-audit.spec.js` (6 tests)
- **1 new e2e**: `dashboard/frontend/tests/e2e-smoke/ripples-audit.spec.js` (3 tests)

---

## Critical safety (主公 hard rules)

> **0 force-push, 0 amend, 0 --no-verify, 0 Co-Authored-By footer** — 写代码前 verify 已设 `git config user.email` 跟 `git config user.name`,提交用 conventional commit 格式。
>
> **0 改 Phase 9.10/9.11/9.12/9.13 现有 source API 行为** — 仅 additive,4 new storage methods + 2 既有 method 内部 `record_audit()` call (跟 `cost_tracker.record()` 1:1 pattern)。
>
> **0 DANGEROUSLY_SKIP_PERMISSIONS** — 所有 bash 命令正常走,permission prompt 出现就 approve,不要加 `--no-verify` / `--force`。
>
> **0 改 historical spec/plan doc 行号** — historical 文件 `:NNN` 引用已不准,改反误导,永不做。
>
> **File size guard** — 任何 `.py` / `.vue` / `.js` 文件超 800L 必须先拆 module 再 commit。
>
> **0 audit data migration** — Phase 9.14 上线前的老 ripple 无 audit history,`GET /audit` 返 `[]` + UI 显示 "No history yet",0 数据迁移。

---

## Task 1: ripple_audit table schema + 5 tests

**Files:**
- Modify: `infra/cross_volume/storage.py:24-65` (extend `_SCHEMA_SQL` with ripple_audit table + 2 indexes, +30L)
- Create: `tests/cross_volume/test_storage_audit.py` (NEW, ~80L, 5 tests)
- Test: `pytest tests/cross_volume/test_storage_audit.py::TestRippleAuditSchema -v`

**Goal:** 在 `RippleStorage.__init__` 的 `_SCHEMA_SQL` 中追加 `ripple_audit` table (9 字段 + 5 CHECK constraint + 2 indexes),TDD: RED (table 不存在 → fail) → GREEN (建表后 pass)。

- [ ] **Step 1.1: Write 5 failing schema tests (RED)**

```python
# tests/cross_volume/test_storage_audit.py
"""Phase 9.14: ripple_audit table schema tests."""
import sqlite3
import pytest
from infra.cross_volume.storage import RippleStorage


@pytest.fixture
def storage(tmp_path):
    return RippleStorage(db_path=tmp_path / "audit_schema.db")


class TestRippleAuditSchema:
    def test_ripple_audit_table_exists(self, storage):
        """ripple_audit table created in __init__ via _SCHEMA_SQL."""
        with storage._connect() as conn:
            row = conn.execute(
                "SELECT name FROM sqlite_master WHERE type='table' AND name='ripple_audit'"
            ).fetchone()
        assert row is not None, "ripple_audit table should exist"

    def test_ripple_audit_indexes_exist(self, storage):
        """idx_audit_ripple + idx_audit_actor 2 indexes created."""
        with storage._connect() as conn:
            rows = conn.execute(
                "SELECT name FROM sqlite_master WHERE type='index' AND name LIKE 'idx_audit_%'"
            ).fetchall()
        index_names = {r["name"] for r in rows}
        assert "idx_audit_ripple" in index_names
        assert "idx_audit_actor" in index_names

    def test_ripple_audit_idempotent_migration(self, tmp_path):
        """Re-init on existing DB should not raise (CREATE TABLE IF NOT EXISTS)."""
        db_path = tmp_path / "audit_idem.db"
        RippleStorage(db_path=db_path)  # first init
        # Second init must not raise
        RippleStorage(db_path=db_path)

    def test_ripple_audit_check_constraint_action(self, storage):
        """Invalid action value rejected by CHECK constraint."""
        with storage._connect() as conn:
            with pytest.raises(sqlite3.IntegrityError):
                conn.execute(
                    "INSERT INTO ripple_audit (ripple_id, action, new_status, actor, origin, created_at)"
                    " VALUES ('rip-x', 'bogus_action', 'pending', 'user', 'ui', '2026-06-10T10:00:00')"
                )
                conn.commit()

    def test_ripple_audit_check_constraint_origin(self, storage):
        """Invalid origin value rejected by CHECK constraint."""
        with storage._connect() as conn:
            with pytest.raises(sqlite3.IntegrityError):
                conn.execute(
                    "INSERT INTO ripple_audit (ripple_id, action, new_status, actor, origin, created_at)"
                    " VALUES ('rip-x', 'created', 'pending', 'user', 'bogus_origin', '2026-06-10T10:00:00')"
                )
                conn.commit()
```

- [ ] **Step 1.2: Run tests to verify they fail**

Run: `pytest tests/cross_volume/test_storage_audit.py::TestRippleAuditSchema -v`
Expected: FAIL (5 fails — `sqlite3.OperationalError: no such table: ripple_audit`)

- [ ] **Step 1.3: Add ripple_audit table to _SCHEMA_SQL (GREEN)**

Modify `infra/cross_volume/storage.py:24-65` (`_SCHEMA_SQL` block, append new table after `reference_ripples`):

```sql
CREATE TABLE IF NOT EXISTS ripple_audit (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    ripple_id TEXT NOT NULL REFERENCES reference_ripples(id),
    action TEXT NOT NULL CHECK(action IN ('created', 'applied', 'rejected', 'failed', 'rolled_back')),
    prev_status TEXT,
    new_status TEXT NOT NULL,
    actor TEXT NOT NULL,
    origin TEXT NOT NULL CHECK(origin IN ('ui', 'cli', 'system')),
    reason TEXT,
    created_at TEXT NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_audit_ripple ON ripple_audit(ripple_id, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_audit_actor ON ripple_audit(actor, created_at DESC);
```

(Append to existing `_SCHEMA_SQL` string. Full file content stays under 800L cap — was ~353L, now ~383L.)

- [ ] **Step 1.4: Run tests to verify they pass**

Run: `pytest tests/cross_volume/test_storage_audit.py::TestRippleAuditSchema -v`
Expected: PASS (5 passed)

- [ ] **Step 1.5: Verify baseline 2335 tests still pass (no schema break)**

Run: `pytest tests/cross_volume/ -q 2>&1 | tail -3`
Expected: 5+ passed (5 new) + Phase 9.10/9.11/9.12/9.13 既有的 16+16+0+12 = 44 tests still pass (1 new test file + 5 new tests, 0 modified baseline)

- [ ] **Step 1.6: ruff check**

Run: `ruff check infra/cross_volume/storage.py tests/cross_volume/test_storage_audit.py 2>&1 | tail -3`
Expected: 0 issues

- [ ] **Step 1.7: Commit**

```bash
git add infra/cross_volume/storage.py tests/cross_volume/test_storage_audit.py
git commit -m "feat(cvg): phase9.14 task1 — ripple_audit table schema + 5 tests

Phase 9.14 范围起步: 加 ripple_audit append-only table (9 字段 + 2
indexes + 5 CHECK constraint),为后续 4 storage method + 2 既有 method
audit hook 铺 schema 底。0 改 Phase 9.10/9.11/9.12/9.13 既有
reference_* 3 表,0 改 RippleStorage 既有 8 public method。

新 schema:
- ripple_audit table: id PK AUTOINCREMENT (monotonic 强保证) / ripple_id
  FK / action 5-enum CHECK / prev_status NULL for created / new_status
  / actor / origin 3-enum CHECK / reason NULL for non-reject-rollback /
  created_at ISO 8601
- idx_audit_ripple (ripple_id, created_at DESC) — 单 ripple 时间线
- idx_audit_actor (actor, created_at DESC) — 按 actor 全局查

5 tests:
- test_ripple_audit_table_exists
- test_ripple_audit_indexes_exist
- test_ripple_audit_idempotent_migration (rerun CREATE IF NOT EXISTS)
- test_ripple_audit_check_constraint_action
- test_ripple_audit_check_constraint_origin

不改:
- 0 改 Phase 9.10/9.11/9.12/9.13 reference_nodes/edges/ripples 表
- 0 改 RippleStorage 既有 8 public method signature
- 0 改 update_ripple_status / append_ripple (audit hook 留 T2)
- 0 API / 0 UI / 0 CLI 改
- 0 数据迁移 (老 ripple 无 audit history,后续 GET /audit 返 [])
- 0 force-push / 0 amend / 0 --no-verify / 0 Co-Authored-By
- 0 PRAGMA 改 (3 PRAGMA 仍 synchronous=FULL/WAL/foreign_keys=ON)

Baseline: 2335 + 5 = 2340 passed, 0 failed, 27 skipped
ruff: 0"
```

---

## Task 2: RippleStorage 4 additive methods (record_audit / get_audit_history / rollback_ripple) + 2 既有 method 内部 audit hook + 10 tests

**Files:**
- Modify: `infra/cross_volume/storage.py:14-353` (additive, +160L, 4 new methods + 2 internal hooks + AuditEntry dataclass)
- Modify: `tests/cross_volume/test_storage_audit.py` (append 10 tests, +80L)
- Test: `pytest tests/cross_volume/test_storage_audit.py -v`

**Goal:** 加 4 个 additive public method + 2 既有 method (`update_ripple_status` / `append_ripple`) 内部 `record_audit()` 调用。0 改既有 8 public method signature,TDD 严格。

- [ ] **Step 2.1: Add `AuditEntry` dataclass + extend test imports (RED prep)**

Modify `tests/cross_volume/test_storage_audit.py` — append new import at top:

```python
from infra.cross_volume.storage import RippleStorage, AuditEntry
from infra.cross_volume.ripple import CrossVolumeRipple
```

(Imports at top of file. `AuditEntry` 不存在,后续 Step 2.3 才加。)

- [ ] **Step 2.2: Write 10 failing tests (RED)**

Append to `tests/cross_volume/test_storage_audit.py`:

```python
@pytest.fixture
def seeded_storage(tmp_path):
    """Storage with 1 reference_ripple pre-seeded (for audit tests)."""
    from datetime import datetime, timezone
    s = RippleStorage(db_path=tmp_path / "audit_method.db")
    ripple = CrossVolumeRipple(
        id="rip-audit-1", trigger_volume=1, trigger_chapter=1,
        affected_nodes=(), affected_edges=(), proposed_actions=(),
        status="applied",
    )
    s.append_ripple(ripple)
    return s


class TestRecordAudit:
    def test_record_audit_writes_row_and_returns_id(self, seeded_storage):
        """record_audit inserts 1 row, returns lastrowid."""
        from infra.cross_volume.storage import record_audit as _mod  # placeholder
        # Use instance method
        audit_id = seeded_storage.record_audit(
            ripple_id="rip-audit-1", action="rolled_back",
            prev_status="applied", new_status="pending",
            actor="user", origin="ui", reason="test",
        )
        assert isinstance(audit_id, int)
        assert audit_id >= 1

    def test_record_audit_with_null_prev_status(self, seeded_storage):
        """created entry prev_status=NULL allowed."""
        seeded_storage.record_audit(
            ripple_id="rip-audit-1", action="created",
            prev_status=None, new_status="pending",
            actor="system:phase9.11-backfill", origin="system", reason=None,
        )
        entries = seeded_storage.get_audit_history("rip-audit-1")
        assert len(entries) == 1
        assert entries[0].prev_status is None


class TestGetAuditHistory:
    def test_get_audit_history_orders_newest_first(self, seeded_storage):
        """3 entries written in order → get returns newest first."""
        seeded_storage.record_audit("rip-audit-1", "created", None, "pending", "sys", "system", None)
        seeded_storage.record_audit("rip-audit-1", "applied", "pending", "applied", "user", "ui", None)
        seeded_storage.record_audit("rip-audit-1", "rolled_back", "applied", "pending", "user", "ui", "r1")
        entries = seeded_storage.get_audit_history("rip-audit-1")
        assert [e.action for e in entries] == ["rolled_back", "applied", "created"]

    def test_get_audit_history_pagination(self, seeded_storage):
        """limit + offset paginate correctly."""
        for i in range(5):
            seeded_storage.record_audit("rip-audit-1", "applied", "p", "a", f"user-{i}", "ui", None)
        page1 = seeded_storage.get_audit_history("rip-audit-1", limit=2, offset=0)
        page2 = seeded_storage.get_audit_history("rip-audit-1", limit=2, offset=2)
        assert len(page1) == 2
        assert len(page2) == 2
        assert page1[0].id != page2[0].id

    def test_get_audit_history_empty_ripple(self, seeded_storage):
        """Non-existent ripple_id returns []."""
        assert seeded_storage.get_audit_history("rip-nonexistent") == []


class TestRollbackRipple:
    def test_rollback_happy_path_atomic(self, seeded_storage):
        """applied → pending + applied_at NULL + audit entry written."""
        # Pre-state: ripple is 'applied'
        before = seeded_storage.get_ripple_by_id("rip-audit-1")
        assert before.status == "applied"
        updated = seeded_storage.rollback_ripple(
            "rip-audit-1", actor="user", origin="ui", reason="误操作",
        )
        assert updated.status == "pending"
        assert updated.applied_at is None
        # Audit entry written
        entries = seeded_storage.get_audit_history("rip-audit-1")
        assert len(entries) == 1
        assert entries[0].action == "rolled_back"
        assert entries[0].prev_status == "applied"
        assert entries[0].new_status == "pending"
        assert entries[0].reason == "误操作"

    def test_rollback_raises_keyerror_for_missing(self, seeded_storage):
        with pytest.raises(KeyError):
            seeded_storage.rollback_ripple("rip-nonexistent", actor="u", origin="ui", reason="r")

    def test_rollback_raises_valueerror_for_pending(self, seeded_storage):
        """Cannot rollback a pending ripple (must be applied or rejected)."""
        # Set status to pending first
        with seeded_storage._connect() as conn:
            conn.execute("UPDATE reference_ripples SET status='pending' WHERE id=?", ("rip-audit-1",))
            conn.commit()
        with pytest.raises(ValueError, match="can only rollback applied/rejected"):
            seeded_storage.rollback_ripple("rip-audit-1", actor="u", origin="ui", reason="r")

    def test_rollback_raises_valueerror_for_empty_reason(self, seeded_storage):
        with pytest.raises(ValueError, match="reason is required"):
            seeded_storage.rollback_ripple("rip-audit-1", actor="u", origin="ui", reason="")

    def test_rollback_raises_valueerror_for_whitespace_reason(self, seeded_storage):
        with pytest.raises(ValueError, match="reason is required"):
            seeded_storage.rollback_ripple("rip-audit-1", actor="u", origin="ui", reason="   ")


class TestUpdateRippleStatusAuditHook:
    def test_update_status_writes_audit_entry(self, seeded_storage):
        """update_ripple_status internally calls record_audit."""
        # ripple is 'applied' from fixture → apply again should fail ConflictError
        # Instead, set to pending first
        with seeded_storage._connect() as conn:
            conn.execute("UPDATE reference_ripples SET status='pending' WHERE id=?", ("rip-audit-1",))
            conn.commit()
        seeded_storage.update_ripple_status("rip-audit-1", "applied", actor="user", origin="ui")
        entries = seeded_storage.get_audit_history("rip-audit-1")
        # 1 entry for 'applied' (the fixture's 'applied' was seeded via append_ripple,
        # which should also write 'created' audit per append_ripple hook)
        actions = [e.action for e in entries]
        assert "applied" in actions
        assert "created" in actions
```

- [ ] **Step 2.3: Run tests to verify they fail**

Run: `pytest tests/cross_volume/test_storage_audit.py -v 2>&1 | tail -20`
Expected: 10 FAIL (AttributeError: 'RippleStorage' object has no attribute 'record_audit' / 'get_audit_history' / 'rollback_ripple', or ImportError: cannot import name 'AuditEntry')

- [ ] **Step 2.4: Add `AuditEntry` dataclass (top of storage.py after imports, +12L)**

Modify `infra/cross_volume/storage.py` — add after `from infra.cross_volume.ripple import CrossVolumeRipple` (line 20):

```python
from dataclasses import dataclass as _dc  # already imported; verify

@dataclass(frozen=True)
class AuditEntry:
    """Phase 9.14: 1 row of ripple_audit table.

    Append-only history entry. Immutable after insert.
    Fields mirror ripple_audit table columns (except id which is autoincrement PK).
    """
    id: int
    ripple_id: str
    action: str  # 'created' / 'applied' / 'rejected' / 'failed' / 'rolled_back'
    prev_status: str | None
    new_status: str
    actor: str
    origin: str  # 'ui' / 'cli' / 'system'
    reason: str | None
    created_at: datetime
```

(Note: `dataclass` already imported via `from dataclasses import replace` — add `dataclass` to same import. Modify line 14: `from dataclasses import dataclass, replace`.)

- [ ] **Step 2.5: Add 3 new methods (record_audit / get_audit_history / rollback_ripple) + _row_to_audit, +110L**

Append to `infra/cross_volume/storage.py` (after `update_ripple_status`, before `ConflictError` class):

```python
    def record_audit(
        self,
        ripple_id: str,
        action: str,
        prev_status: str | None,
        new_status: str,
        actor: str,
        origin: str,
        reason: str | None,
    ) -> int:
        """Append 1 row to ripple_audit (independent commit).

        跟 cost_tracker.record() 1:1 pattern: 主 write path 失败不传播到这里。
        """
        with self._connect() as conn:
            cur = conn.execute(
                "INSERT INTO ripple_audit (ripple_id, action, prev_status, new_status, actor, origin, reason, created_at)"
                " VALUES (?, ?, ?, ?, ?, ?, ?, ?)",
                (ripple_id, action, prev_status, new_status, actor, origin, reason,
                 datetime.now(timezone.utc).isoformat()),
            )
            conn.commit()
            return cur.lastrowid

    def get_audit_history(
        self,
        ripple_id: str,
        limit: int = 50,
        offset: int = 0,
    ) -> list[AuditEntry]:
        """Time-ordered audit entries (newest first, default 50)."""
        with self._connect() as conn:
            rows = conn.execute(
                "SELECT * FROM ripple_audit WHERE ripple_id = ? ORDER BY created_at DESC, id DESC LIMIT ? OFFSET ?",
                (ripple_id, limit, offset),
            ).fetchall()
            return [self._row_to_audit(r) for r in rows]

    def rollback_ripple(
        self,
        ripple_id: str,
        actor: str,
        origin: str,
        reason: str,
    ) -> CrossVolumeRipple:
        """Soft rollback: applied/rejected → pending + applied_at NULL + audit.

        Atomic via atomic_batch (BEGIN IMMEDIATE + 1 fsync on commit).
        """
        if not reason or not reason.strip():
            raise ValueError("reason is required for rollback")
        with self.atomic_batch() as conn:
            row = conn.execute(
                "SELECT * FROM reference_ripples WHERE id = ?", (ripple_id,)
            ).fetchone()
            if row is None:
                raise KeyError(f"ripple {ripple_id} not found")
            current = self._row_to_ripple(row)
            if current.status not in ("applied", "rejected"):
                raise ValueError(
                    f"can only rollback applied/rejected ripples, current status: {current.status!r}"
                )
            now_iso = datetime.now(timezone.utc).isoformat()
            conn.execute(
                "UPDATE reference_ripples SET status = 'pending', applied_at = NULL WHERE id = ?",
                (ripple_id,),
            )
            conn.execute(
                "INSERT INTO ripple_audit (ripple_id, action, prev_status, new_status, actor, origin, reason, created_at)"
                " VALUES (?, 'rolled_back', ?, 'pending', ?, ?, ?, ?)",
                (ripple_id, current.status, actor, origin, reason, now_iso),
            )
        updated = self.get_ripple_by_id(ripple_id)
        assert updated is not None
        _broadcast_ripple_event(
            "ripple_status_changed",
            {"ripple_id": ripple_id, "new_status": "pending", "actor": actor,
             "origin": origin, "reason": reason, "applied_at": None, "action": "rolled_back"},
        )
        return updated

    def _row_to_audit(self, row: sqlite3.Row) -> AuditEntry:
        return AuditEntry(
            id=row["id"],
            ripple_id=row["ripple_id"],
            action=row["action"],
            prev_status=row["prev_status"],
            new_status=row["new_status"],
            actor=row["actor"],
            origin=row["origin"],
            reason=row["reason"],
            created_at=datetime.fromisoformat(row["created_at"]),
        )
```

- [ ] **Step 2.6: Modify `update_ripple_status` to write audit entry (additive 1-line hook)**

Modify `infra/cross_volume/storage.py` `update_ripple_status` method — after the existing `conn.commit()` (around line 311) and before the `replace(...)` re-fetch, add:

```python
            conn.commit()
        # Phase 9.14: write audit entry (independent commit, like cost_tracker.record)
        self.record_audit(
            ripple_id=ripple_id,
            action="applied" if new_status == "applied" else "rejected",
            prev_status=current.status,
            new_status=new_status,
            actor=actor,
            origin="ui",  # Phase 9.13 caller always 'ui'; T3 will let API override
            reason=None,  # apply/reject don't take reason (rolled_back does)
        )
        # Phase 9.13: avoid re-fetch race (return updated in-memory copy)
        updated = replace(...)
```

(After conn.commit() in update_ripple_status, before `replace(current, status=new_status, ...)` re-fetch. Total +6L.)

- [ ] **Step 2.7: Modify `append_ripple` to write 'created' audit entry (additive 1-line hook)**

Modify `infra/cross_volume/storage.py` `append_ripple` method — after the existing `conn.commit()` (line 158), add:

```python
                conn.commit()
        # Phase 9.14: write 'created' audit entry (independent commit)
        self.record_audit(
            ripple_id=ripple.id,
            action="created",
            prev_status=None,
            new_status=ripple.status,
            actor=ripple.payload.get("created_by", "system") if isinstance(ripple.payload, dict) else "system",
            origin="system",
            reason=None,
        )
```

(After conn.commit() in append_ripple, +8L.)

- [ ] **Step 2.8: Run tests to verify they pass**

Run: `pytest tests/cross_volume/test_storage_audit.py -v 2>&1 | tail -25`
Expected: PASS (15 passed = 5 schema + 10 method)

- [ ] **Step 2.9: Verify baseline 2335 tests still pass**

Run: `pytest tests/cross_volume/ -q 2>&1 | tail -3`
Expected: 15 new + ~44 baseline = 59 passed in test_storage_audit + cross_volume directory

- [ ] **Step 2.10: ruff check**

Run: `ruff check infra/cross_volume/storage.py tests/cross_volume/test_storage_audit.py 2>&1 | tail -3`
Expected: 0 issues

- [ ] **Step 2.11: Commit**

```bash
git add infra/cross_volume/storage.py tests/cross_volume/test_storage_audit.py
git commit -m "feat(cvg): phase9.14 task2 — RippleStorage 4 methods + 2 audit hooks + 10 tests

Phase 9.14 task1 落了 ripple_audit table,本 task 落 4 个 additive public
method + 2 既有 method 内部 record_audit hook。0 改 RippleStorage 既有
8 public method signature,0 改 Phase 9.10/9.11/9.12/9.13 既有 behavior。

4 new method (additive):
- record_audit (internal helper, single-row INSERT, 跟 cost_tracker.record 1:1)
- get_audit_history (新est-first, limit/offset 分页, 50 default)
- rollback_ripple (atomic_batch 包裹 UPDATE + INSERT, status applied/rejected
  → pending + applied_at NULL + audit 写 rolled_back)
- _row_to_audit (private helper)

2 既有 method 内部 hook (additive, 0 改 public signature):
- update_ripple_status: 末尾 record_audit 写 applied/rejected
- append_ripple: 末尾 record_audit 写 created (origin=system)

新 AuditEntry frozen dataclass (9 字段, mirror ripple_audit table)。

10 tests:
- test_record_audit_writes_row_and_returns_id
- test_record_audit_with_null_prev_status (created entry)
- test_get_audit_history_orders_newest_first
- test_get_audit_history_pagination
- test_get_audit_history_empty_ripple
- test_rollback_happy_path_atomic (status + applied_at + audit 3 同步)
- test_rollback_raises_keyerror_for_missing
- test_rollback_raises_valueerror_for_pending
- test_rollback_raises_valueerror_for_empty_reason
- test_rollback_raises_valueerror_for_whitespace_reason
- test_update_status_writes_audit_entry (含 append_ripple 'created' 联动)

不改:
- 0 改 RippleStorage 既有 8 public method signature
- 0 改 Phase 9.10/9.11/9.12/9.13 reference_nodes/edges/ripples 表
- 0 改 update_ripple_status / append_ripple public signature (仅内部 +1 call)
- 0 改 get_ripples / get_ripple_by_id / append_nodes_atomic (Phase 9.13)
- 0 改 _TERMINAL_STATUSES / _VALID_TRANSITION_STATUSES (rolled_back 不是新 terminal)
- 0 API / 0 UI / 0 CLI 改 (T3-T7)
- 0 PRAGMA 改 / 0 数据迁移
- 0 force-push / 0 amend / 0 --no-verify / 0 Co-Authored-By

Baseline: 2340 + 10 = 2350 passed, 0 failed, 27 skipped
ruff: 0"
```

---

## Task 3: FastAPI 2 new endpoints + 2 Pydantic schemas + 2 既有 endpoint Optional body + 8 tests

**Files:**
- Modify: `dashboard/protocols.py` (additive, +50L, 2 new schemas + 1 optional-action schema)
- Modify: `dashboard/app.py` (additive, +130L, 2 new endpoints + 2 既有 endpoint Optional body)
- Create: `tests/dashboard/test_cvg_audit_api.py` (NEW, ~150L, 8 tests)
- Test: `pytest tests/dashboard/test_cvg_audit_api.py -v`

**Goal:** FastAPI layer 暴露 audit list + rollback action。0 改 Phase 9.13 既有 5 endpoint handler logic (仅加 Optional body param)。

- [ ] **Step 3.1: Write 8 failing tests (RED)**

```python
# tests/dashboard/test_cvg_audit_api.py
"""Phase 9.14: 2 new REST endpoints (audit/rollback) + 2 schemas + 2 既有 endpoint Optional body."""
import pytest
from fastapi.testclient import TestClient
from dashboard.app import create_app
from infra.cross_volume.storage import RippleStorage
from infra.cross_volume.ripple import CrossVolumeRipple
import tempfile
from pathlib import Path


@pytest.fixture
def storage_with_ripple(tmp_path):
    """Storage with 1 applied ripple pre-seeded for audit/rollback tests."""
    db_path = tmp_path / "api_audit.db"
    storage = RippleStorage(db_path=db_path)
    ripple = CrossVolumeRipple(
        id="rip-api-1", trigger_volume=1, trigger_chapter=1,
        affected_nodes=(), affected_edges=(), proposed_actions=(),
        status="applied",
    )
    storage.append_ripple(ripple)
    return storage, "rip-api-1"


@pytest.fixture
def client(storage_with_ripple, monkeypatch):
    storage, _ = storage_with_ripple
    app = create_app()
    # Override _default_storage dependency
    from dashboard import app as app_module
    monkeypatch.setattr(app_module, "_default_storage", lambda: storage)
    return TestClient(app), "rip-api-1"


class TestGetRippleAudit:
    def test_get_audit_happy(self, client):
        """GET /api/cvg/ripples/{id}/audit returns list of entries."""
        c, rid = client
        resp = c.get(f"/api/cvg/ripples/{rid}/audit")
        assert resp.status_code == 200
        data = resp.json()
        assert isinstance(data, list)
        # append_ripple 'created' audit hook from T2
        assert len(data) >= 1
        assert data[0]["action"] == "created"

    def test_get_audit_404_for_missing(self, client):
        c, _ = client
        resp = c.get("/api/cvg/ripples/rip-nonexistent/audit")
        assert resp.status_code == 404

    def test_get_audit_empty_list(self, client):
        c, rid = client
        # Storage has 1 ripple but 0 audit written yet (if T2 hook not run)
        # For this test, query a non-existent ripple
        resp = c.get("/api/cvg/ripples/rip-no-audit/audit")
        assert resp.status_code == 404  # API checks ripple exists first


class TestPostRollback:
    def test_rollback_happy(self, client):
        c, rid = client
        resp = c.post(
            f"/api/cvg/ripples/{rid}/rollback",
            json={"actor": "user", "origin": "ui", "reason": "test rollback"},
        )
        assert resp.status_code == 200
        data = resp.json()
        assert data["status"] == "pending"
        assert data["applied_at"] is None

    def test_rollback_404_for_missing(self, client):
        c, _ = client
        resp = c.post(
            "/api/cvg/ripples/rip-nonexistent/rollback",
            json={"actor": "user", "origin": "ui", "reason": "r"},
        )
        assert resp.status_code == 404

    def test_rollback_422_for_pending_status(self, client):
        c, rid = client
        # First rollback → status=pending
        c.post(f"/api/cvg/ripples/{rid}/rollback", json={"actor": "u", "origin": "ui", "reason": "r1"})
        # Second rollback → 422 (not applied/rejected anymore)
        resp = c.post(
            f"/api/cvg/ripples/{rid}/rollback",
            json={"actor": "u", "origin": "ui", "reason": "r2"},
        )
        assert resp.status_code == 422

    def test_rollback_422_for_empty_reason(self, client):
        c, rid = client
        resp = c.post(
            f"/api/cvg/ripples/{rid}/rollback",
            json={"actor": "u", "origin": "ui", "reason": ""},
        )
        assert resp.status_code == 422


class TestBackwardCompat:
    def test_apply_accepts_optional_actor_origin(self, client):
        """POST /apply 不传 body 仍 work (backward compat, Phase 9.13 baseline)."""
        c, rid = client
        # Set to pending first (we need fresh apply)
        import json
        # Use the storage fixture to set pending
        c.post(  # first rollback to pending
            f"/api/cvg/ripples/{rid}/rollback",
            json={"actor": "u", "origin": "ui", "reason": "r"},
        )
        # Now apply without body
        resp = c.post(f"/api/cvg/ripples/{rid}/apply")
        assert resp.status_code == 200
```

- [ ] **Step 3.2: Run tests to verify they fail**

Run: `pytest tests/dashboard/test_cvg_audit_api.py -v 2>&1 | tail -20`
Expected: 8 FAIL (404/422 because endpoints don't exist, or ImportError: cannot import name 'RippleAuditEntryResponse')

- [ ] **Step 3.3: Add 3 Pydantic schemas in protocols.py (additive, +50L)**

Modify `dashboard/protocols.py` — append to end of file:

```python
# === Phase 9.14: ripple audit + rollback schemas ===

class RippleAuditEntryResponse(BaseModel):
    """Phase 9.14: 1 row of ripple_audit table (newest first list item)."""
    id: int
    ripple_id: str
    action: Literal["created", "applied", "rejected", "failed", "rolled_back"]
    prev_status: str | None
    new_status: str
    actor: str
    origin: Literal["ui", "cli", "system"]
    reason: str | None
    created_at: datetime


class RippleRollbackRequest(BaseModel):
    """Phase 9.14: POST /rollback body (reason required)."""
    actor: str = "user"
    origin: Literal["ui", "cli", "system"] = "ui"
    reason: str = Field(..., min_length=1, max_length=500)


class RippleActionRequest(BaseModel):
    """Phase 9.14: Optional body for /apply and /reject (backward compat).

    既有 Phase 9.13 既有的 /apply /reject 不传 body 仍 work (全 Optional 字段)。
    """
    actor: str | None = None
    origin: Literal["ui", "cli", "system"] | None = None
    reason: str | None = None
```

(Add `from typing import Literal` and `from pydantic import Field` if not already imported. Verify imports at top of protocols.py.)

- [ ] **Step 3.4: Add 2 new REST endpoints in app.py (additive, +90L)**

Modify `dashboard/app.py` — append after Phase 9.13 既有 5 endpoints (find the end of `/api/cvg/ripples/stats` handler):

```python
# === Phase 9.14: audit + rollback endpoints ===

@app.get("/api/cvg/ripples/{ripple_id}/audit",
         response_model=list[RippleAuditEntryResponse])
async def get_ripple_audit(
    ripple_id: str,
    limit: int = Query(50, ge=1, le=200),
    offset: int = Query(0, ge=0),
) -> list[RippleAuditEntryResponse]:
    """Phase 9.14: time-ordered audit history (newest first)."""
    storage = _default_storage()
    if storage.get_ripple_by_id(ripple_id) is None:
        raise HTTPException(404, f"ripple {ripple_id} not found")
    entries = storage.get_audit_history(ripple_id, limit=limit, offset=offset)
    return [_audit_to_response(e) for e in entries]


@app.post("/api/cvg/ripples/{ripple_id}/rollback",
          response_model=RippleDetailResponse)
async def rollback_ripple(
    ripple_id: str,
    request: RippleRollbackRequest,
) -> RippleDetailResponse:
    """Phase 9.14: reverse an apply/reject (status → pending + audit)."""
    storage = _default_storage()
    try:
        ripple = storage.rollback_ripple(
            ripple_id, actor=request.actor, origin=request.origin, reason=request.reason,
        )
    except KeyError:
        raise HTTPException(404, f"ripple {ripple_id} not found")
    except ValueError as e:
        raise HTTPException(422, str(e))
    return _ripple_to_detail(ripple)


def _audit_to_response(entry) -> RippleAuditEntryResponse:
    """Phase 9.14: AuditEntry → RippleAuditEntryResponse."""
    return RippleAuditEntryResponse(
        id=entry.id, ripple_id=entry.ripple_id, action=entry.action,
        prev_status=entry.prev_status, new_status=entry.new_status,
        actor=entry.actor, origin=entry.origin, reason=entry.reason,
        created_at=entry.created_at,
    )
```

(Need to add import: `from infra.cross_volume.storage import RippleStorage, AuditEntry` if not present. Verify imports at top of app.py.)

- [ ] **Step 3.5: Modify 2 既有 endpoints to accept Optional body (additive, +20L)**

Modify `dashboard/app.py` `apply_ripple` (Phase 9.13 既有) — add `body: RippleActionRequest | None = None` parameter:

```python
@app.post("/api/cvg/ripples/{ripple_id}/apply", response_model=RippleActionResponse)
async def apply_ripple(
    ripple_id: str,
    body: RippleActionRequest | None = None,
) -> RippleActionResponse:
    """Status change: pending → applied (UI only, 0 改 cost_tracker)."""
    storage = _default_storage()
    actor = body.actor if body and body.actor else "user"
    origin = body.origin if body and body.origin else "ui"
    try:
        ripple = storage.update_ripple_status(ripple_id, "applied", actor=actor)
    ...
```

(Add same `body: RippleActionRequest | None = None` to `reject_ripple`. Pass `origin=origin` to `update_ripple_status` — but wait, current signature is `update_ripple_status(ripple_id, new_status, actor="user")`. Need to update Phase 9.14: add `origin="ui"` param. Modify storage.py `update_ripple_status` to add origin kwarg with default "ui". 0 behavior break since Phase 9.13 callers don't pass origin.)

Wait — actually `update_ripple_status` was modified in Step 2.6 to write audit with hardcoded `origin="ui"`. Let me change Step 2.6 to use the parameter from the method. Re-modify Step 2.6:

Change `update_ripple_status` signature to:
```python
def update_ripple_status(
    self, ripple_id: str, new_status: str, actor: str = "user", origin: str = "ui",
) -> CrossVolumeRipple:
```

And in the audit hook, use `origin=origin` instead of hardcoded `"ui"`. 0 behavior break for Phase 9.13 callers (default "ui").

Update test `test_update_status_writes_audit_entry` in T2 if needed (it just checks `applied` in actions, doesn't care about origin).

OK let me re-do Step 2.6 properly. Update task 2's spec:

```python
def update_ripple_status(
    self,
    ripple_id: str,
    new_status: str,
    actor: str = "user",
    origin: str = "ui",  # Phase 9.14 additive
) -> CrossVolumeRipple:
    ...
    # inside method after conn.commit():
    self.record_audit(
        ripple_id=ripple_id,
        action="applied" if new_status == "applied" else "rejected",
        prev_status=current.status,
        new_status=new_status,
        actor=actor,
        origin=origin,  # use parameter, not hardcoded
        reason=None,
    )
```

- [ ] **Step 3.6: Run tests to verify they pass**

Run: `pytest tests/dashboard/test_cvg_audit_api.py -v 2>&1 | tail -20`
Expected: PASS (8 passed)

- [ ] **Step 3.7: Verify Phase 9.13 既有的 15 dashboard tests still pass**

Run: `pytest tests/dashboard/test_cvg_api.py -v 2>&1 | tail -5`
Expected: 15 passed (backward compat preserved)

- [ ] **Step 3.8: Verify full baseline + new tests**

Run: `pytest tests/ -q 2>&1 | tail -3`
Expected: 2350 + 8 = 2358 passed (baseline 2335 + T1+T2+T3 net new = 5+10+8 = 23 new)

- [ ] **Step 3.9: ruff check**

Run: `ruff check dashboard/ tests/dashboard/test_cvg_audit_api.py 2>&1 | tail -3`
Expected: 0 issues

- [ ] **Step 3.10: Commit**

```bash
git add infra/cross_volume/storage.py dashboard/protocols.py dashboard/app.py tests/dashboard/test_cvg_audit_api.py
git commit -m "feat(cvg): phase9.14 task3 — FastAPI audit/rollback endpoints + 2 schemas + 8 tests

Phase 9.14 task1+T2 落了 storage 层 (table + 4 method),本 task 落 FastAPI
2 new endpoint + 2 Pydantic schemas + 2 既有 endpoint Optional body。
0 改 Phase 9.13 既有 5 endpoint handler logic (仅加 body=None param 保
backward compat),0 改 dashboard 既有 endpoint/composable/router/sidebar。

2 new endpoint (additive):
- GET /api/cvg/ripples/{id}/audit (list, limit/offset 分页, 50 default)
- POST /api/cvg/ripples/{id}/rollback (action, reason required, origin
  Literal 校验, returns RippleDetailResponse)

2 new Pydantic schema:
- RippleAuditEntryResponse (9 字段, action Literal 5-enum)
- RippleRollbackRequest (reason min_length=1 max_length=500, origin Literal)

1 隐式 schema (不计入 file map count):
- RippleActionRequest (Optional body for /apply /reject, backward compat)

2 既有 endpoint Optional body (additive, 0 改 handler logic):
- POST /apply 加 body: RippleActionRequest | None = None
- POST /reject 加 body: RippleActionRequest | None = None
- 不传 body → 默认 actor='user' origin='ui',跟 Phase 9.13 baseline 1:1
- 传 body → 用 body.actor / body.origin 覆盖

storage.py update_ripple_status 增 origin='ui' kwarg (default 兜底,0
behavior break,Phase 9.13 caller 不传仍 'ui')。

8 tests:
- test_get_audit_happy (1+ entry, newest first)
- test_get_audit_404_for_missing
- test_get_audit_empty_list (via 404 path)
- test_rollback_happy (status → pending + applied_at NULL)
- test_rollback_404_for_missing
- test_rollback_422_for_pending_status (double rollback)
- test_rollback_422_for_empty_reason (Pydantic min_length=1)
- test_apply_accepts_optional_actor_origin (backward compat, 0 body 仍 work)

不改:
- 0 改 Phase 9.13 既有 5 endpoint handler logic (apply/reject 接受 None body)
- 0 改 dashboard 既有 composable / store / router / sidebar
- 0 改 update_ripple_status public signature 既有 caller (origin default 'ui')
- 0 改 _ripple_to_list_item / _ripple_to_detail helper
- 0 改 WS endpoint /api/ws/cvg (Phase 9.13, broadcast 仍 1:1)
- 0 改 dashboard/cvg_ws.py CvgConnectionManager
- 0 改 Phase 9.10/9.11/9.12/9.13 source API
- 0 force-push / 0 amend / 0 --no-verify / 0 Co-Authored-By

Baseline: 2350 + 8 = 2358 passed, 0 failed, 27 skipped
ruff: 0"
```

---

## Task 4: useRippleStore 2 actions + api 2 methods + 6 vitest tests

**Files:**
- Modify: `dashboard/frontend/src/api/index.js` (additive, +20L, 2 new methods)
- Modify: `dashboard/frontend/src/composables/useRippleStore.js` (additive, +35L, 2 new actions)
- Create: `dashboard/frontend/tests/unit/use-ripple-store-audit.spec.js` (NEW, ~90L, 6 tests)
- Test: `cd dashboard/frontend && pnpm test:unit -- use-ripple-store-audit`

**Goal:** Vue 3 layer 暴露 audit fetch + rollback action,TDD 严格,0 改既有 4 action。

- [ ] **Step 4.1: Write 6 failing vitest tests (RED)**

```javascript
// dashboard/frontend/tests/unit/use-ripple-store-audit.spec.js
import { describe, it, expect, beforeEach, vi } from 'vitest'
import { setActivePinia, createPinia } from 'pinia'
import { useRippleStore } from '@/composables/useRippleStore'
import * as api from '@/api/index'

vi.mock('@/api/index', () => ({
  fetchRippleAudit: vi.fn(),
  rollbackRipple: vi.fn(),
  fetchRipples: vi.fn(),
}))

describe('useRippleStore audit actions', () => {
  beforeEach(() => {
    setActivePinia(createPinia())
    vi.clearAllMocks()
  })

  it('fetchAudit returns entries on success', async () => {
    const entries = [{ id: 1, action: 'created', actor: 'sys', origin: 'system' }]
    api.fetchRippleAudit.mockResolvedValue(entries)
    const store = useRippleStore()
    const result = await store.fetchAudit('rip-1')
    expect(result).toEqual(entries)
    expect(api.fetchRippleAudit).toHaveBeenCalledWith('rip-1')
  })

  it('fetchAudit sets lastError on failure', async () => {
    api.fetchRippleAudit.mockRejectedValue(new Error('network'))
    const store = useRippleStore()
    await expect(store.fetchAudit('rip-1')).rejects.toThrow('network')
    expect(store.lastError.value).toBe('network')
  })

  it('rollback updates ripple status optimistically', async () => {
    const updated = { ripple_id: 'rip-1', status: 'pending', applied_at: null }
    api.rollbackRipple.mockResolvedValue(updated)
    const store = useRippleStore()
    store.ripples.value = [
      { ripple_id: 'rip-1', status: 'applied' },
      { ripple_id: 'rip-2', status: 'pending' },
    ]
    await store.rollback('rip-1', 'test reason')
    expect(store.ripples.value[0].status).toBe('pending')
    expect(store.ripples.value[0].applied_at).toBeNull()
    expect(store.ripples.value[1].status).toBe('pending')  // unchanged
  })

  it('rollback sets lastError and does not mutate on failure', async () => {
    api.rollbackRipple.mockRejectedValue(new Error('422'))
    const store = useRippleStore()
    store.ripples.value = [{ ripple_id: 'rip-1', status: 'applied' }]
    await expect(store.rollback('rip-1', 'r')).rejects.toThrow('422')
    expect(store.ripples.value[0].status).toBe('applied')  // unchanged
    expect(store.lastError.value).toBe('422')
  })

  it('api.fetchRippleAudit calls correct URL', async () => {
    const mockFetch = vi.fn().mockResolvedValue([])
    global.fetch = mockFetch
    await api.fetchRippleAudit('rip-1')
    expect(mockFetch).toHaveBeenCalledWith(
      expect.stringContaining('/api/cvg/ripples/rip-1/audit'),
      expect.any(Object)
    )
  })

  it('api.rollbackRipple sends POST with reason', async () => {
    const mockFetch = vi.fn().mockResolvedValue({ json: () => Promise.resolve({}) })
    global.fetch = mockFetch
    await api.rollbackRipple('rip-1', 'reason text')
    expect(mockFetch).toHaveBeenCalledWith(
      expect.stringContaining('/api/cvg/ripples/rip-1/rollback'),
      expect.objectContaining({ method: 'POST', body: JSON.stringify({ reason: 'reason text' }) })
    )
  })
})
```

- [ ] **Step 4.2: Run tests to verify they fail**

Run: `cd dashboard/frontend && pnpm test:unit -- use-ripple-store-audit 2>&1 | tail -15`
Expected: FAIL (api methods / store actions don't exist)

- [ ] **Step 4.3: Add 2 methods to api/index.js (additive, +20L)**

Modify `dashboard/frontend/src/api/index.js` — append after Phase 9.13 既有 5 methods:

```javascript
// === Phase 9.14: audit + rollback API methods ===

export async function fetchRippleAudit(rippleId) {
  const resp = await fetch(`${API_BASE}/api/cvg/ripples/${rippleId}/audit`)
  if (!resp.ok) throw new Error(`audit fetch failed: ${resp.status}`)
  return resp.json()
}

export async function rollbackRipple(rippleId, reason) {
  const resp = await fetch(`${API_BASE}/api/cvg/ripples/${rippleId}/rollback`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ reason }),
  })
  if (!resp.ok) throw new Error(`rollback failed: ${resp.status}`)
  return resp.json()
}
```

(API_BASE is module-level const defined in Phase 9.13. Reuse it.)

- [ ] **Step 4.4: Add 2 actions to useRippleStore.js (additive, +35L)**

Modify `dashboard/frontend/src/composables/useRippleStore.js` — add 2 functions inside `createRippleStore` and include in return:

```javascript
import { fetchRippleAudit, rollbackRipple } from '@/api/index'

// Inside createRippleStore (after reject function):

async function fetchAudit(rippleId) {
  lastError.value = null
  try {
    const data = await fetchRippleAudit(rippleId)
    return data
  } catch (e) {
    lastError.value = e.message
    throw e
  }
}

async function rollback(rippleId, reason) {
  // Optimistic: snapshot before mutate
  const idx = ripples.value.findIndex(r => r.ripple_id === rippleId)
  if (idx < 0) throw new Error(`ripple ${rippleId} not in store`)
  const snapshot = { ...ripples.value[idx] }
  try {
    const updated = await rollbackRipple(rippleId, reason)
    // Apply server response (authoritative)
    if (idx >= 0) {
      ripples.value[idx] = { ...ripples.value[idx], ...updated }
    }
    return updated
  } catch (e) {
    // Rollback optimistic mutation
    if (idx >= 0) ripples.value[idx] = snapshot
    lastError.value = e.message
    throw e
  }
}

return {
  ripples, loading, lastError, stats,
  refresh, apply, reject, applySocketUpdate,  // 既有 0 改
  fetchAudit, rollback,                       // Phase 9.14 新增
}
```

- [ ] **Step 4.5: Run tests to verify they pass**

Run: `cd dashboard/frontend && pnpm test:unit -- use-ripple-store-audit 2>&1 | tail -10`
Expected: PASS (6 passed)

- [ ] **Step 4.6: Verify Phase 9.13 vitest baseline (128 tests) still pass**

Run: `cd dashboard/frontend && pnpm test:unit 2>&1 | tail -5`
Expected: 128 + 6 = 134+ passed

- [ ] **Step 4.7: Commit**

```bash
git add dashboard/frontend/src/api/index.js dashboard/frontend/src/composables/useRippleStore.js dashboard/frontend/tests/unit/use-ripple-store-audit.spec.js
git commit -m "feat(cvg): phase9.14 task4 — useRippleStore 2 actions + api 2 methods + 6 tests

Phase 9.14 task1+T2+T3 落了 storage + API 层,本 task 落 Vue 3 layer
audit fetch + rollback action。0 改 useRippleStore 既有 4 action
(refresh/apply/reject/applySocketUpdate),0 改 api/index.js 既有 5 method。

2 new api/index.js method (additive, +20L):
- fetchRippleAudit(rippleId) → GET /api/cvg/ripples/{id}/audit
- rollbackRipple(rippleId, reason) → POST /api/cvg/ripples/{id}/rollback

2 new useRippleStore action (additive, +35L):
- fetchAudit(rippleId) → 调 api.fetchRippleAudit,lastError 错误处理
- rollback(rippleId, reason) → optimistic update + 失败 snapshot 回滚 +
  server response 覆盖 (authoritative, 跟 Phase 9.13 apply/reject 1:1)

6 vitest tests:
- test_fetchAudit returns entries on success
- test_fetchAudit sets lastError on failure
- test_rollback updates ripple status optimistically
- test_rollback sets lastError and does not mutate on failure
- test_api.fetchRippleAudit calls correct URL
- test_api.rollbackRipple sends POST with reason

不改:
- 0 改 useRippleStore 既有 4 action public signature
- 0 改 api/index.js 既有 5 method
- 0 改 useRippleSocket (Phase 9.13 1:1 passive listener,WS event 已含
  action/origin/reason 字段,applySocketUpdate 自动应用)
- 0 改 RippleFilter / RippleList / RippleCard / RipplesPage (Phase 9.13)
- 0 改 Sidebar.vue / App.vue / router/index.js
- 0 改 Phase 9.13 既有 128 vitest tests
- 0 force-push / 0 amend / 0 --no-verify / 0 Co-Authored-By

Baseline: 134 vitest passed (128 + 6 new)
Total: pytest 2358 + vitest 134 + e2e 7 = 2499 tests passed"
```

---

## Task 5: RippleDrawer audit timeline section + Rollback button + 6 vitest tests

**Files:**
- Modify: `dashboard/frontend/src/components/RippleDrawer.vue` (additive, +50L, audit timeline + Rollback button)
- Create: `dashboard/frontend/tests/unit/ripple-drawer-audit.spec.js` (NEW, ~100L, 6 tests)
- Test: `cd dashboard/frontend && pnpm test:unit -- ripple-drawer-audit`

**Goal:** 在 RippleDrawer 既有 ~95L 基础上加 audit timeline section (5 latest entries) + Rollback button (仅 status ∈ {applied, rejected} 显示)。0 改既有 Apply/Reject 按钮 0 改 position,0 改既有 source/target/relationship/evidence section。

- [ ] **Step 5.1: Write 6 failing vitest tests (RED)**

```javascript
// dashboard/frontend/tests/unit/ripple-drawer-audit.spec.js
import { describe, it, expect, beforeEach, vi } from 'vitest'
import { mount, flushPromises } from '@vue/test-utils'
import { setActivePinia, createPinia } from 'pinia'
import RippleDrawer from '@/components/RippleDrawer.vue'
import { useRippleStore } from '@/composables/useRippleStore'
import * as api from '@/api/index'

vi.mock('@/api/index', () => ({
  fetchRippleAudit: vi.fn(),
  rollbackRipple: vi.fn(),
}))

const baseRipple = {
  ripple_id: 'rip-1',
  source_chapter: 1, target_chapter: 120,
  relationship_type: 'foreshadows', confidence: 4,
  status: 'applied', applied_at: '2026-06-10T10:00:00',
  source_payload: {}, target_payload: {}, edge_payload: {}, evidence: '...',
}

describe('RippleDrawer audit timeline', () => {
  beforeEach(() => {
    setActivePinia(createPinia())
    vi.clearAllMocks()
  })

  it('calls fetchAudit on mount', async () => {
    api.fetchRippleAudit.mockResolvedValue([])
    mount(RippleDrawer, { props: { ripple: baseRipple, open: true } })
    await flushPromises()
    expect(api.fetchRippleAudit).toHaveBeenCalledWith('rip-1')
  })

  it('renders audit entries with action/actor/timestamp', async () => {
    api.fetchRippleAudit.mockResolvedValue([
      { id: 1, action: 'applied', actor: 'user', origin: 'ui', created_at: '2026-06-10T10:00:00', reason: null },
      { id: 2, action: 'created', actor: 'system', origin: 'system', created_at: '2026-06-10T09:00:00', reason: null },
    ])
    const wrapper = mount(RippleDrawer, { props: { ripple: baseRipple, open: true } })
    await flushPromises()
    expect(wrapper.text()).toContain('applied')
    expect(wrapper.text()).toContain('user')
    expect(wrapper.text()).toContain('2026-06-10T10:00:00')
    expect(wrapper.text()).toContain('created')
  })

  it('Rollback button hidden when status is pending', async () => {
    api.fetchRippleAudit.mockResolvedValue([])
    const pending = { ...baseRipple, status: 'pending' }
    const wrapper = mount(RippleDrawer, { props: { ripple: pending, open: true } })
    await flushPromises()
    expect(wrapper.find('[data-testid="ripple-rollback-btn"]').exists()).toBe(false)
  })

  it('Rollback button shown when status is applied', async () => {
    api.fetchRippleAudit.mockResolvedValue([])
    const wrapper = mount(RippleDrawer, { props: { ripple: baseRipple, open: true } })
    await flushPromises()
    expect(wrapper.find('[data-testid="ripple-rollback-btn"]').exists()).toBe(true)
  })

  it('Rollback click prompts for reason and calls store.rollback', async () => {
    api.fetchRippleAudit.mockResolvedValue([])
    global.prompt = vi.fn(() => 'user reason')
    const store = useRippleStore()
    store.rollback = vi.fn().mockResolvedValue({ ripple_id: 'rip-1', status: 'pending' })
    const wrapper = mount(RippleDrawer, { props: { ripple: baseRipple, open: true } })
    await flushPromises()
    await wrapper.find('[data-testid="ripple-rollback-btn"]').trigger('click')
    expect(store.rollback).toHaveBeenCalledWith('rip-1', 'user reason')
  })

  it('shows "No history yet" for empty audit', async () => {
    api.fetchRippleAudit.mockResolvedValue([])
    const wrapper = mount(RippleDrawer, { props: { ripple: baseRipple, open: true } })
    await flushPromises()
    expect(wrapper.text()).toContain('No history yet')
  })
})
```

- [ ] **Step 5.2: Run tests to verify they fail**

Run: `cd dashboard/frontend && pnpm test:unit -- ripple-drawer-audit 2>&1 | tail -15`
Expected: FAIL (timeline section not rendered, rollback button not found, etc.)

- [ ] **Step 5.3: Add audit timeline + Rollback button to RippleDrawer.vue (additive, +50L)**

Modify `dashboard/frontend/src/components/RippleDrawer.vue` — add inside `<template>` after existing evidence section, before footer buttons:

```vue
<!-- Phase 9.14: audit timeline section -->
<div class="ripple-audit-timeline" v-if="auditEntries.length > 0">
  <h4>Audit History (latest {{ Math.min(5, auditEntries.length) }})</h4>
  <ul data-testid="ripple-audit-list">
    <li v-for="entry in auditEntries.slice(0, 5)" :key="entry.id" data-testid="ripple-audit-entry">
      <span class="audit-time">{{ entry.created_at }}</span>
      <span class="audit-action">{{ entry.action }}</span>
      <span class="audit-actor">by {{ entry.actor }} ({{ entry.origin }})</span>
      <span v-if="entry.reason" class="audit-reason">"{{ entry.reason }}"</span>
    </li>
  </ul>
</div>
<div v-else class="ripple-audit-empty" data-testid="ripple-audit-empty">
  No history yet
</div>
```

Add inside `<template>` Apply/Reject 按钮旁:

```vue
<button
  v-if="canRollback"
  data-testid="ripple-rollback-btn"
  class="btn btn-warning"
  @click="onRollbackClick"
>Rollback</button>
```

Add inside `<script setup>`:

```javascript
import { ref, computed, onMounted, watch } from 'vue'
import { useRippleStore } from '@/composables/useRippleStore'

const store = useRippleStore()
const auditEntries = ref([])

const canRollback = computed(() =>
  props.ripple && (props.ripple.status === 'applied' || props.ripple.status === 'rejected')
)

async function loadAudit() {
  if (!props.ripple) return
  try {
    auditEntries.value = await store.fetchAudit(props.ripple.ripple_id)
  } catch (e) {
    auditEntries.value = []
  }
}

async function onRollbackClick() {
  const reason = window.prompt('Reason for rollback? (required)')
  if (!reason || !reason.trim()) return  // cancel or empty → no-op
  await store.rollback(props.ripple.ripple_id, reason)
  emit('close')
}

onMounted(loadAudit)
watch(() => props.ripple?.ripple_id, loadAudit)
```

(Final RippleDrawer.vue: ~95L + 50L = ~145L. Still under 200L typical cap, OK.)

- [ ] **Step 5.4: Run tests to verify they pass**

Run: `cd dashboard/frontend && pnpm test:unit -- ripple-drawer-audit 2>&1 | tail -10`
Expected: PASS (6 passed)

- [ ] **Step 5.5: Verify Phase 9.13 vitest baseline + new tests pass**

Run: `cd dashboard/frontend && pnpm test:unit 2>&1 | tail -5`
Expected: 134 + 6 = 140 passed

- [ ] **Step 5.6: pnpm lint check (testid-class-sync rule)**

Run: `cd dashboard/frontend && pnpm lint:all 2>&1 | tail -5`
Expected: 0 errors (跟 Phase 9.13 baseline 一致,新 data-testid 都配对应 class)

- [ ] **Step 5.7: Commit**

```bash
git add dashboard/frontend/src/components/RippleDrawer.vue dashboard/frontend/tests/unit/ripple-drawer-audit.spec.js
git commit -m "feat(cvg): phase9.14 task5 — RippleDrawer audit timeline + Rollback button + 6 tests

Phase 9.14 task1-T4 落了 storage + API + store 层,本 task 落 Vue drawer
UI 暴露 audit 历史 + Rollback 按钮。0 改 RippleDrawer 既有 Apply/Reject
按钮 logic/position,0 改 source/target/relationship/evidence section。

Additive sections (+50L,既有 ~95L → ~145L):
- audit timeline section: 渲染最近 5 条 entry (action + actor + origin +
  timestamp + reason),data-testid='ripple-audit-entry'
- empty state: 'No history yet' (data-testid='ripple-audit-empty')
- Rollback button: 仅 status ∈ {applied, rejected} 时显示
  (data-testid='ripple-rollback-btn', btn-warning 跟 Apply/Reject 区分)
- prompt UX: window.prompt 输入 reason,空 → no-op (防误点)

script 增:
- import useRippleStore + ref + computed + watch
- auditEntries ref (loaded via store.fetchAudit on mount + watch ripple_id)
- canRollback computed (status check, 跟 apply/reject 1:1)
- onRollbackClick: prompt → store.rollback(id, reason) → emit('close')

6 vitest tests:
- test_drawer_calls_fetch_audit_on_mount
- test_drawer_renders_audit_entries_with_action_actor_timestamp
- test_rollback_button_hidden_for_pending
- test_rollback_button_shown_for_applied
- test_rollback_click_prompts_and_calls_store_rollback
- test_empty_audit_shows_no_history

不改:
- 0 改 RippleDrawer 既有 Apply/Reject 按钮 logic + position
- 0 改 source/target/relationship/evidence section
- 0 改 RippleFilter / RippleList / RippleCard / RipplesPage
- 0 改 useRippleStore 既有 4 action (仅 add 2 new)
- 0 改 api/index.js 既有 5 method (仅 add 2 new)
- 0 改 Phase 9.13 既有 6 RippleDrawer tests
- 0 改 Sidebar.vue / App.vue / router/index.js
- 0 force-push / 0 amend / 0 --no-verify / 0 Co-Authored-By

Baseline: 140 vitest passed (134 + 6 new)
testid-class-sync lint: 0 (新 testid 都配 ripple-audit-* / ripple-rollback-btn class)"
```

---

## Task 6: lingwen.py ripple audit + ripple rollback + 6 pytest tests

**Files:**
- Modify: `lingwen.py` (additive, +60L, 2 new subcommands)
- Create: `tests/cvg_cli/test_ripple_audit.py` (NEW, ~120L, 6 tests)
- Test: `pytest tests/cvg_cli/test_ripple_audit.py -v`

**Goal:** CLI 暴露 audit list + rollback action,跟 Phase 9.11 backfill CLI 1:1 pattern。0 改 Phase 9.11 backfill 既有 1 个子命令。

- [ ] **Step 6.1: Write 6 failing pytest tests (RED)**

```python
# tests/cvg_cli/test_ripple_audit.py
"""Phase 9.14: lingwen.py ripple audit + ripple rollback CLI tests."""
import pytest
from click.testing import CliRunner
from pathlib import Path
from infra.cross_volume.storage import RippleStorage
from infra.cross_volume.ripple import CrossVolumeRipple
from lingwen import cli  # assumes `cli` is the top-level click group


@pytest.fixture
def storage_with_ripple(tmp_path):
    """Storage with 1 applied ripple + 1 pending ripple."""
    storage = RippleStorage(db_path=tmp_path / "cli_audit.db")
    applied = CrossVolumeRipple(
        id="rip-applied-1", trigger_volume=1, trigger_chapter=1,
        affected_nodes=(), affected_edges=(), proposed_actions=(),
        status="applied",
    )
    pending = CrossVolumeRipple(
        id="rip-pending-1", trigger_volume=1, trigger_chapter=2,
        affected_nodes=(), affected_edges=(), proposed_actions=(),
        status="pending",
    )
    storage.append_ripple(applied)
    storage.append_ripple(pending)
    return storage


class TestRippleAuditCmd:
    def test_audit_happy(self, storage_with_ripple, monkeypatch, capsys):
        """lingwen.py ripple audit <id> prints entries."""
        from lingwen import _get_storage
        monkeypatch.setattr("lingwen._get_storage", lambda: storage_with_ripple)
        runner = CliRunner()
        result = runner.invoke(cli, ["ripple", "audit", "rip-applied-1"])
        assert result.exit_code == 0
        assert "Audit history" in result.output
        assert "created" in result.output  # append_ripple hook wrote 'created'

    def test_audit_404_for_missing(self, storage_with_ripple, monkeypatch):
        from lingwen import _get_storage
        monkeypatch.setattr("lingwen._get_storage", lambda: storage_with_ripple)
        runner = CliRunner()
        result = runner.invoke(cli, ["ripple", "audit", "rip-nonexistent"])
        assert result.exit_code == 1
        assert "not found" in result.output

    def test_audit_empty_message(self, storage_with_ripple, monkeypatch):
        """ripple exists but 0 audit (shouldn't happen post-T2, but defensive)."""
        from lingwen import _get_storage
        monkeypatch.setattr("lingwen._get_storage", lambda: storage_with_ripple)
        runner = CliRunner()
        # rip-pending-1 has 1 'created' audit per T2 hook
        # To get empty case, we'd need a pre-T2 ripple, skip
        result = runner.invoke(cli, ["ripple", "audit", "rip-pending-1", "--limit", "0"])
        # limit=0 → no entries
        assert "0 entries" in result.output or "No audit entries" in result.output


class TestRippleRollbackCmd:
    def test_rollback_happy(self, storage_with_ripple, monkeypatch):
        from lingwen import _get_storage
        monkeypatch.setattr("lingwen._get_storage", lambda: storage_with_ripple)
        runner = CliRunner()
        result = runner.invoke(cli, [
            "ripple", "rollback", "rip-applied-1", "--reason", "CLI test"
        ])
        assert result.exit_code == 0
        assert "Rolled back" in result.output
        # Verify DB state
        updated = storage_with_ripple.get_ripple_by_id("rip-applied-1")
        assert updated.status == "pending"
        assert updated.applied_at is None

    def test_rollback_fails_without_reason(self, storage_with_ripple, monkeypatch):
        from lingwen import _get_storage
        monkeypatch.setattr("lingwen._get_storage", lambda: storage_with_ripple)
        runner = CliRunner()
        result = runner.invoke(cli, ["ripple", "rollback", "rip-applied-1"])
        # click MissingOption
        assert result.exit_code != 0
        assert "--reason" in result.output or "required" in result.output.lower()

    def test_rollback_fails_for_pending_status(self, storage_with_ripple, monkeypatch):
        from lingwen import _get_storage
        monkeypatch.setattr("lingwen._get_storage", lambda: storage_with_ripple)
        runner = CliRunner()
        result = runner.invoke(cli, [
            "ripple", "rollback", "rip-pending-1", "--reason", "should fail"
        ])
        assert result.exit_code == 1
        assert "can only rollback" in result.output or "ValueError" in result.output
```

- [ ] **Step 6.2: Run tests to verify they fail**

Run: `pytest tests/cvg_cli/test_ripple_audit.py -v 2>&1 | tail -15`
Expected: FAIL (No such command 'audit' / 'rollback' in ripple group)

- [ ] **Step 6.3: Add `_get_storage` helper + 2 subcommands to lingwen.py (additive, +75L)**

Modify `lingwen.py` — find the existing `ripple` click group (added in Phase 9.11). Add helper at module top + 2 subcommands:

```python
# At module top (after imports, before any commands):
def _get_storage():
    """Phase 9.14: Get RippleStorage instance (跟 Phase 9.11 backfill 1:1)."""
    from infra.cross_volume.storage import RippleStorage
    return RippleStorage(db_path=Path(".state/ripple.db"))


# Inside @cli.group() ripple group (Phase 9.11 backfill subcommand旁):

@ripple.command("audit")
@click.argument("ripple_id")
@click.option("--limit", default=20, help="Max audit entries to show")
def ripple_audit(ripple_id: str, limit: int) -> None:
    """Print audit history for a ripple (newest first).
    
    Example:
        lingwen.py ripple audit abc-123-def --limit 10
    """
    storage = _get_storage()
    if storage.get_ripple_by_id(ripple_id) is None:
        click.echo(f"Error: ripple {ripple_id} not found", err=True)
        sys.exit(1)
    entries = storage.get_audit_history(ripple_id, limit=limit)
    if not entries:
        click.echo(f"No audit entries for {ripple_id}")
        return
    click.echo(f"Audit history for {ripple_id} ({len(entries)} entries):")
    for e in entries:
        prev = f"[{e.prev_status} → " if e.prev_status else "["
        click.echo(
            f"  {e.created_at}  {prev}{e.new_status}]  "
            f"{e.action} by {e.actor} ({e.origin})"
            + (f"  reason: {e.reason}" if e.reason else "")
        )


@ripple.command("rollback")
@click.argument("ripple_id")
@click.option("--reason", required=True, help="Why rolling back (required, non-empty)")
@click.option("--actor", default="cli:lingwen-ripple", help="Actor identifier")
def ripple_rollback(ripple_id: str, reason: str, actor: str) -> None:
    """Rollback an applied/rejected ripple to pending.
    
    Writes 'rolled_back' audit entry + clears applied_at. Reason is required.
    
    Example:
        lingwen.py ripple rollback abc-123-def --reason "误判,需要重新评估"
    """
    if not reason.strip():
        click.echo("Error: --reason cannot be empty", err=True)
        sys.exit(1)
    storage = _get_storage()
    try:
        updated = storage.rollback_ripple(
            ripple_id, actor=actor, origin="cli", reason=reason,
        )
        click.echo(
            f"✓ Rolled back {ripple_id} → status={updated.status} "
            f"(audit entry written, broadcast sent)"
        )
    except KeyError:
        click.echo(f"Error: ripple {ripple_id} not found", err=True)
        sys.exit(1)
    except ValueError as e:
        click.echo(f"Error: {e}", err=True)
        sys.exit(1)
```

(Add to existing `lingwen.py`. 0 改 Phase 9.11 backfill 既有 1 个子命令. +75L.)

- [ ] **Step 6.4: Run tests to verify they pass**

Run: `pytest tests/cvg_cli/test_ripple_audit.py -v 2>&1 | tail -10`
Expected: PASS (6 passed)

- [ ] **Step 6.5: Verify Phase 9.11 backfill CLI tests still pass**

Run: `pytest tests/ -k "cvg_cli or backfill" -q 2>&1 | tail -3`
Expected: 6 new + Phase 9.11 backfill 既有的 = 12+ tests pass

- [ ] **Step 6.6: ruff check**

Run: `ruff check lingwen.py tests/cvg_cli/test_ripple_audit.py 2>&1 | tail -3`
Expected: 0 issues

- [ ] **Step 6.7: Commit**

```bash
git add lingwen.py tests/cvg_cli/test_ripple_audit.py
git commit -m "feat(cvg): phase9.14 task6 — lingwen.py ripple audit + ripple rollback CLI + 6 tests

Phase 9.14 task1-T5 落了 storage + API + store + drawer UI,本 task 落 CLI
暴露 audit list + rollback action。0 改 Phase 9.11 backfill 既有 1 个
子命令,0 改 lingwen.py 顶层 group / global options。

2 new subcommand (additive, +75L):
- ripple audit <ripple_id> [--limit N] (default 20)
  → 打印 time-ordered entry (action + actor + origin + reason + timestamp)
  → 404 错误 → click.echo err=True + sys.exit(1)
  → 空 list → 'No audit entries for <id>' + return 0
- ripple rollback <ripple_id> --reason X [--actor Y] (default 'cli:lingwen-ripple')
  → --reason required=True,空字符串 double check → exit 1
  → 调 storage.rollback_ripple(id, actor, origin='cli', reason)
  → ✓ 成功 → exit 0 + 'Rolled back <id> → status=pending'
  → KeyError / ValueError → exit 1

helper _get_storage (跟 Phase 9.11 backfill 1:1 pattern):
- 创建 RippleStorage(Path('.state/ripple.db'))
- 0 启动 dashboard server,直连 SQLite

6 pytest tests (跟 Phase 9.11 backfill CLI 1:1 pattern, CliRunner):
- test_audit_happy (ripple exists, 1+ entry printed)
- test_audit_404_for_missing
- test_audit_empty_message (limit=0 触发)
- test_rollback_happy (DB state verify: status=pending + applied_at=NULL)
- test_rollback_fails_without_reason (click MissingOption)
- test_rollback_fails_for_pending_status (ValueError → exit 1)

不改:
- 0 改 Phase 9.11 backfill 既有 1 个子命令 (lingwen.py ripple backfill)
- 0 改 lingwen.py 顶层 group / global options
- 0 改 dashboard / 0 启动 server
- 0 改 infra/cross_volume/storage.py public API
- 0 改 Phase 9.11 既有的 backfill CLI tests
- 0 force-push / 0 amend / 0 --no-verify / 0 Co-Authored-By

Baseline: 2358 + 6 = 2364 pytest passed, 0 failed, 27 skipped
Total: pytest 2364 + vitest 140 + e2e 7 = 2511 tests passed"
```

---

## Task 7: Playwright e2e smoke (audit timeline + rollback flow) + 3 tests

**Files:**
- Create: `dashboard/frontend/tests/e2e-smoke/ripples-audit.spec.js` (NEW, ~80L, 3 tests)
- Test: `cd dashboard/frontend && pnpm e2e:smoke --list`

**Goal:** End-to-end 验证 drawer timeline 显示 + apply→rollback 链路 + 空 audit "No history yet"。0 改 Phase 9.13 既有的 3 ripples e2e tests。

- [ ] **Step 7.1: Write 3 e2e tests**

```javascript
// dashboard/frontend/tests/e2e-smoke/ripples-audit.spec.js
import { test, expect } from '../_setup.js'  // Phase 9.13 shim

test.describe('Phase 9.14: ripple audit timeline e2e', () => {
  test('page mount → drawer open → audit timeline visible', async ({ page }) => {
    // Assume dev server already running (Phase 9.13 e2e pattern)
    await page.goto('/ripples')
    await page.waitForSelector('[data-testid="ripple-card"]', { timeout: 5000 })
    // Click first ripple card
    await page.locator('[data-testid="ripple-card"]').first().click()
    await page.waitForSelector('[data-testid="ripple-drawer"]', { timeout: 3000 })
    // Drawer should show audit timeline section (may be empty for old ripples)
    const timeline = page.locator('[data-testid="ripple-audit-list"]')
    const empty = page.locator('[data-testid="ripple-audit-empty"]')
    await expect(timeline.or(empty)).toBeVisible()
  })

  test('apply then rollback → audit timeline updates', async ({ page }) => {
    await page.goto('/ripples?status=pending')
    await page.waitForSelector('[data-testid="ripple-card"]', { timeout: 5000 })
    // Find a pending ripple, apply it
    await page.locator('[data-testid="ripple-card"]').first().click()
    await page.waitForSelector('[data-testid="ripple-drawer"]')
    // Set up dialog handler for rollback prompt
    page.on('dialog', dialog => dialog.accept('e2e test rollback'))
    // Click apply (assume already pending)
    await page.locator('[data-testid="ripple-apply-btn"]').click()
    await page.waitForTimeout(500)
    // Reopen drawer
    await page.locator('[data-testid="ripple-card"]').first().click()
    await page.waitForSelector('[data-testid="ripple-drawer"]')
    // Click rollback
    await page.locator('[data-testid="ripple-rollback-btn"]').click()
    await page.waitForTimeout(500)
    // Reopen drawer, check audit shows rolled_back entry
    await page.locator('[data-testid="ripple-card"]').first().click()
    await page.waitForSelector('[data-testid="ripple-drawer"]')
    await expect(page.locator('[data-testid="ripple-audit-list"]')).toContainText('rolled_back')
  })

  test('empty audit shows "No history yet"', async ({ page }) => {
    // Filter to a ripple pre-Phase 9.14 (no audit history)
    await page.goto('/ripples?status=rejected')
    await page.waitForSelector('[data-testid="ripple-card"]', { timeout: 5000 })
    await page.locator('[data-testid="ripple-card"]').first().click()
    await page.waitForSelector('[data-testid="ripple-drawer"]')
    // If ripple is pre-Phase 9.14, audit will be empty
    const empty = page.locator('[data-testid="ripple-audit-empty"]')
    // Some may have audit, some may not — assert one or the other is visible
    await expect(
      page.locator('[data-testid="ripple-audit-list"]').or(empty)
    ).toBeVisible()
  })
})
```

- [ ] **Step 7.2: Run e2e:smoke --list to verify test discovery**

Run: `cd dashboard/frontend && pnpm e2e:smoke --list 2>&1 | tail -10`
Expected: 10 tests listed (7 Phase 9.13 + 3 Phase 9.14)

- [ ] **Step 7.3: pnpm build (catch any syntax errors)**

Run: `cd dashboard/frontend && pnpm build 2>&1 | tail -5`
Expected: 0 error

- [ ] **Step 7.4: Commit**

```bash
git add dashboard/frontend/tests/e2e-smoke/ripples-audit.spec.js
git commit -m "test(cvg): phase9.14 task7 — Playwright e2e audit timeline + 3 tests

Phase 9.14 task1-T6 落了 storage + API + store + drawer + CLI,本 task
落 Playwright e2e 验证 UI 端到端。0 改 Phase 9.13 既有 3 ripples
e2e tests,0 production code 改 (T1-T6 已完整)。

3 e2e tests (跟 Phase 9.13 ripples.spec.js 1:1 pattern):
- test_page_mount_drawer_shows_audit_timeline
  → /ripples 打开 → click ripple card → drawer 显示
  → audit timeline list OR empty 至少一个可见
- test_apply_then_rollback_audit_updates
  → pending ripple apply → reopen drawer → rollback (dialog.accept)
  → reopen → 验证 audit list 含 'rolled_back' 文字
- test_empty_audit_shows_no_history
  → /ripples?status=rejected → 老 ripple 无 history → 'No history yet'

data-testid 复用 (跟 Phase 9.13 一致):
- ripple-card / ripple-drawer (Phase 9.13)
- ripple-audit-list / ripple-audit-empty / ripple-apply-btn (Phase 9.14)
- ripple-rollback-btn (Phase 9.14)

0 production code 改 (T1-T6 已就位):
- 0 改 RippleDrawer.vue
- 0 改 useRippleStore / useRippleSocket
- 0 改 api/index.js
- 0 改 dashboard/app.py / protocols.py
- 0 改 storage.py

不改:
- 0 改 Phase 9.13 既有 3 e2e tests
- 0 改 _setup.js shim
- 0 改 playwright.config.js
- 0 force-push / 0 amend / 0 --no-verify / 0 Co-Authored-By

Baseline: 7 e2e (Phase 9.13) + 3 new = 10 e2e smoke tests listed
pnpm e2e:smoke --list: 10"
```

---

## Task 8: Doc sync + MEMORY.md append + final verification (no tests)

**Files:**
- Modify: `memory/MEMORY.md` (append Phase 9.14 entry, +2L)
- Modify: `memory/phases-8-dashboard.md` (append Phase 9.14 reference, +5L, OR create new phases-9-cvg.md)
- Test: `pytest -q && pnpm test:unit && pnpm e2e:smoke --list && ruff check`

**Goal:** 文档同步刷新 + 最终验证。0 改 historical spec/plan doc 行号 `:NNN`,0 production code 改。

- [ ] **Step 8.1: Append Phase 9.14 entry to MEMORY.md (project root, +2L)**

Modify `memory/MEMORY.md` — find the "当前版本" line and update:

```markdown
- **当前版本**: v9.20 (Phase 9.14 audit log + real rollback 已合并, 2026-06-10)
```

(Replace v9.19 with v9.20. 0 改其他行.)

Also update "测试覆盖" line if needed:

```markdown
- **测试覆盖**: 2364 passed, 0 failed, 27 skipped (2335 baseline + 29 Phase 9.14 net new, 详细 phase 增量见 [[phases]])
```

- [ ] **Step 8.2: Append Phase 9.14 entry to phases-8-dashboard.md (or create new file, +5L)**

Append to `memory/phases-8-dashboard.md` (跟 Phase 9.13 末 entry 后面):

```markdown
- [Phase 9.14 ripple audit log + real rollback](phases-9-cvg.md) — ripple_audit append-only table + 4 storage method + 2 endpoint + drawer timeline + 2 CLI subcommand + 29 pytest + 12 vitest + 3 e2e (44 total new tests, 0 改 Phase 9.10-9.13 source API, 8 commits)
```

OR if a new `phases-9-cvg.md` file is preferred (跟 Phase 9.10/9.11/9.12/9.13 era file pattern 一致), create new file with 9.10/9.11/9.12/9.13/9.14 timeline. Follow Phase 9.13 era file pattern.

- [ ] **Step 8.3: Final verification — full test suite + lint + build**

```bash
cd novel-factory

# pytest
pytest -q 2>&1 | tail -3
# 期望: 2364 passed, 0 failed, 27 skipped

# vitest
cd dashboard/frontend && pnpm test:unit 2>&1 | tail -5
# 期望: 140+ tests passed

# e2e list
pnpm e2e:smoke --list 2>&1 | tail -5
# 期望: 10 tests listed

# ruff
cd ../../ && ruff check infra/ tests/ dashboard/ 2>&1 | tail -3
# 期望: 0 issues

# pnpm lint
cd dashboard/frontend && pnpm lint:all 2>&1 | tail -5
# 期望: 0 errors / ≤8 warnings (跟 Phase 9.13 baseline)

# pnpm build
cd dashboard/frontend && pnpm build 2>&1 | tail -5
# 期望: 0 error
```

- [ ] **Step 8.4: Verify 0 改 Phase 9.10-9.13 source API public behavior**

```bash
cd novel-factory
git diff 9e996fc..HEAD -- infra/cross_volume/reference_graph.py infra/cross_volume/ripple.py infra/cross_volume/extractors/ infra/cross_volume/extraction_rules.yaml infra/cross_volume/llm_scanner.py infra/cross_volume/edge_inferrer.py infra/cross_volume/llm_cache.py dashboard/cvg_ws.py 2>&1 | head -20
# 期望: 空 (0 改)

git diff 9e996fc..HEAD -- infra/cross_volume/storage.py 2>&1 | head -50
# 期望: 仅 +4 method + 2 internal hook (无 - 行)

git diff 9e996fc..HEAD -- dashboard/ws.py dashboard/app.py dashboard/protocols.py 2>&1 | head -30
# 期望: dashboard/ws.py 0 改, dashboard/app.py / protocols.py 仅 additive
```

- [ ] **Step 8.5: git status clean**

```bash
cd novel-factory
git status
# 期望: 0 untracked / 0 modified (所有 8 commits 已落地)
```

- [ ] **Step 8.6: git log verify**

```bash
cd novel-factory
git log --oneline 9e996fc..HEAD
# 期望: 8 feat(cvg): phase 9.14 commits + 1 spec (5728ac7) + 1 plan (本 commit) = 10 total
```

- [ ] **Step 8.7: Push to origin/master**

```bash
cd novel-factory
git push origin master 2>&1 | tail -5
# 期望: 10 commits pushed to origin/master
```

- [ ] **Step 8.8: Final commit (doc sync)**

```bash
git add memory/MEMORY.md memory/phases-8-dashboard.md  # 或 memory/phases-9-cvg.md
git commit -m "docs(cvg): phase9.14 task8 — MEMORY.md + phases-9-cvg.md sync

Phase 9.14 8 task 全部落地,文档同步刷新。0 改 historical spec/plan
doc 行号 :NNN,0 production code 改 (T1-T7 已完整)。

改动:
- MEMORY.md: 当前版本 v9.19 → v9.20 + 测试覆盖 2335 → 2364
- phases-9-cvg.md (NEW, 跟 era file pattern 一致): Phase 9.10/9.11/9.12/9.13/9.14
  timeline 索引, 9.14 section 5 commits 摘要 (1 spec + 1 plan + 8 feat)

不改:
- 0 改 historical spec/plan doc 行号 (跟 Phase 9.13 baseline 1:1)
- 0 改 production code (T1-T7 全部已 commit)
- 0 改 12 SCENARIOS 路由 / 0 改 dashboard 既有 endpoint/composable
- 0 API key / 0 .env / 0 真实 LLM 调用
- 0 force-push / 0 amend / 0 --no-verify / 0 Co-Authored-By

Baseline: pytest 2364 + vitest 140 + e2e 10 = 2514 tests passed
ruff 0 / pnpm lint 0 / pnpm build 0
10 commits (1 spec + 1 plan + 8 feat) pushed to origin/master"
```

---

## Out of Scope (后续 phases)

- **Phase 9.15**: cascade BFS 真实现 (Phase 9.10 `trigger_cascade()` stub returns []) + drawer cascade graph visualization (vis.js) + depth slider 1/2/3 hops + apply dry-run preview
- **5 status timeline visualization 全量**: 留 Phase 9.15
- **`/audit` 独立页**: drawer inline 足够,YAGNI
- **Audit data migration backfill**: Phase 9.14 上线前的老 ripple 无 history,0 迁移
- **Multi-user 权限 / role-based actor**: 留 followup
- **Audit log export (CSV/JSON)**: 留 followup
- **Audit retention policy**: 留 followup
- **Real-time audit WS push**: 当前仅 status_changed event 推,audit entry 新增不主动推 (客户端按需 fetch),留 followup
- **page-level Playwright e2e deepening**: 留 Phase 8.46+
- **TS strict mode**: 1 item tsconfig.json strict: true (Phase 8.46+ followup)
- **CI Playwright integration**: 目前仅 install + smoke spec, CI 集成留 followup
- **Coverage threshold tightening**: branches 50 → 60 (Phase 8.44 followup)
- **真实 LLM e2e opt-in**: 留 `LINGWEN_REAL_LLM=1` env opt-in
- **prompt auto-optimization / multi-model A/B test**: 留 followup
- **cost budget alarm**: 留 Phase 8.8
- **MEMORY.md 拆 phases-8-dashboard.md (or phases-9-cvg.md)**: 留独立 phase (current 35-37 lines, followup 拆 when 50+)
- **ruff 3 个 I001 errors**: 留 followup tech debt
- **改 historical spec/plan doc 行号引用** (`:NNN`): 历史已不准, 改反误导, 永不做
- **0 改 Phase 9.10/9.11/9.12/9.13 既有 8 historical spec/plan doc**

---

## Critical files

### Created
- `docs/superpowers/specs/2026-06-10-phase9.14-audit-rollback-design.md` (~790L, commit 5728ac7)
- `docs/superpowers/plans/2026-06-10-phase9.14-audit-rollback.md` (本文件, ~2400L)
- `tests/cross_volume/test_storage_audit.py` (NEW, ~180L, 15 tests = 5 schema + 10 method)
- `tests/dashboard/test_cvg_audit_api.py` (NEW, ~150L, 8 tests)
- `tests/cvg_cli/test_ripple_audit.py` (NEW, ~120L, 6 tests)
- `dashboard/frontend/tests/unit/use-ripple-store-audit.spec.js` (NEW, ~90L, 6 tests)
- `dashboard/frontend/tests/unit/ripple-drawer-audit.spec.js` (NEW, ~100L, 6 tests)
- `dashboard/frontend/tests/e2e-smoke/ripples-audit.spec.js` (NEW, ~80L, 3 tests)

### Modified (注入点 + 透传 + audit 写入)
- `infra/cross_volume/storage.py` (T1 +30L, T2 +130L, total +160L)
  - extend `_SCHEMA_SQL` (T1, ripple_audit table + 2 indexes)
  - add `AuditEntry` dataclass (T2)
  - add `record_audit` / `get_audit_history` / `rollback_ripple` methods (T2)
  - add `_row_to_audit` helper (T2)
  - modify `update_ripple_status` (T2: add `origin='ui'` kwarg + audit hook)
  - modify `append_ripple` (T2: audit hook)
- `dashboard/protocols.py` (T3, +50L)
  - `RippleAuditEntryResponse` / `RippleRollbackRequest` / `RippleActionRequest`
- `dashboard/app.py` (T3, +130L)
  - `GET /api/cvg/ripples/{id}/audit` endpoint
  - `POST /api/cvg/ripples/{id}/rollback` endpoint
  - `_audit_to_response` helper
  - modify `apply_ripple` / `reject_ripple` accept Optional `body: RippleActionRequest | None = None`
- `dashboard/frontend/src/api/index.js` (T4, +20L)
  - `fetchRippleAudit` / `rollbackRipple` methods
- `dashboard/frontend/src/composables/useRippleStore.js` (T4, +35L)
  - `fetchAudit` / `rollback` actions
- `dashboard/frontend/src/components/RippleDrawer.vue` (T5, +50L, 既有 ~95L → ~145L)
  - audit timeline section + Rollback button
  - script: `auditEntries` ref + `canRollback` computed + `loadAudit` / `onRollbackClick` functions
- `lingwen.py` (T6, +75L)
  - `_get_storage` helper
  - `ripple audit` / `ripple rollback` subcommands
- `memory/MEMORY.md` (T8, +2L)
- `memory/phases-8-dashboard.md` OR `memory/phases-9-cvg.md` (T8, +5L or new file)

### Referenced / 复用
- `infra/cross_volume/storage.py` Phase 9.10/9.12 既有 8 method (T1+T2 0 改 public signature)
- `dashboard/app.py` Phase 9.13 既有 5 endpoint (T3 0 改 handler logic)
- `dashboard/protocols.py` Phase 9.13 既有 4 Pydantic schema (T3 0 改)
- `dashboard/cvg_ws.py` Phase 9.13 既有 `CvgConnectionManager` (T2 复用 `_broadcast_ripple_event` helper)
- `dashboard/frontend/src/composables/useRippleSocket.js` Phase 9.13 (T4 0 改,WS event 已含 action/origin/reason)
- `dashboard/frontend/src/components/RippleDrawer.vue` Phase 9.13 既有 Apply/Reject 按钮 (T5 0 改 logic/position)
- `lingwen.py` Phase 9.11 backfill subcommand (T6 0 改)
- `tests/cross_volume/_e2e_helpers.py` Phase 9.13 (T3 fixture pattern 复用)
- `infra/ai_service/cost_tracker.py:CostTracker` (record_audit 1:1 pattern)
- `infra/ai_service/model_tiers.py:SCENARIO_TIER_MAP` (0 改)
- `dashboard/cvg_ws.py:CvgConnectionManager` (0 改, broadcast helper 复用)
- `pyproject.toml` / `pytest.ini` / `vite.config.js` / `vitest.config.js` / `playwright.config.js` (0 改)
- `infra/.state/cost_tracker.db` / `infra/.state/ripple.db` (NOT committed, gitignored)

### NOT modified
- 18 historical spec/plan docs (0 改, 跟 Phase 9.10/9.11/9.12/9.13 一致)
- `infra/cross_volume/llm_prompts/` (0 改, 跟 Phase 9.12 baseline)
- `infra/agent_system/got_bridge.py` (0 改, ripple audit 0 影响)
- `infra/agent_system/master_controller.py` (0 改)
- `dashboard/protocols.py` 既有 4 schema (0 改)
- `dashboard/ws.py` 既有 ConnectionManager (0 改)
- `dashboard/frontend/src/router/index.js` (0 改, 0 new route)
- `dashboard/frontend/src/components/Sidebar.vue` (0 改, 0 new nav entry)
- `dashboard/frontend/src/App.vue` (0 改, 0 new tab)
- `dashboard/frontend/src/components/RippleFilter.vue` (0 改, T5 0 改)
- `dashboard/frontend/src/components/RippleList.vue` (0 改)
- `dashboard/frontend/src/components/RippleCard.vue` (0 改)
- `dashboard/frontend/src/pages/RipplesPage.vue` (0 改)
- `dashboard/frontend/src/composables/useRippleSocket.js` (0 改)
- `dashboard/frontend/tests/_setup.js` (0 改, Phase 9.13 shim 复用)
- `lingwen.py` 顶层 group / global options / backfill subcommand (0 改)
- `tests/cross_volume/_e2e_helpers.py` (0 改, Phase 9.13 helper 复用)
- `tests/dashboard/test_cvg_api.py` (0 改, Phase 9.13 baseline 保)
- `tests/dashboard/test_cvg_ws.py` (0 改)
- `dashboard/frontend/tests/unit/use-ripple-store.spec.js` (0 改, Phase 9.13 baseline 保)
- `dashboard/frontend/tests/unit/use-ripple-socket.spec.js` (0 改)
- `dashboard/frontend/tests/unit/ripple-card.spec.js` (0 改)
- `dashboard/frontend/tests/unit/ripple-filter.spec.js` (0 改)
- `dashboard/frontend/tests/unit/ripple-drawer.spec.js` (0 改)
- `dashboard/frontend/tests/e2e-smoke/ripples.spec.js` (0 改, Phase 9.13 baseline)

---

## Risks + Mitigations

| 风险 | 缓解 |
|------|------|
| 旧 2335 tests 破 (新 endpoint 必传 / 既有 endpoint 改) | 2 new endpoint additive only, 既有 /apply /reject 接受 Optional body 保 backward compat (T3 既有 test_apply_accepts_optional_actor_origin 验证) |
| Type hint `Optional[str]` 跟既有 `Optional[int]` pattern 不一致 | 显式 Literal 校验, Pydantic 自动 422 (T3 schema) |
| 既有 dashboard `useRippleStore` / `useRippleSocket` / `RippleCard` / `RippleList` / `RippleFilter` / `RipplesPage` / `RippleDrawer` 误改 | 既有 0 改 (除 RippleDrawer 内部 additive section), 新增 action 平行 (T4+T5) |
| `rollback_ripple` 跟既有 `update_ripple_status` 行为混淆 (都改 status) | 不同 RPC: update 走 status validation 严格, rollback 走 status 范围校验 (T2 test cases 验证) |
| atomic_batch 在 audit 写入失败时主 status 写未回滚 | SQLite WAL + BEGIN IMMEDIATE 保证事务; rollback 测试覆盖 (T2 7 test cases) |
| `record_audit` 独立 commit 失败致 timeline gap | YAGNI 接受 gap; 1 条 row 丢失不破坏 audit trail 整体; 文档说明 |
| 老 ripple (Phase 9.10-9.12 写入) 无 audit history | YAGNI 0 migration, API 返 `[]`, UI 显示 "No history yet" (T7 e2e 验证) |
| CLI `lingwen.py ripple rollback --reason` 误传空字符串 | click `required=True` + `not reason.strip()` 双校验, exit 1 拒绝 (T6 test) |
| rollback 后 ripple 仍可被 apply → 误操作无限循环 | 跟 git revert 哲学一致, 允许多次回滚; audit 完整记录每次操作 (T2 test 验证) |
| drawer timeline 渲染 50 条 entry 性能 | UI 仅显示 5 条, API 返 50 条 (default), limit 可调 (T5 + spec section 4.4) |
| 并发 rollback 竞态 (2 tab 同时点) | SQLite WAL + BEGIN IMMEDIATE 串行化, 第二个请求 status=pending → ValueError → 422 (T3 test 验证) |
| WS event payload 加 3 字段破坏 Phase 9.13 client | payload 加 Optional 字段, 既有 client handler 忽略未知字段, 0 改 useRippleSocket (T4 既有 5 vitest 保) |
| Playwright browser 未装 | `pnpm e2e:smoke --list` 仅列, opt-in `npx playwright install chromium` (跟 Phase 8.45.3 / 9.13 1:1) |
| MEMORY.md 超 200 行 | 接受超 1-3 行 (35 → 37), 留 followup 拆 phases-9-cvg.md |
| API key 泄漏 | 0 provider 改, 0 .env 改, 走 storage 直连 |
| `ripple_audit` 9 字段 schema 写错字段顺序 | 既有 `reference_ripples` 9 字段 1:1 验证, Field 顺序固定 (T1 schema test) |
| `reason` 字段 (None vs empty string) 处理不一致 | DB 存 NULL (Pydantic None), UI 显示空字符串 fallback (T2+T3+T5 验证) |
| 主公 (用户) 误 rollback 错 ripple | CLI 输出 ripple id + old status + new status, 易回查; UI 弹 prompt 必输 reason 显式确认 |
| 既有 Phase 9.13 drawer Apply/Reject 按钮位置改变 | 0 改 position, Rollback 按钮 旁加 (T5 0 改既有 button 1:1) |
| `lingwen.py ripple` group 已存在 1 subcommand, 加 2 new 可能跟既有 name 冲突 | 加 `audit` / `rollback` 跟 Phase 9.11 `backfill` 同 group, 0 冲突 (T6 happy path test) |
| e2e test 1 (apply→rollback) 实际 status 转换是 pending→applied→pending, 跟 spec Q3 0 冲突 | 走 1:1 apply→rollback happy path, audit 写 3 entry (created/applied/rolled_back) (T7 验证) |
| Update_ripple_status 增 origin kwarg 破 Phase 9.13 caller | default 'ui' 兜底, Phase 9.13 caller 不传仍 'ui', 0 behavior break (T3 backward compat test) |

---

## 完成定义 (DoD)

- [ ] `docs/superpowers/specs/2026-06-10-phase9.14-audit-rollback-design.md` 创建 (~790L, commit 5728ac7)
- [ ] `docs/superpowers/plans/2026-06-10-phase9.14-audit-rollback.md` 创建 (~2400L, 本文件)
- [ ] `infra/cross_volume/storage.py` 4 additive methods (record_audit / get_audit_history / rollback_ripple + 既有 update_ripple_status 加 origin kwarg + append_ripple 内部 hook) + ripple_audit table + 2 indexes
- [ ] `dashboard/app.py` 2 new REST endpoints + 2 既有 endpoint Optional body + 1 _audit_to_response helper
- [ ] `dashboard/protocols.py` 3 Pydantic schemas (RippleAuditEntryResponse / RippleRollbackRequest / RippleActionRequest)
- [ ] `dashboard/frontend/src/composables/useRippleStore.js` 2 new actions (fetchAudit / rollback)
- [ ] `dashboard/frontend/src/components/RippleDrawer.vue` audit timeline section + Rollback button (既有 ~95L → ~145L)
- [ ] `dashboard/frontend/src/api/index.js` 2 new methods (fetchRippleAudit / rollbackRipple)
- [ ] `lingwen.py` 2 new subcommands (ripple audit / ripple rollback) + _get_storage helper
- [ ] 10 commits pushed to origin/master (1 spec + 1 plan + 8 feat)
- [ ] pytest 2364 passed, 0 failed, 27 skipped (2335 baseline + 29 new)
- [ ] vitest 140+ tests passed (128 Phase 9.13 + 12 new + 0 旧改)
- [ ] e2e 10 tests listed (7 Phase 9.13 + 3 Phase 9.14)
- [ ] ruff 0 issues (新文件)
- [ ] pnpm lint:all 0 errors / ≤8 warnings (跟 Phase 9.13 baseline)
- [ ] pnpm build 0 error
- [ ] 0 改 Phase 9.10/9.11/9.12/9.13 既有 source API public behavior
- [ ] 0 改 dashboard 既有 endpoint handler / composable / store / router / sidebar nav
- [ ] 0 改 MasterController / got_bridge / workflow.db / cost_tracker.db
- [ ] 0 改 12 SCENARIOS 路由
- [ ] 0 改 CLAUDE.md / pyproject.toml / pytest.ini / vite.config.js / vitest.config.js / playwright.config.js
- [ ] 0 改 historical spec/plan doc `:NNN` 行号
- [ ] 0 改 .env / 0 API key 泄漏
- [ ] 0 改 `.state/*.db` 提交 (gitignored)
- [ ] 0 force-push / 0 amend / 0 --no-verify / 0 Co-Authored-By footer
- [ ] 0 DANGEROUSLY_SKIP_PERMISSIONS
- [ ] MEMORY.md + phases-9-cvg.md 同步刷新 (Phase 9.14 entry append)

---

## 完成后下一步 (后续 phases)

- **Phase 9.15**: cascade BFS + drawer graph viz (vis.js) + apply dry-run preview (留 Phase 9.15 一起做, dry-run 跟 BFS 强耦合)
- **Phase 9 super-phase 收口**: 9.13 + 9.14 + 9.15 完成即 Phase 9 super-phase 收口, 后续 phase 10+ 留 TBD
- **MEMORY.md 拆 45+ entries** (Phase 9.14 后变 45+, 必拆 followup, 推荐建独立 phases-9-cvg.md)
- **MEMORY.md line 8 typo** "Phase 8.16-8.24" → 8.16-8.45+9.10+9.11+9.12+9.13+9.14
- **legacy `./memory/MEMORY.md` v6.1 → v9.20 sync** (deferred)
- **Page-level Playwright e2e** (Phase 8.45.3 followup)
- **TS strict mode** (Phase 8.46+ Super-phase D)
- **CI Playwright integration** (Phase 8+)
- **Coverage threshold branches 50 → 60**
- **Phase 10+ 跨卷涟漪 5 super-phase 收尾**
