# Phase 7.4 — Multi-Variant Polish 实现 Plan

> Doc 4 §11 Phase 7.4
> Spec: `docs/superpowers/specs/2026-06-04-phase7.4-multi-variant-polish-design.md`
> 目标 baseline: 2046 → 2053 passed, 0 failed, ruff 0

---

## Task 1: RED — 7 个新测试先写 (失败态)

**Files**:
- Modify: `tests/agent_system/test_got_bridge.py` (+3 tests)
- Modify: `tests/agent_system/test_master_controller_e2e_real_llm.py` (+4 tests)

### Step 1.1: 加 3 个 test_got_bridge 测试 (handler 路由 + merge 逻辑)

在 `test_got_bridge.py` 的 `TestAgentComputeFnRouting` 类后追加:

```python
# === TestPolishHandlerRouting (Phase 7.4 NEW) ===

class TestPolishHandlerRouting:
    """Phase 7.4: _handler_polish 拆为 _make_polish_handler 工厂, 按 scenario 路由"""

    def test_emotional_pacing_routes_to_polish_emotional_pacing(self):
        """scenario=emotional_pacing → master.polish_emotional_pacing 被调 (非 polish_chapter)"""
        master = _StubMaster()
        # 给 stub 加 emotional_pacing 方法 (区分于 polish_chapter)
        master.emotional_pacing_calls = []
        def _stub_emotional(content):
            master.emotional_pacing_calls.append(content)
            return content + " [emotional]"
        master.polish_emotional_pacing = _stub_emotional

        compute = AgentComputeFn(master)
        node = _node("pol1", "emotional_pacing")
        result = compute(node, {"content": "raw text"})
        assert result.fail is False
        assert "[emotional]" in result.output["content"]
        assert len(master.emotional_pacing_calls) == 1
        # 验证 polish_chapter 没被调
        assert not any(call[0] == "polish_chapter" for call in master.calls)

    def test_ai_trace_removal_routes_to_polish_ai_trace_removal(self):
        """scenario=ai_trace_removal → master.polish_ai_trace_removal 被调 (非 polish_chapter)"""
        master = _StubMaster()
        master.ai_trace_calls = []
        def _stub_ai_trace(content):
            master.ai_trace_calls.append(content)
            return content + " [ai_removed]"
        master.polish_ai_trace_removal = _stub_ai_trace

        compute = AgentComputeFn(master)
        node = _node("pol2", "ai_trace_removal")
        result = compute(node, {"content": "raw text"})
        assert result.fail is False
        assert "[ai_removed]" in result.output["content"]
        assert len(master.ai_trace_calls) == 1
        assert not any(call[0] == "polish_chapter" for call in master.calls)

    def test_hook_extraction_still_routes_to_polish_chapter(self):
        """反向: hook_extraction 仍走 master.polish_chapter (向后兼容)"""
        master = _StubMaster()
        compute = AgentComputeFn(master)
        node = _node("pol3", "hook_extraction")
        result = compute(node, {"content": "raw text"})
        assert result.fail is False
        assert master.calls[0][0] == "polish_chapter"


# === TestPolishMergeHandler (Phase 7.4 NEW) ===

class TestPolishMergeHandler:
    """Phase 7.4: _handler_polish_merge 按 max(len) 合并多个上游 content"""

    def test_polish_merge_picks_longer_content(self):
        """输入 2 个上游, 返 max(len(content)) 版本"""
        master = _StubMaster()
        compute = AgentComputeFn(master)
        node = _node("merge1", "polish_merge")
        inputs = {
            "polish_emotional_pacing": {"content": "short"},
            "polish_ai_trace_removal": {"content": "this is a much longer content version"},
        }
        result = compute(node, inputs)
        assert result.fail is False
        # max(len("short")=5, len("this is a much longer content version")=36) = 36
        assert result.output["content"] == "this is a much longer content version"

    def test_polish_merge_with_single_upstream(self):
        """单上游也能 merge (兜底, 不报错)"""
        master = _StubMaster()
        compute = AgentComputeFn(master)
        node = _node("merge2", "polish_merge")
        inputs = {"polish_emotional_pacing": {"content": "only one"}}
        result = compute(node, inputs)
        assert result.fail is False
        assert result.output["content"] == "only one"

    def test_polish_merge_no_content_returns_error(self):
        """无 content 上游 → _error (不调 master)"""
        master = _StubMaster()
        compute = AgentComputeFn(master)
        node = _node("merge3", "polish_merge")
        result = compute(node, {"upstream": {"other": "value"}})
        assert result.fail is True
        assert "polish_merge requires" in result.error
```

### Step 1.2: 加 4 个 test_master_controller_e2e_real_llm 测试 (YAML 拓扑 + E2E)

在 `test_master_controller_e2e_real_llm.py` 末尾添加新 class:

```python
# === TestNovelWritingMultiVariantPolish (Phase 7.4 NEW) ===

class TestNovelWritingMultiVariantPolish:
    """Phase 7.4: novel_writing.yaml 7 节点 — review_chapter 后 2 个并行 polish + AGGREGATION merge"""

    MINIMAL_OUTLINE = {
        "chapters": [
            {"num": 1, "title": "第一章", "events": ["event_a"], "word_count_target": 2500}
        ]
    }

    def _run_novel_writing(self, tmp_path: Path, router: AIRouter):
        master = make_master_with_router(tmp_path, router)
        result = master.run_workflow(
            workflow_name="novel_writing",
            initial_inputs={
                "chapter_num": 1,
                "outline": self.MINIMAL_OUTLINE,
                "characters": [],
                "memory_context": {},
                "style_guide": {},
                "timeline": [],
                "use_llm": True,
            },
        )
        return result

    def test_seven_nodes_all_completed(self, tmp_path: Path):
        """Phase 7.4: novel_writing 7 节点 (read/write/review/polish_e/polish_a/merge/emit)"""
        router, _ = make_stub_router()
        result = self._run_novel_writing(tmp_path, router)

        assert result["summary"].completed == 7
        assert result["summary"].failed == 0
        assert result["summary"].paused is False

    def test_novel_writing_has_parallel_polish_nodes(self, tmp_path: Path):
        """Phase 7.4: review_chapter 后应有 2 个 polish 节点 (emotional_pacing + ai_trace_removal)"""
        router, _ = make_stub_router()
        result = self._run_novel_writing(tmp_path, router)

        executions = result["executions"]
        assert "polish_emotional_pacing" in executions
        assert "polish_ai_trace_removal" in executions
        # 拓扑: review → 2 个 polish → merge → emit
        assert "polish_merge" in executions
        assert "emit_chapter" in executions

    def test_polish_merge_output_has_content(self, tmp_path: Path):
        """Phase 7.4: polish_merge.output 含非空 content (供 emit 落盘)"""
        router, _ = make_stub_router()
        result = self._run_novel_writing(tmp_path, router)

        merge_output = result["executions"]["polish_merge"].output
        assert isinstance(merge_output, dict)
        assert "content" in merge_output
        content = merge_output["content"]
        assert isinstance(content, str)
        assert len(content) > 0, "polish_merge output.content should be non-empty"

    def test_at_least_six_llm_calls(self, tmp_path: Path):
        """Phase 7.4: 7 节点, LLM 调用 >= 6 次
        write(1) + audit(1) + emotional_pacing(2: dialogue+pacing) + ai_trace_removal(2: rule+dialogue)
        = 6 次. read_snapshot/emit_chapter/polish_merge 旁路或纯 Python."""
        router, providers = make_stub_router()
        self._run_novel_writing(tmp_path, router)

        total_calls = sum(len(p.calls) for p in providers.values())
        assert total_calls >= 6, (
            f"expected at least 6 LLM calls (write + audit + 2 polish × 2 LLM each), "
            f"got {total_calls}. per-provider: {[(n, len(p.calls)) for n, p in providers.items()]}"
        )


# === TestNovelWritingMultiVariantPolishYAML (Phase 7.4 NEW) ===

def test_novel_writing_emit_depends_on_polish_merge_yaml_static():
    """Phase 7.4: YAML 静态检查 — emit_chapter.depends_on == [polish_merge]"""
    from pathlib import Path

    import yaml

    yaml_path = Path(__file__).parent.parent.parent / "infra" / "got" / "workflows" / "novel_writing.yaml"
    assert yaml_path.exists(), f"workflow YAML not found: {yaml_path}"
    data = yaml.safe_load(yaml_path.read_text(encoding="utf-8"))

    nodes_by_id = {n["id"]: n for n in data["nodes"]}
    assert "polish_merge" in nodes_by_id, "polish_merge node must exist"
    assert "polish_emotional_pacing" in nodes_by_id
    assert "polish_ai_trace_removal" in nodes_by_id
    # polish_chapter 旧节点应已删除
    assert "polish_chapter" not in nodes_by_id, "old polish_chapter node should be removed"

    emit = nodes_by_id["emit_chapter"]
    assert "polish_merge" in emit["depends_on"], (
        f"emit_chapter.depends_on should contain polish_merge, got {emit['depends_on']}"
    )

    # polish_merge 应为 aggregation 类型
    merge_node = nodes_by_id["polish_merge"]
    assert merge_node["type"] == "aggregation", (
        f"polish_merge.type should be aggregation, got {merge_node['type']}"
    )
    # polish_merge 应依赖 2 个 polish 节点
    assert "polish_emotional_pacing" in merge_node["depends_on"]
    assert "polish_ai_trace_removal" in merge_node["depends_on"]
```

### Step 1.3: 同步 4 处 hardcode 5→7 节点 (现有测试)

**文件 1**: `tests/agent_system/test_master_controller_workflow.py:169`
```python
# Phase 7.3: 5 节点全 COMPLETED (含 polish_chapter)
assert result["summary"].completed == 5
# → 改为
# Phase 7.4: 7 节点全 COMPLETED (含 2 个并行 polish + 1 merge)
assert result["summary"].completed == 7
```

**文件 2**: `tests/agent_system/test_master_controller_workflow.py:193`
```python
# Phase 7.3: 默认起点 read_snapshot,跑完整 5 节点链
assert result["summary"].completed == 5
# → 改为
# Phase 7.4: 默认起点 read_snapshot,跑完整 7 节点链
assert result["summary"].completed == 7
```

**文件 3**: `tests/agent_system/test_decision_api.py` (1 处 `summary.completed == 5`)
- 先 grep 找位置,然后改为 7

**文件 4**: `tests/agent_system/test_master_controller_e2e_real_llm.py:231, 298`
```python
# 旧:
assert result["summary"].completed == 5
expected_ids = {"read_snapshot", "write_chapter", "review_chapter", "polish_chapter", "emit_chapter"}
# 新:
assert result["summary"].completed == 7
expected_ids = {
    "read_snapshot", "write_chapter", "review_chapter",
    "polish_emotional_pacing", "polish_ai_trace_removal",
    "polish_merge", "emit_chapter",
}
```

**文件 5**: `tests/agent_system/test_master_controller_e2e_real_llm.py:236, 243-246`
```python
# 旧: 4 次 LLM (write + audit + dialogue + pacing)
"""Phase 7.3: novel_writing 5 节点 — write(1) + audit(1) + dialogue(1) + pacing(1) = 4 次 LLM
read_snapshot/emit_chapter 旁路,polish_chapter 走 ai_trace_removal → master.polish_chapter
→ 2 LLM 路径 (Phase 7.2)"""
# ... assert total_calls >= 4
# 新: 6 次 LLM (write + audit + dialogue×2 + pacing + dialogue)
"""Phase 7.4: novel_writing 7 节点 — write(1) + audit(1) + emotional_pacing{2} + ai_trace_removal{2} = 6 次 LLM
read_snapshot/emit_chapter/polish_merge 旁路或纯 Python"""
# ... assert total_calls >= 6
```

### Step 1.4: 验证 RED

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pytest tests/agent_system/test_got_bridge.py::TestPolishHandlerRouting -v
pytest tests/agent_system/test_got_bridge.py::TestPolishMergeHandler -v
pytest tests/agent_system/test_master_controller_e2e_real_llm.py::TestNovelWritingMultiVariantPolish -v
pytest tests/agent_system/test_master_controller_e2e_real_llm.py::test_novel_writing_emit_depends_on_polish_merge_yaml_static -v
```

**Expected**: 7 个新测试 + 4 个老测试同步后全部 FAIL (因为 yaml + master + handler 还没改)
- test_novel_writing_emit_depends_on_polish_merge_yaml_static: yaml 里没有 polish_merge → KeyError
- 其他: 仍 5 节点, summary.completed==7 assert 失败

### Step 1.5: 同步 1 处 `test_got_bridge.py` 老 `test_ai_trace_removal_routes_to_polish` 期望调整

老 test 期望 `master.calls[0][0] == "polish_chapter"`(因为之前 scenario 都调 polish_chapter),改后 scenario=ai_trace_removal 调 `polish_ai_trace_removal`。**老 stub master 没有 polish_ai_trace_removal 方法**。

**2 选 1**:
- (A) 给 `_StubMaster` 加 `polish_emotional_pacing` + `polish_ai_trace_removal` 方法
- (B) 把老 test 改名为 `test_hook_extraction_routes_to_polish_chapter` 并改 scenario=hook_extraction (语义更准确)

**选 A** (向后兼容 + 最小改动)。在 `_StubMaster` 类的 `polish_chapter` 方法后追加:

```python
# Phase 7.4: 新增 2 个 variant entry methods (用于 test_got_bridge 路由测试)
def polish_emotional_pacing(self, content: str) -> str:
    self.calls.append(("polish_emotional_pacing", {"content_len": len(content)}))
    return content + " [emotional]"

def polish_ai_trace_removal(self, content: str) -> str:
    self.calls.append(("polish_ai_trace_removal", {"content_len": len(content)}))
    return content + " [ai_removed]"
```

并把老 `test_ai_trace_removal_routes_to_polish` 改为:
```python
def test_ai_trace_removal_routes_to_polish_ai_trace_removal(self):
    """Phase 7.4: scenario=ai_trace_removal 走新的 polish_ai_trace_removal entry method"""
    master = _StubMaster()
    compute = AgentComputeFn(master)
    node = _node("pol1", "ai_trace_removal")
    result = compute(node, {"content": "raw text"})
    assert result.fail is False
    assert "[ai_removed]" in result.output["content"]
    assert master.calls[0][0] == "polish_ai_trace_removal"
```

(把断言 `[polished]` 改 `[ai_removed]`, `polish_chapter` 改 `polish_ai_trace_removal`)

### Step 1.6: 重新验证 RED

```bash
pytest tests/agent_system/test_got_bridge.py -v
pytest tests/agent_system/test_master_controller_e2e_real_llm.py -v
```

**Expected**: 7 个新测试 FAIL (因为 handler / yaml 还没改), 老测试 summary.completed==5 失败

---

## Task 2: GREEN — 实施 3 个源文件改动

**Files**:
- Modify: `infra/got/workflows/novel_writing.yaml`
- Modify: `infra/agent_system/master_controller.py`
- Modify: `infra/agent_system/got_bridge.py`

### Step 2.1: novel_writing.yaml — 5 → 7 节点

替换 `infra/got/workflows/novel_writing.yaml` 整个 `nodes:` 部分:

```yaml
workflow: novel_writing
version: 2  # 升级: 5→7 节点 + AGGREGATION 引入

nodes:
  # === Stage 1: 数据输入 ===
  - id: read_snapshot
    type: input
    name: Read WorldSnapshot
    description: 读取当前 WorldSnapshot (角色 / 设定 / 已写章节摘要)
    depends_on: []
    prompt_scenario: null
    output_schema: null
    token_budget: 0
    timeout_s: 30

  # === Stage 2: 内容生成 ===
  - id: write_chapter
    type: generation
    name: Write Chapter
    description: 根据 WorldSnapshot + 主线约束,生成章节正文
    depends_on: [read_snapshot]
    prompt_scenario: chapter_writing
    output_schema: ChapterDraft
    token_budget: 8000
    timeout_s: 120

  # === Stage 3: 质量审核 ===
  - id: review_chapter
    type: evaluation
    name: Review Chapter
    description: 对章节做 S1-S8 质量审核 + LLM 深度质检
    depends_on: [write_chapter]
    prompt_scenario: chapter_review
    output_schema: QualityReport
    token_budget: 4000
    timeout_s: 90

  # === Stage 3.6: 并行多变体润色 (Phase 7.4) ===
  # review_chapter 后分叉, 两个节点无相互依赖, 同时 ready
  - id: polish_emotional_pacing
    type: synthesis
    name: Polish Emotional Pacing
    description: 情绪节奏 variant — 对话自然化 (LLM) + 节奏调整 (LLM), 跳过去 AI 痕迹规则
    depends_on: [review_chapter]
    prompt_scenario: emotional_pacing
    output_schema: PolishedDraft
    token_budget: 6000
    timeout_s: 90

  - id: polish_ai_trace_removal
    type: synthesis
    name: Polish AI Trace Removal
    description: AI 痕迹 variant — 规则去 AI 痕迹 + 对话自然化 (LLM), 跳过节奏 LLM
    depends_on: [review_chapter]
    prompt_scenario: ai_trace_removal
    output_schema: PolishedDraft
    token_budget: 6000
    timeout_s: 90

  # === Stage 3.7: AGGREGATION 合并 (Phase 7.4 NEW) ===
  - id: polish_merge
    type: aggregation
    name: Merge Polish Variants
    description: 合并 2 个并行 polish 节点结果 (Phase 7.4 策略: max(len) 选较长版本)
    depends_on: [polish_emotional_pacing, polish_ai_trace_removal]
    prompt_scenario: polish_merge
    output_schema: PolishedDraft
    token_budget: 0
    timeout_s: 30

  # === Stage 4: 落盘输出 (Phase 7.4: 改 depends_on) ===
  - id: emit_chapter
    type: output
    name: Emit Chapter
    description: 将合并润色后的章节写入 content_repo + 更新索引
    depends_on: [polish_merge]    # ← 改: 从 [polish_chapter] 改为 [polish_merge]
    prompt_scenario: null
    output_schema: null
    token_budget: 0
    timeout_s: 30
```

(同时把文件头注释里的 "5 阶段" 改为 "7 阶段 + AGGREGATION",并把"不实施"段加一条"✓ Phase 7.4 实现" )

### Step 2.2: master_controller.py — 新增 2 个 entry methods

在 `polish_chapter` (line 582) 后追加:

```python
def polish_emotional_pacing(self, content: str) -> str:
    """情绪节奏 variant — 对话自然化 + 节奏调整 (Phase 7.4)

    跳过 remove_ai_gloss 规则, 留给 ai_trace_removal 节点处理
    (两个 variant 是正交优化维度)
    """
    polished = self.polisher.optimize_dialogue_llm(content)
    polished = self.polisher.adjust_pacing_llm(polished)
    return polished

def polish_ai_trace_removal(self, content: str) -> str:
    """AI 痕迹 variant — 规则去 AI 痕迹 + 对话自然化 (Phase 7.4)

    跳过 adjust_pacing_llm, 留给 emotional_pacing 节点处理
    """
    polished = self.polisher.remove_ai_gloss(content)
    polished = self.polisher.optimize_dialogue_llm(polished)
    return polished
```

### Step 2.3: got_bridge.py — 拆 _handler_polish + 加 _handler_polish_merge

**Step 2.3a**: 替换 `_handler_polish` 函数:

```python
def _make_polish_handler(method_name: str) -> HandlerFn:
    """Phase 7.4: 工厂函数 — 每个 polish scenario 走独立 entry method

    把 _handler_polish 拆为闭包, 通过 getattr(master, method_name) 路由:
    - ai_trace_removal → master.polish_ai_trace_removal
    - emotional_pacing → master.polish_emotional_pacing
    - hook_extraction → master.polish_chapter (向后兼容, 走完整 3 路径)
    """
    def handler(master: MasterController, inputs: dict[str, Any]) -> dict[str, Any]:
        content = _resolve_field(inputs, "content", "")
        if not content:
            return {"_error": "content is required for polish scenarios"}
        method = getattr(master, method_name)
        return {"content": method(content)}
    return handler


def _handler_polish_merge(master: MasterController, inputs: dict[str, Any]) -> dict[str, Any]:
    """Phase 7.4: scenario=polish_merge — 合并多个上游 polish 节点 output

    策略: 选 max(len(content)) (避免拼接两个 variant 的矛盾, 优先保留更多润色结果)

    YAGNI: 后续可扩展 LLM 评分择优 / 按段落拼接 (Phase 7.5+)
    """
    contents: list[str] = []
    for upstream in inputs.values():
        if isinstance(upstream, dict) and "content" in upstream:
            contents.append(upstream["content"])
    if not contents:
        return {"_error": "polish_merge requires >= 1 upstream with content"}
    return {"content": max(contents, key=len)}
```

**Step 2.3b**: 替换 SCENARIO_HANDLERS 注册表中 polisher 段:

```python
# === 注册表 — 静态映射,所有 handler 在模块加载时定义 ===
SCENARIO_HANDLERS: dict[str, HandlerFn] = {
    # content_writer
    "chapter_writing": _handler_chapter_writing,
    "chapter_outline": _handler_outline_review,  # 复用 outline 生成
    # auditor
    "chapter_review": _handler_chapter_review,
    "foreshadow_scan": _handler_chapter_review,  # 复用 audit (后续可独立)
    "worldview_check": _handler_chapter_review,
    "character_consistency": _handler_chapter_review,
    "ripple_audit": _handler_chapter_review,
    # polisher (Phase 7.4: 拆为按 scenario 路由)
    "ai_trace_removal": _make_polish_handler("polish_ai_trace_removal"),
    "emotional_pacing": _make_polish_handler("polish_emotional_pacing"),
    "hook_extraction": _make_polish_handler("polish_chapter"),  # 兜底
    # outline_master
    "outline_review": _handler_outline_review,
    "subplot_suggest": _handler_outline_review,
    # AGGREGATION (Phase 7.4 NEW)
    "polish_merge": _handler_polish_merge,
}
```

### Step 2.4: 验证 GREEN

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pytest tests/agent_system/test_got_bridge.py -v
pytest tests/agent_system/test_master_controller_e2e_real_llm.py -v
pytest tests/agent_system/test_master_controller_workflow.py::TestMasterControllerRunWorkflow -v
pytest tests/agent_system/test_decision_api.py -v
```

**Expected**: 7 个新测试 + 4 个同步测试 + 老测试全 PASS

### Step 2.5: 全量验证

```bash
pytest -q
ruff check .
```

**Expected**: 2053 passed, 0 failed, 17 skipped, ruff 0

---

## Task 3: 回归 + 推送 (1 个 feat commit)

### Step 3.1: 全量测试

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pytest -q
```

**Expected**: 2053 passed, 0 failed, 17 skipped

### Step 3.2: ruff 检查

```bash
ruff check .
```

**Expected**: 0 warnings

### Step 3.3: commit + push

```bash
git add -A
git status
git commit -m "$(cat <<'EOF'
feat(agent_system): phase 7.4 — multi-variant polish + AGGREGATION merge

Doc 4 §11 Phase 7.4 (Phase 7.x polish 段, 继 7.1/7.2/7.3).

novel_writing.yaml 5→7 节点 (version 1→2):
- review_chapter 后分叉: polish_emotional_pacing (SYNTHESIS) +
  polish_ai_trace_removal (SYNTHESIS) 并行, depends_on 都指向 review_chapter
- 新 polish_merge 节点 (AGGREGATION) depends_on=[两个并行 polish],
  合并后供 emit_chapter 落盘

MasterController 新增 2 entry methods:
- polish_emotional_pacing(content) → 调 optimize_dialogue_llm + adjust_pacing_llm
- polish_ai_trace_removal(content) → 调 remove_ai_gloss + optimize_dialogue_llm
- 既有 polish_chapter(content) 保留作 hook_extraction 兜底

got_bridge.py:
- _handler_polish 拆为 _make_polish_handler(method_name) 工厂, 按 scenario 路由
- 新 _handler_polish_merge, 策略 max(len(content)) 选较长版本
- SCENARIO_HANDLERS 注册表 3 polish + 1 polish_merge 同步

7 新测试 (3 test_got_bridge 路由 + 3 test_got_bridge merge + 4 E2E):
- TestPolishHandlerRouting: emotional_pacing/ai_trace_removal/hook_extraction 路由
- TestPolishMergeHandler: max(len) 合并 + 单上游兜底 + 无 content 报错
- TestNovelWritingMultiVariantPolish: 7 节点全 COMPLETED + 拓扑 + merge content
- test_novel_writing_emit_depends_on_polish_merge_yaml_static: YAML 静态契约

同步 4 处 hardcode (5→7 节点): test_master_controller_workflow + test_decision_api
+ test_master_controller_e2e_real_llm (4 处: completed 断言 + expected_ids + LLM >= 4→6)

EOF
)"
git push origin master
```

### Step 3.4: 验证

```bash
git log --oneline -3
git status
```

**Expected**: 看到 "feat(agent_system): phase 7.4 ..." commit + 工作树干净

---

## Task 4: 更新 MEMORY.md

**File**: `~/.claude/projects/-home-ailearn-projects-AI-Incursion-domains-IP---projects-LingWen/memory/MEMORY.md`

**Step 4.1**: 找到 Phase 7.2 entry 的位置(line 10),在 Phase 7.2 entry 之前或之后(按时间倒序,在 7.2 之后)插入:

```markdown
- **Phase 7.4 (2026-06-04)**: Doc 4 §11 Phase 7.4 — Multi-Variant Polish 节点 — novel_writing.yaml 5→7 节点 (version 1→2) 拆 polish_chapter 为 2 并行 SYNTHESIS (emotional_pacing + ai_trace_removal, depends_on 都指向 review_chapter) + 1 AGGREGATION (polish_merge, depends_on=[两个并行]) + emit 改 depends_on=[polish_merge]; MasterController 新增 polish_emotional_pacing (调 optimize_dialogue_llm + adjust_pacing_llm) / polish_ai_trace_removal (调 remove_ai_gloss + optimize_dialogue_llm) 2 entry methods, 保留 polish_chapter 兜底; got_bridge.py 拆 _handler_polish → _make_polish_handler(method_name) 工厂按 scenario 路由 + 新 _handler_polish_merge (max(len(content)) 选较长版本, 避免拼接矛盾); 7 新测试 (3 路由 + 3 merge + 4 E2E 含 YAML 静态契约); 4 处 hardcode 5→7 同步 (test_master_controller_workflow + test_decision_api + test_master_controller_e2e_real_llm 4 处); 2046→2053 passed, ruff 0; spec docs/superpowers/specs/2026-06-04-phase7.4-multi-variant-polish-design.md; plan docs/superpowers/plans/2026-06-04-phase7.4-multi-variant-polish.md; 1 feat commit 推送 origin/master
```

**Step 4.2**: 验证行数

```bash
wc -l ~/.claude/projects/-home-ailearn-projects-AI-Incursion-domains-IP---projects-LingWen/memory/MEMORY.md
```

**Expected**: < 200 lines

---

## 完成定义 (DoD)

- [ ] Task 1: 7 新测试先写 → RED 状态
- [ ] Task 2: YAML + Master + handler 3 文件改 → GREEN
- [ ] Task 3: pytest 2053 passed + ruff 0 + 1 feat commit 推送
- [ ] Task 4: MEMORY.md +1 行 Phase 7.4 entry,行数 < 200
- [ ] 工作树干净 (`git status` 无未提交改动)
- [ ] 浏览器手测 (可选,前端无改动可不测)
