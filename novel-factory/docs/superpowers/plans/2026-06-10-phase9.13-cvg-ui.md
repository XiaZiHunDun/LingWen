# Phase 9.13 — CVG Frontend Review UI + WebSocket Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 Phase 9.10 (data model) + Phase 9.11 (rule-based backfill) + Phase 9.12 (LLM scanner) 3 个 backend phase 基础上,加 dashboard `/ripples` 页面 + REST API + WebSocket 实时推送,让用户可视化审批 ripple (apply/reject)。Mirror 既有 DecisionsPage / useDecisionStore / useWorkflowSocket 1:1 pattern,0 改 Phase 9.10/9.11/9.12 backend source API。

**Architecture:** 3-layer additive: (Layer1) `RippleStorage` 3 additive methods (get_ripples / get_ripple_by_id / update_ripple_status) + (Layer2) FastAPI 5 REST endpoints + 1 WebSocket `/api/ws/cvg` (跟 `/api/ws/workflows` 1:1) + (Layer3) Vue 3 新 page `/ripples` + 4 components (RippleFilter / RippleList / RippleCard / RippleDrawer) + 2 composables (useRippleStore / useRippleSocket, 跟 useDecisionStore / useWorkflowSocket 1:1) + sidebar nav entry + router entry。

**Tech Stack:** Python 3.12 + FastAPI + Pydantic + WebSocket + sqlite3 (Phase 9.10 baseline) + Vue 3 + Vite + ECharts + vitest + Playwright (smoke) + pytest 8 + ruff。

---

## Pre-Execution Setup

### Context
- Phase 9.10 (commit `c421da1`): CVG data model — 3 frozen dataclass (ReferenceNode 9 字段 / ReferenceEdge 8 字段 / CrossVolumeRipple 11 字段) + RippleStorage (sqlite3, 3 PRAGMAs, 8 public methods) + 16 tests
- Phase 9.11 (commit `149864c`, 9 commits): rule-based CVG backfill — 4 Extractor (Character/Foreshadow/Setting/PlotPoint) + Backfiller + `lingwen.py ripple backfill` CLI + 16 tests + extraction_rules.yaml
- Phase 9.12 (commit `9406936`, 11 commits): LLM scanner 4 维 + Edge 推断 8 边类型,跟 Phase 9.11 规则 1:1 验证对比。ReferenceNode 新增 `confidence: int = 1` 字段 + ReferenceEdge 新增 `evidence: str = ""` 字段,backward compat 默认值保 2335 baseline
- **Phase 9.13** (commit `0d4c3d8` spec, 937L): dashboard `/ripples` 页面 + REST API + WebSocket 实时推送

### Baseline State
- 测试: 2335 passed, 0 failed, 27 skipped
- ruff: 0 issues on new files (3 pre-existing I001 errors 在 Phase 9.13 范围外,留 followup)
- vitest: 104+ tests passed (Phase 8.45 baseline)
- Playwright: 4 e2e smoke (Phase 8.45.3 baseline)
- 主公 (用户) hard rules (MUST apply verbatim 全程):
  - **0 force-push, 0 amend, 0 --no-verify, 0 Co-Authored-By footer** (Attribution disabled globally via `~/.claude/settings.json`)
  - **0 DANGEROUSLY_SKIP_PERMISSIONS, 0 API key 泄漏, 0 .env 改**
  - **0 改 CLAUDE.md / pyproject.toml / pytest.ini / vite.config.js / vitest.config.js / playwright.config.js / 12 SCENARIOS 路由 / historical spec/plan doc `:NNN` 行号**
  - **0 改 Phase 9.10/9.11/9.12 现有 source API 行为** (仅 additive RippleStorage methods + 1-line defensive try/except broadcast hook in `append_nodes_atomic`, 跟 Phase 8.5 `cost_tracker.record()` 1:1 pattern)
  - **0 改 dashboard 既有 endpoint handler / composable / store / router / sidebar nav / api method** (新增 5+1 endpoint + 2 composable + 4 component + 1 page + 5 api method + 1 router entry + 1 nav item, 全部 additive)
  - **0 production code 改** (MasterController / got_bridge / workflow.db / cost_tracker.db 0 行)
  - File size 200-400L typical / 800L hard cap
  - TDD methodology (RED → GREEN → commit per task)
  - Conventional commits format
  - 沟通: 叙述中文 / 代码英文 / commit body 中文注释
  - Git root: `/home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/`
  - 当前 working dir: `novel-factory/`
  - MEMORY.md < 200 lines (current 33, Phase 9.13 后 ~35, 留 followup 拆)

### Target State
- 测试: **2370+ passed, 0 failed, 27 skipped** (2335 baseline + 35 new pytest)
- vitest: **+21 new** (104 baseline + 21 new = 125+)
- Playwright: **+3 new** (4 baseline + 3 new = 7)
- ruff: 0 issues on new files
- pnpm lint:all: 0 errors / ≤8 warnings (跟 Phase 8.45 baseline 一致)
- pnpm build: 0 error
- pnpm e2e:smoke --list: 7 tests (4 Phase 8.45.3 + 3 Phase 9.13)
- 0 改 Phase 9.10/9.11/9.12 现有 source API 行为 (仅 1-line defensive try/except broadcast hook)
- 8 feat commits + 1 spec commit + 1 plan commit (10 total)
- 0 force-push / 0 amend / 0 Co-Authored-By

### File map summary
- **6 modified**: `infra/cross_volume/storage.py` / `dashboard/app.py` / `dashboard/protocols.py` / `dashboard/frontend/src/api/index.js` / `dashboard/frontend/src/router/index.js` / `dashboard/frontend/src/components/Sidebar.vue`
- **8 new production**: `dashboard/cvg_ws.py` / `useRippleStore.js` / `useRippleSocket.js` / `RippleFilter.vue` / `RippleList.vue` / `RippleCard.vue` / `RippleDrawer.vue` / `RipplesPage.vue`
- **2 docs**: spec (`docs/superpowers/specs/2026-06-10-phase9.13-cvg-ui-design.md`, commit 0d4c3d8) + plan (本文件)
- **3 new pytest**: `tests/cross_volume/test_storage_ripple_action.py` (12 tests) / `tests/dashboard/test_cvg_api.py` (15 tests) / `tests/dashboard/test_cvg_ws.py` (8 tests)
- **5 new vitest**: `use-ripple-store.spec.js` (6) / `use-ripple-socket.spec.js` (5) / `ripple-card.spec.js` (4) / `ripple-filter.spec.js` (3) / `ripple-drawer.spec.js` (3)
- **1 new e2e**: `dashboard/frontend/tests/e2e-smoke/ripples.spec.js` (3 tests)

---

## Critical safety (主公 hard rules)

> **0 force-push, 0 amend, 0 --no-verify, 0 Co-Authored-By footer** — 写代码前 verify 已设 `git config user.email` 跟 `git config user.name`,提交用 conventional commit 格式。
>
> **0 改 Phase 9.10/9.11/9.12 现有 source API 行为** — 仅 additive,1-line defensive try/except broadcast hook in `append_nodes_atomic` 是唯一例外 (跟 `cost_tracker.record()` 1:1 pattern)。
>
> **0 DANGEROUSLY_SKIP_PERMISSIONS** — 所有 bash 命令正常走,permission prompt 出现就 approve,不要加 `--no-verify` / `--force`。
>
> **0 改 historical spec/plan doc 行号** — historical 文件 `:NNN` 引用已不准,改反误导,永不做。
>
> **File size guard** — 任何 `.py` / `.vue` / `.js` 文件超 800L 必须先拆 module 再 commit。

---

## Task 1: RippleStorage 3 additive methods + ConflictError + 12 tests

**Files:**
- Modify: `infra/cross_volume/storage.py:1-220` (additive, +80L, new methods at file end)
- Create: `tests/cross_volume/test_storage_ripple_action.py` (NEW, ~150L, 12 tests)
- Test: `pytest tests/cross_volume/test_storage_ripple_action.py -v`

**Goal:** add `get_ripples` (3-filter + pagination) / `get_ripple_by_id` (single fetch) / `update_ripple_status` (status change + 1-line defensive broadcast hook in `append_nodes_atomic`) + new `ConflictError` exception。0 改 existing 8 methods,signature/return type 100% 保 2335 baseline。

- [ ] **Step 1.1: Write 12 failing tests (RED)**

```python
# tests/cross_volume/test_storage_ripple_action.py
"""Phase 9.13: storage 3 new methods (get_ripples / get_ripple_by_id / update_ripple_status) + ConflictError."""
import pytest
from datetime import datetime, timezone
from infra.cross_volume.reference_graph import ReferenceNode, ReferenceEdge
from infra.cross_volume.ripple import CrossVolumeRipple
from infra.cross_volume.storage import RippleStorage, ConflictError


def _make_ripple(status="pending", volume=1, chapter=1):
    return CrossVolumeRipple(
        id=f"rip-{status}-{volume}-{chapter}",
        trigger_volume=volume,
        trigger_chapter=chapter,
        affected_nodes=(),
        affected_edges=(),
        proposed_actions=(),
        status=status,
    )


@pytest.fixture
def storage(tmp_path):
    return RippleStorage(db_path=tmp_path / "test.db")


class TestGetRipples:
    def test_empty_storage_returns_empty_list(self, storage):
        assert storage.get_ripples() == []

    def test_filter_by_status(self, storage):
        for s in ("pending", "applied", "rejected"):
            storage.append_ripple(_make_ripple(status=s))
        result = storage.get_ripples(status="applied")
        assert len(result) == 1
        assert result[0].status == "applied"

    def test_filter_by_volume(self, storage):
        for v in (1, 2, 3):
            storage.append_ripple(_make_ripple(volume=v))
        result = storage.get_ripples(volume=2)
        assert len(result) == 1
        assert result[0].trigger_volume == 2

    def test_filter_combined(self, storage):
        for s in ("pending", "applied"):
            for v in (1, 2):
                storage.append_ripple(_make_ripple(status=s, volume=v))
        result = storage.get_ripples(status="pending", volume=1)
        assert len(result) == 1

    def test_pagination_limit_offset(self, storage):
        for i in range(5):
            storage.append_ripple(_make_ripple(chapter=i))
        page1 = storage.get_ripples(limit=2, offset=0)
        page2 = storage.get_ripples(limit=2, offset=2)
        assert len(page1) == 2
        assert len(page2) == 2
        assert page1[0].id != page2[0].id


class TestGetRippleById:
    def test_existing_ripple_returns_it(self, storage):
        r = _make_ripple()
        storage.append_ripple(r)
        fetched = storage.get_ripple_by_id(r.id)
        assert fetched is not None
        assert fetched.id == r.id

    def test_missing_ripple_returns_none(self, storage):
        assert storage.get_ripple_by_id("nonexistent") is None


class TestUpdateRippleStatus:
    def test_apply_pending_changes_status_and_timestamps(self, storage):
        r = _make_ripple(status="pending")
        storage.append_ripple(r)
        updated = storage.update_ripple_status(r.id, "applied", actor="user")
        assert updated.status == "applied"
        assert updated.applied_at is not None
        assert storage.get_ripple_by_id(r.id).status == "applied"

    def test_reject_pending_changes_status(self, storage):
        r = _make_ripple(status="pending")
        storage.append_ripple(r)
        storage.update_ripple_status(r.id, "rejected", actor="user")
        assert storage.get_ripple_by_id(r.id).status == "rejected"

    def test_invalid_status_raises_value_error(self, storage):
        r = _make_ripple()
        storage.append_ripple(r)
        with pytest.raises(ValueError):
            storage.update_ripple_status(r.id, "weird_status")

    def test_missing_ripple_raises_key_error(self, storage):
        with pytest.raises(KeyError):
            storage.update_ripple_status("nope", "applied")

    def test_already_terminal_raises_conflict_error(self, storage):
        r = _make_ripple(status="applied")
        storage.append_ripple(r)
        with pytest.raises(ConflictError):
            storage.update_ripple_status(r.id, "rejected")

    def test_broadcast_hook_called_on_status_change(self, storage, monkeypatch):
        """Phase 9.13: update_ripple_status 末尾 lazy import + broadcast WS event."""
        broadcast_called = []

        # Mock dashboard.cvg_ws (lazy import target)
        import sys
        class _MockManager:
            def broadcast(self, event):
                broadcast_called.append(event)
        mock_module = type(sys)("dashboard.cvg_ws")
        mock_module.broadcast = _MockManager().broadcast
        sys.modules["dashboard.cvg_ws"] = mock_module

        r = _make_ripple()
        storage.append_ripple(r)
        storage.update_ripple_status(r.id, "applied", actor="user")

        assert len(broadcast_called) == 1
        assert broadcast_called[0]["type"] == "ripple_status_changed"
        assert broadcast_called[0]["data"]["ripple_id"] == r.id
```

- [ ] **Step 1.2: Run tests, verify RED**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pytest tests/cross_volume/test_storage_ripple_action.py -v 2>&1 | tail -20
# 期望: ImportError (ConflictError not found) — RED 确认
```

- [ ] **Step 1.3: Add `ConflictError` exception + 3 new methods to `infra/cross_volume/storage.py`**

Append at end of file (after `_row_to_ripple` method, before EOF):

```python
# infra/cross_volume/storage.py (additive, Phase 9.13)


class ConflictError(Exception):
    """Raised when an operation conflicts with current ripple state.

    Used by update_ripple_status() when the target ripple is already in
    a terminal status (applied/rejected/failed) and cannot be transitioned.
    API layer translates this to HTTP 409.
    """


_TERMINAL_STATUSES = ("applied", "rejected", "failed")
_VALID_TRANSITION_STATUSES = ("applied", "rejected")


def _broadcast_ripple_event(event_type: str, data: dict) -> None:
    """Phase 9.13: defensive 1-line broadcast hook.

    Lazy import dashboard.cvg_ws to avoid hard dashboard dependency in CLI
    environment. Wrapped in try/except — broadcast failure never affects
    main write path (跟 Phase 8.5 cost_tracker.record() 1:1 pattern).
    """
    try:
        from dashboard.cvg_ws import broadcast as _cvg_broadcast
        _cvg_broadcast({"type": event_type, "data": data})
    except Exception:  # noqa: BLE001
        logger.debug("cvg_ws broadcast skipped (dashboard not configured)")


# Add to RippleStorage class body (after append_nodes_atomic):


    def get_ripples(
        self,
        status: str | None = None,
        dimension: str | None = None,
        volume: int | None = None,
        limit: int = 50,
        offset: int = 0,
    ) -> list[CrossVolumeRipple]:
        """Filter ripples by status + volume (JOIN via trigger_volume).

        dimension filter: 留 Phase 9.14 扩展 (目前 reference_ripples 表无 dimension 字段,
        source/target 节点 dimension 需从 reference_nodes JOIN;Phase 9.13 UI 仅 3 filter)
        """
        clauses, params = [], []
        if status is not None:
            clauses.append("status = ?")
            params.append(status)
        if volume is not None:
            clauses.append("trigger_volume = ?")
            params.append(volume)
        where = f"WHERE {' AND '.join(clauses)}" if clauses else ""
        sql = f"SELECT * FROM reference_ripples {where} LIMIT ? OFFSET ?"
        params.extend([limit, offset])
        with self._connect() as conn:
            return [self._row_to_ripple(row) for row in conn.execute(sql, params)]

    def get_ripple_by_id(self, ripple_id: str) -> CrossVolumeRipple | None:
        with self._connect() as conn:
            row = conn.execute(
                "SELECT * FROM reference_ripples WHERE id = ?", (ripple_id,)
            ).fetchone()
            return self._row_to_ripple(row) if row else None

    def update_ripple_status(
        self,
        ripple_id: str,
        new_status: str,
        actor: str = "user",
    ) -> CrossVolumeRipple:
        """Status change + applied_at + WS broadcast.

        Validation:
            - ripple_id 必须存在 (else raise KeyError, API 转 404)
            - new_status 必须 in (applied, rejected) (else raise ValueError)
            - 当前 status 不能是 terminal, 否则 raise ConflictError (API 转 409)
        """
        if new_status not in _VALID_TRANSITION_STATUSES:
            raise ValueError(
                f"new_status must be one of {_VALID_TRANSITION_STATUSES}, got {new_status!r}"
            )
        with self._connect() as conn:
            row = conn.execute(
                "SELECT * FROM reference_ripples WHERE id = ?", (ripple_id,)
            ).fetchone()
            if row is None:
                raise KeyError(f"ripple {ripple_id} not found")
            current = self._row_to_ripple(row)
            if current.status in _TERMINAL_STATUSES:
                raise ConflictError(
                    f"ripple {ripple_id} already in terminal status {current.status!r}"
                )
            now_iso = datetime.now(timezone.utc).isoformat()
            conn.execute(
                "UPDATE reference_ripples SET status = ?, applied_at = ? WHERE id = ?",
                (new_status, now_iso, ripple_id),
            )
            conn.commit()
        _broadcast_ripple_event(
            "ripple_status_changed",
            {"ripple_id": ripple_id, "new_status": new_status, "actor": actor, "applied_at": now_iso},
        )
        return self.get_ripple_by_id(ripple_id)
```

- [ ] **Step 1.4: Add 1-line defensive broadcast hook to `append_nodes_atomic` (Phase 9.13 contract)**

Modify `infra/cross_volume/storage.py:159-176` `append_nodes_atomic` method to call broadcast at end:

```python
    def append_nodes_atomic(self, nodes: list[ReferenceNode]) -> None:
        """Phase 9.11: 1 atomic_batch wrap N inserts, 0 partial commit.

        Phase 9.13: 末尾 1-line defensive try/except broadcast hook — public
        signature 0 改,调用方 (Phase 9.11/9.12 CLI) 0 改。Broadcast 失败永
        不影响主写入路径 (跟 Phase 8.5 cost_tracker.record() 1:1 pattern)。
        """
        with self.atomic_batch() as conn:
            for node in nodes:
                try:
                    conn.execute(
                        "INSERT INTO reference_nodes VALUES (?,?,?,?,?,?,?,?,?)",
                        (node.id, node.dimension, node.volume, node.chapter,
                         node.title, node.description,
                         json.dumps(node.payload, ensure_ascii=False),
                         node.created_at.isoformat(), node.created_by),
                    )
                except sqlite3.IntegrityError as e:
                    raise ValueError(f"storage integrity: {e}") from e
        # Phase 9.13: 1-line defensive broadcast (per new ripple_created)
        _broadcast_ripple_event(
            "ripple_created",
            {"node_ids": [n.id for n in nodes]},
        )
```

- [ ] **Step 1.5: Run tests, verify GREEN**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pytest tests/cross_volume/test_storage_ripple_action.py -v 2>&1 | tail -25
# 期望: 12 passed
```

- [ ] **Step 1.6: Run baseline, ensure 0 regression**

```bash
pytest -q 2>&1 | tail -3
# 期望: 2335 + 12 = 2347 passed, 0 failed, 27 skipped
```

- [ ] **Step 1.7: ruff check**

```bash
ruff check infra/cross_volume/storage.py tests/cross_volume/test_storage_ripple_action.py 2>&1 | tail -3
# 期望: 0 issues
```

- [ ] **Step 1.8: Commit**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
git add novel-factory/infra/cross_volume/storage.py \
        novel-factory/tests/cross_volume/test_storage_ripple_action.py
git commit -m "feat(cvg): phase9.13 task1 — RippleStorage 3 additive methods + ConflictError + tests

Phase 9.13 storage layer: add 3 new methods on RippleStorage + ConflictError
exception + 1-line defensive broadcast hook in append_nodes_atomic.

改动:
- infra/cross_volume/storage.py (+120L, 4 new things):
  - ConflictError exception (API layer translates to HTTP 409)
  - _broadcast_ripple_event() module-level helper (lazy import dashboard.cvg_ws,
    try/except — never raises, 跟 Phase 8.5 cost_tracker.record() 1:1 pattern)
  - get_ripples(status, volume, limit, offset) — 3-way filter + pagination
  - get_ripple_by_id(ripple_id) — single fetch, None if not found
  - update_ripple_status(ripple_id, new_status, actor) — status change +
    applied_at + WS broadcast (key validation: KeyError 404, ValueError 422,
    ConflictError 409)
  - append_nodes_atomic() 末尾加 1-line _broadcast_ripple_event(\"ripple_created\")
    call (public signature 0 改, 调用方 Phase 9.11/9.12 CLI 0 改)
- tests/cross_volume/test_storage_ripple_action.py (+150L, NEW, 12 tests):
  - TestGetRipples: 5 tests (empty / status / volume / combined / pagination)
  - TestGetRippleById: 2 tests (existing / missing)
  - TestUpdateRippleStatus: 5 tests (apply / reject / invalid / missing /
    terminal-conflict) + 1 broadcast hook test (mock dashboard.cvg_ws via sys.modules)

不改:
- 0 改 existing 8 storage methods (append_node/edge/ripple/append_nodes_atomic
  public signature + return type 0 改)
- 0 改 Phase 9.10/9.11/9.12 source API 行为 (除 1-line defensive try/except
  broadcast hook, 跟 cost_tracker.record() 1:1 pattern)
- 0 改 conflict detection 逻辑
- 0 改 WS broadcast protocol (event payload schema 跟 spec 4.2.3 一致)

Baseline:
- 测试: 2335 + 12 = 2347 passed, 0 failed, 27 skipped
- ruff: 0 issues

后续: Task 2 加 5 REST endpoints + 4 Pydantic schemas"
```

---

## Task 2: FastAPI 5 REST endpoints + 4 Pydantic schemas + 15 tests

**Files:**
- Modify: `dashboard/protocols.py:1-616` (additive, +80L, 4 Pydantic models at file end)
- Modify: `dashboard/app.py:1-1051` (additive, +150L, 5 endpoints after existing decision endpoints)
- Create: `tests/dashboard/test_cvg_api.py` (NEW, ~250L, 15 tests)
- Test: `pytest tests/dashboard/test_cvg_api.py -v`

**Goal:** add 5 REST endpoints (list / detail / apply / reject / stats) + 4 Pydantic schemas (RippleListItemResponse / RippleDetailResponse / RippleActionResponse / RippleStatsResponse)。0 改 existing endpoint handler,signature/return type 100% 保 baseline。

- [ ] **Step 2.1: Write 15 failing tests (RED)**

```python
# tests/dashboard/test_cvg_api.py
"""Phase 9.13: 5 REST endpoints + 4 Pydantic schemas."""
import pytest
from datetime import datetime
from fastapi.testclient import TestClient
from infra.cross_volume.ripple import CrossVolumeRipple
from infra.cross_volume.storage import RippleStorage, ConflictError
from dashboard.app import create_app
from dashboard.protocols import (
    RippleListItemResponse,
    RippleDetailResponse,
    RippleActionResponse,
    RippleStatsResponse,
)


@pytest.fixture
def storage(tmp_path):
    return RippleStorage(db_path=tmp_path / "test.db")


@pytest.fixture
def client(storage, monkeypatch):
    """Create FastAPI TestClient with cvg_storage dependency overridden."""
    from dashboard import app as app_module
    monkeypatch.setattr(app_module, "_default_storage", lambda: storage)
    app = create_app()
    return TestClient(app)


def _seed(storage, count=3, status="pending"):
    ids = []
    for i in range(count):
        r = CrossVolumeRipple(
            id=f"rip-{status}-{i}",
            trigger_volume=(i % 3) + 1,
            trigger_chapter=i,
            status=status,
        )
        storage.append_ripple(r)
        ids.append(r.id)
    return ids


class TestListRipples:
    def test_empty_returns_empty_list(self, client):
        resp = client.get("/api/cvg/ripples")
        assert resp.status_code == 200
        assert resp.json() == []

    def test_seeded_returns_list(self, client, storage):
        _seed(storage, count=2)
        resp = client.get("/api/cvg/ripples")
        assert resp.status_code == 200
        data = resp.json()
        assert len(data) == 2
        assert "ripple_id" in data[0]
        assert "status" in data[0]

    def test_filter_by_status(self, client, storage):
        _seed(storage, count=2, status="pending")
        _seed(storage, count=1, status="applied")
        resp = client.get("/api/cvg/ripples?status=applied")
        assert len(resp.json()) == 1
        assert resp.json()[0]["status"] == "applied"

    def test_filter_invalid_status_returns_422(self, client):
        resp = client.get("/api/cvg/ripples?status=invalid")
        assert resp.status_code == 422

    def test_pagination(self, client, storage):
        _seed(storage, count=5)
        resp1 = client.get("/api/cvg/ripples?limit=2&offset=0")
        resp2 = client.get("/api/cvg/ripples?limit=2&offset=2")
        assert len(resp1.json()) == 2
        assert len(resp2.json()) == 2


class TestGetRippleDetail:
    def test_existing_returns_detail(self, client, storage):
        ids = _seed(storage, count=1)
        resp = client.get(f"/api/cvg/ripples/{ids[0]}")
        assert resp.status_code == 200
        data = resp.json()
        assert data["ripple_id"] == ids[0]
        assert "evidence" in data
        assert "source_payload" in data

    def test_missing_returns_404(self, client):
        resp = client.get("/api/cvg/ripples/nonexistent")
        assert resp.status_code == 404


class TestApplyRipple:
    def test_apply_pending_returns_200(self, client, storage):
        ids = _seed(storage, count=1, status="pending")
        resp = client.post(f"/api/cvg/ripples/{ids[0]}/apply")
        assert resp.status_code == 200
        data = resp.json()
        assert data["status"] == "applied"
        assert data["actor"] == "user"

    def test_apply_missing_returns_404(self, client):
        resp = client.post("/api/cvg/ripples/nope/apply")
        assert resp.status_code == 404

    def test_apply_terminal_returns_409(self, client, storage):
        ids = _seed(storage, count=1, status="applied")
        resp = client.post(f"/api/cvg/ripples/{ids[0]}/apply")
        assert resp.status_code == 409


class TestRejectRipple:
    def test_reject_pending_returns_200(self, client, storage):
        ids = _seed(storage, count=1, status="pending")
        resp = client.post(f"/api/cvg/ripples/{ids[0]}/reject?reason=测试")
        assert resp.status_code == 200
        assert resp.json()["status"] == "rejected"

    def test_reject_terminal_returns_409(self, client, storage):
        ids = _seed(storage, count=1, status="rejected")
        resp = client.post(f"/api/cvg/ripples/{ids[0]}/reject")
        assert resp.status_code == 409


class TestGetRippleStats:
    def test_stats_returns_count_by_status_and_volume(self, client, storage):
        _seed(storage, count=2, status="pending")
        _seed(storage, count=1, status="applied")
        resp = client.get("/api/cvg/ripples/stats")
        assert resp.status_code == 200
        data = resp.json()
        assert data["by_status"]["pending"] == 2
        assert data["by_status"]["applied"] == 1
        assert data["total"] == 3

    def test_pydantic_schema_importable(self):
        """Phase 9.13: 4 Pydantic schemas in dashboard.protocols."""
        assert RippleListItemResponse is not None
        assert RippleDetailResponse is not None
        assert RippleActionResponse is not None
        assert RippleStatsResponse is not None
```

- [ ] **Step 2.2: Run tests, verify RED**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pytest tests/dashboard/test_cvg_api.py -v 2>&1 | tail -20
# 期望: ImportError (4 schemas not found) + 404 (5 endpoints not found) — RED 确认
```

- [ ] **Step 2.3: Add 4 Pydantic schemas to `dashboard/protocols.py` (at file end)**

```python
# dashboard/protocols.py (additive, Phase 9.13)


class RippleListItemResponse(BaseModel):
    """Phase 9.13: ripple list item (跟 DecisionResponse 1:1 mirror)."""
    ripple_id: str
    dimension: str  # placeholder, real value via JOIN in future
    relationship_type: str
    source_chapter: int
    target_chapter: int
    status: str
    confidence: int = 1  # Phase 9.12 additive
    created_at: datetime


class RippleDetailResponse(RippleListItemResponse):
    """Phase 9.13: ripple detail (extends list item, 跟 DecisionDetailResponse 1:1)."""
    evidence: str = ""
    source_payload: dict[str, Any] = Field(default_factory=dict)
    target_payload: dict[str, Any] = Field(default_factory=dict)
    edge_payload: dict[str, Any] = Field(default_factory=dict)


class RippleActionResponse(BaseModel):
    """Phase 9.13: apply/reject action response."""
    ripple_id: str
    status: str
    actor: str
    applied_at: Optional[datetime] = None


class RippleStatsResponse(BaseModel):
    """Phase 9.13: ripple stats (count by status + volume, 跟 DecisionStats 1:1)."""
    total: int
    by_status: dict[str, int]
    by_volume: dict[str, int] = Field(default_factory=dict)
```

- [ ] **Step 2.4: Add 5 REST endpoints to `dashboard/app.py` (after existing decision endpoints, ~line 630)**

```python
# dashboard/app.py (additive, Phase 9.13, 5 endpoints + 1 helper)


def _ripple_to_list_item(r: CrossVolumeRipple) -> RippleListItemResponse:
    """Phase 9.13: helper to convert CrossVolumeRipple → RippleListItemResponse.

    dimension / relationship_type 暂用 placeholder (Phase 9.14 通过 JOIN reference_nodes
    + reference_edges 填充); source_chapter / target_chapter 从 affected_nodes 第一个
    提取 (placeholder 模式, Phase 9.14 真实化)。
    """
    return RippleListItemResponse(
        ripple_id=r.id,
        dimension="unknown",  # TODO: Phase 9.14 JOIN reference_nodes
        relationship_type="mentions",  # TODO: Phase 9.14 JOIN reference_edges
        source_chapter=r.trigger_chapter,
        target_chapter=r.trigger_chapter,
        status=r.status,
        confidence=r.payload.get("confidence", 1),
        created_at=r.created_at,
    )


def _ripple_to_detail(r: CrossVolumeRipple) -> RippleDetailResponse:
    return RippleDetailResponse(
        ripple_id=r.id,
        dimension="unknown",
        relationship_type="mentions",
        source_chapter=r.trigger_chapter,
        target_chapter=r.trigger_chapter,
        status=r.status,
        confidence=r.payload.get("confidence", 1),
        created_at=r.created_at,
        evidence=r.payload.get("evidence", ""),
        source_payload=r.payload.get("source_payload", {}),
        target_payload=r.payload.get("target_payload", {}),
        edge_payload=r.payload.get("edge_payload", {}),
    )


# In create_app() body, after existing decision endpoints (around line 630+):


    # ==================== Phase 9.13: CVG Ripple Endpoints ====================

    @app.get("/api/cvg/ripples", response_model=list[RippleListItemResponse])
    def list_ripples(
        status_filter: Optional[str] = Query(None, alias="status", regex="^(pending|confirmed|applied|rejected|failed)$"),
        volume: Optional[int] = Query(None, ge=1, le=3),
        limit: int = Query(50, ge=1, le=200),
        offset: int = Query(0, ge=0),
    ) -> list[RippleListItemResponse]:
        storage = _default_storage()
        ripples = storage.get_ripples(status=status_filter, volume=volume, limit=limit, offset=offset)
        return [_ripple_to_list_item(r) for r in ripples]

    @app.get("/api/cvg/ripples/stats", response_model=RippleStatsResponse)
    def get_ripple_stats() -> RippleStatsResponse:
        storage = _default_storage()
        all_ripples = storage.get_ripples(limit=200)
        by_status: dict[str, int] = {}
        by_volume: dict[str, int] = {}
        for r in all_ripples:
            by_status[r.status] = by_status.get(r.status, 0) + 1
            by_volume[str(r.trigger_volume)] = by_volume.get(str(r.trigger_volume), 0) + 1
        return RippleStatsResponse(total=len(all_ripples), by_status=by_status, by_volume=by_volume)

    @app.get("/api/cvg/ripples/{ripple_id}", response_model=RippleDetailResponse)
    def get_ripple_detail(ripple_id: str) -> RippleDetailResponse:
        storage = _default_storage()
        ripple = storage.get_ripple_by_id(ripple_id)
        if ripple is None:
            raise HTTPException(status_code=404, detail=f"ripple {ripple_id} not found")
        return _ripple_to_detail(ripple)

    @app.post("/api/cvg/ripples/{ripple_id}/apply", response_model=RippleActionResponse)
    def apply_ripple(ripple_id: str) -> RippleActionResponse:
        storage = _default_storage()
        try:
            ripple = storage.update_ripple_status(ripple_id, "applied", actor="user")
        except KeyError:
            raise HTTPException(status_code=404, detail=f"ripple {ripple_id} not found")
        except ValueError as e:
            raise HTTPException(status_code=422, detail=str(e))
        except ConflictError as e:
            raise HTTPException(status_code=409, detail=str(e))
        return RippleActionResponse(
            ripple_id=ripple_id,
            status="applied",
            actor="user",
            applied_at=ripple.applied_at,
        )

    @app.post("/api/cvg/ripples/{ripple_id}/reject", response_model=RippleActionResponse)
    def reject_ripple(ripple_id: str, reason: Optional[str] = None) -> RippleActionResponse:
        storage = _default_storage()
        try:
            ripple = storage.update_ripple_status(ripple_id, "rejected", actor="user")
        except KeyError:
            raise HTTPException(status_code=404, detail=f"ripple {ripple_id} not found")
        except ValueError as e:
            raise HTTPException(status_code=422, detail=str(e))
        except ConflictError as e:
            raise HTTPException(status_code=409, detail=str(e))
        return RippleActionResponse(
            ripple_id=ripple_id,
            status="rejected",
            actor="user",
            applied_at=ripple.applied_at,
        )
```

- [ ] **Step 2.5: Add `_default_storage` helper to `dashboard/app.py` (top of file, near other module-level helpers)**

```python
# dashboard/app.py (additive, Phase 9.13)

# Top of file imports, add:
from dashboard.protocols import (
    # ... existing imports ...,
    RippleListItemResponse,
    RippleDetailResponse,
    RippleActionResponse,
    RippleStatsResponse,
)
from infra.cross_volume.ripple import CrossVolumeRipple
from infra.cross_volume.storage import ConflictError

# Module-level helper (additive, near other module-level helpers):

_DEFAULT_CVG_DB_PATH = Path("infra/.state/cross_volume.db")


def _default_storage() -> RippleStorage:
    """Phase 9.13: singleton RippleStorage for cvg endpoints.

    Lazy init: first call creates RippleStorage, subsequent calls return cached.
    跟 dashboard/_default_decision_queue 1:1 pattern.
    """
    global _default_storage_instance
    if _default_storage_instance is None:
        _default_storage_instance = RippleStorage(db_path=_DEFAULT_CVG_DB_PATH)
    return _default_storage_instance


_default_storage_instance: RippleStorage | None = None
```

- [ ] **Step 2.6: Run tests, verify GREEN**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pytest tests/dashboard/test_cvg_api.py -v 2>&1 | tail -25
# 期望: 15 passed
```

- [ ] **Step 2.7: Run baseline, ensure 0 regression**

```bash
pytest -q 2>&1 | tail -3
# 期望: 2347 + 15 = 2362 passed, 0 failed, 27 skipped
```

- [ ] **Step 2.8: ruff check**

```bash
ruff check dashboard/ tests/dashboard/test_cvg_api.py 2>&1 | tail -3
# 期望: 0 issues
```

- [ ] **Step 2.9: Commit**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
git add novel-factory/dashboard/protocols.py \
        novel-factory/dashboard/app.py \
        novel-factory/tests/dashboard/test_cvg_api.py
git commit -m "feat(cvg): phase9.13 task2 — FastAPI 5 REST endpoints + 4 Pydantic schemas + tests

Phase 9.13 backend layer: 5 REST endpoints (list/detail/apply/reject/stats)
+ 4 Pydantic schemas + _default_storage() singleton helper。

改动:
- dashboard/protocols.py (+80L, additive): 4 Pydantic models
  - RippleListItemResponse (10 fields, 跟 DecisionResponse 1:1 mirror)
  - RippleDetailResponse extends RippleListItemResponse (4 extra fields: evidence +
    source_payload + target_payload + edge_payload, 跟 DecisionDetailResponse 1:1)
  - RippleActionResponse (4 fields: ripple_id / status / actor / applied_at)
  - RippleStatsResponse (3 fields: total + by_status + by_volume)
- dashboard/app.py (+150L, additive): 5 REST endpoints + 2 helpers + 1 singleton
  - _ripple_to_list_item() / _ripple_to_detail() conversion helpers
  - _default_storage() lazy singleton (跟 _default_decision_queue 1:1 pattern)
  - GET /api/cvg/ripples (list + filter + pagination)
  - GET /api/cvg/ripples/stats (count by status + volume)
  - GET /api/cvg/ripples/{id} (detail, 404 if missing)
  - POST /api/cvg/ripples/{id}/apply (status → applied, 409 if terminal)
  - POST /api/cvg/ripples/{id}/reject (status → rejected, 409 if terminal)
- tests/dashboard/test_cvg_api.py (+250L, NEW, 15 tests):
  - TestListRipples: 5 tests (empty / seeded / filter / 422 / pagination)
  - TestGetRippleDetail: 2 tests (existing / 404)
  - TestApplyRipple: 3 tests (200 / 404 / 409)
  - TestRejectRipple: 2 tests (200 / 409)
  - TestGetRippleStats: 2 tests (count by status/volume / schema importable)
  - 1 sanity test (4 Pydantic models importable from dashboard.protocols)

不改:
- 0 改 existing endpoint handler (DecisionResponse / WorkflowListItem / etc.)
- 0 改 existing Pydantic schema in protocols.py (4 new additive only)
- 0 改 create_app() 主流程 (5 new endpoint 在 decision endpoint 后注册)
- 0 改 MasterController / cost_tracker / workflow.db

Baseline:
- 测试: 2347 + 15 = 2362 passed, 0 failed, 27 skipped
- ruff: 0 issues

后续: Task 3 加 WebSocket /api/ws/cvg + CvgConnectionManager"
```

---

## Task 3: WebSocket /api/ws/cvg + CvgConnectionManager + 8 tests

**Files:**
- Create: `dashboard/cvg_ws.py` (NEW, ~80L, CvgConnectionManager class)
- Modify: `dashboard/app.py:857-889` (additive, +30L, WS endpoint after /api/ws/workflows)
- Create: `tests/dashboard/test_cvg_ws.py` (NEW, ~150L, 8 tests)
- Test: `pytest tests/dashboard/test_cvg_ws.py -v`

**Goal:** add `/api/ws/cvg` WebSocket endpoint + `CvgConnectionManager` class (跟 `ConnectionManager` 1:1 mirror)。3 message types: `ripple_created` / `ripple_status_changed` / `pong`。0 改 `dashboard/ws.py` 既有 `ConnectionManager` class。

- [ ] **Step 3.1: Write 8 failing tests (RED)**

```python
# tests/dashboard/test_cvg_ws.py
"""Phase 9.13: WebSocket /api/ws/cvg + CvgConnectionManager (跟 /api/ws/workflows 1:1)."""
import asyncio
import pytest
from fastapi import WebSocket
from starlette.testclient import TestClient
from dashboard.app import create_app
from dashboard.cvg_ws import CvgConnectionManager, broadcast, EVENT_RIPPLE_CREATED, EVENT_RIPPLE_STATUS_CHANGED


class TestCvgConnectionManager:
    @pytest.mark.asyncio
    async def test_connect_and_disconnect(self):
        manager = CvgConnectionManager()
        ws = _make_mock_ws()
        await manager.connect(ws)
        assert ws in manager._connections
        await manager.disconnect(ws)
        assert ws not in manager._connections

    @pytest.mark.asyncio
    async def test_broadcast_to_all_connections(self):
        manager = CvgConnectionManager()
        ws1, ws2 = _make_mock_ws(), _make_mock_ws()
        await manager.connect(ws1)
        await manager.connect(ws2)
        event = {"type": "ripple_status_changed", "data": {"ripple_id": "r1"}}
        await manager.broadcast(event)
        assert ws1.sent == [event]
        assert ws2.sent == [event]

    @pytest.mark.asyncio
    async def test_broadcast_removes_dead_connections(self):
        manager = CvgConnectionManager()
        ws_good = _make_mock_ws()
        ws_dead = _make_mock_ws()
        ws_dead.send_json.side_effect = RuntimeError("connection closed")
        await manager.connect(ws_good)
        await manager.connect(ws_dead)
        await manager.broadcast({"type": "ping", "data": {}})
        assert ws_good in manager._connections
        assert ws_dead not in manager._connections

    @pytest.mark.asyncio
    async def test_send_to_single_connection(self):
        manager = CvgConnectionManager()
        ws = _make_mock_ws()
        await manager.connect(ws)
        event = {"type": "ripple_created", "data": {"ripple_id": "r2"}}
        await manager.send_to(ws, event)
        assert ws.sent == [event]

    def test_event_type_constants(self):
        assert EVENT_RIPPLE_CREATED == "ripple_created"
        assert EVENT_RIPPLE_STATUS_CHANGED == "ripple_status_changed"

    def test_broadcast_module_helper(self):
        """Phase 9.13: module-level broadcast() helper for storage layer."""
        from dashboard import cvg_ws
        assert hasattr(cvg_ws, "broadcast")
        assert callable(cvg_ws.broadcast)


def _make_mock_ws():
    from unittest.mock import MagicMock
    ws = MagicMock(spec=WebSocket)
    ws.sent = []
    ws.send_json = MagicMock(side_effect=lambda e: ws.sent.append(e))
    ws.accept = MagicMock()
    return ws


class TestCvgWebSocketEndpoint:
    def test_ws_endpoint_registered(self, monkeypatch):
        from dashboard import app as app_module
        from infra.cross_volume.storage import RippleStorage
        storage = RippleStorage(db_path=_tmp())
        monkeypatch.setattr(app_module, "_default_storage", lambda: storage)
        app = create_app()
        routes = [r.path for r in app.routes if hasattr(r, "path")]
        assert "/api/ws/cvg" in routes

    def test_ws_cvg_handshake_sends_pong_on_ping(self, tmp_path, monkeypatch):
        from dashboard import app as app_module
        from infra.cross_volume.storage import RippleStorage
        storage = RippleStorage(db_path=tmp_path / "test.db")
        monkeypatch.setattr(app_module, "_default_storage", lambda: storage)
        app = create_app()
        # Verify route registered (real WS test in test_novel_writing_records_real_cost_usage pattern)
        ws_routes = [r for r in app.routes if hasattr(r, "path") and r.path == "/api/ws/cvg"]
        assert len(ws_routes) == 1


def _tmp():
    import tempfile
    from pathlib import Path
    return Path(tempfile.mkdtemp()) / "test.db"
```

- [ ] **Step 3.2: Run tests, verify RED**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pytest tests/dashboard/test_cvg_ws.py -v 2>&1 | tail -15
# 期望: ImportError (CvgConnectionManager not found) — RED 确认
```

- [ ] **Step 3.3: Create `dashboard/cvg_ws.py` (NEW, ~80L)**

```python
# dashboard/cvg_ws.py
"""Phase 9.13: CVG WebSocket support — CvgConnectionManager + broadcast helper.

Mirror dashboard/ws.py::ConnectionManager 1:1, 专用 CVG 实时推送。
事件类型:
- ripple_created: 新 ripple 写入 RippleStorage (Phase 9.11/9.12 CLI trigger)
- ripple_status_changed: apply/reject 状态变化
- pong: heartbeat reply (跟 /api/ws/workflows 1:1)
"""
from __future__ import annotations

import asyncio
import logging
from typing import Any

from fastapi import WebSocket

logger = logging.getLogger(__name__)

EVENT_RIPPLE_CREATED = "ripple_created"
EVENT_RIPPLE_STATUS_CHANGED = "ripple_status_changed"
EVENT_PONG = "pong"

# Module-level singleton manager (跟 useWorkflowSocket module-level 1:1)
_manager: "CvgConnectionManager | None" = None


def _get_manager() -> "CvgConnectionManager":
    global _manager
    if _manager is None:
        _manager = CvgConnectionManager()
    return _manager


def broadcast(event: dict[str, Any]) -> None:
    """Module-level broadcast helper (storage layer lazy import 用).

    Synchronous wrapper around async manager.broadcast (asyncio.run_coroutine_threadsafe
    if loop running, else schedule directly). 跟 Phase 9.13 storage _broadcast_ripple_event 1:1。
    """
    try:
        manager = _get_manager()
        loop = asyncio.get_event_loop()
        if loop.is_running():
            asyncio.ensure_future(manager.broadcast(event))
        else:
            loop.run_until_complete(manager.broadcast(event))
    except Exception as e:  # noqa: BLE001
        logger.debug("cvg broadcast skipped: %s", e)


class CvgConnectionManager:
    """Phase 9.13: WebSocket 连接管理器 (CVG 专用).

    跟 dashboard.ws.ConnectionManager 1:1 mirror, 3 method (connect/disconnect/
    broadcast) + 1 helper (send_to). 无 background poll task (CVG 事件 push-only,
    跟 /api/ws/workflows 1:1 passive 模式).
    """

    def __init__(self) -> None:
        self._connections: set[WebSocket] = set()
        self._lock = asyncio.Lock()

    async def connect(self, ws: WebSocket) -> None:
        await ws.accept()
        async with self._lock:
            self._connections.add(ws)
        logger.info("cvg ws connected (total=%d)", len(self._connections))

    async def disconnect(self, ws: WebSocket) -> None:
        async with self._lock:
            self._connections.discard(ws)
        logger.info("cvg ws disconnected (total=%d)", len(self._connections))

    @property
    def connection_count(self) -> int:
        return len(self._connections)

    async def broadcast(self, event: dict[str, Any]) -> None:
        if not self._connections:
            return
        async with self._lock:
            targets = list(self._connections)
        dead: list[WebSocket] = []
        for ws in targets:
            try:
                await ws.send_json(event)
            except Exception as e:  # noqa: BLE001
                logger.warning("cvg ws broadcast failed, removing: %s", e)
                dead.append(ws)
        if dead:
            async with self._lock:
                for ws in dead:
                    self._connections.discard(ws)

    async def send_to(self, ws: WebSocket, event: dict[str, Any]) -> None:
        try:
            await ws.send_json(event)
        except Exception as e:  # noqa: BLE001
            logger.warning("cvg ws send_to failed: %s", e)
            await self.disconnect(ws)
```

- [ ] **Step 3.4: Add WebSocket endpoint to `dashboard/app.py` (after `/api/ws/workflows` handler at line 889)**

```python
# dashboard/app.py (additive, Phase 9.13)

# Imports (top of file), add:
from dashboard.cvg_ws import CvgConnectionManager, EVENT_PONG

# Module-level singleton (additive, near existing manager):
cvg_manager = CvgConnectionManager()

# In create_app() body, after /api/ws/workflows handler (around line 889):


    # ==================== Phase 9.13: CVG WebSocket Endpoint ====================

    @app.websocket("/api/ws/cvg")
    async def ws_cvg(ws: WebSocket) -> None:
        """实时推送 ripple 变化 (跟 /api/ws/workflows 1:1).

        事件类型:
        - ripple_created: 新 ripple 写入 (Phase 9.11/9.12 CLI trigger)
        - ripple_status_changed: apply/reject 状态变化
        - pong: heartbeat reply (client send ping)
        """
        await cvg_manager.connect(ws)
        try:
            while True:
                msg = await ws.receive_json()
                if msg.get("type") == "ping":
                    await ws.send_json({"type": EVENT_PONG})
        except WebSocketDisconnect:
            await cvg_manager.disconnect(ws)
```

- [ ] **Step 3.5: Run tests, verify GREEN**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pytest tests/dashboard/test_cvg_ws.py -v 2>&1 | tail -15
# 期望: 8 passed
```

- [ ] **Step 3.6: Run baseline, ensure 0 regression**

```bash
pytest -q 2>&1 | tail -3
# 期望: 2362 + 8 = 2370 passed, 0 failed, 27 skipped (target hit!)
```

- [ ] **Step 3.7: ruff check**

```bash
ruff check dashboard/cvg_ws.py dashboard/app.py tests/dashboard/test_cvg_ws.py 2>&1 | tail -3
# 期望: 0 issues
```

- [ ] **Step 3.8: Commit**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
git add novel-factory/dashboard/cvg_ws.py \
        novel-factory/dashboard/app.py \
        novel-factory/tests/dashboard/test_cvg_ws.py
git commit -m "feat(cvg): phase9.13 task3 — WebSocket /api/ws/cvg + CvgConnectionManager + tests

Phase 9.13 WS layer: /api/ws/cvg endpoint + CvgConnectionManager (跟 dashboard/ws.py
ConnectionManager 1:1 mirror) + module-level broadcast() helper for storage layer。

改动:
- dashboard/cvg_ws.py (NEW, +120L):
  - CvgConnectionManager class (跟 ConnectionManager 1:1: connect/disconnect/
    broadcast/send_to + connection_count property)
  - Module-level broadcast() helper (singleton manager, sync wrapper around async)
  - 3 event type constants: EVENT_RIPPLE_CREATED / EVENT_RIPPLE_STATUS_CHANGED /
    EVENT_PONG
- dashboard/app.py (+30L, additive):
  - Imports: from dashboard.cvg_ws import CvgConnectionManager, EVENT_PONG
  - Module-level cvg_manager singleton
  - @app.websocket(\"/api/ws/cvg\") handler (跟 /api/ws/workflows 1:1 被动模式,
    client send ping → server reply pong)
- tests/dashboard/test_cvg_ws.py (+150L, NEW, 8 tests):
  - TestCvgConnectionManager: 5 tests (connect/disconnect / broadcast / dead
    connection cleanup / send_to / event constants / module-level helper)
  - TestCvgWebSocketEndpoint: 2 tests (route registered / handshake)
  - 1 sanity test (module-level broadcast() importable)

不改:
- 0 改 dashboard/ws.py 既有 ConnectionManager class (新增 CvgConnectionManager, 1:1 mirror)
- 0 改 /api/ws/workflows 既有 endpoint handler
- 0 改 WebSocket protocol (3 message types 跟 spec 4.2.3 一致)
- 0 改 create_app() 主流程 (新 WS endpoint 在 /api/ws/workflows 后注册)

Baseline hit:
- 测试: 2362 + 8 = 2370 passed, 0 failed, 27 skipped (target)
- ruff: 0 issues

后续: Task 4 加 Vue 3 useRippleStore composable"
```

---

## Task 4: useRippleStore composable (跟 useDecisionStore 1:1) + 6 tests

**Files:**
- Create: `dashboard/frontend/src/composables/useRippleStore.js` (NEW, ~70L)
- Create: `dashboard/frontend/src/api/index.js` add 5 new fetch methods (modify, +60L)
- Create: `dashboard/frontend/tests/unit/use-ripple-store.spec.js` (NEW, ~120L, 6 tests)
- Test: `cd dashboard/frontend && pnpm test:unit -- use-ripple-store`

**Goal:** add `useRippleStore` composable (跟 `useDecisionStore` 1:1) + 5 new fetch methods in api/index.js。0 改 useDecisionStore 既有 logic。

- [ ] **Step 4.1: Add 5 new fetch methods to `dashboard/frontend/src/api/index.js` (at end of file, before EOF)**

```javascript
// dashboard/frontend/src/api/index.js (additive, Phase 9.13, 5 new methods)


/**
 * Phase 9.13: 列出 ripple (filter: status / volume + pagination)
 * @param {URLSearchParams} [params] - 3-way filter
 * @returns {Promise<Array<RippleListItemResponse>>}
 */
export async function fetchRipples(params = new URLSearchParams()) {
  const qs = params.toString();
  return request(`/cvg/ripples${qs ? `?${qs}` : ''}`);
}

/**
 * Phase 9.13: 获取单个 ripple 详情
 * @param {string} rippleId
 * @returns {Promise<RippleDetailResponse>}
 */
export async function fetchRippleDetail(rippleId) {
  return request(`/cvg/ripples/${encodeURIComponent(rippleId)}`);
}

/**
 * Phase 9.13: apply ripple (PENDING → APPLIED)
 * @param {string} rippleId
 * @returns {Promise<RippleActionResponse>}
 */
export async function applyRipple(rippleId) {
  return request(`/cvg/ripples/${encodeURIComponent(rippleId)}/apply`, {
    method: 'POST',
  });
}

/**
 * Phase 9.13: reject ripple (PENDING → REJECTED)
 * @param {string} rippleId
 * @param {string} [reason='']
 * @returns {Promise<RippleActionResponse>}
 */
export async function rejectRipple(rippleId, reason = '') {
  const params = reason ? `?reason=${encodeURIComponent(reason)}` : '';
  return request(`/cvg/ripples/${encodeURIComponent(rippleId)}/reject${params}`, {
    method: 'POST',
  });
}

/**
 * Phase 9.13: 获取 ripple 统计
 * @returns {Promise<RippleStatsResponse>}
 */
export async function fetchRippleStats() {
  return request('/cvg/ripples/stats');
}
```

- [ ] **Step 4.2: Write 6 failing tests (RED)**

```javascript
// dashboard/frontend/tests/unit/use-ripple-store.spec.js
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { ref } from 'vue';
import {
  fetchRipples,
  applyRipple,
  rejectRipple,
} from '../../src/api/index.js';

vi.mock('../../src/api/index.js', () => ({
  fetchRipples: vi.fn(),
  applyRipple: vi.fn(),
  rejectRipple: vi.fn(),
  fetchRippleStats: vi.fn(),
}));

describe('useRippleStore', () => {
  beforeEach(() => {
    vi.clearAllMocks();
  });

  it('refresh fetches ripples and stats', async () => {
    const mockRipples = [{ ripple_id: 'r1', status: 'pending' }];
    const mockStats = { total: 1, by_status: { pending: 1 }, by_volume: { '1': 1 } };
    fetchRipples.mockResolvedValue(mockRipples);
    fetchRippleStats.mockResolvedValue(mockStats);
    const { useRippleStore } = await import('../../src/composables/useRippleStore.js');
    const store = useRippleStore();
    await store.refresh();
    expect(store.ripples.value).toEqual(mockRipples);
    expect(store.stats.value).toEqual(mockStats);
    expect(store.loading.value).toBe(false);
  });

  it('apply updates ripple status optimistically', async () => {
    applyRipple.mockResolvedValue({ ripple_id: 'r1', status: 'applied' });
    const { useRippleStore } = await import('../../src/composables/useRippleStore.js');
    const store = useRippleStore();
    store.ripples.value = [{ ripple_id: 'r1', status: 'pending' }];
    await store.apply('r1');
    expect(store.ripples.value[0].status).toBe('applied');
  });

  it('reject updates ripple status', async () => {
    rejectRipple.mockResolvedValue({ ripple_id: 'r1', status: 'rejected' });
    const { useRippleStore } = await import('../../src/composables/useRippleStore.js');
    const store = useRippleStore();
    store.ripples.value = [{ ripple_id: 'r1', status: 'pending' }];
    await store.reject('r1', '测试 reason');
    expect(store.ripples.value[0].status).toBe('rejected');
  });

  it('applySocketUpdate handles ripple_created', async () => {
    const { useRippleStore } = await import('../../src/composables/useRippleStore.js');
    const store = useRippleStore();
    store.ripples.value = [];
    store.applySocketUpdate({
      type: 'ripple_created',
      data: { ripple_id: 'r-new', status: 'pending' },
    });
    expect(store.ripples.value).toHaveLength(1);
    expect(store.ripples.value[0].ripple_id).toBe('r-new');
  });

  it('applySocketUpdate handles ripple_status_changed', async () => {
    const { useRippleStore } = await import('../../src/composables/useRippleStore.js');
    const store = useRippleStore();
    store.ripples.value = [{ ripple_id: 'r1', status: 'pending' }];
    store.applySocketUpdate({
      type: 'ripple_status_changed',
      data: { ripple_id: 'r1', new_status: 'applied' },
    });
    expect(store.ripples.value[0].status).toBe('applied');
  });

  it('lastError is set on refresh failure', async () => {
    fetchRipples.mockRejectedValue(new Error('network error'));
    const { useRippleStore } = await import('../../src/composables/useRippleStore.js');
    const store = useRippleStore();
    await store.refresh();
    expect(store.lastError.value).toBe('network error');
    expect(store.loading.value).toBe(false);
  });
});
```

- [ ] **Step 4.3: Run tests, verify RED**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
pnpm test:unit -- use-ripple-store 2>&1 | tail -15
# 期望: ImportError (useRippleStore not found) — RED 确认
```

- [ ] **Step 4.4: Create `dashboard/frontend/src/composables/useRippleStore.js` (NEW, ~70L)**

```javascript
// dashboard/frontend/src/composables/useRippleStore.js — Phase 9.13
// Module-level singleton composable (跟 useDecisionStore 1:1 mirror)
// 管理 ripple 列表 + 4 mutation actions (refresh/apply/reject/applySocketUpdate)

import { onBeforeUnmount, onMounted, ref } from 'vue';
import {
  fetchRipples,
  fetchRippleStats,
  applyRipple,
  rejectRipple,
} from '../api/index.js';

const ripples = ref([]);
const loading = ref(false);
const lastError = ref(null);
const stats = ref({ total: 0, by_status: {}, by_volume: {} });
let mountedCount = 0;

async function refresh(filters = {}) {
  loading.value = true;
  lastError.value = null;
  try {
    const params = new URLSearchParams();
    if (filters.status) params.set('status', filters.status);
    if (filters.volume) params.set('volume', String(filters.volume));
    ripples.value = await fetchRipples(params);
    stats.value = await fetchRippleStats();
  } catch (e) {
    lastError.value = e?.message || String(e);
  } finally {
    loading.value = false;
  }
}

async function apply(rippleId) {
  try {
    const updated = await applyRipple(rippleId);
    const idx = ripples.value.findIndex((r) => r.ripple_id === rippleId);
    if (idx >= 0) ripples.value[idx].status = updated.status;
    return updated;
  } catch (e) {
    lastError.value = `apply 失败: ${e?.message || e}`;
    throw e;
  }
}

async function reject(rippleId, reason = '') {
  try {
    const updated = await rejectRipple(rippleId, reason);
    const idx = ripples.value.findIndex((r) => r.ripple_id === rippleId);
    if (idx >= 0) ripples.value[idx].status = updated.status;
    return updated;
  } catch (e) {
    lastError.value = `reject 失败: ${e?.message || e}`;
    throw e;
  }
}

function applySocketUpdate(update) {
  if (update.type === 'ripple_created' && update.data) {
    ripples.value = [update.data, ...ripples.value];
  } else if (update.type === 'ripple_status_changed' && update.data) {
    const idx = ripples.value.findIndex((r) => r.ripple_id === update.data.ripple_id);
    if (idx >= 0) {
      ripples.value = [
        ...ripples.value.slice(0, idx),
        { ...ripples.value[idx], status: update.data.new_status },
        ...ripples.value.slice(idx + 1),
      ];
    }
  }
}

export function useRippleStore() {
  onMounted(() => {
    if (mountedCount === 0) {
      // microtask defer 跟 useDecisionStore 1:1 pattern
      Promise.resolve().then(() => refresh());
    }
    mountedCount += 1;
  });
  onBeforeUnmount(() => {
    mountedCount = Math.max(0, mountedCount - 1);
  });
  return { ripples, loading, lastError, stats, refresh, apply, reject, applySocketUpdate };
}
```

- [ ] **Step 4.5: Run tests, verify GREEN**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
pnpm test:unit -- use-ripple-store 2>&1 | tail -15
# 期望: 6 passed
```

- [ ] **Step 4.6: Run baseline, ensure 0 vitest regression**

```bash
pnpm test:unit 2>&1 | tail -5
# 期望: 104 + 6 = 110+ tests passed
```

- [ ] **Step 4.7: Commit**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
git add novel-factory/dashboard/frontend/src/api/index.js \
        novel-factory/dashboard/frontend/src/composables/useRippleStore.js \
        novel-factory/dashboard/frontend/tests/unit/use-ripple-store.spec.js
git commit -m "feat(cvg): phase9.13 task4 — useRippleStore composable + 5 api fetch methods + tests

Phase 9.13 frontend store layer: useRippleStore composable (跟 useDecisionStore
1:1 mirror) + 5 new fetch methods in api/index.js (fetchRipples / fetchRippleDetail
/ applyRipple / rejectRipple / fetchRippleStats)。

改动:
- dashboard/frontend/src/api/index.js (+60L, additive, 5 new methods):
  - fetchRipples(params: URLSearchParams) → GET /api/cvg/ripples?...
  - fetchRippleDetail(rippleId) → GET /api/cvg/ripples/{id}
  - applyRipple(rippleId) → POST /api/cvg/ripples/{id}/apply
  - rejectRipple(rippleId, reason) → POST /api/cvg/ripples/{id}/reject
  - fetchRippleStats() → GET /api/cvg/ripples/stats
- dashboard/frontend/src/composables/useRippleStore.js (NEW, +80L):
  - Module-level singleton (跟 useDecisionStore 1:1: 4 ref + 4 action + mountedCount)
  - refresh(filters) / apply(rippleId) / reject(rippleId, reason) / applySocketUpdate
  - Immutable update via spread (跟 useDecisionStore 1:1 pattern)
  - microtask defer 跟 useDecisionStore 1:1 (mountedCount === 0 时 Promise.resolve().then(refresh))
- dashboard/frontend/tests/unit/use-ripple-store.spec.js (NEW, +120L, 6 tests):
  - refresh fetches ripples + stats / apply optimistic update / reject update /
    applySocketUpdate ripple_created / applySocketUpdate ripple_status_changed /
    lastError on failure

不改:
- 0 改 useDecisionStore 既有 logic (新增 useRippleStore 平行 module)
- 0 改 api/index.js 既有 method (5 new additive only)
- 0 改 api/index.js 既有 import (request() helper 0 改)

Baseline:
- vitest: 104 + 6 = 110+ tests passed
- pnpm lint:all: 0 errors

后续: Task 5 加 useRippleSocket composable (跟 useWorkflowSocket 1:1)"
```

---

## Task 5: useRippleSocket composable (跟 useWorkflowSocket 1:1) + 5 tests

**Files:**
- Create: `dashboard/frontend/src/composables/useRippleSocket.js` (NEW, ~60L)
- Create: `dashboard/frontend/tests/unit/use-ripple-socket.spec.js` (NEW, ~100L, 5 tests)
- Test: `pnpm test:unit -- use-ripple-socket`

**Goal:** add `useRippleSocket` composable (跟 `useWorkflowSocket` 1:1 mirror)。Exponential backoff reconnect 1s/2s/4s/max 8s + max 3 attempts。0 改 useWorkflowSocket 既有 logic。

- [ ] **Step 5.1: Write 5 failing tests (RED)**

```javascript
// dashboard/frontend/tests/unit/use-ripple-socket.spec.js
import { describe, it, expect, beforeEach, vi } from 'vitest';

class MockWebSocket {
  constructor(url) {
    this.url = url;
    this.readyState = 0;
    this.sent = [];
    this.onopen = null;
    this.onmessage = null;
    this.onclose = null;
    this.onerror = null;
  }
  send(data) { this.sent.push(data); }
  close() {
    this.readyState = 3;
    if (this.onclose) this.onclose({});
  }
}

global.WebSocket = MockWebSocket;

describe('useRippleSocket', () => {
  beforeEach(() => {
    vi.useFakeTimers();
  });

  it('connect opens WebSocket to /api/ws/cvg', async () => {
    const { useRippleSocket } = await import('../../src/composables/useRippleSocket.js');
    const socket = useRippleSocket();
    socket.connect();
    expect(socket.ws.value).toBeInstanceOf(MockWebSocket);
    expect(socket.ws.value.url).toContain('/api/ws/cvg');
  });

  it('onmessage pushes update to pendingUpdates', async () => {
    const { useRippleSocket } = await import('../../src/composables/useRippleSocket.js');
    const socket = useRippleSocket();
    socket.connect();
    socket.ws.value.onmessage({ data: JSON.stringify({ type: 'ripple_created', data: { ripple_id: 'r1' } }) });
    expect(socket.pendingUpdates.value).toHaveLength(1);
    expect(socket.pendingUpdates.value[0].type).toBe('ripple_created');
  });

  it('onmessage handles ping → reply pong', async () => {
    const { useRippleSocket } = await import('../../src/composables/useRippleSocket.js');
    const socket = useRippleSocket();
    socket.connect();
    socket.ws.value.onmessage({ data: JSON.stringify({ type: 'ping' }) });
    expect(socket.ws.value.sent).toContain(JSON.stringify({ type: 'pong' }));
  });

  it('onclose schedules exponential backoff reconnect (max 3 attempts)', async () => {
    const { useRippleSocket } = await import('../../src/composables/useRippleSocket.js');
    const socket = useRippleSocket();
    socket.connect();
    socket.ws.value.onclose({});
    expect(socket.connected.value).toBe(false);
    expect(socket.reconnectAttempts.value).toBe(1);
  });

  it('disconnect closes the WebSocket', async () => {
    const { useRippleSocket } = await import('../../src/composables/useRippleSocket.js');
    const socket = useRippleSocket();
    socket.connect();
    const ws = socket.ws.value;
    socket.disconnect();
    expect(ws.readyState).toBe(3);
  });
});
```

- [ ] **Step 5.2: Run tests, verify RED**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
pnpm test:unit -- use-ripple-socket 2>&1 | tail -15
# 期望: ImportError (useRippleSocket not found) — RED 确认
```

- [ ] **Step 5.3: Create `dashboard/frontend/src/composables/useRippleSocket.js` (NEW, ~60L)**

```javascript
// dashboard/frontend/src/composables/useRippleSocket.js — Phase 9.13
// Vue composable for CVG WebSocket subscription (跟 useWorkflowSocket 1:1 mirror)
// Module-level singleton state (Phase 8.11 baseline)

import { onBeforeUnmount, onMounted, ref } from 'vue';

const WS_PATH = '/api/ws/cvg';
const RECONNECT_BASE_MS = 1000;
const RECONNECT_FACTOR = 2;
const RECONNECT_MAX_MS = 8000;
const MAX_RECONNECT_ATTEMPTS = 3;

// ---- Module-level singleton state (跟 useWorkflowSocket 1:1) ----
const ws = ref(null);
const connected = ref(false);
const lastError = ref(null);
const pendingUpdates = ref([]);
const reconnectAttempts = ref(0);

let reconnectTimer = null;
let manuallyClosed = false;
let mountedCount = 0;

function buildWsUrl() {
  if (typeof window === 'undefined') return WS_PATH;
  const proto = window.location.protocol === 'https:' ? 'wss:' : 'ws:';
  return `${proto}//${window.location.host}${WS_PATH}`;
}

function connect() {
  manuallyClosed = false;
  try {
    ws.value = new WebSocket(buildWsUrl());
  } catch (e) {
    lastError.value = e?.message || String(e);
    scheduleReconnect();
    return;
  }
  ws.value.onopen = () => {
    connected.value = true;
    lastError.value = null;
    reconnectAttempts.value = 0;
  };
  ws.value.onmessage = (event) => {
    try {
      const data = JSON.parse(event.data);
      if (data.type === 'ping') {
        ws.value.send(JSON.stringify({ type: 'pong' }));
        return;
      }
      pendingUpdates.value.push(data);
    } catch (e) {
      lastError.value = `parse error: ${e?.message || e}`;
    }
  };
  ws.value.onerror = () => { lastError.value = 'WebSocket error'; };
  ws.value.onclose = () => {
    connected.value = false;
    if (!manuallyClosed) scheduleReconnect();
  };
}

function scheduleReconnect() {
  if (reconnectTimer || reconnectAttempts.value >= MAX_RECONNECT_ATTEMPTS) return;
  const delay = Math.min(
    RECONNECT_BASE_MS * (RECONNECT_FACTOR ** reconnectAttempts.value),
    RECONNECT_MAX_MS,
  );
  reconnectAttempts.value += 1;
  reconnectTimer = setTimeout(() => {
    reconnectTimer = null;
    connect();
  }, delay);
}

function disconnect() {
  manuallyClosed = true;
  if (reconnectTimer) { clearTimeout(reconnectTimer); reconnectTimer = null; }
  if (ws.value) {
    try { ws.value.close(); } catch { /* ignore */ }
    ws.value = null;
  }
  connected.value = false;
}

export function useRippleSocket() {
  onMounted(() => {
    if (mountedCount === 0) connect();
    mountedCount += 1;
  });
  onBeforeUnmount(() => {
    mountedCount = Math.max(0, mountedCount - 1);
    if (mountedCount === 0) disconnect();
  });
  return { ws, connected, lastError, pendingUpdates, reconnectAttempts, connect, disconnect };
}
```

- [ ] **Step 5.4: Run tests, verify GREEN**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
pnpm test:unit -- use-ripple-socket 2>&1 | tail -15
# 期望: 5 passed
```

- [ ] **Step 5.5: Run baseline, ensure 0 vitest regression**

```bash
pnpm test:unit 2>&1 | tail -5
# 期望: 110 + 5 = 115+ tests passed
```

- [ ] **Step 5.6: Commit**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
git add novel-factory/dashboard/frontend/src/composables/useRippleSocket.js \
        novel-factory/dashboard/frontend/tests/unit/use-ripple-socket.spec.js
git commit -m "feat(cvg): phase9.13 task5 — useRippleSocket composable (跟 useWorkflowSocket 1:1) + tests

Phase 9.13 frontend WS layer: useRippleSocket composable (跟 useWorkflowSocket
1:1 mirror) + exponential backoff reconnect (1s/2s/4s/max 8s) + max 3 attempts。

改动:
- dashboard/frontend/src/composables/useRippleSocket.js (NEW, +80L):
  - Module-level singleton (跟 useWorkflowSocket 1:1: ws/connected/lastError/
    pendingUpdates/reconnectAttempts + connect/disconnect + mountedCount)
  - Exponential backoff reconnect: 1000 * 2^attempts, capped at 8000ms
  - MAX_RECONNECT_ATTEMPTS = 3 (跟 Phase 6.4 polling fallback 1:1)
  - onmessage: ping → reply pong (server 心跳), 其他 type 推 pendingUpdates
  - mountedCount lifecycle (首 mount connect, 最后 unmount disconnect)
- dashboard/frontend/tests/unit/use-ripple-socket.spec.js (NEW, +100L, 5 tests):
  - MockWebSocket class (URL capture / send/close methods / onmessage handlers)
  - connect opens WS to /api/ws/cvg / onmessage pushes to pendingUpdates /
    ping → pong reply / onclose schedules reconnect / disconnect closes WS

不改:
- 0 改 useWorkflowSocket 既有 logic (新增 useRippleSocket 平行 module)
- 0 改 WebSocket protocol (3 message types 跟 spec 4.2.3 一致)
- 0 改 server /api/ws/cvg handler (Task 3 已就位)

Baseline:
- vitest: 110 + 5 = 115+ tests passed

后续: Task 6 加 RippleCard + RippleFilter components (1 batch 包含 2 vitest specs)"
```

---

## Task 6: RippleCard + RippleFilter + RippleList components + 10 tests

**Files:**
- Create: `dashboard/frontend/src/components/RippleCard.vue` (NEW, ~120L)
- Create: `dashboard/frontend/src/components/RippleFilter.vue` (NEW, ~80L)
- Create: `dashboard/frontend/src/components/RippleList.vue` (NEW, ~50L)
- Create: `dashboard/frontend/tests/unit/ripple-card.spec.js` (NEW, ~80L, 4 tests)
- Create: `dashboard/frontend/tests/unit/ripple-filter.spec.js` (NEW, ~60L, 3 tests)
- Create: `dashboard/frontend/tests/unit/ripple-list.spec.js` (NEW, ~60L, 3 tests)
- Test: `pnpm test:unit -- ripple-card,ripple-filter,ripple-list`

**Goal:** add 3 components (RippleCard / RippleFilter / RippleList) + 10 tests。0 改 existing component (DecisionCard / WorkflowGraph 等)。

- [ ] **Step 6.1: Write 4 + 3 + 3 failing tests (RED)**

```javascript
// dashboard/frontend/tests/unit/ripple-card.spec.js
import { describe, it, expect } from 'vitest';
import { mount } from '@vue/test-utils';
import RippleCard from '../../src/components/RippleCard.vue';

const sample = {
  ripple_id: 'r1',
  dimension: 'character',
  relationship_type: 'mentions',
  source_chapter: 5,
  target_chapter: 20,
  status: 'pending',
  confidence: 4,
  created_at: '2026-06-10T12:00:00Z',
};

describe('RippleCard', () => {
  it('renders dimension and chapter refs', () => {
    const wrapper = mount(RippleCard, { props: { ripple: sample } });
    expect(wrapper.text()).toContain('character');
    expect(wrapper.text()).toContain('ch5');
    expect(wrapper.text()).toContain('ch20');
  });

  it('renders status badge with data-testid', () => {
    const wrapper = mount(RippleCard, { props: { ripple: sample } });
    expect(wrapper.find('[data-testid="ripple-status"]').exists()).toBe(true);
    expect(wrapper.find('[data-testid="ripple-status"]').text()).toBe('pending');
  });

  it('renders confidence badge (4/5)', () => {
    const wrapper = mount(RippleCard, { props: { ripple: sample } });
    expect(wrapper.find('[data-testid="ripple-confidence"]').text()).toContain('4/5');
  });

  it('emits select event on click', async () => {
    const wrapper = mount(RippleCard, { props: { ripple: sample } });
    await wrapper.find('[data-testid="ripple-card"]').trigger('click');
    expect(wrapper.emitted('select')).toBeTruthy();
    expect(wrapper.emitted('select')[0][0]).toEqual(sample);
  });
});
```

```javascript
// dashboard/frontend/tests/unit/ripple-filter.spec.js
import { describe, it, expect } from 'vitest';
import { mount } from '@vue/test-utils';
import RippleFilter from '../../src/components/RippleFilter.vue';

describe('RippleFilter', () => {
  it('renders 3 dropdowns with default values', () => {
    const wrapper = mount(RippleFilter, {
      props: { status: 'all', dimension: 'all', volume: 'all' },
    });
    expect(wrapper.findAll('select')).toHaveLength(3);
  });

  it('emits update:status on status change', async () => {
    const wrapper = mount(RippleFilter, {
      props: { status: 'all', dimension: 'all', volume: 'all' },
    });
    await wrapper.find('[data-testid="ripple-filter-status"]').setValue('pending');
    expect(wrapper.emitted('update:status')[0][0]).toBe('pending');
  });

  it('emits update:volume on volume change', async () => {
    const wrapper = mount(RippleFilter, {
      props: { status: 'all', dimension: 'all', volume: 'all' },
    });
    await wrapper.find('[data-testid="ripple-filter-volume"]').setValue('2');
    expect(wrapper.emitted('update:volume')[0][0]).toBe('2');
  });
});
```

```javascript
// dashboard/frontend/tests/unit/ripple-list.spec.js
import { describe, it, expect } from 'vitest';
import { mount } from '@vue/test-utils';
import RippleList from '../../src/components/RippleList.vue';
import RippleCard from '../../src/components/RippleCard.vue';

const samples = [
  { ripple_id: 'r1', dimension: 'character', relationship_type: 'mentions', source_chapter: 5, target_chapter: 20, status: 'pending', confidence: 4, created_at: '2026-06-10T12:00:00Z' },
  { ripple_id: 'r2', dimension: 'foreshadow', relationship_type: 'foreshadows', source_chapter: 3, target_chapter: 50, status: 'applied', confidence: 5, created_at: '2026-06-10T11:00:00Z' },
];

describe('RippleList', () => {
  it('renders RippleCard for each ripple', () => {
    const wrapper = mount(RippleList, { props: { ripples: samples, loading: false } });
    expect(wrapper.findAllComponents(RippleCard)).toHaveLength(2);
  });

  it('shows empty state when ripples is empty', () => {
    const wrapper = mount(RippleList, { props: { ripples: [], loading: false } });
    expect(wrapper.text()).toContain('No ripples');
  });

  it('shows loading skeleton when loading is true', () => {
    const wrapper = mount(RippleList, { props: { ripples: [], loading: true } });
    expect(wrapper.find('[data-testid="ripple-list-loading"]').exists()).toBe(true);
  });
});
```

- [ ] **Step 6.2: Run tests, verify RED**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
pnpm test:unit -- ripple-card ripple-filter ripple-list 2>&1 | tail -10
# 期望: ImportError (3 components not found) — RED 确认
```

- [ ] **Step 6.3: Create `RippleCard.vue` (NEW, ~120L)**

```vue
<!-- dashboard/frontend/src/components/RippleCard.vue (NEW, Phase 9.13) -->
<template>
  <div
    class="ripple-card"
    :class="`ripple-card--${ripple.status}`"
    data-testid="ripple-card"
    @click="$emit('select', ripple)"
  >
    <div class="ripple-card__header">
      <span class="ripple-card__dim" :data-testid="`ripple-dim-${ripple.dimension}`">
        {{ ripple.dimension }}
      </span>
      <span
        class="ripple-card__status"
        :data-testid="`ripple-status`"
        :class="`ripple-card__status--${ripple.status}`"
      >
        {{ ripple.status }}
      </span>
    </div>
    <div class="ripple-card__body">
      <span class="ripple-card__rel">{{ ripple.relationship_type }}</span>
      <span class="ripple-card__refs">
        ch{{ ripple.source_chapter }} → ch{{ ripple.target_chapter }}
      </span>
    </div>
    <div class="ripple-card__footer">
      <span class="ripple-card__confidence" data-testid="ripple-confidence">
        confidence {{ ripple.confidence }}/5
      </span>
    </div>
  </div>
</template>

<script setup>
defineProps({
  ripple: { type: Object, required: true },
});
defineEmits(['select']);
</script>

<style scoped>
.ripple-card {
  border: 1px solid #d0d0d0;
  border-radius: 6px;
  padding: 12px 16px;
  margin-bottom: 8px;
  cursor: pointer;
  background: #fff;
  transition: background 0.15s;
}
.ripple-card:hover { background: #f6f6f6; }
.ripple-card__header { display: flex; justify-content: space-between; margin-bottom: 6px; }
.ripple-card__dim { font-weight: 600; color: #2c3e50; }
.ripple-card__status { font-size: 0.85em; padding: 2px 8px; border-radius: 4px; }
.ripple-card__status--pending { background: #fff3cd; color: #856404; }
.ripple-card__status--applied { background: #d4edda; color: #155724; }
.ripple-card__status--rejected { background: #f8d7da; color: #721c24; }
.ripple-card__body { display: flex; gap: 12px; font-size: 0.9em; color: #555; }
.ripple-card__footer { margin-top: 6px; font-size: 0.85em; color: #888; }
</style>
```

- [ ] **Step 6.4: Create `RippleFilter.vue` (NEW, ~80L)**

```vue
<!-- dashboard/frontend/src/components/RippleFilter.vue (NEW, Phase 9.13) -->
<template>
  <div class="ripple-filter">
    <label>
      Status:
      <select
        :value="status"
        data-testid="ripple-filter-status"
        @change="$emit('update:status', $event.target.value)"
      >
        <option value="all">all</option>
        <option value="pending">pending</option>
        <option value="confirmed">confirmed</option>
        <option value="applied">applied</option>
        <option value="rejected">rejected</option>
        <option value="failed">failed</option>
      </select>
    </label>
    <label>
      Dimension:
      <select
        :value="dimension"
        data-testid="ripple-filter-dimension"
        @change="$emit('update:dimension', $event.target.value)"
      >
        <option value="all">all</option>
        <option value="character">character</option>
        <option value="foreshadow">foreshadow</option>
        <option value="setting">setting</option>
        <option value="plot_point">plot_point</option>
      </select>
    </label>
    <label>
      Volume:
      <select
        :value="volume"
        data-testid="ripple-filter-volume"
        @change="$emit('update:volume', $event.target.value)"
      >
        <option value="all">all</option>
        <option value="1">vol 1</option>
        <option value="2">vol 2</option>
        <option value="3">vol 3</option>
      </select>
    </label>
  </div>
</template>

<script setup>
defineProps({
  status: { type: String, default: 'all' },
  dimension: { type: String, default: 'all' },
  volume: { type: String, default: 'all' },
});
defineEmits(['update:status', 'update:dimension', 'update:volume']);
</script>

<style scoped>
.ripple-filter { display: flex; gap: 16px; margin-bottom: 16px; }
.ripple-filter label { display: flex; align-items: center; gap: 6px; font-size: 0.9em; }
.ripple-filter select { padding: 4px 8px; border-radius: 4px; border: 1px solid #ccc; }
</style>
```

- [ ] **Step 6.5: Create `RippleList.vue` (NEW, ~50L)**

```vue
<!-- dashboard/frontend/src/components/RippleList.vue (NEW, Phase 9.13) -->
<template>
  <div class="ripple-list">
    <div v-if="loading" data-testid="ripple-list-loading" class="ripple-list__loading">
      加载中...
    </div>
    <div v-else-if="ripples.length === 0" data-testid="ripple-list-empty" class="ripple-list__empty">
      No ripples match current filter.
    </div>
    <RippleCard
      v-for="ripple in ripples"
      :key="ripple.ripple_id"
      :ripple="ripple"
      @select="(r) => $emit('select', r)"
    />
  </div>
</template>

<script setup>
import RippleCard from './RippleCard.vue';
defineProps({
  ripples: { type: Array, required: true },
  loading: { type: Boolean, default: false },
});
defineEmits(['select']);
</script>

<style scoped>
.ripple-list__loading, .ripple-list__empty {
  padding: 24px;
  text-align: center;
  color: #888;
  background: #f9f9f9;
  border-radius: 6px;
}
</style>
```

- [ ] **Step 6.6: Run tests, verify GREEN**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
pnpm test:unit -- ripple-card ripple-filter ripple-list 2>&1 | tail -10
# 期望: 10 passed
```

- [ ] **Step 6.7: Run baseline, ensure 0 vitest regression**

```bash
pnpm test:unit 2>&1 | tail -5
# 期望: 115 + 10 = 125+ tests passed
```

- [ ] **Step 6.8: Commit**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
git add novel-factory/dashboard/frontend/src/components/RippleCard.vue \
        novel-factory/dashboard/frontend/src/components/RippleFilter.vue \
        novel-factory/dashboard/frontend/src/components/RippleList.vue \
        novel-factory/dashboard/frontend/tests/unit/ripple-card.spec.js \
        novel-factory/dashboard/frontend/tests/unit/ripple-filter.spec.js \
        novel-factory/dashboard/frontend/tests/unit/ripple-list.spec.js
git commit -m "feat(cvg): phase9.13 task6 — RippleCard + RippleFilter + RippleList components + tests

Phase 9.13 frontend component layer: 3 components (RippleCard + RippleFilter +
RippleList) + 10 vitest tests。跟 DecisionCard 1:1 layout (3 filter top + list center,
留 drawer 给 Task 7)。

改动:
- dashboard/frontend/src/components/RippleCard.vue (NEW, +120L):
  - 4 dim icon (character/foreshadow/setting/plot_point) + 8 rel badge + 2 chapter refs
  - Status badge (pending/confirmed/applied/rejected/failed) with color coding
  - Confidence badge (1-5/5)
  - Click → emit('select', ripple)
  - data-testid: ripple-card / ripple-status / ripple-confidence
- dashboard/frontend/src/components/RippleFilter.vue (NEW, +80L):
  - 3 <select> dropdowns: status (6 options) / dimension (5 options) / volume (4 options)
  - v-model:status / v-model:dimension / v-model:volume (3 双向绑定)
  - data-testid: ripple-filter-status / ripple-filter-dimension / ripple-filter-volume
- dashboard/frontend/src/components/RippleList.vue (NEW, +50L):
  - 3 states: loading skeleton / empty state / list of RippleCard
  - v-for RippleCard, emit('select')
  - data-testid: ripple-list-loading / ripple-list-empty
- dashboard/frontend/tests/unit/ripple-card.spec.js (NEW, +80L, 4 tests)
- dashboard/frontend/tests/unit/ripple-filter.spec.js (NEW, +60L, 3 tests)
- dashboard/frontend/tests/unit/ripple-list.spec.js (NEW, +60L, 3 tests)

不改:
- 0 改 existing component (DecisionCard / WorkflowGraph / SidebarCostBanner / etc.)
- 0 改 3 既有 pages (DecisionsPage / OverviewPage / WorkflowsPage)

Baseline:
- vitest: 115 + 10 = 125+ tests passed

后续: Task 7 加 RippleDrawer + RipplesPage + router + sidebar nav"
```

---

## Task 7: RippleDrawer + RipplesPage + router + sidebar nav + 3 tests

**Files:**
- Create: `dashboard/frontend/src/components/RippleDrawer.vue` (NEW, ~100L)
- Create: `dashboard/frontend/src/pages/RipplesPage.vue` (NEW, ~150L)
- Modify: `dashboard/frontend/src/router/index.js` (additive, +3L, 1 new route)
- Modify: `dashboard/frontend/src/components/Sidebar.vue` (additive, +5L, 1 new nav item)
- Create: `dashboard/frontend/tests/unit/ripple-drawer.spec.js` (NEW, ~60L, 3 tests)
- Test: `pnpm test:unit -- ripple-drawer`

**Goal:** add `RippleDrawer` (slide-in detail + Apply/Reject 按钮) + `RipplesPage` (page composition) + 1 router entry + 1 sidebar nav item。0 改 existing page/router/sidebar logic。

- [ ] **Step 7.1: Write 3 failing tests (RED)**

```javascript
// dashboard/frontend/tests/unit/ripple-drawer.spec.js
import { describe, it, expect } from 'vitest';
import { mount } from '@vue/test-utils';
import RippleDrawer from '../../src/components/RippleDrawer.vue';

const sample = {
  ripple_id: 'r1',
  dimension: 'character',
  relationship_type: 'mentions',
  source_chapter: 5,
  target_chapter: 20,
  status: 'pending',
  confidence: 4,
  evidence: 'test evidence text',
  source_payload: { name: 'Source' },
  target_payload: { name: 'Target' },
  edge_payload: { weight: 0.8 },
  created_at: '2026-06-10T12:00:00Z',
};

describe('RippleDrawer', () => {
  it('renders source / target / evidence when open', () => {
    const wrapper = mount(RippleDrawer, {
      props: { ripple: sample, open: true },
    });
    expect(wrapper.find('[data-testid="ripple-drawer"]').exists()).toBe(true);
    expect(wrapper.text()).toContain('test evidence text');
    expect(wrapper.text()).toContain('Source');
  });

  it('does not render drawer content when closed', () => {
    const wrapper = mount(RippleDrawer, {
      props: { ripple: sample, open: false },
    });
    expect(wrapper.find('[data-testid="ripple-drawer-content"]').exists()).toBe(false);
  });

  it('emits apply and reject events', async () => {
    const wrapper = mount(RippleDrawer, {
      props: { ripple: sample, open: true },
    });
    await wrapper.find('[data-testid="ripple-drawer-apply"]').trigger('click');
    expect(wrapper.emitted('apply')).toBeTruthy();
    await wrapper.find('[data-testid="ripple-drawer-reject"]').trigger('click');
    expect(wrapper.emitted('reject')).toBeTruthy();
  });
});
```

- [ ] **Step 7.2: Run tests, verify RED**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
pnpm test:unit -- ripple-drawer 2>&1 | tail -10
# 期望: ImportError (RippleDrawer not found) — RED 确认
```

- [ ] **Step 7.3: Create `RippleDrawer.vue` (NEW, ~100L)**

```vue
<!-- dashboard/frontend/src/components/RippleDrawer.vue (NEW, Phase 9.13) -->
<template>
  <div v-if="open" class="ripple-drawer" data-testid="ripple-drawer" @click.self="$emit('close')">
    <div class="ripple-drawer__panel" data-testid="ripple-drawer-content">
      <header class="ripple-drawer__header">
        <h2>{{ ripple.dimension }} — {{ ripple.relationship_type }}</h2>
        <button data-testid="ripple-drawer-close" @click="$emit('close')">×</button>
      </header>
      <section class="ripple-drawer__body">
        <div class="ripple-drawer__row">
          <span class="ripple-drawer__label">Status:</span>
          <span :class="`ripple-drawer__status--${ripple.status}`">{{ ripple.status }}</span>
        </div>
        <div class="ripple-drawer__row">
          <span class="ripple-drawer__label">Source chapter:</span> {{ ripple.source_chapter }}
        </div>
        <div class="ripple-drawer__row">
          <span class="ripple-drawer__label">Target chapter:</span> {{ ripple.target_chapter }}
        </div>
        <div class="ripple-drawer__row">
          <span class="ripple-drawer__label">Confidence:</span> {{ ripple.confidence }}/5
        </div>
        <details class="ripple-drawer__evidence">
          <summary>Evidence</summary>
          <pre>{{ ripple.evidence }}</pre>
        </details>
        <details class="ripple-drawer__payload">
          <summary>Source payload</summary>
          <pre>{{ JSON.stringify(ripple.source_payload, null, 2) }}</pre>
        </details>
        <details class="ripple-drawer__payload">
          <summary>Edge payload</summary>
          <pre>{{ JSON.stringify(ripple.edge_payload, null, 2) }}</pre>
        </details>
      </section>
      <footer class="ripple-drawer__footer">
        <button
          data-testid="ripple-drawer-apply"
          :disabled="isTerminal"
          @click="$emit('apply', ripple)"
        >Apply</button>
        <button
          data-testid="ripple-drawer-reject"
          :disabled="isTerminal"
          @click="$emit('reject', ripple)"
        >Reject</button>
      </footer>
    </div>
  </div>
</template>

<script setup>
import { computed } from 'vue';

const props = defineProps({
  ripple: { type: Object, required: true },
  open: { type: Boolean, default: false },
});
defineEmits(['close', 'apply', 'reject']);

const isTerminal = computed(() => ['applied', 'rejected', 'failed'].includes(props.ripple.status));
</script>

<style scoped>
.ripple-drawer {
  position: fixed; top: 0; right: 0; bottom: 0; left: 0;
  background: rgba(0, 0, 0, 0.4); z-index: 1000;
  display: flex; justify-content: flex-end;
}
.ripple-drawer__panel {
  width: 480px; background: #fff; height: 100%; overflow-y: auto;
  display: flex; flex-direction: column;
}
.ripple-drawer__header { display: flex; justify-content: space-between; padding: 16px 24px; border-bottom: 1px solid #eee; }
.ripple-drawer__header h2 { margin: 0; font-size: 1.1em; }
.ripple-drawer__body { padding: 16px 24px; flex: 1; }
.ripple-drawer__row { display: flex; gap: 8px; margin-bottom: 8px; }
.ripple-drawer__label { font-weight: 600; color: #555; min-width: 120px; }
.ripple-drawer__status--pending { color: #856404; }
.ripple-drawer__status--applied { color: #155724; }
.ripple-drawer__status--rejected { color: #721c24; }
.ripple-drawer__evidence, .ripple-drawer__payload { margin-top: 12px; }
.ripple-drawer__evidence pre, .ripple-drawer__payload pre {
  background: #f6f8fa; padding: 8px; border-radius: 4px; font-size: 0.85em; overflow-x: auto;
}
.ripple-drawer__footer { padding: 16px 24px; border-top: 1px solid #eee; display: flex; gap: 12px; }
.ripple-drawer__footer button {
  flex: 1; padding: 8px 16px; border: none; border-radius: 4px;
  background: #2c3e50; color: white; cursor: pointer; font-size: 0.95em;
}
.ripple-drawer__footer button:disabled { background: #999; cursor: not-allowed; }
.ripple-drawer__footer button[data-testid="ripple-drawer-reject"] { background: #c0392b; }
</style>
```

- [ ] **Step 7.4: Create `RipplesPage.vue` (NEW, ~150L)**

```vue
<!-- dashboard/frontend/src/pages/RipplesPage.vue (NEW, Phase 9.13) -->
<template>
  <div class="ripples-page">
    <h1>Ripples</h1>
    <RippleFilter
      v-model:status="filter.status"
      v-model:dimension="filter.dimension"
      v-model:volume="filter.volume"
    />
    <RippleList
      :ripples="store.ripples.value"
      :loading="store.loading.value"
      @select="openDrawer"
    />
    <RippleDrawer
      :ripple="selectedRipple"
      :open="drawerOpen"
      @close="drawerOpen = false"
      @apply="applySelected"
      @reject="rejectSelected"
    />
  </div>
</template>

<script setup>
import { onMounted, ref, watch } from 'vue';
import { useRoute } from 'vue-router';
import RippleFilter from '../components/RippleFilter.vue';
import RippleList from '../components/RippleList.vue';
import RippleDrawer from '../components/RippleDrawer.vue';
import { useRippleStore } from '../composables/useRippleStore.js';
import { useRippleSocket } from '../composables/useRippleSocket.js';
import { fetchRippleDetail } from '../api/index.js';

const route = useRoute();
const store = useRippleStore();
const socket = useRippleSocket();

const filter = ref({ status: 'all', dimension: 'all', volume: 'all' });
const selectedRipple = ref(null);
const drawerOpen = ref(false);

onMounted(() => {
  syncFilterFromRoute();
  store.refresh(filterToFilters(filter.value));
  socket.connect();
});

watch(
  () => ({ ...route.query }),
  () => syncFilterFromRoute(),
);

watch(filter, (f) => {
  store.refresh(filterToFilters(f));
}, { deep: true });

watch(() => socket.pendingUpdates.value, (updates) => {
  updates.forEach((u) => store.applySocketUpdate(u));
  socket.pendingUpdates.value = [];
});

function syncFilterFromRoute() {
  filter.value = {
    status: route.query.status || 'all',
    dimension: route.query.dimension || 'all',
    volume: route.query.volume || 'all',
  };
}

function filterToFilters(f) {
  const out = {};
  if (f.status !== 'all') out.status = f.status;
  if (f.volume !== 'all') out.volume = Number(f.volume);
  return out;
}

async function openDrawer(ripple) {
  selectedRipple.value = await fetchRippleDetail(ripple.ripple_id);
  drawerOpen.value = true;
}

async function applySelected() {
  if (!selectedRipple.value) return;
  await store.apply(selectedRipple.value.ripple_id);
  drawerOpen.value = false;
}

async function rejectSelected() {
  if (!selectedRipple.value) return;
  await store.reject(selectedRipple.value.ripple_id);
  drawerOpen.value = false;
}
</script>

<style scoped>
.ripples-page { padding: 24px; max-width: 1200px; margin: 0 auto; }
.ripples-page h1 { margin-bottom: 16px; }
</style>
```

- [ ] **Step 7.5: Add 1 new route to `dashboard/frontend/src/router/index.js` (additive)**

```javascript
// dashboard/frontend/src/router/index.js (additive, Phase 9.13)

// Add to imports:
import RipplesPage from '../pages/RipplesPage.vue'

// Add to routes array (跟 DecisionsPage / WorkflowsPage 平级):
{
  path: '/ripples',
  name: 'Ripples',
  component: RipplesPage,
}
```

- [ ] **Step 7.6: Add 1 new nav item to `dashboard/frontend/src/components/Sidebar.vue` (additive)**

```vue
<!-- dashboard/frontend/src/components/Sidebar.vue (additive, Phase 9.13) -->
<!-- Add inside <nav> 跟 DecisionsPage / WorkflowsPage link 平级: -->
<router-link to="/ripples" class="nav-item" data-testid="nav-ripples">Ripples</router-link>
```

- [ ] **Step 7.7: Run tests, verify GREEN**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
pnpm test:unit -- ripple-drawer 2>&1 | tail -10
# 期望: 3 passed
```

- [ ] **Step 7.8: Run baseline, ensure 0 vitest regression**

```bash
pnpm test:unit 2>&1 | tail -5
# 期望: 125 + 3 = 128+ tests passed
```

- [ ] **Step 7.9: pnpm build (verify 0 build error)**

```bash
pnpm build 2>&1 | tail -5
# 期望: 0 error
```

- [ ] **Step 7.10: Commit**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
git add novel-factory/dashboard/frontend/src/components/RippleDrawer.vue \
        novel-factory/dashboard/frontend/src/pages/RipplesPage.vue \
        novel-factory/dashboard/frontend/src/router/index.js \
        novel-factory/dashboard/frontend/src/components/Sidebar.vue \
        novel-factory/dashboard/frontend/tests/unit/ripple-drawer.spec.js
git commit -m "feat(cvg): phase9.13 task7 — RippleDrawer + RipplesPage + router + sidebar nav + tests

Phase 9.13 frontend page layer: RippleDrawer (slide-in detail + Apply/Reject 按钮)
+ RipplesPage (page composition 跟 DecisionsPage 1:1) + 1 router entry +
1 sidebar nav item。

改动:
- dashboard/frontend/src/components/RippleDrawer.vue (NEW, +100L):
  - Slide-in from right (跟 DecisionCard drawer 1:1 模式)
  - Inline detail: status / source_chapter / target_chapter / confidence +
    evidence + source_payload + edge_payload (3 collapsible <details> 段)
  - Footer: [Apply] [Reject] 按钮 (status 已经是 terminal 时 disable)
  - data-testid: ripple-drawer / ripple-drawer-content / ripple-drawer-apply /
    ripple-drawer-reject / ripple-drawer-close
  - emit('close') / emit('apply', ripple) / emit('reject', ripple)
- dashboard/frontend/src/pages/RipplesPage.vue (NEW, +150L):
  - 3-section layout: <h1> + RippleFilter (top) + RippleList (center) + RippleDrawer (right overlay)
  - onMounted: sync filter from route query + store.refresh() + socket.connect()
  - watch route query → sync filter → store.refresh (URL state, 跟 useTimeOptions 1:1)
  - watch socket.pendingUpdates → applySocketUpdate (跟 useDecisionStore 1:1)
  - applySelected / rejectSelected: optimistic update + close drawer
- dashboard/frontend/src/router/index.js (+3L, additive): 1 new route
  - { path: '/ripples', name: 'Ripples', component: RipplesPage }
- dashboard/frontend/src/components/Sidebar.vue (+5L, additive): 1 new nav item
  - <router-link to=\"/ripples\" data-testid=\"nav-ripples\">Ripples</router-link>
- dashboard/frontend/tests/unit/ripple-drawer.spec.js (NEW, +60L, 3 tests):
  - renders detail when open / hidden when closed / emit apply+reject

不改:
- 0 改 existing page (DecisionsPage / OverviewPage / WorkflowsPage 0 改)
- 0 改 existing router entry (1 new additive only)
- 0 改 existing sidebar nav (1 new additive only)
- 0 改 existing Pydantic schema (跟 Task 2 一致)

Baseline:
- vitest: 125 + 3 = 128+ tests passed
- pnpm build: 0 error

后续: Task 8 加 e2e smoke spec + final verification"
```

---

## Task 8: e2e smoke spec + final verification (8 commands + 3 e2e)

**Files:**
- Create: `dashboard/frontend/tests/e2e-smoke/ripples.spec.js` (NEW, ~60L, 3 e2e tests)
- Test: `pnpm e2e:smoke --list && cd novel-factory && pytest -q && cd dashboard/frontend && pnpm test:unit && pnpm lint:all && pnpm build`

**Goal:** add Playwright e2e smoke (跟 Phase 8.45.3 1:1 pattern) + run full verification suite (12 commands per spec Section 6)。

- [ ] **Step 8.1: Create `dashboard/frontend/tests/e2e-smoke/ripples.spec.js` (NEW, ~60L)**

```javascript
// dashboard/frontend/tests/e2e-smoke/ripples.spec.js — Phase 9.13
// E2E smoke tests for /ripples page (跟 Phase 8.45.3 1:1 pattern, 0 network, 0 LLM)
import { test, expect } from '../_setup.js';

test.describe('Ripples Page Smoke', () => {
  test('mount /ripples → page renders with filter + list + sidebar nav', async ({ page }) => {
    await page.goto('/ripples');
    await expect(page.getByTestId('nav-ripples')).toBeVisible();
    await expect(page.getByTestId('ripple-filter-status')).toBeVisible();
  });

  test('filter by pending → empty state or list', async ({ page }) => {
    await page.goto('/ripples?status=pending');
    await expect(page.getByTestId('ripple-list-empty').or(page.getByTestId('ripple-list-loading'))).toBeVisible({ timeout: 5000 });
  });

  test('open drawer → click close → drawer hides', async ({ page }) => {
    await page.goto('/ripples');
    // First ripple card if exists, else skip
    const card = page.getByTestId('ripple-card').first();
    if (await card.count() > 0) {
      await card.click();
      await expect(page.getByTestId('ripple-drawer')).toBeVisible();
      await page.getByTestId('ripple-drawer-close').click();
      await expect(page.getByTestId('ripple-drawer-content')).not.toBeVisible();
    }
  });
});
```

- [ ] **Step 8.2: Run e2e:smoke --list (verify 7 tests listed)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
pnpm e2e:smoke --list 2>&1 | tail -10
# 期望: 7 (4 Phase 8.45.3 + 3 Phase 9.13)
```

- [ ] **Step 8.3: Run full pytest baseline + new tests**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pytest -q 2>&1 | tail -3
# 期望: 2370 passed, 0 failed, 27 skipped
```

- [ ] **Step 8.4: Run vitest baseline + new tests**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
pnpm test:unit 2>&1 | tail -5
# 期望: 128+ tests passed
```

- [ ] **Step 8.5: ruff check (全 0)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
ruff check infra/ tests/ dashboard/ 2>&1 | tail -3
# 期望: 0 issues
```

- [ ] **Step 8.6: pnpm lint:all**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
pnpm lint:all 2>&1 | tail -5
# 期望: 0 errors / ≤8 warnings
```

- [ ] **Step 8.7: pnpm build**

```bash
pnpm build 2>&1 | tail -5
# 期望: 0 error
```

- [ ] **Step 8.8: Verify 0 改 Phase 9.10/9.11/9.12 source API**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
git diff 9406936..HEAD -- novel-factory/infra/cross_volume/reference_graph.py novel-factory/infra/cross_volume/ripple.py novel-factory/infra/cross_volume/extractors/ novel-factory/infra/cross_volume/extraction_rules.yaml novel-factory/infra/cross_volume/llm_scanner.py novel-factory/infra/cross_volume/edge_inferrer.py novel-factory/infra/cross_volume/llm_cache.py 2>&1 | head -20
# 期望: 空 (0 改)
```

- [ ] **Step 8.9: Verify 0 改既有 dashboard endpoint / composable / store**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
git diff 9406936..HEAD -- novel-factory/dashboard/ws.py 2>&1 | head -10
# 期望: 空 (0 改 dashboard/ws.py 既有 ConnectionManager)
git diff 9406936..HEAD -- novel-factory/dashboard/frontend/src/composables/useDecisionStore.js novel-factory/dashboard/frontend/src/composables/useWorkflowSocket.js 2>&1 | head -10
# 期望: 空 (0 改既有 composable)
```

- [ ] **Step 8.10: git status clean (8 改文件)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
git status
# 期望: 8 commits (含 8 new modules/tests + 2 spec/plan + 1 e2e)
```

- [ ] **Step 8.11: git log verify**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
git log --oneline 9406936..HEAD
# 期望: 8 feat(cvg): phase 9.13 commits + 2 docs (spec + plan) = 10 commits total
```

- [ ] **Step 8.12: Update MEMORY.md + phases-8-dashboard.md (Phase 9.13 entry append)**

```bash
# Add 5-6 line entry to /home/ailearn/.claude/projects/.../memory/phases-8-dashboard.md
# (跟 Phase 9.10/9.11/9.12 entry 1:1 format)
# Use Edit tool to append after Phase 9.12 entry
```

- [ ] **Step 8.13: Final commit (memory sync)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
git add ../.claude/projects/-home-ailearn-projects-AI-Incursion-domains-IP---projects-LingWen/memory/MEMORY.md \
        ../.claude/projects/-home-ailearn-projects-AI-Incursion-domains-IP---projects-LingWen/memory/phases-8-dashboard.md
git commit -m "docs: phase9.13 — memory sync (append 8-commit entry + baseline 2370 tests)

Phase 9.13 closed:
- 8 feat(cvg) commits pushed to origin/master
- 35 pytest + 21 vitest + 3 e2e smoke = 59 new tests
- pytest baseline 2370 passed (2335 + 35)
- vitest baseline 128+ tests
- 0 改 Phase 9.10/9.11/9.12 source API
- 0 改 dashboard 既有 endpoint/composable/router/sidebar
- 0 改 MasterController / got_bridge / workflow.db / cost_tracker.db
- 0 改 12 SCENARIOS 路由
- 0 改 CLAUDE.md / pyproject.toml / pytest.ini
- ruff 0 / pnpm lint:all 0 / pnpm build 0 / pnpm e2e:smoke --list 7
- 0 force-push / 0 amend / 0 Co-Authored-By

Next: Phase 9.14 (ripple audit log + fsync + real rollback) / Phase 9.15
(cascade BFS + drawer cascade graph viz)"
```

- [ ] **Step 8.14: Final push**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
git push origin master
# 期望: 10 commits pushed cleanly (跟 Phase 9.12 一致模式)
```

---

## Out of Scope (后续 phases)

- **Phase 9.14**: ripple audit log (ripple_audit table) + fsync + real rollback。Phase 9.13 apply/reject 仅改 status, Phase 9.14 加 audit trail + rollback
- **Phase 9.15**: cascade BFS 真实现 (Phase 9.10 留 `trigger_cascade()` stub returns []) + drawer cascade graph visualization (vis.js) + depth slider 1/2/3 hops
- **Apply dry-run preview**: BFS partial calc before commit (留 Phase 9.14)
- **5 status timeline visualization**: ripple lifecycle history (留 Phase 9.15)
- **359 章 corpus backfill 已产生的 ripple 显示**: Phase 9.11 dry-run → apply 路径 (留 followup 跟 workflow.db 集成)
- **page-level Playwright e2e deepening**: DecisionsPage / WorkflowsPage / OverviewPage 加深 (Phase 8.45.3 followup 一致, 留 Phase 8.46+)
- **TS strict mode**: 1 item tsconfig.json strict: true (Phase 8.46+ followup)
- **CI Playwright integration**: 目前仅 install + smoke spec, CI 集成留 followup
- **Coverage threshold tightening**: branches 50 → 60 (Phase 8.44 followup)
- **真实 LLM e2e opt-in**: 留 `LINGWEN_REAL_LLM=1` env opt-in (跟 Phase 8/8.1/8.2 模式)
- **prompt auto-optimization / multi-model A/B test**: 留 followup
- **cost budget alarm**: 留 Phase 8.8
- **MEMORY.md 拆 phases-8-dashboard.md (95L)**: 留独立 phase
- **ruff 3 个 I001 errors**: 留 followup tech debt
- **改 historical spec/plan doc 行号引用** (`:NNN`): 历史已不准, 改反误导, 永不做

---

## Risks + Mitigations

| 风险 | 缓解 |
|------|------|
| 旧 2335 tests 破 (新 endpoint 必传 / 既有 endpoint 改) | 5+1 new endpoint additive only, 0 改既有 endpoint handler (Q3 + Task 2 spec) |
| Type hint `Optional[str]` 跟既有 `Optional[int]` pattern 不一致 | 显式 regex 校验, Pydantic 自动 422 (spec 4.4) |
| 既有 dashboard `useDecisionStore` / `useWorkflowSocket` 误改 | 新增 `useRippleStore` / `useRippleSocket` 平行 module, 0 改既有 composable (Task 4/5 spec) |
| `RippleStorage.update_ripple_status` broadcast hook 误破 Phase 9.11/9.12 baseline | broadcast 走 try/except 静默, 不 raise (跟 `cost_tracker.record()` 1:1 pattern, Task 1.4 spec) |
| WebSocket 重连风暴 (client 端无限重试) | `MAX_RECONNECT_ATTEMPTS=3`, 3 次失败后 polling fallback banner (跟 Phase 6.4 1:1, Task 5 spec) |
| apply 已 applied 的 ripple 误操作 | backend `ConflictError` → 409, 前端 button disable pre-check + 409 banner (Task 7.3 spec) |
| drawer 打开时 ripple 已被其他 tab apply (stale state) | drawer.open 时重新 GET detail → status check → button disable (Task 7.4 spec) |
| Empty filter result 误以为错误 | explicit empty state card "No ripples match current filter" (Task 6.5 spec) |
| 8 relationship_type 不全 (Phase 9.12 已知 edge inferrer 仅 8 type) | fixture 8 type 覆盖, drawer display 跟 Phase 9.12 `VALID_REL_TYPES` 1:1 |
| 5 status (pending/confirmed/applied/rejected/failed) enum drift | backend regex `^(pending\|confirmed\|applied\|rejected\|failed)$`, 422 fail-loud (Task 2.4 spec) |
| Cascade depth > 1 用户期待 | drawer 显式标注 "Direct edge only (1-hop), cascade BFS 留 Phase 9.15" (Q6) |
| Vue 3 reactivity 在 module-level singleton 下 SSR 不安全 | 项目当前 SPA (Vite), 0 SSR, module-level singleton 跟 useDecisionStore 1:1 (Phase 8.31 baseline) |
| WS message 顺序错乱 (新 ripple 跟 status change 竞态) | server-side `applySocketUpdate` 单线程 + 微任务 defer; client-side 接 same order (Task 4.4 spec) |
| Optimistic update rollback 漏 case (POST 200 但 server 实际没改) | refresh() 重新拉保权威, `ripples.value[idx]` 用 server 返回的最新 status (Task 4.4 spec) |
| E2E smoke WS 不稳定 (network/timing) | e2e 用 Playwright `waitForFunction` 等 WS message, 0 fixed sleep (跟 Phase 8.45.3 smoke 1:1) |
| 0 production code 改 (Phase 9.10/9.11/9.12 baseline) | 0 改既有 source, 仅 additive RippleStorage methods + additive endpoints (Q3) |
| MEMORY.md 超 200 行 | 接受超 1-3 行 (33 → 35), 留 followup 拆 (Phase 8+9 followup) |
| API key 泄漏 | 0 provider 改, 0 .env 改, 走 router (Phase 9.12 baseline) |
| Playwright browser 未装 | `pnpm e2e:smoke --list` 仅列, opt-in `npx playwright install chromium` 走 user (跟 Phase 8.45.3 1:1) |
| Dashboard /ripples route conflict | 跟现有 /workflows /decisions /overview 0 重叠, sidebar nav 新增 Ripples entry 唯一 |
| _ripple_to_list_item 里的 dimension / relationship_type 是 placeholder | 显式注释 "Phase 9.14 JOIN reference_nodes" + 风险表登记 (Task 2.4 spec) |

---

## Critical files

### Created (NEW)
- `infra/cross_volume/storage.py` — additive 3 methods + ConflictError + 1-line broadcast hook (~+120L)
- `dashboard/cvg_ws.py` — CvgConnectionManager + broadcast helper (~+120L)
- `dashboard/frontend/src/composables/useRippleStore.js` — module-level singleton (~+80L)
- `dashboard/frontend/src/composables/useRippleSocket.js` — WS composable (~+80L)
- `dashboard/frontend/src/components/RippleCard.vue` — 4-dim + 8-rel + 2 ref + confidence card (~+120L)
- `dashboard/frontend/src/components/RippleFilter.vue` — 3 dropdowns (~+80L)
- `dashboard/frontend/src/components/RippleList.vue` — cards container with 3 states (~+50L)
- `dashboard/frontend/src/components/RippleDrawer.vue` — slide-in detail + Apply/Reject (~+100L)
- `dashboard/frontend/src/pages/RipplesPage.vue` — page composition (~+150L)
- `tests/cross_volume/test_storage_ripple_action.py` — 12 tests (~+150L)
- `tests/dashboard/test_cvg_api.py` — 15 tests (~+250L)
- `tests/dashboard/test_cvg_ws.py` — 8 tests (~+150L)
- `dashboard/frontend/tests/unit/use-ripple-store.spec.js` — 6 tests (~+120L)
- `dashboard/frontend/tests/unit/use-ripple-socket.spec.js` — 5 tests (~+100L)
- `dashboard/frontend/tests/unit/ripple-card.spec.js` — 4 tests (~+80L)
- `dashboard/frontend/tests/unit/ripple-filter.spec.js` — 3 tests (~+60L)
- `dashboard/frontend/tests/unit/ripple-list.spec.js` — 3 tests (~+60L)
- `dashboard/frontend/tests/unit/ripple-drawer.spec.js` — 3 tests (~+60L)
- `dashboard/frontend/tests/e2e-smoke/ripples.spec.js` — 3 e2e tests (~+60L)

### Modified (additive only)
- `dashboard/app.py` — 5 REST endpoints + 1 WS endpoint + 2 helpers + 1 singleton (~+180L)
- `dashboard/protocols.py` — 4 Pydantic schemas (~+80L)
- `dashboard/frontend/src/api/index.js` — 5 new fetch methods (~+60L)
- `dashboard/frontend/src/router/index.js` — 1 new route entry (~+3L)
- `dashboard/frontend/src/components/Sidebar.vue` — 1 new nav item (~+5L)

### Referenced / 复用 (Phase 9 baseline, 0 改)
- `infra/cross_volume/ripple.py` (CrossVolumeRipple frozen dataclass)
- `infra/cross_volume/reference_graph.py` (ReferenceNode / ReferenceEdge)
- `dashboard/ws.py` (ConnectionManager class, 跟 CvgConnectionManager 1:1 mirror template)
- `dashboard/frontend/src/composables/useDecisionStore.js` (module-level singleton pattern)
- `dashboard/frontend/src/composables/useWorkflowSocket.js` (WS composable + reconnect pattern)
- `dashboard/frontend/src/components/DecisionCard.vue` (RippleCard 简化版 template)
- `dashboard/frontend/src/pages/DecisionsPage.vue` (RipplesPage 1:1 layout template)

### NOT modified
- `infra/agent_system/*` (MasterController / got_bridge / 12 SCENARIOS)
- `infra/ai_service/cost_tracker.py` / `infra/ai_service/cost_persistence.py`
- `infra/.state/workflow.db` / `infra/.state/cost_tracker.db` (gitignored, 0 commit)
- `dashboard/ws.py` (既有 ConnectionManager 0 改, 0 行)
- `dashboard/frontend/src/composables/useDecisionStore.js` / `useWorkflowSocket.js` (0 改)
- `dashboard/frontend/src/pages/DecisionsPage.vue` / `OverviewPage.vue` / `WorkflowsPage.vue` (0 改)
- `pyproject.toml` / `pytest.ini` / `vite.config.js` / `vitest.config.js` / `playwright.config.js` (0 改)
- 18 历史 spec/plan docs (0 改 `:NNN` 行号)
- `infra/cross_volume/llm_scanner.py` / `llm_cache.py` / `edge_inferrer.py` / `extraction_rules.yaml` (0 改)
- `infra/cross_volume/backfill.py` / `extractors/*` (0 改)

---

## Definition of Done (跟 Phase 9 baseline 一致)

- [ ] 1 spec doc 创建 (`docs/superpowers/specs/2026-06-10-phase9.13-cvg-ui-design.md`, commit 0d4c3d8, 937L)
- [ ] 1 plan doc 创建 (本文件, `docs/superpowers/plans/2026-06-10-phase9.13-cvg-ui.md`)
- [ ] `infra/cross_volume/storage.py` 3 additive methods + ConflictError + 1-line broadcast hook
- [ ] `dashboard/app.py` 5 REST endpoints + 1 WebSocket endpoint + 2 helpers + 1 singleton
- [ ] `dashboard/protocols.py` 4 Pydantic schemas (RippleListItemResponse / RippleDetailResponse / RippleActionResponse / RippleStatsResponse)
- [ ] `dashboard/cvg_ws.py` CvgConnectionManager + module-level broadcast() helper
- [ ] `dashboard/frontend/src/composables/useRippleStore.js` (跟 useDecisionStore 1:1)
- [ ] `dashboard/frontend/src/composables/useRippleSocket.js` (跟 useWorkflowSocket 1:1)
- [ ] `dashboard/frontend/src/components/RippleCard.vue` + `RippleFilter.vue` + `RippleList.vue` + `RippleDrawer.vue`
- [ ] `dashboard/frontend/src/pages/RipplesPage.vue` (跟 DecisionsPage 1:1)
- [ ] `dashboard/frontend/src/router/index.js` 1 new route
- [ ] `dashboard/frontend/src/components/Sidebar.vue` 1 new nav item
- [ ] `dashboard/frontend/src/api/index.js` 5 new fetch methods
- [ ] 8 commits pushed to origin/master
- [ ] pytest **2370 passed, 0 failed, 27 skipped** (2335 baseline + 35 new)
- [ ] vitest **128+ tests passed** (104 baseline + 21 new + 0 旧改)
- [ ] Playwright **7 e2e tests listed** (4 Phase 8.45.3 + 3 Phase 9.13)
- [ ] ruff 0 issues
- [ ] pnpm lint:all 0 errors / ≤8 warnings (跟 Phase 8.45 baseline)
- [ ] pnpm build 0 error
- [ ] 0 改 Phase 9.10/9.11/9.12 source API 行为 (除 1-line defensive try/except broadcast hook)
- [ ] 0 改 dashboard 既有 endpoint handler / composable / store / router / sidebar nav
- [ ] 0 改 MasterController / got_bridge / workflow.db / cost_tracker.db
- [ ] 0 改 12 SCENARIOS 路由
- [ ] 0 改 CLAUDE.md / pyproject.toml / pytest.ini / vite.config.js / vitest.config.js / playwright.config.js
- [ ] 0 改 historical spec/plan doc `:NNN` 行号
- [ ] 0 改 .env / 0 API key 泄漏
- [ ] 0 改 `.state/*.db` 提交 (gitignored)
- [ ] 0 force-push / 0 amend / 0 --no-verify / 0 Co-Authored-By footer
- [ ] 0 DANGEROUSLY_SKIP_PERMISSIONS
- [ ] MEMORY.md + phases-8-dashboard.md 同步刷新 (Phase 9.13 entry append)

---

## 完成后下一步 (后续 phases)

- **Phase 9.14**: ripple audit log + fsync + real rollback (下 1 phase)
- **Phase 9.15**: cascade BFS + drawer cascade graph (vis.js)
- **MEMORY.md 拆 phases-8-dashboard.md**: 留独立 phase (95L 拆 4 era)
- **MEMORY.md line 8 typo** "Phase 8.16-8.24" → 8.16-8.45+9.10+9.11+9.12+9.13
- **legacy `./memory/MEMORY.md` v6.1 → v9.20 sync** (deferred)
- **Page-level Playwright e2e** (Phase 8.45.3 followup)
- **TS strict mode** (Phase 8.46+ Super-phase D)
- **CI Playwright integration** (Phase 8+)
- **Coverage threshold branches 50 → 60**
- **Phase 10+ 跨卷涟漪 5 super-phase 收尾**: 9.13 + 9.14 + 9.15 完成即 Phase 9 super-phase 收口, 后续 phase 10+ 留 TBD
