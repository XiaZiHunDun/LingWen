# Phase 8.4 Implementation Plan — Rename Misleading Test File

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `tests/agent_system/test_master_controller_e2e_real_llm.py` 重命名为 `tests/agent_system/test_master_controller_stub_router_e2e.py`, 消除 Phase 8 真 LLM 测试引入后的命名冲突. 0 测试代码改动, 0 production code 改动, 1 git commit 推送 origin/master.

**Architecture:**
- 1 git mv (保留 file history)
- 4 active code 引用 sed 改 (`_e2e_helpers.py` + `test_novel_writing_real_llm.py` + `minimal_e2e.yaml`)
- 18 历史 spec/plan docs sed 改
- MEMORY.md 8 entries sed 改 (本机文件, 不在 commit)
- 0 测试代码改动 / 0 production code 改动

**Tech Stack:** git, sed, pytest, ruff (跟 Phase 8/8.1/8.2/8.3 一致, 无新依赖)

---

## Context: 为什么本期要做

Phase 7 (commit 671db78) 引入的文件 `tests/agent_system/test_master_controller_e2e_real_llm.py` (540L, 14 tests) 名字含 "real_llm" 但实际用 `StubProvider` 注入 mock router 跑生产路径.

Phase 8 (commit d6e7c25) 引入真 LLM 测试 `test_novel_writing_real_llm.py` 后, 命名冲突明显:
- 旧 `test_master_controller_e2e_real_llm.py` = StubProvider 测生产路径 (4-classes 14 tests)
- 新 `test_novel_writing_real_llm.py` = 真实 Anthropic API (2 tests, opt-in SKIP 默认)

**主公决策** (2026-06-05): 选 `test_master_controller_stub_router_e2e.py` (选项 A: 保留 `e2e` 后缀 + 替换 `real_llm` 为 `stub_router`).

**当前 baseline**: 2090 passed, 0 failed, 21 skipped
**目标 baseline**: 2090 passed, 0 failed, 21 skipped (不变)

**TDD 风格**: N/A — 纯 rename 任务, 0 测试代码改动

---

## Task 1: 写 spec + plan docs

### Step 1.1: 写 spec doc

**File**: `docs/superpowers/specs/2026-06-05-phase8.4-rename-misleading-test-file-design.md` (NEW, ~80L)

**Sections**: Context, Goals, Non-Goals, Design, Risks+Mitigations, Verification, Future Work.

### Step 1.2: 写 plan doc (本文件)

**File**: `docs/superpowers/plans/2026-06-05-phase8.4-rename-misleading-test-file.md` (NEW, ~200L)

**Sections**: Goal, Architecture, Context, Task 1-4 steps, Critical files, DoD.

---

## Task 2: git mv + sed active code

### Step 2.1: git mv 文件

**Files**:
- `tests/agent_system/test_master_controller_e2e_real_llm.py` → `tests/agent_system/test_master_controller_stub_router_e2e.py`

```bash
git mv tests/agent_system/test_master_controller_e2e_real_llm.py \
       tests/agent_system/test_master_controller_stub_router_e2e.py
```

**为什么用 `git mv`**: 保留 `git log --follow` 完整 file history (Phase 7 commit 671db78 + 4 后续 phase 改动).

### Step 2.2: sed 改 3 active code 文件 (4 行)

```bash
sed -i 's|test_master_controller_e2e_real_llm\.py|test_master_controller_stub_router_e2e.py|g' \
    tests/agent_system/_e2e_helpers.py

sed -i 's|test_master_controller_e2e_real_llm\.py|test_master_controller_stub_router_e2e.py|g' \
    tests/agent_system/test_novel_writing_real_llm.py

sed -i 's|test_master_controller_e2e_real_llm\.py|test_master_controller_stub_router_e2e.py|g' \
    infra/got/workflows/minimal_e2e.yaml
```

**关键约束**: sed pattern 用 `\.py` 限定后缀, 避免误改其他上下文.

### Step 2.3: sed 改 18 历史 spec/plan docs

```bash
grep -rl "test_master_controller_e2e_real_llm" docs/superpowers/ | xargs sed -i \
  's|test_master_controller_e2e_real_llm|test_master_controller_stub_router_e2e|g'
```

**关键约束**: sed pattern **不**加 `\.py` 限定, 历史 docs 可能提及"模块 docstring"等无后缀上下文. 验证 grep 18 文件.

### Step 2.4: sed 改 MEMORY.md (本机文件, 不在 commit)

```bash
sed -i 's|test_master_controller_e2e_real_llm|test_master_controller_stub_router_e2e|g' \
    /home/ailearn/.claude/projects/-home-ailearn-projects-AI-Incursion-domains-IP---projects-LingWen/memory/MEMORY.md
```

---

## Task 3: 验证 baseline 仍然 pass

### Step 3.1: 跑全套测试

```bash
pytest -q 2>&1 | tail -3
```

**期望**: `2090 passed, 0 failed, 21 skipped` (跟 Phase 8.3 baseline 一致)

### Step 3.2: 跑重命名后的文件

```bash
pytest tests/agent_system/test_master_controller_stub_router_e2e.py -v
```

**期望**: `14 passed` (跟 Phase 7 一致, 0 测试代码改动)

### Step 3.3: grep 验证 0 残留

```bash
grep -r "test_master_controller_e2e_real_llm" tests/ infra/ dashboard/ pyproject.toml pytest.ini
```

**期望**: 空输出

### Step 3.4: ruff 0 issues

```bash
ruff check tests/agent_system/ 2>&1 | tail -3
```

**期望**: `0 issues`

### Step 3.5: git log --follow 验证 history 保留

```bash
git log --oneline --follow tests/agent_system/test_master_controller_stub_router_e2e.py | head -10
```

**期望**: 包含 `671db78` (Phase 7) + 4 后续 phase 改动 (fac604f / 0b704a1 / 10643f8 / 56f9f00)

---

## Task 4: 1 feat commit + push

### Step 4.1: git add + commit

**Commit message** (no Co-Authored-By per project global setting):

```
test(agent_system): phase 8.4 — rename misleading 'real_llm' test file to 'stub_router_e2e'

Phase 7 引入的 tests/agent_system/test_master_controller_e2e_real_llm.py (540L, 14
tests) 名字含 'real_llm' 但实际用 StubProvider 注入 mock router 跑生产路径. Phase 8
引入真 LLM 测试 test_novel_writing_real_llm.py 后, 命名冲突明显: 旧 'real_llm' 指
StubProvider 实现 LLM 形接口, 新 'real_llm' 指真 Anthropic API.

本次 git mv 重命名 test_master_controller_e2e_real_llm.py →
test_master_controller_stub_router_e2e.py (保留 'e2e' 后缀 + pytest test_ 前缀,
新增 'stub_router' 关键字). 跟 _e2e_helpers.make_stub_router() 函数名直接呼应.

改动:
- git mv 1 文件 (保留 file history)
- sed 改 3 active code 引用 (2 docstring + 1 yaml 注释)
- sed 改 18 历史 spec/plan docs (仅改文件名, 不改行号)
- sed 改 MEMORY.md 8 entries (本机文件)

不改:
- 0 测试代码改动
- 0 production code 改动
- 0 CLAUDE.md / pyproject.toml / pytest.ini 改动
- 0 spec/plan doc 行号引用 改动
- 0 infra/.state/* 提交

Baseline:
- 测试: 2090 passed, 0 failed, 21 skipped (不变)
- ruff 0
- git log --follow 保留完整 file history
```

```bash
git add tests/agent_system/test_master_controller_stub_router_e2e.py \
        tests/agent_system/_e2e_helpers.py \
        tests/agent_system/test_novel_writing_real_llm.py \
        infra/got/workflows/minimal_e2e.yaml \
        docs/superpowers/

git commit -m "..."
```

**注意**: 不 `git add .` / `git add -A` (避免误加 state DB / .env). 显式列文件.

### Step 4.2: git push

```bash
git push origin master
```

**注意**: 不 `--force`, 不 `--no-verify`. 0 冲突预期 (本地 master 跟 origin/master 同步).

---

## Critical files

### Created
- `docs/superpowers/specs/2026-06-05-phase8.4-rename-misleading-test-file-design.md` (~80L)
- `docs/superpowers/plans/2026-06-05-phase8.4-rename-misleading-test-file.md` (本文件, ~200L)

### Modified
- 1 git mv (`tests/agent_system/test_master_controller_e2e_real_llm.py` → `test_master_controller_stub_router_e2e.py`)
- 3 active code sed (`_e2e_helpers.py` + `test_novel_writing_real_llm.py` + `minimal_e2e.yaml`)
- 18 历史 spec/plan docs sed
- MEMORY.md 8 entries sed (本机文件, 不在 commit)

### NOT modified
- 测试文件内容 (0 测试代码改动)
- 任何 production code (`master_controller.py` / `prompts.py` / providers / `got_bridge.py` 等)
- `pytest.ini` / `pyproject.toml` / `CLAUDE.md` / `.claude/`
- 18 历史 docs 行号引用 (`:NNN`)
- `infra/.state/*` 状态 DB

## Out of scope (YAGNI)

- 重构 14 tests 内容
- 拆 MEMORY.md 到 `phases.md` topic file
- 重命名 `_e2e_helpers.py` / `test_novel_writing_real_llm.py`
- 改 18 历史 docs 行号引用
- 接 CI / 加 pytest marker

## DoD

- [ ] 2 new docs 写好 (spec + plan)
- [ ] 1 git mv 完成
- [ ] 3 active code sed 完成
- [ ] 18 历史 docs sed 完成
- [ ] MEMORY.md 8 entries sed 完成 (本机)
- [ ] `pytest -q` 2090 passed, 0 failed, 21 skipped
- [ ] `pytest tests/agent_system/test_master_controller_stub_router_e2e.py -v` 14 passed
- [ ] `grep -r "test_master_controller_e2e_real_llm" tests/ infra/ dashboard/ pyproject.toml pytest.ini` 空
- [ ] `git log --follow` 保留 671db78 history
- [ ] `ruff check tests/agent_system/` 0 issues
- [ ] 1 feat commit pushed to origin/master
- [ ] 0 force-push, 0 --no-verify, 0 Co-Authored-By footer
- [ ] 0 production / test code changes
- [ ] 0 `infra/.state/*` 提交
