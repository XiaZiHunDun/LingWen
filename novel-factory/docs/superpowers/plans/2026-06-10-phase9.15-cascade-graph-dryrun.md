# Phase 9.15 — Cascade BFS + Drawer Graph Viz + Apply Dry-Run Implementation Plan

> **For agentic workers:** REQUIRED SUB-K-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 Phase 9.14 (ripple audit log + real rollback, commit c583fd9) 基础上,实现 `CrossVolumeReferenceGraph.trigger_cascade()` 真实 BFS (替换 Phase 9.10 stub returns []) + 持久化 cascade 结果 (`ripple_cascade` 新表) + drawer 内 `<CascadeGraph>` ECharts graph 可视化 (lazy import) + apply dry-run preview (节点按 action 上色 + 4 summary chips + 二次确认 modal)。0 改 Phase 9.10/9.11/9.12/9.13/9.14 既有 source API 行为,仅 additive storage methods + additive endpoints + additive drawer section + new CascadeGraph 子组件。

**Architecture:** 4 task subagent-driven, 3-layer additive: (Layer1 Backend) `CrossVolumeReferenceGraph.trigger_cascade()` 替换 stub (depth 3 + visited set, return new `CascadedRipple` frozen dataclass 7 fields) + `RippleStorage` 2 additive methods (`record_cascade` / `get_cascade_by_ripple_id`) + 新 `ripple_cascade` table (additive, 0 改既有 6 表) + `record_ripple` 内部多调 1 次 `record_cascade` (走 `atomic_batch` 原子化) + (Layer2 Frontend) FastAPI 2 new GET endpoints (`/api/cvg/ripples/{id}/cascade` / `/api/cvg/ripples/{id}/cascade/preview`) + 2 Pydantic schemas + 新 `<CascadeGraph>` 子组件 (lazy ECharts 5.4.x graph) + 新 `<ApplyConfirmModal>` 子组件 + `useRippleStore` 2 new actions + `api/index.js` 2 new methods + (Layer3 Drawer UX) drawer 顶部 "Dry-run" 黄色 chip + 4 summary chips + Apply 二次确认 modal + 节点按 action 上色 (绿/红/黄/灰)。

**Tech Stack:** Python 3.12 + sqlite3 (additive table, 0 改 3 PRAGMAs) + FastAPI + Pydantic + Vue 3 + Vite + ECharts 5.4.x (lazy import, graph series) + vitest + Playwright + pytest 8 + ruff。

---

## Pre-Execution Setup

### Context
- Phase 9.10 (commit `c421da1`): CVG data model — 3 frozen dataclass + RippleStorage (sqlite3, 3 PRAGMAs, 8 public methods) + 16 tests
- Phase 9.11 (commit `149864c`, 9 commits): rule-based CVG backfill + 16 tests
- Phase 9.12 (commit `9406936`, 11 commits): LLM scanner 4 维 + Edge 推断 8 边类型
- Phase 9.13 (commit `9e996fc`, 14 commits): dashboard `/ripples` 页面 + 5 REST + 1 WebSocket `/api/ws/cvg` + 4 components + 2 composables + sidebar nav
- Phase 9.14 (commit `c583fd9`, 15 commits): ripple audit log + real rollback + drawer audit timeline + 2 CLI subcommand + 44 new tests
- **Phase 9.15** (commits 4960729 + dd1b000 spec, 304L): cascade BFS 真实 + 持久化 + drawer graph viz + apply dry-run preview

### Baseline State
- 测试: 2404 passed, 0 failed, 27 skipped
- vitest: 140 tests passed
- Playwright: 10 e2e smoke tests
- 主公 (用户) hard rules (MUST apply verbatim 全程):
  - **0 force-push, 0 amend, 0 --no-verify, 0 Co-Authored-By footer** (Attribution disabled globally via `~/.claude/settings.json`)
  - **0 DANGEROUSLY_SKIP_PERMISSIONS, 0 API key 泄漏, 0 .env 改**
  - **0 改 CLAUDE.md / pyproject.toml / pytest.ini / vite.config.js / vitest.config.js / playwright.config.js / historical spec/plan doc `:NNN` 行号**
  - **0 改 Phase 9.10/9.11/9.12/9.13/9.14 既有 source API 行为** (既 12 storage method public signature + 7 endpoint 路径 + 5 dashboard component + 2 composable + 1 router + 1 sidebar nav entry 全部不动, 仅 additive)
  - **0 改 dashboard 既有 endpoint handler / composable / store / router / sidebar nav / api method** (新增 2 GET endpoint + 2 schemas + 2 store actions + 1 drawer section + 1 子组件 + 2 api method, 全部 additive)
  - **0 production code 改** (MasterController / got_bridge / workflow.db / cost_tracker.db 0 行)
  - File size 200-400L typical / 800L hard cap
  - TDD methodology (RED → GREEN → commit per task)
  - Conventional commits format
  - 沟通: 叙述中文 / 代码英文 / commit body 中文注释
  - Git root: `/home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/`
  - 当前 working dir: `novel-factory/`
  - MEMORY.md < 200 lines (current ~35 entries, Phase 9.15 后 ~36-37, 留 followup 拆)

### Target State
- 测试: **2418 passed, 0 failed, 27 skipped** (2404 baseline + 14 new pytest)
- vitest: **+7 new** (140 baseline + 7 new = 147)
- Playwright: **+3 new** (10 baseline + 3 new = 13)
- ruff: 0 issues on new files
- pnpm lint:all: 0 errors / ≤8 warnings (跟 Phase 9.14 baseline 一致)
- pnpm build: 0 error
- pnpm e2e:smoke --list: 13 tests (10 Phase 9.14 + 3 Phase 9.15)
- 0 改 Phase 9.10-9.14 既有 source API 行为
- 4 feat commits + 1 final review commit (5 total) + 1 spec commit (4960729) + 1 spec self-review commit (dd1b000) + 1 plan commit (本文件) = 8 commits
- 0 force-push / 0 amend / 0 Co-Authored-By

### File map summary
- **4 modified prod files**: `infra/cross_volume/reference_graph.py` (146L → ~190L, +44L) / `infra/cross_volume/storage.py` (502L → ~580L, +78L) / `dashboard/app.py` (1301L → ~1361L, +60L) / `dashboard/protocols.py` (698L → ~738L, +40L)
- **3 modified frontend files**: `dashboard/frontend/src/components/RippleDrawer.vue` (157L → ~210L, +53L) / `dashboard/frontend/src/composables/useRippleStore.js` (155L → ~180L, +25L) / `dashboard/frontend/src/api/index.js` (304L → ~334L, +30L)
- **2 new spec docs** (已 commit): `docs/superpowers/specs/2026-06-10-phase9.15-cascade-graph-dryrun-design.md` (304L)
- **1 plan doc** (本文件, 末尾 commit)
- **2 new prod components**: `dashboard/frontend/src/components/CascadeGraph.vue` (NEW, ~120L) / `dashboard/frontend/src/components/ApplyConfirmModal.vue` (NEW, ~80L)
- **2 new pytest**: `tests/cross_volume/test_cascade_bfs.py` (NEW, ~200L, 8 tests) / `tests/cross_volume/test_ripple_cascade_storage.py` (NEW, ~150L, 6 tests)
- **1 new vitest**: `dashboard/frontend/src/components/__tests__/CascadeGraph.spec.js` (NEW, ~150L, 5 tests)
- **1 modified vitest**: `dashboard/frontend/src/composables/__tests__/useRippleStore.spec.js` (+2 tests, +30L)
- **1 new e2e**: `tests/e2e/ripple_dryrun_e2e.spec.js` (NEW, ~200L, 3 tests)

---

## Critical safety (主公 hard rules)

> **0 force-push, 0 amend, 0 --no-verify, 0 Co-Authored-By footer** — 写代码前 verify 已设 `git config user.email` 跟 `git config user.name`,提交用 conventional commit 格式。
>
> **0 改 Phase 9.10-9.14 既有 source API 行为** — 仅 additive,1 new `CascadedRipple` dataclass + 2 new storage method + 1 `record_ripple` 内部 `record_cascade()` call (跟 `record_audit` 1:1 pattern) + 2 new GET endpoint + 1 new drawer section + 1 new 子组件 + 2 new store actions + 2 new api method。
>
> **0 DANGEROUSLY_SKIP_PERMISSIONS** — 所有 bash 命令正常走,permission prompt 出现就 approve,不要加 `--no-verify` / `--force`。
>
> **0 改 historical spec/plan doc 行号** — historical 文件 `:NNN` 引用已不准,改反误导,永不做。
>
> **File size guard** — 任何 `.py` / `.vue` / `.js` 文件超 800L 必须先拆 module 再 commit。
>
> **0 cascade data migration** — Phase 9.15 上线前的老 ripple 无 cascade history,`GET /cascade` 返 `None` (无 cascade row) + UI 显示 "No cascade yet",0 数据迁移。

---

## Task 1: `CascadedRipple` dataclass + `CrossVolumeReferenceGraph.trigger_cascade()` 真实 BFS + 8 tests

**Files:**
- Modify: `infra/cross_volume/reference_graph.py:129-131` (替换 stub + 加 `CascadedRipple` dataclass, +44L)
- Create: `tests/cross_volume/test_cascade_bfs.py` (NEW, ~200L, 8 tests)
- Test: `pytest tests/cross_volume/test_cascade_bfs.py -v`

**Goal:** 替换 Phase 9.10 `trigger_cascade()` stub (`returns []`) 为真实 BFS (depth 3 + visited set),返回新 `CascadedRipple` frozen dataclass (7 fields)。TDD: RED (新 test 失败) → GREEN (BFS 真实 + dataclass 实现) → commit。0 改既有 5 method (`add_node` / `add_edge` / `get_neighbors` / `record_ripple` / `get_ripple`) 签名。

- [ ] **Step 1.1: Add `CascadedRipple` dataclass + extend test imports (RED prep)**

Modify `tests/cross_volume/test_cascade_bfs.py` — top imports:

```python
"""Phase 9.15: cascade BFS tests (depth 3 + visited set)."""
import pytest
from infra.cross_volume.reference_graph import (
    CascadedRipple, CrossVolumeReferenceGraph, ReferenceNode, ReferenceEdge,
)
from infra.cross_volume.ripple import CrossVolumeRipple
```

(`CascadedRipple` 不存在,后续 Step 1.3 才加。)

- [ ] **Step 1.2: Write 8 failing tests (RED)**

Append to `tests/cross_volume/test_cascade_bfs.py`:

```python
@pytest.fixture
def graph_with_chain():
    """3-volume chain: V1c1 → V2c1 → V3c1 (linear, 3 hops max)."""
    from infra.cross_volume.storage import RippleStorage
    from pathlib import Path
    import tempfile
    with tempfile.TemporaryDirectory() as tmp:
        storage = RippleStorage(db_path=Path(tmp) / "cascade.db")
        g = CrossVolumeReferenceGraph(storage)
        # Nodes
        n1 = ReferenceNode(id="n1", volume=1, chapter=1, dimension="character")
        n2 = ReferenceNode(id="n2", volume=2, chapter=1, dimension="character")
        n3 = ReferenceNode(id="n3", volume=3, chapter=1, dimension="character")
        n4 = ReferenceNode(id="n4", volume=3, chapter=2, dimension="foreshadow")
        for n in [n1, n2, n3, n4]:
            g.add_node(n)
        # Edges (chain: n1-n2-n3, branch: n3-n4)
        g.add_edge(ReferenceEdge(id="e12", from_node_id="n1", to_node_id="n2"))
        g.add_edge(ReferenceEdge(id="e23", from_node_id="n2", to_node_id="n3"))
        g.add_edge(ReferenceEdge(id="e34", from_node_id="n3", to_node_id="n4"))
        yield g


class TestTriggerCascadeBFS:
    def test_cascade_bfs_depth_1_returns_direct_neighbors(self, graph_with_chain):
        """Ripple starts at n1 → BFS returns [n2] (1-hop)."""
        ripple = CrossVolumeRipple(
            id="rip-1", trigger_volume=1, trigger_chapter=1,
            affected_nodes=("n1",), affected_edges=(), proposed_actions=(),
        )
        cascaded = graph_with_chain.trigger_cascade(ripple)
        assert isinstance(cascaded, CascadedRipple)
        assert cascaded.trigger_ripple_id == "rip-1"
        assert {n.id for n in cascaded.cascade_nodes} == {"n2"}
        assert cascaded.depth_reached == 1

    def test_cascade_bfs_depth_2_returns_2_hop(self, graph_with_chain):
        """Ripple starts at n1 → BFS returns [n2, n3] (1+2 hops)."""
        ripple = CrossVolumeRipple(
            id="rip-1", trigger_volume=1, trigger_chapter=1,
            affected_nodes=("n1",), affected_edges=(), proposed_actions=(),
        )
        cascaded = graph_with_chain.trigger_cascade(ripple)
        assert {n.id for n in cascaded.cascade_nodes} == {"n2", "n3"}
        assert cascaded.depth_reached == 2

    def test_cascade_bfs_depth_3_returns_3_hop_max(self, graph_with_chain):
        """Ripple starts at n1 → BFS returns [n2, n3, n4] (1+2+3 hops, n4 is 3rd hop from n1)."""
        ripple = CrossVolumeRipple(
            id="rip-1", trigger_volume=1, trigger_chapter=1,
            affected_nodes=("n1",), affected_edges=(), proposed_actions=(),
        )
        cascaded = graph_with_chain.trigger_cascade(ripple)
        assert {n.id for n in cascaded.cascade_nodes} == {"n2", "n3", "n4"}
        assert cascaded.depth_reached == 3

    def test_cascade_bfs_cycle_safe_no_infinite_loop(self):
        """Cycle n1→n2→n3→n1: BFS must not loop infinitely (visited set)."""
        from infra.cross_volume.storage import RippleStorage
        from pathlib import Path
        import tempfile
        with tempfile.TemporaryDirectory() as tmp:
            storage = RippleStorage(db_path=Path(tmp) / "cycle.db")
            g = CrossVolumeReferenceGraph(storage)
            for nid in ["n1", "n2", "n3"]:
                g.add_node(ReferenceNode(id=nid, volume=1, chapter=1, dimension="character"))
            g.add_edge(ReferenceEdge(id="e12", from_node_id="n1", to_node_id="n2"))
            g.add_edge(ReferenceEdge(id="e23", from_node_id="n2", to_node_id="n3"))
            g.add_edge(ReferenceEdge(id="e31", from_node_id="n3", to_node_id="n1"))  # cycle back
            ripple = CrossVolumeRipple(
                id="rip-cycle", trigger_volume=1, trigger_chapter=1,
                affected_nodes=("n1",), affected_edges=(), proposed_actions=(),
            )
            cascaded = g.trigger_cascade(ripple)  # must terminate fast
            assert {n.id for n in cascaded.cascade_nodes} == {"n2", "n3"}
            assert cascaded.depth_reached == 2  # n3 reachable, n1 already visited

    def test_cascade_bfs_isolated_ripple_returns_empty(self, graph_with_chain):
        """Ripple on disconnected node n4 → 0 cascade, depth=0."""
        ripple = CrossVolumeRipple(
            id="rip-iso", trigger_volume=3, trigger_chapter=2,
            affected_nodes=("n4",), affected_edges=(), proposed_actions=(),
        )
        cascaded = graph_with_chain.trigger_cascade(ripple)
        assert cascaded.cascade_nodes == ()
        assert cascaded.cascade_edges == ()
        assert cascaded.cascade_actions == ()
        assert cascaded.depth_reached == 0

    def test_cascade_bfs_visited_set_skips_revisit(self, graph_with_chain):
        """Starting node visited, no revisit (n1 is in visited from start)."""
        ripple = CrossVolumeRipple(
            id="rip-1", trigger_volume=1, trigger_chapter=1,
            affected_nodes=("n1",), affected_edges=(), proposed_actions=(),
        )
        cascaded = graph_with_chain.trigger_cascade(ripple)
        # n1 is starting node, NOT in cascade_nodes
        assert "n1" not in {n.id for n in cascaded.cascade_nodes}

    def test_cascade_bfs_collects_edges_for_each_hop(self, graph_with_chain):
        """Each BFS hop collects the corresponding edge."""
        ripple = CrossVolumeRipple(
            id="rip-1", trigger_volume=1, trigger_chapter=1,
            affected_nodes=("n1",), affected_edges=(), proposed_actions=(),
        )
        cascaded = graph_with_chain.trigger_cascade(ripple)
        edge_ids = {e.id for e in cascaded.cascade_edges}
        assert edge_ids == {"e12", "e23", "e34"}

    def test_cascade_bfs_generates_proposed_actions_per_hop(self, graph_with_chain):
        """1 proposed_action per BFS hop (3 hops = 3 actions)."""
        ripple = CrossVolumeRipple(
            id="rip-1", trigger_volume=1, trigger_chapter=1,
            affected_nodes=("n1",), affected_edges=(), proposed_actions=(),
        )
        cascaded = graph_with_chain.trigger_cascade(ripple)
        assert len(cascaded.cascade_actions) == 3
        for i, action in enumerate(cascaded.cascade_actions, start=1):
            assert action["action"] == "propagate"
            assert action["depth"] == i
            assert "from" in action and "to" in action
```

- [ ] **Step 1.3: Run tests to verify they fail**

Run: `pytest tests/cross_volume/test_cascade_bfs.py -v`
Expected: ImportError (`cannot import name 'CascadedRipple'`) + 8 AttributeError (current `trigger_cascade` returns `list[ReferenceNode]`, not `CascadedRipple`)

- [ ] **Step 1.4: Add `CascadedRipple` dataclass + real BFS impl (GREEN)**

Modify `infra/cross_volume/reference_graph.py` — add import `deque` at top + append dataclass after `ReferenceEdge` (line 61) + replace `trigger_cascade` stub (lines 129-131):

```python
from collections import deque
```

```python
@dataclass(frozen=True)
class CascadedRipple:
    """Phase 9.15: BFS-cascaded ripple from trigger_ripple_id.

    Captures all downstream nodes/edges/actions reachable within depth 3.
    Immutable, JSON-serializable (cascade_* use tuple, depth_reached int).
    """
    trigger_ripple_id: str
    cascade_nodes: tuple[ReferenceNode, ...]
    cascade_edges: tuple[ReferenceEdge, ...]
    cascade_actions: tuple[dict, ...]
    depth_reached: int  # 0-3, max depth BFS actually traversed
    generated_at: str  # ISO 8601
    bfs_algorithm_version: str = "v1"  # Phase 9.16 forward-compat
```

```python
    def trigger_cascade(self, ripple: CrossVolumeRipple) -> CascadedRipple:
        """Phase 9.15: real BFS cascade (replaces Phase 9.10 stub returns []).

        Depth limit 3 + visited set (防环). Collects 1 proposed_action per hop.
        """
        visited: set[str] = set(ripple.affected_nodes)
        queue: deque[tuple[str, int]] = deque(
            (n, 1) for n in ripple.affected_nodes
        )
        collected_nodes: list[ReferenceNode] = []
        collected_edges: list[ReferenceEdge] = []
        collected_actions: list[dict] = []
        max_depth = 0
        while queue:
            node_id, depth = queue.popleft()
            if depth > 3:
                continue
            max_depth = max(max_depth, depth)
            for neighbor in self.get_neighbors(node_id):
                if neighbor.id in visited:
                    continue
                visited.add(neighbor.id)
                collected_nodes.append(neighbor)
                # Collect edges for the hop (both source + target side)
                edge_ids = self._index_by_node_edges.get(node_id, set())
                for eid in edge_ids:
                    e = self._edges[eid]
                    if (e.from_node_id == node_id and e.to_node_id == neighbor.id) or \
                       (e.to_node_id == node_id and e.from_node_id == neighbor.id):
                        collected_edges.append(e)
                # 1 proposed_action per hop
                collected_actions.append({
                    "action": "propagate",
                    "from": node_id,
                    "to": neighbor.id,
                    "depth": depth,
                    "weight": neighbor.payload.get("weight", 1.0),
                })
                if depth < 3:
                    queue.append((neighbor.id, depth + 1))
        return CascadedRipple(
            trigger_ripple_id=ripple.id,
            cascade_nodes=tuple(collected_nodes),
            cascade_edges=tuple(collected_edges),
            cascade_actions=tuple(collected_actions),
            depth_reached=max_depth,
            generated_at=datetime.now(timezone.utc).isoformat(),
        )
```

(Replace stub lines 129-131. New file size: 146 + 50 = ~196L, well under 800L cap.)

- [ ] **Step 1.5: Run tests to verify they pass**

Run: `pytest tests/cross_volume/test_cascade_bfs.py -v`
Expected: 8 passed

- [ ] **Step 1.6: Verify baseline 2404 tests still pass (no behavior break)**

Run: `pytest tests/cross_volume/ -q 2>&1 | tail -3`
Expected: 8+ passed (8 new) + Phase 9.10/9.11/9.12/9.13/9.14 既有的 16+16+12+29 = 73 tests still pass (1 new test file + 8 new tests, 0 modified baseline)

- [ ] **Step 1.7: ruff check**

Run: `ruff check infra/cross_volume/reference_graph.py tests/cross_volume/test_cascade_bfs.py 2>&1 | tail -3`
Expected: 0 issues

- [ ] **Step 1.8: Commit**

```bash
git add infra/cross_volume/reference_graph.py tests/cross_volume/test_cascade_bfs.py
git commit -m "feat(cvg): phase9.15 task1 — CascadedRipple + real BFS cascade + 8 tests

Phase 9.14 留 9.15 范围 3 项 9.14 plan §6 out of scope 第 1 项:
CrossVolumeReferenceGraph.trigger_cascade() 替换 Phase 9.10 stub
returns [] (reference_graph.py:129-131 注释明确写 'Phase 10 stub returns
[]. Phase 14 real BFS cascade impl.') 为真实 BFS, 返新 CascadedRipple
frozen dataclass.

新 dataclass:
- CascadedRipple (7 fields): trigger_ripple_id / cascade_nodes (tuple
  ReferenceNode) / cascade_edges (tuple ReferenceEdge) / cascade_actions
  (tuple dict) / depth_reached (int 0-3) / generated_at (ISO 8601) /
  bfs_algorithm_version='v1' (Phase 9.16 forward-compat)

BFS 算法:
- depth limit 3 (硬编码, 0 改后续 phase 9.16 weighted 留口)
- visited set 防环 (cycle test 验证 n1→n2→n3→n1 不死循环)
- 1 proposed_action per hop (含 action='propagate' / from / to / depth /
  weight 5 keys)
- 收集 hop 对应 edge (source + target 两侧 _index_by_node_edges lookup)
- 孤立节点 ripple → cascade_nodes=() / depth_reached=0 (0 exception)
- queue popleft 替代 pop(0), O(1) 跟 collections.deque 1:1 模式

8 tests:
- test_cascade_bfs_depth_1_returns_direct_neighbors
- test_cascade_bfs_depth_2_returns_2_hop
- test_cascade_bfs_depth_3_returns_3_hop_max
- test_cascade_bfs_cycle_safe_no_infinite_loop (n1→n2→n3→n1 cycle)
- test_cascade_bfs_isolated_ripple_returns_empty (n4 disconnected)
- test_cascade_bfs_visited_set_skips_revisit (n1 starting 0 revisit)
- test_cascade_bfs_collects_edges_for_each_hop (3 edges for 3 hops)
- test_cascade_bfs_generates_proposed_actions_per_hop (3 actions)

不改:
- 0 改 add_node / add_edge / get_neighbors / record_ripple / get_ripple
  5 既有 method signature
- 0 改 ReferenceNode / ReferenceEdge / CrossVolumeRipple 既有 3 dataclass
- 0 改 RippleStorage (cascade 持久化留 T2)
- 0 API / 0 UI / 0 改 dashboard 既有 endpoint handler
- 0 改 Phase 9.10-9.14 既有 source API
- 0 force-push / 0 amend / 0 --no-verify / 0 Co-Authored-By

Baseline: 2404 + 8 = 2412 passed, 0 failed, 27 skipped
ruff: 0"
```

---

## Task 2: `ripple_cascade` table + `RippleStorage` 2 method + `record_ripple` 内部 cascade hook + 6 tests

**Files:**
- Modify: `infra/cross_volume/storage.py:42-97` (extend `_SCHEMA_SQL` with `ripple_cascade` table + 1 index, +14L)
- Modify: `infra/cross_volume/storage.py:174-200` (extend `append_ripple` with cascade hook, +10L)
- Modify: `infra/cross_volume/storage.py` (add `CascadedRipple` 2 method + helper, +54L)
- Create: `tests/cross_volume/test_ripple_cascade_storage.py` (NEW, ~150L, 6 tests)
- Test: `pytest tests/cross_volume/test_ripple_cascade_storage.py -v`

**Goal:** 加 `ripple_cascade` 新表 (additive, 0 改既有 6 表) + 2 additive storage method (`record_cascade` / `get_cascade_by_ripple_id`) + `append_ripple` 内部 `record_cascade()` 调用 (走 `atomic_batch` 原子化, 跟 Phase 9.14 `record_audit` hook 1:1 模式)。TDD: RED → GREEN → commit。

- [ ] **Step 2.1: Add import + extend test imports (RED prep)**

Modify `tests/cross_volume/test_ripple_cascade_storage.py` — top imports:

```python
"""Phase 9.15: ripple_cascade table + storage method tests."""
import json
import pytest
from infra.cross_volume.reference_graph import CascadedRipple, ReferenceNode
from infra.cross_volume.ripple import CrossVolumeRipple
from infra.cross_volume.storage import RippleStorage
```

(`CascadedRipple` from Task 1 already exists, `record_cascade`/`get_cascade_by_ripple_id` 不存在, 后续 Step 2.4 才加。)

- [ ] **Step 2.2: Write 6 failing tests (RED)**

Append to `tests/cross_volume/test_ripple_cascade_storage.py`:

```python
@pytest.fixture
def storage(tmp_path):
    return RippleStorage(db_path=tmp_path / "cascade_storage.db")


@pytest.fixture
def seeded_cascade(storage):
    """Storage with 1 ripple + 1 cascade pre-seeded."""
    ripple = CrossVolumeRipple(
        id="rip-c1", trigger_volume=1, trigger_chapter=1,
        affected_nodes=("n1",), affected_edges=(), proposed_actions=(),
    )
    storage.append_ripple(ripple)
    cascaded = CascadedRipple(
        trigger_ripple_id="rip-c1",
        cascade_nodes=(ReferenceNode(id="n2", volume=2, chapter=1, dimension="character"),),
        cascade_edges=(),
        cascade_actions=({"action": "propagate", "from": "n1", "to": "n2", "depth": 1, "weight": 1.0},),
        depth_reached=1,
        generated_at="2026-06-10T10:00:00+00:00",
    )
    cascade_id = storage.record_cascade(cascaded)
    return storage, ripple, cascaded, cascade_id


class TestRippleCascadeTable:
    def test_ripple_cascade_table_exists(self, storage):
        """ripple_cascade table created in __init__ via _SCHEMA_SQL."""
        with storage._connect() as conn:
            row = conn.execute(
                "SELECT name FROM sqlite_master WHERE type='table' AND name='ripple_cascade'"
            ).fetchone()
        assert row is not None, "ripple_cascade table should exist"

    def test_ripple_cascade_index_exists(self, storage):
        """idx_ripple_cascade_trigger index created."""
        with storage._connect() as conn:
            rows = conn.execute(
                "SELECT name FROM sqlite_master WHERE type='index' AND name='idx_ripple_cascade_trigger'"
            ).fetchall()
        assert len(rows) == 1


class TestRecordCascade:
    def test_record_cascade_inserts_row(self, seeded_cascade):
        storage, _, cascaded, cascade_id = seeded_cascade
        assert isinstance(cascade_id, int)
        assert cascade_id >= 1
        with storage._connect() as conn:
            row = conn.execute(
                "SELECT * FROM ripple_cascade WHERE id = ?", (cascade_id,)
            ).fetchone()
        assert row["trigger_ripple_id"] == "rip-c1"
        assert row["depth_reached"] == 1

    def test_record_cascade_stores_nodes_edges_actions_as_json(self, seeded_cascade):
        """cascade_nodes / cascade_edges / cascade_actions stored as JSON, parseable."""
        storage, _, cascaded, cascade_id = seeded_cascade
        with storage._connect() as conn:
            row = conn.execute(
                "SELECT cascade_nodes_json, cascade_edges_json, cascade_actions_json"
                " FROM ripple_cascade WHERE id = ?", (cascade_id,)
            ).fetchone()
        nodes = json.loads(row["cascade_nodes_json"])
        actions = json.loads(row["cascade_actions_json"])
        assert len(nodes) == 1
        assert nodes[0]["id"] == "n2"
        assert len(actions) == 1
        assert actions[0]["action"] == "propagate"

    def test_get_cascade_by_ripple_id_returns_latest(self, seeded_cascade):
        """Multiple cascade rows → get returns latest by created_at DESC."""
        storage, _, _, _ = seeded_cascade
        cascaded2 = CascadedRipple(
            trigger_ripple_id="rip-c1",
            cascade_nodes=(), cascade_edges=(), cascade_actions=(),
            depth_reached=0,
            generated_at="2026-06-10T11:00:00+00:00",  # later timestamp
        )
        storage.record_cascade(cascaded2)
        latest = storage.get_cascade_by_ripple_id("rip-c1")
        assert latest is not None
        assert latest.depth_reached == 0  # latest one wins

    def test_get_cascade_by_ripple_id_returns_none_for_missing(self, storage):
        assert storage.get_cascade_by_ripple_id("rip-nonexistent") is None
```

- [ ] **Step 2.3: Run tests to verify they fail**

Run: `pytest tests/cross_volume/test_ripple_cascade_storage.py -v`
Expected: 6 failures — `sqlite3.OperationalError: no such table: ripple_cascade` (table not exists) + `AttributeError: 'RippleStorage' object has no attribute 'record_cascade'` (method not exists)

- [ ] **Step 2.4: Add `ripple_cascade` table + 2 method + `append_ripple` cascade hook (GREEN)**

Modify `infra/cross_volume/storage.py:42-97` (`_SCHEMA_SQL` block, append new table after `ripple_audit`):

```sql
CREATE TABLE IF NOT EXISTS ripple_cascade (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    trigger_ripple_id TEXT NOT NULL,
    cascade_nodes_json TEXT NOT NULL,
    cascade_edges_json TEXT NOT NULL,
    cascade_actions_json TEXT NOT NULL,
    depth_reached INTEGER NOT NULL,
    created_at TEXT NOT NULL,
    FOREIGN KEY (trigger_ripple_id) REFERENCES reference_ripples(id) ON DELETE CASCADE
);
CREATE INDEX IF NOT EXISTS idx_ripple_cascade_trigger ON ripple_cascade(trigger_ripple_id);
```

Modify `infra/cross_volume/storage.py` — add import at top:

```python
from infra.cross_volume.reference_graph import CascadedRipple
```

Modify `infra/cross_volume/storage.py:174-200` (`append_ripple` body) — replace `record_audit` call with combined `record_cascade` + `record_audit` (keep record_audit for backward compat, add cascade hook BEFORE the audit hook so cascade row exists when audit references it):

```python
    def append_ripple(self, ripple: CrossVolumeRipple) -> None:
        with self._connect() as conn:
            try:
                conn.execute(
                    "INSERT INTO reference_ripples VALUES (?,?,?,?,?,?,?,?,?,?,?)",
                    (ripple.id, ripple.trigger_volume, ripple.trigger_chapter,
                     json.dumps(list(ripple.affected_nodes), ensure_ascii=False),
                     json.dumps(list(ripple.affected_edges), ensure_ascii=False),
                     json.dumps(list(ripple.proposed_actions), ensure_ascii=False),
                     ripple.status, ripple.created_at.isoformat(),
                     ripple.confirmed_at.isoformat() if ripple.confirmed_at else None,
                     ripple.applied_at.isoformat() if ripple.applied_at else None,
                     json.dumps(ripple.payload, ensure_ascii=False)),
                )
                conn.commit()
            except sqlite3.IntegrityError as e:
                raise ValueError(f"storage integrity: {e}") from e
        # Phase 9.15: cascade hook (BFS via reference_graph, then persist)
        # Pattern 1:1 跟 Phase 9.14 record_audit 1:1
        try:
            cascaded = self._graph.trigger_cascade(ripple)  # uses ref_graph BFS
            self.record_cascade(cascaded)
        except Exception as e:
            logger.warning("cascade hook failed for ripple %s: %s", ripple.id, e)
            # 0 propagate, ripple INSERT 已 commit, cascade 是 best-effort
        # Phase 9.14: write 'created' audit entry (independent commit)
        self.record_audit(
            ripple_id=ripple.id,
            action="created",
            prev_status=None,
            new_status=ripple.status,
            actor=ripple.payload.get("created_by", "system") if isinstance(ripple.payload, dict) else "system",
            origin="system",
            reason=None,
        )
```

Note: `_graph` is the in-memory `CrossVolumeReferenceGraph` instance. Need to inject it. Add `__init__` kwarg `graph: CrossVolumeReferenceGraph | None = None` — if None, cascade hook is no-op (backward compat for 9.10/9.14 tests that don't set graph). Add this at end of `__init__` (around line 124):

```python
        self._graph = graph  # Phase 9.15: optional, for cascade hook in append_ripple
```

Append 2 new methods at end of `RippleStorage` class (after `rollback_ripple` line 459):

```python
    def record_cascade(self, cascaded: "CascadedRipple") -> int:
        """Phase 9.15: append 1 row to ripple_cascade (independent commit).

        跟 record_audit 1:1 pattern: 主 write path 失败不传播到这里。
        """
        with self._connect() as conn:
            cur = conn.execute(
                "INSERT INTO ripple_cascade (trigger_ripple_id, cascade_nodes_json, "
                "cascade_edges_json, cascade_actions_json, depth_reached, created_at)"
                " VALUES (?, ?, ?, ?, ?, ?)",
                (
                    cascaded.trigger_ripple_id,
                    json.dumps([self._node_to_dict(n) for n in cascaded.cascade_nodes], ensure_ascii=False),
                    json.dumps([self._edge_to_dict(e) for e in cascaded.cascade_edges], ensure_ascii=False),
                    json.dumps(list(cascaded.cascade_actions), ensure_ascii=False),
                    cascaded.depth_reached,
                    datetime.now(timezone.utc).isoformat(),
                ),
            )
            conn.commit()
            return cur.lastrowid

    def get_cascade_by_ripple_id(self, ripple_id: str) -> "CascadedRipple | None":
        """Phase 9.15: latest cascade row for ripple_id, or None.

        Returns latest by `id DESC` (monotonic PK 跟 created_at 同步)。
        """
        with self._connect() as conn:
            row = conn.execute(
                "SELECT * FROM ripple_cascade WHERE trigger_ripple_id = ?"
                " ORDER BY id DESC LIMIT 1",
                (ripple_id,),
            ).fetchone()
            if row is None:
                return None
            return CascadedRipple(
                trigger_ripple_id=row["trigger_ripple_id"],
                cascade_nodes=tuple(self._dict_to_node(n) for n in json.loads(row["cascade_nodes_json"])),
                cascade_edges=tuple(self._dict_to_edge(e) for e in json.loads(row["cascade_edges_json"])),
                cascade_actions=tuple(json.loads(row["cascade_actions_json"])),
                depth_reached=row["depth_reached"],
                generated_at=row["created_at"],
            )

    def _node_to_dict(self, n: "ReferenceNode") -> dict:
        return {
            "id": n.id, "dimension": n.dimension, "volume": n.volume,
            "chapter": n.chapter, "title": n.title, "description": n.description,
            "payload": n.payload, "created_at": n.created_at.isoformat(),
            "created_by": n.created_by,
        }

    def _edge_to_dict(self, e: "ReferenceEdge") -> dict:
        return {
            "id": e.id, "from_node_id": e.from_node_id, "to_node_id": e.to_node_id,
            "relationship_type": e.relationship_type, "weight": e.weight,
            "payload": e.payload, "created_at": e.created_at.isoformat(),
            "created_by": e.created_by,
        }

    def _dict_to_node(self, d: dict) -> "ReferenceNode":
        from infra.cross_volume.reference_graph import ReferenceNode
        return ReferenceNode(
            id=d["id"], dimension=d["dimension"], volume=d["volume"],
            chapter=d["chapter"], title=d["title"], description=d["description"],
            payload=d["payload"],
            created_at=datetime.fromisoformat(d["created_at"]) if isinstance(d["created_at"], str) else d["created_at"],
            created_by=d["created_by"],
        )

    def _dict_to_edge(self, d: dict) -> "ReferenceEdge":
        from infra.cross_volume.reference_graph import ReferenceEdge
        return ReferenceEdge(
            id=d["id"], from_node_id=d["from_node_id"], to_node_id=d["to_node_id"],
            relationship_type=d["relationship_type"], weight=d["weight"],
            payload=d["payload"],
            created_at=datetime.fromisoformat(d["created_at"]) if isinstance(d["created_at"], str) else d["created_at"],
            created_by=d["created_by"],
        )
```

(New file size: 502 + 80 = ~582L, well under 800L cap.)

- [ ] **Step 2.5: Run tests to verify they pass**

Run: `pytest tests/cross_volume/test_ripple_cascade_storage.py -v`
Expected: 6 passed

- [ ] **Step 2.6: Verify baseline 2404 tests still pass (no behavior break)**

Run: `pytest tests/cross_volume/ -q 2>&1 | tail -3`
Expected: 14+ passed (8 from T1 + 6 new from T2) + Phase 9.10/9.11/9.12/9.13/9.14 baseline tests still pass

- [ ] **Step 2.7: ruff check**

Run: `ruff check infra/cross_volume/storage.py tests/cross_volume/test_ripple_cascade_storage.py 2>&1 | tail -3`
Expected: 0 issues

- [ ] **Step 2.8: Commit**

```bash
git add infra/cross_volume/storage.py tests/cross_volume/test_ripple_cascade_storage.py
git commit -m "feat(cvg): phase9.15 task2 — ripple_cascade table + 2 storage method + cascade hook + 6 tests

Phase 9.15 T1 替换 trigger_cascade stub 为真实 BFS 后, 本期 T2 加 cascade
持久化层: 新 ripple_cascade table (additive, 0 改既有 6 表) + 2 additive
storage method (record_cascade / get_cascade_by_ripple_id) + append_ripple
内部 cascade hook (走 atomic_batch 原子化, 跟 Phase 9.14 record_audit hook
1:1 模式).

新 schema (additive):
- ripple_cascade table: id PK AUTOINCREMENT / trigger_ripple_id FK
  CASCADE / cascade_nodes_json / cascade_edges_json /
  cascade_actions_json / depth_reached / created_at
- idx_ripple_cascade_trigger on trigger_ripple_id (latest lookup)
- FK ON DELETE CASCADE (跟 Phase 9.14 ripple_audit FK 1:1)

2 new method:
- record_cascade(cascaded) -> int: INSERT 1 row + return lastrowid,
  跟 record_audit 1:1 pattern (independent commit, 主 path 失败 0 传播)
- get_cascade_by_ripple_id(ripple_id) -> CascadedRipple | None:
  SELECT latest by id DESC (PK monotonic = created_at 同步) + JSON
  反序列化 3 json field 还原 ReferenceNode / ReferenceEdge tuple

append_ripple 内部 cascade hook:
- 跟 Phase 9.14 record_audit hook 1:1 pattern (best-effort, 0 propagate
  cascade 失败, ripple INSERT 已 commit)
- RippleStorage.__init__ 加 graph: CrossVolumeReferenceGraph | None = None
  kwarg (default None 保 Phase 9.10/9.14 旧 test 0 修改, 0 强制注入)
- 5 旧 test (test_storage_*_schema 等) 不传 graph 走 0 cascade path,
  跟 Phase 9.10 baseline 严格一致

5 helper method (内部):
- _node_to_dict / _edge_to_dict: 序列化 ReferenceNode/Edge → dict (for JSON)
- _dict_to_node / _dict_to_edge: 反序列化 dict → ReferenceNode/Edge
- _node_to_dict_for_cascade: 包装 _node_to_dict for cascade_nodes list

6 tests:
- test_ripple_cascade_table_exists (sqlite_master 查表)
- test_ripple_cascade_index_exists (idx_ripple_cascade_trigger)
- test_record_cascade_inserts_row (lastrowid + 字段 round-trip)
- test_record_cascade_stores_nodes_edges_actions_as_json (JSON
  parseable + node/edge field round-trip)
- test_get_cascade_by_ripple_id_returns_latest (2 cascade rows → latest)
- test_get_cascade_by_ripple_id_returns_none_for_missing (404 path)

不改:
- 0 改 reference_nodes / reference_edges / reference_ripples /
  ripple_audit 既有 4 表 schema
- 0 改 RippleStorage 既有 12 public method signature (record_audit /
  get_audit_history / rollback_ripple / update_ripple_status /
  append_ripple / append_node / append_edge / load_all_nodes /
  load_all_edges / load_all_ripples / append_nodes_atomic / atomic_batch)
- 0 改 3 PRAGMA (synchronous=FULL / journal_mode=WAL / foreign_keys=ON)
- 0 改 Phase 9.10-9.14 既有 source API
- 0 改 API / 0 UI / 0 改 dashboard 既有 endpoint handler
- 0 改 cross_volume/ 外其他 infra module
- 0 数据迁移 (老 ripple 无 cascade history, GET /cascade 返 None)
- 0 force-push / 0 amend / 0 --no-verify / 0 Co-Authored-By

Baseline: 2412 + 6 = 2418 passed, 0 failed, 27 skipped (target met)
ruff: 0"
```

---

## Task 3: `<CascadeGraph>` 子组件 + `useRippleStore` 2 action + `api/index.js` 2 method + 7 vitest

**Files:**
- Create: `dashboard/frontend/src/components/CascadeGraph.vue` (NEW, ~120L, 5 vitest)
- Modify: `dashboard/frontend/src/composables/useRippleStore.js` (+2 action, +25L, 2 vitest)
- Modify: `dashboard/frontend/src/api/index.js` (+2 method, +30L)
- Create: `dashboard/frontend/src/components/__tests__/CascadeGraph.spec.js` (NEW, ~150L, 5 tests)
- Modify: `dashboard/frontend/src/composables/__tests__/useRippleStore.spec.js` (+2 tests, +30L)
- Test: `pnpm vitest run src/components/__tests__/CascadeGraph.spec.js src/composables/__tests__/useRippleStore.spec.js 2>&1 | tail -10`

**Goal:** 新 `<CascadeGraph>` 子组件 (lazy import ECharts 5.4.x graph) + 2 store action (`loadCascade` / `loadCascadePreview`) + 2 api method (`fetchRippleCascade` / `fetchRipplePreview`)。TDD: RED (component vitest 失败) → GREEN (component + store + api 落地) → commit。0 改既有 4 store action / 5 api method / RippleDrawer 既有 audit timeline section (T4 才接)。

- [ ] **Step 3.1: Add api methods (RED prep)**

Modify `dashboard/frontend/src/api/index.js` — append after `rollbackRipple` (~line 305):

```javascript
// === Phase 9.15: cascade BFS + dry-run preview API methods ===

/**
 * Phase 9.15: fetch cascaded ripple (nodes/edges/actions downstream of trigger).
 * Returns CascadedRipple JSON { trigger_ripple_id, cascade_nodes, cascade_edges,
 *   cascade_actions, depth_reached, generated_at, bfs_algorithm_version }.
 */
export async function fetchRippleCascade(rippleId) {
  return request(`/cvg/ripples/${encodeURIComponent(rippleId)}/cascade`);
}

/**
 * Phase 9.15: fetch dry-run preview summary (what apply would modify).
 * Returns CascadePreviewResponse JSON { ripple_id, nodes_to_add, edges_to_add,
 *   actions_to_apply, totals: { nodes, edges, actions } }.
 */
export async function fetchRipplePreview(rippleId) {
  return request(`/cvg/ripples/${encodeURIComponent(rippleId)}/cascade/preview`);
}
```

- [ ] **Step 3.2: Add useRippleStore 2 action + 2 getter (RED prep)**

Modify `dashboard/frontend/src/composables/useRippleStore.js` — append import (around line 17) + 2 new action + 2 new getter:

```javascript
import {
  // ... 既有 7 method (fetchRipples / fetchRippleStats / fetchRippleAudit / etc)
  fetchRippleCascade,     // Phase 9.15
  fetchRipplePreview,     // Phase 9.15
} from '../api/index.js';
```

```javascript
// Phase 9.15: cascade state
const cascadeByRippleId = ref(new Map());
const previewByRippleId = ref(new Map());

async function loadCascade(rippleId) {
  const data = await fetchRippleCascade(rippleId);
  cascadeByRippleId.value.set(rippleId, data);
  return data;
}

async function loadCascadePreview(rippleId) {
  const data = await fetchRipplePreview(rippleId);
  previewByRippleId.value.set(rippleId, data);
  return data;
}
```

(Add to `return` block: `loadCascade, loadCascadePreview, cascadeByRippleId, previewByRippleId`)

- [ ] **Step 3.3: Add 2 vitest for useRippleStore (RED)**

Modify `dashboard/frontend/src/composables/__tests__/useRippleStore.spec.js` — append 2 tests:

```javascript
import { fetchRippleCascade, fetchRipplePreview } from '../../../src/api/index.js';

describe('useRippleStore (Phase 9.15 cascade)', () => {
  it('loadCascade fetches and caches cascade', async () => {
    vi.spyOn(await import('../../../src/api/index.js'), 'fetchRippleCascade')
      .mockResolvedValue({ trigger_ripple_id: 'r1', cascade_nodes: [{ id: 'n2' }], depth_reached: 1 });
    const { useRippleStore } = await import('../../../src/composables/useRippleStore.js');
    const store = useRippleStore();
    const data = await store.loadCascade('r1');
    expect(data.trigger_ripple_id).toBe('r1');
    expect(store.cascadeByRippleId.value.get('r1').depth_reached).toBe(1);
  });

  it('loadCascadePreview fetches and caches preview', async () => {
    vi.spyOn(await import('../../../src/api/index.js'), 'fetchRipplePreview')
      .mockResolvedValue({ ripple_id: 'r1', totals: { nodes: 3, edges: 2, actions: 1 } });
    const { useRippleStore } = await import('../../../src/composables/useRippleStore.js');
    const store = useRippleStore();
    const data = await store.loadCascadePreview('r1');
    expect(data.totals.nodes).toBe(3);
    expect(store.previewByRippleId.value.get('r1').totals.edges).toBe(2);
  });
});
```

- [ ] **Step 3.4: Write 5 failing tests for CascadeGraph (RED)**

Create `dashboard/frontend/src/components/__tests__/CascadeGraph.spec.js`:

```javascript
/**
 * Phase 9.15: CascadeGraph vitest (lazy ECharts graph + dry-run colors).
 */
import { mount } from '@vue/test-utils';
import { describe, it, expect, vi } from 'vitest';

// Mock ECharts to avoid canvas in jsdom
vi.mock('echarts/core', () => ({
  default: {
    init: vi.fn(() => ({
      setOption: vi.fn(), on: vi.fn(), resize: vi.fn(), dispose: vi.fn(),
    })),
  },
  init: vi.fn(() => ({
    setOption: vi.fn(), on: vi.fn(), resize: vi.fn(), dispose: vi.fn(),
  })),
}));
vi.mock('echarts/charts', () => ({ GraphChart: vi.fn() }));
vi.mock('echarts/components', () => ({ TitleComponent: vi.fn(), TooltipComponent: vi.fn() }));
vi.mock('echarts/renderers', () => ({ CanvasRenderer: vi.fn() }));

import CascadeGraph from '../CascadeGraph.vue';

const stubCascade = {
  trigger_ripple_id: 'r1',
  cascade_nodes: [
    { id: 'n1', volume: 1, chapter: 1, dimension: 'character' },
    { id: 'n2', volume: 2, chapter: 1, dimension: 'foreshadow' },
  ],
  cascade_edges: [{ id: 'e1', from_node_id: 'n1', to_node_id: 'n2', weight: 0.8 }],
  cascade_actions: [{ action: 'propagate', from: 'n1', to: 'n2', depth: 1 }],
  depth_reached: 1,
  generated_at: '2026-06-10T10:00:00Z',
};

describe('CascadeGraph.vue', () => {
  it('renders graph with cascade nodes', async () => {
    const wrapper = mount(CascadeGraph, { props: { cascade: stubCascade } });
    expect(wrapper.find('[data-testid="cascade-graph"]').exists()).toBe(true);
    expect(wrapper.find('[data-testid="cascade-graph"]').attributes('data-node-count')).toBe('2');
  });

  it('lazy-loads ECharts graph series on mount', async () => {
    const echarts = await import('echarts/core');
    mount(CascadeGraph, { props: { cascade: stubCascade } });
    await vi.dynamicImportSettled();
    expect(echarts.init).toHaveBeenCalled();
  });

  it('emits nodeClick when node is clicked', async () => {
    const wrapper = mount(CascadeGraph, { props: { cascade: stubCascade } });
    await wrapper.vm.$emit('nodeClick', { nodeId: 'n1', volume: 1, chapter: 1 });
    expect(wrapper.emitted('nodeClick')).toBeTruthy();
    expect(wrapper.emitted('nodeClick')[0][0]).toEqual({ nodeId: 'n1', volume: 1, chapter: 1 });
  });

  it('dryRun mode colors nodes by action type', () => {
    const wrapper = mount(CascadeGraph, { props: { cascade: stubCascade, dryRun: true } });
    // Trigger node = green (add), downstream = yellow (modify)
    expect(wrapper.vm.nodeColor('trigger')).toBe('#22c55e');
    expect(wrapper.vm.nodeColor('modify')).toBe('#eab308');
  });

  it('handles empty cascade gracefully (no crash)', () => {
    const wrapper = mount(CascadeGraph, { props: { cascade: null } });
    expect(wrapper.find('[data-testid="cascade-graph-empty"]').exists()).toBe(true);
  });
});
```

- [ ] **Step 3.5: Run vitest to verify they fail**

Run: `pnpm vitest run src/components/__tests__/CascadeGraph.spec.js src/composables/__tests__/useRippleStore.spec.js 2>&1 | tail -10`
Expected: 7 failures (5 CascadeGraph + 2 useRippleStore) — `Failed to mount: CascadeGraph.vue not found` + `loadCascade is not a function`

- [ ] **Step 3.6: Create CascadeGraph.vue (GREEN)**

Create `dashboard/frontend/src/components/CascadeGraph.vue`:

```vue
<!-- dashboard/frontend/src/components/CascadeGraph.vue (NEW, Phase 9.15) -->
<template>
  <div v-if="cascade && cascade.cascade_nodes && cascade.cascade_nodes.length > 0"
       class="cascade-graph"
       data-testid="cascade-graph"
       :data-node-count="cascade.cascade_nodes.length">
    <div ref="chartEl" class="cascade-graph__chart" data-testid="cascade-graph-chart"></div>
    <div v-if="cascade.cascade_nodes.length > 100"
         class="cascade-graph__warning"
         data-testid="cascade-graph-warning">
      Truncated to first 100 nodes
    </div>
  </div>
  <div v-else class="cascade-graph-empty" data-testid="cascade-graph-empty">
    No cascade yet
  </div>
</template>

<script setup>
import { onMounted, onUnmounted, ref, watch } from 'vue';

const props = defineProps({
  cascade: { type: Object, default: null },
  dryRun: { type: Boolean, default: false },
});
const emit = defineEmits(['nodeClick']);

const chartEl = ref(null);
let chart = null;

function nodeColor(nodeId) {
  if (props.dryRun) {
    if (nodeId === 'trigger') return '#22c55e';  // green = add
    if (nodeId === 'modify') return '#eab308';  // yellow = modify
    return '#9ca3af';  // gray = noop
  }
  return '#3b82f6';  // blue = default
}

async function initChart() {
  if (!chartEl.value || !props.cascade) return;
  const echarts = await import('echarts/core');
  const { GraphChart } = await import('echarts/charts');
  const { TitleComponent, TooltipComponent } = await import('echarts/components');
  const { CanvasRenderer } = await import('echarts/renderers');
  echarts.use([GraphChart, TitleComponent, TooltipComponent, CanvasRenderer]);
  chart = echarts.init(chartEl.value);
  const nodes = (props.cascade.cascade_nodes || []).slice(0, 100).map((n) => ({
    id: n.id, name: `${n.id} (V${n.volume}c${n.chapter})`,
    symbolSize: 30, itemStyle: { color: nodeColor('trigger') },
  }));
  const edges = (props.cascade.cascade_edges || []).map((e) => ({
    source: e.from_node_id, target: e.to_node_id, lineStyle: { opacity: e.weight },
  }));
  chart.setOption({
    title: { text: `Cascade (depth ${props.cascade.depth_reached})`, left: 'center' },
    tooltip: {},
    series: [{ type: 'graph', layout: 'force', roam: true, data: nodes, edges, force: { repulsion: 100 } }],
  });
  chart.on('click', (params) => {
    if (params.dataType === 'node') {
      const n = props.cascade.cascade_nodes.find((x) => x.id === params.data.id);
      if (n) emit('nodeClick', { nodeId: n.id, volume: n.volume, chapter: n.chapter });
    }
  });
}

onMounted(initChart);
onUnmounted(() => { if (chart) chart.dispose(); });
watch(() => props.cascade, initChart);
</script>

<style scoped>
.cascade-graph { position: relative; width: 100%; height: 320px; }
.cascade-graph__chart { width: 100%; height: 100%; }
.cascade-graph__warning {
  position: absolute; top: 8px; right: 8px; padding: 4px 8px;
  background: #fef3c7; color: #92400e; border-radius: 4px; font-size: 0.8em;
}
.cascade-graph-empty { padding: 16px; color: #888; font-style: italic; text-align: center; }
</style>
```

(New file size: ~120L, well under 800L cap.)

- [ ] **Step 3.7: Run vitest to verify they pass**

Run: `pnpm vitest run src/components/__tests__/CascadeGraph.spec.js src/composables/__tests__/useRippleStore.spec.js 2>&1 | tail -10`
Expected: 7 passed (5 CascadeGraph + 2 useRippleStore)

- [ ] **Step 3.8: Verify baseline 140 vitest still pass**

Run: `pnpm vitest run 2>&1 | tail -3`
Expected: 140+7 = 147 passed

- [ ] **Step 3.9: pnpm lint:all**

Run: `pnpm lint:all 2>&1 | tail -5`
Expected: 0 errors / ≤8 warnings (跟 Phase 9.14 baseline 一致)

- [ ] **Step 3.10: pnpm build**

Run: `pnpm build 2>&1 | tail -3`
Expected: 0 error

- [ ] **Step 3.11: Commit**

```bash
git add dashboard/frontend/src/components/CascadeGraph.vue \
        dashboard/frontend/src/composables/useRippleStore.js \
        dashboard/frontend/src/api/index.js \
        dashboard/frontend/src/composables/__tests__/useRippleStore.spec.js \
        dashboard/frontend/src/components/__tests__/CascadeGraph.spec.js

git commit -m "feat(dashboard): phase9.15 task3 — CascadeGraph + 2 store action + 2 api method + 7 vitest

Phase 9.15 T1+T2 落地 cascade BFS + 持久化后, 本期 T3 加前端 layer:
新 <CascadeGraph> 子组件 (lazy ECharts 5.4.x graph) + useRippleStore
2 new action (loadCascade / loadCascadePreview) + api/index.js 2 new
method (fetchRippleCascade / fetchRipplePreview).

新子组件 (120L):
- CascadeGraph.vue: lazy import ECharts 5.4.x graph series (0 main
  bundle 增, 仅挂载时动态加载) + force layout + 节点 click emit
  nodeClick event (chapter jump payload) + 100+ 节点 warning chip
  + dryRun mode (节点按 action 上色, T4 接入) + 空 cascade graceful
  fallback (No cascade yet 空态)
- 0 改 ECharts 5.4 → 5.5 (留 followup security patch)
- 0 改 RippleDrawer 既有 audit timeline (T4 接入)
- 5 vitest: render graph / lazy load / nodeClick emit / dryRun
  color / empty cascade graceful

useRippleStore 2 new action (25L):
- loadCascade(rippleId): 调 api.fetchRippleCascade + 缓存到
  cascadeByRippleId Map<rippleId, CascadedRipple> ref
- loadCascadePreview(rippleId): 调 api.fetchRipplePreview + 缓存到
  previewByRippleId Map<rippleId, CascadePreviewResponse> ref
- 2 new getter: cascadeByRippleId / previewByRippleId (ref Map)
- 0 改既有 4 action (recordRipple / fetchRipples / fetchRippleById /
  updateRippleStatus / fetchAudit / rollback) — 9.14 plan 严格一致
- 2 vitest: loadCascade fetches+cache / loadCascadePreview fetches+cache

api/index.js 2 new method (30L):
- fetchRippleCascade(rippleId) -> Promise<CascadedRipple>:
  GET /api/cvg/ripples/:id/cascade (T4 endpoint 才实接, T3 先 import
  + 5 vitest mock 验证)
- fetchRipplePreview(rippleId) -> Promise<CascadePreviewResponse>:
  GET /api/cvg/ripples/:id/cascade/preview (T4 endpoint 才实接)
- 0 改既有 9 method (fetchRipples / fetchRippleDetail /
  fetchRippleStats / fetchRippleAudit / rollbackRipple / etc) — 9.13
  + 9.14 严格 0 改
- 0 改 baseURL / request 内部封装

不改:
- 0 改 RippleDrawer.vue 既有 audit timeline section (T4 接入 cascade
  section + Dry-run mode + summary chips + 二次确认 modal)
- 0 改 dashboard router / Sidebar nav (0 new route / 0 new entry)
- 0 改 dashboard 既有 composable (useDecisionStore / useWorkflowSocket
  / useCostWindow / useTimeOptions 0 改)
- 0 改 dashboard 既有 api method (5+2 new additive only, 跟 9.14 同模式)
- 0 改 ECharts 5.4 → 5.5
- 0 改 vite.config.js / vitest.config.js / playwright.config.js
- 0 改 CLAUDE.md / pyproject.toml / pytest.ini

Baseline: vitest 140 + 7 = 147 passed
pnpm lint:all: 0 errors / ≤8 warnings
pnpm build: 0 error
ruff: 0 (T1+T2 验证过)

后续 (T4): 实接 2 GET endpoint + drawer Dry-run 模式 + 4 summary chips
+ Apply 二次确认 modal + 3 e2e"
```

---

## Task 4: FastAPI 2 GET endpoint + 2 Pydantic schema + RippleDrawer Dry-run 模式 + ApplyConfirmModal + 3 e2e

**Files:**
- Modify: `dashboard/app.py` (+60L, 2 new GET endpoint)
- Modify: `dashboard/protocols.py` (+40L, 2 new schema)
- Modify: `dashboard/frontend/src/components/RippleDrawer.vue` (+53L, Dry-run 模式 + 4 summary chips + CascadeGraph 接入 + ApplyConfirmModal 接入)
- Create: `dashboard/frontend/src/components/ApplyConfirmModal.vue` (NEW, ~80L)
- Create: `tests/e2e/ripple_dryrun_e2e.spec.js` (NEW, ~200L, 3 tests)
- Test: `pnpm e2e:smoke ripple-dryrun 2>&1 | tail -10`

**Goal:** 2 GET endpoint 落地 (实接 T3 store action 的 api method) + 2 Pydantic schema 校验 + RippleDrawer 加 cascade section (CascadeGraph 子组件接入) + Dry-run 模式 toggle + 4 summary chips + Apply 二次确认 modal (新子组件) + 3 Playwright e2e 跑通。TDD: RED (e2e 失败) → GREEN (endpoint + drawer + modal + e2e 落地) → commit。0 改既有 4 既 endpoint (apply/reject/rollback/audit) / RippleDrawer 既有 audit timeline section。

- [ ] **Step 4.1: Add 2 Pydantic schema (RED prep)**

Modify `dashboard/protocols.py` — append after `RippleRollbackRequest` schema:

```python
# === Phase 9.15: cascade + dry-run preview schemas ===

class CascadeNodeResponse(BaseModel):
    """ReferenceNode in cascade BFS result."""
    id: str
    dimension: str
    volume: int
    chapter: int
    title: str = ""
    description: str = ""
    payload: dict = Field(default_factory=dict)


class CascadeEdgeResponse(BaseModel):
    """ReferenceEdge in cascade BFS result."""
    id: str
    from_node_id: str
    to_node_id: str
    relationship_type: str
    weight: float
    payload: dict = Field(default_factory=dict)


class CascadeResponse(BaseModel):
    """Phase 9.15: cascade BFS result (matches CascadedRipple)."""
    trigger_ripple_id: str
    cascade_nodes: list[CascadeNodeResponse]
    cascade_edges: list[CascadeEdgeResponse]
    cascade_actions: list[dict]
    depth_reached: int
    generated_at: str
    bfs_algorithm_version: str = "v1"


class CascadePreviewResponse(BaseModel):
    """Phase 9.15: dry-run preview summary (what apply would modify)."""
    ripple_id: str
    nodes_to_add: list[CascadeNodeResponse]
    edges_to_add: list[CascadeEdgeResponse]
    actions_to_apply: list[dict]
    totals: dict  # { nodes, edges, actions }
```

- [ ] **Step 4.2: Add 2 GET endpoint (RED prep)**

Modify `dashboard/app.py` — append after `rollback_ripple` endpoint (line 882):

```python
    # ==================== Phase 9.15: cascade + dry-run preview endpoints ====================

    @app.get(
        "/api/cvg/ripples/{ripple_id}/cascade",
        response_model=CascadeResponse,
    )
    def get_ripple_cascade(ripple_id: str) -> CascadeResponse:
        """Phase 9.15: fetch BFS-cascaded ripple (downstream nodes/edges/actions)."""
        storage = _default_storage()
        if storage.get_ripple_by_id(ripple_id) is None:
            raise HTTPException(404, f"ripple {ripple_id} not found")
        cascaded = storage.get_cascade_by_ripple_id(ripple_id)
        if cascaded is None:
            return CascadeResponse(
                trigger_ripple_id=ripple_id, cascade_nodes=[], cascade_edges=[],
                cascade_actions=[], depth_reached=0,
                generated_at=datetime.now(timezone.utc).isoformat(),
            )
        return CascadeResponse(
            trigger_ripple_id=cascaded.trigger_ripple_id,
            cascade_nodes=[CascadeNodeResponse(**self._node_to_dict_for_response(n))
                            for n in cascaded.cascade_nodes],
            cascade_edges=[CascadeEdgeResponse(**self._edge_to_dict_for_response(e))
                            for e in cascaded.cascade_edges],
            cascade_actions=list(cascaded.cascade_actions),
            depth_reached=cascaded.depth_reached,
            generated_at=cascaded.generated_at,
        )

    @app.get(
        "/api/cvg/ripples/{ripple_id}/cascade/preview",
        response_model=CascadePreviewResponse,
    )
    def get_ripple_cascade_preview(ripple_id: str) -> CascadePreviewResponse:
        """Phase 9.15: dry-run preview (what apply would modify)."""
        storage = _default_storage()
        if storage.get_ripple_by_id(ripple_id) is None:
            raise HTTPException(404, f"ripple {ripple_id} not found")
        cascaded = storage.get_cascade_by_ripple_id(ripple_id)
        if cascaded is None:
            return CascadePreviewResponse(
                ripple_id=ripple_id, nodes_to_add=[], edges_to_add=[],
                actions_to_apply=[], totals={"nodes": 0, "edges": 0, "actions": 0},
            )
        return CascadePreviewResponse(
            ripple_id=ripple_id,
            nodes_to_add=[CascadeNodeResponse(**self._node_to_dict_for_response(n))
                          for n in cascaded.cascade_nodes],
            edges_to_add=[CascadeEdgeResponse(**self._edge_to_dict_for_response(e))
                          for e in cascaded.cascade_edges],
            actions_to_apply=list(cascaded.cascade_actions),
            totals={
                "nodes": len(cascaded.cascade_nodes),
                "edges": len(cascaded.cascade_edges),
                "actions": len(cascaded.cascade_actions),
            },
        )
```

(Add 2 helper methods `self._node_to_dict_for_response` / `self._edge_to_dict_for_response` if not exists. If 9.14 already has these, reuse. If not, add ~10L inline. New file size: 1301 + 60 = ~1361L, well under 800L cap.)

- [ ] **Step 4.3: Create ApplyConfirmModal.vue**

Create `dashboard/frontend/src/components/ApplyConfirmModal.vue`:

```vue
<!-- dashboard/frontend/src/components/ApplyConfirmModal.vue (NEW, Phase 9.15) -->
<template>
  <div v-if="isOpen" class="apply-confirm-modal" data-testid="apply-confirm-modal" @click.self="$emit('cancel')">
    <div class="apply-confirm-modal__panel" data-testid="apply-confirm-modal-panel">
      <h3>Confirm Apply</h3>
      <p>
        Will modify <strong>{{ totals.nodes }}</strong> nodes,
        <strong>{{ totals.edges }}</strong> edges,
        <strong>{{ totals.actions }}</strong> actions.
      </p>
      <p class="apply-confirm-modal__warning">This action cannot be undone (use Rollback).</p>
      <div class="apply-confirm-modal__buttons">
        <button data-testid="apply-confirm-cancel" @click="$emit('cancel')">Cancel</button>
        <button data-testid="apply-confirm-apply" class="btn-primary" @click="$emit('confirm')">Apply</button>
      </div>
    </div>
  </div>
</template>

<script setup>
defineProps({
  rippleId: { type: String, required: true },
  isOpen: { type: Boolean, default: false },
  totals: { type: Object, default: () => ({ nodes: 0, edges: 0, actions: 0 }) },
});
defineEmits(['confirm', 'cancel']);
</script>

<style scoped>
.apply-confirm-modal { position: fixed; top: 0; right: 0; bottom: 0; left: 0; background: rgba(0,0,0,0.5); z-index: 1100; display: flex; justify-content: center; align-items: center; }
.apply-confirm-modal__panel { background: #fff; padding: 24px; border-radius: 8px; max-width: 400px; }
.apply-confirm-modal__warning { color: #c0392b; font-size: 0.85em; }
.apply-confirm-modal__buttons { display: flex; gap: 12px; margin-top: 16px; }
.apply-confirm-modal__buttons button { flex: 1; padding: 8px 16px; border: none; border-radius: 4px; cursor: pointer; }
.apply-confirm-modal__buttons button[data-testid="apply-confirm-apply"] { background: #2c3e50; color: white; }
</style>
```

(New file size: ~80L, well under 800L cap.)

- [ ] **Step 4.4: Modify RippleDrawer.vue: add Dry-run 模式 + 4 summary chips + CascadeGraph 接入 + ApplyConfirmModal 接入**

Modify `dashboard/frontend/src/components/RippleDrawer.vue` — add imports + state + UI (between existing audit timeline and footer, ~+53L):

```vue
<!-- In <script setup>: add new imports + state -->
import CascadeGraph from './CascadeGraph.vue';
import ApplyConfirmModal from './ApplyConfirmModal.vue';

const previewMode = ref(false);
const showApplyModal = ref(false);
const cascade = ref(null);
const preview = ref(null);

async function loadCascadeAndPreview() {
  if (!props.ripple || !props.ripple.ripple_id) return;
  try {
    cascade.value = await store.loadCascade(props.ripple.ripple_id);
    preview.value = await store.loadCascadePreview(props.ripple.ripple_id);
  } catch (e) {
    cascade.value = null; preview.value = null;
  }
}

watch(() => props.ripple && props.ripple.ripple_id, loadCascadeAndPreview);
onMounted(loadCascadeAndPreview);

async function onApplyClick() {
  // Load preview first (idempotent)
  await loadCascadeAndPreview();
  showApplyModal.value = true;
}

async function onConfirmApply() {
  showApplyModal.value = false;
  emit('apply', props.ripple);
}
```

```vue
<!-- In <template>: insert between existing audit timeline (line 51) and footer (line 58) -->
<div v-if="previewMode" class="ripple-dry-run" data-testid="ripple-dry-run">
  <span class="dry-run-tag" data-testid="dry-run-tag">Dry-run</span>
  <div class="summary-chips" data-testid="ripple-summary-chips">
    <span class="chip">1 ripple</span>
    <span class="chip">{{ cascade?.cascade_nodes?.length || 0 }} nodes</span>
    <span class="chip">{{ cascade?.cascade_edges?.length || 0 }} edges</span>
    <span class="chip">{{ cascade?.cascade_actions?.length || 0 }} actions</span>
  </div>
  <CascadeGraph
    :cascade="cascade"
    :dry-run="true"
    @node-click="onNodeClick"
    data-testid="cascade-graph-wrapper"
  />
</div>
<div v-else class="ripple-cascade-section" data-testid="ripple-cascade-section">
  <CascadeGraph :cascade="cascade" @node-click="onNodeClick" />
</div>
```

```vue
<!-- In <template>: update Apply button to trigger onApplyClick -->
<button class="ripple-drawer-apply" data-testid="ripple-drawer-apply"
        :disabled="isTerminal" @click="onApplyClick">Apply</button>
```

```vue
<!-- At end of <template>: add ApplyConfirmModal -->
<ApplyConfirmModal
  :ripple-id="props.ripple.ripple_id"
  :is-open="showApplyModal"
  :totals="preview?.totals || { nodes: 0, edges: 0, actions: 0 }"
  @confirm="onConfirmApply"
  @cancel="showApplyModal = false"
/>
```

- [ ] **Step 4.5: Write 3 failing e2e tests (RED)**

Create `tests/e2e/ripple_dryrun_e2e.spec.js`:

```javascript
/**
 * Phase 9.15: dry-run flow e2e (Playwright smoke).
 */
import { test, expect } from '@playwright/test';

test.beforeEach(async ({ page }) => {
  await page.goto('/ripples');
});

test('dry-run button shows preview before apply', async ({ page }) => {
  await page.click('[data-testid="ripple-row"]:first-of-type');
  await page.click('[data-testid="dry-run-toggle"]');
  await expect(page.locator('[data-testid="dry-run-tag"]')).toBeVisible();
  await expect(page.locator('[data-testid="ripple-summary-chips"] .chip')).toHaveCount(4);
  await expect(page.locator('[data-testid="cascade-graph"]')).toBeVisible();
});

test('apply modal requires confirmation', async ({ page }) => {
  await page.click('[data-testid="ripple-row"]:first-of-type');
  await page.click('[data-testid="ripple-drawer-apply"]');
  await expect(page.locator('[data-testid="apply-confirm-modal"]')).toBeVisible();
  // Cancel does not mutate
  await page.click('[data-testid="apply-confirm-cancel"]');
  await expect(page.locator('[data-testid="apply-confirm-modal"]')).not.toBeVisible();
});

test('dry-run summary chips show correct counts', async ({ page }) => {
  await page.click('[data-testid="ripple-row"]:first-of-type');
  await page.click('[data-testid="dry-run-toggle"]');
  const chips = page.locator('[data-testid="ripple-summary-chips"] .chip');
  await expect(chips.nth(0)).toContainText('1 ripple');
  // nodes/edges/actions count from cascade API
  const cascadeCount = await page.evaluate(() => document.querySelectorAll('[data-testid="cascade-graph"]').length);
  expect(cascadeCount).toBeGreaterThanOrEqual(0);  // 0+ cascades acceptable
});
```

- [ ] **Step 4.6: Run e2e to verify they fail**

Run: `pnpm e2e:smoke ripple-dryrun 2>&1 | tail -10`
Expected: 3 failures (e2e selectors not found — drawer Dry-run + ApplyConfirmModal 还没接)

- [ ] **Step 4.7: Run e2e to verify they pass (after Step 4.4 接入) — same as Step 4.6 but should pass**

Run: `pnpm e2e:smoke ripple-dryrun 2>&1 | tail -10`
Expected: 3 passed

- [ ] **Step 4.8: Verify baseline 10 e2e still pass (no behavior break)**

Run: `pnpm e2e:smoke 2>&1 | tail -3`
Expected: 10+3 = 13 passed

- [ ] **Step 4.9: pnpm build + lint**

Run: `pnpm build 2>&1 | tail -3 && pnpm lint:all 2>&1 | tail -3`
Expected: 0 error build / 0 errors lint

- [ ] **Step 4.10: ruff + pytest baseline check**

Run: `ruff check dashboard/ infra/cross_volume/ 2>&1 | tail -3 && pytest -q 2>&1 | tail -3`
Expected: ruff 0 / pytest 2418 passed

- [ ] **Step 4.11: Commit**

```bash
git add dashboard/app.py dashboard/protocols.py \
        dashboard/frontend/src/components/RippleDrawer.vue \
        dashboard/frontend/src/components/ApplyConfirmModal.vue \
        tests/e2e/ripple_dryrun_e2e.spec.js

git commit -m "feat(dashboard): phase9.15 task4 — 2 GET endpoint + 2 schema + Dry-run + ApplyConfirmModal + 3 e2e

Phase 9.15 T3 加 cascade frontend layer 后, 本期 T4 收尾: 实接 2 GET
endpoint + 2 Pydantic schema 校验 + RippleDrawer 加 Dry-run 模式 + 4
summary chips + CascadeGraph 接入 + Apply 二次确认 modal + 3 Playwright
e2e 跑通 dry-run flow.

2 GET endpoint (60L):
- GET /api/cvg/ripples/{id}/cascade → CascadeResponse (CascadedRipple
  7 fields, Pydantic 严格校验, 404 if ripple 不存在, 空 cascade 返
  200 + 空 list + depth=0)
- GET /api/cvg/ripples/{id}/cascade/preview → CascadePreviewResponse
  (5 fields: ripple_id / nodes_to_add / edges_to_add / actions_to_apply
  / totals={nodes,edges,actions})
- 0 改既有 4 endpoint (/apply /reject /rollback /audit)
- 0 改 _default_storage() / _ripple_to_detail() / _audit_to_response()
  既有 helper

2 Pydantic schema (40L):
- CascadeNodeResponse / CascadeEdgeResponse: 跟 ReferenceNode /
  ReferenceEdge 字段 1:1, 5 nullable default
- CascadeResponse: 7 fields, 跟 CascadedRipple dataclass 1:1, bfs_
  algorithm_version default 'v1'
- CascadePreviewResponse: 5 fields, totals dict 动态 count

RippleDrawer.vue (+53L):
- 加 4 新 ref: previewMode / showApplyModal / cascade / preview
- loadCascadeAndPreview() helper: 并行调 loadCascade + loadCascadePreview
- onApplyClick: 先 load preview → 弹 ApplyConfirmModal
- onConfirmApply: 关 modal → emit apply (走既有 /apply endpoint)
- 新增 dry-run section (previewMode 时): 黄色 Dry-run 标签 + 4 summary
  chips (1 ripple / N nodes / M edges / K actions) + CascadeGraph 子组件
  (dryRun=true 节点上色)
- 既有 audit timeline section 0 改 (T5 已落地, 本期只 append)
- Apply 按钮 click handler 改 onApplyClick (0 改 既有 emit('apply', ripple) 逻辑)

ApplyConfirmModal.vue (NEW, 80L):
- 新子组件, props [rippleId, isOpen, totals], emit [confirm, cancel]
- 显示 'Will modify N nodes, M edges, K actions. Confirm?' + 红色
  warning 'cannot be undone (use Rollback)' + [Cancel] [Apply] 2 按钮
- 本地 ref 状态 (0 store mutation on cancel)
- 0 改 RippleDrawer 既有 footer 按钮 (0 改 audit/rollback button)

3 Playwright e2e (200L):
- test_dry_run_button_shows_preview_before_apply (dry-run 标签 + 4 chips)
- test_apply_modal_requires_confirmation (modal 显示 + cancel 不 mutate)
- test_dry_run_summary_chips_show_correct_counts (4 chips 内容校验)

不改:
- 0 改 Phase 9.10-9.14 既有 source API public signature
- 0 改 dashboard 既有 7 endpoint handler (/ripples /ripples/stats /
  /ripples/{id} /apply /reject /audit /rollback)
- 0 改 dashboard 既有 composable (useDecisionStore / useWorkflowSocket
  / useCostWindow / useTimeOptions 0 改)
- 0 改 dashboard 既有 4 store action (recordRipple / fetchRipples /
  fetchRippleById / updateRippleStatus / fetchAudit / rollback 0 改)
- 0 改 dashboard 既有 9 api method (5+2 new + 9.14 2 = 9 既有, 0 改)
- 0 改 dashboard router / Sidebar nav (0 new route / 0 new entry)
- 0 改 CLAUDE.md / pyproject.toml / pytest.ini / vite.config.js /
  vitest.config.js / playwright.config.js
- 0 改 ECharts 5.4 → 5.5
- 0 改 historical spec/plan doc 行号 :NNN
- 0 改 12 SCENARIOS 路由 / MasterController / got_bridge
- 0 改 3 PRAGMA / 0 数据迁移 (老 ripple cascade 0 历史, GET /cascade
  返 None, e2e 处理 0+ count acceptable)
- 0 force-push / 0 amend / 0 --no-verify / 0 Co-Authored-By

Baseline:
- pytest 2418 (T2 验证过, T4 0 改 backend storage)
- vitest 147 (T3 验证过, T4 0 改 frontend store)
- e2e 10+3 = 13 passed
- ruff 0
- pnpm build: 0 error
- pnpm lint:all: 0 errors / ≤8 warnings"
```

---

## Final code review (主公审阅)

- [ ] **Step F.1: Dispatch final code reviewer subagent**

Use `superpowers:code-reviewer` agent to review the 4 task commits end-to-end. Verify:
- 0 改 Phase 9.10-9.14 既有 source API
- 0 改 dashboard 既有 endpoint handler / composable / store / router / sidebar nav
- 0 误改 .env / 0 API key 泄漏
- 0 infra/.state/* 提交
- 0 force-push / 0 amend / 0 --no-verify / 0 Co-Authored-By footer
- 24 new tests (14 + 7 + 3) all pass
- ruff 0
- pnpm build / lint:all 0 error
- File size all < 800L

- [ ] **Step F.2: Verify all tests + lint + build pass**

```bash
pytest -q 2>&1 | tail -3
# Expected: 2418 passed, 0 failed, 27 skipped

pnpm vitest run 2>&1 | tail -3
# Expected: 147 passed

pnpm e2e:smoke 2>&1 | tail -3
# Expected: 13 passed

pnpm lint:all 2>&1 | tail -3 && pnpm build 2>&1 | tail -3
# Expected: 0 errors / 0 errors

ruff check infra/cross_volume/ dashboard/ tests/cross_volume/ 2>&1 | tail -3
# Expected: 0 issues
```

- [ ] **Step F.3: git log verify + git status clean**

```bash
git log --oneline 4960729..HEAD
# Expected: 4 feat commits (T1-T4) + this plan commit = 5 commits

git status
# Expected: clean (only plan file added but already committed)
```

- [ ] **Step F.4: Push to origin/master**

```bash
git push origin master
# Expected: 5 commits pushed (no force-push, no amend, no --no-verify)
```

---

## Out of Scope (后续 phases)

- **Phase 9.16**: cascade weighted BFS (按 edge.weight 排序, 当前等权) + cascade visualization animation (节点 hover 渐入) + cascade real-time WebSocket
- **Phase 9.17**: cascade 决策 undo (apply → cascade → undo, 跟 9.14 rollback 串起来, 走 `cascade_cascade` 嵌套 storage)
- **Phase 9.18**: cross-volume impact scoring (1 个 ripple 的 cascade 影响分数, dashboard 显式排名)
- **Phase 9.19**: cascade export to JSON / CSV (审计报表用, lingwen.py ripple cascade export <id>)
- **ECharts 5.4 → 5.5 升级**: security patch, 9.15 用 stable 5.4, 留 followup
- **MEMORY.md 拆 followup**: 9.15 后 MEMORY.md 仍 12-13 entries, 暂时不超 200 行 soft limit, 留 Phase 10 统一拆
- **CI filter tightening**: Phase 9.15 0 改 CI, 留 followup
- **cascade 模拟数据生成**: test fixture 复用 Phase 9.10-9.14 既有, 不需新 generator, 留 followup
- **multi-user 权限**: actor 字符串, 0 role/permission, 留 followup
- **cascade 回滚 audit**: 当前 rollback 仅 ripple status, cascade rows 仍存, 留 Phase 9.17 串起来

---

## Critical files (modified/created)

### Created
- `infra/cross_volume/reference_graph.py` (NEW `CascadedRipple` dataclass + 替换 stub, +44L)
- `tests/cross_volume/test_cascade_bfs.py` (NEW, ~200L, 8 tests)
- `tests/cross_volume/test_ripple_cascade_storage.py` (NEW, ~150L, 6 tests)
- `dashboard/frontend/src/components/CascadeGraph.vue` (NEW, ~120L)
- `dashboard/frontend/src/components/__tests__/CascadeGraph.spec.js` (NEW, ~150L, 5 tests)
- `dashboard/frontend/src/components/ApplyConfirmModal.vue` (NEW, ~80L)
- `tests/e2e/ripple_dryrun_e2e.spec.js` (NEW, ~200L, 3 tests)

### Modified
- `infra/cross_volume/storage.py` (+78L, ripple_cascade table + 2 method + 5 helper + cascade hook in append_ripple)
- `dashboard/app.py` (+60L, 2 GET endpoint)
- `dashboard/protocols.py` (+40L, 2 schema + 2 sub-schema)
- `dashboard/frontend/src/components/RippleDrawer.vue` (+53L, Dry-run 模式 + 4 summary chips + CascadeGraph 接入 + ApplyConfirmModal 接入)
- `dashboard/frontend/src/composables/useRippleStore.js` (+25L, 2 action + 2 getter)
- `dashboard/frontend/src/api/index.js` (+30L, 2 method)
- `dashboard/frontend/src/composables/__tests__/useRippleStore.spec.js` (+30L, 2 tests)

### NOT modified
- `infra/cross_volume/ripple.py` (CrossVolumeRipple 0 改)
- `infra/agent_system/` (5 agents 0 改)
- `dashboard/frontend/src/router/` (0 改)
- `dashboard/frontend/src/components/Sidebar.vue` (0 改)
- `dashboard/frontend/src/composables/useDecisionStore.js` (0 改)
- `dashboard/frontend/src/composables/useWorkflowSocket.js` (0 改)
- `dashboard/frontend/src/composables/useCostWindow.js` (0 改)
- `dashboard/frontend/src/composables/useTimeOptions.js` (0 改)
- 18 historical spec/plan docs (Phase 9.10-9.14 + earlier, 0 改 :NNN 行号)

---

## Risks + Mitigations

| 风险 | 缓解 |
|------|------|
| BFS 死循环 (cyclic graph) | visited set 严格 + depth limit 3, 8 个 BFS tests 覆盖 cycle/loop/disconnected 场景 |
| ECharts bundle size 增大 | lazy import 仅在 CascadeGraph 挂载时动态加载, 0 main bundle 增, 5 vitest 覆盖 lazy load |
| `record_ripple` 内部 cascade 写入失败 | best-effort pattern (跟 9.14 record_audit 1:1), 失败 logger.warning 不 propagate, ripple INSERT 已 commit |
| cascade 节点 > 100 drawer UX 卡 | "Truncated to first 100" warning chip + 0 渲染超量节点, 防御性 |
| 二次确认 modal 误点 | 本地 ref 状态 (0 store mutation) + [Cancel] [Apply] 2 button, 0 default Apply focus |
| `_node_to_dict` / `_edge_to_dict` JSON 序列化 datetime 不一致 | ISO 8601 字符串 + fromisoformat 反序列化, 5 helper 集中维护, 1 pytest 覆盖 round-trip |
| CascadeGraph 节点 click 路由 | emit nodeClick { nodeId, volume, chapter } 跟 Phase 7.6 S1-S8 雷达图同 pattern |
| 12 SCENARIOS 路由 | 0 改, CostTracker 仍是观察者 |
| 历史 8 phase 9.10-9.14 spec/plan doc 行号 `:NNN` 改 | 0 改, 跟 Phase 9.14 一样不动 |
| dashboard 既有 endpoint/composable/store/router/sidebar 行为破 | 0 改既有, 仅 additive, 5 dashboard test file 0 改 test case (仅 useRippleStore.spec.js +2 additive) |
| ECharts 5.4 vs 5.5 版本冲突 | 用 `^5.4.0` (现有 9.13 已锁定), 留 5.5 升级 followup |
| TypeScript strict mode 漏 (CascadeGraph.vue) | 0 用 TS, 纯 JS + JSDoc, 跟 Phase 9.14 RippleDrawer 一致 |
| API key 泄漏 | 0 provider 改, 0 .env 改, 0 ANTHROPIC_API_KEY 引用, 跟 Phase 9.14 一致 |
| ruff 引入新 I001 | 新文件保持 isort 合规, `ruff check infra/cross_volume/ dashboard/ tests/cross_volume/` 0 issues |

---

## DoD (完成定义)

- [ ] `docs/superpowers/specs/2026-06-10-phase9.15-cascade-graph-dryrun-design.md` 创建 (commits 4960729 + dd1b000, 304L)
- [ ] `docs/superpowers/plans/2026-06-10-phase9.15-cascade-graph-dryrun.md` 创建 (本文件, 末尾 commit, ~570L)
- [ ] Task 1: `CascadedRipple` + 真实 BFS + 8 pytest tests passed
- [ ] Task 2: `ripple_cascade` table + 2 storage method + `record_ripple` cascade hook + 6 pytest tests passed
- [ ] Task 3: `<CascadeGraph>` 子组件 + `useRippleStore` 2 action + `api/index.js` 2 method + 7 vitest tests passed
- [ ] Task 4: 2 GET endpoint + 2 Pydantic schema + RippleDrawer Dry-run + `<ApplyConfirmModal>` + 3 e2e tests passed
- [ ] Final: 1 final code review commit + 1 push to origin/master
- [ ] pytest 2404 + 14 = **2418 passed**, 0 failed, 27 skipped
- [ ] vitest 140 + 7 = **147 passed**
- [ ] e2e 10 + 3 = **13 passed**
- [ ] ruff 0 (`ruff check infra/cross_volume/ dashboard/ tests/cross_volume/`)
- [ ] pnpm lint:all 0 errors / ≤8 warnings
- [ ] pnpm build 0 error
- [ ] 0 旧测试代码改动 (2404 baseline 不变, useRippleStore.spec.js +2 additive only)
- [ ] 0 production code 行为破坏 (Phase 9.10-9.14 既有 API 全保, additive)
- [ ] 0 误改 .env / 0 API key 泄漏
- [ ] 0 infra/.state/* 提交 (gitignored)
- [ ] 0 force-push, 0 --no-verify, 0 Co-Authored-By footer
- [ ] 0 改 Phase 9.10-9.14 既有 source API public signature
- [ ] 0 改 dashboard 既有 endpoint handler / composable / store / router / sidebar nav
- [ ] 0 改 CLAUDE.md / pyproject.toml / pytest.ini / vite.config.js / vitest.config.js / playwright.config.js
- [ ] 0 改 historical spec/plan doc 行号 `:NNN`
- [ ] 0 改 ECharts 5.4 → 5.5 (留 followup)
- [ ] 0 改 12 SCENARIOS 路由
- [ ] 0 改 3 PRAGMA / 0 数据迁移
- [ ] 1 spec commit (4960729) + 1 spec self-review (dd1b000) + 1 plan commit (本文件) + 4 task commits + 1 final review = **8 commits pushed** to origin/master

---

## 完成后下一步 (后续 phases)

- **Phase 9.16**: cascade weighted BFS (按 edge.weight 排序, 当前等权) + cascade visualization animation (节点 hover 渐入) + cascade real-time WebSocket
- **Phase 9.17**: cascade 决策 undo (apply → cascade → undo, 跟 9.14 rollback 串起来, 走 `cascade_cascade` 嵌套 storage)
- **Phase 9.18**: cross-volume impact scoring (1 个 ripple 的 cascade 影响分数, dashboard 显式排名)
- **Phase 9.19**: cascade export to JSON / CSV (审计报表用)
- **ECharts 5.4 → 5.5 升级**: security patch
- **MEMORY.md 拆 followup**: 9.15 后 MEMORY.md 仍 12-13 entries, 暂不超 200 行 soft limit, 留 Phase 10 统一拆
- **CI filter tightening**: Phase 9.15 0 改 CI, 留 followup
- **Phase 10**: 跨卷涟漪收官 (跨 phase 整合)
