# Phase 7.5 Implementation Plan — polish_merge 升级到 LLM S1-S8 多维加权评分

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `polish_merge` 从 Phase 7.4 的 `max(contents, key=len)` 升级为 LLM S1-S8 8 维评分加权求总分选高者, 韧性兜底 `max(len)`。

**Architecture:**
- 新增 `MasterController.polish_merge_synthesis(content_a, content_b, labels)` — LLM 调 HAIKU tier, 评分 8 维, 加权求总分, winner = 高者
- 新增 `polisher/prompts.py::build_merge_synthesis_prompt` + `get_merge_synthesis_system_prompt` — S1-S8 评分 prompt
- 改 `got_bridge.py::_handler_polish_merge` — 调 master 新方法, 1 upstream 兜底, 2+ upstream 走 LLM
- 改 `novel_writing.yaml` — `polish_merge.token_budget: 0 → 4000`, `timeout_s: 30 → 90`, `version: 2 → 3`

**Tech Stack:** Python 3.13, pytest 9, Pydantic, FastAPI, AIRouter (HAIKU tier), PyYAML

---

## Task 1: RED — 6 个新测试 + 同步现有测试

**Files:**
- Create: `tests/agent_system/test_master_controller.py` (NEW, ~150L)
- Modify: `tests/agent_system/test_got_bridge.py` — 1 new test class, modify 1 old
- Modify: `tests/agent_system/test_master_controller_stub_router_e2e.py` — 2 sync
- Modify: `tests/agent_system/test_master_controller_workflow.py` — stub sync
- Modify: `tests/agent_system/test_decision_integration.py` — stub sync
- Modify: `tests/dashboard/test_decision_api.py` — stub sync (可能)

### Step 1.1: 写 test_master_controller.py (5 个新测试)

创建 `tests/agent_system/test_master_controller.py`:

```python
"""MasterController unit tests — Phase 7.5 S1-S8 weighted scoring for polish_merge

测试 polish_merge_synthesis:
- LLM 评分: winner = 高总分者
- 平分: 选 first (>= ties)
- 空 content: 走 len fallback
- identical content: 走 fallback="identical"
- LLM 失败: 走 len fallback, reason="llm_fail"
"""
from __future__ import annotations

import json
from typing import Any, Optional

import pytest


class _FakeLLMResponse:
    """模拟 polisher.chat() 返的字符串 (json)"""
    def __init__(self, payload: dict) -> None:
        self._payload = payload
    def __str__(self) -> str:
        return json.dumps(self._payload, ensure_ascii=False)


def _make_master(chat_response: Optional[Any] = None, chat_raises: bool = False):
    """构造 MasterController.__new__ stub, 注入 polisher.chat() 行为.

    走 __new__ 绕过 __init__ (避免 build_router 等重操作),只塞 self.polisher
    即可 — polish_merge_synthesis 只读 self.polisher.chat + parse_response.
    """
    from infra.agent_system import master_controller as mc_mod

    class _StubPolisher:
        def __init__(self) -> None:
            self.calls: list[dict] = []

        def chat(self, *, prompt: str, system: str, temperature: float, max_tokens: int):
            self.calls.append({"prompt": prompt, "system": system, "temperature": temperature, "max_tokens": max_tokens})
            if chat_raises:
                raise RuntimeError("simulated LLM failure")
            return _FakeLLMResponse(chat_response)

        def parse_response(self, response: Any, *, format_type: str = "json"):
            if format_type != "json":
                raise ValueError(f"unsupported format_type: {format_type}")
            return json.loads(str(response))

    master = mc_mod.MasterController.__new__(mc_mod.MasterController)
    master.polisher = _StubPolisher()
    return master


class TestPolishMergeSynthesis:
    """Phase 7.5: MasterController.polish_merge_synthesis — S1-S8 8 维加权评分"""

    def test_returns_winner_with_higher_total_score(self):
        """A 平均 8 分, B 平均 5 分 → winner=A, scores_a/b 全填, total_a > total_b"""
        chat_response = {
            "scores_A": {"S1": 8, "S2": 8, "S3": 8, "S4": 8, "S5": 8, "S6": 8, "S7": 8, "S8": 8},
            "scores_B": {"S1": 5, "S2": 5, "S3": 5, "S4": 5, "S5": 5, "S6": 5, "S7": 5, "S8": 5},
            "reason": "A 整体质量更高",
        }
        master = _make_master(chat_response=chat_response)
        result = master.polish_merge_synthesis(
            content_a="A variant content here" * 5,
            content_b="B variant content here" * 3,
            labels=("A", "B"),
        )

        assert result["winner"] == "A"
        assert result["content"] == "A variant content here" * 5
        assert result["scores_total_a"] == 8.0
        assert result["scores_total_b"] == 5.0
        assert result["scores_delta"] == pytest.approx(3.0)
        assert result["scores_a"]["S1"] == 8
        assert result["scores_b"]["S8"] == 5
        assert "fallback" not in result  # 走 LLM 路径不带 fallback

    def test_equal_scores_winner_breaks_tie_to_first(self):
        """A=B=8.0 → winner=labels[0] (>= ties break to first)"""
        chat_response = {
            "scores_A": {"S1": 8, "S2": 8, "S3": 8, "S4": 8, "S5": 8, "S6": 8, "S7": 8, "S8": 8},
            "scores_B": {"S1": 8, "S2": 8, "S3": 8, "S4": 8, "S5": 8, "S6": 8, "S7": 8, "S8": 8},
            "reason": "tie",
        }
        master = _make_master(chat_response=chat_response)
        result = master.polish_merge_synthesis(
            content_a="variant A",
            content_b="variant B",
            labels=("emotional_pacing", "ai_trace_removal"),
        )

        assert result["winner"] == "emotional_pacing"  # >= ties to first
        assert result["scores_total_a"] == result["scores_total_b"] == 8.0
        assert result["scores_delta"] == 0.0

    def test_empty_content_returns_len_fallback(self):
        """A="" → len_fallback, winner=labels[1] (B)"""
        master = _make_master(chat_response={})  # 不应被调
        result = master.polish_merge_synthesis(
            content_a="",
            content_b="non-empty B content",
            labels=("A", "B"),
        )

        assert result["fallback"] == "empty_content"
        assert result["winner"] == "B"
        assert result["content"] == "non-empty B content"
        assert result["scores_a"] == {}
        assert result["scores_b"] == {}
        # LLM 不应被调 (empty 直接 fallback)
        assert master.polisher.calls == []

    def test_identical_content_returns_first_with_fallback_identical(self):
        """A==B → fallback="identical", winner=labels[0], scores 0.0"""
        master = _make_master(chat_response={})  # 不应被调
        same = "identical content here"
        result = master.polish_merge_synthesis(
            content_a=same,
            content_b=same,
            labels=("alpha", "beta"),
        )

        assert result["fallback"] == "identical"
        assert result["winner"] == "alpha"
        assert result["content"] == same
        assert result["scores_total_a"] == 0.0
        assert result["scores_total_b"] == 0.0
        assert result["scores_delta"] == 0.0
        assert master.polisher.calls == []

    def test_llm_failure_returns_len_fallback_with_reason(self):
        """LLM 抛错 → fallback="llm_fail", winner=len 较长者"""
        master = _make_master(chat_raises=True)
        result = master.polish_merge_synthesis(
            content_a="short",
            content_b="this is a much longer content version",
            labels=("A", "B"),
        )

        assert result["fallback"] == "llm_fail"
        # max(len) → B (len 36 > 5)
        assert result["winner"] == "B"
        assert result["content"] == "this is a much longer content version"
        assert result["scores_a"] == {}
        assert result["scores_b"] == {}
        # LLM 调过 1 次 (然后失败)
        assert len(master.polisher.calls) == 1
```

### Step 1.2: test_got_bridge.py — 加 TestPolishMergeHandlerScored (1 测试) + 修 1 老测试

**Step 1.2.1**: 在 `TestPolishMergeHandler` 类**之后**新增 `TestPolishMergeHandlerScored`:

```python
class TestPolishMergeHandlerScored:
    """Phase 7.5: _handler_polish_merge 调 master.polish_merge_synthesis (LLM S1-S8)"""

    def test_handler_returns_winner_with_score_fields(self):
        """2 上游走 LLM 评分路径, 返回 dict 含 winner / scores_total_* 字段"""
        # 构造 _StubMaster with LLM-模拟 chat response
        class _ScoringStub(_StubMaster):
            def __init__(self):
                super().__init__()
                # 加 polish_merge_synthesis 方法
                from infra.agent_system.agents.polisher.prompts import _S1_S8_NAMES  # noqa
                self._llm_response = {
                    "scores_A": {"S1": 9, "S2": 9, "S3": 9, "S4": 9, "S5": 9, "S6": 9, "S7": 9, "S8": 9},
                    "scores_B": {"S1": 5, "S2": 5, "S3": 5, "S4": 5, "S5": 5, "S6": 5, "S7": 5, "S8": 5},
                    "reason": "A wins",
                }
            def polish_merge_synthesis(self, content_a, content_b, *, labels=("A", "B")):
                # 模拟 LLM 评分 — A 9 分, B 5 分
                total_a = 9.0
                total_b = 5.0
                winner = labels[0] if total_a >= total_b else labels[1]
                content = content_a if winner == labels[0] else content_b
                return {
                    "content": content,
                    "winner": winner,
                    "scores_a": self._llm_response["scores_A"],
                    "scores_b": self._llm_response["scores_B"],
                    "scores_total_a": total_a,
                    "scores_total_b": total_b,
                    "scores_delta": total_a - total_b,
                }

        master = _ScoringStub()
        compute = AgentComputeFn(master)
        node = _node("merge_scored", "polish_merge")
        inputs = {
            "polish_emotional_pacing": {"content": "A version here" * 3},
            "polish_ai_trace_removal": {"content": "B version here" * 2},
        }
        result = compute(node, inputs)
        assert result.fail is False
        # A 9 分 > B 5 分 → 选 A
        assert result.output["winner"] == "A"
        assert result.output["scores_total_a"] == 9.0
        assert result.output["scores_total_b"] == 5.0
        assert "scores_a" in result.output
        assert "scores_b" in result.output
        assert result.output["content"] == "A version here" * 3
```

**Step 1.2.2**: 改 `test_polish_merge_picks_longer_content` (Phase 7.4 旧测试):

**现状**:
```python
def test_polish_merge_picks_longer_content(self):
    """输入 2 个上游, 返 max(len(content)) 版本"""
    master = _StubMaster()
    compute = AgentComputeFn(master)
    node = _node("merge1", "polish_merge")
    inputs = {
        "polish_emotional_pacing": {"content": "short"},
        "polish_ai_trace_removal": {"content": "this is a much longer content version"},
    }
    result = compute(node, inputs)
    assert result.fail is False
    assert result.output["content"] == "this is a much longer content version"
```

**改为** (验证仍走 max(len) **当 stub 没 polish_merge_synthesis 方法** — 老 _StubMaster 不变):
保留**不变** (old _StubMaster 没 polish_merge_synthesis, handler 走 TypeError → len fallback 是错的;实际应给 _StubMaster 加个 fallback)

**更稳的方案**: 在 `_StubMaster` 类 (在 test_got_bridge.py:31-60) 加 `polish_merge_synthesis` 默认方法 (用 max(len) 兜底,跟 handler 走 _handler_polish_merge 旧 max(len) 行为一致):

在 `_StubMaster` 类的 `polish_ai_trace_removal` 方法之后追加:

```python
    def polish_merge_synthesis(self, content_a: str, content_b: str, *, labels=("A", "B")) -> dict:
        """Stub: 模拟 master.polish_merge_synthesis 的 max(len) 兜底 (默认 stub 不调 LLM)"""
        if not content_a or not content_b:
            winner = labels[0] if content_a else labels[1]
            content = content_a or content_b
            return {"content": content, "winner": winner, "scores_a": {}, "scores_b": {}, "scores_total_a": 0.0, "scores_total_b": 0.0, "scores_delta": 0.0, "fallback": "empty_content"}
        if content_a == content_b:
            return {"content": content_a, "winner": labels[0], "scores_a": {}, "scores_b": {}, "scores_total_a": 0.0, "scores_total_b": 0.0, "scores_delta": 0.0, "fallback": "identical"}
        winner_label = labels[0] if len(content_a) >= len(content_b) else labels[1]
        content = content_a if winner_label == labels[0] else content_b
        return {"content": content, "winner": winner_label, "scores_a": {}, "scores_b": {}, "scores_total_a": 0.0, "scores_total_b": 0.0, "scores_delta": 0.0, "fallback": "llm_fail"}
```

**这样**: Phase 7.4 老测试 `test_polish_merge_picks_longer_content` 仍 PASS (走 stub 的 max(len) 兜底),新的 `TestPolishMergeHandlerScored` 用 `_ScoringStub` 覆盖走 LLM 路径。

### Step 1.3: test_master_controller_stub_router_e2e.py — 2 个 sync

**Step 1.3.1**: 改 `test_at_least_six_llm_calls` 方法名 + 数字:
- 重命名 → `test_at_least_seven_llm_calls` (Phase 7.5: polish_merge 调 1 LLM)
- `>= 5` → `>= 6` (write + audit + emotional_pacing×2 + ai_trace_removal×1 + polish_merge×1 = 6)

**Step 1.3.2**: 改 `test_novel_writing_emit_depends_on_polish_merge_yaml_static`:
- 在 `assert "polish_chapter" not in nodes_by_id` 之后追加:

```python
    # Phase 7.5: polish_merge token_budget=4000, timeout_s=90
    merge_node = nodes_by_id["polish_merge"]
    assert merge_node["token_budget"] == 4000, f"polish_merge.token_budget should be 4000, got {merge_node['token_budget']}"
    assert merge_node["timeout_s"] == 90
    # version 升级到 3
    assert data["version"] == 3, f"novel_writing workflow version should be 3, got {data['version']}"
```

### Step 1.4: stub sync — 4 个文件加 polish_merge_synthesis 方法

**4 个文件都需加 stub**:
- `tests/agent_system/test_master_controller_workflow.py` `_StubMaster` (line 19)
- `tests/agent_system/test_decision_integration.py` `_StubMaster` (line 95)
- `tests/dashboard/test_decision_api.py` `_StubMasterController` (已有 mock_master, 需检查)
- `tests/agent_system/test_master_controller_stub_router_e2e.py` (verify 是否有 stub)

**添加方法 (跟 Step 1.2.2 同样的兜底实现)**:

```python
    def polish_merge_synthesis(self, content_a, content_b, *, labels=("A", "B")) -> dict:
        """Stub: 走 max(len) 兜底 (默认不调 LLM)"""
        if not content_a or not content_b:
            winner = labels[0] if content_a else labels[1]
            content = content_a or content_b
            return {"content": content, "winner": winner, "scores_a": {}, "scores_b": {}, "scores_total_a": 0.0, "scores_total_b": 0.0, "scores_delta": 0.0, "fallback": "empty_content"}
        if content_a == content_b:
            return {"content": content_a, "winner": labels[0], "scores_a": {}, "scores_b": {}, "scores_total_a": 0.0, "scores_total_b": 0.0, "scores_delta": 0.0, "fallback": "identical"}
        winner_label = labels[0] if len(content_a) >= len(content_b) else labels[1]
        content = content_a if winner_label == labels[0] else content_b
        return {"content": content, "winner": winner_label, "scores_a": {}, "scores_b": {}, "scores_total_a": 0.0, "scores_total_b": 0.0, "scores_delta": 0.0, "fallback": "llm_fail"}
```

### Step 1.5: RED 验证

```bash
pytest tests/agent_system/test_master_controller.py -v
# Expected: ModuleNotFoundError OR collection error (因 master.polish_merge_synthesis 还不存在)

pytest tests/agent_system/test_got_bridge.py::TestPolishMergeHandlerScored -v
# Expected: ImportError OR AttributeError (因 _S1_S8_NAMES not exported in prompts yet)

pytest tests/agent_system/test_master_controller_stub_router_e2e.py::test_novel_writing_emit_depends_on_polish_merge_yaml_static -v
# Expected: FAIL — version=2 not 3, token_budget=0 not 4000
```

**状态**: 6 测试 RED ✅

---

## Task 2: GREEN — 实现 + 6 测试 PASS

**Files:**
- Modify: `infra/agent_system/agents/polisher/prompts.py` — +`build_merge_synthesis_prompt` +`get_merge_synthesis_system_prompt` +`_S1_S8_NAMES`
- Modify: `infra/agent_system/master_controller.py` — +`polish_merge_synthesis` +2 私有方法 +`_S1_S8_KEYS` 常量 + `Dict, Any` import
- Modify: `infra/agent_system/got_bridge.py` — `_handler_polish_merge` 改写
- Modify: `infra/got/workflows/novel_writing.yaml` — version 2→3, polish_merge token_budget 0→4000, timeout 30→90

### Step 2.1: polisher/prompts.py 加 S1-S8 + merge prompts

**Modify**: 在文件**末尾**追加:

```python
# ==================== Merge Synthesis (Phase 7.5) ====================

_S1_S8_NAMES = {
    "S1": "剧情完整性 (Plot Completeness)",
    "S2": "逻辑自洽 (Logic Consistency)",
    "S3": "文笔风格 (Writing Style)",
    "S4": "情感共鸣 (Emotional Resonance)",
    "S5": "节奏控制 (Pacing Control)",
    "S6": "可读性 (Readability)",
    "S7": "主角魅力 (Protagonist Appeal)",
    "S8": "人物弧光 (Character Arc)",
}


def build_merge_synthesis_prompt(content_a: str, content_b: str, labels: tuple[str, str] = ("A", "B")) -> str:
    """Phase 7.5: 构建 polish_merge S1-S8 多维评分 prompt

    把 2 个润色 variant 给 LLM, 按 S1-S8 8 维打分 (0-10), 选总平均分高者。
    label 参数允许上游节点用 input id (e.g. "polish_emotional_pacing") 当 label,
    输出 JSON 的 scores key 用 label 命名 (e.g. "scores_polish_emotional_pacing")。
    """
    label_a, label_b = labels
    s1_s8_list = "\n".join(f"- {k}: {v}" for k, v in _S1_S8_NAMES.items())
    return f"""你是资深网文质量审核官, 需要对比 2 个章节润色 variant, 按 S1-S8 8 维评分 (0-10 分, 越高越好), 并选出优胜者。

## 评分维度
{s1_s8_list}

## Variant {label_a}
{content_a[:3000]}

## Variant {label_b}
{content_b[:3000]}

## 输出格式 (严格 JSON)
{{
  "scores_{label_a}": {{"S1": <int>, "S2": <int>, "S3": <int>, "S4": <int>, "S5": <int>, "S6": <int>, "S7": <int>, "S8": <int>}},
  "scores_{label_b}": {{"S1": <int>, "S2": <int>, "S3": <int>, "S4": <int>, "S5": <int>, "S6": <int>, "S7": <int>, "S8": <int>}},
  "reason": "<50 字以内, 解释为什么选 {label_a} 或 {label_b}>"
}}

只返 JSON, 不要其他文字。"""


def get_merge_synthesis_system_prompt() -> str:
    """Phase 7.5: polish_merge S1-S8 评分系统提示"""
    return """你是一位资深网文质量审核官, 擅长多维度对比 2 个润色版本。
你的工作: 客观评分 S1-S8 8 维, 选总平均分高者。
不要因为 variant 长度或风格偏好而偏袒, 严格按 8 维定义评估。"""
```

### Step 2.2: master_controller.py 加 polish_merge_synthesis

**Modify**: 在 `polish_ai_trace_removal` 方法**之后** (`master_controller.py:622` 之后) 追加:

```python
    def polish_merge_synthesis(
        self,
        content_a: str,
        content_b: str,
        *,
        labels: tuple[str, str] = ("A", "B"),
    ) -> Dict[str, Any]:
        """Phase 7.5: LLM S1-S8 8 维加权评分, 选高者 (polish_merge 节点)

        调 polisher.chat() (HAIKU tier, 4000 tokens) 评分 2 个 variant, 加权
        (等权 0.125 each) 求总分, winner = 高者。韧性契约: LLM 失败 → 走
        max(len) 兜底, reason="llm_fail"。

        Args:
            content_a: variant A 内容
            content_b: variant B 内容
            labels: 2 个 label (默认 ("A", "B")), 也可用上游节点 id
                    (e.g. ("polish_emotional_pacing", "polish_ai_trace_removal"))

        Returns:
            {
                "content": str,             # winner 内容
                "winner": str,              # labels[0] or labels[1]
                "scores_a": dict[str, int], # {S1: 8, ..., S8: 7}
                "scores_b": dict[str, int],
                "scores_total_a": float,    # 0-10 平均
                "scores_total_b": float,
                "scores_delta": float,      # total_a - total_b
                "fallback": str | None,     # 兜底原因 (None=走 LLM)
            }
        """
        empty = self._merge_synthesis_len_fallback
        if not content_a or not content_b:
            return empty(content_a, content_b, labels, reason="empty_content")
        if content_a == content_b:
            return {
                "content": content_a,
                "winner": labels[0],
                "scores_a": {},
                "scores_b": {},
                "scores_total_a": 0.0,
                "scores_total_b": 0.0,
                "scores_delta": 0.0,
                "fallback": "identical",
            }
        try:
            return self._merge_synthesis_llm(content_a, content_b, labels)
        except Exception as e:
            logger.warning("polish_merge_synthesis: LLM failed, len fallback: %s", e)
            return empty(content_a, content_b, labels, reason="llm_fail")

    def _merge_synthesis_llm(
        self,
        content_a: str,
        content_b: str,
        labels: tuple[str, str],
    ) -> Dict[str, Any]:
        """Phase 7.5: 调 polisher LLM 评分 2 variant, 等权求总分选高者"""
        from .agents.polisher.prompts import (
            build_merge_synthesis_prompt,
            get_merge_synthesis_system_prompt,
        )

        prompt = build_merge_synthesis_prompt(content_a, content_b, labels=labels)
        system = get_merge_synthesis_system_prompt()
        response = self.polisher.chat(
            prompt=prompt, system=system, temperature=0.2, max_tokens=2000
        )
        parsed = self.polisher.parse_response(response, format_type="json")

        label_a, label_b = labels
        scores_a = {k: int(parsed[f"scores_{label_a}"][k]) for k in _S1_S8_KEYS}
        scores_b = {k: int(parsed[f"scores_{label_b}"][k]) for k in _S1_S8_KEYS}
        total_a = sum(scores_a.values()) / 8.0
        total_b = sum(scores_b.values()) / 8.0
        winner = label_a if total_a >= total_b else label_b
        content = content_a if winner == label_a else content_b
        return {
            "content": content,
            "winner": winner,
            "scores_a": scores_a,
            "scores_b": scores_b,
            "scores_total_a": total_a,
            "scores_total_b": total_b,
            "scores_delta": total_a - total_b,
        }

    def _merge_synthesis_len_fallback(
        self,
        content_a: str,
        content_b: str,
        labels: tuple[str, str],
        *,
        reason: str,
    ) -> Dict[str, Any]:
        """Phase 7.5: max(len) 兜底 (LLM 失败 / 空 content 时)"""
        if not content_a and not content_b:
            return {
                "content": "",
                "winner": "",
                "scores_a": {},
                "scores_b": {},
                "scores_total_a": 0.0,
                "scores_total_b": 0.0,
                "scores_delta": 0.0,
                "fallback": reason,
            }
        if not content_a:
            return {
                "content": content_b,
                "winner": labels[1],
                "scores_a": {},
                "scores_b": {},
                "scores_total_a": 0.0,
                "scores_total_b": 0.0,
                "scores_delta": 0.0,
                "fallback": reason,
            }
        if not content_b:
            return {
                "content": content_a,
                "winner": labels[0],
                "scores_a": {},
                "scores_b": {},
                "scores_total_a": 0.0,
                "scores_total_b": 0.0,
                "scores_delta": 0.0,
                "fallback": reason,
            }
        winner = labels[0] if len(content_a) >= len(content_b) else labels[1]
        content = content_a if winner == labels[0] else content_b
        return {
            "content": content,
            "winner": winner,
            "scores_a": {},
            "scores_b": {},
            "scores_total_a": 0.0,
            "scores_total_b": 0.0,
            "scores_delta": 0.0,
            "fallback": reason,
        }
```

**Step 2.2.1**: 在文件**顶部** `import` 区 (line 16-17) 修 `Dict, Any` import:

```python
from typing import Any, Dict, List, Optional
```

(确认已有 `Dict` 和 `Any` — 当前是 `Any, Dict, List, Optional` ✅)

**Step 2.2.2**: 加模块级常量 `_S1_S8_KEYS` — 紧跟 `logger = logging.getLogger(__name__)` (line 36) 之后:

```python
_S1_S8_KEYS: tuple[str, ...] = ("S1", "S2", "S3", "S4", "S5", "S6", "S7", "S8")
```

### Step 2.3: got_bridge.py 改写 _handler_polish_merge

**Modify**: 替换 `got_bridge.py:118-131` 的整个 `_handler_polish_merge` 函数:

```python
def _handler_polish_merge(master: MasterController, inputs: dict[str, Any]) -> dict[str, Any]:
    """Phase 7.5: scenario=polish_merge — 调 master.polish_merge_synthesis, LLM S1-S8 加权选高者

    收集 2 个上游 variant (按 input key 排序, 确定性), 调 master 评分。
    1 上游 → 兜底, 0 上游 → _error。LLM 失败由 master 内部兜底 max(len)。
    """
    # 按 input key 排序收集 (确定性 — 同 input 顺序下, winner 一致)
    variants: dict[str, str] = {}
    for upstream_id, upstream in sorted(inputs.items()):
        if isinstance(upstream, dict) and "content" in upstream:
            variants[upstream_id] = upstream["content"]

    if not variants:
        return {"_error": "polish_merge requires >= 1 upstream with content"}

    if len(variants) == 1:
        only_id, only_content = next(iter(variants.items()))
        return {
            "content": only_content,
            "winner": only_id,
            "scores_a": {},
            "scores_b": {},
            "scores_total_a": 0.0,
            "scores_total_b": 0.0,
            "scores_delta": 0.0,
            "fallback": "single_upstream",
        }

    # 2+ variants 调 LLM 评分
    ids = list(variants.keys())
    label_a, label_b = ids[0], ids[1]
    return master.polish_merge_synthesis(
        content_a=variants[label_a],
        content_b=variants[label_b],
        labels=(label_a, label_b),
    )
```

### Step 2.4: novel_writing.yaml 升级

**Modify**:
- Line 20: `version: 2  # ...` → `version: 3  # ...`
- Line 81-82: 改 description 和 token_budget/timeout_s
  - `description: "合并 2 个并行 polish 节点结果 (Phase 7.4 策略: max(len) 选较长版本)"` → `description: "合并 2 个并行 polish 节点结果 (Phase 7.5 策略: LLM S1-S8 多维加权评分, 选高者, 兜底 max(len))"`
- Line 86: `token_budget: 0` → `token_budget: 4000`
- Line 87: `timeout_s: 30` → `timeout_s: 90`

**完整 polish_merge 节点**:
```yaml
  # === Stage 3.7: AGGREGATION 合并 (Phase 7.4 + Phase 7.5 LLM 评分) ===
  - id: polish_merge
    type: aggregation
    name: Merge Polish Variants (LLM-Scored)
    description: "合并 2 个并行 polish 节点结果 (Phase 7.5 策略: LLM S1-S8 多维加权评分, 选高者, 兜底 max(len))"
    depends_on: [polish_emotional_pacing, polish_ai_trace_removal]
    prompt_scenario: polish_merge
    output_schema: PolishedDraft
    token_budget: 4000
    timeout_s: 90
```

### Step 2.5: GREEN 验证

```bash
# 1. 5 个新测试 + 1 handler test PASS
pytest tests/agent_system/test_master_controller.py -v
# Expected: 5 passed

pytest tests/agent_system/test_got_bridge.py::TestPolishMergeHandlerScored -v
# Expected: 1 passed

# 2. 现有测试不回归
pytest tests/agent_system/test_got_bridge.py -v
# Expected: 全部 PASS (含 Phase 7.4 3 个老 TestPolishMergeHandler)

pytest tests/agent_system/test_master_controller_stub_router_e2e.py -v
# Expected: 全部 PASS (含 test_at_least_seven_llm_calls + test_novel_writing_emit_depends_on_polish_merge_yaml_static)

# 3. 全量回归
pytest tests/ -q
# Expected: 2063 passed + 6 new = 2069 passed, 0 failed

# 4. ruff
ruff check .
# Expected: 0 warnings
```

**状态**: 6 测试 GREEN ✅, 全量 2069 passed ✅, ruff 0 ✅

---

## Task 3: Code Review + 1 commit + push

### Step 3.1: Code review (2-stage)

**Spec compliance review** (用 superpowers:spec-reviewer):
- 验证 6 新测试对应 spec §5 列出的 6 测试
- 验证 4 改 src 文件对应 spec §6 列出的 4 src 文件
- 验证韧性契约 (empty/identical/llm_fail 3 fallback)
- 验证 `_S1_S8_KEYS` / `_S1_S8_NAMES` 模块级常量已定义
- 验证 YAML version/token_budget/timeout 数值正确

**Code quality review** (用 superpowers:code-quality-reviewer):
- 检查 immutability (return new dict, 不 mutate input)
- 检查 error handling (try/except 兜底, logger.warning)
- 检查 type hints (Dict[str, Any] / tuple[str, str])
- 检查 ruff 0

### Step 3.2: 1 commit (含 spec 已分开 + 1 个 impl commit)

```bash
git add \
  infra/agent_system/agents/polisher/prompts.py \
  infra/agent_system/master_controller.py \
  infra/agent_system/got_bridge.py \
  infra/got/workflows/novel_writing.yaml \
  tests/agent_system/test_master_controller.py \
  tests/agent_system/test_got_bridge.py \
  tests/agent_system/test_master_controller_stub_router_e2e.py \
  tests/agent_system/test_master_controller_workflow.py \
  tests/agent_system/test_decision_integration.py

git commit -m "$(cat <<'EOF'
feat(agent_system): phase 7.5 — polish_merge LLM S1-S8 multi-dim weighted scoring

升级 polish_merge 从 Phase 7.4 max(len) 选最长到 LLM S1-S8 8 维评分
加权求总分选高者。HAIKU tier, 4000 tokens, 韧性兜底 max(len)。

- MasterController.polish_merge_synthesis: 8 维评分 + 等权 (0.125 each)
  + winner = 高者 (>= ties break to first)
- _S1_S8_KEYS / _S1_S8_NAMES: 模块级常量
- 韧性契约: empty_content / identical / llm_fail 3 路径走 len fallback
- polisher/prompts.py: +build_merge_synthesis_prompt +get_merge_synthesis_system_prompt
- got_bridge._handler_polish_merge: 1 upstream 走 single_upstream fallback,
  2+ upstream 调 master 评分
- YAML: version 2→3, polish_merge token_budget 0→4000, timeout 30→90

6 新测试 + 5 stub sync。Baseline 2063 → 2069 passed.
EOF
)"

git push origin master
```

---

## Task 4: MEMORY.md + 1 行 Phase 7.5 entry

**File**: `~/.claude/projects/-home-ailearn-projects-AI-Incursion-domains-IP---projects-LingWen/memory/MEMORY.md`

### Step 4.1: 查当前行数

```bash
wc -l MEMORY.md
```

**期望**: ~193 行 (Phase 7.4 末) — 加 1 行后应 < 200

### Step 4.2: 在 Phase 7.4 之后**插入** Phase 7.5 entry

**插入位置**: Phase 7.3 之后 / Phase 7.4 之前 (按时间序),或紧跟 Phase 7.4 之后。

**Entry 内容** (压缩版,保持简洁):

```markdown
- **Phase 7.5 (2026-06-04)**: Doc 4 §11 Phase 7.5 — polish_merge LLM S1-S8 多维加权评分 — MasterController.polish_merge_synthesis(content_a, content_b, labels) 8 维评分 + 等权 0.125 + winner = 高者 (>= ties break to first) + 韧性契约 (empty_content/identical/llm_fail 3 路径走 len fallback) + polisher/prompts.py 新增 build_merge_synthesis_prompt + get_merge_synthesis_system_prompt + _S1_S8_NAMES + master_controller._S1_S8_KEYS 模块级常量 + got_bridge._handler_polish_merge 改写 (1 upstream → single_upstream fallback, 2+ upstream → 调 master 评分) + YAML version 2→3, polish_merge token_budget 0→4000, timeout 30→90; 5 新测试 (test_master_controller.py TestPolishMergeSynthesis: winner_high_score/equal_scores_tie_to_first/empty_content_len_fallback/identical_content_fallback/llm_failure_len_fallback) + 1 handler test (TestPolishMergeHandlerScored) + 5 stub sync (test_got_bridge._StubMaster / test_master_controller_workflow._StubMaster / test_decision_integration._StubMaster / test_decision_api._StubMasterController / test_master_controller_stub_router_e2e — 加 polish_merge_synthesis 方法) + 2 yaml 静态收紧 (version=3, token_budget=4000) + 1 LLM 计数收紧 (>= 5 → >= 6 — write + audit + emotional_pacing×2 + ai_trace_removal×1 + polish_merge×1); 2063→2069 passed (+6), ruff 0; spec docs/superpowers/specs/2026-06-04-phase7.5-llm-scored-merge-design.md (commit e336642); plan docs/superpowers/plans/2026-06-04-phase7.5-llm-scored-merge.md; impl commit <hash> 推送 origin/master
```

### Step 4.3: 验证 < 200 行

```bash
wc -l MEMORY.md
# Expected: < 200 lines
```

---

## 完成定义 (Definition of Done)

- [ ] `polisher/prompts.py` 新增 `build_merge_synthesis_prompt` + `get_merge_synthesis_system_prompt` + `_S1_S8_NAMES`
- [ ] `master_controller.py` 新增 `polish_merge_synthesis` + 2 私有方法 + `_S1_S8_KEYS`
- [ ] `got_bridge.py` `_handler_polish_merge` 调 master 新方法
- [ ] `novel_writing.yaml` version 2→3, polish_merge token_budget 0→4000, timeout 30→90
- [ ] 5 个 stub 文件加 `polish_merge_synthesis` 方法
- [ ] 6 个新测试 PASS
- [ ] 现有 2063 tests 全 PASS
- [ ] `ruff check .` 0 warnings
- [ ] 1 impl commit pushed to origin/master
- [ ] MEMORY.md < 200 lines (+ 1 行 Phase 7.5 entry)
