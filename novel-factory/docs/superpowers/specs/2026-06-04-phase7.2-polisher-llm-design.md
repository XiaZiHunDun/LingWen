# Phase 7.2: PolisherTools LLM-化设计

**日期**: 2026-06-04
**范围**: Phase 7.2, Doc 4 §11 Phase 7 后续 (Phase 7.1 spec 跳过的候选 2)
**基线测试**: 2023 passed, 0 failed, 17 skipped
**目标测试**: ~2038 passed (+15 new tests for PolisherTools LLM 化)

---

## Context

Phase 7 (`test_master_controller_stub_router_e2e.py`, commit 671db78) 端到端验证了 `MasterController.run_workflow` → `AgentComputeFn` → master 方法 → Router → LLM 这条生产链。在 Phase 7 实施过程中,实现 subagent 暴露了 **3 个 production 代码 gap**:

| # | 位置 | 症状 | Phase 7.1 处理 | 本阶段处理 |
|---|------|------|--------------|----------|
| 1 | `master_controller.py:562` | `llm_audit` → 不存在 → AttributeError 吞 | ✅ 修复 | - |
| 2 | `agents/polisher/tools.py` | 不继承 `AgentBase`, 3 个方法是 no-op/规则 | 跳过 | ✅ **本阶段** |
| 3 | `got_bridge.py:_handler_chapter_review` | 不传 content 给下游 | ✅ 修复 | - |

本阶段(Phase 7.2)修复 **候选 2**: 把 `PolisherTools` LLM-化,让 `master.polish_chapter` → `polisher.polish_chapter` 实际走真实 LLM 路径(原链路只有 `remove_ai_gloss` 规则生效,`optimize_dialogue`/`adjust_pacing` 是 no-op)。

**主公决策** (2026-06-04 AskUserQuestion):
- 范围: **A. 最小核心** — PolisherTools 继承 AgentBase + 1 主方法 + 2 子方法
- 变体: **默认 + 可选 reader_id** — polish_chapter 接受可选 reader_id,默认 'A',可切换 20 个 reader 变体

---

## 架构

```
MasterController.polish_chapter(content) → str
  ↓
PolisherTools.polish_chapter(chapter_num, content, style_guide=None, reader_id=None) → dict {content, reader, issues}
  ├─ optimize_dialogue_llm(content, reader_id) → str    [LLM 路径]
  ├─ adjust_pacing_llm(content, reader_id) → str         [LLM 路径]
  └─ remove_ai_gloss(content) → str                      [规则路径 - 快速 + 兜底]
```

**链路现状 (修复前)**:
```
MasterController.polish_chapter(content)
  → polisher.remove_ai_gloss(content)        [规则生效]
  → polisher.optimize_dialogue(result)       [no-op, 不变]
  → return str
```

**链路目标 (修复后)**:
```
MasterController.polish_chapter(content)
  → polisher.polish_chapter(chapter_num=0, content=content)  [主入口]
       ├─ optimize_dialogue_llm(content)   [LLM 真实调用,失败 try/except 兜底]
       ├─ adjust_pacing_llm(content)        [LLM 真实调用,失败 try/except 兜底]
       └─ remove_ai_gloss(content)          [规则路径,快速]
       → return {"content": ..., "reader": "A", "issues": [...]}
  → return str(content)  [向后兼容, 取 result["content"]]
```

**契约保持**: `master.polish_chapter(content) → str` 签名不变,LLM 化的副产品(reader/issues)暂不暴露给上层,留待 Phase 7.3 (novel_writing.yaml 加 polish 节点) 再设计传出方式。

---

## 文件改动

| 文件 | 类型 | 说明 | LoC |
|------|------|------|-----|
| `infra/agent_system/agents/polisher/tools.py` | REWRITE | PolisherTools 继承 AgentBase + 3 LLM 方法 + 保留 2 规则方法 | 49 → ~180 |
| `infra/agent_system/agents/polisher/prompts.py` | NEW | 3 组 prompt 模板 (polish/dialogue/pacing) × 2 模板 (_build + _system) | ~150 |
| `tests/agent_system/test_polisher_llm.py` | NEW | 15 测试 (3 LLM 方法 + 1 主入口 + 韧性契约 + 变体集成) | ~280 |
| `MEMORY.md` | UPDATE | Phase 7.2 进度条目 | +1 |
| `infra/agent_system/master_controller.py:583-586` | MODIFY | 调 `self.polisher.polish_chapter(chapter_num=0, content=content)` 替代两步串联 | 2 行 |

**估计**: 1 重写 + 1 新 + 1 改 + 1 新测试 = 4 文件, ~612 行,1 commit, 2023 → 2038 passed (+15)。

---

## 组件设计

### 1. `infra/agent_system/agents/polisher/tools.py` (重写, 49L → ~180L)

```python
# novel-factory/agent_system/agents/polisher/tools.py
from typing import Any, Dict, List, Optional

from ..base import AgentBase

try:
    from .variant_loader import get_variant_loader
    VARIANT_LOADER_AVAILABLE = True
except ImportError:
    VARIANT_LOADER_AVAILABLE = False

from .prompts import (
    build_polish_prompt, get_polish_system_prompt,
    build_dialogue_prompt, get_dialogue_system_prompt,
    build_pacing_prompt, get_pacing_system_prompt,
)


class PolisherTools(AgentBase):
    """润色师工具集 — LLM 增强版 (Phase 7.2)

    继承 AgentBase 获得 LLM 集成能力。
    支持读者 A-T 变体配置,通过 reader_id 参数切换。
    主入口 polish_chapter 串联 3 个路径 (2 LLM + 1 规则):
        optimize_dialogue_llm → adjust_pacing_llm → remove_ai_gloss
    """

    def __init__(self, router=None):
        super().__init__(router)
        self._current_reader_id: Optional[str] = None
        if VARIANT_LOADER_AVAILABLE:
            self._variant_loader = get_variant_loader()

    # ==================== Reader 切换 ====================

    def set_reader(self, reader_id: str) -> None:
        self._current_reader_id = reader_id.upper()

    def get_current_reader(self) -> Optional[str]:
        return self._current_reader_id

    # ==================== 主入口 (Phase 7.2 NEW) ====================

    def polish_chapter(
        self,
        chapter_num: int,
        content: str,
        style_guide: Optional[Dict] = None,
        reader_id: Optional[str] = None,
    ) -> Dict[str, Any]:
        """LLM 化润色主入口

        串联 3 个路径,每个 LLM 调用 try/except 兜底,失败不致命。
        返回 dict: {content, reader, issues, chapter_num}
        """
        effective_reader = reader_id or self._current_reader_id or "A"
        polished = content
        issues: List[Dict] = []

        # 步骤 1: LLM 优化对话
        try:
            polished = self.optimize_dialogue_llm(polished, reader_id=effective_reader)
        except Exception as e:
            issues.append({"type": "dialogue_llm_fail", "severity": "P3", "issue": str(e)})

        # 步骤 2: LLM 调整节奏
        try:
            polished = self.adjust_pacing_llm(polished, reader_id=effective_reader)
        except Exception as e:
            issues.append({"type": "pacing_llm_fail", "severity": "P3", "issue": str(e)})

        # 步骤 3: 规则去 AI 痕迹 (快速 + 兜底,从不失败)
        polished = self.remove_ai_gloss(polished)

        return {
            "content": polished,
            "reader": effective_reader,
            "issues": issues,
            "chapter_num": chapter_num,
        }

    # ==================== LLM 子方法 (Phase 7.2 NEW) ====================

    def optimize_dialogue_llm(
        self,
        content: str,
        reader_id: Optional[str] = None,
    ) -> str:
        effective_reader = reader_id or self._current_reader_id or "A"
        prompt = build_dialogue_prompt(content, effective_reader)
        system = get_dialogue_system_prompt(effective_reader)
        return self.chat(prompt=prompt, system=system, temperature=0.5, max_tokens=3000)

    def adjust_pacing_llm(
        self,
        content: str,
        reader_id: Optional[str] = None,
    ) -> str:
        effective_reader = reader_id or self._current_reader_id or "A"
        prompt = build_pacing_prompt(content, effective_reader)
        system = get_pacing_system_prompt(effective_reader)
        return self.chat(prompt=prompt, system=system, temperature=0.4, max_tokens=3000)

    # ==================== 规则方法 (保留, 不破坏 test_agent_tools.py) ====================

    def remove_ai_gloss(self, content: str) -> str:
        """去除 AI 痕迹 — 规则路径, 快速 + 兜底"""
        replacements = [
            ("首先", ""), ("其次", ""), ("然后", ""),
            ("最后", ""), ("总之", ""), ("可以看出", ""),
        ]
        result = content
        for old, new in replacements:
            result = result.replace(old, new)
        return result

    def apply_style_guide(self, content: str, style_guide: Dict) -> str:
        """应用文风指南 — 规则路径 (out of scope, 不 LLM 化)"""
        result = content
        if style_guide.get("remove_filler"):
            result = self._remove_filler(result)
        return result

    def _remove_filler(self, content: str) -> str:
        filler_phrases = ["值得注意的是", "需要指出的是", "实际上"]
        result = content
        for phrase in filler_phrases:
            result = result.replace(phrase, "")
        return result
```

**为什么不 LLM 化 `apply_style_guide`**:
- 现有 `test_polisher_tools_apply_style` (test_agent_tools.py:108-114) 依赖 `apply_style_guide(content, style) → str` 签名
- 范围 A 选 "最小核心", apply_style_guide 是次要功能
- 后续 phase 可单独 LLM 化(可加 `_llm` 后缀)

**为什么 `polish_chapter` 返回 dict 而非 str**:
- 主公决策 A 是"主方法 polish_chapter",需要暴露 reader/issues 副产品
- dict 便于 GoT bridge / dashboard 后续消费
- master.polish_chapter 仍返回 str (取 result["content"])

**为什么 LLM 方法都 try/except 兜底**:
- 与 master.audit_chapter 模式一致 (`master_controller.py:562-585` 已有 try/except)
- Phase 7.1 韧性契约测试证明 try/except 保护 workflow 不崩溃
- Polisher LLM 失败不应让整章废掉,降级到规则路径

### 2. `infra/agent_system/agents/polisher/prompts.py` (NEW, ~150L)

```python
# novel-factory/agent_system/agents/polisher/prompts.py
"""润色师提示词模板 (Phase 7.2)

3 组模板,每组 1 个 builder + 1 个 system prompt:
- polish: 主入口 (聚合视角) — 暂未使用,保留供后续 phase
- dialogue: 对话自然化
- pacing: 节奏调整

每个 builder 接受 content + reader_id,返回组装好的 prompt 字符串。
reader_id 用于个性化(默认 'A' = 悬念铺设专长读者)。
"""

from typing import Optional

try:
    from .variant_loader import get_variant_loader
    VARIANT_LOADER_AVAILABLE = True
except ImportError:
    VARIANT_LOADER_AVAILABLE = False


def _load_variant_enhancement(reader_id: str, max_enhancements: int = 3) -> str:
    """从 reader 变体配置加载 prompt 增强片段"""
    if not VARIANT_LOADER_AVAILABLE:
        return ""
    try:
        loader = get_variant_loader()
        variant = loader.get_variant(reader_id)
    except Exception:
        return ""

    if not variant:
        return ""

    parts: list[str] = []
    enhancements = variant.get("feedback_preference", {}).get("focus_areas", [])
    if enhancements:
        parts.append(f"读者 {reader_id} 关注领域: {', '.join(enhancements[:max_enhancements])}")
    style = variant.get("style_expertise", {})
    if style:
        primary = style.get("primary", "")
        if primary:
            parts.append(f"读者 {reader_id} 专长: {primary}")
    return "\n".join(parts)


# ==================== Dialogue ====================

def build_dialogue_prompt(content: str, reader_id: str = "A") -> str:
    enhancement = _load_variant_enhancement(reader_id)
    enhancement_block = f"\n{enhancement}\n" if enhancement else ""
    return f"""请优化以下章节中的对话,使其更自然、更符合角色性格。

{enhancement_block}
## 原文
{content[:3000]}

## 要求
1. 保持原意和情节不变
2. 让对话更口语化、自然
3. 角色性格保持一致
4. 去除机械/AI 感

请直接返回优化后的全文 (不要解释)。"""


def get_dialogue_system_prompt(reader_id: str = "A") -> str:
    return f"""你是一位资深网文编辑(读者 {reader_id} 号视角),擅长对话润色。
你的工作:让对话更自然、更有"人味",去除 AI 痕迹,保持原作意图。
不要改变情节或角色行为,只调整语言风格。直接返回润色后全文。"""


# ==================== Pacing ====================

def build_pacing_prompt(content: str, reader_id: str = "A") -> str:
    enhancement = _load_variant_enhancement(reader_id)
    enhancement_block = f"\n{enhancement}\n" if enhancement else ""
    return f"""请调整以下章节的节奏,使其更有张力。

{enhancement_block}
## 原文
{content[:3000]}

## 要求
1. 保持原意和情节不变
2. 调整段落长度 (高潮紧凑 / 过渡舒缓)
3. 加强关键场景的冲击
4. 减少拖沓的过渡段

请直接返回调整后的全文 (不要解释)。"""


def get_pacing_system_prompt(reader_id: str = "A") -> str:
    return f"""你是一位资深网文编辑(读者 {reader_id} 号视角),擅长节奏控制。
你的工作:调整章节节奏,让高潮有力、过渡自然、张弛有度。
保持情节不变,只调整段落长度和叙述密度。直接返回调整后全文。"""


# ==================== Polish (主入口, 暂未使用, 保留) ====================

def build_polish_prompt(content: str, reader_id: str = "A") -> str:
    """主入口 prompt — 暂未使用, polish_chapter 串联子方法, 留待 Phase 7.3 完整版"""
    enhancement = _load_variant_enhancement(reader_id)
    enhancement_block = f"\n{enhancement}\n" if enhancement else ""
    return f"""请综合润色以下章节。

{enhancement_block}
## 原文
{content[:3000]}

请进行综合润色,返回全文。"""


def get_polish_system_prompt(reader_id: str = "A") -> str:
    return f"""你是读者 {reader_id} 号视角的资深网文编辑,擅长综合润色。"""
```

**为什么 `build_polish_prompt` 暂未使用**:
- 主公决策 A 是"最小核心",polish_chapter 串联子方法
- 完整版 polish prompt(单次 LLM 完成全润色)留待 Phase 7.3
- 但仍 export,因为 prompts.py 是模板文件,完整 API 提前备好

**为什么 `_load_variant_enhancement` 用 try/except**:
- 防御性: variant_loader 失败时返回空字符串,不阻塞主流程
- 现有 _build_audit_prompt 没用 try/except(假设加载器可用),这里更保守

### 3. `infra/agent_system/master_controller.py:583-586` (MODIFY, 2 行)

**Before**:
```python
def polish_chapter(self, content: str) -> str:
    """润色章节（委托给polisher）"""
    result = self.polisher.remove_ai_gloss(content)
    result = self.polisher.optimize_dialogue(result)
    return result
```

**After**:
```python
def polish_chapter(self, content: str) -> str:
    """润色章节（委托给polisher, LLM 化路径 Phase 7.2）"""
    result = self.polisher.polish_chapter(chapter_num=0, content=content)
    return result["content"]
```

**为什么 `chapter_num=0`**: master.polish_chapter 旧契约不传 chapter_num,用 0 表示"无章节号"。后续 Phase 7.3 (novel_writing.yaml 加 polish 节点) 会传真实 chapter_num。

**为什么不直接调 `polish_chapter` 返回 str**: polisher.polish_chapter 返回 dict 含 issues/reader,master 不需要但保留供后续 phase 扩展。最小破坏原则:master 签名保持 str,内部取 dict["content"]。

### 4. `tests/agent_system/test_polisher_llm.py` (NEW, ~280L, 15 tests)

**测试结构** (5 测试类 / 15 测试, 镜像 auditor 的 test 模式):

| # | 类 | 测试 | 覆盖 |
|---|----|------|------|
| 1 | `TestPolisherLLMInit` | `test_inherits_agent_base` | PolisherTools 继承 AgentBase |
| 2 | `TestPolisherLLMInit` | `test_no_router_enters_fallback_mode` | `PolisherTools()` 无 router → fallback_mode=True |
| 3 | `TestPolisherLLMInit` | `test_reader_id_default_and_set` | `set_reader()`/`get_current_reader()` 工作 |
| 4 | `TestDialogueLLM` | `test_optimize_dialogue_llm_calls_chat` | StubProvider 调 LLM 1 次, prompt 含 reader_id |
| 5 | `TestDialogueLLM` | `test_optimize_dialogue_uses_reader_variant_enhancement` | reader_id='B' 触发 _load_variant_enhancement |
| 6 | `TestDialogueLLM` | `test_optimize_dialogue_fallback_returns_placeholder` | 无 router 时返回 fallback string, 不抛错 |
| 7 | `TestPacingLLM` | `test_adjust_pacing_llm_calls_chat` | StubProvider 调 LLM 1 次 |
| 8 | `TestPacingLLM` | `test_adjust_pacing_temperature_is_lower` | pacing 温度 0.4 (低于 dialogue 0.5) |
| 9 | `TestPolishChapterMain` | `test_polish_chapter_chains_three_paths` | 主入口串联 3 路径, 返回 dict |
| 10 | `TestPolishChapterMain` | `test_polish_chapter_returns_dict_with_required_keys` | dict 含 content/reader/issues/chapter_num |
| 11 | `TestPolishChapterMain` | `test_polish_chapter_default_reader_is_A` | 不传 reader_id → reader='A' |
| 12 | `TestPolishChapterMain` | `test_polish_chapter_llm_failure_falls_back_to_rules` | **韧性契约**: LLM 抛错时 issues 记录但 content 仍产出 |
| 13 | `TestPolishChapterMain` | `test_polish_chapter_propagates_to_master_polish_chapter` | master.polish_chapter 调 polisher.polish_chapter (取 content) |
| 14 | `TestPolishChapterMain` | `test_polish_chapter_with_all_llm_paths_failing_still_returns_content` | 韧性契约: 两 LLM 全抛错时, content 仍产出 (来自规则), issues 记录 2 条 |
| 15 | `TestRulePaths` | `test_remove_ai_gloss_still_works` | 规则路径不破坏, fallback 模式可用 |

**Stub pattern** (复用 Phase 7 `_e2e_helpers` 模式):
```python
class _StubProvider:
    def __init__(self, response="LLM-stub-output"):
        self.response = response
        self.calls = []
    def generate(self, prompt, **kwargs):
        self.calls.append({"prompt": prompt, **kwargs})
        return self.response
```

**测试 #4 详细断言** (Fix #1 模式镜像):
```python
def test_optimize_dialogue_llm_calls_chat(monkeypatch, tmp_path):
    polisher = PolisherTools(router=_StubRouter("dialogue-output"))
    called = {"count": 0, "prompt": None}
    def fake_chat(prompt, system, temperature, max_tokens, **kwargs):
        called["count"] += 1
        called["prompt"] = prompt
        return "LLM 对话优化输出"
    monkeypatch.setattr(polisher, "chat", fake_chat)
    result = polisher.optimize_dialogue_llm("原文对话", reader_id="A")
    assert called["count"] == 1
    assert "对话" in called["prompt"]  # 提示词模板正确
    assert "读者 A" in called["prompt"]  # reader_id 透传
    assert result == "LLM 对话优化输出"
```

**测试 #12 韧性契约** (关键, 镜像 Phase 7.1 test_audit_chapter_failure):
```python
def test_polish_chapter_llm_failure_falls_back_to_rules(tmp_path):
    router = _StubRouter("")  # 模拟 LLM 不可用
    polisher = PolisherTools(router=router)
    # 让 optimize_dialogue_llm 抛错
    def boom(*a, **kw): raise RuntimeError("simulated LLM failure")
    polisher.optimize_dialogue_llm = boom

    result = polisher.polish_chapter(chapter_num=1, content="首先...其次...")

    # 关键断言: 不崩溃, content 仍产出 (来自 remove_ai_gloss 规则)
    assert "首先" not in result["content"]
    assert len(result["issues"]) == 1
    assert result["issues"][0]["type"] == "dialogue_llm_fail"
    assert result["reader"] == "A"
```

---

## 风险 + 缓解

| 风险 | 缓解 |
|------|------|
| PolisherTools 继承 AgentBase 后, `PolisherTools()` 无 router 现有测试不工作 | AgentBase `__init__(router=None)` 走 fallback_mode, LLM 方法返回 `_fallback_response` 占位符 (line 134); 规则方法 `remove_ai_gloss` / `apply_style_guide` 不依赖 router, 仍工作 |
| `test_polisher_tools_apply_style` 调用 `apply_style_guide` (本次不 LLM 化) | 保留原 `apply_style_guide` 方法不变 (out of scope) |
| `master.polish_chapter` 内部改 2 行, 可能影响其他测试 | 仅替换 2 步串联为 1 步 `polisher.polish_chapter()` + 取 `result["content"]`, 签名保持 `→ str` |
| LLM 子方法在 fallback 模式返回占位符, 与真实 LLM 路径行为不同 | 接受 (与 auditor 一致), 测试覆盖 fallback 模式 (`test_optimize_dialogue_fallback_returns_placeholder`) |
| `prompts.py` 拆分导致循环 import (tools ↔ prompts) | tools 顶层 import prompts, prompts 不 import tools (单向) |
| 20 个 reader 变体加载器失败时 prompt 模板降级 | `_load_variant_enhancement` try/except 返回空字符串 |
| LLM 失败频率高, polish_chapter 一直走规则路径 | 接受 (韧性优先), dashboard 后续可监控 LLM 失败率 |

---

## 验证

```bash
# 1. 跑新测试
pytest tests/agent_system/test_polisher_llm.py -v
# 期望: 15 passed

# 2. 跑 agent_system 全部
pytest tests/agent_system/ -q
# 期望: ~192 passed (177 + 15 新增)

# 3. 全量
pytest -q
# 期望: 2038 passed (2023 + 15)

# 4. ruff
ruff check infra/agent_system/agents/polisher/ tests/agent_system/test_polisher_llm.py
# 期望: All checks passed!

# 5. 验证 master.polish_chapter 集成
pytest tests/agent_system/ -k "polish" -v
# 期望: 现有 polish 测试 + 新增 15 测试, 全过
```

---

## Out of scope (后续 phases)

- **`apply_style_guide` LLM 化** (Phase 7.x) — 范围 A 选"最小核心", 仅保留规则
- **`polish_chapter_with_feedback` 方法** (Phase 7.x) — 返回 reader 反馈报告给 dashboard
- **20 reader 变体全集成** (Phase 7.x) — 当前仅 1 个 variant_enhancement 加载, 不迭代所有变体
- **`prompts.py` 完整版 polish prompt** (Phase 7.3) — 单次 LLM 完成全润色 (当前 polish_chapter 串联子方法)
- **novel_writing.yaml 加 polish 节点** (Phase 7.3) — 完整 4 节点 write→review→polish→emit 工作流
- **polish_chapter 进度暴露到 WebSocket** (Phase 6.5+) — 当前仅 dict 内部记录 issues

---

## Critical files (modified)

- `infra/agent_system/agents/polisher/tools.py` (49L → ~180L) — PolisherTools 继承 AgentBase + 3 LLM 方法
- `infra/agent_system/agents/polisher/prompts.py` (NEW, ~150L) — 3 组提示词模板
- `infra/agent_system/master_controller.py:583-586` (2 行改) — 调 polisher.polish_chapter 主入口
- `tests/agent_system/test_polisher_llm.py` (NEW, ~280L) — 15 测试
- `MEMORY.md` — Phase 7.2 进度条目

## Critical files (referenced)

- `infra/agent_system/agents/auditor/tools.py:14` — AuditorTools(AgentBase) 模板 (验证继承模式)
- `infra/agent_system/agents/auditor/tools.py:156-223` — _build_audit_prompt 模板风格
- `infra/agent_system/agents/base.py:43-97` — AgentBase.__init__ + chat() 接口
- `infra/agent_system/agents/polisher/variants/variant_a.yaml` — reader-a 变体配置参考
- `infra/agent_system/agents/polisher/variant_loader.py` — 现有 variant_loader (复用)
- `tests/agent_system/_e2e_helpers.py` — Phase 7 StubProvider 模式 (复用)
- `tests/agent_system/test_agent_tools.py:100-114` — 现有 polisher 测试 (依赖保留)
- `docs/superpowers/specs/2026-06-04-phase7.1-production-gap-fixes-design.md` — Phase 7.1 spec 跳过的候选 2 来源
