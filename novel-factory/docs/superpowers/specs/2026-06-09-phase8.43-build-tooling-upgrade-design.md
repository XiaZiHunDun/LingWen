# Phase 8.43 Build Tooling Upgrade — Design Spec

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this spec task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 5 build-tooling major version bumps (Vue 3.4→3.5, Vitest 4→5, @vue/test-utils 2→3, ESLint 8→9 + flat config, eslint-plugin-vue 9→10) in 1 super-phase, 7 sub-phases, 7 commits 推送 origin/master.

**Architecture:** 跟 Phase 8.35-8.42 super-phase 同模式 (1 spec + 1 plan + 7 sub-phase impl + 1 memory sync), 低 risk 先 / 高 risk 走 `--no-verify` 集中 (Q3 决策). 1 个 `eslint.config.js` 合并 3 个 `.eslintrc.cjs` (Phase 8.33+8.36+8.42 累计) 走 ESLint 9 flat config 1 入口.

**Tech Stack:** Vue 3.5 / Vitest 5 / @vue/test-utils 3 / ESLint 9 (flat config) / eslint-plugin-vue 10 / Node 20.20 / pnpm 9.15.0 (跟 Phase 8.40 corepack 兼容).

---

## Context

Phase 8.35-8.42 (7 commits 推送, 2026-06-09) 收尾 dashboard polish 8 sub-phases (byTestid helper / testid-class-sync lint / DecisionCard 7 kind testid / page-level spec / husky pre-commit / GitHub Actions CI / eslint-plugin-vue 集成), 累计:
- 33 data-testid (Phase 8.13+8.31+8.32+8.33+8.38 累)
- 16 vitest spec files / 95 tests
- 3 ESLint config (`.eslintrc.cjs` + `.eslintrc-testid-sync.cjs` + `.eslintrc-vue.cjs`) + 1 `husky@9.1.7` pre-commit hook + 1 GitHub Actions workflow (Phase 8.41)
- backend Python 2262 passed / 27 skipped / ruff 0
- frontend vitest 23/95 + pnpm build 0 error

**Phase 8.35-8.42 followup 列表** (`phases-8-dashboard.md` 累计):
- ESLint 9 迁移 (flat config 重写 3 .eslintrc.cjs)
- Vitest 5 升级
- @vue/test-utils 3 升级
- Vue 3.4→3.5
- vitest --coverage / coverage upload CI / lint-staged / backend pytest CI 整合 / pre-commit pytest hook / DecisionCard 4 status testid / OverviewPage e2e 加深 / TS strict

12+ followup items **跨 4 子系统**, 1 spec 不可包打。**主公决策 (2026-06-09 AskUserQuestion)**: 4 独立 super-phase 拆:
- **Super-phase A = Phase 8.43** (本 spec): build tooling 升级 (5 major bump)
- **Super-phase B = Phase 8.44+**: test infra (coverage / lint-staged / backend pytest CI / pre-commit pytest)
- **Super-phase C**: UI completeness (DecisionCard 4 status testid / OverviewPage e2e)
- **Super-phase D**: TS strict (加 tsconfig.json / strict: true / 修 11 .ts 类型错)

**当前状态** (2026-06-09, master @ c6a1e7f):
- `vue: ^3.4.0` (落后 1 major, Vue 3.5.x latest)
- `vitest: ^4.1.8` (落后 1 major, Vitest 5.0.x latest)
- `@vue/test-utils: ^2.4.11` (落后 1 major, 3.x latest)
- `eslint: ^8.57.0` (落后 1 major, ESLint 9.x latest + flat config required)
- `eslint-plugin-vue: ^9.33.0` (落后 1 major, 10.x latest + 兼容 ESLint 9)
- 0 `tsconfig.json` (Phase 8.43+ 留 Phase D 收尾, 0 本 phase scope)
- 0 `lint-staged` package (Phase B 收尾)
- 0 coverage 字段 in `vitest.config.js` (Phase B 收尾)
- 0 Playwright package (Phase C 留)
- `DashboardPage` 不存在 (App.vue 走 `OverviewPage` 作为 dashboard; Phase C 留)

---

## Goals

1. **5 major version bump** 在 1 super-phase 内完成, 7 commits 推送 origin/master
2. **1 个 `eslint.config.js`** 合并 3 个 `.eslintrc.cjs` (Phase 8.33+8.36+8.42 累计) 走 ESLint 9 flat config 1 入口
3. **`pnpm lint:all` 走 ESLint 9 入口** (3 段 spec / testid-sync / vue) 0 errors baseline 维持 (212 vue warnings + 10 testid fixture 软警告)
4. **0 改 .vue / 0 改 spec** (95 tests 走 mount() 基础 API, test-utils 3 兼容, Vue 3.5 0 新 API 走)
5. **0 改 baseline 维持**: 33 data-testid / 16 vitest spec / 9 historical spec/plan docs `:NNN` 行号 / 12 SCENARIOS 路由 / backend Python / composables / 3 stores / router / CLAUDE.md / pyproject.toml / pytest.ini / vitest.config.js 结构 (除加 `coverage/**` ignore + `pool: 'forks'`) / .state/*.db / .env
6. **pre-commit hook 维持** (除 2 commit `--no-verify` 显式注, 8.43.4 + 8.43.5 ESLint 迁移期 bypass)
7. **memory sync** 追加 Phase 8.43 entry 到 `phases-8-dashboard.md`

## Non-goals (留后续 super-phase / phase)

- **Super-phase B (Phase 8.44+)**: vitest --coverage 配置 + coverage upload CI + lint-staged 接入 (用 `pnpm lint:staged` alias) + backend pytest CI 整合进 dashboard-frontend-ci.yml + pre-commit pytest hook
- **Super-phase C (Phase 8.5+)**: DecisionCard 4 status testid (deferred 状态修正, 0 "expired") + OverviewPage e2e 加深 + Phase 8.30b dashboard.spec.ts 加 status testid
- **Super-phase D (Phase 8.6+)**: TS strict mode (加 `tsconfig.json` + `strict: true` + 修 11 .ts 文件)
- **Phase 9+ (独立大方向)**: 跨卷涟漪 (Phase 9 spec 已 commit 8cc1298, 留独立 phase)
- **Phase 10-14**: 跨卷涟漪 5 super-phase
- **MEMORY.md 拆**: `phases-8-dashboard.md` 累 Phase 8.43 后变 38+ entries, 接近 soft cap 180, 必拆 followup
- **CI 加 Node 22 matrix**: 0 改, Node 20 维持 (跟 Phase 8.40 corepack 兼容, 跟本地 dev 一致)
- **Vue 3.5 新 API 引入**: useTemplateRef / defineModel / useId 5+ 新 API 0 改 (Q3 决策, 留 followup)
- **test-utils 3 mount() 高级 API 改**: `findComponent` / `setValue` / `trigger` / `RouterLinkStub` 16 spec 检查 0 高级 API 依赖, 0 改
- **`@vue/server-renderer` bump**: 当前 ^3.5.35 (Vue 3.5 兼容, 0 改)
- **`echarts` / `mermaid` bump**: 0 改 (跟本 spec scope 无关)
- **`@types/node` ^25.x bump**: 当前最新, 0 改
- **`.eslintrc.cjs` legacy 兼容 (eslintrc.cjs 跟 eslint.config.js 共存)**: ESLint 9 flat config 是 native, 0 走 dual mode (Phase 8.43.6 删 3 旧 .cjs)

---

## Design

### 7 sub-phase 顺序 (Q5 决策: 低 risk 先)

| Sub | Action | Risk | Hook | `--no-verify` | 1 commit |
|---|---|---|---|---|---|
| **8.43.1** | Vue 3.4→3.5 (`vue: ^3.4.0` → `^3.5.0`) | 低 (backward compat) | ✅ 过 | 否 | 1 |
| **8.43.2** | Vitest 4→5 (`vitest: ^4.1.8` → `^5.0.0` + config 改) | 中 (config 改 + `pool: 'forks'`) | ✅ 过 | 否 | 1 |
| **8.43.3** | @vue/test-utils 2→3 (`^2.4.11` → `^3.0.0` + setup.ts 加 `enableAutoUnmount`) | 中 (95 tests mount 兼容性) | ✅ 过 | 否 | 1 |
| **8.43.4** | ESLint 8→9 + 1 个 `eslint.config.js` (3 段 spec/testid-sync/vue 合并) | 高 (flat config 重写) | ❌ | **是** | 1 |
| **8.43.5** | eslint-plugin-vue 9→10 (`^9.33.0` → `^10.x`) | 高 (兼容 ESLint 9) | ❌ | **是** | 1 |
| **8.43.6** | 改 `package.json` `lint:all` 走 ESLint 9 入口 + 删 3 `.eslintrc.cjs` + 综合 verify | 中 (lint baseline 维持) | ✅ 过 | 否 | 1 |
| **8.43.7** | Memory sync (`phases-8-dashboard.md` 追加 8.43 entry) + push 7 commits | 低 (纯文档) | ✅ 过 | 否 | 1 |

**总 7 commits, 2 commits 走 `--no-verify` (8.43.4 + 8.43.5 ESLint 迁移期 bypass, commit body 显式注 "8.43.X lint baseline 迁移期 bypass, 8.43.6 恢复 hook")**

### 8.43.1 Vue 3.4→3.5

- `package.json`: `"vue": "^3.4.0"` → `"vue": "^3.5.0"`
- `pnpm-lock.yaml` 锁文件更新
- 0 改 .vue / 0 改 spec / 0 改 ESLint config
- 0 用新 API (useTemplateRef / defineModel / useId 5+ API 留 followup)
- 验证: `pnpm vitest run` (23/95) + `pnpm build` (0 error)
- 1 commit message: `chore(deps): phase 8.43.1 — vue 3.4→3.5 (backward compat)`
- 0 改 baseline

### 8.43.2 Vitest 4→5

- `package.json`: `"vitest": "^4.1.8"` → `"vitest": "^5.0.0"`
- `vitest.config.js` 改:
  - `test.pool: 'forks'` (新增, jsdom fork-isolate 防 OOM, 跟 Phase 6.4 pytest forks 镜像)
  - `test.exclude`: 加 `'coverage/**'` (给 Phase 8.44+ coverage 留)
  - 其他字段 (environment / globals / setupFiles / include) 维持
- 0 改 setup.ts / 0 改 16 spec / 0 改 ESLint
- 验证: `pnpm vitest run` (23/95 baseline) + 跑 2 次 (防 flaky)
- 1 commit message: `chore(deps): phase 8.43.2 — vitest 4→5 + pool: forks + coverage ignore`
- 0 改 baseline

### 8.43.3 @vue/test-utils 2→3

- `package.json`: `"@vue/test-utils": "^2.4.11"` → `"@vue/test-utils": "^3.0.0"`
- `tests/unit/setup.ts` 加:
  ```ts
  import { afterEach } from 'vitest'
  import { enableAutoUnmount } from '@vue/test-utils'
  enableAutoUnmount(afterEach)
  ```
  (v3 推荐, 防 wrapper 泄漏, 跟 Vue 3.5 onUnmounted 同步)
- 0 改 95 tests signature (mount() 基础 API v2/v3 兼容)
- 0 改 `findComponent` / `setValue` / `trigger` (16 spec 检查 0 高级 API 依赖)
- 检查 16 spec import paths (`import { mount, ... } from '@vue/test-utils'` 维持)
- 验证: `pnpm vitest run` (23/95 + 16 spec 全过)
- 1 commit message: `chore(deps): phase 8.43.3 — @vue/test-utils 2→3 + enableAutoUnmount`
- 0 改 baseline (除 setup.ts 加 2 行)

### 8.43.4 ESLint 8→9 + 1 个 `eslint.config.js` (3 段)

- `package.json`: `"eslint": "^8.57.0"` → `"eslint": "^9.x.0"`
- 新文件 `dashboard/frontend/eslint.config.js` (~100L, single file, 3 blocks):

  ```js
  // eslint.config.js — Phase 8.43.4
  // 1 file, 3 blocks: spec dir (Phase 8.33) + .vue testid-sync (Phase 8.36) + .vue vue plugin (Phase 8.42)
  import vuePlugin from 'eslint-plugin-vue'
  import localRules from 'eslint-plugin-local-rules'
  import vueParser from 'vue-eslint-parser'
  import tsParser from '@typescript-eslint/parser'

  export default [
    // Global ignores
    { ignores: ['node_modules/**', 'dist/**', 'tests/e2e/**', 'coverage/**'] },

    // Block 1: Spec dir (Phase 8.33)
    {
      files: ['tests/unit/**/*.spec.ts', 'tests/fixtures/lint-testid/*.spec.ts'],
      languageOptions: { parser: tsParser, ecmaVersion: 2022, sourceType: 'module' },
      plugins: { 'local-rules': localRules },
      rules: { 'local-rules/no-class-selector-in-test': 'error' },
    },

    // Block 2: .vue testid-class-sync (Phase 8.36)
    {
      files: ['src/**/*.vue'],
      languageOptions: {
        parser: vueParser,
        parserOptions: { parser: tsParser, ecmaVersion: 2022, sourceType: 'module' },
      },
      plugins: { 'local-rules': localRules },
      rules: { 'local-rules/testid-class-sync': 'warn' },
    },

    // Block 3: .vue eslint-plugin-vue (Phase 8.42)
    {
      files: ['src/**/*.vue'],
      languageOptions: {
        parser: vueParser,
        parserOptions: { parser: tsParser, ecmaVersion: 2022, sourceType: 'module' },
      },
      plugins: { vue: vuePlugin },
      ...vuePlugin.configs['flat/vue3-recommended'],
      rules: {
        'vue/multi-word-component-names': 'error',
        'vue/no-v-html': 'warn',         // WorkflowGraph mermaid v-html
        'vue/require-default-prop': 'error',
        'vue/no-dupe-keys': 'warn',       // WorkflowGraph mermaid prop+import
      },
    },
  ]
  ```

- 0 改 package.json lint scripts (保留 ESLint 8 旧 scripts 0 改, hook 仍跑旧 entry 但 ESLint 8 已被卸装, 跑 0 通过 = bypass; 8.43.6 改新 entry)
- 0 删 3 .eslintrc.cjs (8.43.6 删)
- 验证: `npx eslint --version` 报 9.x, `node -e "import('./eslint.config.js').then(c => console.log(c.default.length))"` 报 3 (3 blocks)
- 1 commit message: `chore(lint): phase 8.43.4 — eslint 8→9 + flat config 3 段 (--no-verify, 8.43.6 恢复 hook)`
- 0 改 baseline (3 .eslintrc.cjs 仍存在但 ESLint 9 走 flat config 0 读)

### 8.43.5 eslint-plugin-vue 9→10

- `package.json`: `"eslint-plugin-vue": "^9.33.0"` → `"eslint-plugin-vue": "^10.x.0"`
- 0 改 `eslint.config.js` (Block 3 走 vue3-recommended, vue 10 兼容 flat config 1:1)
- 0 改 .vue / 0 改 spec
- 验证: `npx eslint --config eslint.config.js src/components/DecisionCard.vue` (Block 2 + 3 跑同一 vue, 0 errors expected, 跟 Phase 8.42 212 warnings baseline 维持)
- 1 commit message: `chore(lint): phase 8.43.5 — eslint-plugin-vue 9→10 (--no-verify, 8.43.6 恢复 hook)`
- 0 改 baseline

### 8.43.6 改 `pnpm lint:all` 走 ESLint 9 + 删 3 .eslintrc.cjs + 综合 verify

- `package.json` scripts 改:
  ```diff
  - "lint:testid": "eslint tests/unit/**/*.spec.ts tests/fixtures/lint-testid/*.spec.ts --no-eslintrc -c .eslintrc.cjs --no-error-on-unmatched-pattern",
  - "lint:testid-sync": "eslint --no-error-on-unmatched-pattern --no-eslintrc -c .eslintrc-testid-sync.cjs --rule '{\"testid-class-sync\": \"warn\"}' --rulesdir eslint-rules --ext .vue src/",
  - "lint:vue": "eslint --no-error-on-unmatched-pattern --no-eslintrc -c .eslintrc-vue.cjs --ext .vue src/",
  + "lint:testid": "eslint --config eslint.config.js tests/unit/**/*.spec.ts tests/fixtures/lint-testid/*.spec.ts --no-error-on-unmatched-pattern",
  + "lint:testid-sync": "eslint --config eslint.config.js src/**/*.vue",
  + "lint:vue": "eslint --config eslint.config.js src/**/*.vue",
  ```
  (注: `lint:testid-sync` + `lint:vue` 2 命令走同一 ESLint 9 invocation, Block 2 + 3 跑同一 vue glob, 0 重复 invocation. 0 改 lint 语义)

- 删 3 文件: `.eslintrc.cjs` + `.eslintrc-testid-sync.cjs` + `.eslintrc-vue.cjs`
- 7-step verify:
  1. `pnpm lint:all` exit 0 (0 errors, 212 vue warnings baseline 维持)
  2. `pnpm vitest run` 23/95 baseline
  3. `pnpm build` 0 error
  4. `grep -rEn 'data-testid=' src/ --include="*.vue" --include="*.js" | wc -l` = 46 (跟 Phase 8.42 baseline 一致)
  5. `cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory && pytest -q` 2262 passed 0 failed 27 skipped (backend 0 改)
  6. `ruff check .` All checks passed (backend 0 改)
  7. `git rev-parse HEAD origin/master` 2 行同 SHA (push 验证)
- 1 commit message: `chore(lint): phase 8.43.6 — pnpm lint:all 走 ESLint 9 入口 + 删 3 .eslintrc.cjs`
- 0 改 baseline (除 .eslintrc.cjs 删 3 文件)

### 8.43.7 Memory sync + push

- File: `~/.claude/projects/-home-ailearn-projects-AI-Incursion-domains-IP---projects-LingWen/memory/phases-8-dashboard.md`
- Edit H1: `# 灵文 · Phase 实施历史 (Phase 8.16-8.42, dashboard polish)` → `# 灵文 · Phase 实施历史 (Phase 8.16-8.43, dashboard polish)`
- Edit L3 quote: 加 `+ 5 build tooling major bump (Vue 3.5 / Vitest 5 / @vue/test-utils 3 / ESLint 9 + vue 10) flat config rewrite`
- Edit L4 quote: 加 `; 2026-06-09 Phase 8.43 7 commits 追加`
- Append `## Phase 8.43 (2026-06-09)` entry: 7 sub-phase 详情 + 2 commit `--no-verify` 显式注 + 5 major version bump 累 + 0 改 baseline 维持 (33 testid / 16 spec / 9 historical docs / 12 SCENARIOS) + out of scope (Phase 8.44+ Super-phase B/C/D 收尾)
- push 7 commits:
  ```bash
  cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
  git push origin master
  git rev-parse HEAD origin/master  # expect 2 行同 SHA
  ```
- 1 commit message: `docs: phase 8.43 memory sync + push 7 commits to origin/master`

---

## ESLint 9 flat config 1 文件 3 段结构 (Q2 决策)

`dashboard/frontend/eslint.config.js` 1 个文件包含 3 blocks:

| Block | 旧文件 (Phase 8.33/8.36/8.42) | 新 Block | Scope |
|---|---|---|---|
| Block 1 | `.eslintrc.cjs` (Phase 8.33) | tests/unit/**/*.spec.ts + tests/fixtures/lint-testid/*.spec.ts | spec dir |
| Block 2 | `.eslintrc-testid-sync.cjs` (Phase 8.36) | src/**/*.vue | .vue testid-class-sync |
| Block 3 | `.eslintrc-vue.cjs` (Phase 8.42) | src/**/*.vue | .vue vue plugin |

`pnpm lint:testid-sync` + `pnpm lint:vue` 2 命令合并到 1 个 ESLint 9 invocation (走同一 glob `src/**/*.vue`, Block 2 + 3 0 重复). 0 改 lint 语义 (3 rules 跟 7 warnings baseline 维持).

---

## pre-commit hook 生命周期 (Q3 决策, 跟 Q5 顺序 reconcile)

| Sub | lint:all 状态 | Hook | `--no-verify` |
|---|---|---|---|
| 8.43.1 Vue 3.5 | 走 ESLint 8 + vue 9, 0 改 | ✅ 过 | 否 |
| 8.43.2 Vitest 5 | 0 改 | ✅ 过 | 否 |
| 8.43.3 test-utils 3 | 0 改 | ✅ 过 | 否 |
| 8.43.4 ESLint 8→9 + flat config | **ESLint 8 BREAK** (flat config 跟 .eslintrc.cjs 互斥) | ❌ | **是** |
| 8.43.5 vue 9→10 | 仍 BREAK (vue 9 跟 ESLint 9 不兼容) | ❌ | **是** |
| 8.43.6 lint:all 走 ESLint 9 + 删 .eslintrc.cjs | 0 errors baseline 维持 | ✅ 过 | 否 |
| 8.43.7 memory sync | 0 改 .vue | ✅ 过 | 否 |

**2 commit 走 `--no-verify`** (8.43.4 + 8.43.5, commit body 显式注 "8.43.X lint baseline 迁移期 bypass, 8.43.6 恢复 hook"). 0 禁用 husky hook, 0 改 `.husky/pre-commit` 内容, 0 临时 commit. 跟 Phase 8.40 "hook 一旦装 0 旁路" 哲学的 唯一 例外, 显式留档 1 期.

---

## Risks + Mitigations

| Risk | Mitigation |
|---|---|
| 8.43.1 Vue 3.5 0 改 .vue 实际有 hidden breaking | vitest 23/95 + pnpm build 0 error 验证, 0 通过 revert |
| 8.43.2 Vitest 5 config API 错 (v4→v5 rename) | subagent 先跑 dry-run, 1 拆 2a (package.json only) + 2b (config fix); 跟 Phase 8.35-8.42 Task 1 救火模式同 |
| 8.43.3 test-utils 3 mount() rename 触发 95 test fail | 同上, 1 拆 3a (package.json only) + 3b (per-spec fix); 16 spec 检查 0 高级 API 依赖, 0 改 baseline 期望 |
| 8.43.4 ESLint 9 flat config syntax 错 | subagent 先 dry-run 验 `node -e "import('./eslint.config.js')..."`, 1 错 拆 4a (config fix) + 4b (rule import fix) |
| 8.43.5 vue 10 跟 ESLint 9 不兼容 | dry-run 验, fallback 走 vue 9 + ESLint 8 暂留, 改 spec 推迟 vue 10 (但本 spec 期望 vue 10 + ESLint 9 兼容, 实际 vue 10 9 月已发布) |
| 8.43.6 lint:all 改 ESLint 9 入口后 7 warnings baseline 漂 | dry-run 跑 Block 2 + 3 各自看 warnings count, 1 错 revert + 改 Block rule level |
| pre-commit hook 拦截 8.43.4 + 8.43.5 推不出 commit | 2 commit 走 `--no-verify` (Q3 决策, commit body 显式注) |
| 2 commit 走 `--no-verify` 被 code review 标记为 "bypass hook" | 0 禁用 husky hook, 0 改 `.husky/pre-commit` 内容, 0 临时 commit; 8.43.6 1 commit 恢复 hook, 后续 6 commit 全过 hook |
| 删 3 `.eslintrc.cjs` 触发别的 spec 引用 | 0 spec 引用 `.eslintrc.cjs` (Phase 8.33+8.36+8.42 spec 0 引用), grep 验证 |
| ESLint 9 `--no-error-on-unmatched-pattern` flag 失效 | ESLint 9 仍支持 legacy flags, 0 改; `--no-error-on-unmatched-pattern` 跟 ESLint 8 一致 |
| Vue 3.5 跟 `@vue/server-renderer ^3.5.35` 冲突 | `@vue/server-renderer` 已 3.5.35 (跟 Vue 3.5 兼容), 0 改 |
| Vitest 5 + `@vitest/coverage-v8` (Phase 8.44+) 配合 | Phase 8.43.2 留 `coverage/**` exclude 占位, Phase 8.44+ 接入时无需再改 vitest.config.js |
| `@types/node ^25.9.2` 跟 Node 20.20 冲突 | 0 改, 跟 Phase 8.40 一致 |
| `pnpm 11.5 fail on Node 20.20` (Phase 8.42 D1) | 装新 devDep 走 `corepack pnpm@9.15.0` workaround, 跟 Phase 8.40+8.42 pattern 一致 |
| `dashboard/protocols.py` / `dashboard/app.py` 0 改 | 0 改 backend, pytest 2262 baseline 维持 |
| `mermaid` 库 XSS surface (Phase 8.42 2 rule warn) | 维持 `vue/no-v-html: 'warn'` + `vue/no-dupe-keys: 'warn'`, 0 改 |

---

## Verification (主公 review 7 commits 后)

### Step 1: vitest baseline 维持

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
./node_modules/.bin/vitest run 2>&1 | tail -5
# 期望: 23 spec files / 95 tests passed (跟 Phase 8.42 baseline 一致)
```

### Step 2: pytest baseline 维持 (backend 0 改)

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pytest -q 2>&1 | tail -3
# 期望: 2262 passed, 27 skipped (跟 Phase 8.42 baseline 一致)
```

### Step 3: ruff 0 issues (backend 0 改)

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
ruff check . 2>&1 | tail -3
# 期望: All checks passed!
```

### Step 4: 3 lint 命令 0 errors baseline

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
pnpm lint:testid 2>&1 | tail -3
# 期望: 10 errors (Phase 8.33 软 fixture 维持)
pnpm lint:testid-sync 2>&1 | tail -3
# 期望: 0 errors, 7 warnings (跟 Phase 8.36 baseline 一致)
pnpm lint:vue 2>&1 | tail -3
# 期望: 0 errors, 212 warnings (跟 Phase 8.42 baseline 一致)
pnpm lint:all
echo "EXIT=$?"
# 期望: EXIT=0 (跟 Phase 8.40+8.42 policy 一致)
```

### Step 5: pnpm build 0 error

```bash
pnpm build 2>&1 | tail -3
# 期望: dist/ emitted, 0 error
```

### Step 6: data-testid 数量 维持 46

```bash
grep -rEn 'data-testid=' src/ --include="*.vue" --include="*.js" | wc -l
# 期望: 46 (跟 Phase 8.42 baseline 一致)
```

### Step 7: ESLint 9 version

```bash
./node_modules/.bin/eslint --version
# 期望: v9.x.x
./node_modules/.bin/vitest --version
# 期望: 5.x.x (Vitest 5)
```

### Step 8: git status clean + 7 commits

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git status
# 期望: clean
git log --oneline -8
# 期望: 7 Phase 8.43 commits 落 local
git rev-parse HEAD origin/master
# 期望: 2 行同 SHA (push 验证)
```

### Step 9: pre-commit hook 恢复 (8.43.6 后)

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
cat .husky/pre-commit
# 期望: pnpm lint:all (跟 Phase 8.40 一致)
ls -la .husky/pre-commit
# 期望: mode 100755 (可执行位维持)
```

### Step 10: 0 Co-Authored-By trailers (跟 Phase 8.35-8.42 policy 一致)

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git log -7 --format='%(trailers:key=Co-Authored-By,valueonly)'
# 期望: 7 行空 (0 Co-Authored-By)
```

---

## Critical files

### Created
- `docs/superpowers/specs/2026-06-09-phase8.43-build-tooling-upgrade-design.md` (本 spec)
- `docs/superpowers/plans/2026-06-09-phase8.43-build-tooling-upgrade.md` (后续 writing-plans skill 写)
- `dashboard/frontend/eslint.config.js` (~100L, NEW) — 1 file 3 blocks 替换 3 .eslintrc.cjs

### Modified (5 devDep bump + config 调整)
- `dashboard/frontend/package.json` (5 devDep bump + lint scripts 改)
- `dashboard/frontend/pnpm-lock.yaml` (5 new devDep 锁文件)
- `dashboard/frontend/vitest.config.js` (+ `pool: 'forks'` + `coverage/**` exclude)
- `dashboard/frontend/tests/unit/setup.ts` (+ `enableAutoUnmount` 2 行)

### Deleted (8.43.6)
- `dashboard/frontend/.eslintrc.cjs` (Phase 8.33)
- `dashboard/frontend/.eslintrc-testid-sync.cjs` (Phase 8.36)
- `dashboard/frontend/.eslintrc-vue.cjs` (Phase 8.42)

### Memory (8.43.7)
- `~/.claude/projects/-home-ailearn-projects-AI-Incursion-domains-IP---projects-LingWen/memory/phases-8-dashboard.md` (H1 + L3 + L4 edit + 8.43 entry append)

### NOT modified
- 14 .vue in `dashboard/frontend/src/` (Vue 3.5 0 改)
- 16 vitest spec in `dashboard/frontend/tests/unit/` (test-utils 3 0 改 signature)
- `dashboard/frontend/eslint-rules/testid-class-sync.js` (Phase 8.36, 0 改)
- `dashboard/frontend/eslint-rules/no-class-selector-in-test.js` (Phase 8.33, 0 改)
- `dashboard/frontend/src/composables/` (5 composable, 0 改)
- `dashboard/frontend/src/api/index.js` (0 改)
- `dashboard/frontend/.husky/pre-commit` (Phase 8.40, 0 改)
- `dashboard/frontend/index.html` / `vite.config.js` (0 改)
- `dashboard/frontend/tests/fixtures/lint-testid/{clean,dirty}.spec.ts` (Phase 8.33 fixture, 0 改)
- `dashboard/frontend/tests/helpers/by-testid.ts` (Phase 8.35 helper, 0 改)
- backend Python (agent_system / dashboard / cost_tracker / cost_persistence / cost_router)
- 9 historical spec/plan docs (`:NNN` 行号不动)
- 12 SCENARIOS 路由 (Python side)
- CLAUDE.md / pyproject.toml / pytest.ini / .gitignore / 14 historical memory files
- `.github/workflows/dashboard-frontend-ci.yml` (Phase 8.41, 0 改 — Node 20 + pnpm 9 维持)
- `.state/*.db` (gitignored, 0 触碰)
- `.env` (0 改 0 泄漏)

---

## Out of scope (后续 super-phase / phase)

- **Phase 8.44+ (Super-phase B)**: vitest --coverage + coverage upload CI + lint-staged 接入 + backend pytest CI 整合 + pre-commit pytest hook
- **Phase 8.5+ (Super-phase C)**: DecisionCard 4 status testid (deferred 状态修正) + OverviewPage e2e 加深 + Phase 8.30b dashboard.spec.ts 加 status testid
- **Phase 8.6+ (Super-phase D)**: TS strict mode (加 tsconfig.json + strict: true + 修 11 .ts)
- **Phase 9 (跨卷涟漪)**: spec 已 commit 8cc1298, 留独立 phase
- **Phase 10-14 (跨卷涟漪 5 super-phase)**: 留未来
- **MEMORY.md 拆**: `phases-8-dashboard.md` 累 Phase 8.43 后变 38+ entries, 接近 soft cap 180, 必拆 followup
- **CI 加 Node 22 matrix**: 0 改, Node 20 维持
- **Vue 3.5 新 API 引入**: useTemplateRef / defineModel / useId 5+ API 留 followup
- **test-utils 3 高级 API 改**: 16 spec 检查 0 高级 API 依赖, 0 改
- **`@vue/server-renderer` bump**: 当前 ^3.5.35, 0 改
- **`echarts` / `mermaid` bump**: 0 改
- **`@types/node` bump**: 当前 ^25.9.2, 0 改
- **`.eslintrc.cjs` legacy 兼容**: ESLint 9 flat config native, 0 走 dual mode (Phase 8.43.6 删 3 旧 .cjs)
- **`vue-tsc` bump / TS check 加 CI**: 留 followup

---

## 完成定义 (DoD)

- [ ] `docs/superpowers/specs/2026-06-09-phase8.43-build-tooling-upgrade-design.md` 创建 + commit
- [ ] `docs/superpowers/plans/2026-06-09-phase8.43-build-tooling-upgrade.md` 创建 + commit (writing-plans skill)
- [ ] `dashboard/frontend/eslint.config.js` 1 file 3 blocks 创建 (8.43.4)
- [ ] 3 `.eslintrc.cjs` 删 (8.43.6)
- [ ] 5 devDep bump: vue ^3.5 / vitest ^5.0 / @vue/test-utils ^3.0 / eslint ^9.0 / eslint-plugin-vue ^10.0
- [ ] `vitest.config.js` `pool: 'forks'` + `coverage/**` exclude
- [ ] `tests/unit/setup.ts` `enableAutoUnmount(afterEach)` 加
- [ ] `pnpm lint:all` 改 ESLint 9 入口 (8.43.6)
- [ ] 7 commits 推送 origin/master (1 per sub-phase)
- [ ] 2 commit `--no-verify` 显式注 (8.43.4 + 8.43.5)
- [ ] vitest 23/95 passed
- [ ] pytest 2262 passed 0 failed 27 skipped
- [ ] ruff 0 issues
- [ ] pnpm build 0 error
- [ ] pnpm lint:all 0 errors baseline 维持 (212 vue warnings + 10 testid fixture 软警告 + 7 testid-sync warnings)
- [ ] git rev-parse HEAD origin/master 2 行同 SHA
- [ ] pre-commit hook 恢复 (`.husky/pre-commit` 0 改)
- [ ] memory sync `phases-8-dashboard.md` H1 + L3 + L4 edit + 8.43 entry append
- [ ] 0 改 .vue / 0 改 16 vitest spec / 0 改 33 data-testid / 0 改 backend Python / 0 改 9 historical spec/plan docs / 0 改 12 SCENARIOS / 0 改 CLAUDE.md / pyproject.toml / pytest.ini / vitest.config.js 结构 / .state/*.db / .env
- [ ] 0 force-push / 0 amend 除 sub-phase fail 救火 (Q5 risk mitigation 显式 1 拆 2) / 0 Co-Authored-By / 0 DANGEROUSLY_SKIP_PERMISSIONS / 0 误改 .env / 0 API key 泄漏

---

## 后续 super-phase 启动

- **Phase 8.44 (Super-phase B 启动)**: test infra (vitest --coverage + coverage upload CI + lint-staged + backend pytest CI 整合 + pre-commit pytest)
- **Phase 8.5 (Super-phase C 启动)**: UI completeness (DecisionCard 4 status testid + OverviewPage e2e)
- **Phase 8.6 (Super-phase D 启动)**: TS strict
- **Phase 9 (跨卷涟漪 启动)**: 独立大方向
- **MEMORY.md 拆 38+ entries to phases.md topic**: 必拆 followup, 留独立 phase
