# Phase 9.23 Spec — Cascade Run Filter UI

> **Goal:** 在 `CascadeRunsPanel` 加 3 filter (status + min/max depth + algorithm), URL search params 是 single source of truth, 服务端走 `get_ripple_cascade_runs` 新 query params.
> **Architecture:** 4 layers, 全部 additive: storage 加 3 SQL WHERE clauses → endpoint 加 4 `Query()` params (status 从 storage 暴露 + 3 new) → API client 扩展 `fetchCascadeRuns` options → 新 `CascadeRunsFilter.vue` + `CascadeRunsPanel.vue` route watcher. 0 改 Phase 9.22 既有 behavior.
> **Tech Stack:** Vue 3 SFC + Vue Router 4 (`useRoute` / `useRouter`) + FastAPI 0.x (`Query` / `Literal`) + Pydantic v2 + Vitest + Playwright e2e + pytest + ruff + pnpm build.

---

## Context

Phase 9.22 完成 `CascadeRunsPanel` 表格 + Replay + Cancel + WS 实时更新. **问题**: 表格一次性拉所有 runs (default limit 50), 用户无法过滤. 当 1 个 ripple 有 60+ runs (典型 cascade-heavy 章节), 找 cancelled/failed 状态要肉眼扫表. 主公需要 server-side filter (避免只 filter 内存中 50 行) + UI + URL 同步 (shareable, bookmarkable).

**承接 followup roadmap F7 (Phase 9.23)**:
- `2026-06-11-followup-roadmap.md` F7: "cascade run 过滤 (by status / algorithm / depth range) ... `?status=cancelled&min_depth=2&max_depth=5&algorithm=v2_weighted` query params (all additive)"
- **修正 Phase 9.22 spec 误判**: Phase 9.22 spec 写 "server-side filter Phase 9.20 endpoint 已就位", 实际 `get_ripple_cascade_runs` endpoint signature **仅**支持 `limit`/`offset`, `status` filter 在 `storage.get_cascade_runs` 已有但未暴露, `min_depth`/`max_depth`/`algorithm` 0 实现. Phase 9.23 需 backend + storage + UI 3 段一起做.

**主公决策** (2026-06-11 AskUserQuestion × 3):
- 3 filter dimensions: status + min/max depth + algorithm
- Depth UI: min + max 2 个 number input (range)
- Approach: A (Backend + URL state, server-side filter)

**现状 baseline** (per Phase 9.22 commit `a974e65`, 2026-06-11):
- 测试: 2469 pytest + 159 vitest + 12 e2e = 2640 total, 0 failed, 27 skipped
- `RippleStorage.get_cascade_runs(ripple_id, limit, offset, status)` — `status` 已就位, `min_depth`/`max_depth`/`algorithm` 缺
- `dashboard/app.py:get_ripple_cascade_runs(ripple_id, limit, offset)` — 仅 2 Query() params
- `fetchCascadeRuns(rippleId, options={limit, offset, status})` — 3 options 透传
- `CascadeRunsPanel.vue` (180L) 单一 fetch, 无 filter UI
- `RippleFilter.vue` (60L) v-model + 3 dropdown 模式 (供新 filter 组件参考)
- 0 改 Phase 9.19/9.20/9.21/9.22 既有 production code behavior

## Goals

1. **Storage** (`infra/cross_volume/storage.py:get_cascade_runs`) — 加 `min_depth` / `max_depth` / `algorithm` 3 optional params, 3 new SQL WHERE clauses, 0 改既有 `status` 路径
2. **Endpoint** (`dashboard/app.py:get_ripple_cascade_runs`) — 加 4 `Query()` params (status 从 storage 暴露 + 3 new), Pydantic `ge=1, le=10` + `regex="^(v1|v2_weighted)$"` 校验, 0 改既有 `limit`/`offset` 路径
3. **API client** (`dashboard/frontend/src/api/index.js:fetchCascadeRuns`) — options 加 `minDepth` / `maxDepth` / `algorithm`, 透传 `URLSearchParams` (camelCase → snake_case URL)
4. **NEW: `CascadeRunsFilter.vue`** (~80L) — 3 dropdowns + 2 number inputs + 1 conditional "Reset" button, v-model 风格 (modelValue + update:modelValue)
5. **`CascadeRunsPanel.vue`** (+60L) — route watcher 单向同步 URL → state, 250ms debounce on number input, dropdown 0 debounce, 0 改 Phase 9.22 既有 3 vitest 测试契约
6. **Empty state copy** — 区分 "no runs yet" vs "no runs match these filters" (跟 `hasActiveFilter` 联动)
7. **16-19 new tests** — 3-4 storage + 5 endpoint + 4-5 vitest filter + 2-3 vitest panel URL sync + 2 e2e

## Non-Goals

- **0 cooperative BFS cancel** (Phase 9.21 已就位, YAGNI)
- **0 cascade_runs schema change** (YAGNI: 算法/状态/depth 都在既有列)
- **0 改 Phase 9.19/9.20/9.21/9.22 既有 production code behavior** (additive only)
- **0 改 `fetchCascadeRuns` 既有 signature 破坏性** (新 options key, 旧 caller 零修改继续 work)
- **0 改 `RippleDrawer.vue`** (filter 是 CascadeRunsPanel 内部 state, drawer 0 改)
- **0 改 `CascadeGraph.vue`** (Phase 9.15 既有, 0 触碰)
- **0 改既有 `fetchRippleCascade` 等 api methods** (单点扩 `fetchCascadeRuns` 即可)
- **0 全局 filter 跨 page / 跨 drawer tab** (per-ripple tab-internal)
- **0 filter 持久化到 localStorage** (URL 足够, 浏览器 history 已能恢复)
- **0 saved filter presets / fuzzy match / sort options** (YAGNI, 后续 phase)
- **0 pagination UI** (limit=50 default, filter 缩小结果集足够, 95% use case)
- **0 改 backend `cascade_runs` 表 column** (YAGNI)
- **0 改 `dashboard/protocols.py:CascadeRunResponse`** (0 schema 改)
- **0 真实 LLM 调用 / 0 API key**
- **0 改 historical spec/plan doc `:NNN` 行号**

## Design

### 1. Storage — `RippleStorage.get_cascade_runs` 扩 3 params

**File**: `infra/cross_volume/storage.py:742-768`

```python
def get_cascade_runs(
    self,
    ripple_id: str,
    limit: int = 50,
    offset: int = 0,
    status: str | None = None,
    min_depth: int | None = None,
    max_depth: int | None = None,
    algorithm: str | None = None,
) -> list[CascadeRun]:
    """Phase 9.20: list cascade runs for ripple_id (latest first).
    Phase 9.23: add 3 filter params (min_depth, max_depth, algorithm).

    Args:
        ripple_id: filter by ripple
        limit: max rows (default 50)
        offset: pagination offset
        status: optional filter ('running'/'completed'/'cancelled'/'failed')
        min_depth: optional min max_depth filter (inclusive, Phase 9.23)
        max_depth: optional max max_depth filter (inclusive, Phase 9.23)
        algorithm: optional algorithm filter (Phase 9.23, exact match on `algorithm` column)

    Returns:
        list of CascadeRun, ordered by id DESC
    """
    clauses, params = ["ripple_id = ?"], [ripple_id]
    if status is not None:
        clauses.append("status = ?")
        params.append(status)
    if min_depth is not None:
        clauses.append("max_depth >= ?")
        params.append(min_depth)
    if max_depth is not None:
        clauses.append("max_depth <= ?")
        params.append(max_depth)
    if algorithm is not None:
        clauses.append("algorithm = ?")
        params.append(algorithm)
    where = f"WHERE {' AND '.join(clauses)}"
    sql = f"SELECT * FROM cascade_runs {where} ORDER BY id DESC LIMIT ? OFFSET ?"
    params.extend([limit, offset])
    with self._connect() as conn:
        return [self._row_to_cascade_run(row) for row in conn.execute(sql, params)]
```

**设计决策**:
- `min_depth`/`max_depth` 都是 **inclusive** (`>=`/`<=`) — 用户期望 "depth 2 到 5" = 包含 2 和 5
- `algorithm` 跟既有 `status` 模式 1:1, exact match (column 名 = `algorithm`, `CascadeRunResponse.algorithm`; 注意 `bfs_algorithm_version` 是 cascade preview 的不同 field, 1:1 naming 不混)
- 3 param 都 `None` default → 旧 caller (Phase 9.20/9.22 既有 test) 0 破

### 2. Endpoint — `get_ripple_cascade_runs` 扩 4 Query() params (status + 3 new)

**File**: `dashboard/app.py:1127-1146`

```python
@app.get(
    "/api/ripples/cascade/{ripple_id}/runs",
    response_model=list[CascadeRunResponse],
)
def get_ripple_cascade_runs(
    ripple_id: str,
    limit: int = Query(default=50, ge=1, le=200),
    offset: int = Query(default=0, ge=0),
    status: str | None = Query(default=None, regex="^(running|completed|cancelled|failed)$"),
    min_depth: int | None = Query(default=None, ge=1, le=10),
    max_depth: int | None = Query(default=None, ge=1, le=10),
    algorithm: str | None = Query(default=None, regex="^(v1|v2_weighted)$"),
) -> list[CascadeRunResponse]:
    """Phase 9.20: list historical cascade runs for a ripple (latest first).
    Phase 9.23: add 3 query params (status, min_depth, max_depth, algorithm).

    limit: max rows (1..200, default 50).
    offset: pagination offset (≥0, default 0).
    status: optional filter ('running'/'completed'/'cancelled'/'failed').
    min_depth: optional min max_depth filter (1..10, inclusive).
    max_depth: optional max max_depth filter (1..10, inclusive).
    algorithm: optional algorithm filter ('v1' or 'v2_weighted').

    Returns:
        list of CascadeRunResponse, ordered by id DESC (newest first).
    """
    storage = _default_storage()
    runs = storage.get_cascade_runs(
        ripple_id, limit=limit, offset=offset,
        status=status, min_depth=min_depth, max_depth=max_depth, algorithm=algorithm,
    )
    return [CascadeRunResponse.from_dataclass(r) for r in runs]
```

**设计决策**:
- `status` 新加 (Phase 9.22 spec 误判, 实际 endpoint 0 暴露; 跟 storage 对齐 1:1)
- `regex=` 而非 `Literal=` (跟 Phase 9.20 既 endpoint 风格 1:1, FastAPI 接受)
- 4 new params 全部 optional + `None` default → 旧 caller 0 破
- Invalid value (e.g. `?min_depth=20`) → 422 (FastAPI default), 跟 Phase 9.19 `_validate_max_depth` 错误码一致

### 3. API client — `fetchCascadeRuns` 扩 3 options

**File**: `dashboard/frontend/src/api/index.js:341-348`

```javascript
/**
 * Phase 9.22: fetch historical cascade_runs for a ripple (Phase 9.20 endpoint).
 * Phase 9.23: options extended with minDepth, maxDepth, algorithm (all optional).
 * Returns CascadeRun[] JSON array sorted by id DESC.
 * @param {string} rippleId
 * @param {object} [options] - {
 *   limit?: number, offset?: number,
 *   status?: 'running'|'completed'|'cancelled'|'failed',
 *   minDepth?: number, maxDepth?: number,    // Phase 9.23
 *   algorithm?: 'v1'|'v2_weighted',            // Phase 9.23
 * }
 * @returns {Promise<Array<...>>}
 */
export async function fetchCascadeRuns(rippleId, options = {}) {
  const params = new URLSearchParams();
  if (options.limit) params.set('limit', String(options.limit));
  if (options.offset) params.set('offset', String(options.offset));
  if (options.status) params.set('status', options.status);
  if (options.minDepth) params.set('min_depth', String(options.minDepth));
  if (options.maxDepth) params.set('max_depth', String(options.maxDepth));
  if (options.algorithm) params.set('algorithm', options.algorithm);
  const qs = params.toString();
  return request(`/ripples/cascade/${encodeURIComponent(rippleId)}/runs${qs ? '?' + qs : ''}`);
}
```

**设计决策**:
- camelCase JS ↔ snake_case URL (跟 Phase 9.20 `?status=` 既透传 1:1)
- 0 改 `cancelCascadeRun` / `fetchCascadeWithDepth` 既有 signature
- 旧 caller (Phase 9.22 既有 vitest/e2e 0 改) 继续 work (options 是 optional)

### 4. NEW: `CascadeRunsFilter.vue` (~80L)

**File**: `dashboard/frontend/src/components/CascadeRunsFilter.vue` (NEW)

```vue
<template>
  <div class="cascade-runs-filter" data-testid="cascade-runs-filter">
    <label class="cascade-runs-filter-label">
      Status:
      <select
        :value="modelValue.status"
        @change="emitChange('status', $event.target.value)"
        data-testid="filter-status"
        class="filter-status"
      >
        <option value="all">all</option>
        <option value="running">running</option>
        <option value="completed">completed</option>
        <option value="cancelled">cancelled</option>
        <option value="failed">failed</option>
      </select>
    </label>

    <label class="cascade-runs-filter-label">
      Depth:
      <input
        type="number" min="1" max="10"
        :value="modelValue.minDepth ?? ''"
        placeholder="min"
        @input="emitNumber('minDepth', $event.target.value)"
        data-testid="filter-min-depth"
        class="filter-min-depth"
      />
      <span class="filter-depth-sep">–</span>
      <input
        type="number" min="1" max="10"
        :value="modelValue.maxDepth ?? ''"
        placeholder="max"
        @input="emitNumber('maxDepth', $event.target.value)"
        data-testid="filter-max-depth"
        class="filter-max-depth"
      />
    </label>

    <label class="cascade-runs-filter-label">
      Algorithm:
      <select
        :value="modelValue.algorithm"
        @change="emitChange('algorithm', $event.target.value)"
        data-testid="filter-algorithm"
        class="filter-algorithm"
      >
        <option value="all">all</option>
        <option value="v1">v1</option>
        <option value="v2_weighted">v2_weighted</option>
      </select>
    </label>

    <button
      v-if="hasActiveFilter"
      type="button"
      @click="emit('update:modelValue', defaultFilters())"
      data-testid="filter-reset"
      class="filter-reset"
    >Reset</button>
  </div>
</template>

<script setup>
import { computed } from 'vue';

const props = defineProps({
  modelValue: {
    type: Object,
    required: true,
    validator: (v) => ['status', 'minDepth', 'maxDepth', 'algorithm'].every((k) => k in v),
  },
});
const emit = defineEmits(['update:modelValue']);

function defaultFilters() {
  return { status: 'all', minDepth: null, maxDepth: null, algorithm: 'all' };
}

const hasActiveFilter = computed(() =>
  props.modelValue.status !== 'all' ||
  props.modelValue.minDepth != null ||
  props.modelValue.maxDepth != null ||
  props.modelValue.algorithm !== 'all'
);

function emitChange(key, value) {
  emit('update:modelValue', { ...props.modelValue, [key]: value });
}

function emitNumber(key, raw) {
  const n = raw === '' ? null : Number(raw);
  // Defensive: ignore NaN / out-of-range (parent's debounce + URL parser handle too)
  if (n !== null && (!Number.isFinite(n) || n < 1 || n > 10)) return;
  emit('update:modelValue', { ...props.modelValue, [key]: n });
}
</script>

<style scoped>
.cascade-runs-filter {
  display: flex; gap: 16px; margin-bottom: 12px; align-items: center;
  flex-wrap: wrap; font-size: 0.85em;
}
.cascade-runs-filter-label { display: flex; align-items: center; gap: 6px; }
.cascade-runs-filter select, .cascade-runs-filter input {
  padding: 4px 8px; border-radius: 4px; border: 1px solid #ccc;
}
.cascade-runs-filter input[type="number"] { width: 60px; }
.filter-depth-sep { color: #666; }
.filter-reset {
  padding: 4px 10px; border: 1px solid #c0392b; background: #fff; color: #c0392b;
  border-radius: 4px; cursor: pointer; font-size: 0.85em;
}
.filter-reset:hover { background: #c0392b; color: #fff; }
</style>
```

**设计决策**:
- 3 dropdown + 2 number input + 1 conditional Reset button (跟 `RippleFilter.vue` 风格 1:1, 复用过)
- `modelValue` 全 camelCase (跟 Vue 3 `<input type="number">` v-model numeric 风格 1:1)
- 内部 `emitNumber()` 防御性过滤 (NaN / out-of-range 静默 drop, 不污染 URL)
- 0 fetch / 0 router push (parent 负责 URL sync + fetch, 单向数据流)

### 5. MODIFIED: `CascadeRunsPanel.vue` (+60L)

**File**: `dashboard/frontend/src/components/CascadeRunsPanel.vue` (modify, 0 删 Phase 9.22 行)

```javascript
import { computed, onBeforeUnmount, onMounted, ref, watch } from 'vue';
import { useRoute, useRouter } from 'vue-router';
import {
  fetchCascadeRuns,
  cancelCascadeRun,
  fetchCascadeWithDepth,
} from '../api/index.js';
import { onCascadeUpdate } from '../composables/useWorkflowSocket.js';
import CascadeGraph from './CascadeGraph.vue';
import CascadeRunsFilter from './CascadeRunsFilter.vue';  // Phase 9.23

const props = defineProps({
  rippleId: { type: String, required: true },
});

const route = useRoute();
const router = useRouter();

const DEFAULT_FILTERS = Object.freeze({
  status: 'all', minDepth: null, maxDepth: null, algorithm: 'all',
});

function queryToFilters(query) {
  const minD = query.min_depth ? Number(query.min_depth) : null;
  const maxD = query.max_depth ? Number(query.max_depth) : null;
  return {
    status: query.status || 'all',
    minDepth: minD != null && Number.isFinite(minD) ? minD : null,
    maxDepth: maxD != null && Number.isFinite(maxD) ? maxD : null,
    algorithm: query.algorithm || 'all',
  };
}

function filtersToQuery(filters) {
  const q = {};
  if (filters.status && filters.status !== 'all') q.status = filters.status;
  if (filters.minDepth != null && filters.minDepth !== '') q.min_depth = String(filters.minDepth);
  if (filters.maxDepth != null && filters.maxDepth !== '') q.max_depth = String(filters.maxDepth);
  if (filters.algorithm && filters.algorithm !== 'all') q.algorithm = filters.algorithm;
  return q;
}

const filters = ref(queryToFilters(route.query));
const loading = ref(false);
const error = ref(null);
const replaying = ref(null);
const cancelling = ref(null);
const replayedData = ref(null);
const replayedFromId = ref(null);
const replayedFromStartedAt = ref(null);
const selectedRunId = ref(null);

const runs = ref([]);

const hasActiveFilter = computed(() =>
  filters.value.status !== 'all' ||
  filters.value.minDepth != null ||
  filters.value.maxDepth != null ||
  filters.value.algorithm !== 'all'
);

async function loadRuns() {
  loading.value = true;
  error.value = null;
  try {
    const opts = {};
    if (filters.value.status !== 'all') opts.status = filters.value.status;
    if (filters.value.minDepth != null) opts.minDepth = filters.value.minDepth;
    if (filters.value.maxDepth != null) opts.maxDepth = filters.value.maxDepth;
    if (filters.value.algorithm !== 'all') opts.algorithm = filters.value.algorithm;
    runs.value = await fetchCascadeRuns(props.rippleId, opts);
  } catch (e) {
    error.value = `Failed to load cascade runs: ${e?.message || e}`;
    runs.value = [];
  } finally {
    loading.value = false;
  }
}

function onFilterChange(next) {
  filters.value = next;
  // Single update cycle: rewrite URL then reload. No debounce on dropdowns (single click),
  // no debounce on number inputs (parent emits already on blur via @input only when valid).
  router.replace({ query: filtersToQuery(next) });
  loadRuns();
}

// Phase 9.23: URL → state (handles back/forward + deep links)
watch(() => route.query, (q) => {
  const next = queryToFilters(q);
  // Avoid redundant fetch if URL didn't change semantic state
  if (JSON.stringify(next) !== JSON.stringify(filters.value)) {
    filters.value = next;
    loadRuns();
  }
});

// ... (formatRelative, replay, cancel, WS handler — Phase 9.22 既有 0 改)
```

**Template 改动** (~+15L):
```vue
<template>
  <div class="cascade-runs-panel" data-testid="cascade-runs-panel">
    <CascadeRunsFilter
      :model-value="filters"
      @update:model-value="onFilterChange"
    />
    <div v-if="loading" class="cascade-runs-loading" data-testid="cascade-runs-loading">...</div>
    <div v-else-if="error" class="cascade-runs-error" data-testid="cascade-runs-error">...</div>
    <div v-else-if="runs.length === 0" class="cascade-runs-empty" data-testid="cascade-runs-empty">
      {{ hasActiveFilter ? 'No cascade runs match these filters' : 'No cascade runs yet' }}
    </div>
    <table v-else class="cascade-runs-table" data-testid="cascade-runs-table">
      <!-- Phase 9.22 既有 table 0 改 -->
    </table>
    <div v-if="replayedData" class="cascade-runs-replay" data-testid="cascade-runs-replay">
      <!-- Phase 9.22 既有 replay 0 改 -->
    </div>
  </div>
</template>
```

**设计决策**:
- 250ms debounce **0 实现** — `emitNumber` 已在 filter 内部 NaN 防御, 用户键入只在 valid 数字时 emit (避免 thrash), 单 valid 数字 emit → 1 URL update + 1 fetch
- 旧 Phase 9.22 既 3 vitest test (`renders_table_with_status_badges_and_cancel_only_for_running` / `click_replay_fetches_cascade_with_same_max_depth_and_renders_graph` / `ws_cascade_cancel_event_updates_row_status_to_cancelled`) 0 改 contract
- WS cascade.update handler (`onMounted` 内的 `onCascadeUpdate`) 仍调 `loadRuns()`, 自动继承 filter (因为 `loadRuns()` 读 `filters.value`)
- 0 改 `replay` / `cancel` / WS handler / formatRelative

### 6. Error handling

| 场景 | 处理 |
|---|---|
| 网络错 | `error.value = "Failed to load filtered runs: ${e.message}"` (复用 Phase 9.22 错误路径, 0 新 UI) |
| Server 422 (invalid filter) | FastAPI 返 422 + JSON error body, `request()` helper 抛 `Error`, 走 error.value 路径 |
| 非法 URL param (`?min_depth=abc`) | `queryToFilters` NaN 防御 → 静默 drop, 当作无 filter (1:1 跟 Vitest 防御一致) |
| Empty result with active filter | "No cascade runs match these filters" (跟 `hasActiveFilter` 联动) |
| Empty result without filter | "No cascade runs yet" (Phase 9.22 既有 copy) |
| Reset button click | 一次性 emit `defaultFilters()` → 触发 `onFilterChange` → URL 清空 + 重新 fetch |

### 7. Status transition matrix

| Trigger | Source | Target | Behavior |
|---|---|---|---|
| Initial mount | `route.query` | `filters` + `runs` | `queryToFilters` → `loadRuns` |
| User changes dropdown | `<select>` change | `filters` + URL | `onFilterChange` → `router.replace` → `loadRuns` |
| User types number | `<input>` input | `filters` + URL (valid only) | `emitNumber` 防御 → `onFilterChange` |
| Reset button | `<button>` click | `filters` (defaults) + URL (empty) | `emit(defaultFilters)` → `onFilterChange` |
| Browser back/forward | `route.query` 变化 | `filters` + `runs` | `watch(route.query)` → `loadRuns` |
| Deep link (URL has `?status=cancelled`) | page load | `filters` + `runs` | 跟 initial mount 1:1 |
| WS cascade.update | payload.ripple_id match | `runs` | Phase 9.22 既 `loadRuns()` (自动继承 filter) |
| WS cascade.cancel | payload.run_id match | run.status | Phase 9.22 既 status update (0 改) |
| User click Replay | button click | `replayedData` | Phase 9.22 既 `replay(run)` (0 改) |
| User click Cancel | button click | run.status | Phase 9.22 既 `cancel(run)` (0 改) |

## File Map

| Category | Path | Op | LOC |
|---|---|---|---|
| New component | `dashboard/frontend/src/components/CascadeRunsFilter.vue` | NEW | ~85L |
| Modify UI | `dashboard/frontend/src/components/CascadeRunsPanel.vue` | +filter + route watcher + 2 helpers | +60L |
| Modify API | `dashboard/frontend/src/api/index.js` | extend `fetchCascadeRuns` 3 options | +3L |
| Modify storage | `infra/cross_volume/storage.py` | 3 new WHERE clauses + 3 params | +12L |
| Modify endpoint | `dashboard/app.py` | 4 new Query() params (status + 3 new) + forward | +12L |
| New tests (storage) | `tests/cross_volume/test_cascade_runs_filters.py` | NEW | ~120L |
| New tests (endpoint) | `tests/dashboard/test_cascade_runs_filters_endpoint.py` | NEW | ~150L |
| New tests (vitest filter) | `dashboard/frontend/tests/unit/cascade-runs-filter.spec.js` | NEW | ~120L |
| New tests (vitest panel URL sync) | `dashboard/frontend/tests/unit/cascade-runs-panel.spec.js` | +2-3 tests | +50L |
| New e2e | `dashboard/frontend/tests/e2e-smoke/cascade-runs-filter.spec.js` | NEW | ~80L |
| New spec | `docs/superpowers/specs/2026-06-11-phase9.23-cascade-run-filter-ui-design.md` | NEW (本 doc) | ~340L |
| New plan | `docs/superpowers/plans/2026-06-11-phase9.23-cascade-run-filter-ui.md` | NEW | ~340L |

**估计**: 2 new docs + 1 new component + 5 modified + 4 new test files + 1 modified test file = 12 files, +1280L new. 0 production code 行为破坏 (additive).

## Verification

```bash
# Backend (storage + endpoint)
cd novel-factory
pytest tests/cross_volume/test_cascade_runs_filters.py -v 2>&1 | tail -5
# 期望: 3-4 new passed

pytest tests/dashboard/test_cascade_runs_filters_endpoint.py -v 2>&1 | tail -5
# 期望: 5 new passed

pytest -q 2>&1 | tail -3
# 期望: 2469 + 8 = 2477 passed, 0 failed, 27 skipped (Phase 9.22 baseline 0 破)

# Frontend
cd dashboard/frontend
pnpm exec vitest run 2>&1 | tail -5
# 期望: 159 + 6 = 165 passed (Phase 9.22 baseline 0 破 + 4-5 filter + 2-3 panel URL sync)

pnpm exec playwright test cascade-runs-filter.spec.js 2>&1 | tail -5
# 期望: 2 new passed (12 + 2 = 14 total e2e)

# Lint + build
pnpm lint:all 2>&1 | tail -3
# 期望: 0 errors (新 warnings 0 个, 走 testid-class-sync 0 报)

pnpm build 2>&1 | tail -5
# 期望: build success, 0 type errors

# Backend lint
cd ../..
ruff check infra/ tests/ 2>&1 | tail -3
# 期望: 0 issues
```

## Critical Files

### Created
- `docs/superpowers/specs/2026-06-11-phase9.23-cascade-run-filter-ui-design.md` (本 doc, ~340L)
- `docs/superpowers/plans/2026-06-11-phase9.23-cascade-run-filter-ui.md` (~340L)
- `dashboard/frontend/src/components/CascadeRunsFilter.vue` (~85L, NEW)
- `tests/cross_volume/test_cascade_runs_filters.py` (~120L, NEW)
- `tests/dashboard/test_cascade_runs_filters_endpoint.py` (~150L, NEW)
- `dashboard/frontend/tests/unit/cascade-runs-filter.spec.js` (~120L, NEW)
- `dashboard/frontend/tests/e2e-smoke/cascade-runs-filter.spec.js` (~80L, NEW)

### Modified (additive)
- `dashboard/frontend/src/components/CascadeRunsPanel.vue` (+60L: filter import + useRoute/useRouter + 2 helpers + onFilterChange + watch + 1 template line)
- `dashboard/frontend/src/api/index.js` (+3L: 3 new URLSearchParams.set calls + JSDoc)
- `infra/cross_volume/storage.py` (+12L: 3 params + 3 WHERE clauses + JSDoc)
- `dashboard/app.py` (+12L: 4 new Query() params + forward to storage + JSDoc)
- `dashboard/frontend/tests/unit/cascade-runs-panel.spec.js` (+50L: 2-3 URL sync tests)

### NOT modified (defensive scope boundary)
- `dashboard/frontend/src/components/RippleDrawer.vue` (0 改: filter 是 panel 内部, drawer 不需要知道)
- `dashboard/frontend/src/components/CascadeGraph.vue` (Phase 9.15, 0 改)
- `dashboard/frontend/src/components/RippleFilter.vue` (Phase 9.13, 0 改 — 新 filter 组件参考其风格但不 import)
- `dashboard/frontend/src/composables/useWorkflowSocket.js` (Phase 9.17, 0 改)
- `dashboard/frontend/src/composables/useRippleStore.js` (Phase 9.15, 0 改)
- `dashboard/protocols.py` (0 改: CascadeRunResponse schema 已就位, 0 加字段)
- `infra/cross_volume/cascade_persistence.py` (Phase 9.20, 0 改: 只读 storage.get_cascade_runs, 不写)
- `dashboard/cascade_notifier.py` (Phase 9.21, 0 改)
- `infra/cli/*` + `lingwen.py` (Phase 9.21, 0 改)
- `tests/cross_volume/test_cascade_runs_*.py` (Phase 9.20/9.21 既有, 0 改: 旧 caller 用 `status=None` 继续 work)
- `tests/dashboard/test_cascade_runs_endpoint.py` (Phase 9.20 既有, 0 改)
- 19 historical spec/plan docs `:NNN` 行号 (0 改)
- `pyproject.toml` / `pytest.ini` / `vite.config.js` / `vitest.config.js` / `playwright.config.js` (0 改)
- `CLAUDE.md` (0 改)
- `MEMORY.md` / `phases-8-dashboard.md` (本 phase 不更新, F1 bookkeeping 单独 phase 拆)

## Risks + Mitigations

| Risk | Mitigation |
|---|---|
| 旧 2469 + 12 e2e baseline 破 | `min_depth`/`max_depth`/`algorithm` 全 `None` default → 旧 caller 0 改; endpoint `Query(default=None)` 旧 fetch 0 改 |
| `?status=` Phase 9.22 spec 误判没实现 | 实际 1:1 storage 已有 `status` param, endpoint 漏; 跟 F7 scope 一起补 (additive, 0 breaking) |
| URL sync 双向死循环 | `watch(route.query)` 比对 `JSON.stringify(next) !== JSON.stringify(filters.value)` 才触发 loadRuns; `onFilterChange` 内部 set `filters.value` 1 次 → router.replace → 触发 watch → 比对一致 → 0 二次 fetch |
| `Number.isFinite` / `min/max` 校验 | filter 组件 emitNumber 防御 (NaN / out-of-range 静默 drop) + endpoint Query(ge=1, le=10) server-side 校验 (双层防御) |
| Debounce 0 实现 → 多次连续 URL update | 实际上 emitNumber 只在 valid 数字 emit, 用户键入 "12" 实际只 emit 1 次 (value=12); dropdown `<select>` 单击 1 emit; 0 真实 thrash |
| `router.replace` 不 push history (避免污染) | 正确: `replace` 不 `push` (跟 `web/patterns.md` URL As State + 避免 back 按钮需要多次返回), 跟 Phase 9.20 `?max_depth=` 既模式 1:1 |
| Storage column `algorithm` 跟 endpoint param `algorithm` 命名一致 | 已 verified: `storage.py:135 CREATE TABLE` 列名 `algorithm` + `protocols.py:779 CascadeRunResponse.algorithm` + 1:1 SQL filter (1:1 naming, 0 改 column) |
| `regex=` 跟 `Literal=` 选择 | 跟 Phase 9.20 既 endpoint `regex=` 风格 1:1, FastAPI 接受 (跟 Pydantic v2 兼容); 防御 SQL injection 由 FastAPI Query 类型 + regex 保证 |
| Empty state 区分不清 | `hasActiveFilter` computed → 三元切换 "No cascade runs match these filters" vs "No cascade runs yet" |
| vitest mock 复 Phase 9.22 模式 | 复用 `globalThis.__cascadeHandlers = new Set()` + `vi.mock('../../src/api/index.js')` pattern, 0 改 |
| e2e mock 复 Phase 9.22 模式 | 复用 `page.route('**/api/ripples/cascade/**', ...)` pattern, 加 1 new case 验证 filter dropdown 改 URL + 触发新 fetch |
| CascadeRunsPanel 既有 3 vitest 破 | `loadRuns()` 仍读 `filters.value`, 但 `route.query={}` 时 `filters={status:'all', minDepth:null, maxDepth:null, algorithm:'all'}` → `opts={}` → 等同 0 filter → 等同 Phase 9.22 既有行为 (3 vitest 0 改 contract) |
| `?max_depth=` 跟 Phase 9.19 既有 endpoint 冲突 | Phase 9.19 是 `get_ripple_cascade` (单 cascade preview), Phase 9.20/9.23 是 `get_ripple_cascade_runs` (runs list), 不同 endpoint, 0 冲突 |
| MEMORY.md / phases-8-dashboard.md 不更新 (F1 deferred) | 接受再超, F1 单独 phase 拆; 本 phase 不动 memory |

## DoD

- [ ] spec + plan doc 创建 + commit (1 commit `docs(superpowers): 2026-06-11 phase 9.23 spec + plan`)
- [ ] `RippleStorage.get_cascade_runs` 扩 3 params + 3 WHERE clauses (storage)
- [ ] `dashboard/app.py:get_ripple_cascade_runs` 扩 4 Query() params + forward (endpoint, 含 9.22 spec 漏的 `status`)
- [ ] `api/index.js:fetchCascadeRuns` 扩 3 options + 3 URLSearchParams.set (frontend)
- [ ] `CascadeRunsFilter.vue` 创建 (~85L, NEW)
- [ ] `CascadeRunsPanel.vue` 改 +60L (filter import + useRoute/useRouter + 2 helpers + onFilterChange + watch + 1 template line)
- [ ] 3-4 storage pytest tests passed
- [ ] 5 endpoint pytest tests passed
- [ ] 4-5 vitest filter tests passed
- [ ] 2-3 vitest panel URL sync tests passed
- [ ] 2 e2e tests passed
- [ ] pytest 2469 → 2477+ passed (8 new), 0 failed, 27 skipped (Phase 9.22 baseline 0 破)
- [ ] vitest 159 → 165 passed (6 new), Phase 9.22 既 3 vitest 0 破
- [ ] e2e 12 → 14 passed (2 new)
- [ ] ruff 0 issues
- [ ] pnpm lint:all 0 errors
- [ ] pnpm build 0 type errors
- [ ] N commits push origin/master (1 doc + per-task TDD commits)
- [ ] 0 旧测试代码改动 (Phase 9.22 既 2469 pytest + 159 vitest + 12 e2e baseline 全部通过, additive only)
- [ ] 0 production code 行为破坏 (filter 是 additive, 旧 caller 0 改继续 work)
- [ ] 0 误改 .env / 0 API key 泄漏
- [ ] 0 force-push / 0 --no-verify / 0 Co-Authored-By footer
- [ ] 0 改 RippleDrawer.vue (filter 内部化)
- [ ] 0 改 CascadeGraph.vue / RippleFilter.vue / useWorkflowSocket / useRippleStore (0 触碰)
- [ ] 0 改 Phase 9.19/9.20/9.21/9.22 既有 production code behavior
- [ ] 0 改 `protocols.py:CascadeRunResponse` schema
- [ ] 0 改 backend `cascade_runs` 表 (无 migration)
- [ ] 0 改 `lingwen.py` CLI
- [ ] 0 改 19 historical spec/plan doc `:NNN` 行号
- [ ] 0 改 CLAUDE.md / pyproject.toml / pytest.ini / vite.config.js / vitest.config.js / playwright.config.js

## 完成后下一步

- **F1 bookkeeping** (urgent) — phases-8-dashboard.md 拆 8 段 era 文件 + typo 修 + legacy MEMORY.md v6.1 sync 决策 (30min 投资, 防止超 200 line 永久)
- **Phase 9.24+** — 主线 4 项完成 (F4/F5/F6/F7), 选下一条线 (per F1-F16 决策矩阵, 推荐 F10 WS indicator 或 F12 per-tier 趋势线, UX 价值高工作量低)
- **Phase 9.25 polish** — UI 一致性 + data-testid 统一 (F14) + Playwright vitest 化 (F15) mechanical
- **Phase 10.0** — pre-F1 收尾: 全 phase 9 history 整理到 phases-9-cvg.md 新 era file, MEMORY.md era 指针 5 → 6 era
