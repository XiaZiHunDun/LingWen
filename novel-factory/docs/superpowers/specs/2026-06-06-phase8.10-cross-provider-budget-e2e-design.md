# Phase 8.10 — Cross-Provider Budget E2E (Opt-in 验证 OpenAI + MiniMax cost_budget_usd 触阈)

> **For agentic workers:** Phase 8.10 收尾 Phase 8.2 留的 gap: 跨 provider cost_budget_usd 触阈 opt-in 验证。复用 Phase 8.2 helper + Phase 8.9 测试模板,4 new opt-in tests,0 CI 集成 (跟 Phase 8.2 一致),0 production code 改。

**Status**: Design (brainstorming approved 2026-06-06)
**Date**: 2026-06-06
**Phase**: 8.10 (跟 Phase 8.2 + 8.9 同系列)
**Author**: 莎丽 (主公 brainstormed)

---

## §1 Context

### 1.1 现状 (per MEMORY.md + Phase 8.9 commit 533089d)

Phase 8.2 (commit 150be7c) 加了 OpenAI + MiniMax 跨 provider `polish_merge_synthesis` 验证 (各 1 test, 9 步断言),验证 Phase 7.5 评分跨 provider 兼容。Phase 8.9 (commit 533089d) 加了 Anthropic `cost_budget_usd` 触阈 e2e (2 tests: under+over),**仅 Anthropic**。

**问题**: 跨 provider matrix 有 gap。Phase 8.2 验证 3 provider LLM path 走通 + Phase 8.9 验证 Anthropic 上 cost_budget_usd raise path,**OpenAI + MiniMax 上的 cost_budget_usd raise path 未被 real LLM 端到端验证**。

### 1.2 cost_budget_usd 路径 provider-agnostic?

是的。`cost_budget_usd` 触阈逻辑在 MasterController.run_workflow (master_controller.py:127 _current_budget_usd 字段) + AgentComputeFn.__call__ (got_bridge.py:255-264 check_budget 集中调) + CostTracker.check_budget (cost_tracker.py:N)。Provider 差异在 LLM call 本身 (不同 SDK + 不同 pricing),**触阈判断用 cost_tracker.total_cost() 跨 provider 共享**,无 provider-specific code path。

### 1.3 为什么 YAGNI 标签可以摘?

Phase 8.2 时标 YAGNI 的理由: "Phase 8.9 cost_budget_usd 验证 provider-agnostic, 跨 provider 重复验证冗余"。**当前反例**: 跨 provider 的 **real LLM cost 数值** + **触阈行为在不同 provider 的 cost accumulation 模式** 未被验证。gpt-4o-mini 7-8x cheaper 走 raise path 跟 HAIKU 行为是否一致? M2.7 内部 SDK cost tracking 行为是否一致? 这是生产用户在 OpenAI/MiniMax 上跑 budget workflow 时可能踩的坑。

### 1.4 范围 (主公 2026-06-06 brainstorming 决策)

- **Provider 范围**: OpenAI + MiniMax (2 new test classes, 4 new tests, 跟 Phase 8.2 matrix 一致)
- **CI 集成**: 不集成 (跟 Phase 8.2 一致 — 纯 opt-in,主公本地跑)
- **测试模式**: 2 tests per class (under+over),跟 Phase 8.9 Anthropic 一致

---

## §2 Goals / Non-Goals

### 2.1 Goals

1. **2 new test classes** (跟 Phase 8.2 naming 一致):
   - `TestNovelWritingBudgetRealLLMOpenAI` (2 tests: under + over)
   - `TestNovelWritingBudgetRealLLMMiniMax` (2 tests: under + over)
2. **4 new opt-in tests** 默认 SKIP (decorator skipif OPENAI_API_KEY / MINIMAX_API_KEY),opt-in 跑
3. **复用 Phase 8.9 测试模板** + **Phase 8.2 provider matrix helper** (`_make_real_router`)
4. **验证 `cost_budget_usd` 触阈在 OpenAI gpt-4o-mini + MiniMax M2.7 上**:
   - under_budget: 完整跑通 + cost < budget + finally reset
   - over_budget: 触阈 raise + FAILED + finally reset + records >= 1
5. **0 production code 改** (Phase 8.8 cost_budget_usd 路径已就位)
6. **0 CI workflow 改** (跟 Phase 8.2 一致)

### 2.2 Non-Goals (本 phase 不做,留 followup)

- **CI 集成**: 不加 `.github/workflows/real-llm-tests.yml` `-k` filter,OpenAI+MiniMax 留 opt-in
- **Provider 配置改**: 沿用 Phase 8.2 `_PROVIDER_REGISTRY` (gpt-4o-mini + M2.7)
- **Budget 阈值差异化**: 沿用 Phase 8.9 $0.10 under / $0.0001 over
- **0 Phase 8.9 Anthropic tests 改** (backward compat, 1:1 mirror 模式)
- **0 现有 skipif decorators 改** (复用 Phase 8.2 L27-34 _REQUIRES_OPENAI_KEY / _REQUIRES_MINIMAX_KEY)
- **0 historical spec/plan docs `:NNN` 行号改** (8 historical docs + Phase 8.9 spec)
- **0 改 12 SCENARIOS 路由 / 0 改 ComputeResult schema / 0 改 CostTrackerDB schema / 0 改 Dashboard UI**
- **不画 Phase 8.10 进度 entry** 在 Phase 8.9 spec/plan 文档里 (后续 plan 集中记录)

---

## §3 Design

### 3.1 文件结构

**Modify**: `tests/agent_system/test_novel_writing_real_llm.py` (319 → ~510L, +~190L)

**Append position**: 末尾,L319 之后 (Phase 8.9 `TestNovelWritingBudgetRealLLM` 类的 `test_run_workflow_over_budget_raises` 闭括号后)。新加 2 个 class 共享同一份 7-key initial_inputs 模板(用 inline dict 复制 Phase 8.9 L248-259 模式)。

### 3.2 OpenAI class (TestNovelWritingBudgetRealLLMOpenAI)

```python
@_REQUIRES_OPENAI_KEY
class TestNovelWritingBudgetRealLLMOpenAI:
    """Phase 8.10: OpenAI gpt-4o-mini real LLM e2e 验证 cost_budget_usd 触阈.

    复用 Phase 8.9 pattern (skipif OPENAI_API_KEY + _make_real_router("openai") +
    make_master_with_router + inline 7-key initial_inputs dict). 默认 SKIP, opt-in 跑:
        export OPENAI_API_KEY=sk-...
        pytest tests/agent_system/test_novel_writing_real_llm.py::TestNovelWritingBudgetRealLLMOpenAI -v
    成本: gpt-4o-mini × ~5 LLM calls × 2 tests ≈ $0.005-0.015 per run, ~30-60s per test.
    """

    def test_run_workflow_under_budget_passes(self, tmp_path: Path) -> None:
        """Phase 8.10: OpenAI gpt-4o-mini generous budget, 验证 success + cost < budget + finally reset."""
        from infra.ai_service.cost_tracker import CostTracker
        cost_tracker = CostTracker()
        master = make_master_with_router(
            state_dir=tmp_path,
            router=_make_real_router("openai"),
            cost_tracker=cost_tracker,
        )
        result = master.run_workflow(
            workflow_name="novel_writing",
            initial_inputs={
                "chapter_num": 1,
                "outline": {"chapters": [{
                    "num": 1, "title": "第一章 测试", "events": ["e1"],
                    "word_count_target": 800,  # 短内容加速 gpt-4o-mini 处理
                }]},
                "characters": [],
                "memory_context": {},
                "style_guide": {},
                "timeline": [],
                "use_llm": True,
            },  # 跟 Phase 8 现有 pattern (L63-74) + Phase 8.9 L248-259 完全一致
            cost_budget_usd=0.10,  # 10x 预期 $0.001-0.010 (gpt-4o-mini 7-8x cheaper)
            max_backtracks=0,
        )

        summary = result["summary"]
        # 1. 完整跑通
        assert summary.completed >= 5, f"期望 ≥5 completed (5 LLM 节点), 实际 {summary.completed}"
        assert summary.failed == 0, f"期望 0 failed (under-budget), 实际 {summary.failed}"
        # 2. cost 在预算内
        total_cost = cost_tracker.total_cost()
        assert 0 < total_cost < 0.10, f"期望 0 < cost < 0.10, 实际 {total_cost}"
        # 3. finally reset
        assert master._current_budget_usd is None, (
            f"期望 finally reset to None, 实际 {master._current_budget_usd}"
        )

    def test_run_workflow_over_budget_raises(self, tmp_path: Path) -> None:
        """Phase 8.10: OpenAI gpt-4o-mini sub-cent budget, 验证 CostBudgetExceeded + FAILED + finally reset."""
        from infra.ai_service.cost_tracker import CostBudgetExceeded, CostTracker
        _ = CostBudgetExceeded  # 文档化: workflow 内部抛此异常, scheduler 捕获后转 FAILED
        cost_tracker = CostTracker()
        master = make_master_with_router(
            state_dir=tmp_path,
            router=_make_real_router("openai"),
            cost_tracker=cost_tracker,
        )
        result = master.run_workflow(
            workflow_name="novel_writing",
            initial_inputs={
                "chapter_num": 1,
                "outline": {"chapters": [{
                    "num": 1, "title": "第一章 测试", "events": ["e1"],
                    "word_count_target": 800,
                }]},
                "characters": [],
                "memory_context": {},
                "style_guide": {},
                "timeline": [],
                "use_llm": True,
            },  # 跟 Phase 8 现有 pattern 一致
            cost_budget_usd=0.0001,  # sub-cent, 必超
            max_backtracks=0,
        )

        summary = result["summary"]
        # 1. 至少 1 node FAILED (触阈那个)
        assert summary.failed >= 1, (
            f"期望 ≥1 FAILED, 实际 completed={summary.completed} failed={summary.failed}"
        )
        # 2. workflow 中止, 不全跑 (Phase 7.4 = 7 节点)
        total = summary.completed + summary.failed
        assert total < 7, f"期望 total < 7 (workflow 被中止), 实际 {total}"
        # 3. cost 累加 > budget (触阈发生)
        assert cost_tracker.total_cost() > 0.0001
        # 4. finally reset 在 raise 后仍生效
        assert master._current_budget_usd is None, (
            f"期望 finally reset after raise, 实际 {master._current_budget_usd}"
        )
        # 5. 至少 1 笔 record 写入
        assert len(cost_tracker.records()) >= 1
```

### 3.3 MiniMax class (TestNovelWritingBudgetRealLLMMiniMax)

**完全 mirror OpenAI class**,仅改:
- 装饰器: `@_REQUIRES_MINIMAX_KEY` (跟 Phase 8.2 L189 一致)
- class name: `TestNovelWritingBudgetRealLLMMiniMax`
- docstring 第 1 段: `MiniMax M2.7` 替换 `OpenAI gpt-4o-mini`
- docstring 成本: `MiniMax M2.7 × ~5 LLM calls × 2 tests ≈ $0.01-0.05 per run, ~30-60s per test.` (替代 gpt-4o-mini 行)
- docstring 短内容加速注释: `M2.7 处理` 替换 `gpt-4o-mini 处理`
- docstring `10x 预期 $0.005-0.020 (gpt-4o-mini 7-8x cheaper)` 改 `2x 预期 <$0.05 (M2.7 单价未知但 <$0.01/call)`
- `_make_real_router("minimax")` 替换 `_make_real_router("openai")`
- 其余代码 (assertion / inline 7-key dict / `cost_budget_usd=0.0001` 阈值) 完全相同

### 3.4 5 关键设计点

1. **0 production code 改**: 跟 Phase 8.9 一致,纯测试加。`cost_budget_usd` 路径已就位 (Phase 8.8 commits 460dce7 + 8675fad + 907f8b1),不需重做触阈 raise / finally reset 逻辑
2. **0 CI 集成**: 跟 Phase 8.2 一致,4 new opt-in tests 默认 skipif,CI `-k` filter 不动。Phase 8.9 L58 `-k "TestNovelWritingRealLLM or TestPolishMergeSynthesisRealLLM or TestNovelWritingBudgetRealLLM"` 仍只跑 Anthropic 4 tests
3. **inline 7-key initial_inputs dict**: 跟 Phase 8 L63-74 + Phase 8.9 L248-259 pattern 复制,无新 const 抽。每个 test 独立 inline (OpenAI/MiniMax 跟 Anthropic 同样 7-key 顺序)
4. **budget 阈值沿用**: `$0.10 under` / `$0.0001 over`,跟 Phase 8.9 一致。gpt-4o-mini 7-8x cheaper 走 $0.10 留 10x safety;M2.7 单价 <$0.01/call × 5 calls = <$0.05,$0.10 留 2x safety。`$0.0001` sub-cent 必触 (1 LLM call 成本 >$0.0001)
5. **`_ = CostBudgetExceeded` keep-alive**: 跟 Phase 8.9 同样 ruff F401 fix 模式,test 2 local import。ruff-sanctioned (Phase 8.9 计划 L296 已批准,Phase 8.10 同样应用)

### 3.5 CI workflow 0 改 (跟 Phase 8.2 一致)

`.github/workflows/real-llm-tests.yml` **不动**:
- L4 header comment 仍 "Phase 8 + 8.9 (Anthropic) 4 个真实 LLM 测试"
- L5 Provider 仍 "Anthropic only"
- L58 `-k` filter 仍只跑 Anthropic 4 tests

Phase 8.10 测试 opt-in 跑:
```bash
# OpenAI
export OPENAI_API_KEY=sk-...
pytest tests/agent_system/test_novel_writing_real_llm.py::TestNovelWritingBudgetRealLLMOpenAI -v
# 期望: 2 passed

# MiniMax
export MINIMAX_API_KEY=...
pytest tests/agent_system/test_novel_writing_real_llm.py::TestNovelWritingBudgetRealLLMMiniMax -v
# 期望: 2 passed
```

主公手动 setup GitHub secrets (OpenAI + MiniMax keys) 跟 Phase 8.2 一致,留 followup,不在本 phase 范围。

### 3.6 不抽公共 helper (YAGNI)

2 个 new class 共享 inline 7-key dict 模板 (跟 Phase 8.9 同样模式),**不抽** `_make_budget_test_initial_inputs` helper。理由:
- Phase 8.9 Anthropic 测试也用了 inline 7-key dict,没抽 helper
- 抽 helper 反而让 Phase 8.10 跟 Phase 8.9 pattern 不一致 (违反 backward compat)
- 2 个 new class × 2 tests = 4 inline dict,跟 Phase 8.9 同样 2 inline dict 重复 pattern,additive 但 symmetric
- YAGNI 优先: 3 provider × 2 tests = 6 inline dict 是可接受 boilerplate,跟 Phase 8.2 docstring style "9 步断言" 同样模式

---

## §4 Risks + Mitigations

| 风险 | 影响 | 缓解 |
|------|------|------|
| gpt-4o-mini pricing 浮动,$0.10 under threshold 不够 | 中 | gpt-4o-mini 7-8x cheaper than HAIKU,5 calls 实际 $0.001-0.010,$0.10 留 10x safety;若失败放宽到 $0.20 (production 主公 1 行 test fix) |
| MiniMax M2.7 pricing 未知,$0.10 under 不够 | 中 | M2.7 单价 <$0.01/call,5 calls 实际 <$0.05,$0.10 留 2x safety;若失败放宽到 $0.20 (同样 1 行 test fix) |
| 4 new tests 增加 CI 收集时间 | 低 | skipif 自动 SKIP,pytest 收集 4 tests < 1s,0 CI 延迟 |
| `_ = CostBudgetExceeded` keep-alive ruff F401 重复 2 次 | 低 | 跟 Phase 8.9 同样 ruff-sanctioned (文档化 raise 行为),test 2 local import |
| `cost_tracker.total_cost() > 0.0001` OpenAI/MiniMax 上不成立 | 低 | gpt-4o-mini 实际 token 成本 >$0.0001 (1 个 LLM call 就超),M2.7 同理;若失败改用 `> 0` (Phase 8.9 同样模式) |
| OpenAI/MiniMax API 限流 / 网络失败 | 中 | 跟 Phase 8.2 一样 max_retries=1 + timeout=180 (`_make_real_router` L213-215),生产用户可手动重跑 |
| Phase 8.10 跟 Phase 8.2 OpenAI/MiniMax test (polish_merge_synthesis) 共享 provider 资源 | 低 | 2 class 不同 test file 互不干扰,`make_master_with_router` 用 `tmp_path` 状态隔离 (L213 state_dir=tmp_path) |
| 0 旧测试破 | 低 | 0 改 test file 已有 code (L1-319 不动),仅追加 L320+;4 new tests 默认 SKIP 不参与 pytest run |
| _make_real_router("openai"/"minimax") 失败 KeyError 无 API key | 低 | skipif 装饰器预先跳过 (decorator 先 evaluate),不会进 test function |
| Anthropic CI 测试因 Phase 8.10 失败 | 低 | 0 改 CI workflow,0 改 Anthropic tests |

---

## §5 Verification

### 5.1 静态检查 (无 API key 也能跑)

```bash
# 1. pytest collect-only 验证 4 new tests added + decorator 正确
pytest tests/agent_system/test_novel_writing_real_llm.py --collect-only -q
# 期望: 7 classes collected (4 旧 + Phase 8.9 + 2 new)
# 期望: 12 tests collected (4 旧 TestNovelWritingRealLLM + 1 TestPolishMergeSynthesis + 1 OpenAI + 1 MiniMax + 2 Phase 8.9 Anthropic + 4 new × 2 classes)

# 2. pytest baseline + new tests (无 API key = 全部 SKIP)
pytest -q 2>&1 | tail -3
# 期望: 2168 passed, 0 failed, 27 skipped (23 旧 + 4 new opt-in SKIP)
# 注: skipif 装饰器在 class 级别, 每个 test method 独立 SKIP; 2 new classes × 2 tests = 4 SKIP cases

# 3. ruff 0
ruff check tests/agent_system/test_novel_writing_real_llm.py 2>&1 | tail -3
# 期望: 0 issues

# 4. CI workflow 0 改
git diff .github/workflows/real-llm-tests.yml
# 期望: 0 output

# 5. grep 验证 2 new classes + 4 new tests
grep -n "class TestNovelWritingBudgetRealLLM" tests/agent_system/test_novel_writing_real_llm.py
# 期望: 3 lines (Anthropic + OpenAI + MiniMax)
```

### 5.2 Opt-in 真实 LLM 跑 (主公手动,需 API keys)

```bash
# OpenAI opt-in run
export OPENAI_API_KEY=sk-...
pytest tests/agent_system/test_novel_writing_real_llm.py::TestNovelWritingBudgetRealLLMOpenAI -v
# 期望: 2 passed
# 成本: gpt-4o-mini × ~5 LLM calls × 2 tests ≈ $0.005-0.015 per run
# 时间: ~30-60s per test

# MiniMax opt-in run
export MINIMAX_API_KEY=...
pytest tests/agent_system/test_novel_writing_real_llm.py::TestNovelWritingBudgetRealLLMMiniMax -v
# 期望: 2 passed
# 成本: M2.7 × ~5 LLM calls × 2 tests ≈ $0.01-0.05 per run
# 时间: ~30-60s per test
```

### 5.3 git log verify

```bash
git log --oneline -1
# 期望: feat(test): phase 8.10 — cross-provider (OpenAI+MiniMax) budget e2e opt-in (4 tests)

git show --stat HEAD
# 期望: 2 files changed
#   docs/superpowers/specs/2026-06-06-phase8.10-cross-provider-budget-e2e-design.md (NEW, ~270L)
#   tests/agent_system/test_novel_writing_real_llm.py (modified, 319 → ~510L, +~190L)
```

---

## §6 Critical files

### Created
- `docs/superpowers/specs/2026-06-06-phase8.10-cross-provider-budget-e2e-design.md` (NEW, ~270L) — 本 spec

### Modified (注入点)
- `tests/agent_system/test_novel_writing_real_llm.py:319+` (4 new tests added, +~190L)
  - `TestNovelWritingBudgetRealLLMOpenAI` (new class, ~95L) — 2 tests
  - `TestNovelWritingBudgetRealLLMMiniMax` (new class, ~95L) — 2 tests

### Referenced / 复用 (NOT modified)
- `tests/agent_system/test_novel_writing_real_llm.py:23-34` (Phase 8.2 `_REQUIRES_OPENAI_KEY` / `_REQUIRES_MINIMAX_KEY` skipif 装饰器,本 phase 复用)
- `tests/agent_system/test_novel_writing_real_llm.py:226-319` (Phase 8.9 `TestNovelWritingBudgetRealLLM` Anthropic class,1:1 mirror 模板)
- `tests/agent_system/test_novel_writing_real_llm.py:63-74` (Phase 8 inline 7-key initial_inputs dict pattern,复用)
- `tests/agent_system/_e2e_helpers.py:174-221` (Phase 8.2 `_make_real_router(provider_name)` helper,复用)
- `tests/agent_system/_e2e_helpers.py:157-171` (Phase 8.2 `_PROVIDER_REGISTRY` openai + minimax spec,复用)
- `infra/agent_system/master_controller.py:127` (Phase 8.8 `_current_budget_usd` 字段,0 改)
- `infra/agent_system/master_controller.py:282` (Phase 8.8 `run_workflow(cost_budget_usd=)` kwarg,0 改)
- `infra/agent_system/got_bridge.py:255-264` (Phase 8.8 AgentComputeFn check_budget 集中调,0 改)
- `infra/ai_service/cost_tracker.py` (Phase 8.8 `CostTracker.check_budget` + `CostBudgetExceeded` exception,0 改)

### NOT modified
- `infra/agent_system/master_controller.py` (0 改)
- `infra/agent_system/got_bridge.py` (0 改)
- `infra/ai_service/cost_tracker.py` (0 改)
- `infra/ai_service/cost_persistence.py` (0 改)
- `infra/ai_service/model_tiers.py` (0 改)
- `dashboard/protocols.py` (0 改)
- `dashboard/app.py` (0 改)
- `tests/agent_system/_e2e_helpers.py` (0 改 — `_PROVIDER_REGISTRY` 跟 Phase 8.2 一致)
- `tests/agent_system/test_novel_writing_real_llm.py:1-319` (0 改 — 仅追加 320+)
- `.github/workflows/real-llm-tests.yml` (0 改 — 跟 Phase 8.2 一致)
- `.gitignore` (0 改)
- `pyproject.toml` / `pytest.ini` / `CLAUDE.md` (0 改)
- `infra/.state/cost_tracker.db` / `infra/.state/workflow.db` (0 改 — gitignored)
- 0 旧 spec/plan docs (`:NNN` 行号不动)
- 0 historical Phase 8.2 OpenAI/MiniMax tests (保留原样)

---

## §7 DoD (Definition of Done)

- [ ] `docs/superpowers/specs/2026-06-06-phase8.10-cross-provider-budget-e2e-design.md` 创建 (~270L)
- [ ] `tests/agent_system/test_novel_writing_real_llm.py:319+` 追加 2 new classes (4 new tests, +~190L)
- [ ] `TestNovelWritingBudgetRealLLMOpenAI.test_run_workflow_under_budget_passes` (RED → GREEN)
- [ ] `TestNovelWritingBudgetRealLLMOpenAI.test_run_workflow_over_budget_raises` (RED → GREEN)
- [ ] `TestNovelWritingBudgetRealLLMMiniMax.test_run_workflow_under_budget_passes` (RED → GREEN)
- [ ] `TestNovelWritingBudgetRealLLMMiniMax.test_run_workflow_over_budget_raises` (RED → GREEN)
- [ ] 0 旧测试代码改动 (2168 baseline 不变, 0 spec/plan 文档改 `:NNN` 行号)
- [ ] 0 production code 改 (master_controller / got_bridge / cost_tracker 全部 0 改)
- [ ] 0 CI workflow 改 (`.github/workflows/real-llm-tests.yml` 0 diff)
- [ ] pytest 2168 passed, 0 failed, 27 skipped (23 旧 + 4 new opt-in SKIP)
- [ ] ruff 0
- [ ] 1 feat commit 推送 origin/master
- [ ] 0 force-push, 0 --no-verify, 0 Co-Authored-By footer
- [ ] 0 误改 .env / 0 API key 泄漏
- [ ] 0 infra/.state/* DB 提交 (gitignored)

---

## §8 Out of scope (后续 phases / followups)

- **Phase 8.11**: Dashboard 实时 cost banner (走 WebSocket 推 cost_by_scenario 累计 + budget status) — 跟 Phase 8.5/8.7/8.8 Dashboard UI 系列延续
- **Phase 8.12**: Cost budget DB 持久化 (跨进程 / 跨重启 budget state 持久化) — 跟 Phase 8.5 CostTrackerDB 模式延续
- **Phase 8.13**: Per-tier budget (HAIKU / SONNET / OPUS 分别 budget) — 跟 Phase 8.5 SCENARIO_TIER_MAP 延伸
- **Phase 8.14**: Soft warning 三态 (ok / 80% warning / 100% exceeded) — 跟 Phase 8.8 硬中止延伸
- **Phase 8.15**: Phase 8.10 cross-provider opt-in tests 集成到 CI workflow (主公手动 setup GitHub secrets OPENAI/MiniMax + 改 -k filter) — 留 followup,不在本 phase 范围
- **Phase 9**: 跨卷涟漪 (独立大方向)
- **MEMORY.md 拆 16+ entries 到 phases.md topic** (Phase 8.10 后变 16+ entries, 必拆 followup)
- **pre-existing infra/.state/workflow.db 漏 .gitignore** (cbca7b5 claim 不准,Phase 8.9 已 exclude 不 commit,独立 followup)
- **pre-existing infra/.state/reading_power.db 验证 gitignore** (类似 workflow.db 验证,followup)
- **MEMORY.md line 8 typo "23 skipped" → "27 skipped"** (Phase 8.10 后必改,本期 plan/impl 同步)
- **MEMORY.md line 8 增量列表追加 "Phase 8.10 +2"** (本 phase 同步)
- **CI 失败兜底**: 若 Phase 8.10 跨 provider opt-in tests 主公 opt-in 跑失败,$0.10 under 阈值不够,放宽到 $0.20 (1 行 test fix,production 主公决定)
- **CI 改 min retry** (Phase 8.2 OpenAI/MiniMax max_retries=1 fail-fast) — followup,不在本 phase
