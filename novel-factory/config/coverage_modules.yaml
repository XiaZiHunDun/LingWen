# Phase 11.11: per-package coverage floors (pytest-cov, after omit list).
# Global gate: --cov-fail-under=40 in CI.
# Module gate: scripts/verify-coverage-modules.sh (Python 3.13 job only).

global_min_percent: 40

# Legacy CLI / API-key-only scripts excluded from tools denominator (see pyproject [tool.coverage.run] omit).
modules:
  infra:
    min_percent: 40
  dashboard:
    min_percent: 40
  tools:
    min_percent: 30
    note: "Active tools path; legacy omit → v12 target 40%"
