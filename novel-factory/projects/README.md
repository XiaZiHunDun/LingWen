# 灵文工作室 — 用户项目目录

每个新书由 `lingwen.py init-project` 生成，例如：

```bash
python lingwen.py init-project anye-xinbiao --title "暗夜信标"
export LINGWEN_PROJECT_ROOT="$(pwd)/projects/anye-xinbiao"
```

星陨纪元试验田仍在 `novel-factory/` 根目录（`config/project.yaml` → `role: testbed`）。
