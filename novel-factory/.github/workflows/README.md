# Deprecated

GitHub Actions only reads workflows from the **repository root**:

`.github/workflows/`

Do not add workflow YAML here. Edit root workflows and `novel-factory/docs/ci-quality-gates.md`.
