# Phase 9.48 F37: moved to repo root — see /.github/workflows/dashboard-e2e-smoke.yml
# Deprecated copy kept for novel-factory-local reference only.

name: Dashboard E2E Smoke (opt-in)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use repo-root .github/workflows/dashboard-e2e-smoke.yml"
