[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "lingwen-studio-batch-templates"
version = "0.1.0"
description = "LingWen canonical studio_batch_templates package (Phase 47 P3-ARCHDEBT studio-batch batch: templates)"
requires-python = ">=3.11"
dependencies = [
    "lingwen-studio-registry",  # uses factory_root
]
# NOT-LEAF — 1 workspace dep. Provides BatchTemplate + create_batch_template +
# list_batch_templates + get_batch_template + update_batch_template +
# delete_batch_template.

[tool.hatch.build.targets.wheel]
packages = ["src/lingwen_studio_batch_templates"]