[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "lingwen-di"
version = "0.1.0"
description = "LingWen canonical DI module (Phase 81 P3-ARCHDEBT di)"
requires-python = ">=3.11"
dependencies = []  # LEAF — zero workspace deps (only stdlib: logging, dataclasses, typing)

[tool.hatch.build.targets.wheel]
packages = ["src/lingwen_di"]