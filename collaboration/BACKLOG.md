# 待办事项列表

> **最后更新**: 2026-09-22 (Phase 107 bulk delete illustration endpoint — fifth Phase 102+ extension; **REQ-002 v2 stays FULLY CLOSED post-Phase 102** — Phase 107 is additive extension: `bulk_delete_assets` route (DELETE `/api/illustrations?ids=...&slug=...`, I090 5th caller + I091 5th double-write site) added for multi-asset deletion: shared `_delete_asset_inner` helper extracted from `delete_asset` route in Phase 107 to avoid duplicating Phase 106 5-path logic; sequential for-loop calls `_delete_asset_inner` per asset (preserves audit_log.jsonl + SSE arrival order); limit 50 (422 on over-50), dedupe via `dict.fromkeys(raw_ids)` preserving order, 422 on empty ids, 404 on slug; per-asset returns 200 OK + `{deleted, failed, summary}` shape; partial failures retained in `failed[]` with status `"not_found"` (cross-project implicit LoadError) / `"store_error"` (storage.delete_asset raises); pre-resolves `meta_by_id` dict once before loop (50×M filesystem reads → O(1) dict lookup per asset); `mode: Literal["single", "bulk"] = "single"` parameter interpolates into `audit_log.record_event(extra=...)` + `notifications.publish(extra=...)` for downstream analytics; `record_failure(slug, error, *, project_root, threshold, event_type="deletion")` on 2 failure paths (404 LoadError passes `project_root=None` — counter increments but audit_log skipped; StoreError passes real root — full audit_log + publish); 404 asset-not-found treated as **no-op success** via `record_success(slug, event_type="deletion")` (defensive counter reset — inherited from delete_asset helper); successful `storage.delete_asset` → `record_success(slug, event_type="deletion")` + `audit_log.record_event` + `notifications.publish` **double-write** I091 — same ULID via `new_event_id()` — per asset; `extra={"trigger": "manual", "mode": "single"|"bulk"}` consistent with Phase 106 pattern; I090 / I091 EXTENDED 5th (rule fields add `bulk_delete_assets` as 5th caller / 5th double-write site); I095 EXTENDED 5th via docstring (per-asset `record_failure/record_success` inherited from shared helper; counter keys still `(slug, "deletion")`; threshold crossing per `(slug, "deletion")` pair emits exactly one severity="warning" notification regardless of bulk iteration size; `mode='bulk'` parameter does NOT affect counter state); `IllustrationGallery.vue` multi-select state (`selection: Set<string>` + `selectionTick: ref` for Vue 3 Set reactivity) + per-card `<NCheckbox>` overlay + sticky bottom bulk action bar (visible when `selection.size > 0`) containing `<NButton>` cancel + `<NPopconfirm>`-wrapped bulk delete button (Phase 106 NPopconfirm pattern reused); `confirmBulkDelete()` calls `store.bulkDeleteAssets` + `useBulkDeleteToast` (3 variants: success / partial / error) + emits `'bulk-deleted'`; component-local selection survives asset prop changes (cross-page safe); NEW frontend files: `useBulkDeleteToast` composable + `bulkDeleteAssets` typed wrapper (pre-validates 1..50) + `useIllustrationStore.bulkDeleteAssets` action (lazy-imports API wrapper; reactively removes deleted + not_found from local `assets[]`); 10 NEW pytest regression guards G1-G10 (G1 bulk_delete_assets route registered / G2 51-id 422 / G3 empty 422 / G4 per-asset helper fan-out / G5 for-loop iteration / G6 counter isolation preserved / G7 delete_asset uses helper mode="single" / G8 invariant extensions in architecture.yml / G9 dict.fromkeys dedupe / G10 no explicit cross-project check); 10 NEW pytest tests T1-T10 (T1 happy path 5×record_success + 5×audit + 5×publish / T2 partial failure 3 ok + 2 store_error / T3 empty 422 / T4 over-50 422 / T5 slug not found 404 / T6 LoadError raises 404 before helper / T7 dedupe 8 raw → 4 unique / T8 cross-project not_found / T9 audit mode='bulk' discriminator / T10 5 consecutive StoreError → 1 warning emission); 6 NEW vitest tests F1-F6 (F1 selection Set toggle / F2 checkbox overlay / F3 bulk action bar visibility / F4 NPopconfirm wrapping / F5 cross-page selection persistence / F6 dedupe on repeated toggle); 3 NEW vitest tests for `useBulkDeleteToast`; 2 NEW vitest tests for `useIllustrationStore.bulkDeleteAssets`; 16 atomic commits on master (Task A: 4 commits = 1 RED tests + 1 helper refactor + 1 bulk route + 1 fixup; Task B: 7 commits = 1 multi-select RED + 1 typed wrapper + 1 store action + 1 useBulkDeleteToast + 1 multi-select UI + 1 useBulkDeleteToast lazy useMessage + 1 useIllustration bulk wrapper + race comment; Task C: 3 commits = 1 regression guards + 1 invariant extension + 1 docs sync this commit) + 2 prior doc commits (spec + plan). v60.4 → v60.5)
> **更新者**: 协调者（Phase 100 Multi-Model Per Provider 闭环: 11 atomic direct master commits on `master` per 2026-09-15 simplified workflow; 9/9 regression guards GREEN; pytest lingwen-illustrations 218→248 (+30) + studio_api 149→157 (+8) + 9 NEW phase100 guards GREEN; vitest pre-existing failures verified unrelated (4/4 confirmed: human-first-nav, architecture-guards, GenerateIllustrationDialog pre-existing); pnpm tsc 0 new errors (48 pre-existing baseline unchanged); ruff clean on introduced files (7 pre-existing in other test files); knip 8 unused exports (3 pre-existing reference image + 5 new from T7 wrappers — acceptable, available for future use); I092 invariant recorded in architecture.yml; cluster cumulative Phase 90-100 = 11 phases / 1 NEW package + 5 carryover closures + 5 REQ-002 v2 sub-projects delivered (image providers + i2i + ProjectSettings+LRU + notification center + **multi-model per provider**); **REQ-002 v2 remaining**: 2 of 7 (atomic provider fallback + v2 settings persistence extension) + REQ-004 团队协作 (P4 separate brainstorming). 详见 `docs/superpowers/handoffs/2026-09-18-phase-100-multi-model-handoff.md` + spec `2026-09-18-phase-100-multi-model-design.md` + plan `2026-09-18-phase-100-multi-model.md`）
> **更新者**: 协调者（Phase 88 ARCHDEBT cycle PHYSICAL closure: 5 atomic commits C0-C3; 40/40 C2 regression guards PASS (G1/G1b revised to exclude top-level `infra/__pycache__/` for tracked `__init__.py`; G8 revised to recursive pycache check not "empty" check; G11 switched to PyYAML parsed-value per Phase 61 lesson); 75/75 prior-phase guards preserved; cluster cumulative Phase 53-88 = 16 phases / ~20464 LOC dead code + ~1.5 MB disk + 8 zero-consumer dirs + 6 真迁移 + 1 MERGE; v54.19 → v54.20）
> **更新者**: 协调者（Phase 86 ARCHDEBT-MINI 第 2 例 (Phase 78 was first): 4 atomic commits C0-C3; 12 files DELETE (-1670 bytes); 17/17 C3 regression guards GREEN (no expected-fail — NON-INVASIVE pattern); 13/13 prior-phase guards preserved without modification (only test_phase18_8 docstring updated); cluster cumulative Phase 53-86 = 14 phases / ~20138 LOC dead code + 8 zero-consumer dirs + 6 真迁移 + 1 MERGE; v54.17 → v54.18）> **优先级**: P0 > P1 > P2 > P3> **事实来源**: 本仓库当前版本在 `CLAUDE.md` v56.0；并行开发入口见根 `COORDINATION.md`---## 如何认领任务1. 从列表中选择一个任务2. 检查「状态」是 📋 待开始3. 将「状态」改为 🔄 进行中，填入「认领人」4. 大型任务在 `ACTIVE_TASK.md` 中添加详情5. 开始工作> 小型任务直接在 BACKLOG 中跟踪；大型任务移入 `ACTIVE_TASK.md`---## P0 - 阻塞项（无）| ID | 标题 | 描述 | 认领人 | 状态 | 创建日期 ||----|------|------|--------|------|----------|| - | - | 当前无 P0 阻塞项 | - | - | - |---## P1 - 立即处理（追平双会话遗留）| ID | 标题 | 描述 | 认领人 | 状态 | 创建日期 ||----|------|------|--------|------|----------|| P1-SHIM | batch-templates 契约 shim | 后端已 codegen `lingwen_shared` 的 batch-templates DTO（`StudioBatchTemplate`/`Create|Update`/`ListResponse`）但未更新 `apps/dashboard-contracts` 的 *DTO shim → 前端暂消费不到新类型。前端补 shim 后 Pilot 方可落地模板 UI | 前端 A（自服务完成） | ✅ 完成 | 2026-09-02 |---## P2 - Phase 26+ 候选（继承 carryover，按价值排序）

| ~~P2-EXTRACT-ENUM~~ | ✅ **Phase 92 DONE (2026-09-16)** — see handoff | ~~lingwen-shared TaskType enum 加 STRUCTURED_EXTRACTION — Phase 90 carryover: prompt_builder 现用 QUALITY_ANALYSIS 作 STRUCTURED_EXTRACTION 的语义替代。修：packages/lingwen-shared/src/lingwen_shared/contracts/python/llm.py:TaskType 加新成员（影响所有 LLMService consumer）。低成本单源改动~~ | 协调者（自服务） | ✅ Phase 92 完成（master direct commits: spec + plan + feat + test + refactor + test + docs, `STRUCTURED_EXTRACTION = "structured_extraction"` added to TaskType enum + `TASK_CONFIGS` entry in lingwen-llm-service (1500 max_tokens + 0.3 temperature for deterministic JSON) + prompt_builder switched to STRUCTURED_EXTRACTION; 7 lingwen-shared tests + 9 prompt_builder tests + 3 llm-service tests + 20 phase90 guards (G10 a/b/c) all GREEN; ruff clean except pre-existing E741; uv.lock auto-synced httpx dep for lingwen-illustrations package) | 2026-09-15 || ID | 标题 | 描述 | 认领人 | 状态 | 创建日期 ||----|------|------|--------|------|----------|| P2-INSIGHT | Pilot + Insight 看板集成 | 让 batch 进度汇入主看板 | 前端 A（自服务完成） | ✅ 完成 | 2026-09-02 || P2-DRAWER | per-chapter preview drawer | PilotLivePanel 章节预览抽屉 | 前端 A（自服务完成） | ✅ 完成 | 2026-09-02 || P2-QUEUE | batch priority queue | 生产排队，多任务有序。**后端 B 自服务列表第一个** | 后端 B（自服务）+ 前端 A（`PilotQueuePanel` 排队面板） | ✅ 完成 | 2026-09-02 || P2-RESTART | auto-restart on failure | 失败自动重启。**后端 B 自服务列表第二个** | 后端 B（自服务） | ✅ 完成 | 2026-09-02 || P2-MULTI | multi-LLM 并发批次 | 并行吞吐能力（较大，后端 B 自服务列表收尾项） | 后端 B（自服务完成） | ✅ 完成 | 2026-09-02 || P2-DTOMIGR | usePilotBatch DTO migration | 迁移至 `@/api/studio` re-export（技术债） | 会话-A（自服务完成） | ✅ 完成 | 2026-09-02 || P2-REG | Phase 114 prod preview regression | 2026-09-03 实测：cytoscape-fcose 历史根因已随图库迁移 vis-network 消失；真实回归为 v16.2.8 迁移遗留 3 处陈旧引用（useCreatorAdvanceBatch 旧名 generateCreatorVolumeSummary + api/index.js 桶内陈旧别名 + 补缺失 fetchCreatorOverview） | 协调者（本会话） | ✅ 修复 `80790b76` | 2026-09-03 || P2-WFSTATE | _last_* 散点 → WorkflowState dataclass | mc_workflow.py 5 个 self._last_* (不同默认值) 易半初始化。整合 dataclass 更类型安全、易测 | 后端 B（自服务） | ✅ 完成（Phase 26 commit `ee3396ae`） | 2026-09-03 || ~~P2-WFRUNNER~~ | ✅ Phase 27 DONE (2026-09-04) — see handoff | ~~run_workflow orchestration 90+ 行偏多。拆 service 后 run_workflow 仅做编排、helper 收口~~ | 后端 B（自服务） | ✅ Phase 27 完成（commit `9b57d16a` 待 ff-merge） | 2026-09-04 || ~~P2-RESUME-VERIFY~~ | ✅ Phase 28 DONE (2026-09-04) — see handoff | ~~start_nodes=None 时 resume_workflow 重跑 E2E 验证。代码 review 列为 important。scheduler 对已完成节点是否幂等无测试覆盖~~ | 后端 B（自服务） | ✅ Phase 28 完成（7 commits 待 ff-merge） | 2026-09-04 || P2-MC-WRITING | 84 pre-existing cascade failures 根因 | tests/got + tests/agent_system 共 84 failing，pre-existing（推测 mc_writing.py 类似 gutted）。需独立 phase | 后端 B（自服务） | ✅ Phase 29 完成（17 commits ff-merge `b1b748ad`，84→14 failed (-70)，1 prod 改动 `mc_editing.py:202`，11 测试文件调整） | 2026-09-04 || ~~P2-ARCHDEBT (剩余)~~ | ~~infra.got.* → packages/lingwen-got/ 迁移 + infra.world_model split~~ | ~~Phase 33+ 候选剩余：infra.got.* 迁 packages/lingwen-got/ + world_model/__init__ split (5 consumer migration)~~ | 后端 B（自服务） | ✅ **Phase 33+34+35 全部完成**（v33.0 LINGWEN-GOT 11 commits + v34.0 WORLD-MODEL-PACKAGE 10 commits；P2-ARCHDEBT 全部 CLOSED） | 2026-09-08 || **P3-ARCHDEBT** (NEW) | infra.{paths, project_config, logging_config, errors, studio_registry} → packages/ 迁移 | 5 个 infra 跨切关注点模块：paths (path resolver) / project_config (settings) / logging_config (logging setup) / errors (error base classes) / studio_registry (FastAPI app registry)。每个需独立 phase 评估 consumer 数 + 迁移策略；预计 multi-week。建议从依赖最少的 `errors` 开始（基础类被广泛 import 但消费者数可控）。**Phase 36 errors pilot 已闭环**（infra/errors → packages/lingwen-errors/; 4 packages + 8 intra-infra migrated; invariant #51 NEW; 6 phase36 guards GREEN）。**Phase 37 paths 已闭环**（86 consumers；invariant #52 NEW；6 phase37 guards GREEN）。**Phase 38 project_config 已闭环**（26 consumers；7 function-body via `\1` sed；1 fixup；invariant #53 NEW；7 atomic commits）。**Phase 39 logging_config 已闭环**（7 consumers + 1 wildcard；LEAF package；invariant #54 NEW；6 atomic commits）。**Phase 40a studio_registry 5/5a 已闭环**（50 consumers：47 production + 1 wildcard；NOT-LEAF 3-dep package；shim-not-delete pattern；C1.5 `factory_root()` fixup；invariant #55 NEW；8 atomic commits）。**Phase 40b studio_registry 5/5b 待启动**（~33 tests/ edits + DELETE shim + DELETE wildcard；4-5 atomic commits）。 | 后端 B（自服务） | 🔄 **Phase 36+37+38+39+40a 全部完成（P3-ARCHDEBT 5/5a CLOSED）**；Phase 40b 继续 tests/ migration + shim delete | 2026-09-09 || **P2-30-FAILURES** | ~~Phase 29 剩余 14 failed 一次性清零~~ | ~~5 类根因：① 7 stub `_state` 注入（一行修复）② 1 `_collect_decision_specs_from_graph` re-export 或 test 迁移 ③ 2 `_impl_audit_chapter` 韧性契约（真 prod 改动，需 design 确认边界）④ 2 `CostTrackerDB._connect()` API 迁移 ⑤ 2 env-var tests 重写 → `make_master_with_router()`~~ | 后端 B（自服务） | ✅ Phase 30 完成（9 commits ff-merge `38b854e7`，14→0 failed (-14)，2 prod 改动 mc_writing.py + cost_persistence.py + 5 test 文件调整） | 2026-09-07 || **P2-ARCHDEBT-MINI** | ~~P2-ARCHDEBT 子集 2/4 清理~~ | ~~**Sub-task A**：chapter_golden_path 反向 import 修复（迁 apps/studio_api/tests/golden_path_smoke.py）；**Sub-task B**：4 薄代理 → OrchestratorProxyMixin（mc_orchestrator_proxy.py new + MasterController MRO）；**12 文件 doc 同步** 修 stale "5 薄代理" → "4 薄代理"~~ | 后端 B（自服务） | ✅ Phase 31 完成（6 commits ff-merge `4dbe8939`，sub-task A -87/+106 行 + sub-task B mc_workflow 119→99 行 + 5 refactor-guard tests + 12 文件 doc 同步） | 2026-09-07 || **P2-SHIM-CLEANUP** | ~~P2-ARCHDEBT PHASE-COMPAT shim 删除 3/4~~ | ~~**Sub-task 1**：`infra/subplot/data_structures.py` (32 行, 0 消费者) 删除。**Sub-task 2**：`infra/world_model/data_structures.py` (69 行, 0 消费者) 删除。**Sub-task 3**：`packages/lingwen-core/src/lingwen_core/agents/master_controller.py` (11 行) 删除 + 6 test consumers (12 sites) 迁移到 canonical `lingwen_pipeline.master_controller`。**+1 fixup commit** 修 5 处漏检的 relative imports (infra/subplot/__init__.py:19 / infra/world_model/{__init__.py:60,key_point_graph.py:24,snapshot_store.py:24} / packages/lingwen-core/src/lingwen_core/agents/got_bridge.py:32)。**+10 regression guard tests** in tests/test_phase32_shim_cleanup.py。**N.14 lesson 1 (6th time)**：literal-text grep misses relative imports (`from .X`, `from ..X`); future shim-deletion phases must include `grep -rn "from \\.X\\b"` in audit~~ | 后端 B（自服务） | ✅ Phase 32 完成（8 commits ff-merge `9980e393`, -112 行 shim + 5 fixup files + 10 guard tests + 5 doc flips; invariant #48 NEW） | 2026-09-07 |> **后端 B 自服务顺序**：P2-QUEUE → P2-RESTART → P2-MULTI。每任务：rebase origin/master → 实现 → 完整 `pytest` + `ruff check` + `ruff format --check` 全绿 → 自 ff-merge 到 master → 认领下一个。**common 前置**：worktree 内跑测试/codegen 需 `export PYTHONPATH=$PWD/packages/lingwen-shared/src:$PYTHONPATH`（见 COORDINATION.md §6.5）。---## 已完成（近期）| 版本/阶段 | 内容 | 状态 ||------|------|------|| **v60.6 Phase 108 (bulk regenerate illustration endpoint — sixth Phase 102+ extension)** | master direct commits (per 2026-09-15 simplified workflow): mirrors Phase 107 bulk_delete pattern; PUT `/api/illustrations?slug=...&ids=...` (I090 6th caller + I095 6th EXTENDED via docstring); limit 10 (lower than delete's 50 — LLM calls 5-30s/asset); per-asset uniform `provider` / `model` / `fallback_chain` (no per-asset override — scope limit); 4 status values: `"ok"` / `"not_found"` (LoadError or asset id absent in chapter) / `"unknown_model"` (Phase 108 NEW) / `"stage_error"` (Stage 1/2/3 pipeline error); `_regenerate_asset_inner` helper extracted from regenerate_illustration route is **counter-only shim** (Option A architecture — pipeline.regenerate_illustration ALREADY emits record_event + publish at pipeline.py:585-607 with same ULID via `new_event_id()`, so helper does NOT call audit_log/publish directly to avoid breaking I091 "shared ULID double-write" invariant); `_load_regeneration_settings` NEW helper reads `.lingwen/illustration_settings.yaml` with permissive fallback (missing → {}; malformed → {}); sequential for-loop calls `_regenerate_asset_inner` per asset; pre-resolves `meta_by_id` dict once before loop / frontend: `useBulkRegenerateToast` composable (3 variants: success / partial / error — lazy useMessage for test isolation) + `bulkRegenerateAssets` typed wrapper (pre-validates 1..10) + `useIllustrationStore.bulkRegenerateAssets` action (lazy-imports API wrapper; reactively replaces regenerated metadata in local `assets[]`); `IllustrationGallery.vue` adds bulk-regenerate button alongside bulk-delete in sticky bottom bulk action bar wrapped in `<NPopconfirm>` mirroring Phase 106/107 pattern / 12 NEW pytest T1-T12 in `test_bulk_regenerate_api.py` (T1 happy 3×record_success / T2 partial 2 ok + 1 stage_error / T3 empty 422 / T4 over-10 422 / T5 slug not found 404 / T6 LoadError 404 before helper / T7 dedupe 8 raw → 4 unique / T8 cross-project not_found / T9 unknown_model status / T10 3 consecutive StoreError → 1 warning / T11 abort controller timeout / T12 perf guard sequential for-loop) + 10 NEW pytest G1-G10 regression guards in `test_phase108_bulk_regenerate.py` (G1 route registered / G2 11-id 422 / G3 empty 422 / G4 per-asset helper fan-out / G5 for-loop iteration / G6 counter isolation preserved / G7 regenerate_illustration unchanged (pipeline owns emit) / G8 invariant extensions in architecture.yml / G9 dict.fromkeys dedupe / G10 helper does NOT call audit_log/publish — Option A guard) + 8 NEW vitest F1-F8 IllustrationGallery bulk-regenerate + 3 NEW vitest useBulkRegenerateToast + 2 NEW vitest useIllustrationStore.bulkRegenerateAssets / 2 invariant extensions — I090 6th caller + I095 6th EXTENDED via docstring only (rule fields add `bulk_regenerate_assets` as 6th caller; I095 EXTENDED notes per-asset record_failure/record_success inherited from shared helper; counter keys still `(slug, "regeneration")`; threshold crossing per `(slug, "regeneration")` pair emits exactly one severity="warning" notification regardless of bulk iteration size; helper does NOT call audit_log/publish — Option A: pipeline.regenerate_illustration owns emit to avoid breaking I091). **Validation**: pytest test_bulk_regenerate_api 12/12 NEW T1-T12 GREEN + test_phase108_bulk_regenerate 10/10 NEW G1-G10 GREEN + Phase 102/103/104/105/106/107 preserved GREEN + ruff clean on introduced + pnpm tsc 0 new errors (48 pre-existing baseline unchanged) + vitest IllustrationGallery 8/8 NEW F1-F8 + useBulkRegenerateToast 3/3 NEW + useIllustrationStore 2/2 NEW + Phase 106/107 frontend tests preserved. **8 lessons**: (1) Option A architecture prevents double-emission breaking I091 (helper is counter-only; pipeline owns audit_log + publish); (2) 3-tuple helper return (status, new_meta, exc) preserves exception detail for STAGE_HTTP_CODES mapping; (3) `dict.fromkeys` dedupes preserving order; (4) Sequential for-loop preserves audit_log arrival order; (5) Lazy useMessage enables test isolation without n-message-provider; (6) Pipeline emit block scope ONLY on success path, NOT on failure paths (counter incremented but audit_log.record_event + notifications.publish deferred to caller); (7) Lower limit for regenerate (10 vs delete's 50) reflects 5-30s/asset LLM latency; (8) Per-asset uniform params (no per-asset override) simplifies scope. **Cluster cumulative**: Phase 90-108 = 19 phases / 1 NEW package + 5 carryover closures + 7 REQ-002 v2 sub-projects delivered + **6 Phase 102+ extensions (Phase 103 default_models + Phase 104 notify_threshold per event_type + Phase 105 cleanup_route failure tracking + Phase 106 delete_asset failure tracking + Phase 107 bulk_delete_assets + Phase 108 bulk_regenerate_assets)**. **regeneration event_type FULLY wired for bulk path** — I090 6th caller + I095 6th EXTENDED via docstring (counter keys `(slug, "regeneration")`); I091 inherits via pipeline.regenerate_illustration's existing emit (no new double-write site — Option A). 详见 `docs/superpowers/handoffs/2026-09-22-phase-108-bulk-regenerate-handoff.md` + spec `docs/superpowers/specs/2026-09-22-phase-108-bulk-regenerate-design.md` + plan `docs/superpowers/plans/2026-09-22-phase-108-bulk-regenerate.md` | ✅ pytest 22/22 NEW (T1-T12 + G1-G10) + Phase 102-107 preserved + vitest 13/13 NEW + ruff clean + tsc 0 new |
| **v60.5 Phase 107 (bulk delete illustration endpoint — fifth Phase 102+ extension)** | master direct commits (per 2026-09-15 simplified workflow): 4-module change (1 backend route `apps/studio_api/routes/illustrations.py` extract NEW `_delete_asset_inner(slug, asset_id, *, project_root, threshold, mode: Literal["single", "bulk"] = "single")` helper from existing `delete_asset` route (Phase 106 5-path logic verbatim) + new `bulk_delete_assets` route `@app.delete("/api/illustrations")` (I090 5th caller + I091 5th double-write site) sequential for-loop calls `_delete_asset_inner` per asset; limit 50 (422 on over-50), dedupe via `dict.fromkeys(raw_ids)` preserving order, 422 on empty ids, 404 on slug; per-asset returns 200 OK + `{deleted, failed, summary}` shape; partial failures retained in `failed[]` with status `"not_found"` / `"store_error"`; pre-resolves `meta_by_id` dict once before loop; `mode` parameter interpolated into `audit_log.record_event(extra=...)` + `notifications.publish(extra=...)` for downstream analytics / 1 modified component `IllustrationGallery.vue` multi-select state (`selection: Set<string>` + `selectionTick: ref`) + per-card `<NCheckbox>` overlay + sticky bottom bulk action bar containing `<NPopconfirm>`-wrapped bulk delete button / 3 NEW frontend files — `useBulkDeleteToast` composable (3 variants: success / partial / error) + `bulkDeleteAssets` typed wrapper (pre-validates 1..50) + `useIllustrationStore.bulkDeleteAssets` action (lazy-imports API wrapper; reactively removes deleted + not_found from local assets[]) / 5 test files — 10 NEW pytest T1-T10 in `test_bulk_delete_api.py` (T1 happy 5×record_success + 5×audit + 5×publish / T2 partial 3 ok + 2 store_error / T3 empty 422 / T4 over-50 422 / T5 slug not found 404 / T6 LoadError raises 404 before helper / T7 dedupe 8 raw → 4 unique / T8 cross-project not_found / T9 audit mode='bulk' discriminator / T10 5 consecutive StoreError → 1 warning emission) + 10 NEW pytest G1-G10 regression guards in `test_phase107_bulk_delete.py` (G1 route registered / G2 51-id 422 / G3 empty 422 / G4 per-asset helper fan-out / G5 for-loop iteration / G6 counter isolation preserved / G7 delete_asset uses helper mode="single" / G8 invariant extensions in architecture.yml / G9 dict.fromkeys dedupe / G10 no explicit cross-project check) + 6 NEW vitest F1-F6 IllustrationGallery multi-select + 3 NEW vitest useBulkDeleteToast + 2 NEW vitest useIllustrationStore.bulkDeleteAssets / 3 invariant extensions — I090 / I091 / I095 EXTENDED 5th via docstring only). **Validation**: pytest apps/studio_api/tests/test_bulk_delete_api.py 10/10 NEW T1-T10 GREEN + tests/test_phase107_bulk_delete.py 10/10 NEW G1-G10 GREEN + Phase 102/103/104/105/106 10/10 preserved GREEN + ruff clean on introduced + pnpm tsc 0 new errors (48 pre-existing baseline unchanged) + vitest IllustrationGallery 6/6 NEW F1-F6 GREEN + useBulkDeleteToast 3/3 NEW + useIllustrationStore 2/2 NEW + Phase 106 frontend tests preserved. **9 lessons**: (1) Helper refactor preserves single-delete contract (test isolation crucial); (2) Pre-resolve `meta_by_id` dict once before loop (50×M filesystem reads → O(1) dict lookup); (3) `dict.fromkeys` dedupes preserving order; (4) Sequential for-loop intentional (preserves audit_log + SSE arrival order); (5) Vue 3 Set reactivity requires `selectionTick.value++`; (6) `mode='bulk'` parameter does NOT change counter state (analytics only); (7) NPopconfirm cancel path purely frontend (inherited from Phase 106); (8) `Literal` type import safe at module top (no runtime cost); (9) NPopconfirm wrapping mirrors single-delete pattern (`positive-text="确认删除"` / `negative-text="取消"`). **Cluster cumulative**: Phase 90-107 = 18 phases / 1 NEW package + 5 carryover closures + 7 REQ-002 v2 sub-projects delivered + **5 Phase 102+ extensions (Phase 103 default_models + Phase 104 notify_threshold per event_type + Phase 105 cleanup_route failure tracking + Phase 106 delete_asset failure tracking + Phase 107 bulk_delete_assets)**. **I090/I091/I095 coverage COMPLETE** — 5 callers / 5 double-write sites / 5 extensions via docstring; all 4 event_types have ≥1 caller for both single and bulk paths. 详见 `docs/superpowers/handoffs/2026-09-22-phase-107-bulk-delete-handoff.md` + spec `2026-09-22-phase-107-bulk-delete-design.md` + plan `docs/superpowers/plans/2026-09-22-phase-107-bulk-delete.md` | ✅ pytest 20/20 NEW (T1-T10 + G1-G10) + Phase 102-106 10/10 preserved + vitest 11/11 NEW + ruff clean + tsc 0 new |
| **v60.4 Phase 106 (delete illustration endpoint failure tracking — fourth Phase 102+ extension)** | master direct commits (per 2026-09-15 simplified workflow): 4-module change (1 backend route `apps/studio_api/routes/illustrations.py` add `_load_deletion_settings(project_root)` NEW helper reads `.lingwen/illustration_settings.yaml` with permissive fallback missing file → {}; malformed yaml → {}; mirrors Phase 105 `_load_cleanup_settings` for module-locality; wire `delete_asset` route into notifications failure tracking: 404 LoadError on `project_root_for` → `record_failure(slug, e, project_root=None, threshold=resolve_threshold({}, "deletion"), event_type="deletion")` (counter increments; audit_log skipped since no real project context); 404 asset-not-found → `record_success(slug, event_type="deletion")` **no-op success** — asset is already gone, idempotent; matches Phase 105 cleanup_route "0 deleted under limit" pattern; StoreError on `storage.delete_asset` → `record_failure(slug, e, project_root=real_root, threshold=resolved, event_type="deletion")` + HTTPException 500; successful `storage.delete_asset` → `record_success(slug, event_type="deletion")` in `else` branch of try/except; `audit_log.record_event` + `notifications.publish` **double-write** I091 completion — same ULID via `new_event_id()` — for all 4 event_types now; `extra={"trigger": "manual"}` consistent with cleanup_route pattern; `import yaml` + `from lingwen_illustrations import audit_log, notifications` at module top / 1 frontend component `IllustrationCard.vue` 🗑 button wrapped in `<NPopconfirm>` (Naive UI) — `positive-text="确认删除"` / `negative-text="取消"`; data-testid="delete-btn" + `:aria-label` preserved for a11y; cancel path is purely frontend (no `emit('delete')` fires, no network call, no counter touched) / 3 test files — 6 NEW pytest T1-T6 deletion failure paths + 6 NEW pytest T7-T9 deletion isolation + helper resilience + 6 NEW pytest G1-G6 regression guards + 3 NEW vitest F1-F3 NPopconfirm (F4 Esc-dismissal removed in `781cf019`) / 1 invariant extensions — I090 / I091 / I095 EXTENDED via docstring only). **Validation**: pytest apps/studio_api/tests/test_illustrations_api.py 6/6 NEW T1-T6 GREEN + packages/lingwen-illustrations/tests/test_phase106_deletion_isolation.py 6/6 NEW T7-T9 GREEN + tests/test_phase106_delete_endpoint_failure_tracking.py 6/6 NEW G1-G6 GREEN + Phase 102/103/104/105 guards 34/34 preserved GREEN + ruff clean on introduced + pnpm tsc 0 new errors (48 pre-existing baseline unchanged). 12 atomic commits on master: 2 docs (spec `f459ddad` + plan `cd04e148`) + 2 test (T1-T6 RED `3ca5a216` + T7-T9 RED-ish `c82a3ce0`) + 1 helper feat (`d50ee0ad`) + 1 wire feat (`03e1606c`) + 2 frontend (NPopconfirm tests RED `388be067` + NPopconfirm wrapping `5d7adbbf`) + 1 fixup (remove F4 `781cf019`) + 1 G1-G6 regression guards `8d4f4a49` + 1 invariant extension `23466a79` + 1 docs sync (this commit). **7 lessons**: (1) 404 asset-not-found = no-op success (defensive counter reset, idempotent — symmetric with Phase 105 cleanup_route "0 deleted under limit"); (2) naive-ui NPopconfirm doesn't support Esc-by-default (F4 test removed — behavior is click-outside / button-click only); (3) Tuple-keyed counter isolation (Phase 104 invariant inherited unchanged — verified via G5); (4) `_resolve_threshold` defensive fallback (`_load_deletion_settings` returns `{}` on missing/malformed yaml → `resolve_threshold({}, "deletion")` returns INFINITY); (5) I095 EXTENDED via docstring preserves YAGNI (4th extension of same invariant name — G6 verifies via parsed YAML value); (6) Frontend module imports safe at top (`import yaml` + `from lingwen_illustrations import audit_log, notifications` — lightweight modules, no import-time side effects verified in cleanup_route.py:27); (7) NPopconfirm cancel path is purely frontend (no `emit('delete')` fires, no counter touched — backend never sees cancel). **Cluster cumulative**: Phase 90-106 = 17 phases / 1 NEW package + 5 carryover closures + 7 REQ-002 v2 sub-projects delivered + **4 Phase 102+ extensions (Phase 103 default_models + Phase 104 notify_threshold per event_type + Phase 105 cleanup_route failure tracking + Phase 106 delete_asset failure tracking)**. **deletion event_type is now FULLY wired** — all 4 event_types have ≥1 caller + counter isolation + I090/I091/I095 invariant coverage complete; no more orphan references. 详见 `docs/superpowers/handoffs/2026-09-21-phase-106-delete-endpoint-failure-tracking-handoff.md` + spec `2026-09-21-phase-106-delete-endpoint-failure-tracking-design.md` + plan `docs/superpowers/plans/2026-09-21-phase-106-delete-endpoint-failure-tracking.md` | ✅ |
| **v60.2 Phase 104 (notify_threshold per event_type — second Phase 102+ extension)** | 11 atomic direct master commits on `master` per 2026-09-15 simplified workflow = 2 docs (spec `3dba2cce` + plan `10260061`) + 1 backend validator int\|dict + 8 guards RED `be4a14f0` + 1 ruff cleanup `d8f45200` + 1 notifications tuple-keyed counter + event_type args `e2c71045` + 1 pipeline `_resolve_threshold` helper + 2 caller sites `0b80a870` + 1 defensive fallback fixup `59c6423c` + 1 TS type widening + NOTIFY_EVENT_TYPES const `b332987e` + 1 store normalize/denormalize + 4 tests `2dbe94d3` + 1 normalize failure-path defaults fixup `4a7ad765` + 1 ProjectSettingsIllustration Notify Thresholds subsection + 6 tests `89229a5f` + 1 I095 EXTENDED + architecture.yml + CLAUDE.md I095 line text `07e88357`. 6-module change: (1 backend schema `notify_threshold: int \| dict[str, int]` + Pydantic `mode="before"` `_validate_notify_threshold` validator auto-expanding int legacy to 4-key dict + KNOWN_NOTIFY_EVENT_TYPES ClassVar + reject unknown event_type/boolean/value < 1) / (1 notifications.py counter keys widen from bare slug to (slug, event_type) tuple + INFINITY_THRESHOLD sentinel `math.inf` + record_failure signature adds `event_type: str` kwarg + record_success signature adds `event_type: str` positional arg + `_emit_failure_warning` signature adds `event_type`) / (1 pipeline helper `_resolve_threshold` defensively handling int/dict/bool + 2 caller sites generate_illustration + regenerate_illustration pass event_type kwarg) / (1 frontend type `api/illustrations.ts` NotifyEventType + NOTIFY_EVENT_TYPES 4-element const + notify_threshold widens to `number \| Record<NotifyEventType, number>` Pick-based whitelist unchanged) / (1 store helper `useProjectSettings.normalizeNotifyThreshold` + `denormalizeNotifyThreshold` — int → 4-key dict; dict → unchanged; undefined → empty dict; failure-path defaults to 3) / (1 component `ProjectSettingsIllustration.vue` new "Notify Thresholds" subsection — 4 fixed rows × NumberInput + ∞ placeholder + Reset-all button). **Phase 102 test file key update**: 1 test file (`test_notifications.py`) Phase 102 tests rewritten from `_consecutive_failures[slug]` to `_consecutive_failures[(slug, event_type)]` keys (no semantic change). **I095 EXTENDED** via docstring only — same invariant name, wider scope: keyed per `(project_slug, event_type)` tuple, exactly one warning per `(slug, event_type)` pair. REQ-002 v2 FULLY CLOSED post-Phase 102 — Phase 104 is the second Phase 102+ extension (Phase 103 was first). 详见 `docs/superpowers/handoffs/2026-09-21-phase-104-notify-threshold-per-event-type-handoff.md` + spec `2026-09-21-phase-104-notify-threshold-per-event-type-design.md` + plan `2026-09-21-phase-104-notify-threshold-per-event-type.md` | ✅ |
| **v60.1 Phase 103 (Per-Chapter default_models Overrides — first Phase 102+ extension)** | 14 atomic direct master commits: 3 docs (spec + plan + plan fix) + 7 feat/test (schema RED + schema GREEN + YAGNI revert + pipeline semantic tests + TS Pick whitelist + store sync test + UI column + UI followup refactor) + 8 regression guards G1-G8 + I094 EXTENDED via docstring + CLAUDE.md v60.0 → v60.1 + handoff + BACKLOG + MEMORY sync. 5-module change (1 backend schema + 1 frontend type + 1 frontend component + 2 test files). **Pipeline code UNTOUCHED** — `merge_chapter_settings` shallow merge already replaces entire default_models dict correctly; `resolve_model` reads effective_settings which is already chapter-aware. Cross-reference validator reuses existing `fallback_models` pattern (Phase 100/102). Pick-based TypeScript whitelist mirrors backend whitelist automatically. Cascade picker UX reuse (Phase 100 `fetchProviderModels` API). REQ-002 v2 FULLY CLOSED post-Phase 102 — Phase 103 is the first Phase 102+ extension. 详见 `docs/superpowers/handoffs/2026-09-20-phase-103-per-chapter-default-models-handoff.md` + spec `2026-09-20-phase-103-per-chapter-default-models-design.md` + plan `2026-09-20-phase-103-per-chapter-default-models.md` | ✅ || v25.9 (Phase 25.9) | human_review 流水线修复：mc_workflow.py 还原（git history 5c4259e5）+ 新 GoTScheduler API 对齐 + 4 dashboard smoke 解 skip + cascade +15 fixed / 0 new。master `0a6f4346` 4 commit | ✅ || v25.1 (Phase 25.1) | 双会话并行开发合流：Track A event_types 过滤开关 + Track B batch templates，0 冲突 ff-merge 至 `a1826b33` | ✅ || v25.0 (Phase 25) | SSE batch 增强 Filter/Replay/Auth（服务端 event_types 过滤 + 连接重放 + 读取门控） | ✅ || v24.0 (Phase 24) | SSE 实时 batch 进度（`studio_batch_streamer.py` + `/events` 路由）替代 3s 轮询 | ✅ || v23.0 (Phase 23) | Pilot 页（`PilotPage.vue` + 5 组件 + `usePilotBatch`） | ✅ || v22.0 (Phase 22) | test-env + ruff format 清理 | ✅ || v21.0 (Phase 21) | shim cleanup（删 `infra/consistency/` + `infra/agent_system/`） | ✅ |> 完整历史版本链见 `CLAUDE.md` 版本块（v21.0 → v25.1）。---## 需求池（待评估）| ID | 标题 | 来源 | 描述 | 优先级建议 ||----|------|------|------|------------|| REQ-001 | 创作者模式增强 | 用户 | 陪伴/推进模式功能扩展（Track A 已交付 A/B/C/D/E 全部切片：`useCreatorMode` 单一来源、陪伴「今日下一步」、推进「批改节奏带」、写栏「模式引导条」、「差异收尾」清单） | 完成 || REQ-002 | 多模态支持 | 用户 | 封面/插图生成 | P3 || REQ-003 | 移动端适配 | 用户 | Dashboard 移动端体验优化。✅ `34f0b0f2`：接通锁层移动抽屉（汉堡+遮罩+Esc/导航收起），旧 style.css 选择器失配修复；核心页经审计已由 flex-wrap + 既有 media query 自然适配 | 完成 || REQ-004 | 团队协作功能 | 用户 | 多用户协作编辑 | P4 |---## 素材生成任务（Trae Worker）| ID | 标题 | 描述 | 认领人 | 状态 | 创建日期 ||----|------|------|--------|------|----------|| ASSET-001~011 | sidebar SVG 图标（11 模块 + 1 Pilot follow-up） | 侧栏 emoji → Phosphor 双色 SVG；Vue SFC 扁平；`--lingwen-icon-accent` CSS var 主题；nav 配置 `.map()` 集成 | 协调者（自服务） | ✅ 完成 | 2026-09-10 |> 风格要求：现代科技/动漫风格、暗色底、霓虹紫蓝、渐变发光。---## 标签索引| 标签 | 说明 ||------|------|| 📋 | 待开始 || 🔄 | 进行中 || ⏳ | 待验证/待规划 || ✅ | 已完成 || 🔴 | 阻塞 || ❌ | 已取消 |---## 新增任务模板```markdown| Px-XXX | 任务标题 | 简短描述 | 待认领 | 📋 待开始 | YYYY-MM-DD |```---## 最近变更| 时间 | 更新者 | 变更 ||------|--------|------|| 2026-09-22 | 协调者 | **v60.5 Phase 107 (bulk delete illustration endpoint — fifth Phase 102+ extension — REQ-002 v2 stays FULLY CLOSED; Phase 107 is additive extension)**: master direct commits (per 2026-09-15 simplified workflow): 16 atomic commits = 2 docs (spec `1e2fcac8` + plan `5485f879`) + 4 backend (T1 RED tests `44cfc5cc` + T2 helper refactor `70c529f6` + T3 bulk route `f50d0888` + T3 fixup `59e340c3`) + 7 frontend (F1 multi-select RED `f764d107` + F2 typed wrapper `b616ddd0` + F3 store action `1f32c898` + F4 useBulkDeleteToast `8322a3d5` + F5 multi-select UI `c836b26b` + F5 fixup `33a32384` + F6 lazy useMessage `2a117839`) + 3 docs sync (T12 G1-G10 regression guards `d2f3315d` + T13 I090/I091/I095 EXTENDED `79945b33` + T14 docs sync this commit). **4-module change** (1 backend route `apps/studio_api/routes/illustrations.py` extract NEW `_delete_asset_inner(slug, asset_id, *, project_root, threshold, mode: Literal["single", "bulk"] = "single")` helper from existing `delete_asset` route + new `bulk_delete_assets` route `@app.delete("/api/illustrations")` sequential for-loop calls `_delete_asset_inner` per asset; limit 50, dedupe via `dict.fromkeys(raw_ids)`, 422 on empty ids, 404 on slug; per-asset returns 200 OK + `{deleted, failed, summary}`; partial failures retained in `failed[]`; pre-resolves `meta_by_id` dict once before loop) / (1 modified component `IllustrationGallery.vue` multi-select state + per-card `<NCheckbox>` overlay + sticky bottom bulk action bar containing `<NPopconfirm>`-wrapped bulk delete button) / (3 NEW frontend files — `useBulkDeleteToast` composable + `bulkDeleteAssets` typed wrapper + `useIllustrationStore.bulkDeleteAssets` action) / (5 test files — 10 NEW pytest T1-T10 + 10 NEW pytest G1-G10 + 6 NEW vitest F1-F6 + 3 NEW vitest useBulkDeleteToast + 2 NEW vitest useIllustrationStore) / (3 invariant extensions — I090 / I091 / I095 EXTENDED 5th via docstring only). **Validation**: pytest 20/20 NEW (T1-T10 + G1-G10) + Phase 102-106 10/10 preserved + vitest 11/11 NEW + ruff clean on introduced + pnpm tsc 0 new errors (48 pre-existing baseline unchanged). **9 lessons**: (1) Helper refactor preserves single-delete contract (test isolation crucial — Phase 106 T1-T6 + G1-G6 unchanged GREEN); (2) Pre-resolve `meta_by_id` dict once before loop (50×M filesystem reads → O(1) dict lookup); (3) `dict.fromkeys` dedupes preserving order; (4) Sequential for-loop intentional (preserves audit_log + SSE arrival order; parallel would break I091); (5) Vue 3 Set reactivity requires `selectionTick.value++` (or Set replacement); (6) `mode='bulk'` parameter does NOT change counter state (analytics only); (7) NPopconfirm cancel path purely frontend (inherited from Phase 106); (8) `Literal` type import safe at module top (no runtime cost); (9) NPopconfirm wrapping mirrors single-delete pattern (`positive-text="确认删除"` / `negative-text="取消"`). **Cluster cumulative**: Phase 90-107 = 18 phases / 1 NEW package (lingwen-illustrations) + 5 carryover closures + 7 REQ-002 v2 sub-projects delivered + **5 Phase 102+ extensions (Phase 103 default_models + Phase 104 notify_threshold per event_type + Phase 105 cleanup_route failure tracking + Phase 106 delete_asset failure tracking + Phase 107 bulk_delete_assets)**. **I090/I091/I095 coverage COMPLETE** — 5 callers / 5 double-write sites / 5 extensions via docstring; all 4 event_types have ≥1 caller for both single and bulk paths. **Future work**: telemetry-driven chain reorder (gated on Phase 102-107 failure tracker data accumulation) / delete from NotificationsPage dropdown (Phase 108+) / bulk REGENERATE (Phase 110+ YAGNI eval) / ARCHDEBT-REAL continuation (如申请). 详见 `docs/superpowers/handoffs/2026-09-22-phase-107-bulk-delete-handoff.md` + spec `2026-09-22-phase-107-bulk-delete-design.md` + plan `docs/superpowers/plans/2026-09-22-phase-107-bulk-delete.md` |
| 2026-09-21 | 协调者 | **v60.2 Phase 104 (notify_threshold per event_type — second Phase 102+ extension — REQ-002 v2 stays FULLY CLOSED; Phase 104 is additive extension)**: master direct commits (per 2026-09-15 simplified workflow): 11 atomic commits = 2 docs (spec `3dba2cce` + plan `10260061`) + 1 backend validator int\|dict + 8 guards RED `be4a14f0` + 1 ruff cleanup `d8f45200` + 1 notifications tuple-keyed counter + event_type args `e2c71045` + 1 pipeline `_resolve_threshold` helper + 2 caller sites `0b80a870` + 1 defensive fallback fixup `59c6423c` + 1 TS type widening + NOTIFY_EVENT_TYPES const `b332987e` + 1 store normalize/denormalize + 4 tests `2dbe94d3` + 1 normalize failure-path defaults fixup `4a7ad765` + 1 ProjectSettingsIllustration Notify Thresholds subsection + 6 tests `89229a5f` + 1 I095 EXTENDED + architecture.yml + CLAUDE.md I095 line text `07e88357` (Task 9) + handoff + BACKLOG + MEMORY + CURRENT_STATUS sync (Task 10 — this commit). **6-module change** (1 backend schema `notify_threshold: int \| dict[str, int]` + Pydantic `mode="before"` `_validate_notify_threshold` validator auto-expanding int legacy to 4-key dict + KNOWN_NOTIFY_EVENT_TYPES ClassVar + reject unknown event_type / boolean / value < 1) / (1 notifications.py counter keys widen from bare slug to (slug, event_type) tuple + INFINITY_THRESHOLD sentinel `math.inf` + record_failure signature adds `event_type: str` kwarg + record_success signature adds `event_type: str` positional arg + `_emit_failure_warning` signature adds `event_type` was hardcoded "generation" in Phase 102; Phase 104 uses pipeline-passed value for `audit_log.event` + `NotificationEvent.event_type`) / (1 pipeline helper `_resolve_threshold` defensively handling int/dict/bool — bool excluded to avoid silent truthy acceptance; missing dict keys = INFINITY for never-warn; negated isinstance `isinstance(nt, (int, float))` not `not bool` check) + 2 caller sites generate_illustration + regenerate_illustration resolve per-type `_resolve_threshold(effective_settings, "generation"/"regeneration")` and pass `event_type` kwarg to record_failure + record_success) / (1 frontend type `api/illustrations.ts` NotifyEventType + NOTIFY_EVENT_TYPES 4-element const + notify_threshold widens to `number \| Record<NotifyEventType, number>` Pick-based whitelist unchanged) / (1 store helper `useProjectSettings.normalizeNotifyThreshold` + `denormalizeNotifyThreshold` — int → 4-key dict via Object.fromEntries; dict → unchanged; undefined → empty dict; failure-path defaults to 3 not raise; preserves user UX when backend sends unexpected value) / (1 component `ProjectSettingsIllustration.vue` new "Notify Thresholds" subsection — 4 fixed rows × NumberInput + ∞ placeholder for unconfigured types + Reset-all-to-default button; `formatThreshold` + `onThresholdChange` + `resetAllThresholds` script-setup methods; replaces existing notify_threshold slider). **Phase 102 test file key update**: 1 test file (`test_notifications.py`) Phase 102 tests rewritten from `_consecutive_failures[slug]` to `_consecutive_failures[(slug, event_type)]` keys (no semantic change; same warning emit logic). **I095 EXTENDED** via docstring only — same invariant name, wider scope description: keyed per `(project_slug, event_type)` tuple, exactly one warning per `(slug, event_type)` pair; `_consecutive_failures` state mutation only via record_failure/record_success helpers; `_warning_emitted` flag still per pair prevents spam. NO new invariant introduced per YAGNI. **Validation**: pytest lingwen-illustrations validator 8/8 G1-G4b + G5/G5b (Task 3 RED→GREEN) + notifications 6/6 NEW (Task 4 record_failure/record_success with event_type + counter isolation) + pipeline Phase 102 preserved after 1 test file tuple-key update + studio_api ProjectSettings schema GREEN + vitest useProjectSettings 4/4 NEW (Task 7 normalize/denormalize round-trip) + ProjectSettingsIllustration Notify Thresholds 6/6 NEW (Task 8 4 rows + NumberInput change + reset-all + empty displays '∞' + legacy int uniform + partial dict empty cells) + Phase 102/103 frontend tests preserved (chapter_overrides 5/5 + default_models 7/7) + pnpm tsc 0 new errors (48 pre-existing baseline unchanged) + ruff clean on introduced (2 fixups `d8f45200` ruff --fix + `59c6423c` defensive fallback). **6 lessons**: (1) Tuple-keyed counter isolation — failures in different event types do NOT bleed into each other's counters (independent failure windows); (2) Pydantic `mode="before"` validator cleanly widens `int` to `int \| dict` via auto-expansion (existing settings.yaml with int legacy works without migration; partial dict empty = opt-out-all; partial dict missing key = INFINITY for never-warn); (3) Strict partial-dict semantics — explicit opt-in per type via dict presence; unconfigured keys default to INFINITY for never-warn; matches Phase 103 default_models "partial = explicit opt-in" pattern; (4) `_resolve_threshold` defensive fallback for non-dict non-numeric — bool exclusion via negated isinstance prevents silent truthy acceptance (e.g. `True` would have triggered legacy int branch); (5) `_normalize_int` failure-path defaults to 3 not raise — preserves user UX when backend sends unexpected value (frontend telemetry-friendly); (6) I095 EXTENDED via docstring preserves YAGNI — same invariant name with wider scope description (keyed per tuple, exactly one warning per pair); no new invariant introduced. **Cluster cumulative**: Phase 90-104 = 15 phases / 1 NEW package (lingwen-illustrations) + 5 carryover closures (Phase 91-95) + 7 REQ-002 v2 sub-projects delivered (Phase 96-102, REQ-002 v2 FULLY CLOSED post-Phase 102) + **2 Phase 102+ extensions (Phase 103 default_models + Phase 104 notify_threshold per event_type)**. **Phase 104 is the second Phase 102+ extension** — Phase 103 was first; architecture supports small additive feature extensions without breaking existing invariants. **Future work**: cleanup_route failure tracking（候选 Phase 105 小扩展）/ deletion event_type tracking（依赖 DELETE endpoint 加入）/ telemetry-driven chain reorder（依赖 Phase 102 failure tracker 数据积累）/ ARCHDEBT-REAL continuation（如申请）。详见 `docs/superpowers/handoffs/2026-09-21-phase-104-notify-threshold-per-event-type-handoff.md` + spec `2026-09-21-phase-104-notify-threshold-per-event-type-design.md` + plan `2026-09-21-phase-104-notify-threshold-per-event-type.md` |
| 2026-09-21 | 协调者 | **v60.1 Phase 103 (Per-Chapter default_models Overrides — first Phase 102+ extension — REQ-002 v2 stays FULLY CLOSED; Phase 103 is additive extension)**: master direct commits (per 2026-09-15 simplified workflow): 14 atomic commits = 3 docs (spec `85b9b33f` + plan `7284636c` + plan fix `93782e41`) + 4 backend (schema RED `d59526a9` + schema GREEN `06b296bc` + YAGNI revert `6c596b43` + pipeline semantic tests `5cd327c4`) + 1 frontend type (TS Pick whitelist `96b0ec63`) + 1 store sync test `41b3fed4` + 2 UI (default_models column `769e5ce3` + UI followup refactor `53e0d8a8`) + 3 guards/docs (8 regression guards G1-G8 `d259f615` + G8 explicit SHA fixup `1db22827` + CLAUDE.md v60.0 → v60.1 + I094 extended `45528b81`) + handoff `+BACKLOG+MEMORY sync` (Task 11 — this commit). **5-module change** (1 backend schema `_CHAPTER_OVERRIDABLE_FIELDS += "default_models"` + cross-reference validator against `KNOWN_PROVIDERS` + `provider.KNOWN_MODELS` / 1 frontend type `Pick<ProjectSettings, ...>` adds `'default_models'` / 1 frontend component `ProjectSettingsIllustration.vue` Default Models column with cascade picker / 2 test files — backend schema + pipeline semantic). **Pipeline code UNTOUCHED** — `merge_chapter_settings` shallow merge (`{**settings, **subset}`) already replaces entire `default_models` dict correctly (whole-dict, not per-key); `resolve_model` reads `effective_settings["default_models"]` which is already chapter-merged. Cross-reference validator reuses existing `fallback_models` pattern (Phase 100/102). Pick-based TypeScript whitelist mirrors backend whitelist automatically (no store-side code change). Cascade picker UX reuse (Phase 100 `fetchProviderModels(provider)` API for symmetric UX with `fallback_models` keyed table). **I094 EXTENDED** via docstring only (Phase 102 invariant name reused with wider scope description; NO new invariant introduced per YAGNI). **Validation**: pytest lingwen-illustrations schema 10/10 (Task 3) + pipeline 6/6 (Task 5) + regression guards 8/8 G1-G8 (Task 9) GREEN + studio_api ProjectSettings schema GREEN + vitest useProjectSettings 2/2 NEW (Task 7) + ProjectSettingsIllustration default_models 7/7 (Task 8 + 8.5) + chapter_overrides 5/5 preserved (Phase 102) + 36-test full illustrations suite GREEN + pnpm tsc 0 new errors (48 pre-existing baseline unchanged) + ruff clean on introduced. **10 lessons**: (1) Shallow merge semantic for dict override (`{**settings, **subset}` replaces whole dict not per-key); (2) Pipeline code untouched (resolve_model reads already-merged effective_settings); (3) No new invariants introduced (YAGNI — Pydantic field_validator contract self-documents); (4) Pick-based whitelist mirror propagates type-system-wide automatically; (5) Cascade picker UX reuse (Phase 100 fetchProviderModels API); (6) Pre-commit guard G8 explicit SHA pattern (use Phase 102 boundary SHA `f25ace5a` not `HEAD~12` to survive subsequent commits); (7) Vue template inline TS cast limitation (extract handler to `<script setup>` method, use `$event.target.value` not `(e.target as HTMLSelectElement).value`); (8) DI parameters are YAGNI when no test uses them (revert via `6c596b43`); (9) Spec commit message can drift from spec body (Task 10 message claimed "+ handoff" but only I094 + CLAUDE.md in scope — split into Task 10+11 commits); (10) Task 8 followup refactor pattern (`53e0d8a8`: single update on provider swap + inline model-name change + 7th test + objectContaining matcher). **Cluster cumulative**: Phase 90-103 = 14 phases / 1 NEW package (lingwen-illustrations) + 5 carryover closures (Phase 91-95) + 7 REQ-002 v2 sub-projects delivered (Phase 96-102, REQ-002 v2 FULLY CLOSED post-Phase 102) + **1 Phase 102+ extension** (Phase 103). **Phase 103 is the first Phase 102+ extension** — proving the architecture supports small additive feature extensions without breaking existing invariants. **Future work**: REQ-004 团队协作 REMOVED from BACKLOG 2026-09-20 (single-user writing assistant positioning) + notify_threshold per event_type (small extension) + telemetry-driven chain reorder (gated on Phase 102 failure tracker data) + ARCHDEBT-REAL continuation if requested (Phase 88 PHYSICALLY COMPLETE — all infra/ top-level subdirs closed). 详见 `docs/superpowers/handoffs/2026-09-20-phase-103-per-chapter-default-models-handoff.md` + spec `2026-09-20-phase-103-per-chapter-default-models-design.md` + plan `2026-09-20-phase-103-per-chapter-default-models.md` || 2026-09-20 | 协调者 | **v60.0 Phase 102 (REQ-002 v2: Settings Persistence Extension — seventh and final REQ-002 v2 sub-project delivered; REQ-002 v2 FULLY CLOSED post-Phase 102)**: master direct commits (per 2026-09-15 simplified workflow): 3 new ProjectSettings fields (fallback_models: dict[str, str] with Pydantic validator cross-referencing provider KNOWN_MODELS / chapter_overrides: dict[int, dict] with whitelist subset validator max_assets/confirm_before_generate/auto_generate/fallback_chain / notify_threshold: int=3 with >=1 validator) + pipeline.merge_chapter_settings helper (immutable, chapter_num=None fast path, override-wins-on-conflict) + pipeline.resolve_model is_fallback kwarg 4-tier resolution (explicit > fallback_models > default_models > provider default) + notifications record_failure/record_success state machine + _consecutive_failures + _warning_emitted idempotent flag + severity="warning" ULID-shared with audit_log + pipeline.generate_illustration + regenerate_illustration wired with all 3 helpers + api/illustrations.ts ProjectSettings interface (Pick-based whitelist for chapter_overrides) + useProjectSettings store 3 fields + ProjectSettingsIllustration.vue 3 sections (fallback_models keyed table + chapter_overrides editable table with sentinel drop + notify_threshold slider) + 11 new frontend tests (5 component + 6 store) + 12 regression guards G1-G12 + I094 + I095 NEW invariants. **Validation**: pytest lingwen-illustrations + studio_api settings all GREEN + 12/12 phase102 guards PASS + frontend vitest useProjectSettings + ProjectSettingsIllustration GREEN + pnpm tsc 0 new errors (48 pre-existing baseline unchanged) + ruff clean on introduced. **7 lessons**: (1) `_warning_emitted` flag needed for threshold idempotency (counter stays elevated but only emits warning once per streak); (2) `ulid.ULID()` not `ulid.new().str` (matches existing new_event_id helper); (3) `audit_log` module-level import required for monkeypatch compat; (4) Frontend tsconfig uses `Record<number, T>` (JSON serialization prevents fractional keys); (5) TypeScript chapter_overrides requires `Pick` not `Omit` (over-allow would trigger HTTP 422); (6) Sibling `describe` blocks don't share `beforeEach`; (7) Store fetch leaves new fields undefined on old-shape response → consumer applies defaults via `??`. **Cluster cumulative**: Phase 90-102 = 13 phases / 1 NEW package (lingwen-illustrations) + 5 carryover closures + **7 REQ-002 v2 sub-projects delivered (image provider adapters + reference image i2i + ProjectSettings extension + LRU archive + notification center + multi-model per provider + atomic provider fallback + settings persistence extension)**. **REQ-002 v2 FULLY CLOSED** post-Phase 102. **Future work**: REQ-004 团队协作 (P4 separate brainstorming) + telemetry-driven chain reorder (gated on Phase 102 failure tracker data accumulation) + per-chapter default_models overrides (if requested). 详见 `docs/superpowers/handoffs/2026-09-20-phase-102-settings-persistence-extension-handoff.md` + spec `2026-09-20-phase-102-settings-persistence-extension-design.md` + plan `2026-09-20-phase-102-settings-persistence-extension.md` || 2026-09-20 | 协调者 | **v59.0 Phase 101 (REQ-002 v2: Atomic Provider Fallback — sixth REQ-002 v2 sub-project delivered)**: master direct commits (per 2026-09-15 simplified workflow): New `lingwen_illustrations.fallback` module (Attempt frozen dataclass + dispatch_with_fallback async coroutine + provider_factory testability param + _dedupe_preserve_order + _filter_known_providers helpers) `8f3ba54c` + circular import fixup (move resolve_model import inside function) `b017aff8` + 11 chain retry/exhaustion/i2i tests `3c7ec954` + ProviderExhaustedError exception class `3c92cd92` + pipeline.generate_illustration + regenerate_illustration text-only paths use dispatch_with_fallback (i2i paths bypass) `679798fa` `08d26e0d` + ProjectSettings.fallback_chain list[str] field with Pydantic v2 back-compat `c3d481d4` + GenerateRequest.fallback_chain optional override + RegenerateRequest fallback_chain query param (comma-separated) + _resolve_provider_for_request returns (provider, fallback_chain) tuple `363a0b13` + STAGE_HTTP_CODES[ProviderExhaustedError]=502 + _err_detail.attempts in payload `6ad22f03` + api/illustrations.ts fallback_chain param `435b1011` + useProjectSettings store fallback_chain field `cbe7278f` + ProjectSettingsIllustration.vue multi-select picker `1d741c97` + 13 regression guards G1-G10 + I093 NEW invariant `a5cb6613`. **Validation**: pytest lingwen-illustrations 268/268 (was 250, +18 new: 11 fallback module + 4 pipeline integration + 3 docstrings) + studio_api settings 15/15 (was 12, +3 new fallback_chain tests) + studio_api illustrations 33/33 (was 28, +5 new fallback_chain + STAGE_HTTP_CODES tests) + 13 NEW phase101 guards GREEN + vitest useProjectSettings 12/12 (2 default-equality updated) + ProjectSettingsIllustration 3/3 (no regression) + pnpm tsc 0 new errors (48 pre-existing baseline unchanged) + ruff clean on introduced (auto-fix import sort). **7 lessons**: (1) provider_factory parameter preserves test patching (Phase 100 module-level get_provider reference broken test monkeypatching — fix: pipeline passes its own get_provider as factory); (2) Single-provider failure now raises ProviderExhaustedError (not bare GenerateError) — existing `except IllustrationError` catches continue to work but tests asserting `with pytest.raises(GenerateError)` needed updates; (3) body_fallback_chain should override settings even when body_provider is None (debugging/one-off override scenario); (4) i2i path bypass confirmed via G9 — provider-specific i2i models are not interchangeable; (5) Audit extra schema: `a.__dict__` produces 4-field dicts (provider/model/error/ts) — back-compat preserved if Attempt grows more fields; (6) Pydantic default-fill preserves yaml back-compat (Phase 98/100 pattern); (7) No worktree / no PR / 14 atomic commits directly on master (2026-09-15 simplified workflow holds for multi-week phases). **Cluster cumulative**: Phase 90-101 = 12 phases / 1 NEW package (lingwen-illustrations) + 5 carryover closures + 6 REQ-002 v2 sub-projects delivered (image provider adapters + reference image i2i + ProjectSettings extension + LRU archive + notification center + multi-model per provider + **atomic provider fallback**). **REQ-002 v2 remaining**: 1 of 7 (v2 settings persistence extension — addresses deferred Phase 95 item) + REQ-004 团队协作 (P4 separate brainstorming). 详见 `docs/superpowers/handoffs/2026-09-20-phase-101-atomic-provider-fallback-handoff.md` + spec `2026-09-18-phase-101-atomic-provider-fallback-design.md` + plan `2026-09-18-phase-101-atomic-provider-fallback.md` |
| 2026-09-18 | 协调者 | **v58.0 Phase 100 (REQ-002 v2: Multi-Model Per Provider — fifth REQ-002 v2 sub-project delivered)**: master direct commits (per 2026-09-15 simplified workflow): UnknownModelError + KNOWN_MODELS + DEFAULT_MODEL constants in 3 providers (minimax/openai/stability) + ProviderAdapter.models + .default_model fields + pipeline.resolve_model() 3-tier resolution + _load_illustration_settings extraction + _MODEL_FOR_PROVIDER removal + POST/PUT model field + GET /providers/{name}/models endpoint + UnknownModelError → 422 + ProjectSettingsIllustration 3 per-provider dropdowns + GenerateIllustrationDialog model picker (i2i-disabled) + 9 regression guards G1-G9 + I092 NEW invariant. **Validation**: pytest lingwen-illustrations 218→248 (+30) + studio_api 149→157 (+8) + 9 NEW phase100 guards GREEN + vitest pre-existing failures verified unrelated (4/4 confirmed: human-first-nav, architecture-guards, GenerateIllustrationDialog pre-existing) + pnpm tsc 0 new errors (48 pre-existing baseline unchanged) + ruff clean on introduced files (7 pre-existing in other test files); knip 8 unused exports (3 pre-existing reference image + 5 new from T7 wrappers — acceptable, available for future use). **11 atomic commits list**: `4a400f4a` T1 spec / `572555c5` T2 plan / `674c0388` T3 UnknownModelError / `faf57ad8` T4 MiniMax provider / `636cfdd2` T4 OpenAI / `dbebd181` T4 Stability / `515ef56a` T4a ruff autofix / `a6fd363e` T5 ProviderAdapter extension / `2832dc45` T5a defensive guard / `b62e27d1` T6 pipeline.resolve_model / `85bb8647` T6 followup / `94e3990d` T7 routes model field + GET providers / `6fb1f622` T7 followup schema / `6c51c673` T8 api/illustrations.ts wrappers / `cd7245d2` T8 fixup trailing newline / `c27c7d4d` T9 ProjectSettingsIllustration dropdowns / `8d5057c7` T10 GenerateIllustrationDialog model picker / `29e70270` T10 dead code removal / `ff5cbed7` T11 9 regression guards + I092. **5 lessons**: (1) Resolution layering fragile to ordering bugs (test all 3 input combinations); (2) ProviderAdapter dataclass extension preserves monkeypatch compat via Phase 97 importlib pattern; (3) Settings YAML loader belongs in one helper (Phase 98 inline reader → Phase 100 _load_illustration_settings); (4) i2i model parameter asymmetry is v1 limitation (UI disable + docstring); (5) Full vs curated catalog trade-off (full ships with 11 models; telemetry may prune). **2 dev adaptations from plan**: regenerate uses query param (not body) matching Phase 96 provider convention; test files co-located with components (not in __tests__/) matching existing folder convention. **Cluster cumulative**: Phase 90-100 = 11 phases / 1 NEW package + 5 carryover closures + 5 REQ-002 v2 sub-projects delivered (image provider adapters + reference image i2i + ProjectSettings extension + LRU archive + notification center + **multi-model per provider**). **REQ-002 v2 remaining**: 2 of 7 (atomic provider fallback + v2 settings persistence extension). 详见 `docs/superpowers/handoffs/2026-09-18-phase-100-multi-model-handoff.md` + spec `2026-09-18-phase-100-multi-model-design.md` + plan `2026-09-18-phase-100-multi-model.md` |
| 2026-09-18 | 协调者 | **v57.0 Phase 99 (REQ-002 v2: Notification Center — fourth REQ-002 v2 sub-project delivered)**: master direct commits (per 2026-09-15 simplified workflow): notifications.py module (I091 fan-out + 9 unit tests) + audit_log.read_history pagination + id kwarg (5 compat tests) + pipeline double-write generate/regenerate (4 integration tests) + cleanup_route double-write (1 event per deleted asset + dry_run synthetic) + routes/notifications.py SSE + REST history (6 route tests) + useNotificationStream (3 tests) + useNotificationStore Pinia (3 tests) + api/notifications.ts typed wrappers + NotificationBell + Dropdown + ListItem (8 tests) + NotificationsPage + tabs (2 tests) + App.vue header mount + /notifications route + sidebar nav + IconSidebarNotifications.vue + 15 regression guards G1-G12 + G13 e2e + I091 NEW invariant. **Validation**: pytest lingwen-illustrations + studio_api all GREEN (preserved baselines + new tests) + phase99 guards 15/15 PASS + frontend vitest 17 notification-center tests GREEN + pnpm tsc 0 new errors + ruff clean on introduced. **7 lessons**: (1) ULID > UUID for SSE + cursor; (2) Double-write is dual invariant I090+I091 — same id MUST flow to both; (3) In-memory SSE + JSONL durability known trade-off; (4) localStorage read-state opt-in; (5) Sidebar icons + nav must ship together; (6) SSE handler needs `asyncio.wait_for` + heartbeat for cancellation; (7) python-ulid 3.x API differs from 2.x (ulid.new() → ulid.ULID()). **Cluster cumulative**: Phase 90-99 = 10 phases / 1 NEW package + 5 carryover closures + 4 REQ-002 v2 sub-projects delivered. **REQ-002 v2 remaining**: multi-model per provider / atomic provider fallback / v2 settings persistence extension (3 of 7). 详见 `docs/superpowers/handoffs/2026-09-18-phase-99-notification-center-handoff.md` + spec `2026-09-18-phase-99-notification-center-design.md` + plan `2026-09-18-phase-99-notification-center.md` |
| 2026-09-18 | 协调者 | **v56.1 Phase 97 REQ-002 v2: Reference image i2i (second REQ-002 v2 sub-project delivered)**: master direct commits (per 2026-09-15 simplified workflow): 14 atomic commits = spec `38d0ffd3` + plan `7cda306f` (2 docs) + reference_image.py storage (save/load/delete/info + MAX_REFERENCE_BYTES=10MB + JPG/PNG only) `7c6c0dc9` (Task 2 — 14 new tests) + 3 providers (minimax i2i endpoint `image_base64` field + openai hard error `GenerateError(retryable=False)` + stability multipart with `image=` file + `strength=` param + `_DEFAULT_STRENGTH=0.35`) `22c954f4` (Task 3 — 6 new tests) + ProviderAdapter dataclass (frozen dataclass replacing function-based registry; 4 fields: name/generate/generate_with_reference/supports_i2i; dynamic `importlib.import_module` lookup preserves Phase 96 monkeypatch compat) `4537515e` (Task 4 — 6 new tests) + pipeline.reference_image_bytes dispatch + IllustrationMetadata.used_reference_image field `80f227e2` (Task 5 — 4 new tests) + routes/reference_image.py 3 endpoints (POST/GET/DELETE) + python-multipart dep + registration in `routes/__init__.py` facade (not app.py per established convention) `7b5b539f` (Task 6 — 9 new tests) + illustrations route multipart file + use_project_reference bool + 422 i2i unsupported (Request-based parsing, NOT FastAPI UploadFile, due to Body+File mix limitation documented in inline comment) `d2dfa9cb` (Task 7 — 3 new tests) + ReferenceImageUpload.vue component with Blob→URL.createObjectURL wrapping + URL.revokeObjectURL on 3 lifecycle paths (re-load/delete/unmount) `90602079` (Task 8 — 6 new tests) + useProjectSettings 4 methods (fetch/upload/delete/blob) `29105c78` (Task 9 — 5 new tests) + ProjectSettingsIllustration embeds ReferenceImageUpload `ddae40da` (Task 10 — 3 new tests + Blob/URL reconciliation) + useIllustrationStore multipart dispatch (File instance triggers FormData; else JSON; `_ignored` destructuring strips File from JSON path) `28a3ed84` (Task 11 — 3 new tests) + GenerateIllustrationDialog use_project_reference toggle + per-call file input + openai auto-uncheck watch `45b68091` (Task 12 — 5 new tests) + api/illustrations.ts typed wrappers (4 functions + 2 interfaces) `9ed8dcaa` (Task 13) + 16 regression guards G1-G12 `36054d51` (Task 14 — G10 updated to match Request-based parsing pattern). **Validation**: pytest lingwen-illustrations 178/178 (was 162, +16 new tests; +5 new from provider adapter test changes) + studio_api 125/125 (was 113, +12 new); vitest apps/dashboard 2055/2055 (was 2036, +19 new tests across 5 spec files); pnpm tsc 0 new errors (48 pre-existing baseline unchanged); ruff clean on introduced. **3 lessons**: (1) ProviderAdapter dataclass via dynamic `importlib.import_module` lookup preserves Phase 96 monkeypatch contract — `monkeypatch.setattr("lingwen_illustrations.providers.minimax.generate", fake)` still works because adapter reads `module.generate` at call time; (2) FastAPI `Body(...) + File(...)` mix is invalid → Request-based multipart parsing with `await request.form()` is the workaround (Task 7 subagent discovered this and documented inline); (3) Blob→URL.createObjectURL wrapping belongs at the `<img :src>` consumer (component), not the store — store returns raw Blob per test contract, component wraps for DOM with `URL.revokeObjectURL` on 3 lifecycle paths. **REQ-002 v2 second sub-project delivered**. Remaining v2 sub-projects: LRU archive + notification center + multi-model per provider + atomic provider fallback + ProjectSettings extension (auto_generate/max_assets/confirm_before_generate). 详见 `docs/superpowers/handoffs/2026-09-18-phase-97-reference-image-i2i-handoff.md` + spec `2026-09-18-phase-97-reference-image-i2i-design.md` + plan `2026-09-18-phase-97-reference-image-i2i.md` ||
| 2026-09-17 | 协调者 | **v56.0 Phase 96 image provider adapters (REQ-002 v2 multi-provider — first REQ-002 v2 sub-project delivered)**：master direct commits (per 2026-09-15 simplified workflow): spec `ebd30455` + plan `86417a33` + 3 provider adapters (`44d8aeeb` minimax + `5c3c6e3c` openai DALL-E 3 + `ad7a5bb1` stability SD3 + `bf290ca4` multipart fix + `8b083f2d` `_b64_decode` helper) + providers/__init__.py registry `b06355dd` (KNOWN_PROVIDERS + get_provider dispatch) + image_generator.py → thin wrapper `8c2baa80` + GenerateError + provider kwarg `608f7c37` + test (`0e961de7` drop unused exc binding + `2032361d` retarget Phase 90 G11a/b to providers.minimax) + IllustrationMetadata + provider field `70545e60` (backwards compat) + pipeline dispatches via get_provider + provider param `f6947dbb` + APIConfig + openai_api_host + stability_api_key/host `e53adc66` + _project_helpers refactor `fb1b9ffb` + **PUT/GET `/api/projects/{slug}/settings` endpoint `af3b21a8`** (闭环 Phase 95 deferred item: v2 persistence for project settings) + illustrations route + provider field + `_api_credentials_for` `c4aefe70` + background task reads default_provider `11f639e9` + useProjectSettings Pinia store + tests `2010610c` + ProjectSettingsIllustration + default_provider dropdown `b06ad249` + GenerateIllustrationDialog + provider picker (per-call override) `3027ab0f` + 14 NEW regression guards `676ca5eb` + ruff auto-fix `4fa777cb` + CLAUDE.md v55.5 → v56.0 + I088 NEW invariant `509d8002`. **Validation**: pytest lingwen-illustrations (storage + pipeline + providers) + studio_api (illustrations_api + projects settings) all GREEN + phase90 guards GREEN (G11a/b retargeted to providers.minimax) + 14/14 phase96 NEW guards GREEN + vitest useProjectSettings + ProjectSettingsIllustration + GenerateIllustrationDialog GREEN + pnpm tsc 0 new errors; ruff clean on introduced. **3 lessons**: (1) Provider registry via KNOWN_PROVIDERS + get_provider dispatch is the simplest extensibility (vs ABC polymorphism); (2) Per-project settings persistence via project root yaml (PUT/GET) keeps state scoped without DB schema changes — natural fit for multi-project studio; (3) GenerateError + provider attribution surfaces failures with clear ownership (debug + log + retry routing). **Phase 95 deferred item CLOSED**: v2 persistence for project settings → PUT/GET `/api/projects/{slug}/settings` yaml endpoint + ProjectSettingsIllustration + useProjectSettings store + background task reads default_provider. **REQ-002 v2 first sub-project delivered**: image provider adapters (3 providers) — remaining: reference image i2i / LRU archive / notification center. **Future work**: REQ-002 v2 sub-projects (i2i / LRU / notifications) + REQ-004 团队协作 (P4 separate brainstorming). 详见 `docs/superpowers/handoffs/2026-09-17-phase-96-image-provider-adapters-handoff.md` + spec `2026-09-17-phase-96-image-provider-adapters-design.md` + plan `2026-09-17-phase-96-image-provider-adapters.md` ||
| 2026-09-14 | 协调者 | **v54.11 Phase 79 P3-ARCHDEBT infra/story_contracts → packages/lingwen-story-contracts 真迁移 闭环 (ARCHDEBT-REAL cycle 第一例)**：8 atomic commits on `phase-79-p3-archdebt-story-contracts` = C0 spec `03031031` / C1 scaffold `cd123d2b` / C2a 2 production function-body imports + 4 docstring narratives `0836f500` / C2b 5 test files MIGRATE rename (97-99% blame preserved per I079 §A5) `e2427480` / C3 FULL DELETE infra/story_contracts/ 7 files (-848 LOC) + I080 NEW `097b2773` / C4 prior-phase guards fixup (test_phase53d drop story_contracts from remaining_subdirs + assert not exists + __pycache__ residue lesson per N.14 v22) `af2e6041` / C5 12 regression guards G1-G12 `a331082f` / C6 doc sync v54.11。闭环 ARCHDEBT-CANDIDATES.md Top 5 候选 #1 (`infra/story_contracts/`, 848 LOC, 7 source files, 8 consumers)。10 public symbols (StoryContractEngine + StoryContractPaths + GenreRouter + RouteResult + AntiPattern + AntiPatternAggregator + ContractPayload + ContractPersister + StoryContractInjector + inject_story_contract)。LEAF package (0 workspace deps, only stdlib: dataclasses/datetime/pathlib/json/re/typing)。7 production consumer sites updated (2 function-body imports + 4 docstring narratives + 1 hook config in injector.py:111)。5 test files MIGRATE single C2b commit rename blame preserved。`pyproject.toml` workspace member declaration + `dependencies = []` (LEAF)。`.lingwen/architecture.yml` + 2 prior-phase guards (test_phase53d + test_phase18_8) all updated。**30/30 functional pytest gate GREEN** + **12/12 NEW regression guards GREEN** + 13/13 prior-phase preserved (Phase 53d 7 + Phase 18_8 5 + Phase 77 not rerun locally)。**3 lessons**: (1) `__pycache__/` 残留 lesson (N.14 v23) — `git rm -r` 不清 gitignored bytecode, test_phase53d G5 fail because `(infra_dir / "story_contracts").exists()` 返回 True from `__pycache__/` residue; (2) Regex anchored checks beat raw substring — G11 第一版 false-positive on `assert not (infra_dir / "story_contracts").exists()` 是合法的 positive-deletion guard, 修复: strip docstring + assert-not-exists block 后再 regex search remaining_subdirs context; (3) Worktree `.venv/bin/python` — `__editable__` finder pattern 只指向 main repo `packages/*/src`, worktree 第一次 import 仍 ModuleNotFoundError; 修复: `uv pip install -e packages/lingwen-story-contracts/` (from worktree) + pytest 用 `.venv/bin/python`。**Cluster cumulative**: Phase 53 + 53b + 53c + 53d + 53e + 78 + 79 = 7 phases / ~17870 LOC dead code + 8 zero-consumer dirs + 1 真迁移 package。**Next candidates**: ARCHDEBT-REAL cycle 第 2 例 — `infra/subplot/` (508 LOC, 10 consumers) 或 `infra/story_contracts/` 后续依赖 packages (Phase 80+)；OR non-ARCHDEBT 工作 (Write Workspace / World / Reading Power 增强)。详见 `docs/superpowers/handoffs/2026-09-14-phase-79-p3-archdebt-story-contracts-handoff.md` + spec `2026-09-14-phase-79-p3-archdebt-story-contracts-design.md` ||
| 2026-09-16 | 协调者 | **v55.2 Phase 92 P2-EXTRACT-ENUM closure (Phase 90 carryover deviation 1)**：master direct commits (per 2026-09-15 simplified workflow): spec + plan + feat (TaskType enum + STRUCTURED_EXTRACTION = "structured_extraction" member) + test (lingwen-shared test_llm_dto round-trip + G10a enum exists) + refactor (TASK_CONFIGS entry + G10c + prompt_builder switched from QUALITY_ANALYSIS to STRUCTURED_EXTRACTION + G10b) + test (G10a/b/c full triple) + docs (BACKLOG row + CLAUDE.md v55.1 → v55.2 + CURRENT_STATUS new row + handoff). `lingwen-shared.contracts.python.llm.TaskType` 6 → 7 members (新增 STRUCTURED_EXTRACTION). `LLMService.TASK_CONFIGS` 5 → 6 entries (1500 max_tokens + 0.3 temperature — deterministic JSON). `lingwen-illustrations.prompt_builder.extract_scene` switch from `QUALITY_ANALYSIS` semantic stand-in → dedicated `STRUCTURED_EXTRACTION` (no comment "v2 follow-up"). `uv.lock` auto-sync httpx dep for lingwen-illustrations (was already in pyproject.toml:10, just never locked). **Validation**: pytest lingwen-shared 7/7 + lingwen-illustrations prompt_builder 9/9 + lingwen-llm-service 3/3 + phase90 guards 20/20 (incl. G10a enum exists + G10b prompt_builder uses STRUCTURED_EXTRACTION + G10c TASK_CONFIGS includes STRUCTURED_EXTRACTION) + studio_api 90/90 unchanged + ruff clean (5 errors fixed: 4 by `--fix`, 1 pre-existing W292 in service.py + 1 pre-existing E741 in test_phase90:102 not introduced). **3 lessons**: (1) Pre-existing ruff errors should NOT be touched — only fix what you introduce (W292 service.py:282 + E741 test_phase90:102 pre-existed Phase 92); (2) `uv.lock` auto-syncs when running pytest (uv-managed .venv editable install); (3) Multi-package pytest combined run fails on rootdir conflict — must use separate `--rootdir` per package OR run from each package dir (Phase 89 lesson, 2nd recurrence). **Carryover closed**: P2-EXTRACT-ENUM (was 4 v2 carryovers, now **3 remaining**: image_generator b64_json real-API decoding + regenerate non-atomic PUT atomic + ProjectSettingsPage doesn't exist). **Next candidates**: REQ-002 v2 sub-projects (image provider adapters / i2i / LRU archive / notification center) OR REQ-004 团队协作 (P4 brainstorm). 详见 `docs/superpowers/handoffs/2026-09-16-phase-92-extract-enum-handoff.md` + spec `2026-09-16-phase-92-extract-enum-design.md` + plan `2026-09-16-phase-92-extract-enum.md` ||
| 2026-09-16 | 协调者 | **v55.3 Phase 93 image_generator b64_json real decode closure (Phase 90 carryover deviation 3)**：master direct commits (per 2026-09-15 simplified workflow): spec + plan + feat (`image_generator.py` +9 LOC: parse JSON envelope + base64-decode `data[0].b64_json` instead of `return resp.content` raw; new imports `base64`/`binascii`/`json`; docstring updated explaining v1 raw-return was broken for real API) + test (1 updated `test_generate_returns_decoded_jpeg_bytes` uses realistic API mock + 6 NEW tests: malformed JSON / missing data array / empty data array / missing b64_json field / invalid base64 / multi-data-item first-pick) + test (G11 a/b regression guards: `b64_json` decode present + `response_format=b64_json` requested; helper `_has_raw_resp_content_return` strips docstrings per N.14 v21) + docs. **Validation**: pytest lingwen-illustrations **78/78** (was 72, +6 decode tests + 1 updated realistic-mock test) + test_pipeline 4/4 (downstream consumer unchanged) + phase90 guards **22/22** (was 20, +G11 a/b) + studio_api 90/90 unchanged; ruff clean on introduced (1 auto-fixed I001 import order; 1 pre-existing E741 in test_phase90:102 untouched). **Key insight**: v1 `return resp.content` was broken-but-passing-test (mock test injected raw JPEG bytes in `resp.content`, never reflecting real API JSON envelope). Pipeline silently saved JSON string as `.jpg` — never production-tested against real MiniMax API. Phase 93 fixed the broken behavior AND closes the v1 deviation. **4 lessons**: (1) Mock tests that match broken behavior can mask implementation bugs — verify mocks reflect actual API response shape, not convenient test data; (2) `base64.b64decode(..., validate=True)` + isinstance isinstance isinstance pattern is the safe decode triad (rejects malformed bytes / wrong types at each level); (3) `binascii.Error` is alias of `ValueError` in stdlib — catch both; (4) G11a defensive helper `_has_raw_resp_content_return` is the N.14 v21 pattern (strip docstrings before regex search) applied to a new failure surface (not pre-existing). **Carryover closed**: image_generator b64_json (was 3 v2 carryovers, now **2 remaining**: regenerate non-atomic PUT atomic + ProjectSettingsPage doesn't exist). **Next candidates**: regenerate non-atomic (Phase 94 candidate) OR ProjectSettingsPage (Phase 95) OR REQ-002 v2 sub-projects. 详见 `docs/superpowers/handoffs/2026-09-16-phase-93-b64-json-real-decode-handoff.md` + spec `2026-09-16-phase-93-b64-json-real-decode-design.md` + plan `2026-09-16-phase-93-b64-json-real-decode.md` ||
| 2026-09-16 | 协调者 | **v55.4 Phase 94 regenerate atomic via PUT closure (Phase 90 carryover deviation 4)**：master direct commits (per 2026-09-15 simplified workflow): spec + plan + feat backend (storage.replace_asset +57 LOC: temp-file + POSIX `rename(2)` atomic swap; pipeline.regenerate_illustration +76 LOC: re-runs stages 1-4 + preserves asset_id; PUT endpoint +52 LOC; 9 NEW tests: 5 storage (preserve-id, no-temp-leftover, fallback-to-save, invalidates-listing, preserves-on-failure) + 4 endpoint (preserves-id, 404-when-missing, extract-error-preserves-original, load-error-preserves-original)) + test (G12 a/b/c/d regression guards: PUT endpoint registered regex + storage.replace_asset signature + pipeline.regenerate_illustration signature + frontend uses PUT NOT DELETE+POST with negative check `deleteAsset(slug` not in body) + feat frontend (useIllustrationStore.regenerate switched from DELETE+POST to single PUT; loading + error state management added; replaces in-place via findIndex+splice) + docs. **Validation**: pytest lingwen-illustrations storage **16/16** (was 11, +5) + lingwen-illustrations **83/83** (was 78, +5) + studio_api illustrations_api **12/12** (was 8, +4) + studio_api **94/94** (was 90, +4) + phase90 guards **26/26** (was 22, +G12 a/b/c/d) + vitest illustrations 17/17 + stores 9/9 unchanged + pnpm tsc 0 new errors (pre-existing FactionGraphCanvas.spec.ts errors untouched); ruff clean on introduced (1 pre-existing E741 in test_phase90:102 untouched). **Key insight**: v1 frontend had 16-line regenerate() with comment "intentionally non-atomic" + "v2: backend could add PUT" — the comment WAS the bug ticket. Phase 94 closes the explicit v1 workaround. **4 lessons**: (1) Frontend comments describing workarounds ("v2 should add X") are work items in scope for next iteration; (2) POSIX `rename(2)` atomicity on same filesystem is the cheapest correctness for file swaps (no DB, no lockfile); (3) Nondestructive error path requires Stage-level exception mapping (any stage failure → HTTP error → original preserved); (4) G12d negative check (`deleteAsset(slug` NOT in body) is essential — without it, a future dev could "add PUT" alongside old DELETE call and re-introduce the v1 bug pattern. **Carryover closed**: regenerate non-atomic (was 2 v2 carryovers, now **1 remaining**: ProjectSettingsPage doesn't exist). **Next candidates**: Phase 95 ProjectSettingsPage (~20 min, last carryover) OR REQ-002 v2 sub-projects (image provider adapters recommended). 详见 `docs/superpowers/handoffs/2026-09-16-phase-94-regenerate-atomic-handoff.md` + spec `2026-09-16-phase-94-regenerate-atomic-design.md` + plan `2026-09-16-phase-94-regenerate-atomic.md` ||
| 2026-09-16 | 协调者 | **v55.5 Phase 95 ProjectSettingsPage substitution closure — 闭环 Phase 90 carryover deviation 5 + 闭环 carryover CHAIN**:master direct commits (per 2026-09-15 simplified workflow): spec + plan + test (G13 a/b/c regression guards: G13a `test_project_settings_illustration_mounted_in_settings_page` — component imported AND rendered in SettingsPage.vue template; G13b `test_no_dedicated_project_settings_page_route` — `pages/ProjectSettingsPage.vue` must NOT exist (negative guard enforces substitution); G13c `test_project_settings_illustration_has_its_own_spec` — dedicated spec preserved ≥4 tests) + docs. NO CODE CHANGE (pure documentation + regression guard). **Validation**: pytest phase90 guards **29/29** (was 26, +G13 a/b/c) + vitest ProjectSettingsIllustration 4/4 unchanged + pnpm tsc 0 new errors; ruff clean on introduced (1 pre-existing E741 in test_phase90:102 untouched). **Key decision**: substitution closure NOT full closure — Phase 90 spec mentioned dedicated `pages/ProjectSettingsPage.vue` route, but actual implementation nested `ProjectSettingsIllustration.vue` component inside global `pages/SettingsPage.vue` (illustration-prefs-panel section). Phase 95 validates the substitution (works, tested, discoverable, no per-project URL overhead) rather than re-creating the dedicated route. **3 lessons**: (1) Deviations ≠ bugs; substitutions ≠ failures — when a carryover represents a design choice rather than a missing feature, "validate the choice" closes the carryover; (2) Phase 41+ lesson 2 (history archive commit-blame protection) applies to design specs, not just docs — historical design specs should NOT be retroactively edited to mark substitutions; (3) G13b is a negative guard for design intent — most regression guards assert "X exists" (positive), G13b asserts "X does NOT exist" (negative) to prevent accidental re-introduction. **Phase 90 carryover CHAIN FULLY CLOSED**: 5 deviations all closed across 6 phases — P2-EXTRACT-ENUM (Phase 92) + P2-ILLUSTRATIONS-BIBLE-CANONICAL (Phase 91) + image_generator b64_json (Phase 93) + regenerate non-atomic (Phase 94) + ProjectSettingsPage substitution (Phase 95). **Future work**: REQ-002 v2 sub-projects (~~image provider adapters~~ ✅ **Phase 96 DONE (2026-09-17)** / reference image i2i / LRU archive / notification center) + REQ-004 团队协作 (P4 separate brainstorming) + ~~v2 persistence for project settings~~ ✅ **Phase 96 CLOSED (2026-09-17)** — see handoff (PUT/GET `/api/projects/{slug}/settings` yaml endpoint + ProjectSettingsIllustration + useProjectSettings store + background task reads default_provider; deferred Phase 95 → closed by Phase 96 Task 12). 详见 `docs/superpowers/handoffs/2026-09-16-phase-95-project-settings-substitution-handoff.md` + spec `2026-09-16-phase-95-project-settings-substitution-design.md` + plan `2026-09-16-phase-95-project-settings-substitution.md` || 2026-09-14 | 协调者 | **v54.12 Phase 80 P3-ARCHDEBT infra/subplot → packages/lingwen-subplot 真迁移 闭环 (ARCHDEBT-REAL cycle 第二例, NOT-LEAF)**：8 atomic commits on `phase-80-p3-archdebt-subplot` = C0 spec `3742e794` / C1 scaffold `9dc59da0` (NOT-LEAF 1 dep lingwen-core) / C2a 1 production function-body import + 2 intra-subplot absolute imports + 4 docstring narratives `9ad1f030` / C2b MIGRATE 4 test files rename (98-99% blame preserved per I079 §A5) + IN-PLACE rewrite 3 cross-pkg test imports `a898f2a0` / C3 FULL DELETE infra/subplot/ 4 files (-508 LOC) + I081 NEW `f6450482` / C4 prior-phase guards fixup (test_phase53d drop subplot from remaining_subdirs + assert not exists + test_phase18_8 docstring + test_phase35 early-return safe verification) `d88c6bf7` / C5 12 regression guards G1-G12 `082392c7` / C6 doc sync v54.12。闭环 ARCHDEBT-CANDIDATES.md Top 5 候选 #2 (`infra/subplot/`, 508 LOC, 4 source files, 10 consumers)。15 public symbols (Plot + PlotType + PlotPurpose + PlotStatus + MAX_ACTIVE_SUBPLOTS + PlotRegistry + 4 exceptions + 4 lifecycle consts + 3 query funcs)。NOT-LEAF (1 workspace dep: lingwen-core for `lingwen_core.domain.subplot` types)。1 production consumer updated (lingwen-world-model/links.py:41 function-body import); 2 intra-subplot absolute imports normalized (queries.py:18 + registry.py:32 — pre-existing code smell rewritten from `from infra.subplot.X` to `from lingwen_subplot.X`)。4 test files MIGRATE rename single C2b commit; 3 in-place test imports (test_links + test_phase2_integration in lingwen-world-model + test_phase35 in tests/ — early-return safe per Phase 78 lesson)。**94/94 subplot functional pytest gate GREEN** + **25/25 lingwen-world-model tests GREEN** + **17/17 test_phase35 GREEN** + **12/12 NEW regression guards GREEN** + **30/30 prior-phase preserved** (Phase 53d 7 + Phase 18_8 5 + Phase 35 17 + 1 bonus)。**2 lessons**: (1) `__all__` 列表 vs 实际 import 不一致 (pre-existing bug in infra/subplot/__init__.py — `VALID_TRANSITIONS` etc. listed but not imported — symbols 实际可从 `lingwen_subplot.lifecycle` 直接 module-level import); (2) Intra-package absolute imports 是 future cleanup opportunity (relative imports `.X` 更 idiomatic)。**Cluster cumulative**: Phase 53 + 53b + 53c + 53d + 53e + 78 + 79 + 80 = 8 phases / ~18378 LOC dead code + 8 zero-consumer dirs + 2 真迁移 packages (LEAF + NOT-LEAF)。**Next candidates**: ARCHDEBT-REAL cycle 第 3 例 — `infra/di/` (309 LOC, TRUE LEAF 类似 Phase 37 paths) 或 `infra/util/` (338 LOC); OR non-ARCHDEBT 工作。详见 `docs/superpowers/handoffs/2026-09-14-phase-80-p3-archdebt-subplot-handoff.md` + spec `2026-09-14-phase-80-p3-archdebt-subplot-design.md` || 2026-09-14 | 协调者 | **v54.13 Phase 81 P3-ARCHDEBT infra/di → packages/lingwen-di 真迁移 闭环 (ARCHDEBT-REAL cycle 第三例, TRUE LEAF)**：7 atomic commits on `phase-81-p3-archdebt-di` = C0 spec `192de05d` / C1 scaffold `08da624d` (TRUE LEAF 0 deps) / C2 MIGRATE test_infra_modules.py rename (99% blame preserved per I079 §A5) + IN-PLACE docstring fixup `ef5df84e` / C3 FULL DELETE infra/di/ 2 files (-309 LOC) + I082 NEW `fc101cd8` / C4 prior-phase guards fixup (test_phase53d drop di from remaining_subdirs + assert not exists + test_phase18_8 docstring + test_infra_init_no_stale_di still passes) `01a598dc` / C5 12 regression guards G1-G12 `a702888e` / C6 doc sync v54.13。闭环 ARCHDEBT-CANDIDATES.md Top 5 候选 #3 (`infra/di/`, 309 LOC, 2 source files, 3 consumer sites)。8 public symbols (Layer + Runtime + Tag + get_runtime + make + make_with_deps + provide + reset_runtime)。TRUE LEAF (0 workspace deps, only stdlib: logging + dataclasses + typing)。0 production consumer — only test consumers (test_infra_modules.py had 2 function-body imports `from infra.di.layer import`)。1 test file MIGRATE rename single C2 commit (99% blame preserved); 1 in-place docstring fixup in test_infra_init_no_deferred_re_exports.py:19 (FORBIDDEN_PATTERNS tuple retained as defense in depth)。**18/18 migrated tests pass** + **13/13 prior-phase preserved** (Phase 53d 7 + Phase 18_8 5 incl. test_infra_init_no_stale_di + 1 bonus) + **12/12 NEW regression guards GREEN**。**Note**: test_infra_modules.py 有 14 个 pre-existing ModuleNotFoundError 引用 non-migrated modules (`infra.permission` / `infra.llm_cache` / `infra.types` 从未作为 packages 存在) — inherited from master, NOT introduced by Phase 81。**1 lesson**: Pre-existing failures carry over — test migration 暴露 baseline 不 100% pass 时, G12 应 verify collection success 而非 100% pass。**Cluster cumulative**: Phase 53 + 53b + 53c + 53d + 53e + 78 + 79 + 80 + 81 = 9 phases / ~18687 LOC dead code + 8 zero-consumer dirs + **3 真迁移 packages (LEAF + NOT-LEAF + TRUE LEAF — 模式 verification across 3 package shapes)**。**Next candidates**: ARCHDEBT-REAL cycle 第 4 例 — `infra/util/` (338 LOC, TRUE LEAF retry helper) 或 `infra/config/` (77 LOC trivial); OR non-ARCHDEBT 工作。详见 `docs/superpowers/handoffs/2026-09-14-phase-81-p3-archdebt-di-handoff.md` + spec `2026-09-14-phase-81-p3-archdebt-di-design.md` || 2026-09-14 | 协调者 | **v54.14 Phase 82 P3-ARCHDEBT infra/util → packages/lingwen-util 真迁移 闭环 (ARCHDEBT-REAL cycle 第四例, NOT-LEAF, 简化版本)**：8 atomic commits on `phase-82-p3-archdebt-util` = C0 spec `ea5a90de` / C1 scaffold `5a2c76de` (NOT-LEAF 1 dep lingwen-errors) / C2 in-place migration `b6df145b` (no test files MIGRATE — 0 test consumers) / C3 FULL DELETE infra/util/ 2 files (-338 LOC) `aa223934` / C3.5 I083 NEW invariant `338327f2` / C4 prior-phase guards fixup + C3.5 fixup (intra-package absolute import rename) `41f73436` / C5 12 regression guards G1-G12 `333691b1` / C6 doc sync v54.14。闭环 ARCHDEBT-CANDIDATES.md Top 5 候选 #4 (`infra/util/`, 338 LOC, 2 source files, 2 consumer sites)。6 public symbols (RetryConfig + retry + retry_async + with_retry + is_transient_error + backoff_delay)。NOT-LEAF (1 workspace dep: lingwen-errors)。0 production cross-package consumer — only intra-infra compat re-export (infra/__init__.py:17); 0 test files to MIGRATE (no test consumers); 2 in-place consumer fixups (infra/__init__.py:17 re-export + tests/test_phase18_10_stale_imports.py:18 allowlist removal)。**12/12 NEW regression guards GREEN** (G1+G2+G4+G5+G7+G10+G11+G12 GREEN; G3+G6+G8 marked n/a for no-MIGRATE) + **14/14 prior-phase preserved** (Phase 53d 7 + Phase 18_8 5 + Phase 18_10 2)。**1 lesson**: Intra-package absolute imports break after C3 (Phase 80 v2 lesson) — `infra/util/__init__.py:14` copied with `from infra.util.retry` (pre-existing code smell); Phase 82 C3 fixup renamed to `from lingwen_util.retry`. **Future heuristic**: pre-C1 grep `from infra.<subdir_name>\.` in source files when copying. **Cluster cumulative**: Phase 53 + 53b + 53c + 53d + 53e + 78 + 79 + 80 + 81 + 82 = 10 phases / ~19025 LOC dead code + 8 zero-consumer dirs + **4 真迁移 packages (LEAF + NOT-LEAF + TRUE LEAF + NOT-LEAF retry — 4 package shapes verification)**。**Next candidates**: ARCHDEBT-REAL cycle 第 5 例 — `infra/config/` (77 LOC trivial) 或 `infra/tools/` (subplot-related, ~1750 LOC NOT-LEAF); OR non-ARCHDEBT 工作。详见 `docs/superpowers/handoffs/2026-09-14-phase-82-p3-archdebt-util-handoff.md` + spec `2026-09-14-phase-82-p3-archdebt-util-design.md` || 2026-09-14 | 协调者 | **v54.15 Phase 83 P3-ARCHDEBT infra/config → packages/lingwen-config 真迁移 闭环 (ARCHDEBT-CANDIDATES.md Top 5 闭环, 第五例 ARCHDEBT-REAL cycle, TRUE LEAF + pyyaml)**：7 atomic commits on `phase-83-p3-archdebt-config` = C0 spec `975527bc` / C1 scaffold `09746660` (TRUE LEAF + pyyaml, with pre-C1 fixups: intra-package absolute import + filesystem path) / C2 in-place migration `284de78e` (3 cross-pkg tools/ + 1 intra-infra re-export) / C3 FULL DELETE infra/config/ 2 files (-77 LOC) `40809926` / C3.5 I084 NEW invariant `ccb843c0` / C4 prior-phase guards fixup `7d7456dd` / C5 12 regression guards G1-G12 `ff9cb8e2` / C6 doc sync v54.15。**闭环 ARCHDEBT-CANDIDATES.md Top 5 (5/5)**: Phase 79 story_contracts + Phase 80 subplot + Phase 81 di + Phase 82 util + Phase 83 config。1 public symbol (APIConfig singleton + get_api_config factory)。TRUE LEAF (0 workspace deps + 1 third-party dep pyyaml)。4 in-place consumer fixups (3 cross-pkg tools/ + 1 intra-infra)。Pre-C1 fixup #1: intra-package absolute import rename (Phase 80/82 v2 lesson caught in C1); Pre-C1 fixup #2: filesystem path refactor (Phase 56b2 lesson — LINGWEN_CONFIG_PATH env var + pyproject.toml parent detection)。**11/11 NEW regression guards GREEN** (G3+G6+G8 marked n/a for no-MIGRATE) + **13/13 prior-phase preserved**。**2 lessons**: (1) Pre-C1 grep for intra-package absolute imports — caught in C1 before commit (vs Phase 80/82 slipped into C3); (2) Filesystem path literal needs pre-C1 fixup — Phase 83 C1 refactored to LINGWEN_CONFIG_PATH env var + pyproject.toml parent detection. **MILESTONE**: ARCHDEBT-CANDIDATES.md Top 5 closed (5/5 真迁移 + 8 zero-consumer dirs). **Cluster cumulative**: Phase 53 + 53b + 53c + 53d + 53e + 78 + 79 + 80 + 81 + 82 + 83 = 11 phases / ~19102 LOC dead code + 8 zero-consumer dirs + **5 真迁移 packages (LEAF + NOT-LEAF + TRUE LEAF + NOT-LEAF retry + TRUE LEAF config — 4 package shapes verified)**。**Next candidates**: ARCHDEBT-REAL cycle 第 6 例 — `infra/tools/` (~1750 LOC NOT-LEAF workflow + consistency multi-week); OR non-ARCHDEBT 工作. **ARCHDEBT-CANDIDATES.md Top 5 闭环** — 全部 5 个候选完成。详见 `docs/superpowers/handoffs/2026-09-14-phase-83-p3-archdebt-config-handoff.md` + spec `2026-09-14-phase-83-p3-archdebt-config-design.md` || 2026-09-14 | 协调者 | **v54.10 Phase 78 ARCHDEBT-MINI llm_benchmarks + poc cleanup 闭环**：git rm 17 files / 4 dirs / -1854 LOC (infra/llm_benchmarks/ 7 files 751 LOC + tests/infra/llm_benchmarks/ 7 files 570 LOC + infra/poc/ 2 files 439 LOC + tests/poc/test_end_to_end.py 1 file 94 LOC); atomic C1 pathspec BOTH infra + tests per I079 §A5; I074 6 → 8 目录; .lingwen/architecture.yml 删除 llm_benchmarks standalone entry; .gitignore 删除 infra/llm_benchmarks/results/ orphan; test_phase53d_event_sourcing.py preserved-list fixup (N.14 lesson 1 v22 第 2 次变体); 25 regression guards (G1-G12); v54.9 → v54.10; 5 atomic commits on `phase-57-p3-archdebt-reading-power` = C0 spec `4f4f61b1` / C1 git rm `7651f79a` / C2 stale refs `064e2afc` / C3 25 guards `72bc23dd` / C4 doc sync v54.10。**Cluster cumulative**: Phase 53 + 53b + 53c + 53d + 53e + 78 = 6 phases / ~17022 LOC dead code / 8 zero-consumer dirs saturation。**Lessons**: (1) N.14 lesson 1 v22 第 2 次变体 (prior-phase guard hardcoded representative); (2) Early-return safe pattern (N.14 v22 第 1 次变体 sibling); (3) I079 §A5 half-migration defense worked — no 4th recurrence; (4) ARCHDEBT-MINI cluster has saturated infra/ top-level. **Carryover**: 未来 ARCHDEBT 需迁移 active subdirs (story_contracts 848 LOC + subplot 508 LOC) → packages/* (NOT-LEAF, multi-week)。详见 `docs/superpowers/handoffs/2026-09-14-phase-78-archdebt-llm-benchmarks-poc-handoff.md` || 2026-09-12 | 协调者 | **v54.3 Phase 58 cross_volume 16 failures fix 闭环**：220/220 cross_volume tests GREEN (was 204/220)。4 atomic commits on `phase-57-p3-archdebt-reading-power` = C1 fix(cli) lazy-init Command base.py `5d4d0d30` / C2 fix(cross-volume) test paths to apps.studio_api.X `d87a5a08` / C3 test(phase-58) 5 guards + v54.2→v54.3 `d90e29d7` / C4 docs。**2 root causes collapsed 16 fails**：(RC2) `Command.__init__` eagerly validated `ProjectPaths.get()` → 12 tests broke on construction → fix: lazy @property pattern in base.py (32 + / 4 -); (RC1) tests used legacy `dashboard.X` import paths (Vue frontend, never Python) → 4 tests patched wrong module → fix: update 3 test files to canonical `apps.studio_api.X`. **Validation**: 220/220 GREEN from 3 cwds (repo root / package dir / tests dir; Phase 56b2 lesson); 5/5 phase58 guards GREEN (G1 no eager ProjectPaths.get() / G2 no eager project_max_chapter / G3 no dashboard.X in cross_volume tests / G4 220/220 / G5 lazy construction succeeds); 158/158 phase5x preserved; 163/163 phase5x+58 cumulative. **Lessons**：(1) handoff hypotheses overcount root causes — 5 RCs in handoff §146 collapsed to 2 via `pytest -xvs`; (2) `assert 0 == 1` where `len([]) == 0` is signature of monkeypatch path mismatch; (3) eager construction with side effects is a test smell — "construction should be side-effect-free" applies to CLI base classes; (4) 1 production code edit (base.py) + 3 test file edits — minimal blast radius. **Carryover CLOSED**: Phase 56c §146 16 carryover failures → 0. **Next candidates**: Phase 53c infra/tools/ residual audit OR product brainstorm (Write Workspace / World / Reading Power). 详见 `docs/superpowers/handoffs/2026-09-12-phase-58-cross-volume-failures-handoff.md` || 2026-09-09 | 协调者 | **v39.0 P3-ARCHDEBT (studio_registry) 5/5a 闭环**：P3-ARCHDEBT 5/5a 完成。8 commits on phase-40-p3-archdebt-studio-registry (C0 spec+plan / C1 scaffold / C1.5 factory_root() fixup / C2a intra-infra 6 sites / C2b bulk 41 sites: 17 apps + 18 packages + 5 apps test patches + 1 doc / C3 shim-not-delete (infra/studio_registry.py → 1-line shim) / C4 invariant #55 NEW + v38.0→v39.0 / C5 11 phase40a guards + Phase 38 guard fixup + handoff) ff-merge。infra/studio_registry.py (1 module, 422 lines, 18 public symbols: 1 frozen dataclass StudioProject + 17 public funcs) → packages/lingwen-studio-registry/ (5 sub-modules: models + discovery + state + summary + reports, 491 lines total)。47 production edits migrated; 0 function-body import misses; 0 relative imports; 0 filesystem path literals; 1 wildcard infra/studio/__init__.py:2 paired with C3 shim approach。**NOT-LEAF package** — 3 workspace deps: lingwen-paths + lingwen-project-config + lingwen-core (first non-LEAF P3-ARCHDEBT package)。**CRITICAL C1.5 fixup**: factory_root() path-relative formula broke after file relocation (used env var + parents[4] instead)。**Validation gates**: 11/11 phase40a guards GREEN + 7/7 phase38 preserved (after C5 fixup: test_canonical_symbols_migrated swapped infra/studio_registry.py → infra/project_characters.py as intra-infra representative) + 6/6 phase37 + 6/6 phase36 + 39 phase32-35 + 69 lingwen-core+pipeline baselines + 73 lingwen-creator (isolated) + ruff clean (new files)。9 pre-existing ruff I001 unchanged。**Carryover**: P3-ARCHDEBT 5/5b (~33 tests/ root edits + DELETE shim + DELETE wildcard + +33 regression guards; 4-5 atomic commits) → Phase 40b || 2026-09-08 | 协调者 | v35.0 P3-ARCHDEBT (errors pilot) 闭环：P3-ARCHDEBT 1/5 完成。6 commits on phase-36-p3-archdebt-errors (C0 spec / C0b plan / C1 scaffold / C2 14-consumer migration / C3 infra/errors.py deletion / C4 invariant #51 NEW + version bump / C5 6 phase36 guards + doc sync) ff-merge。infra/errors.py (1 module, 380 lines, 23 public symbols) → packages/lingwen-errors/。14 consumer migration (4 packages: lingwen-quality/world-model/pipeline/llm + 8 intra-infra)。Validation: 6 phase36 guards GREEN + 7 baselines preserved (lingwen-core 68/68 + lingwen-world-model 201/201 + lingwen-got 208/208 + studio_api 82/82 + lingwen-quality + lingwen-pipeline + lingwen-llm) + ruff clean + grep audit 0 hits (infra.errors.* 引用清零)。C5 T4 follow-up fixes: architecture.yml I051 scope test name `test_infra_errors_module_deleted` → `test_infra_errors_deleted` + CLAUDE.md v35.0 entry package list (6 wrong → 4 correct)。carryover 1 项：P3-ARCHDEBT remaining 4/5 (paths/project_config/logging_config/studio_registry) → Phase 37+ || 2026-09-07 | 协调者 | v31.0 ARCHDEBT-MINI 闭环：P2-ARCHDEBT 子集 2/4 清理。6 commits `8f8c3e1a..4dbe8939` ff-merge：Sub-task A chapter_golden_path 反向 import 修复（create_golden_dashboard_client + run_human_review_smoke + HumanReviewSmokeResult 从 lingwen-core 迁 apps/studio_api/tests/golden_path_smoke.py；-87 行 / +106 行；fixes I001 spirit violation）+ Sub-task B 4 薄代理 (advance_step/dispatch_task/verify_task/get_workflow_status) 从 WorkflowMixin 抽到 mc_orchestrator_proxy.py（MasterController MRO 加 OrchestratorProxyMixin + +5 refactor-guard tests + mc_workflow.py 119→99 行）+ 12 文件 doc 同步（"5 薄代理" → "4 薄代理"，Phase 27 拆 WorkflowRunner 后 stale）+ handoff。carryover 1 项：P2-ARCHDEBT (剩余 infra.got 迁移 + shim cleanup) || 2026-09-07 | 协调者 | v30.0 TACKLE-14-FAILURES 闭环：tests/agent_system 14→0 failed (-14)，tests/got 156 / lingwen-core 68 / ruff 不变。9 commits `2de95a80..38b854e7` ff-merge：1 spec + 1 plan + 5 atomic tasks (T1 stub `_state` 注入 / T5 env-var rewrite / T4 cost record_at + migration / T2 WorkflowRunner 迁移 / T3 audit try/except) + 1 handoff。**2 真 prod 改动**（mc_writing.py 韧性契约 broad except + log warning + 空 audit report 兜底 / cost_persistence.py `record_at()` public helper）+ 5 test 文件调整 + 2 处 stale assertion 修正。carryover 1 项：P2-ARCHDEBT || 2026-09-07 | 协调者 | v29.0 P2-MC-WRITING 闭环：tests/agent_system 84→14 failed (-70)，tests/got 156 / lingwen-core 68 / ruff 不变。17 commits `106dd21d..b1b748ad` ff-merge：1 spec + 1 plan + 5 production-fix (skill-registry pilot memory-hook budget-endpoints master-controller) + 1 真 prod 改动 (`mc_editing.py:202` 修 Phase 15.0 P3-SPLIT 迁移遗留 `from mc_utils import ...`) + 8 test-only (master-controller-e2e phase7-1 master-controller-with-usage agent-config cost-persistence path-asserts) + 1 handoff。剩余 14 failed 按 5 类根因归档（7 stub `_state` / 1 export / 2 audit 韧性 / 2 `_connect()` API / 2 env-var）→ P2-30-FAILURES Phase 候选。carryover 2 项：P2-30-FAILURES / P2-ARCHDEBT || 2026-09-04 | 协调者 | v28.0 P2-RESUME-VERIFY 闭环：5 E2E tests 用真实 GoTScheduler + ThoughtGraph（非 MagicMock）验证 scheduler 对已 COMPLETED 节点幂等 + DECISION resume continuation + start_nodes=None derivation + state.start_nodes 持久化 + WorkflowRunner.run→resume 完整 cycle。0 改范围 9 类文件不动 (workflow_runner / mc_workflow / workflow_state / got_bridge / infra.got / graph / shim / facade / HANDOFF*)；0 new failure (master 84 baseline = worktree 84, +5 NEW PASS)；2 RED 学习点（scheduler.run 二次调用 paused=False 不报告旧 paused；scheduler.run(start_nodes) 不限制执行范围到 start_nodes chain）。7 commits `05e4f91b` spec + `d3f164c8` plan + 5 tests + `7f9049f3` handoff 待 ff-merge。carryover 2 项：P2-MC-WRITING（独立大 phase 调查 84 pre-existing cascade）/ P2-ARCHDEBT（战术分散 + 删 PHASE-COMPAT shim） || 2026-09-04 | 协调者 | v27.0 P2-WFRUNNER 闭环：`WorkflowRunner` service class 从 WorkflowMixin 抽出（workflow_runner.py 307 行 new）+ mc_workflow.py 404 → 119 行 (-70%)；TDD 21 tests + 2 refactor guards；0 改范围 7 类文件不动；0 new failure (master 90 → worktree 84, net -6)。13 commits `9b57d16a` 待 ff-merge。carryover 3 项：P2-RESUME-VERIFY（紧接 Phase 28 建议）/ P2-MC-WRITING（独立大 phase）/ P2-ARCHDEBT（战术分散 + 4 薄代理 → OrchestratorProxyMixin） || 2026-09-03 | 协调者 | v25.9 human_review 流水线修复闭环：mc_workflow.py 还原（105→376 行）+ 4 dashboard smoke 解 skip + cascade +15 fixed / 0 new failures；master `0a6f4346` 4 commit ff-merge；carryover 5 项 Phase 26+ 候选 || 2026-09-03 | 协调者 | v25.4 收尾：前端类型债清零（vue-tsc 0 error）+ batch templates 前端闭环（PilotTemplatePanel）+ socksio 依赖修复 + stash@{0} 清理。P2-DTOMIGR 由会话-A 完成，P2 候选全部完成 || 2026-09-02 | Track A | REQ-001 切片 E「差异收尾」：`CreatorDeviationFinalize.vue` 可操作收尾清单，复用 `utils/batchDeviation.ts`（与节奏带共享批次偏差推导）+ 按批次持久化复核状态。至此 REQ-001 全部切片完成 || 2026-09-02 | 协调者 | 黑板从 v12/Phase15 刷新至 v25.1：废弃过期 P1/P2/P15 记录，改为 P1-SHIM + Phase 26+ 候选 backlog || 2026-09-21 | 协调者 | **v60.3 Phase 105 (cleanup_route failure tracking — third Phase 102+ extension — REQ-002 v2 stays FULLY CLOSED post-Phase 102; Phase 105 is additive extension)**: 8 atomic direct master commits per 2026-09-15 simplified workflow = 2 docs (spec `d84d2dbd` + plan `b0b99ee0`) + 1 refactor (relocate _resolve_threshold pipeline.py → notifications.resolve_threshold + back-compat re-export `8392ce8c`) + 1 feat (cleanup_route _load_cleanup_settings + 2 record_failure calls + 1 record_success call + record_failure signature accepts project_root: Path | None `a89b04e8`) + 1 test (T1-T6 cleanup_route failure path tests `d7e7b8f8`) + 1 test (T7-T9 counter isolation + helper relocation + back-compat `a2b7454c`) + 1 test (G1-G6 regression guards `5a8e7299`) + 1 docs sync (this commit). **6-module change** (1 notifications.resolve_threshold NEW function relocated from pipeline.py with same defensive fallback; missing notify_threshold key → INFINITY; bool → INFINITY) + (1 notifications.record_failure + _emit_failure_warning signature widens to project_root: Path | None — None skips audit_log.record_event but still fires publish for SSE fan-out; supports LoadError on missing slug path) + (1 cleanup_route _load_cleanup_settings NEW helper reads .lingwen/illustration_settings.yaml with permissive fallback missing file → {}; malformed yaml → {}) + (1 cleanup_route failure tracking: 404 LoadError → record_failure(project_root=None, event_type="cleanup"); StoreError on lru_cleanup → record_failure(project_root=real, event_type="cleanup"); successful lru_cleanup → record_success(event_type="cleanup") in else branch; 422 validation chapter_num missing NOT tracked; dry_run path NOT tracked; _load_max_assets silent fallback NOT tracked) + (1 pipeline.py back-compat re-export from lingwen_illustrations.notifications import resolve_threshold as _resolve_threshold — Phase 102/104 callers unchanged) + (1 I095 EXTENDED via docstring only — same invariant name; wider scope adds cleanup_route as the second caller after pipeline.py; project_root: Path | None added for LoadError on missing slug path; resolve_threshold RELOCATED to notifications.py with pipeline back-compat). **6 NEW regression guards G1-G6** (tests/test_phase105_cleanup_route_failure_tracking.py): G1 cleanup_route has ≥2 record_failure(event_type="cleanup") calls / G2 ≥1 record_success call / G3 resolve_threshold same callable as pipeline._resolve_threshold / G4 pipeline.py has ≥2 _resolve_threshold call sites / G5 3 cleanup failures leave generation counter = 0 / G6 _load_cleanup_settings permissive fallback. **9 NEW pytest tests T1-T9**: T1 LoadError record_failure / T2 StoreError record_failure / T3 successful record_success / T4 422 validation no record / T5 dry_run no record / T6 threshold crossing emits 1 warning / T7 counter isolation / T8 resolve_threshold values / T9 back-compat callable same. **Validation**: pytest lingwen-illustrations notifications 9/9 preserved + pipeline 6/6 preserved + T7-T9 3/3 NEW + Phase 103/104 guards 13/13 + 8/8 (21/21 total in package) + apps/studio_api/test_cleanup_route_failure_tracking.py 6/6 NEW T1-T6 + tests/test_phase105_cleanup_route_failure_tracking.py 6/6 NEW G1-G6 + Phase 102 guards 12/12 preserved + ruff clean on introduced. **7 lessons**: (1) Tuple-keyed counter isolation; (2) N/A this phase; (3) N/A this phase; (4) _resolve_threshold defensive fallback preserved across relocation; (5) N/A this phase; (6) I095 EXTENDED via docstring preserves YAGNI (Phase 105 EXTENDS again, 3rd extension of same invariant name); (7) **NEW Phase 105**: cleanup_route uses `from ... import X` for project_root_for + lru_cleanup (local bindings); patching source module attribute does NOT affect cleanup_route local binding — tests must patch cleanup_route.X directly. **Cluster cumulative**: Phase 90-105 = 16 phases / 1 NEW package + 5 carryover closures + 7 REQ-002 v2 sub-projects + **3 Phase 102+ extensions (Phase 103 default_models + Phase 104 notify_threshold per event_type + Phase 105 cleanup_route failure tracking)**. **Phase 105 is the third Phase 102+ extension** — proving the architecture supports multi-module small additive extensions without breaking existing invariants. **Future work**: deletion event_type tracking (when DELETE endpoint added) / telemetry-driven chain reorder (gated on Phase 102 failure tracker data accumulation) / ARCHDEBT-REAL continuation (if requested). 详见 `docs/superpowers/handoffs/2026-09-21-phase-105-cleanup-route-failure-tracking-handoff.md` + spec `2026-09-21-phase-105-cleanup-route-failure-tracking-design.md` + plan `2026-09-21-phase-105-cleanup-route-failure-tracking.md` |

---

## ✅ Phase 90 已完成（2026-09-15 → 2026-09-16）

| 版本/阶段 | 内容 | 验证 |
|----------|------|------|
| **v55.0 Phase 90 (REQ-002 多模态: 封面/插图生成)** | master 34 atomic commits: spec + plan + scaffold `packages/lingwen-illustrations/` + I087 / 7 backend submodules (exceptions / metadata / style_templates / storage / prompt_builder / image_generator / pipeline) / 53 backend tests / FastAPI 4 routes + auto-gen background / useIllustration composable + Pinia store / 4 frontend components + 3 page mods / 27 frontend tests / 7 regression guards G1-G7. 新包 workspace deps: lingwen-llm-service + lingwen-project-characters + lingwen-paths. 5 spec deviations tracked: P2-EXTRACT-ENUM + P2-ILLUSTRATIONS-BIBLE-CANONICAL + image_generator b64_json real-API decode deferred + regenerate non-atomic + ProjectSettingsPage→SettingsPage substitution. Handoff: `docs/superpowers/handoffs/2026-09-15-phase-90-illustrations-handoff.md` | ✅ pytest 53/53 + 8/8 + 15/15; ruff clean; vitest 27/27; tsc 0 new errors; knip clean; vite build exit 0 |

| 2026-09-21 | 协调者 | **v60.4 Phase 106 (delete illustration endpoint failure tracking — fourth Phase 102+ extension — REQ-002 v2 stays FULLY CLOSED post-Phase 102; Phase 106 is additive extension)**: 12 atomic direct master commits per 2026-09-15 simplified workflow = 2 docs (spec `f459ddad` + plan `cd04e148`) + 2 test (T1-T6 RED `3ca5a216` + T7-T9 RED-ish `c82a3ce0`) + 1 helper feat (`d50ee0ad`) + 1 wire feat (`03e1606c`) + 2 frontend (NPopconfirm tests RED `388be067` + NPopconfirm wrapping `5d7adbbf`) + 1 fixup (remove F4 `781cf019`) + 1 G1-G6 regression guards `8d4f4a49` + 1 invariant extension `23466a79` + 1 docs sync (this commit). **4-module change** (1 backend route `apps/studio_api/routes/illustrations.py` add `_load_deletion_settings(project_root)` NEW helper reads `.lingwen/illustration_settings.yaml` with permissive fallback missing file → {}; malformed yaml → {}; mirrors Phase 105 `_load_cleanup_settings` for module-locality; wire `delete_asset` route into notifications failure tracking: 404 LoadError on `project_root_for` → `record_failure(slug, e, project_root=None, threshold=resolve_threshold({}, "deletion"), event_type="deletion")` counter increments; audit_log skipped since no real project context; 404 asset-not-found → `record_success(slug, event_type="deletion")` **no-op success** — asset is already gone, idempotent; matches Phase 105 cleanup_route "0 deleted under limit" pattern; StoreError on `storage.delete_asset` → `record_failure(slug, e, project_root=real_root, threshold=resolved, event_type="deletion")` + HTTPException 500; successful `storage.delete_asset` → `record_success(slug, event_type="deletion")` in `else` branch of try/except; `audit_log.record_event` + `notifications.publish` **double-write** I091 completion — same ULID via `new_event_id()` — for all 4 event_types now; `extra={"trigger": "manual"}` consistent with cleanup_route pattern; `import yaml` + `from lingwen_illustrations import audit_log, notifications` at module top safe — lightweight modules, no import-time side effects verified in cleanup_route.py:27) / (1 frontend component `IllustrationCard.vue` 🗑 button wrapped in `<NPopconfirm>` (Naive UI) — `positive-text="确认删除"` / `negative-text="取消"`; data-testid="delete-btn" + `:aria-label` preserved for a11y; cancel path is purely frontend — no `emit('delete')` fires, no network call, no counter touched) / (3 test files — 6 NEW pytest T1-T6 deletion failure paths + 6 NEW pytest T7-T9 deletion isolation + helper resilience + 6 NEW pytest G1-G6 regression guards + 3 NEW vitest F1-F3 NPopconfirm — F4 Esc-dismissal removed in `781cf019`) / (1 invariant extensions — I090 / I091 / I095 EXTENDED via docstring only). **Validation**: pytest apps/studio_api/tests/test_illustrations_api.py 6/6 NEW T1-T6 deletion failure paths GREEN + packages/lingwen-illustrations/tests/test_phase106_deletion_isolation.py 6/6 NEW T7-T9 GREEN + tests/test_phase106_delete_endpoint_failure_tracking.py 6/6 NEW G1-G6 GREEN + Phase 102/103/104/105 guards 34/34 preserved GREEN + ruff clean on introduced + pnpm tsc 0 new errors (48 pre-existing baseline unchanged). **7 lessons**: (1) 404 asset-not-found = no-op success (defensive counter reset, idempotent — symmetric with Phase 105 cleanup_route "0 deleted under limit"); (2) naive-ui NPopconfirm doesn't support Esc-by-default (F4 test removed — behavior is click-outside / button-click only); (3) Tuple-keyed counter isolation (Phase 104 invariant inherited unchanged — verified via G5); (4) `_resolve_threshold` defensive fallback (`_load_deletion_settings` returns `{}` on missing/malformed yaml → `resolve_threshold({}, "deletion")` returns INFINITY); (5) I095 EXTENDED via docstring preserves YAGNI (4th extension of same invariant name — G6 verifies via parsed YAML value); (6) Frontend module imports safe at top (`import yaml` + `from lingwen_illustrations import audit_log, notifications` — lightweight modules, no import-time side effects verified in cleanup_route.py:27); (7) NPopconfirm cancel path is purely frontend (no `emit('delete')` fires, no counter touched — backend never sees cancel). **Cluster cumulative**: Phase 90-106 = 17 phases / 1 NEW package (lingwen-illustrations) + 5 carryover closures + 7 REQ-002 v2 sub-projects delivered + **4 Phase 102+ extensions (Phase 103 default_models + Phase 104 notify_threshold per event_type + Phase 105 cleanup_route failure tracking + Phase 106 delete_asset failure tracking)**. **Phase 106 is the fourth Phase 102+ extension** — proving the architecture supports multi-endpoint + frontend small additive feature extensions without breaking existing invariants. **deletion event_type is now FULLY wired** — all 4 event_types have ≥1 caller + counter isolation + I090/I091/I095 invariant coverage complete; no more orphan references. **Future work**: telemetry-driven chain reorder (gated on Phase 102 failure tracker data accumulation — Phase 107 candidate) / bulk DELETE endpoint (YAGNI for v1) / delete from NotificationsPage dropdown (Phase 108+) / ARCHDEBT-REAL continuation (if requested). 详见 `docs/superpowers/handoffs/2026-09-21-phase-106-delete-endpoint-failure-tracking-handoff.md` + spec `2026-09-21-phase-106-delete-endpoint-failure-tracking-design.md` + plan `docs/superpowers/plans/2026-09-21-phase-106-delete-endpoint-failure-tracking.md` |
