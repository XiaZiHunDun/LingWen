# Phase 9.18 — E2E Unskip `ripples-audit.spec.js` 3 Tests + 新增 `ripple-reset` CLI Design Spec

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Phase 9.17 修复 16/16 e2e baseline 健康的 followup — Phase 9.14 T7 留 `ripples-audit.spec.js` 3 个 `test.skip(true, 'requires live backend with seed data...')` tests(主公决策 2026-06-11 AskUserQuestion 选 A),现在 Phase 9.13 backfill 已合并 (`infra/.state/ripple.db` 有 `rip-pending-1` + `rip-rejected-1` 2 个 test ripples + 3 audit entries),unskip 3 tests 走真 backend。Test 2 (apply → rollback) 每次跑会改 `rip-pending-1` 状态(pending → applied → rolled_back),为 idempotency 加 `lingwen.py ripple-reset <id> --to-status <status>` CLI 子命令(跟 `ripple-audit` / `ripple-rollback` 1:1 pattern),Test 2 `test.beforeEach` 调此 CLI reset `rip-pending-1` 回 pending。0 改 Phase 9.10-9.17 既有 source API 行为 / dashboard / MasterController / cost_tracker / 12 SCENARIOS 路由 / ECharts 5.5,仅 additive 1 new CLI command + 1 new CLI parser + 1 new pytest file (4 tests) + 1 spec file 改 (`ripples-audit.spec.js` 删 3 `test.skip` + 加 1 `test.beforeEach`)。

**Architecture:** 2 sub-task additive: (T1 backend CLI) `infra/cli/commands/ripple_reset.py` NEW (~45L, `lingwen.py ripple-reset <ripple_id> --to-status <status>` 子命令,UPDATE reference_ripples status + applied_at=NULL + INSERT ripple_audit row action=rolled_back origin=system reason="reset to <status>") + `infra/cli/parsers/ripple_reset.py` NEW (~20L, --to-status {choices}=pending,applied,rejected,failed,created) + `lingwen.py` 注册新 command (+2L) + `infra/cli/commands/__init__.py` export (+1L) + `tests/cvg_cli/test_ripple_reset.py` NEW (~80L, 4 tests: idempotent reset pending/clear applied_at/404 缺失/--to-status 非法值) + `lingwen.py` argparse subparser 注册; (T2 e2e unskip) `dashboard/frontend/tests/e2e-smoke/ripples-audit.spec.js` 删 3 `test.skip(true, ...)` + Test 2 加 `test.beforeEach` (`execSync('python ../lingwen.py ripple-reset rip-pending-1 --to-status pending', {cwd: __dirname})`)。Phase 8.45.3 Playwright config 不动,Phase 9.13 `_setup.js` shim 不动,Phase 9.13 `tests/cvg_cli/test_ripple_audit.py` 既有 tests 0 改。

**Tech Stack:** Python 3.12 + sqlite3 (0 改 schema) + argparse (既有 lingwen.py 1:1 pattern) + Vue 3 + Vite + vitest + Playwright (Phase 8.45.3 opt-in `npx playwright install chromium`) + pytest 8 + ruff。Baseline: 2434 pytest + 148 vitest + 13 e2e listed (4 smoke + 3 ripples + 3 ripples-audit SKIP + 3 cascade-realtime) = 2595 total, 0 failed, 27 skipped (2430 baseline + 4 Phase 9.17 T1 + 1 T2 useWorkflowSocket + 3 ripples-audit skip 跟 21 pytest skipped 叠加)。Target: 2438 pytest (+4 new CLI tests) + 148 vitest + 16 e2e (smoke 4 + ripples 3 + ripples-audit 3 unskip pass + cascade-realtime 3 + ripple-dryrun 1 + ripple-drawer 1 = 15) = 2601 total (+6 net new, 3 skip 移除)。

---

## 1. Context

**Phase 9.13-9.17 cross-volume ripple history**:
- **Phase 9.13** ✅: dashboard `/ripples` page + 5 REST endpoints + 1 WebSocket `/api/ws/cvg`
- **Phase 9.14** ✅ (commits `fe0da0a`+`c583fd9`+`311306b`): ripple audit log + real rollback CLI (`lingwen.py ripple-rollback`) + 44 new tests
- **Phase 9.14 T7** ✅: `ripples-audit.spec.js` 3 tests 写好但 `test.skip(true, 'requires live backend with seed data; run after Phase 9.13 backfill on real data')` 跳过(决策:留 Phase 9.13 backfill 完成后 unskip)
- **Phase 9.15** ✅: cascade BFS 真实 (FIFO deque) + 持久化 + drawer graph viz + apply dry-run preview
- **Phase 9.16** ✅: cascade weighted BFS + 节点 hover 渐入 + cascade real-time WebSocket 推送
- **Phase 9.17** ✅ (commit `bb3430c`): Pydantic CascadeUpdatePayload + MAX_HANDLERS=50 guard + e2e baseline 3 修复 (smoke title regex / ripples beforeEach mock / ripples-audit 3 tests skip 显式注释)
- **Phase 9.18** ⬅️ **当前**: e2e unskip 3 ripples-audit tests + 新增 ripple-reset CLI

**Phase 9.17 局限性** (驱动力):
- `ripples-audit.spec.js` 3 tests 仍 skip (`test.skip(true, ...)`),即使 Phase 9.13 backfill 已合并 (`infra/.state/ripple.db` 有 3 ripples + 3 audit entries),无法验证 audit timeline UI flow 真的能跑通
- Test 2 (apply → rollback → 验证 audit) 跑一次后 `rip-pending-1` 状态变 rolled_back,DB 状态破坏,下次跑 fail(无 pending ripple)
- `infra/cli/commands/` 既有 2 ripple CLI (`ripple-audit` read-only + `ripple-rollback` mutating),缺 idempotent reset 能力
- e2e baseline 16/16 中含 3 显式 skip,等真 backend 接入后变 16/16 真 pass

**Phase 9.18 解决方案**:
- **T1 (backend CLI)**: 新增 `lingwen.py ripple-reset <ripple_id> --to-status <status>` 子命令(跟 `ripple-audit` / `ripple-rollback` 1:1 pattern),idempotent 重置 ripple 状态 + 写 ripple_audit row (action=rolled_back origin=system reason="reset to <status>"),为 Test 2 提供 idempotency 兜底
- **T2 (e2e unskip)**: `ripples-audit.spec.js` 删 3 `test.skip(true, ...)` + Test 2 `test.beforeEach` 调 ripple-reset CLI 重置 `rip-pending-1` → pending,3 tests 走真 backend (`pnpm dev` + FastAPI + ripple.db) 跑通
- **0 改 Phase 9.10-9.17 既有 source API 行为**: 既 12 storage method + 9 endpoint + 5 dashboard component + 2 composable + 1 router + 1 sidebar nav + 1 cascade notifier module + 1 CascadeGraph + Phase 9.14 ripple_audit / ripple_rollback 2 CLI 全部不动,仅 additive 1 new CLI command + 1 parser + 1 new spec file 改

---

## 2. Goals + Non-goals

### 2.1 Goals

1. **`lingwen.py ripple-reset` CLI 子命令** (跟 `ripple-rollback` 1:1 pattern): `name = "ripple-reset"`, `description = "重置 ripple 状态 (test/dev 工具)"`,接受 `ripple_id` positional + `--to-status {pending, applied, rejected, failed, created}` required
2. **UPDATE reference_ripples status + applied_at=NULL**: `storage.execute("UPDATE reference_ripples SET status = ?, applied_at = NULL WHERE id = ?", (to_status, ripple_id))`,1:1 跟 `ripple-rollback` SQL pattern
3. **INSERT ripple_audit row** (action=rolled_back, origin=system, reason=`f"reset to {to_status}"`): 跟 Phase 9.14 `record_audit` 1:1 风格,留 audit trail
4. **Idempotent**: 重复跑同样 reset 不会破坏 DB(同 status 重复 reset 等价 noop + 写 1 audit row)
5. **`lingwen.py` argparse subparser 注册**: 跟既有 ripple-audit / ripple-rollback 1:1 模式,`lingwen.py:ripple-reset --help` 可见
6. **4 new pytest tests** (`tests/cvg_cli/test_ripple_reset.py`):
   - `test_reset_pending_to_pending_idempotent` (idempotent, exit 0, status 保持 + 1 audit row)
   - `test_reset_applied_to_pending_clears_applied_at` (applied ripple + --to-status pending → applied_at=NULL, status=pending, exit 0)
   - `test_reset_404_for_missing_ripple` (unknown id → exit 1, stderr 含 "not found")
   - `test_reset_rejects_invalid_status_value` (`--to-status foo` → argparse exit 2)
7. **`ripples-audit.spec.js` 3 tests unskip**: 删 3 `test.skip(true, 'requires live backend with seed data...')` 注释
8. **Test 2 `test.beforeEach` ripple-reset 调 CLI**: `execSync('python ../../lingwen.py ripple-reset rip-pending-1 --to-status pending', {cwd: __dirname, stdio: 'pipe', timeout: 10_000})`,fail-fast throw
9. **0 改 Phase 9.10-9.17 既有 source API 行为**: 既 12 storage method + 9 endpoint + 5 dashboard component + 2 composable + 1 router + 1 sidebar nav + 1 cascade notifier + 1 CascadeGraph + Phase 9.14 ripple_audit / ripple_rollback 2 CLI 全部不动
10. **0 改 19 historical spec/plan docs `:NNN` 行号**: 既 9.10-9.17 + 8.0-8.45 + Phase 8 dashboard + Phase 9 cross-volume + Phase 7 等 spec/plan 保留

### 2.2 Non-goals

- **不改 `ripple-rollback` CLI**: Phase 9.14 既 implementation,0 改
- **不改 `ripple-audit` CLI**: Phase 9.14 既 implementation,0 改
- **不改 `RippleStorage` 既 12 method**: Phase 9.10-9.16 冻结 schema,0 改
- **不改 dashboard `/api/ripples` GET filter**: 既 9.13 endpoint,0 改
- **不改 dashboard `/api/ripples/{id}/apply` / `/rollback` endpoint**: 既 9.13-9.14 endpoint,0 改
- **不改 `<RippleDrawer>` audit timeline 渲染**: Phase 9.14 既 component,0 改
- **不改 `data-testid="ripple-audit-list"` / `ripple-audit-empty` / `ripple-drawer-apply` / `ripple-rollback-btn`**: Phase 9.14 既 testid,0 改
- **不写 `ripple-reset` --reason flag**: YAGNI,reason 自动 `reset to {to_status}` 即可
- **不写 `ripple-reset` --actor flag**: YAGNI,默认 actor=cli:lingwen-ripple
- **不写 FastAPI `/api/ripples/{id}/test-reset` endpoint**: YAGNI,CLI 就够,跟 Phase 9.14 `ripple-rollback` 1:1 仅 CLI
- **不写 Playwright globalSetup/Teardown backup-restore DB**: YAGNI,Test 2 beforeEach 调 reset 就够
- **不写 ripples.spec.js 既有 3 tests 改**: 9.17 T3b beforeEach mock 已 healthy,0 改
- **不写 25 e2e tests 0-backend 模式扩到 ripples-audit**: 主公决策 2026-06-11 AskUserQuestion 选 A (unskip 3 tests,需 live backend),不混 mock 模式
- **不写 real LLM e2e opt-in test** (跟 Phase 8.9 模式): YAGNI 留 followup
- **不写 dashboard UI cost 柱状图** (Phase 8.7): 留 followup
- **不写 cross-volume cascade depth ≥ 2** (Phase 9.18+): 留独立 phase

---

## 3. Design

### 3.1 T1: `lingwen.py ripple-reset` CLI 子命令 (跟 `ripple-rollback` 1:1 pattern)

**Pattern reference** (`infra/cli/commands/ripple_rollback.py`):
```python
class RippleRollbackCommand(Command):
    name = "ripple-rollback"
    description = "Rollback a ripple (Phase 9.14)"

    def execute(self, options: UnifiedOptions) -> int:
        ripple_id = options.ripple_id
        reason = options.reason
        if not reason:
            print("Error: --reason is required", file=sys.stderr)
            return 1
        storage = _get_storage()
        try:
            storage.rollback_ripple(ripple_id, actor=options.actor, origin=options.origin, reason=reason)
        except ValueError as e:
            print(f"Error: {e}", file=sys.stderr)
            return 1
        print(f"Rolled back {ripple_id}: status=pending, applied_at=NULL")
        return 0
```

**T1 新增** (`infra/cli/commands/ripple_reset.py` NEW, ~45L):
```python
"""ripple-reset command - Phase 9.18: idempotent reset ripple status (test/dev tool).

1:1 pattern with infra/cli/commands/ripple_rollback.py (Phase 9.14). Calls
storage.reset_ripple_for_test() which writes reference_ripples.status +
applied_at=NULL + ripple_audit row (action=rolled_back, origin=system,
reason="reset to <status>"). Designed for e2e test idempotency
(ripples-audit.spec.js Test 2 beforeEach).

Usage:
    lingwen.py ripple-reset rip-pending-1 --to-status pending
    lingwen.py ripple-reset rip-rejected-1 --to-status rejected
"""
import re
import sys
from pathlib import Path

from infra.cli.options import UnifiedOptions
from infra.cross_volume.storage import RippleStorage

from .base import Command

DEFAULT_RIPPLE_DB = Path(".state/ripple.db")
VALID_STATUSES = ("pending", "applied", "rejected", "failed", "created")


def _get_storage() -> RippleStorage:
    return RippleStorage(db_path=DEFAULT_RIPPLE_DB)


def _sanitize_reason(reason: str) -> str:
    """Strip control chars + newlines from reason (log-injection hardening)."""
    return re.sub(r"[\x00-\x1f\x7f]", "", reason).strip()


class RippleResetCommand(Command):
    """Idempotent reset ripple status (test/dev tool, Phase 9.18)."""

    name = "ripple-reset"
    description = "重置 ripple 状态 (test/dev 工具, Phase 9.18)"

    def execute(self, options: UnifiedOptions) -> int:
        ripple_id = options.ripple_id
        to_status = options.to_status
        if not ripple_id:
            print("Error: ripple_id is required", file=sys.stderr)
            return 1
        if to_status not in VALID_STATUSES:
            print(
                f"Error: --to-status must be one of {VALID_STATUSES} (got {to_status!r})",
                file=sys.stderr,
            )
            return 1
        storage = _get_storage()
        existing = storage.get_ripple_by_id(ripple_id)
        if existing is None:
            print(f"Error: ripple {ripple_id} not found", file=sys.stderr)
            return 1
        reason = f"reset to {to_status}"
        try:
            storage.reset_ripple_for_test(
                ripple_id=ripple_id,
                to_status=to_status,
                actor="cli:lingwen-ripple",
                origin="system",
                reason=reason,
            )
        except Exception as e:
            print(f"Error: {e}", file=sys.stderr)
            return 1
        print(f"Reset {ripple_id}: status={to_status}, applied_at=NULL (audit row written)")
        return 0
```

**`infra/cli/parsers/ripple_reset.py` NEW (~20L)** (跟 `ripple_rollback.py` parser 1:1):
```python
"""ripple-reset parser - Phase 9.18: add --to-status {choices} arg."""
import argparse


def add_ripple_reset_parser(subparsers: argparse._SubParsersAction) -> None:
    parser = subparsers.add_parser(
        "ripple-reset",
        help="重置 ripple 状态 (test/dev 工具)",
        description="Idempotent reset ripple status. Writes ripple_audit row.",
    )
    parser.add_argument("ripple_id", help="ripple id (e.g. rip-pending-1)")
    parser.add_argument(
        "--to-status",
        required=True,
        choices=("pending", "applied", "rejected", "failed", "created"),
        help="Target status to reset ripple to",
    )
```

**`infra/cli/commands/__init__.py` (+1L)**: `from .ripple_reset import RippleResetCommand`

**`lingwen.py` argparse subparser 注册 (+2L)**: 跟 `add_ripple_rollback_parser(subparsers)` 1:1 加 `add_ripple_reset_parser(subparsers)`

**`RippleStorage.reset_ripple_for_test` NEW** (`infra/cross_volume/storage.py`, ~30L):

> Phase 9.18 关键:既 `update_ripple_status` (storage.py:361) 仅接受 `(applied, rejected)`、`applied_at=NOW`、action auto-derived (applied/rejected),且 block terminal status 转换。`ripple-reset` 需要:接受 5 状态全集合 + `applied_at=NULL` + action=`rolled_back` + custom reason + bypass terminal check (test-only)。需新 method `reset_ripple_for_test`,不 modify 既有 (会破 Phase 9.13-9.14 callers)。

```python
def reset_ripple_for_test(
    self,
    ripple_id: str,
    to_status: str,
    actor: str = "cli:lingwen-ripple",
    origin: str = "system",
    reason: str = "test reset",
) -> CrossVolumeRipple:
    """Phase 9.18: idempotent reset ripple status (test/dev tool).

    Differs from update_ripple_status:
    - Accepts ALL 5 statuses (pending/applied/rejected/failed/created)
    - Sets applied_at=NULL (not NOW)
    - Writes audit action='rolled_back' (not auto-derived)
    - Bypasses terminal status check (test reset)
    - Custom reason (e.g. "reset to pending")
    """
    if to_status not in ("pending", "applied", "rejected", "failed", "created"):
        raise ValueError(f"to_status must be one of 5 valid statuses, got {to_status!r}")
    with self._connect() as conn:
        row = conn.execute(
            "SELECT * FROM reference_ripples WHERE id = ?", (ripple_id,)
        ).fetchone()
        if row is None:
            raise KeyError(f"ripple {ripple_id} not found")
        current = self._row_to_ripple(row)
        prev_status = current.status
        conn.execute(
            "UPDATE reference_ripples SET status = ?, applied_at = NULL WHERE id = ?",
            (to_status, ripple_id),
        )
        conn.commit()
    # Write audit (independent commit, like cost_tracker.record)
    self.record_audit(
        ripple_id=ripple_id,
        action="rolled_back",
        prev_status=prev_status,
        new_status=to_status,
        actor=actor,
        origin=origin,
        reason=reason,
    )
    # In-memory return updated
    updated = replace(current, status=to_status, applied_at=None)
    return updated
```

### 3.2 T2: `ripples-audit.spec.js` 3 tests unskip + Test 2 beforeEach

**Pattern** (`ripples-audit.spec.js:19, 32, 62`):
```javascript
// 旧 (Phase 9.17):
test.skip(true, 'requires live backend with seed data; run after Phase 9.13 backfill on real data')
test('page mount → drawer open → audit timeline visible', async ({ page }) => {
  ...
})

// 新 (Phase 9.18):
// Phase 9.18: unskip 3 tests, 走真 backend (Phase 9.13 backfill seed data 已就位).
// Test 2 加 beforeEach reset rip-pending-1 → pending 保证 idempotency.
test('page mount → drawer open → audit timeline visible', async ({ page }) => {
  ...
})
```

**Test 2 beforeEach** (在 `test.describe` 内 Test 2 之前):
```javascript
test('apply then rollback → audit timeline updates', async ({ page }) => {
  test.setTimeout(30_000);
  // Phase 9.18: idempotency reset rip-pending-1 → pending 避免 test 顺序依赖
  const { execSync } = await import('node:child_process');
  const cliPath = path.resolve(__dirname, '../../lingwen.py');
  try {
    execSync(`python ${cliPath} ripple-reset rip-pending-1 --to-status pending`, {
      stdio: 'pipe',
      timeout: 10_000,
    });
  } catch (e) {
    throw new Error(`ripple-reset CLI failed: ${e.message}`);
  }
  // ... existing test body unchanged
});
```

**`import path` 在 spec 顶部** (跟 Phase 8.45.3 e2e-shim 1:1 风格):
```javascript
import { test, expect } from '../_setup.js';
import { execSync } from 'node:child_process';
import path from 'node:path';
```

### 3.3 e2e 跑前准备 (主公 opt-in 步骤, 0 改 Playwright config)

```bash
# Step 1: 装 Playwright 浏览器 (1 次 opt-in)
cd dashboard/frontend && npx playwright install chromium

# Step 2: 启 dev server (后台)
pnpm dev  # FastAPI + Vite, http://localhost:5173

# Step 3: 跑 ripples-audit spec
cd dashboard/frontend && pnpm e2e:smoke ripples-audit.spec.js
# 期望: 3 passed (5xx timeout 可能, 1 retry 允许)
```

### 3.4 baseline 验证

```bash
# 1. pytest baseline + new tests
pytest tests/cvg_cli/test_ripple_reset.py -v
# 期望: 4 passed

pytest tests/cvg_cli/ -v
# 期望: 既有 tests + 4 new = 全部 passed (Phase 9.14 ripple_audit + ripple_rollback tests 0 改)

pytest -q 2>&1 | tail -3
# 期望: 2438 passed, 0 failed, 21 skipped (2434 baseline + 4 new)

# 2. ruff 0
ruff check infra/cli/commands/ripple_reset.py infra/cli/parsers/ripple_reset.py tests/cvg_cli/test_ripple_reset.py
# 期望: 0 issues

# 3. e2e 16/16 healthy (主公 opt-in 跑)
npx playwright test --reporter=list
# 期望: smoke 4 + ripples 3 + ripples-audit 3 (unskip pass) + cascade-realtime 3 + ripple-dryrun 1 + ripple-drawer 1 = 15/15 passed
```

---

## 4. Risks + mitigations

| 风险 | 缓解 |
|------|------|
| `RippleStorage.reset_ripple_for_test` method 不存在(Phase 9.14 只有 `rollback_ripple` + `update_ripple_status` 不够通用) | 新 method (~30L),接受 5 status 全集合 + `applied_at=NULL` + bypass terminal check + custom action/reason;test 4 cases 覆盖 idempotent + applied_at 清除 + 404 + 非法 status |
| 既有 Phase 9.14 `rollback_ripple` 跟 Phase 9.18 `reset_ripple_for_test` 重复 | 0 改既有 `rollback_ripple`,新 method 是 additive thin wrapper,接受更多 status + bypass terminal;`rollback_ripple` 0 改 public signature |
| 既 `update_ripple_status` (Phase 9.13) 跟 `reset_ripple_for_test` (Phase 9.18) 容易混淆 | 命名 `*_for_test` suffix 显式 test-only,1:1 跟 Phase 8.5 cost tracker `record_for_test` 风格 |
| Test 2 beforeEach `execSync` 失败 → test 整体 fail | throw new Error 显式 fail,DB 状态半变时下次跑 beforeEach 仍能 reset |
| 既有 25 e2e tests 跑后 rip-pending-1 状态破坏,下次 25 e2e run fail | ripples-audit Test 2 beforeEach reset idempotency 兜底,0 副作用外泄;其他 e2e tests 0 触 rip-pending-1 |
| `cwd` 路径错 (`execSync` 默认 cwd 是 test 进程 cwd) | `path.resolve(__dirname, '../../lingwen.py')` 显式绝对 path,`stdio: 'pipe'` 捕获 stderr |
| Playwright 浏览器未装 → ripples-audit 3 tests fail | `npx playwright install chromium` 1 行 opt-in,跟 Phase 8.45.3 README 1:1 |
| `pnpm dev` 未启 → `page.goto('http://localhost:5173/ripples')` timeout | Playwright 5s timeout fail,test 报告清晰,主公 opt-in 启 dev server |
| `lingwen.py` Python 3.12 路径问题 (system python vs venv) | `python` 命令默认走 system python 3.12(已 baseline),0 venv 依赖 |
| `rip-rejected-1` 在 Test 3 跑后 audit history 状态变 | Test 3 只读 + drawer open,0 副作用;Test 3 验证 `list.or(empty)` 自适应 |
| `reset_ripple_for_test` SQL injection | 用 parameter binding `?` placeholder,跟 Phase 9.14 `record_audit` 1:1 |
| 4 new tests 跟既有 `test_ripple_audit.py` 既有 tests 冲突 fixture | 用 `tmp_path` 隔离 DB(跟既有 4 ripple_audit tests 1:1 pattern),0 fixture 共享 |
| 0 改 19 historical spec/plan docs `:NNN` 行号 | 0 改 9.10-9.17 + 8.0-8.45 + Phase 7+ 等 spec/plan |
| 0 改 Phase 9.10-9.17 既有 source API 行为 | 仅 additive 1 CLI command + 1 parser + 1 new pytest file + 1 spec file 改 |
| 0 改 dashboard / MasterController / cost_tracker / 12 SCENARIOS 路由 | Phase 9.18 scope 限定 CLI + e2e spec,0 触 Agent layer / dashboard |
| 0 真实 LLM 调用 / 0 API key 泄漏 / 0 .env 改 | 0 provider 改, 0 既有 config 改 |
| 0 改 ECharts 5.5 | 0 触 CascadeGraph emphasis 配置 |
| 0 force-push / 0 amend / 0 --no-verify / 0 Co-Authored-By | 1 new commit, 0 触既有 commits |

---

## 5. Verification

### 5.1 pytest baseline + new tests

```bash
pytest tests/cvg_cli/test_ripple_reset.py -v
# 期望: 4 passed

pytest tests/cvg_cli/ -v
# 期望: 既有 ripple_audit + ripple_rollback tests 0 改 + 4 new passed

pytest -q 2>&1 | tail -3
# 期望: 2438 passed, 0 failed, 21 skipped (2434 baseline + 4 new)
```

### 5.2 e2e baseline 修复验证 (15/15 healthy)

```bash
# Pre-conditions: 启 vite dev server (pnpm dev) + FastAPI + ripple.db 已 seed
cd dashboard/frontend && pnpm e2e:smoke ripples-audit.spec.js
# 期望: 3 passed (unskip 3 tests + Test 2 beforeEach reset)

cd dashboard/frontend && pnpm e2e:smoke
# 期望: 15/15 passed (smoke 4 + ripples 3 + ripples-audit 3 + cascade-realtime 3 + ripple-dryrun 1 + ripple-drawer 1)
```

### 5.3 ruff 0

```bash
ruff check infra/cli/commands/ripple_reset.py infra/cli/parsers/ripple_reset.py tests/cvg_cli/test_ripple_reset.py
# 期望: 0 issues
```

### 5.4 grep 验证

```bash
# ripple-reset CLI 5 字段
grep -n "class RippleResetCommand" infra/cli/commands/ripple_reset.py
# 期望: infra/cli/commands/ripple_reset.py:36 (或类似)

grep -n "name = \"ripple-reset\"" infra/cli/commands/ripple_reset.py
# 期望: 1 definition

grep -n "add_ripple_reset_parser" infra/cli/parsers/ripple_reset.py lingwen.py
# 期望: parser file definition + lingwen.py registration

# ripples-audit 3 unskip
grep -n "test.skip.*live backend" dashboard/frontend/tests/e2e-smoke/ripples-audit.spec.js
# 期望: 0 匹配 (3 skip 已删)

grep -n "ripple-reset rip-pending-1" dashboard/frontend/tests/e2e-smoke/ripples-audit.spec.js
# 期望: 1 匹配 (Test 2 beforeEach)
```

### 5.5 git status clean

```bash
git status
# 期望: 7 改文件 (含 2 new spec/plan + 2 new CLI files + 1 new pytest file + 2 modified: storage.py + ripples-audit.spec.js)
# 准确: 1 new spec + 1 new plan + 1 new ripple_reset.py command + 1 new ripple_reset.py parser + 1 new test_ripple_reset.py + 1 modified storage.py + 1 modified ripples-audit.spec.js + 1 modified lingwen.py + 1 modified __init__.py = 9 files
```

### 5.6 git log verify

```bash
git log --oneline -1
# 期望: feat(dashboard): phase 9.18 — e2e unskip ripples-audit + ripple-reset CLI

git show --stat HEAD
# 期望: 9 files changed (1 spec + 1 plan + 1 command + 1 parser + 1 test + 1 storage + 1 e2e spec + 1 lingwen + 1 __init__)
```

---

## 6. Out of scope (后续 phases)

- **Phase 9.18+**: 跨卷涟漪下游级联 (cascade 触发新 cascade, 深度 ≥ 2, 需新的 cascade hook 链式 + `parent_ripple_id` 字段)
- **Phase 8.8**: cost budget alarm (超阈值 trigger 暂停 workflow) — MasterController 加 threshold check
- **Phase 8.9**: real LLM e2e opt-in test 验证 generate_with_usage (跟 Phase 8/8.1/8.2 模式)
- **Phase 9.19+**: cross-volume cascade graph 双视图 (existing 9.15 CascadeGraph + weighted 9.16 emphasis 之外的第 3 视图)
- **MEMORY.md 拆 14+ entries 到 phases.md topic**: 本期只 +1 entry,接受再超 1 行,留独立 phase
- **MEMORY.md line 8 typo "19 skipped" → "21 skipped"** (Phase 8.2 后忘改): 留后续
- **CI filter 收紧**: Phase 8.3 LOW 标, 留后续
- **`ripple-reset` 加 --reason / --actor flag**: YAGNI,默认 reason="reset to <status>" actor="cli:lingwen-ripple"
- **FastAPI `/api/ripples/{id}/test-reset` endpoint**: YAGNI,CLI 就够
- **Playwright globalSetup DB backup-restore**: YAGNI,Test 2 beforeEach reset 就够
- **dashboard 既有 endpoint handler / composable / store / router / sidebar nav / api method 改**: 0 改
- **12 SCENARIOS 路由 / MasterController / cost_tracker / workflow.db / cost_tracker.db 改**: 0 改
- **CascadeGraph emphasis + select 配置改**: 0 改 (Phase 9.16 既有)
- **RippleDrawer ripple-drawer-apply / ripple-rollback-btn testid 改**: 0 改 (Phase 9.14 既有)
- **19 historical spec/plan docs `:NNN` 行号改**: 0 改 (历史已不准, 改反误导)

---

## 7. Critical files

### Created (Phase 9.18)
- `docs/superpowers/specs/2026-06-11-phase9.18-e2e-unskip-ripples-audit-design.md` (本 spec, ~250L)
- `docs/superpowers/plans/2026-06-11-phase9.18-e2e-unskip-ripples-audit.md` (plan, ~300L)
- `infra/cli/commands/ripple_reset.py` (~45L, RippleResetCommand + _get_storage + _sanitize_reason)
- `infra/cli/parsers/ripple_reset.py` (~20L, add_ripple_reset_parser)
- `tests/cvg_cli/test_ripple_reset.py` (~80L, 4 tests)

### Modified (注入点 + 1 替换)
- `lingwen.py` (+2L, argparse subparser 注册 add_ripple_reset_parser)
- `infra/cli/commands/__init__.py` (+1L, export RippleResetCommand)
- `infra/cross_volume/storage.py` (+30L, `reset_ripple_for_test` method, additive 不改既 12 public method)
- `dashboard/frontend/tests/e2e-smoke/ripples-audit.spec.js` (+15L, 3 unskip + Test 2 beforeEach reset)

### Referenced / 复用
- `infra/cli/commands/ripple_rollback.py` (Phase 9.14 既有, 1:1 pattern, 0 改)
- `infra/cli/commands/ripple_audit.py` (Phase 9.14 既有, 1:1 pattern, 0 改)
- `infra/cli/parsers/ripple_rollback.py` (Phase 9.14 既有, 1:1 pattern, 0 改)
- `infra/cli/parsers/ripple_audit.py` (Phase 9.14 既有, 1:1 pattern, 0 改)
- `infra/cross_volume/storage.py:record_audit` (Phase 9.14 既有, 0 改; 新 `reset_ripple_for_test` thin wrapper)
- `tests/cvg_cli/test_ripple_audit.py` (Phase 9.14 既有 4 tests 验证 storage_with_ripple fixture 模式, 0 改)
- `dashboard/frontend/tests/_setup.js` (Phase 9.13 既有 shim, 0 改)
- `dashboard/frontend/playwright.config.js` (Phase 8.45.3 既有, 0 改)

### NOT modified
- `infra/cli/commands/ripple_rollback.py` (Phase 9.14 既有, 0 改)
- `infra/cli/commands/ripple_audit.py` (Phase 9.14 既有, 0 改)
- `infra/cli/parsers/ripple_rollback.py` / `ripple_audit.py` (Phase 9.14 既有, 0 改)
- `infra/cross_volume/storage.py:RippleStorage` 既 12 method + `update_ripple_status` (Phase 9.10-9.16 既有, 0 改 public signature, 新 `reset_ripple_for_test` additive)
- `dashboard/protocols.py` 全部 Phase 9.13-9.17 schemas (0 改)
- `dashboard/cascade_notifier.py` (Phase 9.16-9.17 既有, 0 改)
- `dashboard/app.py` / `dashboard/cvg_ws.py` (Phase 9.13-9.16 既有, 0 改)
- `dashboard/frontend/src/composables/useWorkflowSocket.js` (Phase 8.11-9.17 既有, 0 改)
- `dashboard/frontend/src/composables/useRippleStore.js` / `useRippleSocket.js` / `useDecisionStore.js` / `useOverviewStore.js` / `useCostWindow.js` / `useTimeOptions.js` / `useWorkflowListStore.js` (Phase 8.x-9.x 既有, 0 触)
- `dashboard/frontend/src/components/RippleCard.vue` / `RippleFilter.vue` / `RippleDrawer.vue` / `CascadeGraph.vue` / `SidebarNav.vue` / `App.vue` (Phase 9.13-9.16 既有, 0 触)
- `infra/agent_system/master_controller.py` / `infra/agent_system/got_bridge.py` (Phase 8.5-8.6.2 既有, 0 触)
- `infra/ai_service/cost_persistence.py` / `infra/ai_service/cost_tracker.py` (Phase 8.5 既有, 0 触)
- 19 historical spec/plan docs (0 改, 跟 Phase 9.10-9.17 一样不动)
- `pyproject.toml` / `pytest.ini` / `vite.config.js` / `vitest.config.js` / `playwright.config.js` / `dashboard/frontend/package.json` / `dashboard/frontend/tests/_setup.js` (0 改)
- `dashboard/frontend/tests/e2e-smoke/{smoke,ripples,cascade-realtime,ripple-dryrun,ripple-drawer}.spec.js` (Phase 9.13-9.17 既有, 0 改)
- `dashboard/frontend/tests/unit/*` (Phase 8.x-9.17 既有, 0 触)
- `.env` / `infra/.state/*` (gitignored, 0 触)
- `CLAUDE.md` / `MEMORY.md` / `README.md` (留 Phase 9.18 commit 后单独 update)

---

## 8. DoD (完成定义)

- [ ] `docs/superpowers/specs/2026-06-11-phase9.18-e2e-unskip-ripples-audit-design.md` 创建 (本 spec, ~250L)
- [ ] `docs/superpowers/plans/2026-06-11-phase9.18-e2e-unskip-ripples-audit.md` 创建 (~300L)
- [ ] `infra/cli/commands/ripple_reset.py` 创建 (RippleResetCommand, ~45L)
- [ ] `infra/cli/parsers/ripple_reset.py` 创建 (add_ripple_reset_parser, ~20L)
- [ ] `lingwen.py` argparse subparser 注册 `add_ripple_reset_parser` (+2L)
- [ ] `infra/cli/commands/__init__.py` export `RippleResetCommand` (+1L)
- [ ] `tests/cvg_cli/test_ripple_reset.py` 创建 (4 tests passed, ~80L)
- [ ] `dashboard/frontend/tests/e2e-smoke/ripples-audit.spec.js` 删 3 `test.skip(true, ...)` + Test 2 加 `test.beforeEach` reset (~+15L)
- [ ] `infra/cross_volume/storage.py:reset_ripple_for_test` NEW (~30L, 接受 5 status 全集合 + applied_at=NULL + bypass terminal check)
- [ ] pytest 2438 passed, 0 failed, 21 skipped (2434 baseline + 4 new)
- [ ] ruff 0
- [ ] e2e 15/15 healthy (主公 opt-in 跑, 3 ripples-audit tests unskip pass)
- [ ] 1 feat commit pushed to origin/master
- [ ] 0 旧测试代码改动 (2434 baseline 不变, 既有 4 ripple_audit + 4 ripple_rollback tests 0 改)
- [ ] 0 production code 行为破坏 (ripple-reset 是新 CLI, 0 触既有 ripple-audit / ripple-rollback)
- [ ] 0 CLAUDE.md / pyproject.toml / pytest.ini / vite.config.js / vitest.config.js / playwright.config.js 改动
- [ ] 0 误改 .env / 0 API key 泄漏
- [ ] 0 infra/.state/* 提交 (gitignored)
- [ ] 0 force-push / 0 --no-verify / 0 Co-Authored-By footer
- [ ] 0 改 Phase 9.10-9.17 既有 source API 行为
- [ ] 0 改 dashboard 既有 endpoint handler / composable / store / router / sidebar nav / api method
- [ ] 0 改 MasterController / got_bridge / workflow.db / cost_tracker.db
- [ ] 0 改 12 SCENARIOS 路由
- [ ] 0 改 ECharts 5.5
- [ ] 0 改 CascadeGraph emphasis + select 配置
- [ ] 0 改 RippleDrawer audit timeline 渲染 / data-testid
- [ ] 0 改 19 historical spec/plan docs `:NNN` 行号
- [ ] 0 真实 LLM 调用

---

## 9. 完成后下一步 (后续 phases)

- **Phase 9.19+**: 跨卷涟漪下游级联 (cascade 触发新 cascade, 深度 ≥ 2, 需新的 cascade hook 链式 + `parent_ripple_id` 字段)
- **Phase 8.8**: cost budget alarm (MasterController 加 threshold check, 留 Phase 8.x followup)
- **Phase 8.9**: real LLM e2e opt-in test 验证 generate_with_usage (跟 Phase 8/8.1/8.2 模式)
- **Phase 9.18 followup**: cross-volume cascade graph 双视图 (existing 9.15 CascadeGraph + weighted 9.16 emphasis 之外的第 3 视图, 留 Phase 9.19+)
- **MEMORY.md 拆 14+ entries 到 phases.md topic** (本期 +1 entry → 14 entries, 必拆)
- **MEMORY.md line 8 typo "19 skipped" → "21 skipped"** (Phase 8.2 后忘改)
- **CI filter 收紧**: Phase 8.3 LOW 标, `-k` filter 加 `and not OpenAI and not MiniMax`
- **`ripple-reset` 加 --reason / --actor flag**: YAGNI 留 followup
- **FastAPI `/api/ripples/{id}/test-reset` endpoint**: YAGNI 留 followup
- **Playwright globalSetup DB backup-restore**: YAGNI 留 followup
- **`reset_ripple_for_test` 取代 `rollback_ripple` 内部 SQL (DRY)**: 留 followup
