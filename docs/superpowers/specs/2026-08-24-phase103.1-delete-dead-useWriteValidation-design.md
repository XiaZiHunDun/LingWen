# Phase 103.1 — Delete Dead `useWriteValidation.ts`

> **Date**: 2026-08-24
> **Phase**: 103.1
> **Source**: Phase 103 code-quality review follow-up (LOW note)
> **Status**: Design

---

## 1. Context

Phase 103 stripped all dead re-exports of `useWriteValidation` from `useCreatorWrite/index.ts` and `composables/index.ts`. The result: `useWriteValidation.ts` itself has no runtime consumers — only JSDoc references in `useCreatorWrite.js` and `useCreatorWrite/index.ts` mention the function name.

knip now reports `Unused files (1): src/composables/useCreatorWrite/useWriteValidation.ts` because the source file's exports (`useWriteValidation`, `WriteValidationDeps`, `WriteValidationReturn`) are all unreferenced.

This is a tiny cleanup that resolves the residual Unused files item from Phase 103.

---

## 2. Goal

Delete `src/composables/useCreatorWrite/useWriteValidation.ts` and prune its 2 JSDoc references so knip's `Unused files` count returns to 0.

---

## 3. Non-Goals

- **NOT** re-implementing or restoring `useWriteValidation` — its functionality has been unused since the parent facade `useCreatorWrite.js` was refactored.
- **NOT** modifying `useCreatorWrite.js` beyond the JSDoc line 9 reference removal.
- **NOT** modifying `useCreatorWrite/index.ts` beyond the JSDoc line 6 reference removal.
- **NOT** investigating the "类型骨架占位" note — the function is fully implemented but not consumed.
- **NOT** modifying `composables.d.ts` or `knip.json` — the dead-file deletion should be sufficient.

---

## 4. Design

### 4.1 Change Set

**File 1**: Delete `apps/dashboard/src/composables/useCreatorWrite/useWriteValidation.ts` (139 lines).

**File 2**: `apps/dashboard/src/composables/useCreatorWrite.js` — remove the JSDoc line 9 reference to `useWriteValidation`.

Current line 9 (per exploration):
```
 * - useWriteValidation (写作验证：P0 复检/逻辑检查点击/偏离点击 — 类型骨架占位)
```

Replacement (remove the entire line):
```
[line removed]
```

The line above and below are documentation bullets describing composables in `useCreatorWrite`. Removing line 9 is clean; no other changes needed.

**File 3**: `apps/dashboard/src/composables/useCreatorWrite/index.ts` — remove the JSDoc line 6 reference to `useWriteValidation`.

Current line 6 (per exploration):
```
 * - useWriteValidation (写作验证：P0 复检/逻辑检查点击/章节跳转/偏离点击)
```

Replacement (remove the entire line):
```
[line removed]
```

### 4.2 Risk Analysis

- **Build risk**: None. The file's exports (`useWriteValidation`, `WriteValidationDeps`, `WriteValidationReturn`) have zero runtime consumers — verified via grep across `apps/dashboard/src` and `apps/dashboard/tests`.
- **Test risk**: None. No tests import the file or its exports.
- **Documentation risk**: Low. Two JSDoc lines are removed; surrounding context remains coherent.
- **Behavioral risk**: None. No code in the file is invoked at runtime.

### 4.3 Verification Strategy

After change:
1. `pnpm exec knip` reports `Unused files (0)`.
2. `pnpm exec vitest run` — 1545 tests pass.
3. `pnpm run build` — build succeeds.
4. `pnpm exec vue-tsc --noEmit` — 0 type errors.
5. `pnpm lint:all` — clean.
6. `grep -rn "useWriteValidation" apps/dashboard` should return only the JSDoc references in `useCreatorWrite.js:8` and `useCreatorWrite/index.ts:5` (since line numbers shift by 1 after removal). After edits, zero matches expected.
8. File deleted: `test ! -f apps/dashboard/src/composables/useCreatorWrite/useWriteValidation.ts`.

### 4.4 Rollback Plan

If anything regresses:
```bash
cd /home/ailearn/projects/LingWen && git revert HEAD --no-edit && git push origin master
```

Restores the file + 2 JSDoc lines in 1 revert commit. No data loss.

---

## 5. Files Touched

| File | Change |
|------|--------|
| `apps/dashboard/src/composables/useCreatorWrite/useWriteValidation.ts` | Delete |
| `apps/dashboard/src/composables/useCreatorWrite.js` | Remove 1 JSDoc line |
| `apps/dashboard/src/composables/useCreatorWrite/index.ts` | Remove 1 JSDoc line |
| **Total** | **3 file operations** |

---

## 6. Test Strategy

**No new tests.** Rationale:
- The file had zero runtime consumers; existing tests cannot assert behavior on absent code.
- 1545 tests still cover all production behavior unchanged by this phase.

---

## 7. Commit Strategy

Single atomic commit (3 file ops are tightly coupled — JSDoc refs are meaningless without the file deletion):
```
chore(cleanup): delete dead useWriteValidation.ts (Phase 103.1)

Phase 103.1 — resolve Phase 103 code-quality review LOW follow-up:

Phase 103 stripped re-exports of useWriteValidation from
useCreatorWrite/index.ts and composables/index.ts, leaving the source
file with zero runtime consumers. The only remaining refs were 2
JSDoc lines mentioning the function name.

- Delete apps/dashboard/src/composables/useCreatorWrite/useWriteValidation.ts
  (139 lines, exports useWriteValidation + WriteValidationDeps +
  WriteValidationReturn, zero runtime consumers)
- Remove JSDoc line 9 in useCreatorWrite.js (the
  'useWriteValidation (写作验证...)' doc bullet)
- Remove JSDoc line 6 in useCreatorWrite/index.ts (the
  'useWriteValidation (写作验证...)' doc bullet)

Effect: knip `Unused files` count drops 1 → 0.

测试基线不变: 1545 PASS, 0 type errors, 0 build errors.
```

---

## 8. Open Questions

None. Scope is unambiguous.

---

## 9. Success Criteria

- [ ] `useWriteValidation.ts` deleted
- [ ] 2 JSDoc reference lines removed (one in `useCreatorWrite.js`, one in `useCreatorWrite/index.ts`)
- [ ] `pnpm exec knip` reports `Unused files (0)`
- [ ] 1545 tests pass
- [ ] Build OK
- [ ] vue-tsc clean
- [ ] Lint clean
- [ ] Single atomic commit on master
- [ ] Pushed to origin/master

---

## 10. References

- Phase 103 spec: `docs/superpowers/specs/2026-08-24-phase103-unused-exports-audit-design.md`
- Phase 103 review: code-quality reviewer flagged "useWriteValidation.ts is now a fully dead file" (LOW)
- Phase 103 commit: `71d31257 refactor(cleanup): delete 47 unused exports + 3 dead files (Phase 103)`
- knip output baseline (post-Phase 103): `Unused files (1) src/composables/useCreatorWrite/useWriteValidation.ts`