# Phase 8.16 — Dashboard 时间维度切分 (7d/30d/all) 设计

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) 或 superpowers:executing-plans 实施本 spec.

## Context

**现状 (Phase 8.14 后 baseline 2232 passed 27 skipped)**:
- ✅ `CostTracker` (in-memory, 190L) + `CostTrackerDB` (167L) 暴露 `total_cost()` / `cost_by_scenario()` / `cost_by_tier()` 3 聚合方法 (Phase 8.5/8.7/8.13)
- ✅ `MasterControllerAdapter.get_active_workflow_status()` (protocols.py L222-301) 暴露 `cost_by_scenario` / `cost_by_tier` / `total_cost_usd` 给 dashboard
- ✅ Dashboard WebSocket (`useWorkflowSocket.js` module-level singleton, 153L) 实时推送 workflow 状态 (含 cost fields), App.vue + WorkflowsPage.vue 共享
- ✅ `WorkflowStatus.vue` (318L) + `SidebarCostBanner.vue` (241L) 显示 cost 视图
- ❌ **0 时间维度切分**: dashboard cost 视图永远 = "since process start" (in-memory cost_tracker), 用户无法快速看 7d/30d 累计
- ❌ **0 路由 / 状态**: useWorkflowSocket status 来自 WS push (无 query param 支持 per-client filter), 切时间窗口需新数据源
- ❌ **触发条件**: 主公需快速评估近期 cost (e.g. 昨天 opus 调太多, 7d 总累计是不是超预算), 现有 dashboard 只看累计

**目标**: 1 additive 时间窗口过滤层, 7d/30d/all 3 选项, 默认 "all" 保 backward compat. 仅 cost 视图 (cost_by_scenario/cost_by_tier/total_cost_usd) 受影响, budget 仍走累计 (per scope A 主公决策).

## Goals

- 3 option 时间窗口 selector (7d/30d/全部) 在 WorkflowStatus.vue + SidebarCostBanner.vue 顶部加 segmented control
- 新 HTTP `GET /api/workflows/active?time_window=7d|30d|all` (default "all") 透传 since param
- 新 `useCostWindow` composable (mirror `useWorkflowSocket` module-level singleton pattern) 拉 cost 数据
- 3 CostTracker + 3 CostTrackerDB aggregation methods 加 `since: Optional[datetime] = None` additive param
- 3 _extract_cost_by_* helper 加 `since` 透传 (protocols.py)
- 0 改 WebSocket protocol / useWorkflowSocket / 2 个 budget helper (`_extract_budget_status` / `_extract_budget_per_window` / `_extract_budget_by_tier`)
- 0 改 budget alarm / BudgetService / 12 SCENARIOS 路由
- 1 ceremonial Playwright spec (4 test cases, 跟 Phase 7.6/8.7/8.8/8.11/8.13/8.14/8.15 8 specs 同契约模式)

## Non-goals

- ❌ **budget 联动时间窗口** (per scope A): 预算层 0 改, `cost_budget_status` 等仍走 in-memory `total_cost()` 无 filter
- ❌ **WebSocket 协议加 time_window query**: WS 协议不感知 per-client filter, 改成本太高, 走 HTTP 拉 cost 单独 channel
- ❌ **URL state 持久化** (Vue Router): 留 followup, 当前 SPA 内部 state 够用
- ❌ **Pinia store 中心化**: 1 filter 用 store 过度, 跟 Phase 8.13 costMode / Phase 8.15 tierBudgets local pattern 不一致
- ❌ **cost_records 表加 timestamp 索引**: 360 章数据量级几百行, WHERE 全表扫 < 1ms, YAGNI
- ❌ **TIME_OPTIONS 抽 utilities.js**: 2 处用不抽 (3 strikes rule 留 Phase 8.20)
- ❌ **SidebarCostBanner `hasCost` 改 OR** (跟 Phase 8.14 WorkflowStatus 对称): 留 Phase 8.17 独立 followup, 7d/30d 切时 banner 可能 hidden 接受
- ❌ **历史 cost 时间序列图**: 需 time series 图表 (echarts 多 series), scope 爆炸, 留 Phase 8.23
- ❌ **自定义时间窗口 (date picker)**: YAGNI, 3 预设够用

## Design

### 1. Backend Layer

#### 1.1 `infra/ai_service/cost_tracker.py` (190L → ~210L, +20L)

3 aggregation method 加 `since: Optional[datetime] = None` 透传 param. 0 改 `record()` / `reset()` / `check_budget()` / `count_by_scenario()` / `total_tokens()`.

```python
# L136-138 total_cost 改
def total_cost(self, since: Optional[datetime] = None) -> float:
    """总成本 (USD), Phase 8.16 加 since filter.

    Args:
        since: UTC datetime, 只 sum 记录 ts >= since 的 cost. None = 全部 (default 保 backward compat).
    """
    records = self._records if since is None else [
        r for r in self._records if r.timestamp >= since
    ]
    return sum(r.cost_usd for r in records)

# L146-151 cost_by_scenario 改 (同 pattern)
# L153-158 cost_by_tier 改 (同 pattern)
```

**关键**:
- `since is None` 走旧 path (sum 全 records), 0 旧 caller 破
- `r.timestamp >= since` 含等于 (跟 Phase 8.12 `_is_within_window` UTC window pattern 一致, 边界 inclusive)
- `CostRecord.timestamp` 是 `datetime.now(timezone.utc)` aware, `since` 来自 server `_parse_time_window` 也用 `datetime.now(timezone.utc)`, 比较无时区歧义

#### 1.2 `infra/agent_system/cost_persistence.py` (167L → ~195L, +28L)

3 aggregation method 加 `since` 透传, 注入 `WHERE timestamp >= ?` SQL. 0 改 `init_db()` / `record()` / `records()`.

```python
# L137-144 total_cost 改
def total_cost(self, since: Optional[datetime] = None) -> float:
    """总成本 (USD), Phase 8.16 加 since filter (SQL WHERE timestamp >= ?)."""
    self.init_db()
    with self._connect() as conn:
        if since is None:
            row = conn.execute(
                "SELECT COALESCE(SUM(cost_usd), 0.0) as total FROM cost_records"
            ).fetchone()
        else:
            row = conn.execute(
                "SELECT COALESCE(SUM(cost_usd), 0.0) as total FROM cost_records WHERE timestamp >= ?",
                (since.isoformat(),),
            ).fetchone()
    return float(row["total"])

# L146-154 cost_by_scenario 改 (同 pattern, GROUP BY scenario + optional WHERE)
# L156-164 cost_by_tier 改 (同 pattern, GROUP BY tier + optional WHERE)
```

**SQL 注入安全**: parameterized query `?`, `since.isoformat()` 跟 record 写入的 `timestamp.isoformat()` (L113) 同 format. 0 字符串拼接.

**索引考虑**: cost_records 表 2 索引 (scenario, tier), 无 timestamp 索引. SQL `WHERE timestamp >= ?` 在 360 章 (几百行) 全表扫 < 1ms, 0 性能 regression. 1k+ 行再 `CREATE INDEX idx_cost_records_timestamp`.

#### 1.3 `dashboard/protocols.py` (567L → ~590L, +23L)

3 helper 加 `since` 透传 param. 0 改 3 budget helper (per scope A).

```python
# L304-316 _extract_total_cost 改
def _extract_total_cost(controller: Any, since: Optional[datetime] = None) -> float:
    """Phase 8.16: 加 since 透传 (additive)."""
    cost_tracker = getattr(controller, "cost_tracker", None)
    if cost_tracker is None:
        return 0.0
    try:
        return float(cost_tracker.total_cost(since=since))  # NEW kwarg
    except Exception as exc:
        logger.warning("cost_tracker.total_cost() failed: %s", exc)
        return 0.0

# L319-335 _extract_cost_by_scenario 改 (since 透传)
# L338-358 _extract_cost_by_tier 改 (since 透传)
```

`get_active_workflow_status` (L222) 加 `since: Optional[datetime] = None` kwarg, L293-300 改 3 call site (additive kwarg), 其它 cost_/budget_ 字段 0 改:

```python
def get_active_workflow_status(self, since: Optional[datetime] = None) -> dict[str, Any]:
    # ... existing logic 0 改 ...
    return {
        # ... 其它字段 0 改 ...
        "cost_by_scenario": _extract_cost_by_scenario(self._controller, since=since),  # Phase 8.16
        "cost_by_tier": _extract_cost_by_tier(self._controller, since=since),  # Phase 8.16
        "cost_budget_status": _extract_budget_status(self._controller),  # 0 改 (走 since=None)
        "budget_per_day": _extract_budget_per_window(self._controller, "day"),  # 0 改
        "budget_per_week": _extract_budget_per_window(self._controller, "week"),  # 0 改
        "budget_by_tier": _extract_budget_by_tier(self._controller),  # 0 改
        "total_cost_usd": _extract_total_cost(self._controller, since=since),  # Phase 8.16
    }
```

**3 budget helper 0 改**: `_extract_budget_status` / `_extract_budget_per_window` / `_extract_budget_by_tier` 内部仍调 `cost_tracker.total_cost()` (无 since kwarg, 走 default None = 累计). 预算严肃性保 (per scope A).

#### 1.4 `dashboard/app.py` (1015L → ~1050L, +35L)

新 helper `_parse_time_window` + 改 1 endpoint (1 call site 加 query param).

```python
# L1-20 import 区 additive: from datetime import datetime, timezone (已存在), timedelta
# (verify 已存在, 若缺则加)
from fastapi import Query  # 顶部 import, additive

# 新增 helper (after line 168, 在 _workflow_result_to_dict 之前)
_TIME_WINDOW_DAYS = {"7d": 7, "30d": 30}

def _parse_time_window(window: str) -> Optional[datetime]:
    """Phase 8.16: 翻译 time_window query param → since datetime (UTC).

    Args:
        window: "7d" | "30d" | "all" | 其他 (silent fallback to None)

    Returns:
        datetime (UTC, now-7d 或 now-30d) for "7d"/"30d"
        None for "all" or invalid (silent fallback, 防呆, 跟 Phase 8.13 silent degrade 模式一致)
    """
    if window in _TIME_WINDOW_DAYS:
        return datetime.now(timezone.utc) - timedelta(days=_TIME_WINDOW_DAYS[window])
    return None

# L713-720 GET /api/workflows/active 改
@app.get("/api/workflows/active", response_model=WorkflowStatusResponse)
def get_active_workflow(
    time_window: str = Query("all", description="7d|30d|all (Phase 8.16)"),
) -> WorkflowStatusResponse:
    if master_controller is None:
        return WorkflowStatusResponse(is_active=False, workflow_name=None)
    since = _parse_time_window(time_window)  # Phase 8.16
    return WorkflowStatusResponse(
        **master_controller.get_active_workflow_status(since=since)  # NEW kwarg
    )
```

**Invalid window**: `_parse_time_window` silent fallback to None (跟 Phase 8.13 `_extract_cost_by_*` silent degrade pattern). 0 抛 422, 0 改旧 caller 用错 URL 行为.

**run_workflow / resume_workflow 2 endpoint 0 改**: workflow 启动/恢复不需要时间窗口, 0 透传 since.

### 2. Frontend Layer

#### 2.1 新 composable `dashboard/frontend/src/composables/useCostWindow.js` (NEW, ~110L)

Mirror `useWorkflowSocket.js` module-level singleton pattern. 拉 cost data via HTTP (5s 轮询 + window change re-fetch).

```js
/**
 * useCostWindow.js — 时间窗口成本 composable (Phase 8.16)
 *
 * Mirror useWorkflowSocket.js module-level singleton pattern.
 * 拉 /api/workflows/active?time_window=X 拿 windowed cost, 跟 WS-pushed
 * status (含全量 cost) 共存但不冲突 — 本 composable 维护独立的 windowedCost,
 * 2 cost-displaying component (WorkflowStatus + SidebarCostBanner) 读 windowedCost
 * 覆盖 WS status.cost_* 字段.
 *
 * 用法:
 *   const { timeWindow, windowedCost, setTimeWindow } = useCostWindow();
 *   setTimeWindow('7d')  // → 自动 re-fetch
 *   <button @click="setTimeWindow('7d')" :class="{active: timeWindow === '7d'}">7天</button>
 *
 * 自动:
 * - 首个 mount → 立即 fetch + 启动 5s 轮询
 * - 最后一个 unmount → 停止轮询
 * - timeWindow 变 → 立即 re-fetch
 *
 * 状态:
 * - timeWindow: ref<'7d'|'30d'|'all'> 默认 'all' 保 backward compat
 * - windowedCost: ref<{cost_by_scenario, cost_by_tier, total_cost_usd}|null> 默认 null
 *   (null 时 component fallback 到 useWorkflowSocket status.cost_*)
 */

import { onBeforeUnmount, onMounted, ref, watch } from 'vue';

const VALID_WINDOWS = ['7d', '30d', 'all'];
const POLL_INTERVAL_MS = 5000;

// ---- Module-level singleton state ----
const timeWindow = ref('all');
const windowedCost = ref(null);
const loading = ref(false);
const lastError = ref(null);

let pollTimer = null;
let mountedCount = 0;

async function fetchCost() {
  if (timeWindow.value === 'all') {
    // "all" 不需要 fetch — component 读 WS status.cost_* (default path)
    windowedCost.value = null;
    return;
  }
  loading.value = true;
  lastError.value = null;
  try {
    const url = new URL('/api/workflows/active', window.location.origin);
    url.searchParams.set('time_window', timeWindow.value);
    const res = await fetch(url.toString());
    if (!res.ok) throw new Error(`HTTP ${res.status}`);
    const data = await res.json();
    windowedCost.value = {
      cost_by_scenario: data.cost_by_scenario || {},
      cost_by_tier: data.cost_by_tier || {},
      total_cost_usd: data.total_cost_usd || 0.0,
    };
  } catch (e) {
    lastError.value = e?.message || String(e);
    // Silent degrade: 保留旧 windowedCost (避免 race 闪空)
  } finally {
    loading.value = false;
  }
}

function startPolling() {
  stopPolling();
  pollTimer = setInterval(fetchCost, POLL_INTERVAL_MS);
}

function stopPolling() {
  if (pollTimer) {
    clearInterval(pollTimer);
    pollTimer = null;
  }
}

export function useCostWindow() {
  onMounted(() => {
    if (mountedCount === 0) {
      fetchCost();  // 立即 fetch
      startPolling();
    }
    mountedCount += 1;
    // timeWindow 变 → re-fetch (immediate, 不等下一个 poll tick)
    watch(timeWindow, () => fetchCost());
  });

  onBeforeUnmount(() => {
    mountedCount = Math.max(0, mountedCount - 1);
    if (mountedCount === 0) {
      stopPolling();
    }
  });

  return {
    timeWindow,
    windowedCost,
    loading,
    lastError,
    setTimeWindow: (w) => {
      if (VALID_WINDOWS.includes(w)) {
        timeWindow.value = w;
      }
    },
  };
}
```

**关键设计**:
- Module-level singleton (跟 `useWorkflowSocket.js` 同 pattern): App.vue + WorkflowsPage.vue 共享 `timeWindow` / `windowedCost`
- `timeWindow='all'` → `windowedCost=null` → component fallback 到 `useWorkflowSocket.status.cost_*` (旧 path 0 改)
- `timeWindow='7d'|'30d'` → HTTP fetch + 5s 轮询 → `windowedCost` 填充
- 切 window → 立即 re-fetch (不阻塞到下一个 tick)
- Silent degrade: fetch fail 保留旧 `windowedCost` (不闪空, UX 友好)
- `setTimeWindow` 接受 `'7d'|'30d'|'all'`, 其它值 silent ignore (跟 backend `_parse_time_window` 防御模式一致)

**0 改 `useWorkflowSocket.js`**: WS 仍推全量 status (含 all-time cost), 跟 windowedCost 共存.

**5s 轮询**: 跟 Phase 6.4 `WorkflowsPage` 旧 2s 轮询 (后来被 WS 替代) 节奏接近. 0 WebSocket throttle impact.

#### 2.2 `WorkflowStatus.vue` (318L → ~360L, +42L)

加 1 段 segmented control + 1 prop 接 windowedCost. 0 改 existing cost section / score chart / pending list.

```vue
<template>
  <div class="workflow-status pixel-card">
    <!-- ... existing status header, stats, paused, pending, score chart ... -->

    <!-- Phase 8.16 NEW: time window segmented control (在 cost-section 之前, 仅当 hasCost) -->
    <div v-if="hasCost" class="time-window-tabs" role="tablist" aria-label="成本时间窗口">
      <button
        v-for="opt in TIME_OPTIONS"
        :key="opt.value"
        :class="['time-window-tab', { active: timeWindow === opt.value }]"
        :data-testid="`time-window-${opt.value}`"
        role="tab"
        :aria-selected="timeWindow === opt.value"
        @click="setTimeWindow(opt.value)"
      >
        {{ opt.label }}
      </button>
    </div>

    <div v-if="hasCost" class="cost-section">
      <h4 class="section-title">💰 Token 成本 (累计 USD)</h4>
      <p class="cost-total-usd">
        总计: ${{ totalCostText }}
      </p>
      <CostBarChart
        :cost-by-scenario="displayCostByScenario"
        :cost-by-tier="displayCostByTier"
        v-model:mode="costMode"
      />
    </div>
  </div>
</template>

<script setup>
import { computed, ref } from 'vue';
import ScoreRadarChart from './ScoreRadarChart.vue';
import CostBarChart from './CostBarChart.vue';
import { useCostWindow } from '../composables/useCostWindow.js';  // Phase 8.16

const props = defineProps({
  status: { type: Object, required: true },
});

const emit = defineEmits(['resume']);

// Phase 8.16: time window composable
const { timeWindow, windowedCost, setTimeWindow } = useCostWindow();

// 3 window options (跟 SidebarCostBanner 1:1 mirror, 2 处不抽 utilities.js YAGNI)
const TIME_OPTIONS = [
  { value: '7d', label: '7天' },
  { value: '30d', label: '30天' },
  { value: 'all', label: '全部' },
];

// Phase 8.16: cost display 优先用 windowedCost, fallback 到 status (WS 全量)
const displayCostByScenario = computed(() =>
  windowedCost.value?.cost_by_scenario ?? props.status?.cost_by_scenario ?? {}
);
const displayCostByTier = computed(() =>
  windowedCost.value?.cost_by_tier ?? props.status?.cost_by_tier ?? {}
);
const displayTotalCost = computed(() =>
  windowedCost.value?.total_cost_usd ?? props.status?.total_cost_usd ?? 0.0
);

// existing hasCost 改: 用 display* 走 windowedCost 路径
const hasCost = computed(() => {
  const scn = displayCostByScenario.value;
  const tier = displayCostByTier.value;
  return (Object.keys(scn).length > 0 && Object.values(scn).some((v) => v > 0))
    || (Object.keys(tier).length > 0 && Object.values(tier).some((v) => v > 0));
});

const totalCostText = computed(() => Number(displayTotalCost.value ?? 0).toFixed(4));

// ... existing onResume / costMode / statusLabel / statusClass 0 改 ...
</script>

<style scoped>
/* Phase 8.16 NEW: .time-window-tabs 跟 .time-window-tab (跟 sidebar 同 pattern) */
.time-window-tabs {
  display: flex;
  gap: var(--space-xs);
  margin-top: var(--space-md);
}
.time-window-tab {
  font-size: 8px;
  font-family: 'Press Start 2P', monospace;
  padding: 4px 8px;
  background: var(--bg-primary);
  border: 2px solid var(--border-color);
  cursor: pointer;
}
.time-window-tab:hover { transform: translate(-1px, -1px); }
.time-window-tab.active {
  background: var(--color-accent);
  color: var(--bg-primary);
}
</style>
```

**关键**:
- `display*` computed: `windowedCost` 优先 (7d/30d 模式), fallback 到 `status` (all 模式 = WS 全量)
- `hasCost` 改用 `display*` (跟 Phase 8.14 OR 守卫 pattern 兼容)
- `data-testid` 跟 1 ceremonial spec 对应: `time-window-7d` / `time-window-30d` / `time-window-all`
- `v-model:mode="costMode"` Phase 8.13 旧 path 0 改

#### 2.3 `SidebarCostBanner.vue` (241L → ~285L, +44L)

加同样 segmented control (顶部突出). 0 改 existing rows (total / activeBudget / 3 tier rows).

```vue
<template>
  <div v-if="hasCost" class="sidebar-cost-banner">
    <!-- Phase 8.16 NEW: 顶部 segmented control (在 total 之前) -->
    <div class="time-window-tabs" role="tablist" aria-label="成本时间窗口">
      <button
        v-for="opt in TIME_OPTIONS"
        :key="opt.value"
        :class="['time-window-tab', { active: timeWindow === opt.value }]"
        :data-testid="`sidebar-time-window-${opt.value}`"
        role="tab"
        :aria-selected="timeWindow === opt.value"
        @click="setTimeWindow(opt.value)"
      >
        {{ opt.label }}
      </button>
    </div>
    <!-- ... existing total cost + activeBudget + tier rows 0 改 ... -->
  </div>
</template>

<script setup>
import { computed } from 'vue';
import { useCostWindow } from '../composables/useCostWindow.js';  // Phase 8.16

const props = defineProps({
  status: { type: Object, required: true },
});

// Phase 8.16: time window composable (跟 WorkflowStatus 共享)
const { timeWindow, windowedCost, setTimeWindow } = useCostWindow();

const TIME_OPTIONS = [
  { value: '7d', label: '7天' },
  { value: '30d', label: '30天' },
  { value: 'all', label: '全部' },
];

// display* 模式同 WorkflowStatus
const displayCostByScenario = computed(() =>
  windowedCost.value?.cost_by_scenario ?? props.status?.cost_by_scenario ?? {}
);
const displayTotalCost = computed(() =>
  windowedCost.value?.total_cost_usd ?? props.status?.total_cost_usd ?? 0.0
);

// hasCost 用 displayCostByScenario (Banner 维持 Phase 8.15 旧版仅 scenario check, 不改 OR)
const hasCost = computed(() => {
  const data = displayCostByScenario.value;
  return Object.keys(data).length > 0 && Object.values(data).some((v) => v > 0);
});

const totalUsd = computed(() => Number(displayTotalCost.value ?? 0));
const totalText = computed(() => totalUsd.value.toFixed(4));

// ... existing allBudgets / activeBudget / tierBudgets 0 改 (走 status.budget_* 旧 path) ...
</script>

<style scoped>
/* .time-window-tabs / .time-window-tab 同 WorkflowStatus, scoped CSS 不冲突 (独立 component) */
</style>
```

**关键**:
- TIME_OPTIONS 重复 (跟 WorkflowStatus 1:1 mirror, YAGNI 不抽 utilities.js)
- `hasCost` 维持 Phase 8.15 旧版 (仅 scenario, 不 OR tier) — 7d/30d 切时可能 hidden 接受 (scope A 锁, Phase 8.17 独立 followup)
- `activeBudget` / `tierBudgets` 仍走 `props.status.budget_*` (不走 windowedCost, per scope A 预算不联动)
- 0 改 existing row template / CSS (除顶部 tabs)

#### 2.4 1 ceremonial e2e spec (NEW, ~90L, 跟 Phase 7.6/8.7/8.8/8.11/8.13/8.14/8.15 8 specs 同契约模式)

`dashboard/frontend/tests/e2e/time-window-tabs.spec.js` 4 test cases:

```js
// Phase 8.16: 时间窗口 segmented control ceremonial e2e
// 4 test cases: WorkflowStatus 3 tabs visible / SidebarCostBanner 3 tabs visible /
// click 7d emits / active state class moves
//
// 跟 Phase 7.6/8.7/8.8/8.11/8.13/8.14/8.15 8 specs 同模式:
// Playwright runner 未装, 作为契约文档存在 (header note).
// Mock 走 page.route('**/api/workflows/active**') 模式 (接受 query string).

import { test, expect } from '@playwright/test'

const BASE_URL = process.env.BASE_URL || 'http://localhost:5173'

const ACTIVE_WORKFLOW = (cost_by_scenario, cost_by_tier) => ({
  workflow_name: 'novel_writing',
  is_active: true,
  paused: false,
  completed: 5,
  failed: 0,
  node_count: 7,
  steps: 12,
  cost_by_scenario,
  cost_by_tier,
  total_cost_usd: 0.15,
})

test.describe('Phase 8.16: time window segmented control', () => {
  test('shows_3_tabs_in_workflow_status', async ({ page }) => {
    await page.route('**/api/workflows/active**', (route) => {
      route.fulfill({ status: 200, contentType: 'application/json',
        body: JSON.stringify(ACTIVE_WORKFLOW({ chapter_writing: 0.10 }, { sonnet: 0.05 })) })
    })
    await page.goto(`${BASE_URL}/workflows`)
    await expect(page.locator('[data-testid="time-window-7d"]')).toBeVisible()
    await expect(page.locator('[data-testid="time-window-30d"]')).toBeVisible()
    await expect(page.locator('[data-testid="time-window-all"]')).toBeVisible()
    // default 'all' active
    await expect(page.locator('[data-testid="time-window-all"]')).toHaveClass(/active/)
  })

  test('shows_3_tabs_in_sidebar_banner', async ({ page }) => {
    await page.route('**/api/workflows/active**', (route) => {
      route.fulfill({ status: 200, contentType: 'application/json',
        body: JSON.stringify(ACTIVE_WORKFLOW({ chapter_writing: 0.10 }, { sonnet: 0.05 })) })
    })
    await page.goto(`${BASE_URL}/overview`)  // SidebarCostBanner always visible
    await expect(page.locator('[data-testid="sidebar-time-window-7d"]')).toBeVisible()
    await expect(page.locator('[data-testid="sidebar-time-window-30d"]')).toBeVisible()
    await expect(page.locator('[data-testid="sidebar-time-window-all"]')).toBeVisible()
  })

  test('clicking_7d_fetches_with_time_window_param', async ({ page }) => {
    let lastRequestUrl = null
    await page.route('**/api/workflows/active**', (route) => {
      lastRequestUrl = route.request().url()
      route.fulfill({ status: 200, contentType: 'application/json',
        body: JSON.stringify(ACTIVE_WORKFLOW({ chapter_writing: 0.05 }, { haiku: 0.02 })) })
    })
    await page.goto(`${BASE_URL}/workflows`)
    await page.locator('[data-testid="time-window-7d"]').click()
    // Wait for fetch
    await expect.poll(() => lastRequestUrl, { timeout: 2000 }).toContain('time_window=7d')
  })

  test('active_state_class_moves_on_click', async ({ page }) => {
    await page.route('**/api/workflows/active**', (route) => {
      route.fulfill({ status: 200, contentType: 'application/json',
        body: JSON.stringify(ACTIVE_WORKFLOW({ chapter_writing: 0.10 }, { sonnet: 0.05 })) })
    })
    await page.goto(`${BASE_URL}/workflows`)
    await page.locator('[data-testid="time-window-30d"]').click()
    await expect(page.locator('[data-testid="time-window-30d"]')).toHaveClass(/active/)
    await expect(page.locator('[data-testid="time-window-all"]')).not.toHaveClass(/active/)
  })
})
```

### 3. Risks + Mitigations

| 风险 | 缓解 |
|------|------|
| 旧 2232 测试破 (since 必传) | `since: Optional[datetime] = None` default None 兜底, 3+3 method 旧 caller 0 改, 走 `records if since is None` 走旧 path. |
| Budget alarm 窗口后 cost 误报 / 漏报 | 预算层**不**接 since (per scope A), 3 budget helper 0 改, `cost_tracker.total_cost()` 走 default None = 累计 cost. |
| Dashboard 旧 caller `?time_window=invalid` 返 422 | `_parse_time_window` silent fallback to None (跟 Phase 8.13 silent degrade pattern), 不抛. |
| WS push 全量 cost 跟 windowedCost 冲突 (WS 推未过滤覆盖显示) | `display*` computed pattern: `windowedCost` 优先 (7d/30d), fallback `status` (all 模式 = WS 全量). WS push 不影响 7d/30d 显示. |
| 5s 轮询 + WS 推送 双重 fetch 抖动 | WS 推 workflow events (event-driven, 高频但稀疏), HTTP 5s 轮询 cost (time-driven). 2 channel 各司其职, 不冲突. |
| App.vue + WorkflowsPage.vue 2 parent state 漂移 | useCostWindow module-level singleton (mirror useWorkflowSocket pattern), 2 component 共享同一 timeWindow / windowedCost. |
| `display*` computed 跟 Phase 8.14 hasCost OR 守卫冲突 | hasCost 改用 `display*`, OR 守卫保留 (任一 mode 有正值即 visible), 0 破 Phase 8.14 修复. |
| SidebarCostBanner hasCost 切 7d/30d 后可能 hidden | 接受 (scope A: 仅改 time window, hasCost gate fix 留 Phase 8.17). 1 old behavior 注释. |
| `CostRecord.timestamp` 比较时区不一致 | 都是 UTC aware datetime, 比较无歧义. 跟 Phase 8.12 `_is_within_window` UTC pattern. |
| SQL `WHERE timestamp >= ?` 字符串比较 vs datetime | ISO 8601 字典序 = 时间序, 0 加 datetime 转换, 0 性能 regression. |
| cost_records 无 timestamp 索引 | 360 章几百行全表扫 < 1ms, 0 加索引 (YAGNI). |
| `get_active_workflow_status` 旧 caller 透传 since=None | 0 改 2 caller (run/resume workflow), default None = 旧 path. |
| TIME_OPTIONS 重复 2 处 | 2 处不抽 utilities.js (3 strikes rule 留 Phase 8.20). |
| `useCostWindow` mountedCount watch 误启 (跟 useWorkflowSocket 同 pattern) | mirror 同 lifecycle: `if (mountedCount === 0) startPolling()`, `if (mountedCount === 0) stopPolling()`. 0 重复注册. |
| fetch fail 闪空 | Silent degrade: 保留旧 `windowedCost`, `lastError` set, UI 显示 lastError (留 followup). 0 UX 突然空白. |
| `timeWindow` watch 在 onMounted 内挂 (每次 mount 都挂) | mount 时挂 1 次 (Vue 3 watch 不会在 component remount 时丢失 — 因为 ref 是 module-level), 0 重复挂. |
| API key 泄漏 | 0 provider 改, 0 .env 改, 0 ANTHROPIC_API_KEY 引用. 跟 Phase 8.13/8.14/8.15 一致. |

### 4. Verification

```bash
# 1. pytest baseline + new tests
pytest -q 2>&1 | tail -3
# 期望: 2232 + 11 new = 2243 passed, 0 failed, 27 skipped
# (3 CostTracker + 3 CostTrackerDB + 5 endpoint integration: 7d/30d/all/default/invalid silent fallback)

pytest infra/ai_service/test_cost_tracker.py infra/agent_system/test_cost_persistence.py -v
# 期望: 6 new + 旧 tests all passed (3 + 3)

pytest tests/dashboard/ -k "test_active_workflow_time_window or test_workflow_status_time_window" -v
# 期望: 5 new passed (7d/30d/all/default/invalid)

# 2. ruff 0
ruff check infra/ai_service/ infra/agent_system/ dashboard/ 2>&1 | tail -3

# 3. vite build
pnpm build 2>&1 | tail -5
# 期望: 0 error

# 4. ceremonial e2e sanity
# 1 spec time-window-tabs.spec.js (NEW ~90L, 4 test cases)
# header note 标 "Playwright runner 未装" 跟 7+1 specs 同 convention

# 5. git 推送
git status  # 期望 6 改 + 2 new = 8 文件
git log --oneline -1  # feat(dashboard): phase 8.16 — time window segmented control
git push origin master

# 6. grep 验证
grep -n "since=" infra/ai_service/cost_tracker.py | head -10  # 3 method
grep -n "since=" infra/agent_system/cost_persistence.py | head -10  # 3 method
grep -n "since=" dashboard/protocols.py | head -5  # 3 helper + 1 adapter
grep -n "time_window" dashboard/app.py | head -5  # 1 endpoint + 1 helper
```

### 5. Definition of Done

- [ ] `docs/superpowers/specs/2026-06-07-phase8.16-time-window-design.md` 创建 (~450L, 本文)
- [ ] `docs/superpowers/plans/2026-06-07-phase8.16-time-window.md` 创建 (~600L)
- [ ] `infra/ai_service/cost_tracker.py` 3 method 加 `since: Optional[datetime] = None`
- [ ] `infra/agent_system/cost_persistence.py` 3 method 加 `since` + SQL WHERE 透传
- [ ] `dashboard/protocols.py` 3 _extract_cost helper 加 `since` 透传 + `get_active_workflow_status` 加 since kwarg
- [ ] `dashboard/app.py` 加 `_parse_time_window` helper + `GET /api/workflows/active` 加 `time_window` query param
- [ ] `dashboard/frontend/src/composables/useCostWindow.js` 创建 (NEW ~110L, mirror useWorkflowSocket pattern)
- [ ] `dashboard/frontend/src/components/WorkflowStatus.vue` 加 segmented control + `display*` computed
- [ ] `dashboard/frontend/src/components/SidebarCostBanner.vue` 加 segmented control + `display*` computed
- [ ] `dashboard/frontend/tests/e2e/time-window-tabs.spec.js` 创建 (NEW ~90L, 4 test cases)
- [ ] 11 new pytest tests passed (3 CostTracker + 3 CostTrackerDB + 5 endpoint integration)
- [ ] 1 feat commit 推送 origin/master
- [ ] 1 chore commit (phases.md entry)
- [ ] pytest 2243 passed, 0 failed, 27 skipped (2232 + 11 new)
- [ ] ruff 0
- [ ] pnpm build 0 error
- [ ] 0 旧测试代码改动 (2232 baseline 不变)
- [ ] 0 production code 行为破 (additive `since=None` / `time_window="all"` default 走旧 path)
- [ ] 0 budget 逻辑改 (per scope A, `_extract_budget_*` 0 改)
- [ ] 0 WebSocket protocol 改 (per design, WS 仍推全量)
- [ ] 0 useWorkflowSocket.js 改 (0 改, 新增 useCostWindow.js 平行)
- [ ] 0 historical spec/plan docs `:NNN` 改
- [ ] 0 CLAUDE.md / pyproject.toml / pytest.ini 改
- [ ] 0 .env / 0 API key 泄漏
- [ ] 0 `.state/*.db` 提交 (gitignored)
- [ ] 0 force-push, 0 --no-verify, 0 Co-Authored-By
- [ ] MEMORY.md 暂不动 (留 v9.14 / 2232 baseline, 1 行 phase entry 加 phases.md 即可)

### 6. Future work (out of scope)

- **Phase 8.17**: SidebarCostBanner `hasCost` OR fix (跟 Phase 8.14 WorkflowStatus 对称)
- **Phase 8.18**: 时间窗口 + budget alarm 联动 (per scope B 之前讨论, 7d 视图 + per-run 预算)
- **Phase 8.19**: URL state 持久化 (`?time_window=7d` 走 Vue Router)
- **Phase 8.20**: TIME_OPTIONS 抽 `utils/costWindow.js` (3 strikes 触发)
- **Phase 8.21**: cost_records timestamp 索引 (1k+ 行时)
- **Phase 8.22**: WebSocket 断线 indicator (Phase 8.15 followup) / 历史 cost 时间序列图
- **Phase 9**: 跨卷涟漪 (独立大方向)
- **MEMORY.md 拆 21+ entries 到 phases.md topic** (本 entry 后变 21, 必拆 followup)
