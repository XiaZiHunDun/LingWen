# Phase 15.0 T2 Tasks — SQLite Consolidation

> Spec: `2026-07-11-phase15.0-t2-sqlite-consolidation-design.md`
> Plan: `2026-07-11-phase15.0-t2-sqlite-consolidation-plan.md`
> **9 tasks, 9 commits** (T2.0 spec+plan+tasks 已是前置 doc commits)。

| # | Task | 状态 | Commit |
|---|------|------|--------|
| T2.0 | spec + plan + tasks 三文档落地 | ✅ 已 commit (本批) | `docs(spec/plan/tasks): phase 15.0 T2 — SQLite consolidation` |
| T2.1 | `infra/persistence/registry.py` skeleton + 14 tests | pending | `feat(persistence): phase 15.0 T2.1 — registry skeleton + 14 tests` |
| T2.2 | `infra/persistence/connection.py` get_connection 工厂 + 3 tests | pending | `feat(persistence): phase 15.0 T2.2 — get_connection factory + 3 tests` |
| T2.3 | `infra/persistence/schemas.py` DDL 注册 + 6 tests | pending | `feat(persistence): phase 15.0 T2.3 — schema DDL registry + 6 tests` |
| T2.4 | `infra/persistence/paths.py` 6 个路径常量 + 3 tests | pending | `feat(persistence): phase 15.0 T2.4 — DB path constants + 3 tests` |
| T2.5 | `infra/persistence/bootstrap.py` register_all + 6 storage 绑定 | pending | `feat(persistence): phase 15.0 T2.5 — bootstrap register_all + 6 storage bindings` |
| T2.6 | dashboard `_default_storage` 走 registry | pending | `refactor(dashboard): phase 15.0 T2.6 — _default_storage goes through persistence registry` |
| T2.7 | 删 dashboard/helpers/reading_power_db.py 截断副本 | pending | `refactor(dashboard): phase 15.0 T2.7 — remove dashboard/helpers/reading_power_db truncated duplicate` |
| T2.8 | 旧 backend DeprecationWarning | pending | `chore(infra): phase 15.0 T2.8 — deprecation warnings on legacy backends` |
| T2.9 | ruff + pytest + 验证 | pending | `chore(infra): phase 15.0 T2.9 — ruff pass + final verification` |

## Acceptance

- baseline 3274 + 359 dashboard + ≥ 26 persistence = ≥ 3659 passed
- ruff 0 issue
- 0 改 baseline 0 改 production behavior 0 改 .env/.state
- 0 真 LLM in tests

## 后续

T3 → master_controller 拆分 / T4 → CLI 收敛 / T5 → HTTPException / T6 → 全 phase 验证 + push + HANDOFF