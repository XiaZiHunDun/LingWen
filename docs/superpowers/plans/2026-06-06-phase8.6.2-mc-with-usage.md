# Phase 8.6.2 — MC `*_with_usage` + AgentComputeFn Real Usage Wiring Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** MasterController 暴露 5 parallel `*_with_usage` variants + 4 handlers in got_bridge.py 返 `(output, usage)` tuple + AgentComputeFn.__call__ 检测 tuple 拆 usage 用真实数据喂 `cost_tracker.record()`, 替换 Phase 8.5 `len()//4` 估算.

**Architecture:** 1 MasterController file (5 public variants + 5 `_impl_*(record_usage)` 私有方法 share LLM path) + 1 got_bridge.py (HandlerFn type alias Union + 4 handlers 调 MC variants 返 tuple + AgentComputeFn isinstance tuple detection) + 1 e2e helper (`record_usage` kwarg) + 1 _StubMaster 加 5 methods. Backward compat 双路径: 旧 method 走 dict-return 估算; 新 variant 走 tuple-return 真实.

**Tech Stack:** Python 3.13 / pytest / 复用 Phase 8.6.1 `_UsageRecordingProvider` + `_RecordingRouter` / 复用 Phase 8.6.1 agent `*_with_usage` / 复用 Phase 8.5 `cost_tracker.record()` / 复用 Phase 8.5 `SCENARIO_TIER_MAP`.

---

## File Map

| 类别 | 路径 | 操作 | 行数 |
|------|------|------|------|
| 改 wiring | `infra/agent_system/master_controller.py:520-770` | 加 5 MC `*_with_usage` variants + 5 `_impl_*` 私有方法 | +150L |
| 改 wiring | `infra/agent_system/got_bridge.py:43-266` | HandlerFn type alias Union + 4 handlers return tuple + AgentComputeFn tuple 检测 | +30L |
| 同步 stub | `tests/agent_system/_e2e_helpers.py:make_master_with_router` | 加 `record_usage: bool = False` kwarg, 透传 controller 是否走 *_with_usage | +10L |
| 同步 stub | `tests/agent_system/test_got_bridge.py:_StubMaster` | 加 5 `*_with_usage` methods (return max len fallback) | +30L |
| 新测试 | `tests/agent_system/test_master_controller_with_usage.py` (NEW) | 创建 | ~280L |
| 同步 stub | `tests/agent_system/test_got_bridge.py:TestCostTrackerWiring` | 2 new tests (tuple return + real usage wired) | +50L |
| 同步 e2e | `tests/agent_system/test_master_controller_stub_router_e2e.py` | 1 new e2e test (real usage in production path) | +15L |

**估计**: 1 spec doc + 1 plan doc + 7 改文件 + 1 feat commit. 总改 10 文件, +665L 新, 0 production code 行为破坏.

---

## Task 1: 写 failing tests (RED) — test_master_controller_with_usage.py

### 1.1 Step 1: Read existing fixtures to align with project patterns

Read:
- `tests/agent_system/_e2e_helpers.py:1-50` (make_master_with_router signature)
- `tests/agent_system/test_agent_with_usage.py:1-50` (_UsageRecordingProvider + _RecordingRouter)
- `infra/agent_system/master_controller.py:533-712` (write_chapter/audit_chapter/3 polish method signatures)

### 1.2 Step 2: Write 8 tests in new file `tests/agent_system/test_master_controller_with_usage.py`

```python
# tests/agent_system/test_master_controller_with_usage.py (NEW, ~280L)
"""Phase 8.6.2: MasterController 5 *_with_usage variants + dual-path backward compat.

复用 Phase 8.6.1 _UsageRecordingProvider + _RecordingRouter stubs
(在 tests/agent_system/test_agent_with_usage.py 定义).

测试目标:
- 5 variants 返 (result, usage) tuple, usage 含 input_tokens/output_tokens
- 旧 method 签名 0 改, 返 dict / str (单 path)
- audit 异常 → 旧 try/except 兜底, usage 0
- chat_with_usage sanity 防回归
"""
import pytest

from infra.agent_system.master_controller import MasterController
from tests.agent_system._e2e_helpers import make_master_with_router


# Reuse Phase 8.6.1 stubs (tests/agent_system/test_agent_with_usage.py)
from tests.agent_system.test_agent_with_usage import (
    _UsageRecordingProvider,
    _RecordingRouter,
)


class TestMasterControllerWithUsage:
    """Phase 8.6.2: 5 MC *_with_usage variants + backward compat 双路径."""

    def test_write_chapter_with_usage_returns_tuple(self, tmp_path):
        """Variant 1: write_chapter_with_usage 返 (result, usage) tuple."""
        master = make_master_with_router(
            state_dir=tmp_path, record_usage=True,
        )
        result, usage = master.write_chapter_with_usage(
            chapter_num=1, outline={}, characters=[],
            memory_context={}, style_guide={}, use_llm=True,
        )
        assert isinstance(result, dict)
        assert isinstance(usage, dict)
        assert "input_tokens" in usage
        assert "output_tokens" in usage
        assert "content" in result

    def test_audit_chapter_with_usage_returns_tuple(self, tmp_path):
        """Variant 2: audit_chapter_with_usage 返 (result, usage) tuple."""
        master = make_master_with_router(
            state_dir=tmp_path, record_usage=True,
        )
        result, usage = master.audit_chapter_with_usage(
            chapter_num=1, content="test content",
            characters=[], timeline=[], use_llm=True,
        )
        assert isinstance(result, dict)
        assert isinstance(usage, dict)
        assert "input_tokens" in usage
        assert "output_tokens" in usage

    def test_polish_chapter_with_usage_sums_both_llms(self, tmp_path):
        """Variant 3: polish_chapter_with_usage = 2 LLM (dialogue + pacing) sum."""
        master = make_master_with_router(
            state_dir=tmp_path, record_usage=True,
        )
        result, usage = master.polish_chapter_with_usage(
            chapter_num=1, content="text " * 50, style_guide=None,
        )
        # 2 LLM sum: each 100/50, total 200/100
        assert isinstance(result, str)
        assert usage["input_tokens"] == 200
        assert usage["output_tokens"] == 100

    def test_polish_emotional_pacing_with_usage_sums_sub_llms(self, tmp_path):
        """Variant 4: polish_emotional_pacing_with_usage = 2 LLM (dialogue + pacing) sum."""
        master = make_master_with_router(
            state_dir=tmp_path, record_usage=True,
        )
        result, usage = master.polish_emotional_pacing_with_usage(
            chapter_num=1, content="text " * 50,
        )
        assert isinstance(result, str)
        assert usage["input_tokens"] == 200
        assert usage["output_tokens"] == 100

    def test_polish_ai_trace_removal_with_usage_includes_llm(self, tmp_path):
        """Variant 5: polish_ai_trace_removal_with_usage = 1 LLM + 1 rule, usage only LLM."""
        master = make_master_with_router(
            state_dir=tmp_path, record_usage=True,
        )
        result, usage = master.polish_ai_trace_removal_with_usage(
            chapter_num=1, content="text " * 50,
        )
        assert isinstance(result, str)
        # 1 LLM call: 100/50 (rule remove_ai_gloss 0 token)
        assert usage["input_tokens"] == 100
        assert usage["output_tokens"] == 50

    def test_old_methods_still_return_dict_only(self, tmp_path):
        """Backward compat: 旧 5 method 走 _impl(record_usage=False) 返 dict / str (单 path)."""
        master = make_master_with_router(state_dir=tmp_path)
        # write_chapter / audit_chapter 返 dict
        write_result = master.write_chapter(
            chapter_num=1, outline={}, characters=[],
            memory_context={}, style_guide={}, use_llm=True,
        )
        assert isinstance(write_result, dict)
        # 3 polish methods 返 str
        assert isinstance(master.polish_chapter("text"), str)
        assert isinstance(master.polish_emotional_pacing("text"), str)
        assert isinstance(master.polish_ai_trace_removal("text"), str)

    def test_audit_with_usage_propagates_exception(self, tmp_path, monkeypatch):
        """韧性契约: audit LLM 抛错 → 旧 try/except 兜底, variant usage 0."""
        from infra.agent_system.agents.auditor.tools import AuditorTools
        master = make_master_with_router(state_dir=tmp_path, record_usage=True)
        # Stub audit_chapter_with_usage 抛错
        def raise_exc(**kw):
            raise RuntimeError("LLM 502")
        monkeypatch.setattr(master.auditor, "audit_chapter_with_usage", raise_exc)
        result, usage = master.audit_chapter_with_usage(
            chapter_num=1, content="text", characters=[], timeline=[],
        )
        # 旧 try/except 兜底: result 是 _error dict
        assert isinstance(result, dict)
        assert "_error" in result
        # usage 是 0 (LLM 失败不录)
        assert usage == {"input_tokens": 0, "output_tokens": 0}

    def test_chat_with_usage_unaffected(self):
        """Phase 8.6 已就位 sanity — 防回归."""
        # 不依赖 master, 直接验 chat_with_usage 接口签名
        # (MasterController.chat_with_usage 接受 scenario + prompt)
        import inspect
        from infra.agent_system.master_controller import MasterController
        sig = inspect.signature(MasterController.chat_with_usage)
        assert "scenario" in sig.parameters
        assert "prompt" in sig.parameters
        assert sig.return_annotation == tuple[str, dict[str, int]]
```

### 1.3 Step 3: Run tests, verify RED

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pytest tests/agent_system/test_master_controller_with_usage.py -v 2>&1 | tail -20
# 期望: 8 failed (AttributeError: MasterController 无 write_chapter_with_usage)
```

**Expected RED output**:
```
AttributeError: 'MasterController' object has no attribute 'write_chapter_with_usage'
... 8 failed
```

---

## Task 2: Implement 5 MC variants (GREEN)

### 2.1 Step 1: Modify `master_controller.py:533-588` (write_chapter region)

Find `def write_chapter(` (line 533). Replace with:

```python
    def write_chapter(
        self,
        chapter_num: int,
        outline: Dict,
        characters: List[Dict],
        memory_context: Dict,
        style_guide: Dict,
        use_llm: bool = True
    ) -> Dict:
        """写章节流程（委托给content_writer）

        Phase 8.6.2: 内部调 _impl(record_usage=False) — 旧契约返 dict.
        真实 usage variant 见 write_chapter_with_usage.
        """
        return self._impl_write_chapter(
            chapter_num, outline, characters, memory_context, style_guide, use_llm,
            record_usage=False,
        )

    def write_chapter_with_usage(
        self,
        chapter_num: int,
        outline: Dict,
        characters: List[Dict],
        memory_context: Dict,
        style_guide: Dict,
        use_llm: bool = True
    ) -> tuple[Dict, Dict[str, int]]:
        """Phase 8.6.2: 写章节 variant — 真实 usage.

        调 self.content_writer.generate_chapter_with_usage() 拿真实 token,
        返 (result_dict, usage_dict). 旧契约 (write_chapter) 0 改.
        """
        return self._impl_write_chapter(
            chapter_num, outline, characters, memory_context, style_guide, use_llm,
            record_usage=True,
        )

    def _impl_write_chapter(
        self,
        chapter_num: int,
        outline: Dict,
        characters: List[Dict],
        memory_context: Dict,
        style_guide: Dict,
        use_llm: bool,
        record_usage: bool,
    ):
        # 获取章节大纲
        chapter_outline = self.outline_master.schema.get_chapter_outline(outline, chapter_num)

        # 构建上下文
        context = self.context_builder.build_writing_context(
            chapter_outline=chapter_outline,
            characters=characters,
            memory_context=memory_context,
            relationship_network=self.relationship_tracker.get_network(),
            style_guide=style_guide
        )

        # 获取写作建议
        suggestions = self.writing_suggestion.generate_suggestions(
            self.relationship_tracker, chapter_num
        )

        if use_llm:
            if record_usage:
                # Phase 8.6.2: 真实 usage path
                result, usage = self.content_writer.generate_chapter_with_usage(
                    chapter_num, context,
                )
                return {
                    "content": result["content"],
                    "word_count": result.get("word_count", len(result["content"])),
                    "suggestions": suggestions,
                    "context": context,
                }, usage
            # 旧契约: dict-only
            result = self.content_writer.generate_chapter(chapter_num, context)
            return {
                "content": result["content"],
                "word_count": result.get("word_count", len(result["content"])),
                "suggestions": suggestions,
                "context": context,
            }
        else:
            # 仅返回 prompt (调试 path, 无 LLM, 无 usage)
            prompt = self.content_writer.build_writing_prompt(context)
            if record_usage:
                return {"prompt": prompt, "suggestions": suggestions, "context": context}, {
                    "input_tokens": 0, "output_tokens": 0,
                }
            return {
                "prompt": prompt,
                "suggestions": suggestions,
                "context": context,
            }
```

### 2.2 Step 2: Modify `master_controller.py:590-640` (audit_chapter region)

Replace `def audit_chapter(` block with:

```python
    def audit_chapter(
        self,
        chapter_num: int,
        content: str,
        characters: List[Dict],
        timeline: List[Dict],
        use_llm: bool = True
    ) -> Dict:
        """审核章节（委托给auditor）

        Phase 8.6.2: 内部调 _impl(record_usage=False) — 旧契约返 dict.
        真实 usage variant 见 audit_chapter_with_usage.
        """
        return self._impl_audit_chapter(
            chapter_num, content, characters, timeline, use_llm, record_usage=False,
        )

    def audit_chapter_with_usage(
        self,
        chapter_num: int,
        content: str,
        characters: List[Dict],
        timeline: List[Dict],
        use_llm: bool = True
    ) -> tuple[Dict, Dict[str, int]]:
        """Phase 8.6.2: 审核章节 variant — 真实 usage + try/except 兜底."""
        return self._impl_audit_chapter(
            chapter_num, content, characters, timeline, use_llm, record_usage=True,
        )

    def _impl_audit_chapter(
        self,
        chapter_num: int,
        content: str,
        characters: List[Dict],
        timeline: List[Dict],
        use_llm: bool,
        record_usage: bool,
    ):
        # 角色一致性检查 (规则, 无 LLM)
        char_issues = self.auditor.check_character_consistency(content, characters)
        # AI 痕迹检测 (规则, 无 LLM)
        ai_issues = self.auditor.detect_ai_gloss(content)
        all_issues = char_issues + ai_issues

        if use_llm:
            if record_usage:
                # Phase 8.6.2: 真实 usage path
                try:
                    llm_report, usage = self.auditor.audit_chapter_with_usage(
                        chapter_num=chapter_num,
                        content=content,
                        characters=characters,
                        context={"timeline": timeline},
                    )
                    if "issues" in llm_report:
                        all_issues.extend(llm_report["issues"])
                    if "scores" in llm_report:
                        return self.auditor.generate_audit_report(
                            chapter_num, all_issues, llm_report["scores"],
                        ), usage
                except Exception as e:
                    logger.warning(
                        f"LLM审核失败 (chapter {chapter_num}): {e}",
                        exc_info=True,
                    )
                    return self.auditor.generate_audit_report(
                        chapter_num, all_issues, scores={},
                    ), {"input_tokens": 0, "output_tokens": 0}
            else:
                # 旧契约: dict-only
                try:
                    llm_report = self.auditor.audit_chapter(
                        chapter_num=chapter_num,
                        content=content,
                        characters=characters,
                        context={"timeline": timeline},
                    )
                    if "issues" in llm_report:
                        all_issues.extend(llm_report["issues"])
                    if "scores" in llm_report:
                        return self.auditor.generate_audit_report(
                            chapter_num, all_issues, llm_report["scores"],
                        )
                except Exception as e:
                    logger.warning(
                        f"LLM审核失败 (chapter {chapter_num}): {e}",
                        exc_info=True,
                    )
        return self.auditor.generate_audit_report(chapter_num, all_issues, scores={})
```

### 2.3 Step 3: Modify `master_controller.py:672-712` (3 polish methods region)

Replace `def polish_chapter(` block with:

```python
    def polish_chapter(self, content: str) -> str:
        """润色章节 (委托给polisher, LLM 化路径 Phase 7.2)

        Phase 8.6.2: 旧契约返 str, 内部调 _impl(record_usage=False).
        真实 usage variant 见 polish_chapter_with_usage.
        """
        result = self._impl_polish_chapter(
            chapter_num=0, content=content, style_guide=None, record_usage=False,
        )
        return result["content"]

    def polish_chapter_with_usage(
        self, chapter_num: int, content: str, style_guide: Optional[Dict] = None,
    ) -> tuple[str, dict[str, int]]:
        """Phase 8.6.2: 润色章节 variant — 真实 usage (2 LLM sum)."""
        result, usage = self._impl_polish_chapter(
            chapter_num, content, style_guide, record_usage=True,
        )
        return result["content"], usage

    def _impl_polish_chapter(
        self, chapter_num: int, content: str, style_guide: Optional[Dict], record_usage: bool,
    ):
        if record_usage:
            return self.polisher.polish_chapter_with_usage(chapter_num, content, style_guide)
        return self.polisher.polish_chapter(chapter_num, content, style_guide)
```

Replace `def polish_emotional_pacing(` block with:

```python
    def polish_emotional_pacing(self, content: str) -> str:
        """情绪节奏 variant — 对话自然化 + 节奏调整 (Phase 7.4)

        Phase 8.6.2: 旧契约返 str, 内部调 _impl(record_usage=False).
        韧性契约 (Phase 7.2 模式, fixup M1): 每个 LLM 调 try/except 兜底.
        """
        result = self._impl_polish_emotional_pacing(
            chapter_num=0, content=content, record_usage=False,
        )
        return result

    def polish_emotional_pacing_with_usage(
        self, chapter_num: int, content: str,
    ) -> tuple[str, dict[str, int]]:
        """Phase 8.6.2: 情绪节奏 variant — 真实 usage (2 LLM sum)."""
        return self._impl_polish_emotional_pacing(
            chapter_num, content, record_usage=True,
        )

    def _impl_polish_emotional_pacing(
        self, chapter_num: int, content: str, record_usage: bool,
    ):
        polished = content
        usage_total = {"input_tokens": 0, "output_tokens": 0}
        # LLM 1: optimize_dialogue
        try:
            if record_usage:
                r1, u1 = self.polisher.optimize_dialogue_llm_with_usage(polished)
                usage_total["input_tokens"] += u1["input_tokens"]
                usage_total["output_tokens"] += u1["output_tokens"]
            else:
                r1 = self.polisher.optimize_dialogue_llm(polished)
        except Exception as e:
            logger.warning("polish_emotional_pacing: dialogue_llm failed: %s", e)
            r1 = polished
        # LLM 2: adjust_pacing
        try:
            if record_usage:
                r2, u2 = self.polisher.adjust_pacing_llm_with_usage(r1)
                usage_total["input_tokens"] += u2["input_tokens"]
                usage_total["output_tokens"] += u2["output_tokens"]
            else:
                r2 = self.polisher.adjust_pacing_llm(r1)
        except Exception as e:
            logger.warning("polish_emotional_pacing: pacing_llm failed: %s", e)
            r2 = r1
        if record_usage:
            return r2, usage_total
        return r2
```

Replace `def polish_ai_trace_removal(` block with:

```python
    def polish_ai_trace_removal(self, content: str) -> str:
        """AI 痕迹 variant — 规则去 AI 痕迹 + 对话自然化 (Phase 7.4)

        Phase 8.6.2: 旧契约返 str, 内部调 _impl(record_usage=False).
        韧性契约 (fixup M1): remove_ai_gloss 纯规则从不出错, optimize_dialogue_llm try/except 兜底.
        """
        result = self._impl_polish_ai_trace_removal(
            chapter_num=0, content=content, record_usage=False,
        )
        return result

    def polish_ai_trace_removal_with_usage(
        self, chapter_num: int, content: str,
    ) -> tuple[str, dict[str, int]]:
        """Phase 8.6.2: AI 痕迹 variant — 真实 usage (1 LLM + 1 rule)."""
        return self._impl_polish_ai_trace_removal(
            chapter_num, content, record_usage=True,
        )

    def _impl_polish_ai_trace_removal(
        self, chapter_num: int, content: str, record_usage: bool,
    ):
        cleaned = self.polisher.remove_ai_gloss(content)
        usage_total = {"input_tokens": 0, "output_tokens": 0}
        try:
            if record_usage:
                r, u = self.polisher.optimize_dialogue_llm_with_usage(cleaned)
                usage_total["input_tokens"] += u["input_tokens"]
                usage_total["output_tokens"] += u["output_tokens"]
            else:
                r = self.polisher.optimize_dialogue_llm(cleaned)
        except Exception as e:
            logger.warning("polish_ai_trace_removal: dialogue_llm failed: %s", e)
            r = cleaned
        if record_usage:
            return r, usage_total
        return r
```

### 2.4 Step 4: Run tests, verify GREEN

```bash
pytest tests/agent_system/test_master_controller_with_usage.py -v
# 期望: 8 passed
```

**Expected GREEN**:
```
tests/agent_system/test_master_controller_with_usage.py::TestMasterControllerWithUsage::test_write_chapter_with_usage_returns_tuple PASSED
... (8 total) 8 passed
```

---

## Task 3: Wire got_bridge.py (HandlerFn Union + 4 handlers tuple + AgentComputeFn 检测)

### 3.1 Step 1: Write failing tests in `tests/agent_system/test_got_bridge.py:TestCostTrackerWiring`

Find existing `TestCostTrackerWiring` class, add 2 new tests at the end:

```python
    def test_handler_returns_tuple_when_mc_variant_exists(self, monkeypatch):
        """Phase 8.6.2: _handler_chapter_writing 调 MC *_with_usage variant 返 tuple."""
        from infra.agent_system.got_bridge import _handler_chapter_writing
        from infra.agent_system.master_controller import MasterController

        master = MasterController.__new__(MasterController)  # bypass __init__
        # Stub MC variant 返 (output, usage)
        monkeypatch.setattr(
            master, "write_chapter_with_usage",
            lambda **kw: (
                {"content": "x" * 100, "chapter_num": kw["chapter_num"]},
                {"input_tokens": 100, "output_tokens": 50},
            ),
        )
        result = _handler_chapter_writing(
            master,
            {"chapter_num": 1, "outline": {}, "characters": [],
             "memory_context": {}, "style_guide": {}, "use_llm": True},
        )
        assert isinstance(result, tuple)
        assert len(result) == 2
        assert result[0]["chapter_num"] == 1
        assert result[1] == {"input_tokens": 100, "output_tokens": 50}

    def test_agent_compute_fn_uses_real_usage_from_tuple(self, monkeypatch):
        """Phase 8.6.2: AgentComputeFn.__call__ 检测 tuple 拆 usage 喂 cost_tracker."""
        from infra.ai_service.cost_tracker import CostTracker
        from infra.got.data_structures import ThoughtNode
        from infra.got.scheduler import ComputeResult
        from infra.agent_system.got_bridge import AgentComputeFn
        from infra.agent_system.master_controller import MasterController

        cost_tracker = CostTracker()
        master = MasterController.__new__(MasterController)
        # Stub _handler_chapter_writing 返 tuple
        monkeypatch.setattr(
            "infra.agent_system.got_bridge._handler_chapter_writing",
            lambda m, inputs: (
                {"content": "x" * 100, "chapter_num": 1},
                {"input_tokens": 200, "output_tokens": 100},
            ),
        )

        compute = AgentComputeFn(master, cost_tracker=cost_tracker)
        node = ThoughtNode(
            id="n1", prompt_scenario="chapter_writing", node_type=NodeType.GENERATION,
        )
        result = compute(node, {"chapter_num": 1, "outline": {}, "characters": []})

        assert isinstance(result, ComputeResult)
        assert result.fail is False
        # 真实 usage 200+100=300, 替代 len()//4 估算
        assert result.cost_tokens == 300
        # cost_tracker.record 收到真实数据
        recs = cost_tracker.records()
        assert len(recs) == 1
        assert recs[0].input_tokens == 200
        assert recs[0].output_tokens == 100
```

### 3.2 Step 2: Run tests, verify RED

```bash
pytest tests/agent_system/test_got_bridge.py::TestCostTrackerWiring -v -k "tuple or real_usage"
# 期望: 2 failed (handler 返 dict, AgentComputeFn 走 len()//4 估算)
```

**Expected RED**:
```
TypeError: cannot unpack non-iterable dict object  # handler 返回 dict, 不能 (output, usage) = tuple
... 2 failed
```

### 3.3 Step 3: Modify `got_bridge.py:43` (HandlerFn type alias)

Replace:
```python
HandlerFn = Callable[[MasterController, dict[str, Any]], dict[str, Any]]
```

With:
```python
HandlerFn = Callable[["MasterController", dict[str, Any]], Union[dict[str, Any], tuple[dict[str, Any], dict[str, int]]]]
```

(Need to add `Union` to the imports: `from typing import Any, Callable, Optional, Union`)

### 3.4 Step 4: Modify `got_bridge.py:59-78` (_handler_chapter_writing)

Replace:
```python
def _handler_chapter_writing(master: MasterController, inputs: dict[str, Any]) -> dict[str, Any]:
    """scenario=chapter_writing → content_writer.generate_chapter

    inputs 期望含 chapter_num (顶层 或 上游 read_snapshot 的 output)
    输出注入 chapter_num 字段 (供下游 review_chapter 等用)
    """
    chapter_num = int(_resolve_field(inputs, "chapter_num", 0))
    if not chapter_num:
        return {"_error": "chapter_num is required for chapter_writing"}
    result = master.write_chapter(
        chapter_num=chapter_num,
        outline=_resolve_field(inputs, "outline", {}),
        characters=_resolve_field(inputs, "characters", []),
        memory_context=_resolve_field(inputs, "memory_context", {}),
        style_guide=_resolve_field(inputs, "style_guide", {}),
        use_llm=bool(_resolve_field(inputs, "use_llm", True)),
    )
    if isinstance(result, dict):
        result.setdefault("chapter_num", chapter_num)
    return result
```

With:
```python
def _handler_chapter_writing(
    master: MasterController, inputs: dict[str, Any],
) -> tuple[dict[str, Any], dict[str, int]]:
    """scenario=chapter_writing → content_writer.generate_chapter (Phase 8.6.2 variant)

    调 master.write_chapter_with_usage 返 (result, usage) tuple, AgentComputeFn
    用真实 usage 喂 cost_tracker. 旧契约 (write_chapter) 0 改.

    inputs 期望含 chapter_num (顶层 或 上游 read_snapshot 的 output)
    输出注入 chapter_num 字段 (供下游 review_chapter 等用)
    """
    chapter_num = int(_resolve_field(inputs, "chapter_num", 0))
    if not chapter_num:
        return {"_error": "chapter_num is required for chapter_writing"}, {"input_tokens": 0, "output_tokens": 0}
    result, usage = master.write_chapter_with_usage(
        chapter_num=chapter_num,
        outline=_resolve_field(inputs, "outline", {}),
        characters=_resolve_field(inputs, "characters", []),
        memory_context=_resolve_field(inputs, "memory_context", {}),
        style_guide=_resolve_field(inputs, "style_guide", {}),
        use_llm=bool(_resolve_field(inputs, "use_llm", True)),
    )
    if isinstance(result, dict):
        result.setdefault("chapter_num", chapter_num)
    return result, usage
```

### 3.5 Step 5: Modify `got_bridge.py:81-100` (_handler_chapter_review)

Replace `_handler_chapter_review` body to call `master.audit_chapter_with_usage(...)` and return tuple. Same pattern as `_handler_chapter_writing`.

### 3.6 Step 6: Modify `got_bridge.py:103-117` (_make_polish_handler factory)

Replace:
```python
def _make_polish_handler(method_name: str) -> HandlerFn:
    """Phase 7.4: 工厂函数 — 每个 polish scenario 走独立 entry method

    把 _handler_polish 拆为闭包, 通过 getattr(master, method_name) 路由:
    - ai_trace_removal → master.polish_ai_trace_removal
    - emotional_pacing → master.polish_emotional_pacing
    - hook_extraction → master.polish_chapter (向后兼容, 走完整 3 路径)
    """
    def handler(master: MasterController, inputs: dict[str, Any]) -> dict[str, Any]:
        content = _resolve_field(inputs, "content", "")
        if not content:
            return {"_error": "content is required for polish scenarios"}
        method = getattr(master, method_name)
        return {"content": method(content)}
    return handler
```

With:
```python
def _make_polish_handler(method_name: str) -> HandlerFn:
    """Phase 7.4: 工厂函数 — 每个 polish scenario 走独立 entry method

    Phase 8.6.2: 优先调 {method_name}_with_usage variant 返 (result, usage) tuple.
    Backward compat: 旧 MC 无 variant → 调原 method wrap tuple (usage 0).

    路由:
    - ai_trace_removal → master.polish_ai_trace_removal_with_usage
    - emotional_pacing → master.polish_emotional_pacing_with_usage
    - hook_extraction → master.polish_chapter_with_usage
    """
    def handler(master: MasterController, inputs: dict[str, Any]) -> tuple[dict[str, Any], dict[str, int]]:
        content = _resolve_field(inputs, "content", "")
        if not content:
            return {"_error": "content is required for polish scenarios"}, {"input_tokens": 0, "output_tokens": 0}
        # Phase 8.6.2: 优先调 *_with_usage variant
        with_usage_method = f"{method_name}_with_usage"
        method_with_usage = getattr(master, with_usage_method, None)
        if method_with_usage is not None:
            result, usage = method_with_usage(chapter_num=0, content=content)
            if isinstance(result, str):
                return {"content": result}, usage
            return result, usage
        # Backward compat: 旧 MC 无 variant, 调原 method (wrap tuple, usage 0)
        original = getattr(master, method_name)
        result = original(content)
        if isinstance(result, dict):
            return result, {"input_tokens": 0, "output_tokens": 0}
        return {"content": result}, {"input_tokens": 0, "output_tokens": 0}
    return handler
```

### 3.7 Step 7: Modify `got_bridge.py:255-264` (AgentComputeFn.__call__ cost hot path)

Replace:
```python
        # Phase 8.5: 估算 token + 写 cost_tracker (None 时跳过)
        # Token 估算: len() // 4 (4 chars/token 经验值, ~85% accuracy;
        # 真实 usage 留 Phase 8.6 followup)
        cost_tokens = 0
        if self._cost_tracker is not None:
            in_tok = len(str(inputs)) // 4
            out_tok = len(str(output)) // 4
            tier = SCENARIO_TIER_MAP.get(scenario, ModelTier.SONNET)
            self._cost_tracker.record(scenario, tier, in_tok, out_tok)
            cost_tokens = in_tok + out_tok

        return ComputeResult(output=output, cost_tokens=cost_tokens, fail=False)
```

With:
```python
        # Phase 8.6.2: 双路径 cost recording.
        # Tuple 返 → 真实 usage (Phase 8.6.1 agent *_with_usage variants).
        # Dict 返 → 旧 len()//4 估算 (backward compat, 旧 path 100% 保留).
        cost_tokens = 0
        if isinstance(output, tuple):
            output, usage = output
            if self._cost_tracker is not None:
                tier = SCENARIO_TIER_MAP.get(scenario, ModelTier.SONNET)
                in_tok = usage.get("input_tokens", 0)
                out_tok = usage.get("output_tokens", 0)
                self._cost_tracker.record(scenario, tier, in_tok, out_tok)
                cost_tokens = in_tok + out_tok
        else:
            # Backward compat: dict return → 走 len()//4 估算
            if self._cost_tracker is not None:
                in_tok = len(str(inputs)) // 4
                out_tok = len(str(output)) // 4
                tier = SCENARIO_TIER_MAP.get(scenario, ModelTier.SONNET)
                self._cost_tracker.record(scenario, tier, in_tok, out_tok)
                cost_tokens = in_tok + out_tok

        return ComputeResult(output=output, cost_tokens=cost_tokens, fail=False)
```

### 3.8 Step 8: Add 5 `*_with_usage` methods to `_StubMaster` in `tests/agent_system/test_got_bridge.py`

Find `_StubMaster` class definition in test_got_bridge.py (around line 100-200). Add 5 methods at the end of the class (before the next class):

```python
    # Phase 8.6.2: 5 *_with_usage methods (旧 handler routing tests 不破)
    def write_chapter_with_usage(self, **kwargs):
        return (
            {"content": "stub " * 50, "chapter_num": kwargs.get("chapter_num", 1)},
            {"input_tokens": 100, "output_tokens": 50},
        )

    def audit_chapter_with_usage(self, **kwargs):
        return (
            {"content": "...", "score": 8, "issues": []},
            {"input_tokens": 80, "output_tokens": 40},
        )

    def polish_chapter_with_usage(self, **kwargs):
        # 2 LLM sum: 200/100
        return "polished " * 50, {"input_tokens": 200, "output_tokens": 100}

    def polish_emotional_pacing_with_usage(self, **kwargs):
        return "paced " * 50, {"input_tokens": 200, "output_tokens": 100}

    def polish_ai_trace_removal_with_usage(self, **kwargs):
        return "cleaned " * 50, {"input_tokens": 100, "output_tokens": 50}
```

### 3.9 Step 9: Run tests, verify GREEN

```bash
pytest tests/agent_system/test_got_bridge.py -v
# 期望: 全部 passed (含 2 new + 旧 TestComputeFn TestPolishHandlerRouting TestPolishMergeHandler TestCostTrackerWiring)

pytest tests/agent_system/test_master_controller_workflow.py -v
# 期望: 全部 passed (_StubMaster 改保 baseline)

pytest tests/agent_system/test_decision_integration.py -v
# 期望: 全部 passed (_StubMaster 改保 baseline)
```

---

## Task 4: Update `_e2e_helpers.py:make_master_with_router` + add 1 e2e test

### 4.1 Step 1: Modify `tests/agent_system/_e2e_helpers.py:make_master_with_router`

Find `make_master_with_router` function. Add `record_usage` kwarg. Modify body to inject `_RecordingRouter` when True.

```python
def make_master_with_router(
    state_dir: Path,
    *,
    cost_tracker: Optional["CostTracker"] = None,
    record_usage: bool = False,  # Phase 8.6.2
) -> MasterController:
    """Phase 8.6.2: 加 record_usage kwarg — True 时注入 _RecordingRouter.

    record_usage=False (default) → 走 StubProvider (Phase 7.1 e2e 模式), 25 旧
    e2e tests 零修改. record_usage=True → 注入 _UsageRecordingProvider +
    _RecordingRouter, 验证 AgentComputeFn tuple 路径 (真实 usage 喂 cost_tracker).
    """
    master = MasterController(state_dir=state_dir, cost_tracker=cost_tracker)
    if record_usage:
        from tests.agent_system.test_agent_with_usage import (
            _UsageRecordingProvider,
            _RecordingRouter,
        )
        master._router = _RecordingRouter(_UsageRecordingProvider())
        master.content_writer._router = master._router
        master.auditor._router = master._router
        master.polisher._router = master._router
    return master
```

(Don't change the default `make_stub_router()` helper — that stays for old 25 e2e tests)

### 4.2 Step 2: Add 1 e2e test to `tests/agent_system/test_master_controller_stub_router_e2e.py:TestNovelWritingE2E`

Find `TestNovelWritingE2E` class. Add new test at the end:

```python
    def test_novel_writing_records_real_cost_usage(self, tmp_path: Path) -> None:
        """Phase 8.6.2: 走 production path, cost_tracker 收到真实 usage (非 len()//4 估算)"""
        from infra.ai_service.cost_tracker import CostTracker

        cost_tracker = CostTracker()
        master = make_master_with_router(
            state_dir=tmp_path,
            cost_tracker=cost_tracker,
            record_usage=True,  # Phase 8.6.2
        )
        master.run_workflow(
            workflow_name="novel_writing",
            initial_inputs={
                "chapter_num": 1,
                "outline": {
                    "title": "test",
                    "chapters": [
                        {"chapter_num": 1, "title": "c1", "summary": "s", "key_events": []},
                    ],
                },
                "characters": [],
            },
            max_backtracks=0,
        )
        # 至少 4 LLM calls (write + audit + dialogue + pacing), 每个 100/50 token
        recs = cost_tracker.records()
        assert len(recs) >= 4
        # Real usage 不是 0
        total = sum(r.input_tokens + r.output_tokens for r in recs)
        assert total > 0
        # input_tokens 是 100 的整数倍 (硬编码 _UsageRecordingProvider)
        assert all(r.input_tokens == 100 for r in recs)
```

### 4.3 Step 3: Run tests, verify GREEN

```bash
pytest tests/agent_system/test_master_controller_stub_router_e2e.py -v
# 期望: 16 passed (15 旧 + 1 new)
```

---

## Task 5: 验证 2129 baseline + 12 new tests

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pytest -q 2>&1 | tail -5
# 期望: 2141 passed, 0 failed, 21 skipped (2129 + 12 new)

pytest tests/agent_system/test_master_controller_with_usage.py -v
# 期望: 8 passed

pytest tests/agent_system/test_got_bridge.py -k "cost" -v
# 期望: 5 passed (3 old + 2 new)

pytest tests/agent_system/test_master_controller_stub_router_e2e.py -v
# 期望: 16 passed (15 + 1 new)

# ruff
ruff check infra/agent_system/ tests/agent_system/ 2>&1 | tail -3
# 期望: 0 issues

# grep 验证
grep -rn "write_chapter_with_usage\|audit_chapter_with_usage\|polish.*_with_usage" infra/ --include="*.py" | grep -v "test_master_controller_with_usage"
# 期望: master_controller.py (5 定义 + 5 _impl_) + got_bridge.py (4 handlers 调)
```

**如果失败**: revert, 调查根因 (e.g. handler 漏调 variant, type alias Union mypy 错, _RecordingRouter 不替换 agent._router).

---

## Task 6: 1 feat commit + push

```bash
git add docs/superpowers/specs/2026-06-06-phase8.6.2-mc-with-usage-design.md \
        docs/superpowers/plans/2026-06-06-phase8.6.2-mc-with-usage.md \
        infra/agent_system/master_controller.py \
        infra/agent_system/got_bridge.py \
        tests/agent_system/test_master_controller_with_usage.py \
        tests/agent_system/test_got_bridge.py \
        tests/agent_system/test_master_controller_stub_router_e2e.py \
        tests/agent_system/_e2e_helpers.py

git status  # 确认 8 改文件

git commit -m "feat(cost): phase 8.6.2 — MC *_with_usage variants + AgentComputeFn real usage

Phase 8.6.1 暴露 agent *_with_usage 给 caller, 但 MC 单点入口仍 dict-only
+ AgentComputeFn 仍 len()//4 估算. 本期收尾: MC 暴露 5 variants, 4 handlers
返 (output, usage) tuple, AgentComputeFn 检测 tuple 用真实 usage 喂 cost_tracker.

改动:
- master_controller.py (520-770, +150L): 5 MC *_with_usage variants 委托
  _impl_*(record_usage=True), 旧 method 走 record_usage=False 保 baseline
  (write_chapter_with_usage / audit_chapter_with_usage /
  polish_chapter_with_usage / polish_emotional_pacing_with_usage /
  polish_ai_trace_removal_with_usage)
- got_bridge.py (43-266, +30L): HandlerFn type alias Union[dict, tuple]
  + 4 handlers (_handler_chapter_writing/_handler_chapter_review +
  _make_polish_handler 工厂 + _handler_polish_ai_trace_removal) 调 MC
  variants 返 tuple + AgentComputeFn.__call__ isinstance(output, tuple) 检测,
  tuple 走真实 usage, dict 走 len()//4 估算 (backward compat 双路径)
- _e2e_helpers.py (+10L): make_master_with_router 加 record_usage kwarg
  (default False 保 25 旧 e2e tests 零修改), True 时注入 _RecordingRouter
  + 替换 content_writer/auditor/polisher._router
- test_got_bridge.py _StubMaster (+30L): 加 5 *_with_usage methods (max len
  fallback) 保旧 handler routing tests 不破
- 12 new tests (8 test_master_controller_with_usage NEW + 2 TestCostTrackerWiring
  tuple/real_usage + 1 e2e test_novel_writing_records_real_cost_usage + 1 sanity
  test_chat_with_usage_unaffected)

不改:
- 0 production code 行为破 (旧 path 全保, 双路径)
- 0 polish_merge_synthesis variant (留 Phase 8.7, 内部 LLM 复杂)
- 0 generate_outline / 0 3 utility LLM (YAGNI)
- 0 ComputeResult schema change (cost_tokens 字段不变)
- 0 CostTrackerDB schema change
- 0 Dashboard UI change (留 followup)
- 0 12 SCENARIOS 路由 change
- 0 18 historical spec/plan docs (:NNN 行号不动)
- 0 force-push, 0 --no-verify, 0 Co-Authored-By
- 0 spec/plan doc 行号 (:NNN) 改
- 0 infra/.state/* 提交 (gitignored)
- 0 .env / 0 API key 泄漏

Baseline:
- 测试: 2129 + 12 = 2141 passed, 0 failed, 21 skipped (不变)
- ruff 0
- MEMORY.md 204 → 205-206 lines (5 over soft limit, 接受再超, Phase 9+ 统一拆)

后续:
- Phase 8.7: polish_merge_synthesis *_with_usage + Dashboard UI 画 cost 柱状图
- Phase 8.8: cost budget alarm
- Phase 8.9: real LLM e2e opt-in test 验证 generate_with_usage
- Followup: MEMORY.md 拆 12 entries 到 phases.md topic (208 lines 8 over soft limit)"

git push origin master
```

---

## Critical files

### Created
- `docs/superpowers/specs/2026-06-06-phase8.6.2-mc-with-usage-design.md` (~330L) — 设计文档
- `docs/superpowers/plans/2026-06-06-phase8.6.2-mc-with-usage.md` (~520L) — 实施计划
- `tests/agent_system/test_master_controller_with_usage.py` (~280L) — 8 tests

### Modified (注入点 + 透传 + tuple 路径)
- `infra/agent_system/master_controller.py:520-770` (5 MC variants + 5 _impl_* + record_usage kwarg)
- `infra/agent_system/got_bridge.py:43-266` (HandlerFn Union + 4 handlers tuple + AgentComputeFn tuple 检测)
- `tests/agent_system/test_got_bridge.py:_StubMaster` (5 *_with_usage methods)
- `tests/agent_system/test_got_bridge.py:TestCostTrackerWiring` (2 new tests)
- `tests/agent_system/test_master_controller_stub_router_e2e.py` (1 new e2e)
- `tests/agent_system/_e2e_helpers.py:make_master_with_router` (record_usage kwarg)

### Referenced / 复用
- `infra/agent_system/master_controller.py:642-670` (Phase 8.6 `chat_with_usage`)
- `infra/agent_system/agents/polisher/tools.py:38-199` (Phase 8.6.1 `polish_chapter_with_usage` + 2 sub `*_llm_with_usage`)
- `infra/agent_system/agents/auditor/tools.py` (Phase 8.6.1 `audit_chapter_with_usage`)
- `infra/agent_system/agents/content_writer/tools.py` (Phase 8.6.1 `generate_chapter_with_usage`)
- `tests/agent_system/test_agent_with_usage.py` (Phase 8.6.1 `_UsageRecordingProvider` + `_RecordingRouter` stubs)
- `infra/ai_service/cost_tracker.py:CostTracker` (EXISTING, 0 改)
- `infra/ai_service/model_tiers.py:SCENARIO_TIER_MAP` (EXISTING, 0 改)
- `infra/agent_system/got_bridge.py:AgentComputeFn` (EXISTING Phase 8.5 cost 路径, 加 tuple 分支)

### NOT modified
- `infra/agent_system/polisher/prompts.py` (0 改)
- `infra/agent_system/agents/*/prompts.py` (0 改)
- `infra/agent_system/got_bridge.py:SCENARIO_HANDLERS` 注册表 (0 改, 5 entry points 仍 5)
- `infra/ai_service/cost_tracker.py` (0 改, 已就位)
- `infra/agent_system/cost_persistence.py` (Phase 8.5 已就位, 0 改)
- `dashboard/protocols.py` (0 改, 字段已就位)
- `dashboard/app.py` (0 改, 已读 cost_tracker.total_cost)
- `pyproject.toml` / `pytest.ini` / `CLAUDE.md` (0 改)
- `infra/.state/cost_tracker.db` (NOT committed, gitignored)
- 18 历史 spec/plan docs (0 改, 跟 Phase 8.4 / 8.5 / 8.6 一样不动)

---

## Out of scope (后续 phases)

- **Phase 8.7**: polish_merge_synthesis `*_with_usage` variant — 内部 LLM 评分复杂, 需 polisher.chat → chat_with_usage 重构, 同时画 Dashboard UI 柱状图
- **Phase 8.8**: cost budget alarm (超阈值 trigger 暂停 workflow)
- **Phase 8.9**: real LLM e2e opt-in test 验证 generate_with_usage (跟 Phase 8/8.1/8.2 模式)
- **generate_outline / rewrite_section / expand_scene / suggest_improvements variants**: YAGNI, 12 SCENARIOS 路径下不调用
- **ComputeResult schema change**: cost_tokens 字段已能表达真实 usage, 0 改
- **CostTrackerDB schema change**: 单表 cost_records 已够, 0 改
- **Dashboard UI cost 柱状图**: Phase 8.7 一并画 (Vue + ECharts, 跟 Phase 7.6 S1-S8 雷达图同模式)
- **MEMORY.md 拆 12 entries 到 phases.md topic**: 本期只加 1 entry, 接受再超 5 行, 留独立 phase
- **MEMORY.md line 8 typo "19 skipped" → "21 skipped"**: Phase 8.2 后忘改, 留后续
- **CI filter tightening**: Phase 8.3 LOW 标, 留后续
- **改 18 历史 spec/plan docs 行号引用** (`:NNN`): 历史已不准, 改反误导, 永不做
- **改 12 SCENARIOS 路由**: 0 改, CostTracker 只是观察者
- **强制所有 handlers 返 tuple**: 0 改, 旧 path dict return 兜底 (defensive)

---

## 完成定义 (DoD)

- [ ] `docs/superpowers/specs/2026-06-06-phase8.6.2-mc-with-usage-design.md` 创建 (~330L)
- [ ] `docs/superpowers/plans/2026-06-06-phase8.6.2-mc-with-usage.md` 创建 (~520L)
- [ ] `infra/agent_system/master_controller.py` 5 MC `*_with_usage` variants + 5 _impl_* record_usage kwarg
- [ ] `infra/agent_system/got_bridge.py` HandlerFn Union + 4 handlers tuple + AgentComputeFn tuple 检测
- [ ] `tests/agent_system/_e2e_helpers.py:make_master_with_router` record_usage kwarg (default False)
- [ ] `tests/agent_system/test_got_bridge.py:_StubMaster` 5 *_with_usage methods
- [ ] `tests/agent_system/test_master_controller_with_usage.py` 8 tests passed
- [ ] `tests/agent_system/test_got_bridge.py:TestCostTrackerWiring` 2 new tests passed
- [ ] `tests/agent_system/test_master_controller_stub_router_e2e.py` 1 new e2e test passed
- [ ] 1 feat commit pushed to origin/master
- [ ] pytest 2141 passed, 0 failed, 21 skipped (2129 + 12 new)
- [ ] ruff 0
- [ ] 0 旧测试代码改动 (2129 baseline 不变)
- [ ] 0 production code 行为破坏 (master_controller / got_bridge 双路径保 backward compat)
- [ ] 0 CLAUDE.md / pyproject.toml / pytest.ini 改动
- [ ] 0 误改 .env / 0 API key 泄漏
- [ ] 0 infra/.state/* 提交 (gitignored)
- [ ] 0 force-push, 0 --no-verify, 0 Co-Authored-By footer
- [ ] 0 polish_merge_synthesis variant 改 (留 Phase 8.7)
- [ ] 0 generate_outline / 0 3 utility variants 改 (YAGNI)
- [ ] 0 ComputeResult schema 改
- [ ] 0 CostTrackerDB schema 改
- [ ] 0 dashboard UI 改 (留 Phase 8.7)

---

## 完成后下一步 (后续 phases)

- **Phase 8.7**: polish_merge_synthesis `*_with_usage` variant + Dashboard UI 画 cost by scenario/tier 柱状图 (Vue + ECharts, 跟 Phase 7.6 雷达图同模式)
- **Phase 8.8**: cost budget alarm (超阈值 trigger 暂停 workflow)
- **Phase 8.9**: real LLM e2e opt-in test 验证 generate_with_usage (跟 Phase 8/8.1/8.2 模式)
- **Phase 9**: 跨卷涟漪 (独立大方向)
- **MEMORY.md 拆 12 entries 到 phases.md topic** (Phase 8.6.2 后变 13 entries, 必拆)
- **MEMORY.md line 8 typo "19 skipped" → "21 skipped"** (Phase 8.2 后忘改)
- **CI filter 收紧**: Phase 8.3 LOW 标, `-k` filter 加 `and not OpenAI and not MiniMax`
