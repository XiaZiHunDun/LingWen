# Phase 13.0 Tasks — P1 Stabilization

> **目的**: 把 plan.md 的 5 个 sub-tasks 拆成可逐项追踪的 checklist，每项带 status / owner / deps / DoD。配套 `.claude/prds/p1-stabilization.prd.md` (需求) + `-design.md` (设计) + `-plan.md` (实施步骤)。
> **进度总览**: 5 sub-tasks (T1-T5) + T6 验证。Baseline 3011 pytest + 949 vitest → Target 3030 pytest + 953 vitest。

---

## 总览看板

| Task | 主题 | Status | Owner | 新增 pytest | 新增 vitest | 风险 |
|------|------|--------|-------|-------------|-------------|------|
| T1 | H10 前端 API timeout/Abort | pending | 主公 | 0 | +4 | R3 / R10 |
| T2 | H2 FastAPI middleware (CORS + GZip + 限流) | pending | 主公 | +5 | 0 | R2 / R7 / R8 |
| T3 | H4 Ripple list N+1 → bulk | pending | 主公 | +3 | 0 | R1 / R9 |
| T4 | M4 CLI path hygiene (LINGWEN_PROJECT_ROOT) | pending | 主公 | +4 | 0 | R4 |
| T5 | L6 Shell slug hygiene (LINGWEN_PROJECT_ROOT) | pending | 主公 | 0 | 0 | R4 / R6 |
| T6 | Final verification + push | pending | 主公 | 0 | 0 | — |
| **合计** | **5 项 P1 止血** | — | — | **+19** | **+4** | — |

---

## T1 — H10 Frontend API timeout/Abort

**目标**: 前端 `request()` 默认 15s timeout + AbortSignal 透传，158 个调用点全部受益。

### 子任务清单

- [ ] **T1.1** 写 `dashboard/frontend/tests/unit/api-timeout.spec.ts` 4 tests (timeout 15s / AbortSignal 透传 / 正常返回 / 非 2xx 解析 JSON)
  - deps: 无
  - owner: 主公
  - DoD: `pnpm vitest run tests/unit/api-timeout.spec.ts` RED（缺 timeout impl）
- [ ] **T1.2** 重构 `dashboard/frontend/src/api/index.js:request()`
  - deps: T1.1
  - DoD: 4 tests GREEN
  - 改动: +40L / -10L
  - 关键点: `DEFAULT_TIMEOUT_MS = 15_000` + `AbortController.timeout()` + 双 signal 关联 + 友好文案
- [ ] **T1.3** 全量 vitest 验证 0 回归 (158 调用点)
  - deps: T1.2
  - DoD: `pnpm vitest run` → 953 passed (949 + 4 new), 0 failed
  - 风险: R3 / R10
- [ ] **T1.4** ruff + commit
  - deps: T1.3
  - DoD: `ruff check dashboard/frontend/` → 0 issues, `git commit -m "feat(frontend): phase 13.0 task 1 — api/request timeout 15s"`

### 验证命令

```bash
cd dashboard/frontend && pnpm vitest run tests/unit/api-timeout.spec.ts  # RED
# 改 request() 实现
pnpm vitest run  # GREEN (953 passed)
cd ../.. && ruff check dashboard/frontend/
```

---

## T2 — H2 FastAPI middleware

**目标**: FastAPI 加 3 middleware (CORS + GZip + slowapi 限流)，mutation 路由单独更严。

### 子任务清单

- [ ] **T2.1** 写 `dashboard/tests/test_middleware.py` 5 tests (CORS preflight / GZip / 限流 100/min / mutation 限流 10/min / health 不超额)
  - deps: 无
  - DoD: `pytest dashboard/tests/test_middleware.py` RED
- [ ] **T2.2** 加 `slowapi>=0.1.9` 到 `requirements.txt` + `pip install -e .`
  - deps: 无（可与 T2.1 并行）
  - DoD: `pip show slowapi` 有版本号
- [ ] **T2.3** 在 `dashboard/app.py:create_app()` 加 CORSMiddleware + GZipMiddleware + slowapi Limiter + exception_handler
  - deps: T2.1 + T2.2
  - DoD: 5 tests GREEN
  - 改动: +35L
  - 关键点: `allow_origins=["*"]` (本地全开) + `minimum_size=1024` (GZip ≥1KB) + `default_limits=["100/minute"]`
- [ ] **T2.4** 给 mutation 路由 (例 `rollback_ripple` POST) 加 `@limiter.limit("10/minute")` decorator
  - deps: T2.3
  - DoD: mutation 测试 (T2.1 第 4 项) GREEN
- [ ] **T2.5** 跑 `pytest tests/dashboard/` 0 回归
  - deps: T2.4
  - DoD: 既有 dashboard tests 全部 passed
  - 风险: R7 (slowapi 兼容性) / R8 (GZip vs WS)
- [ ] **T2.6** curl 手动验证
  - deps: T2.5
  - DoD: `curl -I http://localhost:8000/api/health` 含 `access-control-allow-origin: *`
- [ ] **T2.7** ruff + commit
  - deps: T2.6
  - DoD: `ruff check dashboard/`, `git commit -m "feat(dashboard): phase 13.0 task 2 — FastAPI middleware"`

### 验证命令

```bash
cd novel-factory
pytest dashboard/tests/test_middleware.py -v  # RED
# 改 app.py + requirements.txt
pytest dashboard/tests/test_middleware.py -v  # GREEN (5 passed)
pytest tests/dashboard/ -v  # 0 回归
python dashboard/app.py &
sleep 3
curl -I http://localhost:8000/api/health  # 含 access-control-allow-origin
kill %1
```

---

## T3 — H4 Ripple list N+1 → bulk

**目标**: `_ripple_list_items` 从 200 ripple = 400 query 降到 2 query，200 行端到端 ≤ 200ms。

### 子任务清单

- [ ] **T3.1** 写 `tests/dashboard/test_ripple_list_perf.py` 3 tests (bulk dict / 200 行正确 / 200 行 ≤ 200ms)
  - deps: 无
  - DoD: `pytest tests/dashboard/test_ripple_list_perf.py` RED
- [ ] **T3.2** 在 `infra/cross_volume/storage.py:RippleStorage` 加 `get_ripple_impact_scores_bulk(ripple_ids) → dict`
  - deps: T3.1
  - DoD: bulk test GREEN
  - 改动: +20L
  - 关键点: `WHERE ripple_id IN (...)` + dict comprehension
- [ ] **T3.3** 重构 `dashboard/app.py:_ripple_list_items` 用 bulk 预取
  - deps: T3.2
  - DoD: 200 行 perf test GREEN, elapsed < 200ms
  - 改动: +20L / -10L
- [ ] **T3.4** 跑 `pytest tests/cross_volume/ tests/dashboard/test_cascade_runs_endpoint.py tests/dashboard/test_ripple_*.py -v` 0 回归
  - deps: T3.3
  - DoD: 既有 ripple 相关测试全 passed
  - 风险: R1 (大表回归) / R9 (SQLITE_MAX_VARIABLE_NUMBER)
- [ ] **T3.5** 创建 `scripts/bench-ripple-list.sh`
  - deps: T3.4
  - DoD: `bash scripts/bench-ripple-list.sh` → PASS, elapsed < 200ms
  - 改动: +40L
- [ ] **T3.6** ruff + commit
  - deps: T3.5
  - DoD: `ruff check`, `git commit -m "feat(dashboard): phase 13.0 task 3 — ripple list N+1 → bulk"`

### 验证命令

```bash
cd novel-factory
pytest tests/dashboard/test_ripple_list_perf.py -v  # RED
# 改 storage.py + dashboard/app.py
pytest tests/dashboard/test_ripple_list_perf.py -v  # GREEN
pytest tests/cross_volume/ tests/dashboard/test_ripple_*.py -v  # 0 回归
```

---

## T4 — M4 CLI path hygiene

**目标**: 3 个 CLI 命令 (`cascade` / `ripple_rollback` / `ripple_audit`) 从 `$LINGWEN_PROJECT_ROOT` 解析 db path，错位 cwd 立即 exit 2。

### 子任务清单

- [ ] **T4.1** 写 `tests/cvg_cli/test_path_resolution.py` 4 tests (env 设 + db 存在 / env 设 + db 缺失 exit 2 / env 未设 + CWD fallback + WARNING / env 未设 + no fallback exit 2)
  - deps: 无
  - DoD: `pytest tests/cvg_cli/test_path_resolution.py` RED
- [ ] **T4.2** 创建 `infra/cli/path_utils.py:resolve_project_db_path()`
  - deps: T4.1
  - DoD: 4 tests GREEN
  - 改动: +60L
  - 关键点: env 优先 / WARNING + CWD fallback (1 版本 deprecation) / db 缺失 exit 2
- [ ] **T4.3** 改 3 个 CLI 命令 (`infra/cli/commands/{cascade,ripple_rollback,ripple_audit}.py`) 用 helper
  - deps: T4.2
  - DoD: 既 CLI tests 全 passed (env 不设时走 CWD fallback)
  - 改动: +12L / -6L
- [ ] **T4.4** 跑 `pytest tests/cvg_cli/ -v` 0 回归
  - deps: T4.3
  - DoD: 既有 CLI 测试全 passed
  - 风险: R4 (env 改动 break cron)
- [ ] **T4.5** ruff + commit
  - deps: T4.4
  - DoD: `ruff check`, `git commit -m "feat(cli): phase 13.0 task 4 — path resolution env-driven"`

### 验证命令

```bash
cd novel-factory
pytest tests/cvg_cli/test_path_resolution.py -v  # RED
# 改 path_utils.py + 3 CLI commands
pytest tests/cvg_cli/test_path_resolution.py -v  # GREEN (4 passed)
pytest tests/cvg_cli/ -v  # 0 回归

# 手动验证 env 不一致
LINGWEN_PROJECT_ROOT=/nonexistent python lingwen.py cascade rip-1 2>&1
# 期望: exit 2 + [ERROR] .state/ripple.db 不存在
```

---

## T5 — L6 Shell slug hygiene

**目标**: 6 个硬编码 slug 的 shell 脚本全走 `$LINGWEN_PROJECT_ROOT`，未设 env 即 exit 2；新增 CI 守卫脚本。

### 子任务清单

- [ ] **T5.1** 创建 `scripts/_slug_guard.sh:slug_guard()`
  - deps: 无
  - DoD: shell 语法检查通过 (`bash -n scripts/_slug_guard.sh`)
  - 改动: +20L
  - 关键点: env 未设 exit 2 / env 是非目录 exit 2 / 友好错误信息
- [ ] **T5.2** 创建 `tests/scripts/test_slug_env.sh` 3 e2e shell tests
  - deps: T5.1
  - DoD: `bash tests/scripts/test_slug_env.sh` → 3 passed, 0 failed
  - 改动: +60L
- [ ] **T5.3** 改 6 个 shell 脚本 source guard + 删硬编码 slug
  - deps: T5.2
  - DoD: 6 脚本都能 source + slug_guard 通过
  - 改动: +30L / -30L
  - 目标脚本:
    - `scripts/run-project-batch.sh`
    - `scripts/build-all-trial-reads.sh`
    - `scripts/prepare-studio-samples-zip.sh`
    - `scripts/prepare-anye-distribution.sh`
    - `scripts/prepare-huangsha-distribution.sh`
    - `scripts/generate-full-check-report.sh`
- [ ] **T5.4** 创建 `scripts/verify-p1-stabilization.sh` CI 一键扫 5 项改动
  - deps: T5.3 + T1-T4 (因为要扫所有改动)
  - DoD: `bash scripts/verify-p1-stabilization.sh` → 5 checks PASS
  - 改动: +80L
- [ ] **T5.5** commit (含 6 改脚本 + 新文件)
  - deps: T5.4
  - DoD: `git commit -m "feat(scripts): phase 13.0 task 5 — shell slug env-driven"`

### 验证命令

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
bash tests/scripts/test_slug_env.sh  # 3 passed
bash scripts/verify-p1-stabilization.sh  # 5 checks PASS
grep -rE "(anye-xinbiao|huiyu-dangan)" scripts/ 2>/dev/null  # 仅 grep 注释/example, 应 0 业务硬编码
```

---

## T6 — Final verification + push

**目标**: 跑全量验证 + push 9 commits + 更新 HANDOFF。

### 子任务清单

- [ ] **T6.1** 全量 pytest 验证
  - deps: T1-T5
  - DoD: `pytest -q` → **3030 passed**, 0 failed, 27 skipped
- [ ] **T6.2** 全量 vitest 验证
  - deps: T6.1
  - DoD: `pnpm vitest run` → **953 passed**
- [ ] **T6.3** ruff 全量检查
  - deps: T6.2
  - DoD: `ruff check novel-factory/ scripts/ tests/scripts/` → 0 issues
- [ ] **T6.4** e2e-live parity
  - deps: T6.3
  - DoD: `LINGWEN_E2E_LIVE=1 pnpm e2e:live --list` → 5 specs
- [ ] **T6.5** verify-p1-stabilization.sh 5/5
  - deps: T6.4
  - DoD: `bash scripts/verify-p1-stabilization.sh` → 5 PASS
- [ ] **T6.6** bench-ripple-list.sh
  - deps: T6.5
  - DoD: 200 ripple ≤ 200ms
- [ ] **T6.7** verify-studio-maintenance-run.sh (跟 HANDOFF §8 一致)
  - deps: T6.6
  - DoD: PASS
- [ ] **T6.8** 把 `verify-p1-stabilization.sh` 接入 `.github/workflows/test.yml`
  - deps: T6.7
  - DoD: yaml 文件含新 step
- [ ] **T6.9** git log 检查 9 commits 干净
  - deps: T6.8
  - DoD: `git log --oneline -9` 显示 3 doc + 5 feat + 1 ci (本步)
- [ ] **T6.10** git push origin master
  - deps: T6.9
  - DoD: push 成功, GitHub Actions test workflow 触发
- [ ] **T6.11** 更新 HANDOFF.md §0.1 加 v13 changelog 一行
  - deps: T6.10
  - DoD: HANDOFF §0.1 "本会话已完成" 表新增 1 行
  - 1 commit `docs(handoff): phase 13.0 P1 stabilization done`

### 验证命令

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
cd novel-factory && pytest -q 2>&1 | tail -3  # 3030 passed
cd dashboard/frontend && pnpm vitest run 2>&1 | tail -5  # 953 passed
cd ../..
ruff check novel-factory/ scripts/ tests/scripts/  # 0 issues
cd novel-factory && LINGWEN_E2E_LIVE=1 pnpm e2e:live --list  # 5 specs
bash scripts/verify-p1-stabilization.sh  # 5 PASS
bash scripts/bench-ripple-list.sh  # < 200ms
bash scripts/verify-studio-maintenance-run.sh  # PASS

# git
git log --oneline -9
git push origin master
```

---

## 依赖图

```
T1 (H10 frontend)
 │
T2 (H2 middleware)
 │
T3 (H4 ripple N+1)
 │
T4 (M4 CLI path)
 │
T5 (L6 shell slug)  ──  depends on T1-T4 because verify-p1-stabilization.sh 扫所有改动
 │
T6 (final verify + push)  ──  depends on T1-T5
```

**实际可并行**: T1 / T2 / T3 / T4 都是独立模块，可并行做（subagent-driven-development 推荐）。T5 必须等 T1-T4 完成后写 verify-p1-stabilization.sh。T6 串行最后。

---

## DoD 总账（任一未达成 = phase 未完成）

- [ ] spec + plan + tasks 3 文档 commit (`docs(spec):` / `docs(plan):` / `docs(tasks):`)
- [ ] T1-T5 5 个 feat commit
- [ ] T6 CI 接入 commit
- [ ] pytest 3011 → **3030** (+19 new) all passed
- [ ] vitest 949 → **953** (+4 new) all passed
- [ ] ruff 0
- [ ] e2e-live 5/5 PASS
- [ ] verify-studio-maintenance-run.sh PASS
- [ ] bench-ripple-list.sh: 200 ripple ≤ 200ms
- [ ] verify-p1-stabilization.sh: 5 checks PASS
- [ ] test_slug_env.sh: 3 passed
- [ ] 0 旧测试破 (3030 - 19 = 3011 baseline 全部通过)
- [ ] 0 production code 行为破坏 (all additive, 既 path 0 改)
- [ ] 0 .env 改 / 0 API key 泄漏 / 0 force-push / 0 --no-verify / 0 Co-Authored-By footer
- [ ] 9 commits push origin/master
- [ ] HANDOFF §0.1 加 v13 changelog 一行

---

## 完成定义（Definition of Done for tasks.md）

> 本 tasks.md 本身完成 =:
> - 5 个 sub-task (T1-T5) 全部 `status: complete`
> - T6 final verification + push 完成
> - DoD 总账全勾
> - HANDOFF §0.1 更新 v13 一行
> - user 验收 approve

下一步: **Phase 14.0** —— P2 性能（Cascade 邻接表 + Qdrant 异步 + 缓存 TTL）