# Phase 8.31 — data-testid 统一 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) 或 superpowers:executing-plans 实施本 plan task-by-task. Steps use checkbox (`- [ ]`) 语法 for tracking.

**Goal**: 7 缺-testid component + 1 spec 需 inner testid 加 8 个 data-testid, 3 spec 文件 4 处 class selector 改 data-testid, vitest 14 spec files / 55 tests 全过, pytest 2262 baseline 不变.

**Architecture**: 最小变更 — 7 component root 元素加 1 attribute, SidebarCostBanner tier-text span 加 1 attribute, 4 处 spec selector string 替换. 0 production logic 改, 0 CSS 改, 0 backend 改, 0 composable 改.

**Tech Stack**: Vue 3 SFC (kebab-case attribute) + vitest 4.1.8 + @vue/test-utils + jsdom + Vue 3 `data-testid` convention (Phase 8.13 起 23 现有 testid 模式).

---

## 前置

```bash
# 确认 spec 已 commit
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
git log --oneline -1  # 期望: docs(spec): phase 8.31 — data-testid 统一 design
```

---

## Task 1: 8 component / page 加 data-testid (1 feat commit)

**Files:**
- Modify: `src/App.vue` (root div +1 attribute)
- Modify: `src/components/StatCard.vue` (root div +1 attribute)
- Modify: `src/components/WorkflowGraph.vue` (root div +1 attribute)
- Modify: `src/components/CoolpointChart.vue` (root div +1 attribute)
- Modify: `src/components/DecisionCard.vue` (root div +1 attribute)
- Modify: `src/pages/DecisionsPage.vue` (h1 +1 attribute)
- Modify: `src/pages/WorkflowsPage.vue` (h1 +1 attribute)
- Modify: `src/components/SidebarCostBanner.vue` (tier-text span +1 attribute, spec 需 inner)

### Step 1.1: 写 vitest 验证 testid 存在 (RED)

在 `tests/unit/dashboard.spec.ts` (已有 6 tests, 135L) 末尾加 1 个新 test 验证 StatCard 的 data-testid 渲染:

```typescript
// tests/unit/dashboard.spec.ts 末尾追加
test('StatCard renders with data-testid="stat-card" (Phase 8.31 unification)', async () => {
  const wrapper = mount(OverviewPage)
  await flushPromises()
  const statCards = wrapper.findAll('[data-testid="stat-card"]')
  expect(statCards.length).toBe(5)  // OverviewPage mount 5 个 StatCard (跟已有 test 一致)
})
```

在 `tests/unit/sidebar-tier-budget.spec.ts` 末尾加 1 个新 test 验证 tier-text data-testid:

```typescript
// tests/unit/sidebar-tier-budget.spec.ts 末尾追加
test('tier row contains data-testid="sidebar-cost-tier-text" (Phase 8.31 unification)', async () => {
  const wrapper = mount(SidebarCostBanner, {
    props: {
      status: makeStatus(
        { chapter_writing: 0.012, chapter_review: 0.008 },
        { status: 'ok', budget_usd: 0.10, used_usd: 0.02, used_pct: 20.0 },
      ),
    },
  })
  await flushPromises()
  const tierText = wrapper.find('[data-testid="sidebar-cost-tier-text"]')
  expect(tierText.exists()).toBe(true)
})
```

### Step 1.2: 跑新 test, 验证 RED

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
npx vitest run tests/unit/dashboard.spec.ts tests/unit/sidebar-tier-budget.spec.ts 2>&1 | tail -20
# 期望: 2 new test FAIL ("Unable to find [data-testid=\"stat-card\"]" / "...sidebar-cost-tier-text...")
# 期望: 旧 12 tests (6 + 6) 仍 pass
```

### Step 1.3: 8 component / page 加 data-testid (GREEN)

**App.vue** (1 attribute):
```vue
<!-- L1-2 region, 找 root div -->
<template>
  <div class="app" data-testid="app-root">
```

**StatCard.vue** (1 attribute, L2):
```vue
<!-- 前 -->
<div class="stat-card pixel-border">
<!-- 后 -->
<div class="stat-card pixel-border" data-testid="stat-card">
```

**WorkflowGraph.vue** (1 attribute, L6 region):
```vue
<!-- 前 -->
<div class="workflow-graph">
<!-- 后 -->
<div class="workflow-graph" data-testid="workflow-graph">
```

**CoolpointChart.vue** (1 attribute, root 元素):
```vue
<!-- 前 -->
<div class="coolpoint-chart pixel-border">
<!-- 后 -->
<div class="coolpoint-chart pixel-border" data-testid="coolpoint-chart">
```
(如果 class 跟本 plan 不一致, 跟当前实际 file 一致即可, 关键是 root 元素加 testid)

**DecisionCard.vue** (1 attribute, L7):
```vue
<!-- 前 -->
<div class="decision-card pixel-card">
<!-- 后 -->
<div class="decision-card pixel-card" data-testid="decision-card">
```

**DecisionsPage.vue** (1 attribute, L10 h1):
```vue
<!-- 前 -->
<h1 class="page-title">决策中心</h1>
<!-- 后 -->
<h1 class="page-title" data-testid="page-title">决策中心</h1>
```

**WorkflowsPage.vue** (1 attribute, L9 h1):
```vue
<!-- 前 -->
<h1 class="page-title">工作流</h1>
<!-- 后 -->
<h1 class="page-title" data-testid="page-title">工作流</h1>
```

**SidebarCostBanner.vue** (1 attribute, tier-text span):
```vue
<!-- 前 (L88-91 region, 找 sidebar-cost-tier-text span) -->
<span class="sidebar-cost-tier-text">{{ tier.name }}</span>
<!-- 后 -->
<span class="sidebar-cost-tier-text" data-testid="sidebar-cost-tier-text">{{ tier.name }}</span>
```

### Step 1.4: 跑新 test, 验证 GREEN

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
npx vitest run tests/unit/dashboard.spec.ts tests/unit/sidebar-tier-budget.spec.ts 2>&1 | tail -10
# 期望: 2 new test PASS
# 期望: 旧 12 tests 仍 pass
```

### Step 1.5: 跑全 vitest, 验证 baseline 0 破

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
npx vitest run 2>&1 | tail -10
# 期望: 14 test files passed (14), 57 tests passed (55 + 2 new), 0 failed
```

### Step 1.6: 1 feat commit

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
git add novel-factory/dashboard/frontend/src/App.vue \
        novel-factory/dashboard/frontend/src/components/StatCard.vue \
        novel-factory/dashboard/frontend/src/components/WorkflowGraph.vue \
        novel-factory/dashboard/frontend/src/components/CoolpointChart.vue \
        novel-factory/dashboard/frontend/src/components/DecisionCard.vue \
        novel-factory/dashboard/frontend/src/components/SidebarCostBanner.vue \
        novel-factory/dashboard/frontend/src/pages/DecisionsPage.vue \
        novel-factory/dashboard/frontend/src/pages/WorkflowsPage.vue \
        novel-factory/dashboard/frontend/tests/unit/dashboard.spec.ts \
        novel-factory/dashboard/frontend/tests/unit/sidebar-tier-budget.spec.ts

git status --short
# 期望: A  10 files (8 component/page + 2 spec 增新 test)

git -c user.name=lingwen-bot -c user.email=lingwen@local commit -m "$(cat <<'EOF'
feat(dashboard): phase 8.31 — data-testid 统一 (8 component testid)

Phase 8.13 引入 data-testid 约定, 14 vitest spec 96 处 testid 引用.
剩 7 component 完全缺顶层 testid (App/StatCard/WorkflowGraph/CoolpointChart/
DecisionCard/DecisionsPage/WorkflowsPage) + SidebarCostBanner 缺 1 spec 需
inner testid. 跟 Phase 8.13 23 testid 命名一致: kebab-case 镜像 stable
class, 0 conflict.

新 testid (8 个):
- app-root (App.vue root)
- stat-card (StatCard.vue root)
- workflow-graph (WorkflowGraph.vue root)
- coolpoint-chart (CoolpointChart.vue root)
- decision-card (DecisionCard.vue root)
- page-title (DecisionsPage.vue h1)
- page-title (WorkflowsPage.vue h1)
- sidebar-cost-tier-text (SidebarCostBanner tier-text span, spec 需 inner)

新增 2 vitest test 验证:
- dashboard.spec.ts: StatCard 5 个 [data-testid=stat-card] 在位
- sidebar-tier-budget.spec.ts: tier-text [data-testid=sidebar-cost-tier-text] 在位

不设计:
- 0 改 23 现有 data-testid (Phase 8.13 起 9 phase 未动)
- 0 改 DecisionCard / WorkflowGraph inner sub-element (留 Phase 8.32)
- 0 改 component CSS / props / composables / router
- 0 改 backend / agent_system
- 0 改 historical spec/plan :NNN 行号
- 0 改 CLAUDE.md / pyproject.toml / pytest.ini / vitest.config.ts

verification:
- vitest 14 spec files / 57 tests passed (55 baseline + 2 new)
- pytest 2262 / 27 skipped (frontend-only 0 影响)

followup:
- Task 2 (下 plan task): 3 spec 4 处 class selector 改 data-testid
- Phase 8.32: DecisionCard/WorkflowGraph inner sub-element testid
- Phase 8.33: data-testid lint rule
EOF
)" 2>&1 | tail -5

git log --oneline -1
# 期望: feat(dashboard): phase 8.31 — data-testid 统一 (8 component testid)
```

---

## Task 2: 3 spec 4 处 class selector 改 data-testid (1 chore commit)

**Files:**
- Modify: `tests/unit/dashboard.spec.ts` (1 selector 改, L82)
- Modify: `tests/unit/sidebar-tier-budget.spec.ts` (3 selector 改, L111/L137/L165)

### Step 2.1: 改 4 处 selector

**dashboard.spec.ts L82**:
```typescript
// 前
const statCards = wrapper.findAll('.stat-card')
// 后
const statCards = wrapper.findAll('[data-testid="stat-card"]')
```

**sidebar-tier-budget.spec.ts L111**:
```typescript
// 前
const tierText = rows[0].find('.sidebar-cost-tier-text')
// 后
const tierText = rows[0].find('[data-testid="sidebar-cost-tier-text"]')
```

**sidebar-tier-budget.spec.ts L137**:
```typescript
// 前
const fill = opusRow.find('.progress-bar-fill')
// 后
const fill = opusRow.find('[data-testid="progress-bar-fill"]')
```

**sidebar-tier-budget.spec.ts L165**:
```typescript
// 前
const fill = sonnetRow.find('.progress-bar-fill')
// 后
const fill = sonnetRow.find('[data-testid="progress-bar-fill"]')
```

### Step 2.2: 跑 2 改 spec, 验证 0 破

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
npx vitest run tests/unit/dashboard.spec.ts tests/unit/sidebar-tier-budget.spec.ts 2>&1 | tail -10
# 期望: 14 tests pass (8 dashboard + 6 sidebar-tier-budget, 含 2 new from Task 1)
```

### Step 2.3: grep 验证 spec 0 class selector 残留

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
grep -rEn "find\('\.|findAll\('\." tests/unit/ --include="*.spec.ts"
# 期望: 0 行匹配 (4 处全净化)
```

### Step 2.4: 跑全 vitest + 全 pytest, 验证 baseline 0 破

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
npx vitest run 2>&1 | tail -5
# 期望: 14 test files passed (14), 57 tests passed (55 + 2 new), 0 failed

cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pytest -q 2>&1 | tail -3
# 期望: 2262 passed, 27 skipped, 0 failed (frontend-only 0 影响)
```

### Step 2.5: 1 chore commit

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
git add novel-factory/dashboard/frontend/tests/unit/dashboard.spec.ts \
        novel-factory/dashboard/frontend/tests/unit/sidebar-tier-budget.spec.ts

git status --short
# 期望: M  2 files

git -c user.name=lingwen-bot -c user.email=lingwen@local commit -m "$(cat <<'EOF'
chore(dashboard): phase 8.31 — 4 spec class selector → data-testid

Phase 8.30b vitest 改造残留 4 处 class selector (1:1 翻译原 ceremonial
Playwright spec, 没补 data-testid). 本 task 净化: 3 spec 文件 4 处
class selector 改 [data-testid="..."] 跟 Phase 8.13 约定对齐.

改 4 处 (3 spec 文件):
- dashboard.spec.ts L82: .stat-card → [data-testid=stat-card]
- sidebar-tier-budget.spec.ts L111: .sidebar-cost-tier-text → [data-testid=sidebar-cost-tier-text]
- sidebar-tier-budget.spec.ts L137: .progress-bar-fill → [data-testid=progress-bar-fill]
- sidebar-tier-budget.spec.ts L165: .progress-bar-fill → [data-testid=progress-bar-fill]

(注: progress-bar-fill data-testid 自 Phase 8.13 已存在, spec 没用是反向
不一致; 另 2 处 data-testid 在 Task 1 feat commit 加好)

verification:
- vitest 14 spec files / 57 tests passed (Task 1 + 0 break)
- pytest 2262 / 27 skipped baseline 不变
- grep spec 0 处 class selector 残留
EOF
)" 2>&1 | tail -5

git log --oneline -3
# 期望 3 commits:
#   chore(dashboard): phase 8.31 — 4 spec class selector → data-testid
#   feat(dashboard): phase 8.31 — data-testid 统一 (8 component testid)
#   docs(spec): phase 8.31 — data-testid 统一 design
```

---

## Task 3: 推送 + MEMORY 同步 (1 chore commit)

**Files:**
- Modify: `~/.claude/projects/.../memory/phases-8-dashboard.md` (+~12L, Phase 8.31 entry)

### Step 3.1: 2 commit 推 origin

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
git push origin master 2>&1 | tail -5
# 期望: 87b3b10..HEAD  master -> master (3 commits, 含 1 docs spec + 1 feat + 1 chore)
# 实际可能是 ea7ee81..HEAD (Phase 8.16 plan + 8.30 7 commits + 8.31 3 commits, 总 ~12 ahead)
```

### Step 3.2: 同步 phases-8-dashboard.md

读当前 phases-8-dashboard.md (48L, 含 Phase 8.30b entry), 在末尾 append Phase 8.31 entry:

```markdown
## Phase 8.31 — Dashboard data-testid 统一 (2026-06-08)

**范围**: 7 缺-testid component 加顶层 + SidebarCostBanner 1 inner testid + 3 spec 4 处 class selector 改 data-testid.

**commits**:
- `8cd51c3` docs(spec): phase 8.31 — data-testid 统一 design
- (Task 1 feat) feat(dashboard): phase 8.31 — data-testid 统一 (8 component testid)
- (Task 2 chore) chore(dashboard): phase 8.31 — 4 spec class selector → data-testid

**8 新 testid** (kebab-case 镜像 stable class, 跟 Phase 8.13 23 testid 0 冲突):
- app-root (App.vue)
- stat-card (StatCard.vue)
- workflow-graph (WorkflowGraph.vue)
- coolpoint-chart (CoolpointChart.vue)
- decision-card (DecisionCard.vue)
- page-title (DecisionsPage.vue + WorkflowsPage.vue h1)
- sidebar-cost-tier-text (SidebarCostBanner inner, spec 需)

**2 new vitest test**:
- dashboard.spec.ts: StatCard 5 个 [data-testid=stat-card] 在位
- sidebar-tier-budget.spec.ts: tier-text [data-testid=sidebar-cost-tier-text] 在位

**不变**:
- 0 改 23 现有 data-testid
- 0 改 DecisionCard/WorkflowGraph inner sub-element (留 Phase 8.32)
- 0 改 backend / agent_system / composables / router

**verification**:
- vitest 14 spec files / 57 tests passed (55 baseline + 2 new)
- pytest 2262 / 27 skipped baseline 不变 (frontend-only)
- grep spec 0 处 class selector 残留

**followup**:
- Phase 8.32: DecisionCard/WorkflowGraph inner sub-element testid
- Phase 8.33: data-testid lint rule (强制 root 必含)
- Phase 8.34-36: store registry / byTestid helper / class mirror
```

### Step 3.3: 1 memory chore commit

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
# 注意: phases-8-dashboard.md 在 ~/.claude/projects/.../memory/ 下, 不在项目 git 仓库
# 这个 chore commit 仅当 memory 移到仓库内才需要; 当前约定是 memory 文件在外部, 不 commit
# 跳过此 step, memory 文件已 in-place 更新
```

(注: 跟 Phase 8.30b 一样, memory file 不进项目 git, 直接编辑更新即可)

### Step 3.4: 最终 grep 验证

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend

# spec 0 class selector
grep -rEn "find\('\.|findAll\('\." tests/unit/ --include="*.spec.ts" | wc -l
# 期望: 0

# 8 新 testid 在位
grep -rEn 'data-testid="(app-root|stat-card|workflow-graph|coolpoint-chart|decision-card|sidebar-cost-tier-text)"' src/ --include="*.vue" | wc -l
# 期望: 6 (app-root + stat-card + workflow-graph + coolpoint-chart + decision-card + sidebar-cost-tier-text)
# page-title 在 3 个 page 都有 (Overview 已存 + DecisionsPage/WorkflowsPage 新加), 不在此 grep

# git status clean
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
git status
# 期望: nothing to commit, working tree clean
```

---

## Critical files

### 改 (10 files)
- `src/App.vue` — root +1 attribute
- `src/components/StatCard.vue` — root +1 attribute
- `src/components/WorkflowGraph.vue` — root +1 attribute
- `src/components/CoolpointChart.vue` — root +1 attribute
- `src/components/DecisionCard.vue` — root +1 attribute
- `src/pages/DecisionsPage.vue` — h1 +1 attribute
- `src/pages/WorkflowsPage.vue` — h1 +1 attribute
- `src/components/SidebarCostBanner.vue` — tier-text span +1 attribute (inner)
- `tests/unit/dashboard.spec.ts` — +1 test (Task 1) + 1 selector 改 (Task 2)
- `tests/unit/sidebar-tier-budget.spec.ts` — +1 test (Task 1) + 3 selector 改 (Task 2)

### 不改 (explicit lock, 跟 spec 一致)
- 23 个现有 data-testid (Phase 8.13 起 9 phase 未动)
- 14 个 vitest spec 其余 96 selector (除上面 4 处)
- 11 个 component 已有 testid (除 SidebarCostBanner 增 1 inner)
- backend / agent_system / composables / router / api
- 7 historical spec/plan docs
- `CLAUDE.md` / `pyproject.toml` / `pytest.ini` / `vitest.config.ts`
- `.state/*.db` / `.env`

## Out of scope (后续 phases, 跟 spec 一致)

- **Phase 8.32**: DecisionCard / WorkflowGraph inner sub-element testid (8+ 个, visual regression / E2E 需时)
- **Phase 8.33**: data-testid lint rule (强制 src/components root 必含)
- **Phase 8.34**: pinia store testid registry (常量中心化)
- **Phase 8.35**: spec `byTestid()` helper (5+ 重复触发 DRY)
- **Phase 8.36**: remove class mirror on testid 元素 (CSS-only, 反向问题)
- **Phase 9**: 跨卷涟漪 (独立大方向)

## 风险 + 缓解 (跟 spec 一致)

| 风险 | 缓解 |
|------|------|
| 改坏 14 spec 已有 testid 断言 | 0 改 23 现有 testid, 8 个新 testid 镜像 stable class, 跟 0 冲突 |
| `<h1>` 改 testid 影响 SEO / a11y | 0 改 h1 元素, 只加 attribute, semantic HTML 保持 |
| spec 改 selector 后 CSS 改 class 时破测试 | data-testid 是独立 attribute, CSS 改 class 不影响 testid, 更稳 |
| 漏改 1 处 class selector | Task 2.3 grep 验证 0 匹配 |
| inner testid 改坏 sidebar layout | 仅加 attribute, 0 改 class/structure/style |
| vitest 跑出新 warning | Task 1.5 + Task 2.4 baseline 对比 |
| `DecisionsPage` 加 `data-testid="page-title"` 跟 `OverviewPage` 重名 | 重名预期, spec 用 wrapper 隔离, 0 cross-page 冲突 |
| 后端 / agent_system 误改 | 0 改 backend, 14 spec 全 frontend |

## 验证 (主公 review commit 后)

### Step 1: vitest baseline + new tests

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
npx vitest run 2>&1 | tail -5
# 期望: Test Files 14 passed (14), Tests 57 passed (57), Duration < 5s
```

### Step 2: pytest baseline 0 破

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pytest -q 2>&1 | tail -3
# 期望: 2262 passed, 27 skipped, 0 failed
```

### Step 3: grep 验证

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend

# spec 0 class selector
grep -rEn "find\('\.|findAll\('\." tests/unit/ --include="*.spec.ts" | wc -l
# 期望: 0

# 8 新 testid 在位
grep -rEn 'data-testid="(app-root|stat-card|workflow-graph|coolpoint-chart|decision-card|sidebar-cost-tier-text)"' src/ --include="*.vue" | wc -l
# 期望: 6
```

### Step 4: git log verify

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
git log --oneline -5
# 期望最近 3 commits:
#   chore(dashboard): phase 8.31 — 4 spec class selector → data-testid
#   feat(dashboard): phase 8.31 — data-testid 统一 (8 component testid)
#   docs(spec): phase 8.31 — data-testid 统一 design

git show --stat HEAD~2..HEAD
# 期望: 3 commits, 总 10 file changes (8 component/page + 2 spec)
```

### Step 5: 推 origin

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
git push origin master 2>&1 | tail -3
# 期望: 推送 3 commits 成功
```

## 完成定义 (DoD)

- [ ] `docs/superpowers/plans/2026-06-08-phase8.31-data-testid-unification.md` 创建 (本文, ~280L)
- [ ] Task 1: 8 component / page 加 data-testid + 2 new vitest test passed
- [ ] Task 2: 3 spec 4 处 class selector 改 data-testid
- [ ] Task 3: 2 commit 推 origin/master + memory file 更新
- [ ] vitest 14 spec files / 57 tests 全过 (55 baseline + 2 new)
- [ ] pytest 2262 / 27 skipped baseline 不变
- [ ] grep spec 0 处 class selector 残留
- [ ] grep src 6+ 新 testid 在位
- [ ] git log 3 commits (docs spec + feat + chore)
- [ ] 0 production code 行为破 (纯 attribute 加 + selector string 改, 0 logic)
- [ ] 0 改 23 现有 data-testid
- [ ] 0 改 backend / agent_system / composables / router
- [ ] 0 改 7 historical spec/plan docs `:NNN`
- [ ] 0 改 CLAUDE.md / pyproject.toml / pytest.ini / vitest.config.ts
- [ ] 0 改 .state/*.db (gitignored)
- [ ] 0 force-push, 0 --no-verify, 0 Co-Authored-By
- [ ] 0 .env 改, 0 API key 泄漏
- [ ] MEMORY.md 不动 (跟 Phase 8.30b 一致, phases-8-dashboard.md append 1 entry ~30L, < 100)
- [ ] DecisionCard / WorkflowGraph inner sub-element testid 留 Phase 8.32

## 完成后下一步 (后续 phases)

- **Phase 8.32**: DecisionCard / WorkflowGraph inner sub-element testid (visual regression / E2E 需时)
- **Phase 8.33**: data-testid lint rule (强制 root 必含 testid)
- **Phase 8.34**: pinia store testid registry (常量中心化, IDE 跳转)
- **Phase 8.35**: spec `byTestid()` helper (5+ 重复触发 DRY)
- **Phase 8.36**: remove class mirror on testid (CSS-only, 反向问题)
- **Phase 9**: 跨卷涟漪 (独立大方向)
- **MEMORY.md line 8 typo "27 skipped"**: 无 typo, 留 v9.16 修
- **CI filter 收紧**: Phase 8.3 LOW 标, 留 followup
