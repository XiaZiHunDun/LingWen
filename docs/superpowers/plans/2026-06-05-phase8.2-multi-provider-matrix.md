# Phase 8.2 Implementation Plan — Multi-Provider Real LLM Matrix

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 抽 `_make_real_router(provider_name)` 到 `_e2e_helpers.py`, 在 `test_novel_writing_real_llm.py` 加 OpenAI (gpt-4o-mini) + MiniMax (M2.7) 跨 provider 真实 LLM 测试. 验证 Phase 7.5 polish_merge 评分在多 provider 都能走 LLM 路径. 默认 SKIP, opt-in 跑 (主公).

**Architecture:**
- 2 文件改动: `tests/agent_system/_e2e_helpers.py` (新增 `_PROVIDER_REGISTRY` + `_make_real_router` provider_name 参数化) + `tests/agent_system/test_novel_writing_real_llm.py` (删旧 helper + 改 2 现有 TestClass + 加 2 新 TestClass)
- 0 生产代码改动 (跟 Phase 8.1 同样不动 master_controller / prompts / providers)
- 0 state DB changes
- Skipif 模式: 3 skipif 装饰器各管各 provider, 默认全 SKIP
- Lazy import via `importlib` 避免硬依赖 (openai 包没装 → ImportError 清晰)

**Tech Stack:** Python 3.13, pytest 9, ruff, importlib (stdlib)

---

## Task 1: 抽 `_make_real_router(provider_name)` 到 `_e2e_helpers.py`

**Files:**
- Modify: `tests/agent_system/_e2e_helpers.py` (1-116 → 1-170, +50L)

### Step 1.1: 加 `import os` 跟 `import importlib`

**File**: `tests/agent_system/_e2e_helpers.py:14-22` (现有 imports 区)

**OLD**:
```python
from __future__ import annotations

from pathlib import Path
from typing import Union

from infra.agent_system.agent_config import MasterControllerConfig
from infra.agent_system.master_controller import MasterController
from infra.ai_service.base import AIProvider, ProviderConfig
from infra.ai_service.router import AIRouter
```

**NEW**:
```python
from __future__ import annotations

import importlib
import os
from pathlib import Path
from typing import TYPE_CHECKING, Union

from infra.agent_system.agent_config import MasterControllerConfig
from infra.agent_system.master_controller import MasterController
from infra.ai_service.base import AIProvider, ProviderConfig
from infra.ai_service.router import AIRouter

if TYPE_CHECKING:
    pass  # AIRouter is runtime-imported above
```

### Step 1.2: 加 `_PROVIDER_REGISTRY` + `_make_real_router(provider_name)` (追加在 `make_master_with_router` 之后, line 115 之后)

**File**: `tests/agent_system/_e2e_helpers.py:115-116` (末尾)

**Append** (新内容, 紧跟现有 `make_master_with_router` 函数体后):
```python


# ==================== Phase 8.2: Multi-Provider Real LLM Matrix ====================

_PROVIDER_REGISTRY = {
    "anthropic": {
        "class_path": "infra.ai_service.anthropic_provider.AnthropicProvider",
        "env_var": "ANTHROPIC_API_KEY",
        "default_model": "claude-haiku-4-5-20251001",
    },
    "openai": {
        "class_path": "infra.ai_service.openai_provider.OpenAIProvider",
        "env_var": "OPENAI_API_KEY",
        "default_model": "gpt-4o-mini",
    },
    "minimax": {
        "class_path": "infra.ai_service.minimax_provider.MiniMaxProvider",
        "env_var": "MINIMAX_API_KEY",
        "default_model": "MiniMax-M2.7",
    },
}


def _make_real_router(provider_name: str) -> AIRouter:
    """Phase 8.2: 构造单 provider 的 AIRouter (multi-provider matrix).

    Phase 8 只测 Anthropic, Phase 8.2 加 OpenAI + MiniMax 跨 provider 验证
    Phase 7.5 polish_merge 评分在多 provider 都能走 LLM 路径. 抽到 _e2e_helpers.py
    是因为现在 3 个 provider 共用同一个 helper, 分散在多个 test 文件会重复代码.

    Args:
        provider_name: "anthropic" / "openai" / "minimax" (lowercase, 跟
                        @register_provider 注册名一致)

    Returns:
        AIRouter with single provider, primary=provider_name, enable_failover=False

    Raises:
        ValueError: 不支持的 provider_name
        KeyError: 缺少对应 env var (e.g. OPENAI_API_KEY not set)
        ImportError: 依赖未装 (e.g. openai 包缺失)
    """
    if provider_name not in _PROVIDER_REGISTRY:
        raise ValueError(
            f"Unsupported provider_name: {provider_name!r}. "
            f"Supported: {list(_PROVIDER_REGISTRY.keys())}"
        )
    spec = _PROVIDER_REGISTRY[provider_name]
    api_key = os.environ.get(spec["env_var"])
    if not api_key:
        raise KeyError(
            f"Provider {provider_name!r} requires env var {spec['env_var']!r} "
            f"to be set"
        )

    # Lazy import 避免循环 + 允许 provider 缺依赖时 (e.g. openai 包未装) fail gracefully
    module_path, class_name = spec["class_path"].rsplit(".", 1)
    module = importlib.import_module(module_path)
    provider_cls = getattr(module, class_name)

    provider = provider_cls(ProviderConfig(
        api_key=api_key,
        model=spec["default_model"],
        timeout=180,  # 1 LLM call 留 180s headroom
        max_retries=1,  # fail-fast
    ))
    return AIRouter(
        config={provider_name: provider.config},
        primary_provider=provider_name,
        enable_failover=False,  # 单 provider, 失败立即 raise
    )
```

### Step 1.3: 验证 helper 单元

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory

# 不存在的 provider 应 raise ValueError
python -c "from tests.agent_system._e2e_helpers import _make_real_router; _make_real_router('xxx')" 2>&1 | tail -3
# Expected: ValueError: Unsupported provider_name: 'xxx'. Supported: ['anthropic', 'openai', 'minimax']

# 没 API key 应 raise KeyError (Anthropic)
unset ANTHROPIC_API_KEY
python -c "from tests.agent_system._e2e_helpers import _make_real_router; _make_real_router('anthropic')" 2>&1 | tail -3
# Expected: KeyError: 'Provider "anthropic" requires env var "ANTHROPIC_API_KEY" to be set'

# 有 API key 应返 AIRouter
ANTHROPIC_API_KEY=sk-test python -c "
from tests.agent_system._e2e_helpers import _make_real_router
r = _make_real_router('anthropic')
print(type(r).__name__, r._primary_provider if hasattr(r, '_primary_provider') else '?')
"
# Expected: AIRouter anthropic
```

---

## Task 2: 改 `test_novel_writing_real_llm.py` 用 helper + 加 2 TestClass

**Files:**
- Modify: `tests/agent_system/test_novel_writing_real_llm.py` (现有 163L → 净增 ~75L)

### Step 2.1: 删旧 `_make_real_router()` (line 27-44)

**File**: `tests/agent_system/test_novel_writing_real_llm.py:27-44`

**OLD** (整段删除):
```python
def _make_real_router():
    """构造单 Anthropic provider 的 AIRouter, 用 HAIKU 模型 (Phase 7.5 spec 锁 S1-S8 评分用 HAIKU).

    Returns:
        AIRouter: 单 anthropic provider 的 router, primary=anthropic, failover=False
    """
    from infra.ai_service import ProviderConfig
    from infra.ai_service.anthropic_provider import AnthropicProvider
    from infra.ai_service.router import AIRouter

    provider = AnthropicProvider(ProviderConfig(
        api_key=os.environ["ANTHROPIC_API_KEY"],
        model="claude-haiku-4-5-20251001",
        timeout=180,  # 留够 6 LLM call headroom
        max_retries=1,  # fail-fast, 不阻塞
    ))
    return AIRouter(
        config={"anthropic": provider.config},
        primary_provider="anthropic",
        enable_failover=False,  # 单 provider, 失败立即 raise
    )
```

### Step 2.2: 改 2 现有 TestClass 用新 helper (传 "anthropic")

**File**: `tests/agent_system/test_novel_writing_real_llm.py` (2 处)

在文件顶部 (line 1 之后) 加 import:
```python
from tests.agent_system._e2e_helpers import _make_real_router, make_master_with_router
```

把 `TestNovelWritingRealLLM` (line 63+) 内的:
```python
router = _make_real_router()
```
改为:
```python
router = _make_real_router("anthropic")
```

把 `TestPolishMergeSynthesisRealLLM` (line 130+) 内的:
```python
router = _make_real_router()
```
改为:
```python
router = _make_real_router("anthropic")
```

(原 `from tests.agent_system._e2e_helpers import make_master_with_router` 这一行删掉, 已被新 import 替代)

### Step 2.3: 加 `_REQUIRES_OPENAI_KEY` + `_REQUIRES_MINIMAX_KEY` skipif 装饰器

**File**: `tests/agent_system/test_novel_writing_real_llm.py` (line ~25, 紧跟 `_REQUIRES_ANTHROPIC_KEY`)

**OLD**:
```python
_REQUIRES_ANTHROPIC_KEY = pytest.mark.skipif(
    not os.environ.get("ANTHROPIC_API_KEY"),
    reason="Phase 8 real LLM test requires ANTHROPIC_API_KEY env var",
)
```

**NEW** (替换 + 追加):
```python
_REQUIRES_ANTHROPIC_KEY = pytest.mark.skipif(
    not os.environ.get("ANTHROPIC_API_KEY"),
    reason="Phase 8 real LLM test requires ANTHROPIC_API_KEY env var",
)

# Phase 8.2: OpenAI + MiniMax multi-provider matrix skip decorators
_REQUIRES_OPENAI_KEY = pytest.mark.skipif(
    not os.environ.get("OPENAI_API_KEY"),
    reason="Phase 8.2 real LLM test requires OPENAI_API_KEY env var",
)
_REQUIRES_MINIMAX_KEY = pytest.mark.skipif(
    not os.environ.get("MINIMAX_API_KEY"),
    reason="Phase 8.2 real LLM test requires MINIMAX_API_KEY env var",
)
```

### Step 2.4: 加 2 新 TestClass (append 在文件末尾, line 163 之后)

**File**: `tests/agent_system/test_novel_writing_real_llm.py:163` (末尾)

**Append** (新内容):
```python


# ==================== Phase 8.2: Multi-Provider Matrix ====================


@_REQUIRES_OPENAI_KEY
class TestNovelWritingRealLLMOpenAI:
    """Phase 8.2: OpenAI 真实 LLM E2E 验证 polish_merge 跨 provider 兼容.

    跟 TestPolishMergeSynthesisRealLLM (Anthropic) 同样的 9 步断言.
    Cost: gpt-4o-mini 1 LLM call ≈ $0.001-0.003 (7-8x cheaper than HAIKU).

    跑: export OPENAI_API_KEY=sk-...; pytest tests/agent_system/test_novel_writing_real_llm.py -k OpenAI -v
    """

    def test_polish_merge_synthesis_with_distinct_contents(self, tmp_path: Path) -> None:
        """OpenAI: 2 个不同内容 → 走 LLM 评分路径, 产出 S1-S8 真实分数."""
        router = _make_real_router("openai")
        master = make_master_with_router(state_dir=tmp_path, router=router)

        content_a = "少年握紧拳头, 眼中燃烧着不甘。寒风刺骨, 但他咬牙前行。" * 20
        content_b = "林夕深吸一口气, 心中暗自立下誓言。纵然前路艰险, 亦不退缩。" * 20

        result = master.polish_merge_synthesis(
            content_a, content_b, labels=("emotional", "motivation"),
        )

        # 9 步断言 (跟 TestPolishMergeSynthesisRealLLM 一致)
        assert result["fallback"] is None, (
            f"polish_merge_synthesis (OpenAI) 应走 LLM 路径, 但 fallback={result['fallback']!r}"
        )
        _assert_s1_s8_score_dict(result["scores_a"], "scores_a")
        _assert_s1_s8_score_dict(result["scores_b"], "scores_b")
        assert result["winner"] in result["_labels"]
        assert 0.0 <= result["scores_total_a"] <= 10.0
        assert 0.0 <= result["scores_total_b"] <= 10.0
        assert abs(
            result["scores_total_a"] - result["scores_total_b"] - result["scores_delta"]
        ) < 1e-6
        assert result["_labels"] == ["emotional", "motivation"]


@_REQUIRES_MINIMAX_KEY
class TestNovelWritingRealLLMMiniMax:
    """Phase 8.2: MiniMax 真实 LLM E2E 验证 polish_merge 跨 provider 兼容.

    跟 TestPolishMergeSynthesisRealLLM (Anthropic) 同样的 9 步断言.
    Cost: MiniMax M2.7 单价未知, 1 LLM call 预计 < $0.01.

    跑: export MINIMAX_API_KEY=...; pytest tests/agent_system/test_novel_writing_real_llm.py -k MiniMax -v
    """

    def test_polish_merge_synthesis_with_distinct_contents(self, tmp_path: Path) -> None:
        """MiniMax: 2 个不同内容 → 走 LLM 评分路径, 产出 S1-S8 真实分数."""
        router = _make_real_router("minimax")
        master = make_master_with_router(state_dir=tmp_path, router=router)

        content_a = "少年握紧拳头, 眼中燃烧着不甘。寒风刺骨, 但他咬牙前行。" * 20
        content_b = "林夕深吸一口气, 心中暗自立下誓言。纵然前路艰险, 亦不退缩。" * 20

        result = master.polish_merge_synthesis(
            content_a, content_b, labels=("emotional", "motivation"),
        )

        # 9 步断言 (跟 TestPolishMergeSynthesisRealLLM 一致)
        assert result["fallback"] is None, (
            f"polish_merge_synthesis (MiniMax) 应走 LLM 路径, 但 fallback={result['fallback']!r}"
        )
        _assert_s1_s8_score_dict(result["scores_a"], "scores_a")
        _assert_s1_s8_score_dict(result["scores_b"], "scores_b")
        assert result["winner"] in result["_labels"]
        assert 0.0 <= result["scores_total_a"] <= 10.0
        assert 0.0 <= result["scores_total_b"] <= 10.0
        assert abs(
            result["scores_total_a"] - result["scores_total_b"] - result["scores_delta"]
        ) < 1e-6
        assert result["_labels"] == ["emotional", "motivation"]
```

### Step 2.5: 验证默认全 SKIP (无任何 API key)

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
unset ANTHROPIC_API_KEY OPENAI_API_KEY MINIMAX_API_KEY
pytest tests/agent_system/test_novel_writing_real_llm.py -v 2>&1 | tail -15
# Expected: 4 skipped (2 旧 Anthropic + 2 新 OpenAI/MiniMax)
```

### Step 2.6: 全量回归 (无 key 跑应不变)

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pytest tests/ -q 2>&1 | tail -3
# Expected: 2090 passed, 21 skipped (19 旧 + 2 新 opt-in)
```

---

## Task 3: ruff 0 + 1 commit + push

### Step 3.1: ruff 检查

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
ruff check tests/agent_system/_e2e_helpers.py tests/agent_system/test_novel_writing_real_llm.py
# Expected: All checks passed!
```

### Step 3.2: 1 commit + push

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git add tests/agent_system/_e2e_helpers.py tests/agent_system/test_novel_writing_real_llm.py
git commit -m "feat(agent_system): phase 8.2 — multi-provider real LLM matrix (OpenAI + MiniMax)

跨 provider 验证 Phase 7.5 polish_merge 评分在多 provider 都能走 LLM 路径.
Phase 8 只测 Anthropic HAIKU, Phase 8.2 加 OpenAI gpt-4o-mini + MiniMax M2.7.

重构: _make_real_router() 抽到 _e2e_helpers.py 接受 provider_name: str 参数
- _PROVIDER_REGISTRY dict 派发 anthropic/openai/minimax (class_path + env_var + default_model)
- Lazy import via importlib 避免硬依赖 (openai 包没装 → ImportError 清晰)
- _make_real_router(\"xxx\") raise ValueError; 没 API key raise KeyError (不静默)

测试: test_novel_writing_real_llm.py
- 删旧 _make_real_router() (line 27-44, 19 行)
- 改 2 现有 Anthropic TestClass 用 _make_real_router(\"anthropic\")
- 加 2 新 skipif 装饰器 _REQUIRES_OPENAI_KEY / _REQUIRES_MINIMAX_KEY
- 加 2 新 TestClass 各 1 test (跟 TestPolishMergeSynthesisRealLLM 同形, 9 步断言):
  * TestNovelWritingRealLLMOpenAI.test_polish_merge_synthesis_with_distinct_contents
    (gpt-4o-mini 1 LLM call, ~\$0.001-0.003, 5-10s)
  * TestNovelWritingRealLLMMiniMax.test_polish_merge_synthesis_with_distinct_contents
    (M2.7 1 LLM call, cost unknown, 5-10s)

不破现有 2090 tests, 0 production code changes (跟 Phase 8.1 同样不动
master_controller / prompts / providers), 0 state DB changes.

Baseline:
- 无 key (默认): 2090 passed + 21 skipped (19 旧 + 2 新 opt-in)
- 跑全 3 provider: 2090 + 3 = 2093 passed + 18 skipped
- ruff 0"

git push origin master
```

---

## Task 4: MEMORY.md + 1 行 Phase 8.2 entry

**File**: `~/.claude/projects/-home-ailearn-projects-AI-Incursion-domains-IP---projects-LingWen/memory/MEMORY.md`

### Step 4.1: 查行数

```bash
wc -l /home/ailearn/projects/-home-ailearn-projects-AI-Incursion-domains-IP---projects-LingWen/memory/MEMORY.md
# Expected: 198 (Phase 8.1 末)
```

### Step 4.2: 在 Phase 8.1 entry 之后**插入** Phase 8.2 entry

```markdown
- **Phase 8.2 (2026-06-05)**: Doc 4 §11 Phase 8.2 — Multi-Provider Real LLM Matrix (OpenAI + MiniMax) — 跨 provider 验证 Phase 7.5 polish_merge 评分兼容性 (Phase 8 只测 Anthropic): 抽 _make_real_router(provider_name: str) 到 _e2e_helpers.py (3 provider 共用), 新增 _PROVIDER_REGISTRY dict (anthropic/openai/minimax × class_path/env_var/default_model) + lazy import via importlib (避免硬依赖); 改 test_novel_writing_real_llm.py: 删旧 _make_real_router() (19 行) + 改 2 现有 Anthropic TestClass 用 _make_real_router("anthropic") + 加 2 skipif 装饰器 _REQUIRES_OPENAI_KEY / _REQUIRES_MINIMAX_KEY + 加 2 新 TestClass 各 1 test (TestNovelWritingRealLLMOpenAI: gpt-4o-mini 1 LLM call, ~$0.001-0.003, 5-10s / TestNovelWritingRealLLMMiniMax: M2.7 1 LLM call, cost unknown, 5-10s), 9 步断言同 Phase 8 TestPolishMergeSynthesisRealLLM; _make_real_router("xxx") raise ValueError / 缺 env var raise KeyError (不静默); YAGNI: 不加 endpoint/model 参数 / 不加 marker / 不接 CI / 不动生产代码 / 不改 stub helpers; 2090 passed + 21 skipped (19 旧 + 2 新 opt-in), ruff 0; spec docs/superpowers/specs/2026-06-05-phase8.2-multi-provider-matrix-design.md (commit b092276); plan docs/superpowers/plans/2026-06-05-phase8.2-multi-provider-matrix.md; impl commit <hash> 推送 origin/master
```

### Step 4.3: 验证 < 200 行

```bash
wc -l /home/ailearn/projects/-home-ailearn-projects-AI-Incursion-domains-IP---projects-LingWen/memory/MEMORY.md
# Expected: 199 (198 + 1)
```

---

## 完成定义 (DoD)

- [ ] `tests/agent_system/_e2e_helpers.py` 加 `import os, importlib` + `_PROVIDER_REGISTRY` + `_make_real_router(provider_name)` (~50 行)
- [ ] `_make_real_router("xxx")` raise `ValueError`
- [ ] `_make_real_router("openai")` 无 `OPENAI_API_KEY` raise `KeyError`
- [ ] `test_novel_writing_real_llm.py` 删旧 `_make_real_router()` (line 27-44)
- [ ] 改 2 现有 TestClass 用 `_make_real_router("anthropic")`
- [ ] 加 `_REQUIRES_OPENAI_KEY` + `_REQUIRES_MINIMAX_KEY` skipif 装饰器
- [ ] 加 `TestNovelWritingRealLLMOpenAI` (1 test) + `TestNovelWritingRealLLMMiniMax` (1 test)
- [ ] 默认 (无任何 API key): `pytest tests/agent_system/test_novel_writing_real_llm.py -v` → 4 skipped
- [ ] 全量: `pytest tests/ -q` → 2090 passed, 21 skipped
- [ ] `ruff check .` 0 warnings
- [ ] 1 impl commit pushed to origin/master
- [ ] MEMORY.md < 200 lines (+ 1 行 Phase 8.2 entry)

**主公 opt-in 验证** (不在 subagent 跑):
```bash
# Anthropic
export ANTHROPIC_API_KEY=sk-ant-... && pytest tests/agent_system/test_novel_writing_real_llm.py -k "Anthropic or PolishMergeSynthesis" -v

# OpenAI
export OPENAI_API_KEY=sk-... && pytest tests/agent_system/test_novel_writing_real_llm.py -k OpenAI -v

# MiniMax
export MINIMAX_API_KEY=... && pytest tests/agent_system/test_novel_writing_real_llm.py -k MiniMax -v
```
