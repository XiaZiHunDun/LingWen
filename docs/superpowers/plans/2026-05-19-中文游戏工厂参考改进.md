# 中文游戏工厂参考改进方案

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 基于 Claude-Code-Game-Studios 参考项目，为小说工厂设计 9 个改进项的详细实施方案

**Architecture:** 9 个独立改进项，每个作为子系统可单独实施。采用模块化设计，确保各改进可独立部署不影响现有功能。

**Tech Stack:** Python, Bash, JSON, Claude Code Hooks, Claude Code Skills

---

## 改进项总览

| 优先级 | 改进项 | 预估工作量 | 价值 |
|--------|--------|-----------|------|
| P0 | 1. Hooks 系统 | 1天 | 高 |
| P0 | 2. 问题追踪结构化 | 1天 | 高 |
| P1 | 3. CLAUDE.md 拆分 | 2天 | 高 |
| P1 | 4. 质量检查模块化重构 | 3天 | 中 |
| P2 | 5. 模板系统 | 2天 | 中 |
| P2 | 6. Skills 技能系统 | 3天 | 中 |
| P3 | 7. 测试体系 | 3天 | 中 |
| P3 | 8. 部门协调协议 | 2天 | 低 |
| P3 | 9. 版本发布规范化 | 2天 | 中 |

---

## 改进项 1：Hooks 系统

### 目标
添加会话生命周期自动化，实现会话开始/结束自动保存进度、提交前自动检查。

### 文件结构

```
novel-factory/
├── .claude/
│   └── hooks/                    # 新建
│       ├── session-start.sh      # 会话开始钩子
│       ├── session-stop.sh       # 会话结束钩子
│       ├── pre-commit.sh         # 提交前检查
│       └── detect-gaps.sh        # 检测缺失
└── settings.json                  # 修改：添加 hook 配置
```

### 任务分解

#### Task 1: 创建 hooks 目录和基础脚本

**Files:**
- Create: `novel-factory/.claude/hooks/session-start.sh`
- Create: `novel-factory/.claude/hooks/session-stop.sh`
- Create: `novel-factory/.claude/hooks/pre-commit.sh`
- Create: `novel-factory/.claude/hooks/detect-gaps.sh`

- [ ] **Step 1: 创建 session-start.sh**

```bash
#!/bin/bash
# novel-factory/.claude/hooks/session-start.sh
# 会话开始钩子：加载项目状态、显示进度

NOVEL_FACTORY_DIR="$(cd "$(dirname "$0")/../.." && pwd)"
STATE_FILE="$NOVEL_FACTORY_DIR/workflow_state.json"

echo "=== 灵文 · 工业化小说生产系统 ==="
echo ""

if [ -f "$STATE_FILE" ]; then
    # 解析当前阶段（使用 python 避免 jq 依赖）
    python3 -c "
import json
import sys
try:
    with open('$STATE_FILE', 'r') as f:
        state = json.load(f)
    current_phase = state.get('current_phase', 'UNKNOWN')
    current_step = state.get('current_step', 'UNKNOWN')
    print(f'当前阶段: {current_phase}')
    print(f'当前步骤: {current_step}')
    print('')
    # 显示未完成的步骤
    issues = state.get('issues_found', {})
    if issues:
        print(f'未解决问题数: {len(issues)}')
        print('')
except Exception as e:
    print(f'状态文件读取失败: {e}')
"
else
    echo "workflow_state.json 不存在，请先初始化项目"
fi

echo "输入 /help 查看可用命令"
```

- [ ] **Step 2: 创建 session-stop.sh**

```bash
#!/bin/bash
# novel-factory/.claude/hooks/session-stop.sh
# 会话结束钩子：保存进度、更新状态

NOVEL_FACTORY_DIR="$(cd "$(dirname "$0")/../.." && pwd)"
STATE_FILE="$NOVEL_FACTORY_DIR/workflow_state.json"
SESSION_STATE="$NOVEL_FACTORY_DIR/production/session-state/active.md"

# 创建会话状态目录
mkdir -p "$NOVEL_FACTORY_DIR/production/session-state"

# 记录会话结束状态
echo "# 会话状态 - $(date '+%Y-%m-%d %H:%M:%S')

## 当前进度
" > "$SESSION_STATE"

if [ -f "$STATE_FILE" ]; then
    python3 -c "
import json
try:
    with open('$STATE_FILE', 'r') as f:
        state = json.load(f)
    current_phase = state.get('current_phase', 'UNKNOWN')
    current_step = state.get('current_step', 'UNKNOWN')
    with open('$SESSION_STATE', 'a') as f:
        f.write(f'- 阶段: {current_phase}\n')
        f.write(f'- 步骤: {current_step}\n')
        f.write(f'- 累计问题: {len(state.get(\"issues_found\", {}))}\n')
except Exception as e:
    print(f'状态保存失败: {e}')
" >> "$SESSION_STATE"
fi

echo "
## 会话结束
- 已保存工作进度
" >> "$SESSION_STATE"

echo "会话进度已保存"
```

- [ ] **Step 3: 创建 pre-commit.sh**

```bash
#!/bin/bash
# novel-factory/.claude/hooks/pre-commit.sh
# 提交前检查：检查文件完整性、JSON 语法

NOVEL_FACTORY_DIR="$(cd "$(dirname "$0")/../.." && pwd)"
STATE_FILE="$NOVEL_FACTORY_DIR/workflow_state.json"
ERRORS=0

echo "=== 提交前检查 ==="

# 检查 workflow_state.json JSON 语法
if [ -f "$STATE_FILE" ]; then
    python3 -m json.tool "$STATE_FILE" > /dev/null 2>&1
    if [ $? -ne 0 ]; then
        echo "❌ workflow_state.json JSON 语法错误"
        ERRORS=$((ERRORS + 1))
    else
        echo "✓ workflow_state.json JSON 语法正确"
    fi
fi

# 检查是否有临时文件残留
TEMP_FILES=$(find "$NOVEL_FACTORY_DIR" -maxdepth 3 -name "*_temp*" -o -name "*.tmp" 2>/dev/null | head -5)
if [ -n "$TEMP_FILES" ]; then
    echo "⚠️  发现临时文件:"
    echo "$TEMP_FILES"
    ERRORS=$((ERRORS + 1))
fi

# 检查章节文件是否都有**本章完**标记
CHAPTERS_DIR="$NOVEL_FACTORY_DIR/03_内容仓库/04_正文"
if [ -d "$CHAPTERS_DIR" ]; then
    MISSING_END=$(find "$CHAPTERS_DIR" -name "ch*.md" -exec grep -L "**本章完**" {} \; 2>/dev/null | head -5)
    if [ -n "$MISSING_END" ]; then
        echo "⚠️  以下章节缺少 **本章完** 标记:"
        echo "$MISSING_END"
        ERRORS=$((ERRORS + 1))
    else
        echo "✓ 章节文件完整性检查通过"
    fi
fi

if [ $ERRORS -gt 0 ]; then
    echo ""
    echo "发现 $ERRORS 个问题，请修复后提交"
    exit 1
fi

echo "✓ 提交前检查通过"
exit 0
```

- [ ] **Step 4: 创建 detect-gaps.sh**

```bash
#!/bin/bash
# novel-factory/.claude/hooks/detect-gaps.sh
# 检测缺失：章节缺失、大纲不一致

NOVEL_FACTORY_DIR="$(cd "$(dirname "$0")/../.." && pwd)"
CHAPTERS_DIR="$NOVEL_FACTORY_DIR/03_内容仓库/04_正文"
OUTLINE_DIR="$NOVEL_FACTORY_DIR/03_内容仓库/03_阶段大纲"

echo "=== 检测缺失 ==="

# 检测章节编号连续性
if [ -d "$CHAPTERS_DIR" ]; then
    python3 << 'EOF'
import os
import re

chapters_dir = os.environ.get('CHAPTERS_DIR', '.')
chapter_files = [f for f in os.listdir(chapters_dir) if re.match(r'ch\d+\.md', f)]

if not chapter_files:
    print("未找到章节文件")
    exit(0)

# 提取编号
numbers = []
for f in chapter_files:
    m = re.match(r'ch(\d+)\.md', f)
    if m:
        numbers.append(int(m.group(1)))

numbers.sort()
missing = []
for i in range(numbers[0], numbers[-1]):
    if i not in numbers:
        missing.append(i)

if missing:
    print(f"⚠️  缺失章节: {missing[:10]}{'...' if len(missing) > 10 else ''}")
else:
    print(f"✓ 章节连续性检查通过 ({len(numbers)} 章)")
EOF
fi

# 检测大纲文件是否缺失
if [ -d "$OUTLINE_DIR" ]; then
    STAGE_OUTLINES=$(find "$OUTLINE_DIR" -name "*.md" | wc -l)
    echo "✓ 阶段大纲文件数: $STAGE_OUTLINES"
fi

echo "检测完成"
```

- [ ] **Step 5: 修改 settings.json 添加 hook 配置**

**Files:**
- Modify: `novel-factory/.claude/settings.json` (如不存在则创建)

```json
{
  "permissions": {
    "allow": [
      "Bash(bash .claude/hooks/*)",
      "Bash(python3 -m json.tool *)",
      "Bash(find * -name \"*_temp*\")",
      "Read(**/workflow_state.json)",
      "Read(**/03_内容仓库/**/*.md)"
    ],
    "deny": [
      "Bash(rm -rf 03_内容仓库)",
      "Bash(*.tmp)"
    ]
  },
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash .claude/hooks/session-start.sh"
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash .claude/hooks/session-stop.sh"
          }
        ]
      }
    ]
  }
}
```

- [ ] **Step 6: 测试 hooks**

```bash
# 测试 session-start.sh
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
bash .claude/hooks/session-start.sh

# 测试 pre-commit.sh
bash .claude/hooks/pre-commit.sh

# 测试 detect-gaps.sh
bash .claude/hooks/detect-gaps.sh
```

- [ ] **Step 7: 提交**

```bash
git add .claude/hooks/ .claude/settings.json
git commit -m "feat: 添加会话生命周期 Hooks 系统

- session-start.sh: 会话开始显示项目状态
- session-stop.sh: 会话结束保存进度
- pre-commit.sh: 提交前检查文件完整性
- detect-gaps.sh: 检测章节缺失和大纲不一致"
```

---

## 改进项 2：问题追踪结构化

### 目标
将 `workflow_state.json` 中的 `issues_found` 独立为结构化的问题追踪系统，每个问题独立文件。

### 文件结构

```
novel-factory/
├── issues/                      # 新建：问题追踪目录
│   ├── _template.md            # 问题模板
│   ├── ch291.md                # ch291 章节问题
│   ├── ch293.md                # ch293 章节问题
│   └── _index.json            # 问题索引
├── tools/
│   └── issue_tracker.py        # 新建：问题追踪脚本
└── workflow_state.json          # 修改：移除 issues_found
```

### 任务分解

#### Task 2: 问题追踪系统

**Files:**
- Create: `novel-factory/issues/_template.md`
- Create: `novel-factory/issues/_index.json`
- Create: `novel-factory/tools/issue_tracker.py`
- Modify: `novel-factory/run_workflow.sh` (添加 issue 命令)

- [ ] **Step 1: 创建问题模板**

```markdown
# 问题追踪 - {chapter_id}

**严重程度**: {P0|P1|P2}
**发现问题时间**: {YYYY-MM-DD}
**问题类型**: {章节重复|人物状态矛盾|时间线冲突|命名不一致|...}
**状态**: {open|in_progress|resolved|verified}
**发现人**: {agent_name}

## 问题描述

{详细描述问题}

## 影响范围

- 涉及章节: {chXXX, chYYY}
- 影响程度: {高|中|低}

## 修复记录

### 修复尝试 1
- 时间: {YYYY-MM-DD}
- 操作: {做了什么}
- 结果: {成功|失败|部分修复}
- 备注: {观察到的现象}

---

## 关联审核记录

{审核记录文件路径}
```

- [ ] **Step 2: 创建问题索引文件**

```json
{
  "version": "1.0",
  "last_updated": "2026-05-19",
  "total_issues": 0,
  "by_severity": {
    "P0": 0,
    "P1": 0,
    "P2": 0
  },
  "by_status": {
    "open": 0,
    "in_progress": 0,
    "resolved": 0,
    "verified": 0
  },
  "issues": []
}
```

- [ ] **Step 3: 创建 issue_tracker.py**

```python
#!/usr/bin/env python3
"""
问题追踪工具
提供 CLI 接口管理问题追踪系统
"""
import argparse
import json
import os
import sys
from datetime import datetime
from pathlib import Path
from typing import Dict, List, Optional

ISSUES_DIR = Path(__file__).parent.parent / "issues"
INDEX_FILE = ISSUES_DIR / "_index.json"
TEMPLATE_FILE = ISSUES_DIR / "_template.md"


def load_index() -> Dict:
    """加载问题索引"""
    if INDEX_FILE.exists():
        with open(INDEX_FILE, 'r', encoding='utf-8') as f:
            return json.load(f)
    return {
        "version": "1.0",
        "last_updated": datetime.now().strftime("%Y-%m-%d"),
        "total_issues": 0,
        "by_severity": {"P0": 0, "P1": 0, "P2": 0},
        "by_status": {"open": 0, "in_progress": 0, "resolved": 0, "verified": 0},
        "issues": []
    }


def save_index(index: Dict):
    """保存问题索引"""
    index["last_updated"] = datetime.now().strftime("%Y-%m-%d")
    ISSUES_DIR.mkdir(parents=True, exist_ok=True)
    with open(INDEX_FILE, 'w', encoding='utf-8') as f:
        json.dump(index, f, ensure_ascii=False, indent=2)


def create_issue(chapter_id: str, severity: str, issue_type: str,
                 description: str, discovered_by: str = "system") -> str:
    """创建新问题"""
    issue_file = ISSUES_DIR / f"{chapter_id}.md"
    if issue_file.exists():
        # 追加到现有文件
        with open(issue_file, 'a', encoding='utf-8') as f:
            f.write(f"\n## 新问题 - {datetime.now().strftime('%Y-%m-%d %H:%M')}\n")
            f.write(f"\n**严重程度**: {severity}\n")
            f.write(f"**问题类型**: {issue_type}\n")
            f.write(f"**状态**: open\n")
            f.write(f"\n### 问题描述\n\n{description}\n")
        return str(issue_file)

    # 创建新文件
    with open(TEMPLATE_FILE, 'r', encoding='utf-8') as f:
        template = f.read()

    content = template.replace("{chapter_id}", chapter_id)
    content = content.replace("{YYYY-MM-DD}", datetime.now().strftime("%Y-%m-%d"))
    content = content.replace("{P0|P1|P2}", severity)
    content = content.replace("{章节重复|人物状态矛盾|时间线冲突|命名不一致|...}", issue_type)
    content = content.replace("{详细描述问题}", description)
    content = content.replace("{open|in_progress|resolved|verified}", "open")
    content = content.replace("{agent_name}", discovered_by)

    with open(issue_file, 'w', encoding='utf-8') as f:
        f.write(content)

    # 更新索引
    index = load_index()
    index["total_issues"] += 1
    index["by_severity"][severity] = index["by_severity"].get(severity, 0) + 1
    index["issues"].append({
        "chapter_id": chapter_id,
        "issue_file": str(issue_file),
        "severity": severity,
        "status": "open"
    })
    save_index(index)

    return str(issue_file)


def list_issues(status: Optional[str] = None, severity: Optional[str] = None):
    """列出问题"""
    index = load_index()
    issues = index.get("issues", [])

    if status:
        issues = [i for i in issues if i.get("status") == status]
    if severity:
        issues = [i for i in issues if i.get("severity") == severity]

    print(f"共 {len(issues)} 个问题:")
    for issue in issues:
        print(f"  [{issue['severity']}] {issue['chapter_id']} - {issue['status']}")


def update_status(chapter_id: str, new_status: str):
    """更新问题状态"""
    index = load_index()
    issue_file = ISSUES_DIR / f"{chapter_id}.md"

    for issue in index["issues"]:
        if issue["chapter_id"] == chapter_id:
            issue["status"] = new_status
            # 更新统计
            old_status = issue.get("old_status", "open")
            index["by_status"][old_status] = max(0, index["by_status"].get(old_status, 1) - 1)
            index["by_status"][new_status] = index["by_status"].get(new_status, 0) + 1
            break
    else:
        print(f"未找到问题: {chapter_id}")
        return

    save_index(index)
    print(f"已更新 {chapter_id} 状态为 {new_status}")


def main():
    parser = argparse.ArgumentParser(description="问题追踪工具")
    subparsers = parser.add_subparsers(dest="command", help="子命令")

    # create 子命令
    create_parser = subparsers.add_parser("create", help="创建新问题")
    create_parser.add_argument("--chapter", required=True, help="章节ID")
    create_parser.add_argument("--severity", required=True, choices=["P0", "P1", "P2"], help="严重程度")
    create_parser.add_argument("--type", required=True, help="问题类型")
    create_parser.add_argument("--desc", required=True, help="问题描述")
    create_parser.add_argument("--by", default="system", help="发现者")

    # list 子命令
    list_parser = subparsers.add_parser("list", help="列出问题")
    list_parser.add_argument("--status", choices=["open", "in_progress", "resolved", "verified"], help="按状态筛选")
    list_parser.add_argument("--severity", choices=["P0", "P1", "P2"], help="按严重程度筛选")

    # update 子命令
    update_parser = subparsers.add_parser("update", help="更新问题状态")
    update_parser.add_argument("--chapter", required=True, help="章节ID")
    update_parser.add_argument("--status", required=True,
                              choices=["open", "in_progress", "resolved", "verified"], help="新状态")

    args = parser.parse_args()

    if args.command == "create":
        result = create_issue(args.chapter, args.severity, args.type, args.desc, args.by)
        print(f"问题已创建: {result}")
    elif args.command == "list":
        list_issues(args.status, args.severity)
    elif args.command == "update":
        update_status(args.chapter, args.status)
    else:
        parser.print_help()


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: 添加 issue 命令到 run_workflow.sh**

```bash
# 在 run_workflow.sh 中添加以下函数

issue_create() {
    local chapter="$1"
    local severity="$2"
    local issue_type="$3"
    local description="$4"

    python3 tools/issue_tracker.py create \
        --chapter "$chapter" \
        --severity "$severity" \
        --type "$issue_type" \
        --desc "$description"
}

issue_list() {
    python3 tools/issue_tracker.py list "$@"
}

issue_update() {
    local chapter="$1"
    local status="$2"

    python3 tools/issue_tracker.py update \
        --chapter "$chapter" \
        --status "$status"
}
```

- [ ] **Step 5: 迁移现有 issues_found**

```python
#!/usr/bin/env python3
"""
迁移脚本：将 workflow_state.json 中的 issues_found 迁移到新的问题追踪系统
"""
import json
from pathlib import Path

NOVEL_FACTORY_DIR = Path(__file__).parent.parent
STATE_FILE = NOVEL_FACTORY_DIR / "workflow_state.json"
ISSUES_DIR = NOVEL_FACTORY_DIR / "issues"

def migrate():
    with open(STATE_FILE, 'r', encoding='utf-8') as f:
        state = json.load(f)

    issues_found = state.get("issues_found", {})
    if not issues_found:
        print("没有 issues_found 需要迁移")
        return

    print(f"开始迁移 {len(issues_found)} 个问题...")

    # 导入 issue_tracker
    import sys
    sys.path.insert(0, str(NOVEL_FACTORY_DIR / "tools"))
    from issue_tracker import create_issue, load_index, save_index

    index = load_index()

    for chapter_id, issues in issues_found.items():
        for issue in issues:
            create_issue(
                chapter_id=chapter_id,
                severity=issue.get("severity", "P1"),
                issue_type=issue.get("type", "unknown"),
                description=issue.get("description", ""),
                discovered_by=issue.get("discovered_by", "migration")
            )
            print(f"  迁移: {chapter_id} - {issue.get('type', 'unknown')}")

    # 从 workflow_state.json 移除 issues_found
    if "issues_found" in state:
        del state["issues_found"]
        with open(STATE_FILE, 'w', encoding='utf-8') as f:
            json.dump(state, f, ensure_ascii=False, indent=2)
        print(f"\n已从 workflow_state.json 移除 issues_found")

    print("迁移完成")

if __name__ == "__main__":
    migrate()
```

- [ ] **Step 6: 测试问题追踪系统**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory

# 创建测试问题
python3 tools/issue_tracker.py create \
    --chapter ch001 \
    --severity P1 \
    --type "章节重复" \
    --desc "测试问题"

# 列出问题
python3 tools/issue_tracker.py list

# 更新状态
python3 tools/issue_tracker.py update --chapter ch001 --status resolved
```

- [ ] **Step 7: 提交**

```bash
git add issues/ tools/issue_tracker.py
git commit -m "feat: 问题追踪结构化

- 创建 issues/ 目录存储独立问题文件
- 创建 issue_tracker.py 提供 CLI 接口
- 支持 create/list/update 子命令
- 提供迁移脚本将 issues_found 迁移到新系统"
```

---

## 改进项 3：CLAUDE.md 拆分

### 目标
将 1239 行的 CLAUDE.md 拆分为多个分层加载的文档，减少主文件大小。

### 文件结构

```
novel-factory/
├── CLAUDE.md                    # 精简：只保留身份、目录速查、命令速查
├── .claude/
│   └── docs/
│       ├── coordination-rules.md      # 已存在
│       ├── model-tier-guide.md        # 已存在
│       ├── context-management.md      # 已存在
│       ├── design-doc-standards.md    # 已存在
│       ├── agent-definitions.md       # 新建：6个Agent详细定义
│       ├── department-rules.md        # 新建：部门详细规则
│       ├── workflow-detailed.md       # 新建：工作流详细步骤
│       └── quality-dimensions.md      # 新建：质量检查维度详解
```

### 任务分解

#### Task 3: 拆分 CLAUDE.md

**Files:**
- Modify: `novel-factory/CLAUDE.md` (大幅精简)
- Create: `novel-factory/.claude/docs/agent-definitions.md`
- Create: `novel-factory/.claude/docs/department-rules.md`
- Create: `novel-factory/.claude/docs/workflow-detailed.md`
- Create: `novel-factory/.claude/docs/quality-dimensions.md`

- [ ] **Step 1: 创建精简版 CLAUDE.md (目标 < 200 行)**

```markdown
# 灵文 · 工业化小说生产系统

> **重大更新 (2026-05-19)**：已采用分层文档架构，详细信息在各专题文档中。

## 身份

我是小说工作室的主控调度 Agent，负责：
1. **任务编排**：按照工作流步骤依次调度各部门 Agent
2. **状态维护**：更新 workflow_state.json，记录当前进度
3. **大决策确认**：大纲审核通过/不通过、卷/全文汇总定稿、死锁仲裁
4. **进度汇报**：主动向用户汇报当前状态和风险

## 工作原则

- 用户（主公）定方向/标准/审美
- 我执行+建议+调度
- 简洁直接，主动汇报进度
- 遇到风险及时提醒，不隐瞒

## 目录速查

```
01_灵感库/          → 灵感部门输出
02_作家工作室/作家{N}/  → 作家画像+记忆
03_内容仓库/         → 四层结构（大纲+正文）
04_审核员工作室/审核员{N}/ → 审核员画像
05_模拟读者池/读者{N}/  → 读者画像
06_意见仓库/         → 六类审核/评论记录
07_汇总仓库/         → 阶段/卷/全文汇总
08_已发布/          → 最终成品
```

## 核心文件

- `workflow_state.json` - 状态机文件，所有进度由此驱动
- `.claude/docs/` - 分层文档目录

## 详细文档索引

| 文档 | 内容 |
|------|------|
| `coordination-rules.md` | 部门协调规则 |
| `model-tier-guide.md` | 模型分级指南 |
| `context-management.md` | 上下文管理策略 |
| `design-doc-standards.md` | 设计文档标准 |
| `agent-definitions.md` | Agent 详细定义 |
| `department-rules.md` | 部门详细规则 |
| `workflow-detailed.md` | 工作流详细步骤 |
| `quality-dimensions.md` | 质量检查维度 |

## 当前项目状态

**项目名称**：《星陨纪元》
**当前阶段**：PHASE_6_SUMMARY（分层汇总与终审）
**总章节数**：360章

---

## 调度命令速查

| 场景 | 命令 |
|------|------|
| 启动Agent并记录 | `./run_workflow.sh launch <task> <agent> <desc>` |
| 验证Agent完成 | `./run_workflow.sh verify <task> <task_id>` |
| 批量分配作家 | `./run_workflow.sh assign_batch writer ch001-ch010` |
| 批量分配审核员 | `./run_workflow.sh assign_batch reviewer ch001-ch010` |
| 查看状态 | `./run_workflow.sh status` |
| 查看任务 | `./run_workflow.sh tasks` |
| 问题追踪 | `./run_workflow.sh issue <subcommand>` |

---

## 强制反馈循环（最高优先级）

```
审核完成 → 汇总意见 → 判定需修改？ → 是 → 启动Agent修复 → TaskOutput验证 → 才能标记完成
                                        → 否 → 标记定稿 → 进入下一批次
```

### 三条铁律

| 铁律 | 说明 |
|------|------|
| **禁止跳过** | 审核完成后必须进入修改主持流程，禁止直接标记"完成" |
| **验证闭环** | Agent返回后必须TaskOutput验证，才能改step_status为completed |
| **禁止自改** | 主控不得"自己改文件"，必须通过Agent执行修改 |
```

- [ ] **Step 2: 创建 agent-definitions.md**

```markdown
# Agent 详细定义

## 主控调度 Agent

**定义文件**: `.claude/agents/主控调度.md`

### 职责
- 任务编排：按照工作流步骤依次调度各部门 Agent
- 状态维护：更新 workflow_state.json，记录当前进度
- 大决策确认：大纲审核通过/不通过、卷/全文汇总定稿、死锁仲裁
- 进度汇报：主动向用户汇报当前状态和风险

### 工具权限
- Read, Write, Edit, Bash
- Agent (启动子 Agent)

### 调度约束
- 最大并发 Agent 数：10 个
- 超过时等待已完成后再启动新的
- 每次启动必须记录 task_id

---

## 灵感部门 Agent

**定义文件**: `.claude/agents/灵感部门.md`

### 人数
3 人

### 职责
- 生成立项文件（基础层.yaml + 深度层.md）
- 主编轮值，整合后入库

### 调度方式
- 新项目启动时调用
- 输出到 `01_灵感库/`

---

## 作家部门 Agent

**定义文件**: `.claude/agents/作家部门.md`

### 人数
10 人 (作家A - 作家J)

### 职责
- 正文创作
- 每批 10 章产出
- 自评 S/A/B 级提交

### 调度方式
- 主编统筹，按阶段分配（20-40 章/人）
- 每批 10 章分配给不同作家并行

### 输出
- 章节正文 → `03_内容仓库/04_正文/`
- 审核意见处理 → `06_意见仓库/`

---

## 审核部门 Agent

**定义文件**: `.claude/agents/审核部门.md`

### 人数
11 人 (审核员A - 审核员K)

### 职责
- 批量并行审核
- 技术审核 + 市场审核 + 情感专项审核
- 5 项情感检查维度

### 调度方式
- 每批 10 章，分配 5 名审核员并行
- 汇总意见后判定定稿/修改

---

## 读者部门 Agent

**定义文件**: `.claude/agents/读者部门.md`

### 人数
20 人 (读者A - 读者T)

### 职责
- 模拟读者评论
- 三种类型：吐槽型/分析型/共情型

### 调度方式
- 批量并行，20 人可同时阅读并评论
- 用于收集市场反馈

---

## 汇总部门 Agent

**定义文件**: `.claude/agents/汇总部门.md`

### 人数
3 人

### 职责
- 分层汇总（阶段→卷→全文）
- 主笔 + 编辑 + 校验

### 调度方式
- 串行整合
- 审核发现跨卷问题时调用

---

## Subagent 调度可靠性（强制流程）

### 调度前检查
1. 确认目标文件存在
2. 确认输出目录存在
3. 检查当前 running 任务数（不超过 10 个并发）

### task_id 追踪机制
启动的每个 agent 必须记录：
```json
"agent_tasks": {
    "ch164_fix": {
        "task_id": "a1234567890",
        "agent": "作家A",
        "status": "running",
        "chapter": "ch164",
        "launched_at": "2026-05-14T10:00:00"
    }
}
```

### 验证步骤（强制）
```
启动Agent → 等待5秒 → TaskOutput验证状态
                    ↓
           status=failed → 重新启动（最多3次）
           status=completed → 继续
           无响应 → 等待5秒再试
```
```

- [ ] **Step 3: 创建 department-rules.md**

```markdown
# 部门详细规则

## 并行策略

| 层级 | 并行方式 |
|------|---------|
| 部门内 | Subagent + Team 机制并行 |
| 部门间 | 多终端会话并行 |
| 串行流程 | 单会话内角色切换 |

## 迭代终止条件

| 类型 | 终止条件 |
|------|---------|
| 大纲类 | 全量审核员一致通过 |
| 正文类 | 未解决意见≤5条且无逻辑硬伤 |
| 死锁 | 大纲3轮/正文2轮上限，人工仲裁 |

## 重大决策点（人工确认）

- 大纲审核通过/不通过
- 卷/全文汇总定稿
- 死锁（超迭代上限）

---

## 审核员分配策略

| 审核类型 | 卷/批次 | 审核员/卷 | 并行总数 | 备用 |
|---------|---------|----------|---------|------|
| 大纲审核（3卷） | 每卷2人 | 2 | 6 | 4人待命 |
| 正文审核（批量） | 每批10章 | 5 | 5 | 5人待命 |

## 意见仓库结构

```
06_意见仓库/04_正文_审核/
├── 卷1_阶段1_ch006_审核记录.md  ← 审核员A
├── 卷1_阶段1_ch007_审核记录.md  ← 审核员A
├── 卷1_阶段1_ch008_审核记录.md  ← 审核员B
├── 卷1_阶段1_ch009_审核记录.md  ← 审核员B
└── 卷1_阶段1_ch010_审核记录.md  ← 审核员C
```

---

## 汇总部门职责

| 角色 | 职责 | 调度时机 |
|------|------|---------|
| 汇总主笔 | 执行具体修复 | 卷汇总需微调时 |
| 汇总编辑 | 审核修复结果 | 修复完成后 |
| 汇总校验 | 最终一致性检查 | 编辑通过后 |

**调度条件：当审核发现以下问题时，必须调度汇总部门 Agent：**
- 卷汇总存在逻辑矛盾
- 跨卷时间线/人物状态不一致
- 境界描述/结局走向需修正
```
```

- [ ] **Step 4: 创建 workflow-detailed.md**

```markdown
# 工作流详细步骤

## 25 步闭环工作流

### PHASE_1_LAUNCH (立项)
- STEP_01 → STEP_02

### PHASE_2_OUTLINE (全文大纲迭代)
- STEP_03 → STEP_04 → STEP_05

### PHASE_3_VOLUME (卷大纲迭代)
- STEP_06 → STEP_07 → STEP_08 → STEP_09

### PHASE_4_STAGE (阶段大纲迭代)
- STEP_10 → STEP_11 → STEP_12 → STEP_13

### PHASE_5_BODY (正文创作与双轨反馈)
- STEP_14 → STEP_15 → STEP_16 → STEP_17 → STEP_18

### PHASE_6_SUMMARY (分层汇总与终审)
- STEP_19 → STEP_20 → STEP_21 → STEP_22 → STEP_23 → STEP_24

### PHASE_7_CLOSE (归档闭环)
- STEP_25

---

## 状态机文件结构

`workflow_state.json` 结构：

```json
{
  "version": "v3.0",
  "current_phase": "PHASE_X_Y",
  "current_step": "STEP_XX",
  "phases": {
    "PHASE_X_Y": {
      "steps": {
        "STEP_XX": {
          "status": "completed|in_progress|pending",
          "output": [...],
          "verdict": "通过|不通过|需修改"
        }
      }
    }
  },
  "agent_tasks": {...}
}
```
```

- [ ] **Step 5: 创建 quality-dimensions.md**

```markdown
# 质量检查维度详解

## 10 维质量检查

| 维度 | 类型 | 说明 |
|------|------|------|
| 命名一致性 | 规则 | 文件名chXXX.md与章节内标题一致 |
| 内容完整性 | 规则 | **本章完**标记、字数>=500 |
| 章节重复 | 规则 | 跨章节相似度>80%预警 |
| 人物状态 | 规则 | 性别/生死/形态前后矛盾 |
| 时间线 | 规则 | "年前"与"瞬间"同时出现等错误 |
| 情节关联度 | 规则 | 每段落与前后章节的关联程度 |
| 伏笔回收率 | 规则 | 首次出现元素在后续N章内回收 |
| 场景逻辑 | 规则 | 场景转换合理性、孤岛章节检测 |
| 情感节奏 | 规则 | 情绪波动是否合理 |
| 对话风格 | 规则 | 角色对话字数异常检测 |
| 人物弧光 | **LLM** | 角色弧光完整性（通过Agent调用） |

---

## 情感审核专项（5维）

| 专项 | 检查内容 |
|------|---------|
| 情感弧线检查 | 主角/女主情感转折、婚后日常、情感锚点 |
| 配角价值验证 | 非战力价值、独立支线、道具情感承载 |
| 反派动机深度 | 反派前身、行动逻辑、验证者vs毁灭者 |
| 节奏健康度 | 阶段变速、核心冲突、信息密度 |
| 转折合理性 | 隐瞒必要性、理解铺垫、说服场景 |

## 质量等级

| 等级 | 条件 | 处理 |
|------|------|------|
| S级 | 5项全部通过 | 直接进入下一环节 |
| A级 | 4项通过 + 1项警告 | 修复后复查 |
| B级 | 3项通过 + 2项警告 或 有不通过 | 返回修改 |
| 不合格 | >=3项不通过 | 打回重做 |
```

- [ ] **Step 6: 提交**

```bash
git add CLAUDE.md .claude/docs/agent-definitions.md .claude/docs/department-rules.md .claude/docs/workflow-detailed.md .claude/docs/quality-dimensions.md
git commit -m "refactor: 拆分 CLAUDE.md 为分层文档架构

- CLAUDE.md 精简至 < 200 行
- 新增 agent-definitions.md: Agent 详细定义
- 新增 department-rules.md: 部门详细规则
- 新增 workflow-detailed.md: 工作流详细步骤
- 新增 quality-dimensions.md: 质量检查维度"
```

---

## 改进项 4：质量检查模块化重构

### 目标
将 28 个检查脚本重构为统一基类+插件式架构，减少重复代码。

### 文件结构

```
novel-factory/
├── tools/
│   └── consistency/
│       ├── __init__.py
│       ├── base_checker.py          # 新建：检查器基类
│       ├── checkers/                # 新建：插件化检查器
│       │   ├── __init__.py
│       │   ├── character_state.py
│       │   ├── timeline.py
│       │   ├── naming.py
│       │   ├── content_integrity.py
│       │   ├── duplicate.py
│       │   ├── emotional_rhythm.py
│       │   ├── dialogue_style.py
│       │   ├── plot_device.py
│       │   ├── scene_logic.py
│       │   ├── realm_system.py
│       │   ├── faction_relations.py
│       │   ├── character_arc.py
│       │   ├── battle_density.py
│       │   ├── scene_density.py
│       │   ├── character_activity.py
│       │   └── template_sentences.py
│       ├── quality_engine.py         # 新建：统一调度引擎
│       └── run_quality_checks.py     # 修改：调用新引擎
```

### 任务分解

#### Task 4: 质量检查模块化重构

**Files:**
- Create: `novel-factory/tools/consistency/base_checker.py`
- Create: `novel-factory/tools/consistency/checkers/__init__.py`
- Create: `novel-factory/tools/consistency/checkers/character_state.py`
- Create: `novel-factory/tools/consistency/quality_engine.py`
- Modify: `novel-factory/tools/consistency/run_quality_checks.py`

- [ ] **Step 1: 创建 base_checker.py**

```python
#!/usr/bin/env python3
"""
质量检查器基类
所有检查器必须继承此类
"""
from abc import ABC, abstractmethod
from dataclasses import dataclass, field
from typing import List, Dict, Optional
from pathlib import Path


@dataclass
class Issue:
    """问题数据类"""
    chapter_id: str
    severity: str  # P0, P1, P2
    issue_type: str
    description: str
    location: Optional[str] = None  # 具体位置（如行号）
    suggestion: Optional[str] = None  # 修复建议


class BaseChecker(ABC):
    """检查器基类"""

    # 检查器元信息（子类必须设置）
    name: str = "base_checker"
    description: str = "基础检查器"
    severity: str = "P1"

    def __init__(self, chapters_dir: str):
        self.chapters_dir = Path(chapters_dir)
        self.issues: List[Issue] = []

    @abstractmethod
    def check_chapter(self, chapter_path: Path) -> List[Issue]:
        """
        检查单个章节
        子类必须实现此方法
        """
        pass

    def check_all(self) -> List[Issue]:
        """检查所有章节"""
        self.issues = []
        chapter_files = sorted(self.chapters_dir.glob("ch*.md"))

        for chapter_path in chapter_files:
            chapter_issues = self.check_chapter(chapter_path)
            self.issues.extend(chapter_issues)

        return self.issues

    def get_issues_by_severity(self, severity: str) -> List[Issue]:
        """按严重程度筛选问题"""
        return [i for i in self.issues if i.severity == severity]

    def get_issues_by_chapter(self, chapter_id: str) -> List[Issue]:
        """按章节筛选问题"""
        return [i for i in self.issues if i.chapter_id == chapter_id]

    def report(self) -> str:
        """生成报告"""
        if not self.issues:
            return f"✓ {self.name}: 未发现问题"

        lines = [f"✗ {self.name}: 发现 {len(self.issues)} 个问题"]
        for issue in self.issues:
            lines.append(
                f"  [{issue.severity}] {issue.chapter_id}: {issue.issue_type} - {issue.description}"
            )
        return "\n".join(lines)
```

- [ ] **Step 2: 创建 checkers/character_state.py**

```python
#!/usr/bin/env python3
"""
人物状态检查器
继承 BaseChecker，实现检查逻辑
"""
import re
from pathlib import Path
from typing import List, Optional
from dataclasses import dataclass

from base_checker import BaseChecker, Issue


# 人物追踪表
CHARACTER_TRACKER = ["星月", "小九", "铁蛋", "林夜", "苏琳", "墨白"]

# 生死指示词
ALIVE_PATTERNS = {
    True: ['活着', '苏醒', '复活', '重生'],
    False: [
        r'死了', r'陨落', r'消亡', r'化为星光', r'消散',
        r'牺牲(?![的地带])',
    ],
}


class CharacterStateChecker(BaseChecker):
    """人物状态一致性检查器"""

    name = "character_state"
    description = "检查人物性别/生死/形态状态一致性"
    severity = "P0"

    def check_chapter(self, chapter_path: Path) -> List[Issue]:
        """检查单个章节的人物状态"""
        issues = []
        chapter_id = chapter_path.stem  # e.g., "ch001"

        with open(chapter_path, 'r', encoding='utf-8') as f:
            content = f.read()

        # 提取章节编号用于排序
        num = int(re.search(r'ch(\d+)', chapter_id).group(1))

        # 检查每个人物
        for character in CHARACTER_TRACKER:
            if character not in content:
                continue

            # 检查性别一致性
            gender = self._check_gender(content, character)
            if gender:
                # TODO: 与上一个已知的性别状态比较
                pass

            # 检查生死状态
            alive_status = self._check_alive(content, character)
            if alive_status is not None:
                # TODO: 与上一个已知的生死状态比较
                pass

        return issues

    def _check_gender(self, content: str, character: str) -> Optional[str]:
        """检测性别"""
        pattern = character + r'.{0,30}'
        matches = re.finditer(pattern, content)
        for m in matches:
            text = content[m.start():m.end()]
            if re.search(r'他[是|说|道|看|想|觉|笑|冷|沉]', text):
                return 'male'
            if re.search(r'她[是|说|道|看|想|觉|笑|冷|沉]', text):
                return 'female'
        return None

    def _check_alive(self, content: str, character: str) -> Optional[bool]:
        """检测生死状态"""
        for alive, patterns in ALIVE_PATTERNS.items():
            for pattern in patterns:
                if re.search(f'{character}.*{pattern}', content):
                    return alive
        return None
```

- [ ] **Step 3: 创建 quality_engine.py**

```python
#!/usr/bin/env python3
"""
质量检查引擎
统一调度所有检查器
"""
import argparse
from pathlib import Path
from typing import List, Dict

from base_checker import BaseChecker, Issue


class QualityEngine:
    """质量检查引擎"""

    def __init__(self, chapters_dir: str):
        self.chapters_dir = Path(chapters_dir)
        self.checkers: List[BaseChecker] = []

    def register_checker(self, checker: BaseChecker):
        """注册检查器"""
        self.checkers.append(checker)

    def register_all_checkers(self):
        """注册所有检查器（自动发现）"""
        from checkers.character_state import CharacterStateChecker
        from checkers.naming import NamingChecker
        from checkers.content_integrity import ContentIntegrityChecker
        # ... 动态导入其他检查器

        self.register_checker(CharacterStateChecker(self.chapters_dir))
        self.register_checker(NamingChecker(self.chapters_dir))
        self.register_checker(ContentIntegrityChecker(self.chapters_dir))
        # ...

    def run_all(self) -> Dict[str, List[Issue]]:
        """运行所有检查器"""
        results = {}
        for checker in self.checkers:
            issues = checker.check_all()
            results[checker.name] = issues
            print(checker.report())
        return results

    def run_specific(self, checker_names: List[str]) -> Dict[str, List[Issue]]:
        """运行指定的检查器"""
        results = {}
        for name in checker_names:
            for checker in self.checkers:
                if checker.name == name:
                    issues = checker.check_all()
                    results[name] = issues
                    print(checker.report())
                    break
        return results

    def summary(self, results: Dict[str, List[Issue]]) -> str:
        """生成汇总报告"""
        total = sum(len(issues) for issues in results.values())
        by_severity = {"P0": 0, "P1": 0, "P2": 0}

        for issues in results.values():
            for issue in issues:
                by_severity[issue.severity] = by_severity.get(issue.severity, 0) + 1

        lines = [
            "=== 质量检查汇总 ===",
            f"总问题数: {total}",
            f"  P0 (严重): {by_severity['P0']}",
            f"  P1 (中等): {by_severity['P1']}",
            f"  P2 (轻微): {by_severity['P2']}",
        ]
        return "\n".join(lines)


def main():
    parser = argparse.ArgumentParser(description="质量检查引擎")
    parser.add_argument("--chapters-dir", required=True, help="章节目录路径")
    parser.add_argument("--checkers", nargs="+", help="指定检查器名称")
    parser.add_argument("--all", action="store_true", help="运行所有检查器")

    args = parser.parse_args()

    engine = QualityEngine(args.chapters_dir)
    engine.register_all_checkers()

    if args.all:
        results = engine.run_all()
    elif args.checkers:
        results = engine.run_specific(args.checkers)
    else:
        print("请指定 --all 或 --checkers")
        return

    print("\n" + engine.summary(results))


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: 提交**

```bash
git add tools/consistency/base_checker.py tools/consistency/quality_engine.py
git commit -m "refactor: 质量检查模块化重构（第一阶段）

- 创建 BaseChecker 基类
- 创建 QualityEngine 统一调度引擎
- 初步实现 CharacterStateChecker"
```

---

## 改进项 5-9 实施方案概要

### 改进项 5：模板系统

**目标**：创建标准化文档模板

**Files:**
- Create: `novel-factory/docs/templates/chapter-template.md`
- Create: `novel-factory/docs/templates/outline-template.md`
- Create: `novel-factory/docs/templates/review-report-template.md`
- Create: `novel-factory/docs/templates/character-profile-template.md`
- Create: `novel-factory/docs/templates/release-checklist.md`

**内容要点**：
- chapter-template.md：包含章节标准结构（标题、开头、结尾标记、字数统计）
- outline-template.md：包含大纲标准格式（阶段目标、关键情节点、人物弧光）
- review-report-template.md：包含审核报告标准格式（问题列表、严重程度、修复建议）
- character-profile-template.md：包含人物卡片标准格式（基本信息、性格特征、关系网络）
- release-checklist.md：包含发布前检查清单

---

### 改进项 6：Skills 技能系统

**目标**：创建 slash command 技能

**Files:**
- Create: `novel-factory/.skills/_global/workflow-controller/`
- Create: `novel-factory/.skills/start/`
- Create: `novel-factory/.skills/quality-report/`
- Create: `novel-factory/.skills/new-chapter/`
- Create: `novel-factory/.skills/consistency-check/`
- Create: `novel-factory/.skills/publish/`

**内容要点**：
- 每个 skill 包含 `SKILL.md` 文件
- 使用 Skill 工具调用
- 参考 Claude-Code-Game-Studios 的 37 skills 结构

---

### 改进项 7：测试体系

**目标**：为核心脚本添加单元测试

**Files:**
- Create: `novel-factory/tests/test_workflow_state.py`
- Create: `novel-factory/tests/test_quality_checker.py`
- Create: `novel-factory/tests/test_consistency.py`
- Create: `novel-factory/tests/fixtures/sample_chapter.md`
- Create: `novel-factory/tests/fixtures/workflow_state_v3.json`

**内容要点**：
- 使用 pytest 框架
- Arrange-Act-Assert 结构
- 80% 覆盖率目标

---

### 改进项 8：部门协调协议

**目标**：创建详细的部门间协调文档

**Files:**
- Create: `novel-factory/docs/部门协调协议.md`
- Create: `novel-factory/docs/工作流模式库.md`

**内容要点**：
- 9 种工作流模式定义
- 部门间交接标准流程
- 冲突升级路径

---

### 改进项 9：版本发布规范化

**目标**：规范版本发布流程

**Files:**
- Create: `novel-factory/releases/v5.0-2026-05-18/changelog.md`
- Create: `novel-factory/releases/v5.0-2026-05-18/checklist.md`
- Modify: `novel-factory/tools/publish/run_publish.sh`

**内容要点**：
- releases 目录按版本组织
- 每个版本包含 changelog.md
- 发布检查清单自动化
- git tag 集成

---

## 实施顺序建议

1. **P0 (立即实施)**:
   - 改进项 1: Hooks 系统
   - 改进项 2: 问题追踪结构化

2. **P1 (短期实施)**:
   - 改进项 3: CLAUDE.md 拆分
   - 改进项 4: 质量检查模块化重构

3. **P2 (中期实施)**:
   - 改进项 5: 模板系统
   - 改进项 6: Skills 技能系统

4. **P3 (长期实施)**:
   - 改进项 7: 测试体系
   - 改进项 8: 部门协调协议
   - 改进项 9: 版本发布规范化

---

## 附录：Claude-Code-Game-Studios 关键参考点

### 48 个 Agent 分类

| 层级 | 数量 | 示例 |
|------|------|------|
| Tier 1 (Opus) | 3 | creative-director, technical-director, producer |
| Tier 2 (Sonnet) | 8 | game-designer, art-director, audio-director, narrative-director |
| Tier 3 (Sonnet/Haiku) | 37 | gameplay-programmer, ui-programmer, ai-programmer |

### 37 个 Skills

- `/start` - 智能启动
- `/sprint-plan` - 冲刺计划
- `/team-combat` - 团队协作（6 agent 并行）
- `/design-review` - 设计审查
- `/code-review` - 代码审查
- `/playtest-report` - 测试报告
- 等

### 8 个 Hooks

- `session-start.sh` - 会话开始
- `session-stop.sh` - 会话结束
- `detect-gaps.sh` - 检测缺失
- `validate-commit.sh` - 提交验证
- `validate-push.sh` - 推送验证
- `validate-assets.sh` - 资源验证
- `pre-compact.sh` - 压缩前
- `log-agent.sh` - Agent 记录

### 11 个 Path-Scoped Rules

- `gameplay-code.md` → `src/gameplay/**`
- `engine-code.md` → `src/core/**`
- `ai-code.md` → `src/ai/**`
- `network-code.md` → `src/networking/**`
- `ui-code.md` → `src/ui/**`
- `design-docs.md` → `design/gdd/**`
- `test-standards.md` → `tests/**`
- `shader-code.md` → `assets/shaders/**`
- `prototype-code.md` → `prototypes/**`
- `data-files.md` → `assets/data/**`
- `narrative.md` → `design/narrative/**`