# Phase 8.43 Build Tooling Upgrade — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 3 build-tooling major version bumps (Vue 3.4→3.5, ESLint 8→10 + flat config, eslint-plugin-vue 9→10) + 2 config-only commits (vitest.config.js `pool: 'forks'` + `coverage/**` exclude, tests/unit/setup.ts `enableAutoUnmount(afterEach)`) in 7 sub-phases, **6 commits 推送 origin/master** (Task 1-6 各 1 commit, Task 7 = push + memory sync, 0 commit 因为 phases-8-dashboard.md 在 `~/.claude/...` 不在 git repo). 2 commits use `--no-verify` (8.43.4 + 8.43.5 ESLint 迁移期 bypass, 8.43.6 恢复 hook). **Plan deviation**: Vitest 5 stable 未发布 (latest=4.1.8, beta=5.0.0-beta.4 only) + @vue/test-utils 3 未发布 (latest=2.4.11). 主公 2026-06-09 决策: vitest 4.1.8 维持 + test-utils 2.4.11 维持, 那 2 个 dep bump 跳过; ESLint 8→10 (skip 9 maintenance, 跳到真正 latest 10.4.1, peerDeps verified 跟 vue-eslint-parser + eslint-plugin-vue 10 兼容).

> **Spec deviation note**: Spec 8.43.7 说 "7 commits", 但 phases-8-dashboard.md 在 `~/.claude/projects/-home-ailearn-.../memory/` 不在 git repo, 实际 Task 7 = push + memory sync (0 commit). 真实 6 commits push. 主公 review spec 时已批准 "7 commits" 形式, plan 改 6 commits 是精确化, 不改 spec.

**Architecture:** 跟 Phase 8.35-8.42 super-phase 同模式 (1 spec + 1 plan + 6 sub-phase impl + 1 push + memory sync), 低 risk 先 (8.43.1 Vue → 8.43.2 Vitest → 8.43.3 test-utils) / 高 risk 走 `--no-verify` 集中 (8.43.4 ESLint 8→9 + 8.43.5 vue 9→10) / 综合 verify (8.43.6 改 lint:all + 删 3 .cjs) / memory sync + push (8.43.7). 1 个 `eslint.config.js` 合并 3 个 `.eslintrc.cjs` (Phase 8.33+8.36+8.42 累计) 走 ESLint 9 flat config 1 入口.

**Tech Stack:** Vue 3.5 / Vitest 4.1.8 (维持, 5 stable 未发布) / @vue/test-utils 2.4.11 (维持, 3 未发布) / ESLint 10.4.1 (flat config) / eslint-plugin-vue 10.9.2 / Node 20.20 / pnpm 9.15.0 (跟 Phase 8.40 corepack 兼容) / vue-eslint-parser 10.x (peerDep `^8.57 || ^9 || ^10` 兼容) / @typescript-eslint/parser 7 / eslint-plugin-local-rules 1.x.

**Spec:** `docs/superpowers/specs/2026-06-09-phase8.43-build-tooling-upgrade-design.md` (commit `84e63d9`)

---

## Context

Phase 8.35-8.42 收尾 dashboard polish 8 sub-phases (byTestid helper / testid-class-sync lint / DecisionCard 7 kind testid / page-level spec / husky pre-commit / GitHub Actions CI / eslint-plugin-vue 集成), 累计 33 data-testid / 16 vitest spec / 95 tests / 3 ESLint config / 1 husky hook / 1 GitHub Actions workflow. 12+ followup items 跨 4 子系统 (build tooling / test infra / UI / TS strict), 拆 4 super-phase, Phase 8.43 = build tooling 5 major bump.

**Verified baselines (2026-06-09, master @ `84e63d9`)**:
- `pnpm lint:testid` → 10 errors (Phase 8.33 fixture 软警告)
- `pnpm lint:testid-sync` → 7 warnings, 0 errors
- `pnpm lint:vue` → 212 warnings, 0 errors
- `pnpm lint:all` → EXIT=0 (`;` separator 防 lint:testid errors 阻塞)
- `pnpm vitest run` → 16 spec files / 95 tests passed
- `pnpm build` → 0 error
- `pytest -q` (backend) → 2262 passed, 0 failed, 27 skipped
- `ruff check .` → 0 issues
- `data-testid` 数量 → 46 (grep `-rEn 'data-testid=' src/`)
- corepack `pnpm@9.15.0` → OK (workaround Phase 8.40 Node 20.20 兼容)
- 0 Co-Authored-By trailers in recent commits (跟 CLAUDE.md global config 一致)

---

## File Map

### Created
- `dashboard/frontend/eslint.config.js` (~100L, NEW, 8.43.4) — 1 file 3 blocks (Block 1 spec dir / Block 2 .vue testid-sync / Block 3 .vue vue plugin) 替换 3 .eslintrc.cjs

### Modified
- `dashboard/frontend/package.json` (5 devDep bump + lint scripts 改)
- `dashboard/frontend/pnpm-lock.yaml` (lock file 自动跟 pnpm install 更新)
- `dashboard/frontend/vitest.config.js` (+ `pool: 'forks'` + `coverage/**` exclude)
- `dashboard/frontend/tests/unit/setup.ts` (+ `enableAutoUnmount(afterEach)`)
- `~/.claude/projects/.../memory/phases-8-dashboard.md` (H1 + L3 + L4 edit + 8.43 entry append)

### Deleted (8.43.6)
- `dashboard/frontend/.eslintrc.cjs` (Phase 8.33)
- `dashboard/frontend/.eslintrc-testid-sync.cjs` (Phase 8.36)
- `dashboard/frontend/.eslintrc-vue.cjs` (Phase 8.42)

### NOT modified
- 14 .vue in `dashboard/frontend/src/` (Vue 3.5 0 改)
- 16 vitest spec in `dashboard/frontend/tests/unit/` (test-utils 3 0 改 signature)
- `dashboard/frontend/eslint-rules/testid-class-sync.js` (Phase 8.36, 0 改)
- `dashboard/frontend/eslint-rules/no-class-selector-in-test.js` (Phase 8.33, 0 改)
- 5 composables / `api/index.js` / `.husky/pre-commit` (Phase 8.40, 0 改) / `index.html` / `vite.config.js`
- backend Python (agent_system / dashboard / cost_tracker / cost_persistence / cost_router)
- 9 historical spec/plan docs (`:NNN` 行号不动)
- 12 SCENARIOS 路由 (Python side)
- CLAUDE.md / pyproject.toml / pytest.ini / `.github/workflows/dashboard-frontend-ci.yml`
- `.state/*.db` (gitignored) / `.env` (0 改 0 泄漏)

---

## Pre-Execution Setup

每个 task 都用同 pattern:
1. 读 baseline state (确认 before snapshot)
2. 改文件 (specific code 在 step 里)
3. 装新 devDep (`corepack pnpm@9.15.0 install`)
4. 跑 verification (commands + expected output)
5. Commit (`feat`/`chore`/`docs` conventional, 0 Co-Authored-By)

**TDD note**: 5 build-tooling bump 不引新 feature, TDD 走 1 swap + smoke test pattern (改 dep, 跑现有 95 tests 确认 0 break). 8.43.4 + 8.43.5 走 dry-run 验 (import flat config / lint 1 sample file) 防 syntax 错。

**Critical safety** (跟 Phase 8.35-8.42 policy 一致):
- 0 force-push
- 0 amend (除 sub-phase fail 救火显式声明)
- 0 Co-Authored-By footer (跟 global CLAUDE.md config)
- 0 改 .env / 0 API key 泄漏
- 0 改 .state/*.db (gitignored)
- 0 改 historical spec/plan docs `:NNN` 行号
- 0 改 12 SCENARIOS 路由
- 0 改 CLAUDE.md / pyproject.toml / pytest.ini
- 0 改 vitest.config.js 结构 (除 task 2 加 2 字段)

---

## Task 1: 8.43.1 — Vue 3.4→3.5 (low risk, hook ✅)

**Files:**
- Modify: `dashboard/frontend/package.json:20` (devDependency)
- Modify: `dashboard/frontend/pnpm-lock.yaml` (auto by pnpm install)

- [ ] **Step 1: 验证 pre-state**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
grep '"vue":' package.json
# 期望: "vue": "^3.4.0",
git status
# 期望: clean (working tree 0 uncommitted changes)
```

- [ ] **Step 2: Bump vue to ^3.5.0**

Edit `package.json` L20, change:
```diff
-    "vue": "^3.4.0",
+    "vue": "^3.5.0",
```

- [ ] **Step 3: Install (跟 Phase 8.40 corepack pattern)**

```bash
corepack pnpm@9.15.0 install
```

Expected: `Done in N.Ns` + 0 errors. `pnpm-lock.yaml` auto-updated.

- [ ] **Step 4: 验证 vitest baseline 维持**

```bash
corepack pnpm@9.15.0 vitest run 2>&1 | tail -5
# 期望: Test Files 16 passed (16) / Tests 95 passed (95) / 0 failed
```

- [ ] **Step 5: 验证 pnpm build 0 error**

```bash
corepack pnpm@9.15.0 build 2>&1 | tail -5
# 期望: ✓ built in N.NNs / dist/ emitted
```

- [ ] **Step 6: 验证 0 改 .vue (spec 隐含检查: 16 spec 全过隐含 Vue 3.5 backward compat)**

```bash
git diff --stat src/ tests/unit/
# 期望: 0 files changed (0 改 .vue / 0 改 spec)
```

- [ ] **Step 7: Commit**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git add dashboard/frontend/package.json dashboard/frontend/pnpm-lock.yaml
git commit -m "chore(deps): phase 8.43.1 — vue 3.4→3.5 (backward compat)

0 改 .vue / 0 改 spec. vitest 16 spec / 95 tests all passed, pnpm build 0
error. Vue 3.5 0 用新 API (useTemplateRef / defineModel / useId 留 followup).
下一步: 8.43.2 vitest 4→5 + pool: forks + coverage exclude."
```

---

## Task 2: 8.43.2 — vitest.config.js `pool: 'forks'` + coverage exclude (config-only, hook ✅)

**Files:**
- Modify: `dashboard/frontend/vitest.config.js` (+ `pool: 'forks'` + `coverage/**` exclude)
- Modify: `dashboard/frontend/pnpm-lock.yaml` (auto by pnpm install via `pnpm install --lockfile-only` if any, else no change)

> **Plan deviation**: Vitest 5 stable 未发布 (latest=4.1.8, beta=5.0.0-beta.4 only). 主公决策: vitest ^4.1.8 维持, dep bump 跳过. Task 2 收窄为 config-only commit, vitest.config.js 加 2 字段.

- [ ] **Step 1: 验证 pre-state**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
grep '"vitest":' package.json
cat vitest.config.js
# 期望: "vitest": "^4.1.8", + vitest.config.js 0 包含 pool / coverage
```

- [ ] **Step 2: Edit vitest.config.js — 加 pool + coverage exclude**

Edit `dashboard/frontend/vitest.config.js`, replace `defineConfig({` block (L11-20) with:

```js
export default mergeConfig(viteConfig, defineConfig({
  test: {
    environment: 'jsdom',
    globals: true,
    // Phase 8.30: tests/unit/setup.ts 集中 stub echarts 等 jsdom-incompatible 模块
    setupFiles: ['./tests/unit/setup.ts'],
    include: ['src/**/*.spec.{js,ts}', 'tests/unit/**/*.spec.{js,ts}'],
    // Phase 8.30: ceremonial Playwright specs (.spec.js) 仍保留作契约文档,
    // 但走 vitest 跑的是 tests/unit/ + src/ 下的真 e2e tests
    exclude: ['node_modules', 'dist', 'tests/e2e/**', 'coverage/**'],
    // Phase 8.43.2: pool: 'forks' jsdom fork-isolate 防 OOM (跟 Phase 6.4 pytest
    // forks 镜像, 多 spec 跑不串). coverage/** exclude 给 Phase 8.44+ 留占位.
    pool: 'forks',
  },
}))
```

- [ ] **Step 3: Install (vitest.config.js 改触 lockfile diff, 跑 pnpm install 同步)**

```bash
corepack pnpm@9.15.0 install
```

Expected: `Done in N.Ns` + 0 errors (vitest 4.1.8 已装, lockfile 同步).

- [ ] **Step 4: 验证 vitest baseline 维持 (跑 1 次)**

```bash
corepack pnpm@9.15.0 vitest run 2>&1 | tail -5
# 期望: Test Files 16 passed (16) / Tests 95 passed (95) / 0 failed
# 关键: pool: 'forks' jsdom fork-isolate, 16 spec 全过
```

- [ ] **Step 5: 验证 0 flake (跑第 2 次)**

```bash
corepack pnpm@9.15.0 vitest run 2>&1 | tail -3
# 期望: 95 tests passed (跟 step 4 一致, 0 flake)
```

- [ ] **Step 6: 验证 0 改 .vue / 0 改 spec / 0 改 setup.ts**

```bash
git diff --stat src/ tests/unit/setup.ts
# 期望: 0 files changed
```

- [ ] **Step 7: Commit**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git add dashboard/frontend/vitest.config.js dashboard/frontend/pnpm-lock.yaml
git commit -m "chore(config): phase 8.43.2 — vitest.config.js pool: forks + coverage exclude

Vitest 5 stable 未发布 (latest=4.1.8), 主公决策 维持 ^4.1.8. dep bump 跳过,
config-only commit. vitest.config.js 加 pool: 'forks' (jsdom fork-isolate 防
OOM, 跟 Phase 6.4 pytest forks 镜像) + coverage/** exclude (给 Phase 8.44+
coverage 留占位). vitest 16 spec / 95 tests all passed (跑 2 次 0 flake),
0 改 .vue / 0 改 spec / 0 改 setup.ts / 0 dep bump.
下一步: 8.43.3 setup.ts 加 enableAutoUnmount (test-utils 3 也未发布, 维持
^2.4.11)."
```

---

## Task 3: 8.43.3 — tests/unit/setup.ts `enableAutoUnmount(afterEach)` (config-only, hook ✅)

**Files:**
- Modify: `dashboard/frontend/tests/unit/setup.ts` (+ `enableAutoUnmount(afterEach)`)

> **Plan deviation**: @vue/test-utils 3 未发布 (latest=2.4.11). 主公决策: ^2.4.11 维持, dep bump 跳过. Task 3 收窄为 config-only commit, setup.ts 加 1 行 (`enableAutoUnmount` 在 v2.4+ 已存在, 不是 v3 独有).

- [ ] **Step 1: 验证 pre-state**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
grep '"@vue/test-utils":' package.json
cat tests/unit/setup.ts | head -10
# 期望: "@vue/test-utils": "^2.4.11", + setup.ts 0 包含 enableAutoUnmount
```

- [ ] **Step 2: Edit setup.ts — 加 enableAutoUnmount (v2.4+ 已支持, 防 wrapper 泄漏)**

Edit `dashboard/frontend/tests/unit/setup.ts`, 在 L8 `import { vi } from 'vitest'` 之后加 2 行, L9 后插:

```ts
import { vi } from 'vitest'
import { afterEach } from 'vitest'
import { enableAutoUnmount } from '@vue/test-utils'
// Phase 8.43.3: test-utils 3 推荐, afterEach 自动 unmount 全部 wrapper, 防
// wrapper 泄漏 + Vue 3.5 onUnmounted 同步. 0 改 16 spec mount() 基础 API 签名.
enableAutoUnmount(afterEach)
```

(注: 4 行 import + 1 行 enableAutoUnmount call = +5 行)

- [ ] **Step 3: 验证 vitest baseline 维持 (16 spec + 95 tests + 0 fail)**

```bash
corepack pnpm@9.15.0 vitest run 2>&1 | tail -5
# 期望: Test Files 16 passed (16) / Tests 95 passed (95) / 0 failed
# 关键: enableAutoUnmount(afterEach) v2.4+ 已支持, 16 spec mount() 0 改
```

- [ ] **Step 4: 验证 0 改 16 spec (mount API 签名 0 改)**

```bash
git diff --stat tests/unit/ 'src/**/*.spec.ts'
# 期望: 0 files changed (除 setup.ts 5 行加)
```

- [ ] **Step 5: Commit**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git add dashboard/frontend/tests/unit/setup.ts
git commit -m "chore(config): phase 8.43.3 — setup.ts enableAutoUnmount (test-utils 维持 2.4.11)

@vue/test-utils 3 未发布 (latest=2.4.11), 主公决策 维持 ^2.4.11. dep bump
跳过, config-only commit. tests/unit/setup.ts 加 enableAutoUnmount(afterEach)
(v2.4+ 已支持, 防 wrapper 泄漏, 跟 Vue 3.5 onUnmounted 同步). vitest 16 spec /
95 tests all passed, 0 改 16 spec (mount API 0 改).
下一步: 8.43.4 eslint 8→10 + flat config (--no-verify)."
```

---

## Task 4: 8.43.4 — ESLint 8→10 + `eslint.config.js` (high risk, **--no-verify**)

> **Plan deviation**: ESLint 9 在 maintenance, 主公决策 8→10 (跳 9, 走真正 latest 10.4.1, peerDeps 验证 vue-eslint-parser + eslint-plugin-vue 10 都兼容 `^8.57 || ^9 || ^10`).

**Files:**
- Modify: `dashboard/frontend/package.json:28` (devDependency)
- Create: `dashboard/frontend/eslint.config.js` (~100L, NEW, 1 file 3 blocks)
- Modify: `dashboard/frontend/pnpm-lock.yaml` (auto by pnpm install)

- [ ] **Step 1: 验证 pre-state**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
grep '"eslint":' package.json
ls -la .eslintrc*.cjs
# 期望: "eslint": "^8.57.0", + 3 .cjs 文件都在 (lint 入口维持)
```

- [ ] **Step 2: Bump eslint to ^10.0.0**

Edit `package.json` L28, change:
```diff
-    "eslint": "^8.57.0",
+    "eslint": "^10.0.0",
```

- [ ] **Step 3: Install**

```bash
corepack pnpm@9.15.0 install
```

Expected: `Done in N.Ns` + 0 errors (lockfile resolves 10.4.1, peerDep 跟 vue-eslint-parser 10 + eslint-plugin-vue 10 兼容).

- [ ] **Step 4: Create eslint.config.js (1 file 3 blocks, ~100L)**

Create new file `dashboard/frontend/eslint.config.js` with this exact content:

```js
// eslint.config.js — Phase 8.43.4
// 1 file 3 blocks: Block 1 spec dir (Phase 8.33) + Block 2 .vue testid-class-sync
// (Phase 8.36) + Block 3 .vue vue plugin (Phase 8.42). Replaces 3 .eslintrc.cjs
// + 走 ESLint 9 flat config native (0 走 dual mode).
import vuePlugin from 'eslint-plugin-vue'
import localRules from 'eslint-plugin-local-rules'
import vueParser from 'vue-eslint-parser'
import tsParser from '@typescript-eslint/parser'

export default [
  // Global ignores
  {
    ignores: [
      'node_modules/**',
      'dist/**',
      'tests/e2e/**',
      'coverage/**',
    ],
  },

  // Block 1: Spec dir (Phase 8.33 — tests/unit/**/*.spec.ts + fixture)
  {
    files: [
      'tests/unit/**/*.spec.ts',
      'tests/fixtures/lint-testid/*.spec.ts',
    ],
    languageOptions: {
      parser: tsParser,
      ecmaVersion: 2022,
      sourceType: 'module',
    },
    plugins: { 'local-rules': localRules },
    rules: {
      'local-rules/no-class-selector-in-test': 'error',
    },
  },

  // Block 2: .vue testid-class-sync (Phase 8.36 — 走 vue-eslint-parser + TS)
  {
    files: ['src/**/*.vue'],
    languageOptions: {
      parser: vueParser,
      parserOptions: {
        parser: tsParser,
        ecmaVersion: 2022,
        sourceType: 'module',
      },
    },
    plugins: { 'local-rules': localRules },
    rules: {
      'local-rules/testid-class-sync': 'warn',
    },
  },

  // Block 3: .vue eslint-plugin-vue (Phase 8.42 — vue3-recommended + 4 rule override)
  {
    files: ['src/**/*.vue'],
    languageOptions: {
      parser: vueParser,
      parserOptions: {
        parser: tsParser,
        ecmaVersion: 2022,
        sourceType: 'module',
      },
    },
    plugins: { vue: vuePlugin },
    ...vuePlugin.configs['flat/vue3-recommended'],
    rules: {
      'vue/multi-word-component-names': 'error',
      // WorkflowGraph.vue 用 mermaid 库输出 SVG (v-html=svg), mermaid render 是
      // trusted, 但 rule 视角是 XSS 表面. 留 warn (非 error) 提醒 0 阻塞 baseline.
      'vue/no-v-html': 'warn',
      'vue/require-default-prop': 'error',
      // WorkflowGraph.vue: 'mermaid' 同名 prop + import, 实际渲染 OK (import 在
      // props 后续用), 但 rule 报 name collision. 留 warn 提醒 0 阻塞.
      'vue/no-dupe-keys': 'warn',
    },
  },
]
```

- [ ] **Step 5: 验证 ESLint 10 version + dry-run import**

```bash
./node_modules/.bin/eslint --version
# 期望: v10.x.x
node -e "import('./eslint.config.js').then(c => console.log('blocks:', c.default.length))"
# 期望: blocks: 3
```

- [ ] **Step 6: Commit (--no-verify, ESLint 10 迁移期 lint:all 必 fail)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git add dashboard/frontend/package.json dashboard/frontend/pnpm-lock.yaml dashboard/frontend/eslint.config.js
git commit --no-verify -m "chore(lint): phase 8.43.4 — eslint 8→10 + flat config 1 file 3 blocks (--no-verify)

ESLint 8→10 (skip 9 maintenance, 跳到真正 latest 10.4.1, peerDeps verified
跟 vue-eslint-parser 10 + eslint-plugin-vue 10 兼容). flat config native, 0
走 dual mode. 新 eslint.config.js 1 file 3 blocks: Block 1 spec dir (Phase
8.33 local-rules/no-class-selector-in-test) + Block 2 .vue testid-class-sync
(Phase 8.36 local-rules/testid-class-sync) + Block 3 .vue vue plugin (Phase
8.42 vue3-recommended + 4 rule override). 3 .eslintrc.cjs 仍存在 (0 删,
8.43.6 删) 但 ESLint 10 0 读.

--no-verify: ESLint 10 迁移期 lint:all 必 fail (3 .cjs 跟 flat config 互斥).
下一步 8.43.5 (vue 9→10) 同走 --no-verify, 8.43.6 改 lint:all 走 eslint.config.js
入口 + 删 3 .cjs 1 commit 恢复 hook."
```

---

## Task 5: 8.43.5 — eslint-plugin-vue 9→10 (high risk, **--no-verify**)

**Files:**
- Modify: `dashboard/frontend/package.json:30` (devDependency)
- Modify: `dashboard/frontend/pnpm-lock.yaml` (auto by pnpm install)

- [ ] **Step 1: 验证 pre-state (8.43.4 已 commit)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git log --oneline -1
# 期望: chore(lint): phase 8.43.4 — eslint 8→9 + flat config ... (--no-verify)
```

- [ ] **Step 2: Bump eslint-plugin-vue to ^10.0.0**

Edit `dashboard/frontend/package.json` L30, change:
```diff
-    "eslint-plugin-vue": "^9.33.0",
+    "eslint-plugin-vue": "^10.0.0",
```

- [ ] **Step 3: Install**

```bash
corepack pnpm@9.15.0 install
```

Expected: `Done in N.Ns` + 0 errors.

- [ ] **Step 4: 验证 vue 10 跟 ESLint 10 兼容 (Block 3 dry-run on DecisionCard.vue)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
./node_modules/.bin/eslint --config eslint.config.js src/components/DecisionCard.vue 2>&1 | tail -5
# 期望: 0 errors, baseline warnings (跟 Phase 8.42 一致, vue 10 兼容 ESLint 10 + flat config 1:1)
```

- [ ] **Step 5: 验证 Block 2 dry-run (testid-class-sync 7 warnings baseline 维持)**

```bash
./node_modules/.bin/eslint --config eslint.config.js src/components/WorkflowGraph.vue 2>&1 | tail -3
# 期望: 0 errors, 2 warnings (testid-class-sync 跟 Phase 8.36 baseline 一致)
```

- [ ] **Step 6: Commit (--no-verify, ESLint 10 迁移期 lint:all 仍 fail)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git add dashboard/frontend/package.json dashboard/frontend/pnpm-lock.yaml
git commit --no-verify -m "chore(lint): phase 8.43.5 — eslint-plugin-vue 9→10 (--no-verify)

eslint-plugin-vue 9→10 (跟 ESLint 10 兼容, peerDep verified ^8.57 || ^9 ||
^10, vue3-recommended 1:1 mirror). 0 改 eslint.config.js Block 3 (10 →
vue3-recommended 0 API 漂). 0 改 .vue / 0 改 spec. Block 3 dry-run on
DecisionCard.vue 0 errors baseline 维持.

--no-verify: 跟 8.43.4 同因, ESLint 10 迁移期 lint:all 必 fail. 下一步 8.43.6
改 lint:all 走 eslint.config.js 入口 + 删 3 .cjs 1 commit 恢复 hook."
```

---

## Task 6: 8.43.6 — lint:all 走 ESLint 9 入口 + 删 3 .cjs + 综合 verify (medium risk, hook ✅)

**Files:**
- Modify: `dashboard/frontend/package.json:9-12` (3 lint scripts)
- Delete: `dashboard/frontend/.eslintrc.cjs`
- Delete: `dashboard/frontend/.eslintrc-testid-sync.cjs`
- Delete: `dashboard/frontend/.eslintrc-vue.cjs`

- [ ] **Step 1: 验证 pre-state (8.43.5 已 commit)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git log --oneline -2
# 期望: 8.43.5 (--no-verify) + 8.43.4 (--no-verify) 2 commits 落 local
```

- [ ] **Step 2: Edit package.json scripts — 3 lint 改走 eslint.config.js**

Edit `dashboard/frontend/package.json` L9-11, replace 3 lint scripts:

```diff
-    "lint:testid": "eslint tests/unit/**/*.spec.ts tests/fixtures/lint-testid/*.spec.ts --no-eslintrc -c .eslintrc.cjs --no-error-on-unmatched-pattern",
-    "lint:testid-sync": "eslint --no-error-on-unmatched-pattern --no-eslintrc -c .eslintrc-testid-sync.cjs --rule '{\"testid-class-sync\": \"warn\"}' --rulesdir eslint-rules --ext .vue src/",
-    "lint:vue": "eslint --no-error-on-unmatched-pattern --no-eslintrc -c .eslintrc-vue.cjs --ext .vue src/",
+    "lint:testid": "eslint --config eslint.config.js tests/unit/**/*.spec.ts tests/fixtures/lint-testid/*.spec.ts --no-error-on-unmatched-pattern",
+    "lint:testid-sync": "eslint --config eslint.config.js src/**/*.vue",
+    "lint:vue": "eslint --config eslint.config.js src/**/*.vue",
```

(`lint:all`, `lint:staged` L12-13 0 改 — `;` 跟 `&&` 逻辑跟 Phase 8.40 一致)

- [ ] **Step 3: 删 3 .eslintrc.cjs**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
rm .eslintrc.cjs .eslintrc-testid-sync.cjs .eslintrc-vue.cjs
ls -la .eslintrc* 2>&1
# 期望: ls: cannot access '.eslintrc*': No such file or directory
```

- [ ] **Step 4: 验证 lint:all 走 ESLint 10 入口 + 0 errors baseline**

```bash
corepack pnpm@9.15.0 lint:all
echo "EXIT=$?"
# 期望: EXIT=0
# 期望输出 (3 命令 串行):
#   lint:testid → 10 errors (Phase 8.33 fixture 软警告, 0 阻塞因为 ; separator)
#   lint:testid-sync → 0 errors, 7 warnings (Phase 8.36 baseline)
#   lint:vue → 0 errors, 212 warnings (Phase 8.42 baseline)
```

- [ ] **Step 5: 验证 vitest 16/95 baseline 维持**

```bash
corepack pnpm@9.15.0 vitest run 2>&1 | tail -3
# 期望: Test Files 16 passed (16) / Tests 95 passed (95) / 0 failed
```

- [ ] **Step 6: 验证 pnpm build 0 error**

```bash
corepack pnpm@9.15.0 build 2>&1 | tail -3
# 期望: ✓ built in N.NNs / dist/ emitted
```

- [ ] **Step 7: 验证 data-testid 46 维持**

```bash
grep -rEn 'data-testid=' src/ --include="*.vue" --include="*.js" | wc -l
# 期望: 46 (跟 Phase 8.42 baseline 一致, 0 改 .vue)
```

- [ ] **Step 8: 验证 backend pytest baseline (0 改 backend)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
corepack pnpm@9.15.0 exec -- pytest -q 2>&1 | tail -3
# 期望: 2262 passed, 0 failed, 27 skipped (跟 Phase 8.42 baseline 一致)
ruff check . 2>&1 | tail -3
# 期望: All checks passed!
```

- [ ] **Step 9: Commit (hook ✅ 恢复, lint:all exit 0)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git add dashboard/frontend/package.json
git add -u dashboard/frontend/.eslintrc.cjs dashboard/frontend/.eslintrc-testid-sync.cjs dashboard/frontend/.eslintrc-vue.cjs
git status
# 期望: 4 files changed (package.json + 3 .cjs deletions)
git commit -m "chore(lint): phase 8.43.6 — lint:all 走 ESLint 10 入口 + 删 3 .eslintrc.cjs

package.json 3 lint 脚本 改走 --config eslint.config.js (Phase 8.43.4 1 file
3 blocks): lint:testid (Block 1 spec dir) + lint:testid-sync (Block 2 vue
testid-class-sync) + lint:vue (Block 3 vue plugin). 0 改 lint 语义 (10/7/212
baseline 维持). 删 3 .eslintrc.cjs (Phase 8.33+8.36+8.42 累计).

lint:all 走 ESLint 10 入口 EXIT=0. vitest 16/95 维持. pnpm build 0 error.
data-testid 46 维持. pytest 2262 passed 0 failed 27 skipped. ruff 0.
0 改 backend / 0 改 .vue / 0 改 16 spec.

pre-commit hook 恢复 (8.43.4 + 8.43.5 2 commit --no-verify bypass 收尾).
下一步 8.43.7 memory sync + push 6 commits to origin/master."
```

---

## Task 7: 8.43.7 — Memory sync + push 6 commits (low risk, hook ✅, **0 commit**)

**Files:**
- Modify: `~/.claude/projects/-home-ailearn-projects-AI-Incursion-domains-IP---projects-LingWen/memory/phases-8-dashboard.md` (H1 + L3 + L4 + append) — **OUTSIDE git repo**
- Push: 6 commits from Tasks 1-6 to `origin/master` (network action, not a commit)

- [ ] **Step 1: 验证 pre-state (8.43.6 已 commit)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git log --oneline -6
# 期望: 6 Phase 8.43 commits 落 local (8.43.6 + 8.43.5 --no-verify + 8.43.4 --no-verify + 8.43.3 + 8.43.2 + 8.43.1)
git rev-parse HEAD origin/master
# 期望: 2 行不同 SHA (origin/master 仍 @ 84e63d9 spec commit, 本地 6 ahead)
```

- [ ] **Step 2: Edit H1 (L1) — 加 8.43 范围**

Edit `phases-8-dashboard.md` L1, change:
```diff
-# 灵文 · Phase 实施历史 (Phase 8.16-8.42, dashboard polish)
+# 灵文 · Phase 实施历史 (Phase 8.16-8.43, dashboard polish)
```

- [ ] **Step 3: Edit L4 (前 4 行 quote) — 标注 8.43 6 commits 追加**

Edit `phases-8-dashboard.md` L4, change:
```diff
-> 拆分时间: 2026-06-08 Phase 15 bookkeeping; 2026-06-09 Phase 8.35-8.42 7 commits 追加; 2026-06-09 Phase 8.43 7 commits 追加 (5 build tooling major bump + 1 flat config rewrite + 1 memory sync).
+> 拆分时间: 2026-06-08 Phase 15 bookkeeping; 2026-06-09 Phase 8.35-8.42 7 commits 追加; 2026-06-09 Phase 8.43 6 commits 追加 (5 build tooling major bump + 1 flat config rewrite, Task 7 = push + memory sync 0 commit).
```

- [ ] **Step 4: Append `## Phase 8.43 (2026-06-09)` entry at end of file**

Append at end of `phases-8-dashboard.md` (after Phase 8.42 entry):

```markdown

## Phase 8.43 (2026-06-09)

3 build tooling major version bumps + 2 config-only commits (Super-phase A 收尾) — 收尾 Phase 8.35-8.42 留 followup: 3 devDep major bump (Vue 3.5 / ESLint 8→10 / eslint-plugin-vue 10) + ESLint 10 flat config 1 file 3 blocks 替换 3 .eslintrc.cjs + 2 config-only commits (vitest.config.js `pool: 'forks'` + `coverage/**` exclude / setup.ts `enableAutoUnmount(afterEach)`). 7 sub-phases / 6 commits 推 origin/master (Task 1-6 各 1 commit, Task 7 = push + memory sync 0 commit 因为 phases-8-dashboard.md 在 ~/.claude/... 不在 git repo), 2 commits 走 --no-verify (8.43.4 + 8.43.5 ESLint 迁移期 bypass, 8.43.6 恢复 hook). **Plan deviation (主公 2026-06-09 决策)**: Vitest 5 stable 未发布 (latest=4.1.8, beta=5.0.0-beta.4 only) + @vue/test-utils 3 未发布 (latest=2.4.11) → 那 2 个 dep bump 跳过 (vitest ^4.1.8 维持 + test-utils ^2.4.11 维持), 单独走 2 个 config-only commits 收 vitest.config.js + setup.ts 的价值; ESLint 9 在 maintenance → 8→10 (跳 9, 走真正 latest 10.4.1, peerDeps verified 跟 vue-eslint-parser 10 + eslint-plugin-vue 10 兼容 `^8.57 || ^9 || ^10`). **8.43.1 Vue 3.4→3.5** (low risk, backward compat, vitest 16/95 + pnpm build 0 error). **8.43.2 vitest.config.js 加 `pool: 'forks'` + `coverage/**` exclude** (config-only, vitest 5 stable 未发布 dep bump 跳过, jsdom fork-isolate 防 OOM 跟 Phase 6.4 pytest forks 镜像, coverage/** exclude 给 Phase 8.44+ 留占位, vitest 16/95 跑 2 次 0 flake). **8.43.3 setup.ts 加 `enableAutoUnmount(afterEach)`** (config-only, test-utils 3 未发布 dep bump 跳过, v2.4+ 已支持, 防 wrapper 泄漏 跟 Vue 3.5 onUnmounted 同步, 16 spec mount() 基础 API 0 改). **8.43.4 ESLint 8→10** (high risk, --no-verify, skip 9 maintenance 跳到真正 latest 10.4.1, 新 eslint.config.js 1 file 3 blocks: Block 1 spec dir local-rules/no-class-selector-in-test Phase 8.33 + Block 2 .vue testid-class-sync Phase 8.36 + Block 3 .vue vue plugin vue3-recommended + 4 rule override Phase 8.42, 3 .eslintrc.cjs 仍存在 0 删, ESLint 10 0 读 .cjs). **8.43.5 eslint-plugin-vue 9→10** (high risk, --no-verify, 跟 ESLint 10 兼容 peerDep verified, vue3-recommended 1:1 mirror, 0 改 eslint.config.js Block 3, Block 3 dry-run on DecisionCard.vue 0 errors baseline 维持). **8.43.6 lint:all 走 ESLint 10 入口 + 删 3 .eslintrc.cjs** (medium risk, package.json 3 lint 脚本改走 --config eslint.config.js, 0 改 lint 语义 10/7/212 baseline 维持, lint:all EXIT=0 恢复 hook). **8.43.7 memory sync + push** (low risk, phases-8-dashboard.md H1 + L4 edit + 本 entry append, push 6 commits 到 origin/master). **commit sequence**: 6 commits 推 (T1 8.43.1 → T2 8.43.2 config-only → T3 8.43.3 config-only → T4 8.43.4 --no-verify → T5 8.43.5 --no-verify → T6 8.43.6 hook 恢复). **测试总**: vitest 16 spec / 95 tests all passed 维持, pytest 2262 passed 0 failed 27 skipped 维持 (0 改 backend), ruff 0 / pnpm build 0 error. **verification commands**: `pnpm lint:all` EXIT=0 / `pnpm vitest run` 16/95 passed / `pnpm build` 0 error / `grep -rEn 'data-testid=' src/ | wc -l` = 46 / `pytest -q` 2262 passed / `ruff check .` 0 issues / `./node_modules/.bin/eslint --version` v10.x.x / `./node_modules/.bin/vitest --version` 4.1.8 (维持) / `git rev-parse HEAD origin/master` 2 行同 SHA. **0 改**: 14 .vue / 16 vitest spec / 5 composables / api/index.js / .husky/pre-commit (Phase 8.40 维持) / eslint-rules/testid-class-sync.js (Phase 8.36 维持) / eslint-rules/no-class-selector-in-test.js (Phase 8.33 维持) / backend Python (agent_system / dashboard / cost_tracker / cost_persistence 全无 touch) / 9 historical spec/plan docs `:NNN` 行号不动 / 12 SCENARIOS 路由 / CLAUDE.md / pyproject.toml / pytest.ini / vitest.config.js 结构 (除 2 字段) / .state/*.db (gitignored) / .env (0 改 0 泄漏). **out of scope (有 followup)**: Super-phase B (Phase 8.44+) vitest --coverage + coverage upload CI + lint-staged 接入 (用 `pnpm lint:staged` alias) + backend pytest CI 整合进 dashboard-frontend-ci.yml + pre-commit pytest hook → vitest.config.js coverage/** exclude 已就位 / Super-phase C (Phase 8.5+) DecisionCard 4 status testid (deferred 状态修正 0 "expired") + OverviewPage e2e 加深 / Super-phase D (Phase 8.6+) TS strict mode (加 tsconfig.json + strict: true + 修 11 .ts) / Phase 9+ 跨卷涟漪 (独立大方向, spec 已 commit 8cc1298) / Phase 10-14 跨卷涟漪 5 super-phase / MEMORY.md 拆 38+ entries to phases.md topic (本 entry 后变 38+, 必拆 followup) / Vue 3.5 新 API (useTemplateRef / defineModel / useId) / test-utils 3 高级 API (findComponent / setValue / trigger) 留 followup 等 stable / `@vue/server-renderer` (当前 ^3.5.35 维持) / `echarts` / `mermaid` / `@types/node` (当前 ^25.9.2 维持) / CI 加 Node 22 matrix (Node 20 维持) / Vitest 5 + Test-utils 3 stable 等 release 4+ 周后 revisit. 后续: Phase 8.44+ Super-phase B (test infra) 启动.
```

(注: 不需要 placeholder commit SHAs — Task 7 0 commit, 实际 SHAs 走 git push 后 log 查)

- [ ] **Step 5: Push 6 commits to origin/master (Task 7 = push + memory sync, 0 commit)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git status
# 期望: clean (phases-8-dashboard.md 在 ~/.claude/... 不在 git repo, 0 改 git tracked file)
git push origin master
# 期望: 6 commits 推送, output 类似:
#   To github.com:XiaZiHunDun/novel-factory.git
#      84e63d9..<new HEAD>  master -> master

git rev-parse HEAD origin/master
# 期望: 2 行同 SHA (push 验证)

git log origin/master --oneline -6
# 期望: 6 Phase 8.43 commits 落 origin
```

- [ ] **Step 6: 0 Co-Authored-By trailers 验证**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git log -6 --format='%(trailers:key=Co-Authored-By,valueonly)'
# 期望: 6 行空 (0 Co-Authored-By, 跟 Phase 8.35-8.42 policy 一致)
```

- [ ] **Step 7: Task 7 done (0 commit, push + memory sync 完)**

(注: phases-8-dashboard.md 在 `~/.claude/projects/.../memory/`, 不走 git. Task 7 done = 6 commits 推 origin + memory 同步 + 0 commit. Spec 8.43.7 说 "7 commits" 是 off-by-one — plan 精确化为 "6 commits" 因 memory file outside git repo)

---

## Out of Scope (留后续 super-phase / phase)

- **Super-phase B (Phase 8.44+)**: vitest --coverage + coverage upload CI + lint-staged 接入 (用 `pnpm lint:staged` alias) + backend pytest CI 整合 + pre-commit pytest hook
- **Super-phase C (Phase 8.5+)**: DecisionCard 4 status testid (deferred 状态修正) + OverviewPage e2e 加深
- **Super-phase D (Phase 8.6+)**: TS strict mode (加 tsconfig.json + strict: true + 修 11 .ts)
- **Phase 9+**: 跨卷涟漪 (Phase 9 spec 已 commit 8cc1298, 留独立 phase)
- **MEMORY.md 拆**: phases-8-dashboard.md 累 Phase 8.43 后变 38+ entries, 必拆 followup
- **CI 加 Node 22 matrix**: 0 改, Node 20 维持
- **Vue 3.5 新 API 引入**: useTemplateRef / defineModel / useId 5+ API 留 followup
- **test-utils 3 高级 API 改**: findComponent / setValue / trigger 留 followup
- **echarts / mermaid / @types/node bump**: 0 改

---

## Risks + Mitigations

| Risk | Mitigation |
|---|---|
| Vue 3.5 0 改 .vue 实际有 hidden breaking | vitest 23/95 + pnpm build 0 error 验证, 0 通过 revert |
| (已规避) Vitest 5 stable 未发布 | 主公决策 vitest ^4.1.8 维持, Task 2 收窄为 config-only commit (pool + coverage exclude), 0 dep bump |
| (已规避) test-utils 3 未发布 | 主公决策 ^2.4.11 维持, Task 3 收窄为 config-only commit (enableAutoUnmount v2.4+ 已支持), 0 dep bump |
| ESLint 10 flat config syntax 错 | Task 4 step 5 dry-run `node -e "import('./eslint.config.js')..."` 报 3 blocks, syntax 错即 abort commit |
| vue 10 跟 ESLint 10 不兼容 | Task 5 step 4 dry-run on DecisionCard.vue 0 errors baseline 维持, 0 通过 revert + 改 fallback (peerDeps 验证已过 `^8.57 \|\| ^9 \|\| ^10`) |
| lint:all 改 ESLint 10 入口后 7 warnings baseline 漂 | Task 6 step 4 跑 lint:all 显式 0 errors baseline 维持, 1 错 revert + 改 Block rule level |
| pre-commit hook 拦截 8.43.4 + 8.43.5 推不出 commit | 2 commit 走 `--no-verify` (Q3 决策, commit body 显式注) |
| 2 commit 走 `--no-verify` 被 code review 标记为 "bypass hook" | 0 禁用 husky hook, 0 改 `.husky/pre-commit` 内容, 0 临时 commit; 8.43.6 1 commit 恢复 hook, 后续 5 commit 全过 hook |
| 删 3 `.eslintrc.cjs` 触发别的 spec 引用 | 0 spec 引用 `.eslintrc.cjs` (Phase 8.33+8.36+8.42 spec 0 引用), grep 验证 Task 6 step 1 |
| `pnpm 11.5 fail on Node 20.20` (Phase 8.42 D1) | 装新 devDep 走 `corepack pnpm@9.15.0` workaround, 跟 Phase 8.40+8.42 pattern 一致 |
| `phases-8-dashboard.md` 不在 git repo, Task 7 0 commit | 0 commit needed, 6 commits 推完即 Task 7 done (memory 同步是 side effect) |

---

## Critical Files

### Created (1)
- `dashboard/frontend/eslint.config.js` (~100L, 8.43.4)

### Modified (5)
- `dashboard/frontend/package.json` (3 devDep bump: vue/eslint/eslint-plugin-vue + 3 lint scripts 改, 8.43.1-8.43.6)
- `dashboard/frontend/pnpm-lock.yaml` (auto by pnpm install, 8.43.1-8.43.5)
- `dashboard/frontend/vitest.config.js` (+ pool + coverage exclude, 8.43.2)
- `dashboard/frontend/tests/unit/setup.ts` (+ enableAutoUnmount, 8.43.3)
- `~/.claude/projects/.../memory/phases-8-dashboard.md` (H1 + L3 + L4 + 8.43 entry append, 8.43.7)

### Deleted (3, 8.43.6)
- `dashboard/frontend/.eslintrc.cjs`
- `dashboard/frontend/.eslintrc-testid-sync.cjs`
- `dashboard/frontend/.eslintrc-vue.cjs`

### NOT modified (~20)
14 .vue / 16 vitest spec / 5 composables / api/index.js / .husky/pre-commit / eslint-rules/* / index.html / vite.config.js / backend Python / 9 historical spec/plan docs / 12 SCENARIOS / CLAUDE.md / pyproject.toml / pytest.ini / .github/workflows/dashboard-frontend-ci.yml / .state/*.db / .env

---

## 完成定义 (DoD)

- [ ] `docs/superpowers/plans/2026-06-09-phase8.43-build-tooling-upgrade.md` 创建 (本 plan)
- [ ] `dashboard/frontend/eslint.config.js` 1 file 3 blocks 创建 (Task 4)
- [ ] 3 `.eslintrc.cjs` 删 (Task 6)
- [ ] 3 devDep bump: vue ^3.5 / eslint ^10.0 / eslint-plugin-vue ^10.0
- [ ] 2 config-only commits: vitest.config.js `pool: 'forks'` + `coverage/**` exclude (Task 2) / tests/unit/setup.ts `enableAutoUnmount(afterEach)` (Task 3)
- [ ] 2 dep bump 跳过: vitest 维持 ^4.1.8 (5 stable 未发布) / @vue/test-utils 维持 ^2.4.11 (3 未发布) — followup 等 release
- [ ] `vitest.config.js` `pool: 'forks'` + `coverage/**` exclude 加 (Task 2)
- [ ] `tests/unit/setup.ts` `enableAutoUnmount(afterEach)` 加 (Task 3)
- [ ] `pnpm lint:all` 改 ESLint 9 入口 (Task 6)
- [ ] 7 commits 推送 origin/master (Task 1-6 + 7 push)
- [ ] 2 commit `--no-verify` 显式注 (Task 4 + 5)
- [ ] vitest 16 spec / 95 tests passed
- [ ] pytest 2262 passed 0 failed 27 skipped
- [ ] ruff 0 issues
- [ ] pnpm build 0 error
- [ ] pnpm lint:all 0 errors baseline 维持 (212 vue warnings + 10 testid fixture 软 + 7 testid-sync warnings)
- [ ] data-testid 46 维持
- [ ] git rev-parse HEAD origin/master 2 行同 SHA
- [ ] pre-commit hook 恢复 (`.husky/pre-commit` 0 改)
- [ ] memory sync `phases-8-dashboard.md` H1 + L3 + L4 edit + 8.43 entry append
- [ ] 0 改 .vue / 0 改 16 vitest spec / 0 改 33 data-testid / 0 改 backend Python / 0 改 9 historical spec/plan docs / 0 改 12 SCENARIOS / 0 改 CLAUDE.md / pyproject.toml / pytest.ini / vitest.config.js 结构 (除 2 字段) / .state/*.db / .env
- [ ] 0 force-push / 0 amend (除 sub-phase fail 救火) / 0 Co-Authored-By / 0 DANGEROUSLY_SKIP_PERMISSIONS / 0 误改 .env / 0 API key 泄漏

---

## 完成后下一步 (后续 super-phase)

- **Phase 8.44 (Super-phase B 启动)**: test infra (vitest --coverage + coverage upload CI + lint-staged + backend pytest CI 整合 + pre-commit pytest)
- **Phase 8.5 (Super-phase C 启动)**: UI completeness (DecisionCard 4 status testid + OverviewPage e2e)
- **Phase 8.6 (Super-phase D 启动)**: TS strict
- **Phase 9 (跨卷涟漪 启动)**: 独立大方向
- **MEMORY.md 拆 38+ entries to phases.md topic**: 必拆 followup
