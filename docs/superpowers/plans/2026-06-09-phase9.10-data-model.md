# Phase 9.10 — Cross-Volume Ripple Data Model + Persistence Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement Phase 9.10 cross-volume ripple data model + persistence (3 frozen dataclass + 1 CVG container + 1 sqlite3 storage class), 16 tests TDD RED→GREEN, 0 production code 改.

**Architecture:** 2-layer separation — `CrossVolumeReferenceGraph` in-memory dict indexes (3 dict + 3 secondary indexes for O(1)/O(k) lookup) + `RippleStorage` sqlite3 直写 (3 tables + 4 indexes + 3 PRAGMAs). Append-only via `frozen=True` + 0 mutation API. Phase 10 纯数据层, 0 LLM 成本, 0 backfill (defer Phase 11+).

**Tech Stack:** Python 3.12 + sqlite3 stdlib + dataclass + pytest 8 + ruff 0.6+ + mypy (跟 codebase precedent, 0 strict mode).

---

## Pre-Execution Setup

**Step 0.1**: Verify working directory + baseline

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git rev-parse --show-toplevel  # 期望: /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen (git root 上方)
git log --oneline -1            # 期望: 3787bee docs(spec): phase 9.10 — fix reference paths
git status                      # 期望: clean
pytest -q 2>&1 | tail -3        # 期望: 2262 passed, 0 failed, 27 skipped
```

**Step 0.2**: Verify `.gitignore` 含 `infra/.state/*` pattern (跟 workflow.db / cost_tracker.db precedent 一致)

```bash
grep -E "state|\.db" .gitignore | head -5
# 期望: infra/.state/  或  infra/.state/*  (从 Phase 7.1 起)
```

**Step 0.3**: Verify existing `infra/` layout

```bash
ls infra/ | head -10
# 期望: agent_system/ ai_service/ consistency/ cross_volume 不存在 (要新建) hooks/ memory_system/ state/ ...
```

**Step 0.4**: Read spec for reference

```bash
cat novel-factory/docs/superpowers/specs/2026-06-09-phase9.10-data-model-design.md | head -100
```

---

## Critical Safety (主公 hard rules, 0 例外)

| 规则 | 说明 |
|------|------|
| 0 force-push | `git push --force` / `git push -f` 永不用 |
| 0 amend | 所有 fixup 走新 commit, 不用 `git commit --amend` |
| 0 Co-Authored-By footer | Attribution 已在 `~/.claude/settings.json` 全局禁用 |
| 0 state DB 提交 | `infra/.state/*.db` gitignored, 误 add 即 revert |
| 0 DANGEROUSLY_SKIP_PERMISSIONS | 跟 codebase precedent 一致 |
| 0 API key 泄漏 | 0 provider 改, 0 .env 改, 0 ANTHROPIC_API_KEY 引用 |
| 0 改 CLAUDE.md / pyproject.toml / pytest.ini | hard-coded 文件不动 |
| 0 改 12 SCENARIOS 路由 | Phase 10 纯新数据层, 0 触 workflow |
| 0 改 historical spec/plan doc `:NNN` 行号 | 历史已不准, 改反误导, 永不做 |
| 沟通 | 叙述中文 / 代码英文 / commit body 中文注释 |
| 0 production code 改 | 12 SCENARIOS, MasterController, dashboard, workflow.db, cost_tracker.db 全部 0 触 |
| File size | 200-400L typical, 800L hard cap |
| TDD | RED → verify RED → GREEN → verify GREEN → commit |
| Conventional commits | feat / fix / refactor / docs / test / chore / perf / ci |

---

## Task 1: Write 16 Failing Tests (RED)

**Files:**
- Create: `tests/cross_volume/test_reference_graph.py` (~100L, 6 tests)
- Create: `tests/cross_volume/test_ripple.py` (~60L, 3 tests)
- Create: `tests/cross_volume/test_storage.py` (~90L, 7 tests)

- [ ] **Step 1.1**: Create `tests/cross_volume/test_reference_graph.py` (6 tests, ALL FAIL with ImportError)

```python
# tests/cross_volume/test_reference_graph.py
"""Phase 9.10: CrossVolumeReferenceGraph + ReferenceNode + ReferenceEdge tests.

TDD: 这些测试在 step 1 阶段会全部 FAIL (ImportError), 在 Task 3 (CVG impl) 后通过.
"""
import pytest
from infra.cross_volume import (
    CrossVolumeReferenceGraph,
    ReferenceNode,
    ReferenceEdge,
)
from infra.cross_volume.storage import RippleStorage


@pytest.fixture
def storage(tmp_path):
    return RippleStorage(db_path=tmp_path / "ripple.db")


@pytest.fixture
def graph(storage):
    return CrossVolumeReferenceGraph(storage=storage)


class TestReferenceGraph:
    def test_add_and_get_node(self, graph):
        node = ReferenceNode(dimension="character", volume=1, chapter=3,
                             title="李青云", description="主角",
                             payload={"name": "李青云", "role": "主角"})
        graph.add_node(node)
        got = graph.get_node(node.id)
        assert got is not None
        assert got.id == node.id
        assert got.dimension == "character"
        assert got.title == "李青云"

    def test_add_node_rejects_duplicate_id(self, graph):
        node = ReferenceNode(dimension="character", volume=1, chapter=1,
                             title="A", description="")
        graph.add_node(node)
        with pytest.raises(ValueError, match="duplicate node id"):
            graph.add_node(node)  # same id → raise

    def test_add_edge_validates_fk(self, graph):
        node_a = ReferenceNode(dimension="character", volume=1, chapter=1,
                                title="A", description="")
        node_b = ReferenceNode(dimension="foreshadow", volume=2, chapter=5,
                                title="B", description="")
        graph.add_node(node_a)
        graph.add_node(node_b)
        # Missing FK
        with pytest.raises(ValueError, match="from_node_id not found"):
            graph.add_edge(ReferenceEdge(from_node_id="nonexistent", to_node_id=node_b.id))

    def test_get_nodes_by_volume_filter(self, graph):
        n1 = ReferenceNode(dimension="character", volume=1, chapter=1, title="C1", description="")
        n2 = ReferenceNode(dimension="character", volume=1, chapter=2, title="C2", description="")
        n3 = ReferenceNode(dimension="character", volume=2, chapter=1, title="C3", description="")
        for n in (n1, n2, n3):
            graph.add_node(n)
        vol1_nodes = graph.get_nodes_by_volume(1)
        assert len(vol1_nodes) == 2
        assert {n.id for n in vol1_nodes} == {n1.id, n2.id}

    def test_get_nodes_by_dimension_filter(self, graph):
        n_char = ReferenceNode(dimension="character", volume=1, chapter=1, title="C", description="")
        n_fore = ReferenceNode(dimension="foreshadow", volume=1, chapter=2, title="F", description="")
        n_set = ReferenceNode(dimension="setting", volume=1, chapter=3, title="S", description="")
        n_plot = ReferenceNode(dimension="plot_point", volume=1, chapter=4, title="P", description="")
        for n in (n_char, n_fore, n_set, n_plot):
            graph.add_node(n)
        char_nodes = graph.get_nodes_by_dimension("character")
        assert len(char_nodes) == 1
        assert char_nodes[0].id == n_char.id

    def test_get_neighbors_returns_connected(self, graph):
        n_a = ReferenceNode(dimension="character", volume=1, chapter=1, title="A", description="")
        n_b = ReferenceNode(dimension="foreshadow", volume=1, chapter=2, title="B", description="")
        n_c = ReferenceNode(dimension="setting", volume=1, chapter=3, title="C", description="")
        for n in (n_a, n_b, n_c):
            graph.add_node(n)
        graph.add_edge(ReferenceEdge(from_node_id=n_a.id, to_node_id=n_b.id, relationship_type="mentions"))
        graph.add_edge(ReferenceEdge(from_node_id=n_a.id, to_node_id=n_c.id, relationship_type="appears_with"))
        neighbors = graph.get_neighbors(n_a.id)
        assert len(neighbors) == 2
        assert {n.id for n in neighbors} == {n_b.id, n_c.id}
```

- [ ] **Step 1.2**: Create `tests/cross_volume/test_ripple.py` (3 tests)

```python
# tests/cross_volume/test_ripple.py
"""Phase 9.10: CrossVolumeRipple + trigger_cascade stub tests."""
import pytest
from dataclasses import FrozenInstanceError
from infra.cross_volume import CrossVolumeRipple, CrossVolumeReferenceGraph
from infra.cross_volume.storage import RippleStorage


@pytest.fixture
def storage(tmp_path):
    return RippleStorage(db_path=tmp_path / "ripple.db")


@pytest.fixture
def graph(storage):
    return CrossVolumeReferenceGraph(storage=storage)


class TestRipple:
    def test_ripple_is_frozen(self):
        ripple = CrossVolumeRipple(trigger_volume=2, trigger_chapter=5)
        with pytest.raises(FrozenInstanceError):
            ripple.status = "applied"  # mutation should fail

    def test_ripple_default_status_pending(self):
        ripple = CrossVolumeRipple(trigger_volume=2, trigger_chapter=5)
        assert ripple.status == "pending"
        assert ripple.payload == {}
        assert ripple.affected_nodes == ()
        assert ripple.affected_edges == ()

    def test_trigger_cascade_stub_returns_empty(self, graph):
        ripple = CrossVolumeRipple(trigger_volume=2, trigger_chapter=5)
        result = graph.trigger_cascade(ripple)
        assert result == []  # Phase 14 real BFS impl
```

- [ ] **Step 1.3**: Create `tests/cross_volume/test_storage.py` (7 tests)

```python
# tests/cross_volume/test_storage.py
"""Phase 9.10: RippleStorage sqlite3 直写 tests."""
import json
import pytest
from infra.cross_volume import ReferenceNode, ReferenceEdge, CrossVolumeRipple
from infra.cross_volume.storage import RippleStorage


@pytest.fixture
def storage(tmp_path):
    return RippleStorage(db_path=tmp_path / "ripple.db")


class TestRippleStorage:
    def test_storage_node_roundtrip(self, storage):
        node = ReferenceNode(dimension="character", volume=1, chapter=3,
                              title="李青云", description="主角",
                              payload={"name": "李青云", "role": "主角"})
        storage.append_node(node)
        loaded = storage.load_all_nodes()
        assert len(loaded) == 1
        n = loaded[0]
        assert n.id == node.id
        assert n.dimension == "character"
        assert n.title == "李青云"
        assert n.payload == {"name": "李青云", "role": "主角"}

    def test_storage_edge_roundtrip(self, storage):
        n1 = ReferenceNode(dimension="character", volume=1, chapter=1, title="A", description="")
        n2 = ReferenceNode(dimension="foreshadow", volume=1, chapter=2, title="B", description="")
        storage.append_node(n1)
        storage.append_node(n2)
        edge = ReferenceEdge(from_node_id=n1.id, to_node_id=n2.id,
                              relationship_type="foreshadows", weight=0.85,
                              payload={"evidence_chapter": 1})
        storage.append_edge(edge)
        loaded = storage.load_all_edges()
        assert len(loaded) == 1
        e = loaded[0]
        assert e.from_node_id == n1.id
        assert e.to_node_id == n2.id
        assert e.relationship_type == "foreshadows"
        assert e.weight == 0.85

    def test_storage_ripple_roundtrip(self, storage):
        ripple = CrossVolumeRipple(
            trigger_volume=2, trigger_chapter=5,
            affected_nodes=("n1", "n2"), affected_edges=("e1",),
            proposed_actions=({"type": "update_node", "node_id": "n1"},),
            status="pending",
        )
        storage.append_ripple(ripple)
        loaded = storage.load_all_ripples()
        assert len(loaded) == 1
        r = loaded[0]
        assert r.id == ripple.id
        assert r.trigger_volume == 2
        assert r.affected_nodes == ("n1", "n2")
        assert r.proposed_actions == ({"type": "update_node", "node_id": "n1"},)

    def test_atomic_batch_rolls_back_on_exception(self, storage):
        n1 = ReferenceNode(dimension="character", volume=1, chapter=1, title="A", description="")
        with pytest.raises(RuntimeError, match="test failure"):
            with storage.atomic_batch() as conn:
                conn.execute(
                    "INSERT INTO reference_nodes VALUES (?,?,?,?,?,?,?,?,?)",
                    (n1.id, n1.dimension, n1.volume, n1.chapter, n1.title,
                     n1.description, json.dumps(n1.payload, ensure_ascii=False),
                     n1.created_at.isoformat(), n1.created_by),
                )
                raise RuntimeError("test failure")
        assert storage.load_all_nodes() == []  # rolled back

    def test_atomic_batch_commits_on_success(self, storage):
        n1 = ReferenceNode(dimension="character", volume=1, chapter=1, title="A", description="")
        n2 = ReferenceNode(dimension="character", volume=1, chapter=2, title="B", description="")
        with storage.atomic_batch() as conn:
            for n in (n1, n2):
                conn.execute(
                    "INSERT INTO reference_nodes VALUES (?,?,?,?,?,?,?,?,?)",
                    (n.id, n.dimension, n.volume, n.chapter, n.title,
                     n.description, json.dumps(n.payload, ensure_ascii=False),
                     n.created_at.isoformat(), n.created_by),
                )
        assert len(storage.load_all_nodes()) == 2

    def test_fk_violation_raises_value_error(self, storage):
        edge = ReferenceEdge(from_node_id="nonexistent", to_node_id="also_nonexistent",
                              relationship_type="mentions")
        with pytest.raises(ValueError, match="storage integrity"):
            storage.append_edge(edge)  # FK violation → ValueError wrap (per spec §4)

    def test_empty_db_init(self, tmp_path):
        storage = RippleStorage(db_path=tmp_path / "fresh.db")
        assert storage.load_all_nodes() == []
        assert storage.load_all_edges() == []
        assert storage.load_all_ripples() == []
```

- [ ] **Step 1.4**: Run tests to verify RED

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pytest tests/cross_volume/ -v 2>&1 | tail -20
# 期望: 16 failed (ImportError: cannot import name 'CrossVolumeReferenceGraph' from 'infra.cross_volume')
```

- [ ] **Step 1.5**: Commit failing tests (RED checkpoint)

```bash
git add tests/cross_volume/
git commit -m "test(cross-volume): phase 9.10 — 16 failing tests (RED checkpoint)

3 test files (test_reference_graph.py 6 / test_ripple.py 3 / test_storage.py 7)
建立 Phase 9.10 data layer contract:
- test_reference_graph.py: add_node / get_node / FK validation / per-volume
  filter / per-dimension filter / get_neighbors 双向索引
- test_ripple.py: frozen immutability / default status pending / cascade stub
- test_storage.py: node/edge/ripple roundtrip / atomic_batch rollback+commit
  / FK violation / empty DB init

RED 阶段 16 ImportError, GREEN 阶段 (Task 3+4) 期望 16 passed.

0 production code 改, 0 改 12 SCENARIOS, 0 改 CLAUDE.md."
```

---

## Task 2: Implement `__init__.py` + `reference_graph.py` + `ripple.py` (CVG + 3 Dataclass)

**Files:**
- Create: `infra/cross_volume/__init__.py` (~15L)
- Create: `infra/cross_volume/reference_graph.py` (~120L, CVG + ReferenceNode + ReferenceEdge)
- Create: `infra/cross_volume/ripple.py` (~50L, CrossVolumeRipple + trigger_cascade stub)

- [ ] **Step 2.1**: Create `infra/cross_volume/__init__.py`

```python
# infra/cross_volume/__init__.py
"""Phase 9.10: Cross-volume ripple data model + persistence.

公开 API:
- CrossVolumeReferenceGraph: in-memory graph container with storage injection
- ReferenceNode: 4-dim (character/foreshadow/setting/plot_point) graph node
- ReferenceEdge: 8 relationship_type graph edge
- CrossVolumeRipple: scan event dataclass (Phase 10 stub, Phase 11+ LLM filled)
- RippleStorage: sqlite3 直写 (在 infra/cross_volume/storage.py)
"""
from infra.cross_volume.reference_graph import (
    CrossVolumeReferenceGraph,
    ReferenceNode,
    ReferenceEdge,
)
from infra.cross_volume.ripple import CrossVolumeRipple

__all__ = [
    "CrossVolumeReferenceGraph",
    "ReferenceNode",
    "ReferenceEdge",
    "CrossVolumeRipple",
]
```

- [ ] **Step 2.2**: Create `infra/cross_volume/reference_graph.py`

```python
# infra/cross_volume/reference_graph.py
"""Phase 9.10: CrossVolumeReferenceGraph container + ReferenceNode + ReferenceEdge."""
import logging
from dataclasses import dataclass, field
from datetime import datetime, timezone
from typing import Any, Literal
from uuid import uuid4

from infra.cross_volume.ripple import CrossVolumeRipple

logger = logging.getLogger(__name__)

DimensionT = Literal["character", "foreshadow", "setting", "plot_point"]
RelationshipT = Literal[
    "mentions", "evolves", "foreshadows", "pays_off",
    "appears_with", "causes", "conflicts_with", "supports",
]


@dataclass(frozen=True)
class ReferenceNode:
    id: str = field(default_factory=lambda: uuid4().hex)
    dimension: DimensionT = "character"
    volume: int = 1
    chapter: int = 0
    title: str = ""
    description: str = ""
    payload: dict[str, Any] = field(default_factory=dict)
    created_at: datetime = field(default_factory=lambda: datetime.now(timezone.utc))
    created_by: str = "manual"

    def __post_init__(self):
        if self.volume < 1:
            raise ValueError(f"volume must be >= 1, got {self.volume}")
        if self.chapter < 0:
            raise ValueError(f"chapter must be >= 0, got {self.chapter}")
        if len(self.description) > 200:
            raise ValueError(f"description max 200 chars, got {len(self.description)}")


@dataclass(frozen=True)
class ReferenceEdge:
    id: str = field(default_factory=lambda: uuid4().hex)
    from_node_id: str = ""
    to_node_id: str = ""
    relationship_type: RelationshipT = "mentions"
    weight: float = 1.0
    payload: dict[str, Any] = field(default_factory=dict)
    created_at: datetime = field(default_factory=lambda: datetime.now(timezone.utc))
    created_by: str = "manual"

    def __post_init__(self):
        if not self.from_node_id or not self.to_node_id:
            raise ValueError("from_node_id and to_node_id required")
        if self.from_node_id == self.to_node_id:
            raise ValueError("self-loop edge not allowed")
        if not 0.0 <= self.weight <= 1.0:
            raise ValueError(f"weight must be in [0.0, 1.0], got {self.weight}")


class CrossVolumeReferenceGraph:
    def __init__(self, storage) -> None:
        self._storage = storage
        self._nodes: dict[str, ReferenceNode] = {}
        self._edges: dict[str, ReferenceEdge] = {}
        self._ripples: dict[str, CrossVolumeRipple] = {}
        self._index_by_volume: dict[int, set[str]] = {}
        self._index_by_dimension: dict[DimensionT, set[str]] = {}
        self._index_by_node_edges: dict[str, set[str]] = {}
        self._load_from_storage()

    def add_node(self, node: ReferenceNode) -> None:
        if node.id in self._nodes:
            raise ValueError(f"duplicate node id: {node.id}")
        self._nodes[node.id] = node
        self._index_by_volume.setdefault(node.volume, set()).add(node.id)
        self._index_by_dimension.setdefault(node.dimension, set()).add(node.id)
        self._storage.append_node(node)
        logger.debug("add_node: id=%s dim=%s vol=%d", node.id, node.dimension, node.volume)

    def get_node(self, node_id: str) -> ReferenceNode | None:
        return self._nodes.get(node_id)

    def get_nodes_by_volume(self, volume: int) -> list[ReferenceNode]:
        return [self._nodes[nid] for nid in self._index_by_volume.get(volume, set())]

    def get_nodes_by_dimension(self, dim: DimensionT) -> list[ReferenceNode]:
        return [self._nodes[nid] for nid in self._index_by_dimension.get(dim, set())]

    def add_edge(self, edge: ReferenceEdge) -> None:
        if edge.id in self._edges:
            raise ValueError(f"duplicate edge id: {edge.id}")
        if edge.from_node_id not in self._nodes:
            raise ValueError(f"from_node_id not found: {edge.from_node_id}")
        if edge.to_node_id not in self._nodes:
            raise ValueError(f"to_node_id not found: {edge.to_node_id}")
        self._edges[edge.id] = edge
        self._index_by_node_edges.setdefault(edge.from_node_id, set()).add(edge.id)
        self._index_by_node_edges.setdefault(edge.to_node_id, set()).add(edge.id)
        self._storage.append_edge(edge)
        logger.debug("add_edge: id=%s %s->%s", edge.id, edge.from_node_id, edge.to_node_id)

    def get_edge(self, edge_id: str) -> ReferenceEdge | None:
        return self._edges.get(edge_id)

    def get_neighbors(self, node_id: str) -> list[ReferenceNode]:
        edge_ids = self._index_by_node_edges.get(node_id, set())
        neighbors: list[ReferenceNode] = []
        for eid in edge_ids:
            e = self._edges[eid]
            other = e.to_node_id if e.from_node_id == node_id else e.from_node_id
            n = self._nodes.get(other)
            if n is not None:
                neighbors.append(n)
        return neighbors

    def record_ripple(self, ripple: CrossVolumeRipple) -> None:
        if ripple.id in self._ripples:
            raise ValueError(f"duplicate ripple id: {ripple.id}")
        self._ripples[ripple.id] = ripple
        self._storage.append_ripple(ripple)
        logger.debug("record_ripple: id=%s status=%s", ripple.id, ripple.status)

    def get_ripple(self, ripple_id: str) -> CrossVolumeRipple | None:
        return self._ripples.get(ripple_id)

    def trigger_cascade(self, ripple: CrossVolumeRipple) -> list[ReferenceNode]:
        """Phase 10 stub returns []. Phase 14 real BFS cascade impl."""
        return []

    def _load_from_storage(self) -> None:
        for n in self._storage.load_all_nodes():
            self._nodes[n.id] = n
            self._index_by_volume.setdefault(n.volume, set()).add(n.id)
            self._index_by_dimension.setdefault(n.dimension, set()).add(n.id)
        for e in self._storage.load_all_edges():
            self._edges[e.id] = e
            self._index_by_node_edges.setdefault(e.from_node_id, set()).add(e.id)
            self._index_by_node_edges.setdefault(e.to_node_id, set()).add(e.id)
        for r in self._storage.load_all_ripples():
            self._ripples[r.id] = r

    def __len__(self) -> int:
        return len(self._nodes)
```

- [ ] **Step 2.3**: Create `infra/cross_volume/ripple.py`

```python
# infra/cross_volume/ripple.py
"""Phase 9.10: CrossVolumeRipple dataclass + trigger_cascade stub."""
from dataclasses import dataclass, field
from datetime import datetime, timezone
from typing import Any, Literal

RippleStatusT = Literal["pending", "confirmed", "applied", "rejected", "failed"]


@dataclass(frozen=True)
class CrossVolumeRipple:
    """Phase 10: payload={} (no LLM). Phase 11+: LLM scanner fills payload."""
    id: str = field(default_factory=lambda: __import__("uuid").uuid4().hex)
    trigger_volume: int = 1
    trigger_chapter: int = 0
    affected_nodes: tuple[str, ...] = ()
    affected_edges: tuple[str, ...] = ()
    proposed_actions: tuple[dict, ...] = ()
    status: RippleStatusT = "pending"
    created_at: datetime = field(default_factory=lambda: datetime.now(timezone.utc))
    confirmed_at: datetime | None = None
    applied_at: datetime | None = None
    payload: dict[str, Any] = field(default_factory=dict)

    def __post_init__(self):
        if self.trigger_volume < 1:
            raise ValueError(f"trigger_volume must be >= 1, got {self.trigger_volume}")
```

- [ ] **Step 2.4**: Run tests for CVG + ripple (Storage still 0 — expected ImportError on RippleStorage)

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pytest tests/cross_volume/test_reference_graph.py tests/cross_volume/test_ripple.py -v 2>&1 | tail -20
# 期望: 9 ERROR (ImportError: cannot import name 'RippleStorage')
# 原因: Task 2 只做 CVG + 3 dataclass, Storage 在 Task 3
```

- [ ] **Step 2.5**: Commit CVG + 3 dataclass (GREEN part 1)

```bash
git add infra/cross_volume/__init__.py infra/cross_volume/reference_graph.py infra/cross_volume/ripple.py
git commit -m "feat(cross-volume): phase 9.10 — CVG container + 3 frozen dataclass

3 frozen @dataclass (ReferenceNode / ReferenceEdge / CrossVolumeRipple) +
1 container class (CrossVolumeReferenceGraph) with 3 in-memory secondary
indexes (per-volume / per-dimension / per-node-edges for O(1)/O(k) lookup).

跟 codebase precedent 一致:
- frozen=True 强制 append-only (跟 CostRecord / ThoughtNode 同模式)
- uuid4().hex 默认 id 生成 (跟 ThoughtNode 1:1)
- DimensionT / RelationshipT / RippleStatusT Literal types (跟 decision_state.py 1:1)
- tuple[str, ...] 不可变 list (affected_nodes 等)
- 0 自定义 exception class, 全部 ValueError (跟 codebase 5 处 precedent)
- logger.debug / logger.warning 走 stdlib logging, 0 print

CVG API surface (10 public methods):
- add_node / get_node / get_nodes_by_volume / get_nodes_by_dimension
- add_edge / get_edge / get_neighbors (O(k) via 双向 index)
- record_ripple / get_ripple
- trigger_cascade: Phase 10 stub returns [], Phase 14 real BFS impl

0 production code 改, 0 改 12 SCENARIOS, 0 改 MasterController, 0 改 dashboard."
```

---

## Task 3: Implement `storage.py` (RippleStorage)

**Files:**
- Create: `infra/cross_volume/storage.py` (~80L, RippleStorage)

- [ ] **Step 3.1**: Create `infra/cross_volume/storage.py`

```python
# infra/cross_volume/storage.py
"""Phase 9.10: RippleStorage — sqlite3 直写, atomic batch via BEGIN IMMEDIATE."""
import json
import logging
import sqlite3
from contextlib import contextmanager
from datetime import datetime
from pathlib import Path
from typing import Iterator

from infra.cross_volume.reference_graph import ReferenceNode, ReferenceEdge
from infra.cross_volume.ripple import CrossVolumeRipple

logger = logging.getLogger(__name__)

_SCHEMA_SQL = """
CREATE TABLE IF NOT EXISTS reference_nodes (
    id TEXT PRIMARY KEY,
    dimension TEXT NOT NULL,
    volume INTEGER NOT NULL,
    chapter INTEGER NOT NULL,
    title TEXT NOT NULL,
    description TEXT NOT NULL,
    payload TEXT NOT NULL,
    created_at TEXT NOT NULL,
    created_by TEXT NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_nodes_volume ON reference_nodes(volume);
CREATE INDEX IF NOT EXISTS idx_nodes_dim ON reference_nodes(dimension);

CREATE TABLE IF NOT EXISTS reference_edges (
    id TEXT PRIMARY KEY,
    from_node_id TEXT NOT NULL REFERENCES reference_nodes(id),
    to_node_id TEXT NOT NULL REFERENCES reference_nodes(id),
    relationship_type TEXT NOT NULL,
    weight REAL NOT NULL,
    payload TEXT NOT NULL,
    created_at TEXT NOT NULL,
    created_by TEXT NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_edges_from ON reference_edges(from_node_id);
CREATE INDEX IF NOT EXISTS idx_edges_to ON reference_edges(to_node_id);

CREATE TABLE IF NOT EXISTS reference_ripples (
    id TEXT PRIMARY KEY,
    trigger_volume INTEGER NOT NULL,
    trigger_chapter INTEGER NOT NULL,
    affected_nodes TEXT NOT NULL,
    affected_edges TEXT NOT NULL,
    proposed_actions TEXT NOT NULL,
    status TEXT NOT NULL,
    created_at TEXT NOT NULL,
    confirmed_at TEXT,
    applied_at TEXT,
    payload TEXT NOT NULL
);
"""


class RippleStorage:
    def __init__(self, db_path: Path) -> None:
        self._db_path = db_path
        db_path.parent.mkdir(parents=True, exist_ok=True)
        with self._connect() as conn:
            conn.execute("PRAGMA synchronous=FULL")
            conn.execute("PRAGMA journal_mode=WAL")
            conn.execute("PRAGMA foreign_keys=ON")
            conn.executescript(_SCHEMA_SQL)
            conn.commit()
        logger.info("ripple.db initialized at %s", db_path)

    @contextmanager
    def _connect(self) -> Iterator[sqlite3.Connection]:
        conn = sqlite3.connect(str(self._db_path))
        conn.row_factory = sqlite3.Row
        try:
            yield conn
        finally:
            conn.close()

    @contextmanager
    def atomic_batch(self) -> Iterator[sqlite3.Connection]:
        with self._connect() as conn:
            conn.execute("BEGIN IMMEDIATE")
            try:
                yield conn
                conn.commit()
            except Exception:
                conn.rollback()
                raise

    def append_node(self, node: ReferenceNode) -> None:
        with self._connect() as conn:
            try:
                conn.execute(
                    "INSERT INTO reference_nodes VALUES (?,?,?,?,?,?,?,?,?)",
                    (node.id, node.dimension, node.volume, node.chapter,
                     node.title, node.description, json.dumps(node.payload, ensure_ascii=False),
                     node.created_at.isoformat(), node.created_by),
                )
                conn.commit()
            except sqlite3.IntegrityError as e:
                raise ValueError(f"storage integrity: {e}") from e

    def append_edge(self, edge: ReferenceEdge) -> None:
        with self._connect() as conn:
            try:
                conn.execute(
                    "INSERT INTO reference_edges VALUES (?,?,?,?,?,?,?,?)",
                    (edge.id, edge.from_node_id, edge.to_node_id, edge.relationship_type,
                     edge.weight, json.dumps(edge.payload, ensure_ascii=False),
                     edge.created_at.isoformat(), edge.created_by),
                )
                conn.commit()
            except sqlite3.IntegrityError as e:
                raise ValueError(f"storage integrity: {e}") from e

    def append_ripple(self, ripple: CrossVolumeRipple) -> None:
        with self._connect() as conn:
            try:
                conn.execute(
                    "INSERT INTO reference_ripples VALUES (?,?,?,?,?,?,?,?,?,?,?)",
                    (ripple.id, ripple.trigger_volume, ripple.trigger_chapter,
                     json.dumps(list(ripple.affected_nodes), ensure_ascii=False),
                     json.dumps(list(ripple.affected_edges), ensure_ascii=False),
                     json.dumps(list(ripple.proposed_actions), ensure_ascii=False),
                     ripple.status, ripple.created_at.isoformat(),
                     ripple.confirmed_at.isoformat() if ripple.confirmed_at else None,
                     ripple.applied_at.isoformat() if ripple.applied_at else None,
                     json.dumps(ripple.payload, ensure_ascii=False)),
                )
                conn.commit()
            except sqlite3.IntegrityError as e:
                raise ValueError(f"storage integrity: {e}") from e

    def load_all_nodes(self) -> list[ReferenceNode]:
        with self._connect() as conn:
            return [self._row_to_node(row) for row in conn.execute("SELECT * FROM reference_nodes")]

    def load_all_edges(self) -> list[ReferenceEdge]:
        with self._connect() as conn:
            return [self._row_to_edge(row) for row in conn.execute("SELECT * FROM reference_edges")]

    def load_all_ripples(self) -> list[CrossVolumeRipple]:
        with self._connect() as conn:
            return [self._row_to_ripple(row) for row in conn.execute("SELECT * FROM reference_ripples")]

    def _row_to_node(self, row: sqlite3.Row) -> ReferenceNode:
        return ReferenceNode(
            id=row["id"], dimension=row["dimension"], volume=row["volume"],
            chapter=row["chapter"], title=row["title"], description=row["description"],
            payload=json.loads(row["payload"]),
            created_at=datetime.fromisoformat(row["created_at"]),
            created_by=row["created_by"],
        )

    def _row_to_edge(self, row: sqlite3.Row) -> ReferenceEdge:
        return ReferenceEdge(
            id=row["id"], from_node_id=row["from_node_id"], to_node_id=row["to_node_id"],
            relationship_type=row["relationship_type"], weight=row["weight"],
            payload=json.loads(row["payload"]),
            created_at=datetime.fromisoformat(row["created_at"]),
            created_by=row["created_by"],
        )

    def _row_to_ripple(self, row: sqlite3.Row) -> CrossVolumeRipple:
        return CrossVolumeRipple(
            id=row["id"], trigger_volume=row["trigger_volume"],
            trigger_chapter=row["trigger_chapter"],
            affected_nodes=tuple(json.loads(row["affected_nodes"])),
            affected_edges=tuple(json.loads(row["affected_edges"])),
            proposed_actions=tuple(json.loads(row["proposed_actions"])),
            status=row["status"],
            created_at=datetime.fromisoformat(row["created_at"]),
            confirmed_at=datetime.fromisoformat(row["confirmed_at"]) if row["confirmed_at"] else None,
            applied_at=datetime.fromisoformat(row["applied_at"]) if row["applied_at"] else None,
            payload=json.loads(row["payload"]),
        )
```

- [ ] **Step 3.2**: Run all 16 tests, verify GREEN

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pytest tests/cross_volume/ -v 2>&1 | tail -25
# 期望: 16 passed
```

- [ ] **Step 3.3**: Commit RippleStorage (GREEN final)

```bash
git add infra/cross_volume/storage.py
git commit -m "feat(cross-volume): phase 9.10 — RippleStorage sqlite3 直写

3 tables (reference_nodes / reference_edges / reference_ripples) +
4 indexes (idx_nodes_volume / idx_nodes_dim / idx_edges_from / idx_edges_to)
+ 3 PRAGMAs (synchronous=FULL fsync, journal_mode=WAL concurrent, foreign_keys=ON).

7 public methods + 1 context manager (atomic_batch):
- append_node / append_edge / append_ripple: single INSERT autocommit,
  IntegrityError → ValueError wrap
- atomic_batch: BEGIN IMMEDIATE + commit/rollback, fsync on commit
- load_all_nodes / load_all_edges / load_all_ripples: SELECT *, _row_to_*
  反序列化 (JSON payload + ISO 8601 datetime)

跟 CostTrackerDB / WorkflowDB precedent 1:1:
- sqlite3.Row row_factory for column access
- json.dumps(..., ensure_ascii=False) 跟中文 payload 兼容
- datetime.fromisoformat() 跟 Python 3.12 stdlib
- IntegrityError → ValueError wrap (跟 Phase 8.5 cost_persistence.py 1:1)

16 tests passed (6 reference_graph + 3 ripple + 7 storage), 0 production
code 改, 0 改 12 SCENARIOS, 0 改 CLAUDE.md / pyproject.toml."
```

---

## Task 4: Full Verification (pytest + ruff + gitignore + 0 production code 改)

- [ ] **Step 4.1**: Run full pytest, verify baseline + 16 new

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pytest -q 2>&1 | tail -5
# 期望: 2278 passed, 0 failed, 27 skipped (2262 + 16)
```

- [ ] **Step 4.2**: Run ruff on new code

```bash
ruff check infra/cross_volume/ tests/cross_volume/
# 期望: All checks passed!
```

- [ ] **Step 4.3**: Verify gitignore covers ripple.db

```bash
git check-ignore infra/.state/ripple.db
# 期望: infra/.state/ripple.db (gitignored)
# 若未 ignore, 检查 .gitignore 含 "infra/.state/" 或 "*.db", 补全
```

- [ ] **Step 4.4**: Verify 0 production code 改

```bash
git diff --stat 3787bee..HEAD -- infra/ai_service/ infra/agent_system/ dashboard/
# 期望: empty (0 改)
```

- [ ] **Step 4.5**: Verify 0 historical spec/plan doc 改

```bash
git diff --stat 3787bee..HEAD -- novel-factory/docs/superpowers/specs/2026-06-08-* novel-factory/docs/superpowers/plans/2026-06-08-*
# 期望: empty (Phase 9 spec / blueprint 0 改)
```

- [ ] **Step 4.6**: No commit (verification only, no changes)

---

## Task 5: Memory Sync (MEMORY.md + phases-8-dashboard.md entry)

**Files:**
- Modify: `~/.claude/projects/-home-ailearn-projects-AI-Incursion-domains-IP---projects-LingWen/memory/MEMORY.md` (L12 + L26)
- Modify: `~/.claude/projects/-home-ailearn-projects-AI-Incursion-domains-IP---projects-LingWen/memory/phases-8-dashboard.md` (append Phase 9.10 entry)

- [ ] **Step 5.1**: Update MEMORY.md L12 (add Phase 9.10 cross-volume ripple to dashboard phase link)

```bash
# Read current MEMORY.md
cat ~/.claude/projects/-home-ailearn-projects-AI-Incursion-domains-IP---projects-LingWen/memory/MEMORY.md | head -15
```

Edit L12 to include Phase 9.10 cross-volume ripple:

```markdown
- [Phase 8.16-8.45 + 9.10 dashboard polish + build tooling + test infra + UI completeness + cross-volume ripple data layer](phases-8-dashboard.md) — 7d/30d/all 时间窗 / composables / CostTrendChart / vitest 真 e2e 化 / data-testid 顶层统一 / ESLint 8→10 / husky + CI / Codecov / Playwright install / Phase 9.10 cross-volume ripple data model + persistence (3 frozen dataclass + 1 CVG + 1 storage, 16 tests)
```

- [ ] **Step 5.2**: Update MEMORY.md L26 (bump v9.16 → v9.17)

```markdown
- **当前版本**: v9.17 (Phase 9.10 cross-volume ripple data model + persistence 已合并, 2026-06-09)
```

- [ ] **Step 5.3**: Append Phase 9.10 entry to phases-8-dashboard.md

Read phases-8-dashboard.md end, then append:

```markdown
## Phase 9.10 — Cross-Volume Ripple Data Model + Persistence (2026-06-09)

**What**: Phase 9 (跨卷涟漪) 5 super-phases 路线图 Phase 10 实施. 4 source files (`infra/cross_volume/{__init__,reference_graph,ripple,storage}.py`, ~265L) + 3 test files (16 tests) + 1 gitignored DB (`infra/.state/ripple.db`).

**Why**: 主公决策 (2026-06-08 AskUserQuestion): 4 维全 (角色/伏笔/设定/plot point) × 半自动触发. Phase 10 纯数据层 (0 LLM 成本), backfill 留 Phase 11+. Spec 跟 plan 主公 approved 2026-06-09.

**How to apply**: 跟 cost_tracker.db / workflow.db precedent 1:1. 0 production code 改. 2262 + 16 = 2278 passed baseline. Phase 11+ LLM scanner / FastAPI / Vue UI / WebSocket / rollback / cascade BFS 全部 defer.

**Commits** (本 phase):
- (主公 review 时补)
```

- [ ] **Step 5.4**: Verify MEMORY.md line count < 200 (跟 0 例外 hard rule)

```bash
wc -l ~/.claude/projects/-home-ailearn-projects-AI-Incursion-domains-IP---projects-LingWen/memory/MEMORY.md
# 期望: ≤ 200 (0 例外)
```

- [ ] **Step 5.5**: Memory sync 0 commit (memory 文件在 Claude auto-memory, 不在 git repo)

---

## Task 6: Final Feat Commit + Push

- [ ] **Step 6.1**: Verify all changes are committed

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git status
# 期望: clean (3 commits ahead of 3787bee: RED tests + CVG/dacaclass + Storage)
git log --oneline 3787bee..HEAD
# 期望: 3 commits:
#   feat(cross-volume): phase 9.10 — RippleStorage sqlite3 直写
#   feat(cross-volume): phase 9.10 — CVG container + 3 frozen dataclass
#   test(cross-volume): phase 9.10 — 16 failing tests (RED checkpoint)
```

- [ ] **Step 6.2**: Push to origin/master

```bash
git push origin master
# 期望: 3 commits pushed, 0 force-push
```

- [ ] **Step 6.3**: Verify remote state

```bash
git log --oneline -1 origin/master
# 期望: feat(cross-volume): phase 9.10 — RippleStorage sqlite3 直写
git fetch origin && git status
# 期望: Your branch is up to date with 'origin/master'
```

---

## Task 7: Final DoD Verification + 总结

- [ ] **Step 7.1**: Run DoD checklist (跟 spec 一致)

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
echo "=== Files ==="
ls -la infra/cross_volume/ tests/cross_volume/
echo "=== Tests ==="
pytest tests/cross_volume/ -v 2>&1 | tail -5
echo "=== Baseline ==="
pytest -q 2>&1 | tail -3
echo "=== Lint ==="
ruff check infra/cross_volume/ tests/cross_volume/
echo "=== Gitignore ==="
git check-ignore infra/.state/ripple.db
echo "=== 0 production code 改 ==="
git diff --stat 3787bee..HEAD -- infra/ai_service/ infra/agent_system/ dashboard/
echo "=== 0 historical doc 改 ==="
git diff --stat 3787bee..HEAD -- novel-factory/docs/superpowers/specs/2026-06-08-* novel-factory/docs/superpowers/plans/2026-06-08-*
echo "=== 0 Co-Authored-By ==="
git log 3787bee..HEAD --format="%B" | grep -c "Co-Authored-By"
# 期望: 0
```

- [ ] **Step 7.2**: 总结给主公

```
Phase 9.10 完成总结:
- 3 commits pushed to origin/master (RED tests + CVG + Storage)
- 7 new files (4 source ~265L + 3 test ~250L)
- 16 tests passed (6 reference_graph + 3 ripple + 7 storage)
- baseline 2278 passed, 0 failed, 27 skipped
- ruff 0 issues
- 0 production code 改 (12 SCENARIOS / MasterController / dashboard 全部 0 触)
- 0 historical spec/plan doc 改
- 0 CLAUDE.md / pyproject.toml / pytest.ini 改
- 0 API key 泄漏
- MEMORY.md + phases-8-dashboard.md 同步 (v9.17)

后续 phases (defer, 0 改 in Phase 10):
- Phase 11.1-11.4: LLM scanner 4 prompts
- Phase 11.5: backfill.py
- Phase 12.1-12.4: FastAPI + Vue + WebSocket
- Phase 13.1-13.4: audit log + fsync + rollback + FK ON DELETE
- Phase 14.1-14.4: cascade BFS + cache + dual storage + perf

Phase 9 (5 super-phases) 进度: 10/14 (71%). 11-14 defer, 0 触 in Phase 10.
```

---

## Out of Scope (defer to Phase 11-14, 0 触 in Phase 10)

- **Phase 11.1-11.4**: LLM scanner 4 prompts (character / foreshadow / setting / plot_point)
- **Phase 11.5**: `backfill.py` (历史 359 章 LLM 扫描)
- **Phase 12.1-12.2**: FastAPI `POST /api/ripple` + `GET /api/ripple/:id`
- **Phase 12.3**: Vue 3 `ReviewRipple.vue` (impact 列表 + diff preview)
- **Phase 12.4**: WebSocket `/ws/ripple` cascade event push
- **Phase 13.1**: `audit_log` table + trigger
- **Phase 13.2**: fsync tuning (`synchronous=EXTRA`)
- **Phase 13.3**: real rollback logic (savepoint per ripple)
- **Phase 13.4**: FK `ON DELETE CASCADE` 节点删除
- **Phase 14.1**: real BFS cascade impl (替换 `trigger_cascade` stub)
- **Phase 14.2**: cache layer (LRU 1000 nodes)
- **Phase 14.3**: dual storage (SQLite + JSON snapshot)
- **Phase 14.4**: perf benchmark + optimize
- **0 触 in Phase 10**: 12 SCENARIOS 路由, MasterController, decision 流程, dashboard UI, workflow.db, cost_tracker.db

---

## Risks + Mitigations

| 风险 | 缓解 |
|------|------|
| 2262 baseline tests 破 | CVG 纯 add-only, 0 read production code path, 0 mutation API |
| Append-only 被误 mutate | 3 dataclass `frozen=True` + 0 `__hash__` override + 0 `delete_node` method |
| FK violation 在生产 | `PRAGMA foreign_keys=ON` + CVG `add_edge` 内存先检查 |
| 并发写竞争 | WAL mode 1-writer-N-reader; Phase 10 single-process OK |
| DB 损坏 | `synchronous=FULL` fsync; Phase 13 加 audit log 跟 repair tool |
| payload JSON 不可序列化 | caller 责任, `json.dumps` raise `TypeError` propagate |
| LLM scanner 强依赖 data layer | Phase 11 prompt 设计先 verify data model 字段够用 (Section 1 payload schema 满足 4 维最小需求) |
| gitignore 漏 `ripple.db` | 跟 `state/*` pattern 一致, 0 改 `.gitignore` (Phase 8.5 已含) |
| MEMORY.md / phases-8-dashboard.md 追记 | Task 5 explicit step, line count check < 200 |
| uuid import 漏 | ripple.py 用 `__import__("uuid").uuid4()` 避免 import cycle (reference_graph.py 已 import ripple) |
| `__post_init__` 跟 `frozen=True` 不兼容 | Python 3.12 OK (frozen + post_init 标准 pattern, 跟 ThoughtNode 1:1) |
| Task 2 GREEN fail | step 2.4 期望 9 ERROR (Storage ImportError), 0 期望 9 passed (Storage 还没 impl) |

---

## Critical files

### Created
- `novel-factory/infra/cross_volume/__init__.py` (~15L) — 公开 4 exports (CVG + 3 dataclass)
- `novel-factory/infra/cross_volume/reference_graph.py` (~120L) — CVG container + ReferenceNode + ReferenceEdge
- `novel-factory/infra/cross_volume/ripple.py` (~50L) — CrossVolumeRipple + trigger_cascade stub
- `novel-factory/infra/cross_volume/storage.py` (~80L) — RippleStorage sqlite3 直写
- `novel-factory/tests/cross_volume/test_reference_graph.py` (~100L) — 6 tests
- `novel-factory/tests/cross_volume/test_ripple.py` (~60L) — 3 tests
- `novel-factory/tests/cross_volume/test_storage.py` (~90L) — 7 tests

### Modified (only memory files, 0 production code)
- `~/.claude/projects/.../memory/MEMORY.md` (L12 + L26, ~3 lines, 0 commit)
- `~/.claude/projects/.../memory/phases-8-dashboard.md` (append Phase 9.10 entry, 0 commit)

### NOT modified (0 例外 hard rule)
- `novel-factory/infra/ai_service/cost_tracker.py` / `cost_tracker_db.py` (0 改)
- `novel-factory/infra/agent_system/master_controller.py` (0 改)
- `novel-factory/infra/agent_system/got_bridge.py` (0 改)
- `novel-factory/infra/agent_system/workflow_db.py` (0 改)
- `novel-factory/dashboard/frontend/**` (0 改, Phase 10 纯 backend)
- `novel-factory/CLAUDE.md` (0 改)
- `novel-factory/pyproject.toml` (0 改)
- `novel-factory/pytest.ini` (0 改)
- `novel-factory/docs/superpowers/specs/2026-06-08-*` (Phase 9 spec 0 改, `:NNN` 行号不动)
- `novel-factory/docs/superpowers/plans/2026-06-08-*` (Phase 9 blueprint 0 改, `:NNN` 行号不动)
- 16 historical spec/plan docs (0 改, 跟 Phase 8.45 precedent 一致)

### Gitignored (NOT committed)
- `novel-factory/infra/.state/ripple.db` (跟 `state/*` pattern, 0 改 .gitignore)
- `novel-factory/infra/.state/ripple.db-journal` (sqlite3 WAL)
- `novel-factory/infra/.state/ripple.db-wal` (sqlite3 WAL)
- `novel-factory/infra/.state/ripple.db-shm` (sqlite3 WAL)

---

## DoD (Definition of Done)

- [ ] `infra/cross_volume/__init__.py` 创建 (~15L, 公开 4 exports)
- [ ] `infra/cross_volume/reference_graph.py` 创建 (~120L, CVG + ReferenceNode + ReferenceEdge)
- [ ] `infra/cross_volume/ripple.py` 创建 (~50L, CrossVolumeRipple + trigger_cascade stub)
- [ ] `infra/cross_volume/storage.py` 创建 (~80L, RippleStorage)
- [ ] `tests/cross_volume/test_reference_graph.py` 创建 (~100L, 6 tests)
- [ ] `tests/cross_volume/test_ripple.py` 创建 (~60L, 3 tests)
- [ ] `tests/cross_volume/test_storage.py` 创建 (~90L, 7 tests)
- [ ] pytest 2278 passed, 0 failed, 27 skipped (2262 + 16 new)
- [ ] ruff 0 issues
- [ ] `git check-ignore infra/.state/ripple.db` → gitignored
- [ ] 3 atomic commits pushed to origin/master (RED tests + CVG/dacaclass + Storage)
- [ ] 0 production code 改 (12 SCENARIOS, MasterController, dashboard, workflow.db, cost_tracker.db)
- [ ] 0 backend / frontend historical spec/plan doc 改 (`:NNN` 行号不动)
- [ ] 0 改 CLAUDE.md / pyproject.toml / pytest.ini
- [ ] 0 API key 泄漏 / 0 .env 改
- [ ] 0 force-push, 0 --amend, 0 --no-verify, 0 Co-Authored-By footer
- [ ] MEMORY.md (L12 + L26) 同步 (v9.17)
- [ ] phases-8-dashboard.md 追加 Phase 9.10 entry
- [ ] MEMORY.md line count < 200 (跟 0 例外 hard rule)

---

## 完成后下一步 (后续 phases)

- **Phase 11.1-11.4**: LLM scanner 4 prompts (character / foreshadow / setting / plot_point)
- **Phase 11.5**: `backfill.py` (历史 359 章 LLM 扫描)
- **Phase 12.1-12.4**: FastAPI endpoints + Vue review UI + WebSocket
- **Phase 13.1-13.4**: audit log + fsync tuning + rollback + FK ON DELETE
- **Phase 14.1-14.4**: real cascade BFS + cache + dual storage + perf
- **Phase 9 (5 super-phases) 进度**: 10/14 (71%) after Phase 9.10
- **MEMORY.md 拆 40+ entries** (defer, Phase 9 完后统一拆)
- **Phases 8 dashboard 拆 2 sub-era** (defer, MEMORY.md 拆时一起做)
- **legacy ./memory/MEMORY.md v6.1 → v9.17 sync** (defer)
- **Coverage threshold branches 50→60** (defer, followup phase)
- **Playwright browser install + CI integration** (defer, followup phase)
