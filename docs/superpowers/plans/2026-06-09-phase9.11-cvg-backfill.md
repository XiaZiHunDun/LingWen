# Phase 9.11 — CVG Backfill (4-Dim Rule-Based Extractor) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement Phase 9.11 cross-volume ripple backfill (1 YAML config + 4 Extractor class + 1 Backfiller + 1 additive storage method + 1 CLI sub-command), 16 tests TDD RED→GREEN (15 backfill + 1 storage atomicity), 0 production code 改, 0 LLM 成本, 0 Edge 创建.

**Architecture:** 4 Extractor class 走 YAML 注入规则, 各产 ReferenceNode list (deterministic id `{dim}:{canonical_name}`); Backfiller 编排扫 359 章正文 + 大纲 → 4 维去重聚合 (跨章合 1 node + payload `chapter_appearances` 累积) → 1 additive `RippleStorage.append_nodes_atomic` 1 commit 全部 N nodes (复用 Phase 9.10 `atomic_batch` context manager, 0 改现有 API). CLI `lingwen.py ripple backfill --dry-run/--execute --vol N` 默认 dry-run.

**Tech Stack:** Python 3.12 + `re` stdlib + `yaml.safe_load` (跟 codebase 1:1, 0 用 unsafe `yaml.load`) + `sqlite3` stdlib (Phase 9.10) + `click` (Phase 4-7 CLI precedent) + `pytest` 8 + `ruff` 0.6+.

---

## Pre-Execution Setup

**Step 0.1**: Verify working directory + baseline

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git rev-parse --show-toplevel  # 期望: /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
git log --oneline -1            # 期望: c421da1 docs(spec): phase 9.11 — CVG backfill 4-dim rule-based extractor design
git status                      # 期望: clean
pytest -q 2>&1 | tail -3        # 期望: 2278 passed, 0 failed, 27 skipped
```

**Step 0.2**: Verify Phase 9.10 4 source files exist (Backfiller 通过这些 API 注入)

```bash
ls infra/cross_volume/
# 期望: __init__.py reference_graph.py ripple.py storage.py
```

**Step 0.3**: Verify 359 章 corpus 存在 (跟 Phase 9.10 1:1)

```bash
ls 03_内容仓库/04_正文/ | head -5
ls 03_内容仓库/04_正文/ | wc -l
# 期望: ch001.md ch001_大纲.md ch002.md ... 共 718 文件 (359 正文 + 359 大纲)
```

**Step 0.4**: Verify `.gitignore` 含 `infra/.state/ripple.db` (Phase 9.10 已加)

```bash
grep "ripple" .gitignore
# 期望: infra/.state/ripple.db (Phase 9.10 加)
```

**Step 0.5**: Read spec for reference

```bash
head -100 docs/superpowers/specs/2026-06-09-phase9.11-cvg-backfill-design.md
```

---

## Critical Safety (主公 hard rules, 0 例外)

| 规则 | 说明 |
|------|------|
| 0 force-push | `git push --force` / `git push -f` 永不用 |
| 0 amend | 所有 fixup 走新 commit, 不用 `git commit --amend` |
| 0 Co-Authored-By footer | Attribution 已在 `~/.claude/settings.json` 全局禁用 |
| 0 state DB 提交 | `infra/.state/ripple.db` gitignored (Phase 9.10 加), 误 add 即 revert |
| 0 DANGEROUSLY_SKIP_PERMISSIONS | 跟 codebase precedent 一致 |
| 0 API key 泄漏 | 0 LLM provider 改 (Phase 9.11 纯规则, 0 token 消耗), 0 .env 改, 0 ANTHROPIC_API_KEY 引用 |
| 0 改 CLAUDE.md / pyproject.toml / pytest.ini | hard-coded 文件不动 |
| 0 改 12 SCENARIOS 路由 | Phase 9.11 纯 backfill, 0 触 workflow |
| 0 改 historical spec/plan doc `:NNN` 行号 | 历史已不准, 改反误导, 永不做 |
| 0 改 Phase 9.10 4 source 现有 API 行为 | 1 additive `append_nodes_atomic` 仅 +10L 0 行为变 |
| 沟通 | 叙述中文 / 代码英文 / commit body 中文注释 |
| 0 production code 改 | MasterController, dashboard, workflow.db, cost_tracker.db 全部 0 触 |
| File size | 200-400L typical, 800L hard cap |
| TDD | RED → verify RED → GREEN → verify GREEN → commit |
| Conventional commits | feat / fix / refactor / docs / test / chore / perf / ci |
| 1 atomic feat commit | 1 commit 含全部 9 files 改, 跟 Phase 9.10 7 commits 拆分模式不同, 0 sub-step commit |

---

## Task 1: Write 16 Failing Tests (RED)

**Files:**
- Create: `tests/cross_volume/test_backfill.py` (~165L, 15 tests)
- Modify: `tests/cross_volume/test_storage.py` (+30L, 1 new test)

- [ ] **Step 1.1**: Add 1 new test to `tests/cross_volume/test_storage.py` (test_append_nodes_atomic_atomicity, ~30L, ALL FAIL with AttributeError)

```python
# Append to tests/cross_volume/test_storage.py
class TestAppendNodesAtomic:
    def test_append_nodes_atomic_commits_all_on_success(self, tmp_path):
        """Phase 9.11: append_nodes_atomic 写 N=100 nodes 1 call, 0 partial commit."""
        from infra.cross_volume.reference_graph import ReferenceNode
        storage = RippleStorage(db_path=tmp_path / "ripple.db")
        nodes = [
            ReferenceNode(
                id=f"character:test_{i}", dimension="character", volume=1, chapter=1,
                title=f"test_{i}", description="atomic test",
                payload={"name": f"test_{i}", "role": "test"},
            )
            for i in range(100)
        ]
        storage.append_nodes_atomic(nodes)
        loaded = storage.load_all_nodes()
        assert len(loaded) == 100
        loaded_ids = {n.id for n in loaded}
        expected_ids = {f"character:test_{i}" for i in range(100)}
        assert loaded_ids == expected_ids

    def test_append_nodes_atomic_rollback_on_duplicate_id(self, tmp_path):
        """Phase 9.11: append_nodes_atomic 异常时 0 partial commit (atomic_batch 兜底)."""
        from infra.cross_volume.reference_graph import ReferenceNode
        storage = RippleStorage(db_path=tmp_path / "ripple.db")
        nodes = [
            ReferenceNode(
                id=f"character:test_{i}", dimension="character", volume=1, chapter=1,
                title=f"test_{i}", description="",
            )
            for i in range(50)
        ]
        # 注入 1 重复 id 触发 IntegrityError → ValueError → rollback
        nodes.append(
            ReferenceNode(
                id="character:test_25", dimension="character", volume=1, chapter=1,
                title="dup", description="",
            )
        )
        with pytest.raises(ValueError, match="storage integrity"):
            storage.append_nodes_atomic(nodes)
        # rollback → 0 node 写库
        loaded = storage.load_all_nodes()
        assert len(loaded) == 0
```

- [ ] **Step 1.2**: Create `tests/cross_volume/test_backfill.py` (15 tests, ALL FAIL with ImportError)

```python
# tests/cross_volume/test_backfill.py
"""Phase 9.11: Backfiller + 4 Extractor + CLI tests.

TDD: 这些测试在 step 1 阶段会全部 FAIL (ImportError on Backfiller / 4 Extractor),
在 Task 2 (Extractor impl) + Task 3 (Backfiller + storage.append_nodes_atomic) + Task 4 (CLI) 后通过.
"""
import yaml
import pytest
from pathlib import Path

from infra.cross_volume import ReferenceNode
from infra.cross_volume.storage import RippleStorage
from infra.cross_volume.backfill import Backfiller, BackfillStats
from infra.cross_volume.extractors import (
    CharacterExtractor,
    ForeshadowExtractor,
    SettingExtractor,
    PlotPointExtractor,
)
from infra.cross_volume.reference_graph import CrossVolumeReferenceGraph


# ============ 4 Extractor unit tests (8 tests) ============

class TestCharacterExtractor:
    @pytest.fixture
    def rules(self):
        return {
            "name_pattern": "[李青云林凡]{2,3}",
            "alias_map": {"凡": "林凡"},
            "blacklist": ["那个", "什么"],
            "occurrence_threshold": 1,
        }

    @pytest.fixture
    def extractor(self, rules):
        return CharacterExtractor(rules=rules, volume=1)

    def test_extracts_canonical_names(self, extractor, tmp_path):
        chapter = tmp_path / "ch001.md"
        chapter.write_text("李青云走进林凡身旁, 二人对视.", encoding="utf-8")
        nodes = extractor.extract(chapter)
        names = {n.payload["name"] for n in nodes}
        assert names == {"李青云", "林凡"}
        assert len(nodes) == 2  # alias_map 命中 "凡" → "林凡", 0 重复

    def test_blacklist_filters_common_words(self, extractor, tmp_path):
        chapter = tmp_path / "ch001.md"
        chapter.write_text("那个 什么 怎么 这里 那里 如此 这般, 李青云走过.", encoding="utf-8")
        nodes = extractor.extract(chapter)
        names = {n.payload["name"] for n in nodes}
        assert "那个" not in names
        assert "什么" not in names
        assert names == {"李青云"}


class TestForeshadowExtractor:
    @pytest.fixture
    def rules(self):
        return {
            "keywords": ["伏笔", "暗示", "铺垫"],
            "pattern": "(?P<content>[\\u4e00-\\u9fff]{6,30}?(?:伏笔|暗示|铺垫))",
            "chapter_window": 5,
        }

    @pytest.fixture
    def extractor(self, rules):
        return ForeshadowExtractor(rules=rules, volume=1)

    def test_extracts_keyword_context(self, extractor, tmp_path):
        chapter = tmp_path / "ch001_大纲.md"
        chapter.write_text("# 章节一\n\n古剑碎片是本章伏笔, 林凡将继承此剑.\n", encoding="utf-8")
        nodes = extractor.extract(chapter)
        assert len(nodes) >= 1
        assert any("伏笔" in n.payload.get("content", "") for n in nodes)

    def test_empty_chapter_returns_empty_list(self, extractor, tmp_path):
        chapter = tmp_path / "ch001_大纲.md"
        chapter.write_text("# 章节一\n\n普通叙事无伏笔信号.\n", encoding="utf-8")
        nodes = extractor.extract(chapter)
        assert nodes == []


class TestSettingExtractor:
    @pytest.fixture
    def rules(self):
        return {
            "name_pattern": "[\\u4e00-\\u9fff]{2,6}(?:学院|山门|大陆|帝国|秘境|洞府|遗迹|海域|城|宗|派|观|谷)",
            "blacklist": ["这个", "那个"],
            "occurrence_threshold": 1,
        }

    @pytest.fixture
    def extractor(self, rules):
        return SettingExtractor(rules=rules, volume=1)

    def test_extracts_noun_suffixed_settings(self, extractor, tmp_path):
        chapter = tmp_path / "ch001.md"
        chapter.write_text("李青云拜入凌霄宗, 游历青云大陆, 探访古幽秘境.", encoding="utf-8")
        nodes = extractor.extract(chapter)
        names = {n.payload["name"] for n in nodes}
        assert "凌霄宗" in names
        assert "青云大陆" in names
        assert "古幽秘境" in names

    def test_blacklist_filters_pronouns(self, extractor, tmp_path):
        chapter = tmp_path / "ch001.md"
        chapter.write_text("这个 那个 此刻, 李青云走向凌霄宗.", encoding="utf-8")
        nodes = extractor.extract(chapter)
        names = {n.payload["name"] for n in nodes}
        assert names == {"凌霄宗"}


class TestPlotPointExtractor:
    @pytest.fixture
    def rules(self):
        return {
            "pattern": "^##\\s+(.+)$",
            "min_length": 8,
            "max_length": 50,
            "blacklist": ["^## 简介$", "^## 备注$"],
        }

    @pytest.fixture
    def extractor(self, rules):
        return PlotPointExtractor(rules=rules, volume=1)

    def test_extracts_markdown_h2_titles(self, extractor, tmp_path):
        chapter = tmp_path / "ch001_大纲.md"
        chapter.write_text(
            "# 章节一\n\n## 青云觉醒获得古剑碎片\n## 林凡登场\n## 简介\n",
            encoding="utf-8",
        )
        nodes = extractor.extract(chapter)
        titles = {n.payload["title"] for n in nodes}
        assert "青云觉醒获得古剑碎片" in titles
        assert "林凡登场" in titles
        assert "简介" not in titles  # blacklist

    def test_filters_short_titles(self, extractor, tmp_path):
        chapter = tmp_path / "ch001_大纲.md"
        chapter.write_text("## 太短\n## 简介\n## 备注\n", encoding="utf-8")
        nodes = extractor.extract(chapter)
        # min_length=8 过滤 "太短" (2 字) + blacklist 过滤
        assert nodes == []


# ============ Backfiller orchestration tests (3 tests) ============

class TestBackfiller:
    @pytest.fixture
    def rules_yaml(self, tmp_path):
        yaml_content = """
character:
  name_pattern: "[李青云林凡]{2,3}"
  alias_map: {"凡": "林凡"}
  blacklist: []
  occurrence_threshold: 1
foreshadow:
  keywords: ["伏笔", "暗示"]
  pattern: "(?P<content>[\\u4e00-\\u9fff]{6,30}?(?:伏笔|暗示))"
  chapter_window: 5
setting:
  name_pattern: "[\\u4e00-\\u9fff]{2,6}(?:学院|山门|大陆|帝国|秘境|宗)"
  blacklist: []
  occurrence_threshold: 1
plot_point:
  pattern: "^##\\s+(.+)$"
  min_length: 8
  max_length: 50
  blacklist: []
"""
        rules_path = tmp_path / "rules.yaml"
        rules_path.write_text(yaml_content, encoding="utf-8")
        return rules_path

    @pytest.fixture
    def corpus(self, tmp_path):
        corpus_root = tmp_path / "corpus"
        corpus_root.mkdir()
        # ch001: character + setting
        (corpus_root / "ch001.md").write_text("李青云拜入凌霄宗, 林凡登场.", encoding="utf-8")
        (corpus_root / "ch001_大纲.md").write_text(
            "# 章节一\n## 青云觉醒获得古剑碎片\n## 林凡登场\n", encoding="utf-8"
        )
        # ch002: character (跨章聚合 ch001)
        (corpus_root / "ch002.md").write_text("李青云与林凡激战.", encoding="utf-8")
        (corpus_root / "ch002_大纲.md").write_text(
            "# 章节二\n## 凌霄宗大战林凡\n", encoding="utf-8"
        )
        return corpus_root

    def test_backfill_dry_run_does_not_write(self, rules_yaml, corpus, tmp_path):
        """Phase 9.11: --dry-run 0 写库, 仅 print 统计."""
        storage = RippleStorage(db_path=tmp_path / "ripple.db")
        graph = CrossVolumeReferenceGraph(storage=storage)
        backfiller = Backfiller(rules_path=rules_yaml, corpus_root=corpus, graph=graph)
        stats = backfiller.run(dry_run=True)
        assert stats.dry_run is True
        assert stats.total_count >= 1
        # dry-run → 0 写库
        assert storage.load_all_nodes() == []

    def test_backfill_execute_writes_via_append_nodes_atomic(self, rules_yaml, corpus, tmp_path):
        """Phase 9.11: --execute 走 storage.append_nodes_atomic 1 commit."""
        storage = RippleStorage(db_path=tmp_path / "ripple.db")
        graph = CrossVolumeReferenceGraph(storage=storage)
        backfiller = Backfiller(rules_path=rules_yaml, corpus_root=corpus, graph=graph)
        stats = backfiller.run(dry_run=False)
        assert stats.dry_run is False
        assert stats.total_count >= 1
        # execute → 写库
        loaded = storage.load_all_nodes()
        assert len(loaded) == stats.total_count
        # 跨章聚合: 李青云 ch001 + ch002 → 1 node + chapter_appearances=[1, 2]
        liqy = [n for n in loaded if n.payload.get("name") == "李青云"]
        assert len(liqy) == 1
        assert set(liqy[0].payload["chapter_appearances"]) == {1, 2}

    def test_backfill_volume_filter_restricts_scan(self, rules_yaml, corpus, tmp_path):
        """Phase 9.11: --vol 1 抽样式, 仅扫 vol 1 章."""
        storage = RippleStorage(db_path=tmp_path / "ripple.db")
        graph = CrossVolumeReferenceGraph(storage=storage)
        backfiller = Backfiller(rules_path=rules_yaml, corpus_root=corpus, graph=graph)
        stats_all = backfiller.run(dry_run=True, volume_filter=None)
        stats_v1 = backfiller.run(dry_run=True, volume_filter=1)
        # vol filter 0 写库, dry-run 0 写库, 仅统计差异 (vol filter 应 ≤ all)
        assert stats_v1.total_count <= stats_all.total_count


# ============ Integration tests (atomic_batch + dry-run gating) ============

class TestBackfillIntegration:
    def test_backfill_execute_commits_atomically(self, rules_yaml, corpus, tmp_path):
        """Phase 9.11: --execute 走 append_nodes_atomic, 0 partial write."""
        storage = RippleStorage(db_path=tmp_path / "ripple.db")
        graph = CrossVolumeReferenceGraph(storage=storage)
        backfiller = Backfiller(rules_path=rules_yaml, corpus_root=corpus, graph=graph)
        stats = backfiller.run(dry_run=False)
        # 全部 nodes 写库, 0 0 残留
        loaded = storage.load_all_nodes()
        assert len(loaded) == stats.total_count
        assert all(n.created_by.startswith("backfill:") for n in loaded)

    def test_backfill_dry_run_creates_no_db(self, rules_yaml, corpus, tmp_path):
        """Phase 9.11: dry-run 跑后, ripple.db 0 创建 / 0 创建后 0 nodes."""
        non_existent_db = tmp_path / "ripple_dry.db"
        storage = RippleStorage(db_path=non_existent_db)
        graph = CrossVolumeReferenceGraph(storage=storage)
        backfiller = Backfiller(rules_path=rules_yaml, corpus_root=corpus, graph=graph)
        backfiller.run(dry_run=True)
        # dry-run → 0 写库 (但 __init__ 已创建空 DB, load 应 0 nodes)
        assert storage.load_all_nodes() == []


# ============ E2E test (10 章 fixture) ============

class TestBackfillE2E:
    def test_backfill_runs_on_10_chapter_fixture(self, tmp_path):
        """Phase 9.11 E2E: 走 10 章真实 fixture, 验证 4 维 N nodes 范围合理 0 crash."""
        # fixture dir 在 tests/cross_volume/fixtures/
        from tests.cross_volume.fixtures.sample_corpus import SAMPLE_CORPUS_ROOT
        rules_yaml = tmp_path / "rules.yaml"
        rules_yaml.write_text(SAMPLE_RULES_YAML, encoding="utf-8")
        storage = RippleStorage(db_path=tmp_path / "ripple.db")
        graph = CrossVolumeReferenceGraph(storage=storage)
        backfiller = Backfiller(
            rules_path=rules_yaml, corpus_root=SAMPLE_CORPUS_ROOT, graph=graph
        )
        stats = backfiller.run(dry_run=False)
        # 4 维 N nodes 范围合理 (跟 fixture 内容有关, 估 ≥ 5 total, 各 dim ≥ 1)
        assert stats.total_count >= 5
        assert stats.character_count >= 1
        assert stats.plot_point_count >= 1
```

- [ ] **Step 1.3**: Verify all 16 tests FAIL (RED)

```bash
pytest tests/cross_volume/test_storage.py::TestAppendNodesAtomic -v 2>&1 | tail -5
# 期望: 2 failed (AttributeError: 'RippleStorage' object has no attribute 'append_nodes_atomic')

pytest tests/cross_volume/test_backfill.py -v 2>&1 | tail -20
# 期望: 15 failed (ImportError on Backfiller / 4 Extractor / SAMPLE_RULES_YAML 等)
```

- [ ] **Step 1.4**: Commit RED tests (跟 Phase 9.10 1:1 模式, 1 test-only commit)

```bash
git add tests/cross_volume/test_storage.py tests/cross_volume/test_backfill.py
git commit -m "test(backfill): phase 9.11 — 16 failing tests (RED checkpoint)

Phase 9.11 (CVG backfill) 16 net new tests written for TDD:
- 2 storage atomicity tests (append_nodes_atomic atomic + rollback)
- 8 extractor unit tests (4 Extractor × 2 tests each: 抽得对 / 抽错不抽)
- 3 backfiller orchestration tests (dry-run 0 写 / execute 写 + 跨章聚合 / vol filter)
- 2 backfill integration tests (atomic commit / dry-run 0 写)
- 1 E2E test (10 章 fixture, 4 维 N nodes 范围合理)

RED state (Task 1 末): 16 failed (ImportError + AttributeError + SAMPLE_RULES_YAML
undefined 全部 expected). GREEN 在 Task 2 (4 Extractor + yaml) + Task 3
(Backfiller + append_nodes_atomic) + Task 4 (CLI) + Task 5 (fixture) 后达成.

baseline: 2278 passed, 0 failed, 27 skipped
target (post-Task 7): 2278 + 16 = 2294 passed

0 production code 改 / 0 LLM 成本 / 0 Edge 创建.
0 historical spec/plan doc 改 / 0 force-push / 0 amend / 0 --no-verify / 0 Co-Authored-By footer."
```

---

## Task 2: Implement 4 Extractor + `extraction_rules.yaml` (GREEN 8 unit tests)

**Files:**
- Create: `infra/cross_volume/extractors/__init__.py` (~10L)
- Create: `infra/cross_volume/extractors/character_extractor.py` (~80L)
- Create: `infra/cross_volume/extractors/foreshadow_extractor.py` (~70L)
- Create: `infra/cross_volume/extractors/setting_extractor.py` (~70L)
- Create: `infra/cross_volume/extractors/plot_point_extractor.py` (~80L)
- Create: `infra/cross_volume/extraction_rules.yaml` (~60L)

- [ ] **Step 2.1**: Create `infra/cross_volume/extractors/__init__.py`

```python
# infra/cross_volume/extractors/__init__.py
"""Phase 9.11: 4-dim rule-based extractors for CVG backfill.

公开 API (跟 spec 5.1 一致):
- CharacterExtractor: character 维 (regex 中文名 + alias_map + blacklist + threshold)
- ForeshadowExtractor: foreshadow 维 (keyword + chapter_window 聚合)
- SettingExtractor: setting 维 (regex 名词后缀)
- PlotPointExtractor: plot_point 维 (大纲 ## 标题抽取)
"""
from infra.cross_volume.extractors.character_extractor import CharacterExtractor
from infra.cross_volume.extractors.foreshadow_extractor import ForeshadowExtractor
from infra.cross_volume.extractors.setting_extractor import SettingExtractor
from infra.cross_volume.extractors.plot_point_extractor import PlotPointExtractor

__all__ = [
    "CharacterExtractor",
    "ForeshadowExtractor",
    "SettingExtractor",
    "PlotPointExtractor",
]
```

- [ ] **Step 2.2**: Create `infra/cross_volume/extractors/character_extractor.py`

```python
# infra/cross_volume/extractors/character_extractor.py
"""Phase 9.11: CharacterExtractor — regex 中文名 + alias_map + blacklist + threshold."""
import logging
import re
from datetime import datetime, timezone
from pathlib import Path

from infra.cross_volume.reference_graph import ReferenceNode

logger = logging.getLogger(__name__)


class CharacterExtractor:
    def __init__(self, rules: dict, volume: int) -> None:
        self._rules = rules
        self._volume = volume
        self._name_pattern = re.compile(rules["name_pattern"])
        self._alias_map = rules.get("alias_map", {})
        self._blacklist = set(rules.get("blacklist", []))
        self._threshold = rules.get("occurrence_threshold", 1)

    def extract(self, chapter_path: Path) -> list[ReferenceNode]:
        text = chapter_path.read_text(encoding="utf-8")
        chapter_num = self._parse_chapter_num(chapter_path)
        # Step 1: find all candidate names via regex
        raw_names = self._name_pattern.findall(text)
        # Step 2: alias_map substitution
        canonical = [self._alias_map.get(n, n) for n in raw_names]
        # Step 3: blacklist filter
        canonical = [n for n in canonical if n not in self._blacklist]
        # Step 4: dedup within chapter
        canonical = list(dict.fromkeys(canonical))
        # Step 5: build ReferenceNode per unique canonical name
        nodes = []
        for name in canonical:
            node_id = f"character:{name}"
            nodes.append(
                ReferenceNode(
                    id=node_id,
                    dimension="character",
                    volume=self._volume,
                    chapter=chapter_num,
                    title=name,
                    description=f"Character '{name}' from {chapter_path.name}",
                    payload={
                        "name": name,
                        "role": "unknown",
                        "first_appearance_volume": self._volume,
                        "first_appearance_chapter": chapter_num,
                    },
                    created_by="backfill:character_extractor",
                )
            )
        return nodes

    @staticmethod
    def _parse_chapter_num(path: Path) -> int:
        # ch001.md / ch001_大纲.md → 1
        stem = path.stem.replace("_大纲", "")
        digits = "".join(c for c in stem if c.isdigit())
        return int(digits) if digits else 0
```

- [ ] **Step 2.3**: Create `infra/cross_volume/extractors/foreshadow_extractor.py`

```python
# infra/cross_volume/extractors/foreshadow_extractor.py
"""Phase 9.11: ForeshadowExtractor — keyword + chapter_window 聚合."""
import logging
import re
from pathlib import Path

from infra.cross_volume.reference_graph import ReferenceNode

logger = logging.getLogger(__name__)


class ForeshadowExtractor:
    def __init__(self, rules: dict, volume: int) -> None:
        self._rules = rules
        self._volume = volume
        self._keywords = set(rules.get("keywords", []))
        self._pattern = re.compile(rules["pattern"]) if rules.get("pattern") else None
        self._chapter_window = rules.get("chapter_window", 5)

    def extract(self, chapter_path: Path) -> list[ReferenceNode]:
        if not self._pattern:
            return []
        text = chapter_path.read_text(encoding="utf-8")
        chapter_num = self._parse_chapter_num(chapter_path)
        nodes = []
        for m in self._pattern.finditer(text):
            content = m.group("content").strip()
            # 去重同 chapter 同 content
            node_id = f"foreshadow:{content[:20]}"  # 截断 id
            nodes.append(
                ReferenceNode(
                    id=node_id,
                    dimension="foreshadow",
                    volume=self._volume,
                    chapter=chapter_num,
                    title=content[:30],
                    description=f"Foreshadow: {content[:80]}",
                    payload={
                        "content": content,
                        "planted_volume": self._volume,
                        "planted_chapter": chapter_num,
                    },
                    created_by="backfill:foreshadow_extractor",
                )
            )
        return nodes

    @staticmethod
    def _parse_chapter_num(path: Path) -> int:
        stem = path.stem.replace("_大纲", "")
        digits = "".join(c for c in stem if c.isdigit())
        return int(digits) if digits else 0
```

- [ ] **Step 2.4**: Create `infra/cross_volume/extractors/setting_extractor.py`

```python
# infra/cross_volume/extractors/setting_extractor.py
"""Phase 9.11: SettingExtractor — regex 名词后缀 (学院/山门/大陆/...)."""
import logging
import re
from pathlib import Path

from infra.cross_volume.reference_graph import ReferenceNode

logger = logging.getLogger(__name__)


class SettingExtractor:
    def __init__(self, rules: dict, volume: int) -> None:
        self._rules = rules
        self._volume = volume
        self._name_pattern = re.compile(rules["name_pattern"]) if rules.get("name_pattern") else None
        self._blacklist = set(rules.get("blacklist", []))
        self._threshold = rules.get("occurrence_threshold", 1)

    def extract(self, chapter_path: Path) -> list[ReferenceNode]:
        if not self._name_pattern:
            return []
        text = chapter_path.read_text(encoding="utf-8")
        chapter_num = self._parse_chapter_num(chapter_path)
        raw_names = self._name_pattern.findall(text)
        raw_names = [n for n in raw_names if n not in self._blacklist]
        raw_names = list(dict.fromkeys(raw_names))
        nodes = []
        for name in raw_names:
            node_id = f"setting:{name}"
            nodes.append(
                ReferenceNode(
                    id=node_id,
                    dimension="setting",
                    volume=self._volume,
                    chapter=chapter_num,
                    title=name,
                    description=f"Setting '{name}' from {chapter_path.name}",
                    payload={
                        "name": name,
                        "type": "unknown",
                        "first_intro_volume": self._volume,
                        "first_intro_chapter": chapter_num,
                    },
                    created_by="backfill:setting_extractor",
                )
            )
        return nodes

    @staticmethod
    def _parse_chapter_num(path: Path) -> int:
        stem = path.stem.replace("_大纲", "")
        digits = "".join(c for c in stem if c.isdigit())
        return int(digits) if digits else 0
```

- [ ] **Step 2.5**: Create `infra/cross_volume/extractors/plot_point_extractor.py`

```python
# infra/cross_volume/extractors/plot_point_extractor.py
"""Phase 9.11: PlotPointExtractor — 大纲 ## 标题抽取."""
import logging
import re
from pathlib import Path

from infra.cross_volume.reference_graph import ReferenceNode

logger = logging.getLogger(__name__)


class PlotPointExtractor:
    def __init__(self, rules: dict, volume: int) -> None:
        self._rules = rules
        self._volume = volume
        self._pattern = re.compile(rules["pattern"]) if rules.get("pattern") else None
        self._min_length = rules.get("min_length", 1)
        self._max_length = rules.get("max_length", 200)
        self._blacklist = [re.compile(p) for p in rules.get("blacklist", [])]

    def extract(self, chapter_path: Path) -> list[ReferenceNode]:
        if not self._pattern:
            return []
        text = chapter_path.read_text(encoding="utf-8")
        chapter_num = self._parse_chapter_num(chapter_path)
        nodes = []
        for m in self._pattern.finditer(text, re.MULTILINE):
            title = m.group(1).strip()
            # length filter
            if not (self._min_length <= len(title) <= self._max_length):
                continue
            # blacklist filter
            if any(bp.match(title) for bp in self._blacklist):
                continue
            node_id = f"plot_point:{self._volume}:{chapter_num}:{title[:20]}"
            nodes.append(
                ReferenceNode(
                    id=node_id,
                    dimension="plot_point",
                    volume=self._volume,
                    chapter=chapter_num,
                    title=title,
                    description=f"Plot point: {title}",
                    payload={
                        "title": title,
                        "type": "unknown",
                        "volume": self._volume,
                        "chapter": chapter_num,
                        "impact_level": "unknown",
                    },
                    created_by="backfill:plot_point_extractor",
                )
            )
        return nodes

    @staticmethod
    def _parse_chapter_num(path: Path) -> int:
        stem = path.stem.replace("_大纲", "")
        digits = "".join(c for c in stem if c.isdigit())
        return int(digits) if digits else 0
```

- [ ] **Step 2.6**: Create `infra/cross_volume/extraction_rules.yaml`

```yaml
# Phase 9.11: 4-dim rule-based extraction config
# 主公后续改规则: 0 改 Python, 仅改本 YAML

character:
  name_pattern: "[\\u4e00-\\u9fff]{2,4}(?:[·•][\\u4e00-\\u9fff]{2,4})?"
  alias_map:
    "星尘": "星尘剑客"
    "尘": "星尘剑客"
    "林凡": "林凡"
    "凡": "林凡"
  blacklist:
    - "那个"
    - "什么"
    - "怎么"
    - "这里"
    - "那里"
    - "如此"
    - "这般"
    - "此时"
    - "此刻"
  occurrence_threshold: 2

foreshadow:
  keywords:
    - "伏笔"
    - "暗示"
    - "铺垫"
    - "暗藏"
    - "留下"
    - "埋下"
    - "未曾"
  pattern: "(?P<content>[\\u4e00-\\u9fff]{6,30}?(?:伏笔|暗示|铺垫|暗藏|留下|埋下))"
  chapter_window: 5

setting:
  name_pattern: "[\\u4e00-\\u9fff]{2,6}(?:学院|山门|大陆|帝国|秘境|洞府|遗迹|海域|城|宗|派|观|谷)"
  blacklist:
    - "这个"
    - "那个"
    - "此时"
    - "此刻"
  occurrence_threshold: 1

plot_point:
  pattern: "^##\\s+(.+)$"
  min_length: 8
  max_length: 50
  blacklist:
    - "^## 简介$"
    - "^## 备注$"
    - "^## 元数据$"
```

- [ ] **Step 2.7**: Verify 8 unit tests PASS (GREEN)

```bash
pytest tests/cross_volume/test_backfill.py::TestCharacterExtractor \
       tests/cross_volume/test_backfill.py::TestForeshadowExtractor \
       tests/cross_volume/test_backfill.py::TestSettingExtractor \
       tests/cross_volume/test_backfill.py::TestPlotPointExtractor -v 2>&1 | tail -15
# 期望: 8 passed (RED → GREEN for extractor unit tests)
```

- [ ] **Step 2.8**: Commit (1 feat commit 含 4 extractor + 1 yaml + __init__, GREEN partial)

```bash
git add infra/cross_volume/extractors/ infra/cross_volume/extraction_rules.yaml
git commit -m "feat(extractors): phase 9.11 — 4 Extractor + extraction_rules.yaml

Phase 9.11 Backfiller 4 Extractor 实施 (跟 spec 1:1 模式):
- CharacterExtractor: regex 中文名 (2-4 字 + 可含 ·•) + alias_map 替换 + blacklist
  过滤 + threshold ≥ 1 章, deterministic id 'character:{canonical_name}'
- ForeshadowExtractor: keyword 集合 (伏笔/暗示/铺垫/...) + named group pattern 抽
  6-30 字上下文, id 'foreshadow:{content[:20]}'
- SettingExtractor: regex 名词后缀 (学院/山门/大陆/帝国/秘境/洞府/遗迹/海域/城/
  宗/派/观/谷) + blacklist, id 'setting:{name}'
- PlotPointExtractor: 大纲 ## 标题 (min_length=8, max_length=50) + blacklist
  (简介/备注/元数据), id 'plot_point:{vol}:{chapter}:{title[:20]}'

extraction_rules.yaml: 1 config 4 维全, 主公后续改规则 0 改 Python.

GREEN: 8 unit tests (4 Extractor × 2 tests) pass RED→GREEN.
2 storage atomicity tests 仍 RED (Task 3 修).
3 backfiller + 2 integration + 1 e2e tests 仍 RED (Task 3 + Task 4 + Task 5 修).

baseline: 2278 passed, 0 failed, 27 skipped
target (post-Task 7): 2278 + 16 = 2294 passed

0 production code 改 (MasterController / dashboard / cost_tracker 0 touch)
0 Phase 9.10 4 source 现有 API 改 (extractors 纯新增)
0 LLM 成本 (纯规则)
0 Edge 创建 (留 Phase 9.12)
0 historical spec/plan doc 改 / 0 force-push / 0 amend / 0 --no-verify / 0 Co-Authored-By footer."
```

---

## Task 3: Implement Backfiller + `RippleStorage.append_nodes_atomic` (GREEN 5 tests)

**Files:**
- Create: `infra/cross_volume/backfill.py` (~120L)
- Modify: `infra/cross_volume/storage.py` (+30L, 1 new method `append_nodes_atomic` + 2 imports)

- [ ] **Step 3.1**: Modify `infra/cross_volume/storage.py` — add 1 new method + 2 imports

```python
# Modify infra/cross_volume/storage.py
# Add to imports (跟现有 json / sqlite3 一起):
import json
import logging
import sqlite3
from contextlib import contextmanager
from datetime import datetime
from pathlib import Path
from typing import Iterator

from infra.cross_volume.reference_graph import ReferenceEdge, ReferenceNode
from infra.cross_volume.ripple import CrossVolumeRipple

# ... existing class RippleStorage body ...

    # Add new method after `append_ripple` (跟 spec Section 6.1 1:1):
    def append_nodes_atomic(self, nodes: list[ReferenceNode]) -> None:
        """Phase 9.11: 1 atomic_batch wrap N inserts, 0 partial commit.

        复用 Phase 9.10 atomic_batch context manager, 仅封装 N × INSERT.
        """
        with self.atomic_batch() as conn:
            for node in nodes:
                try:
                    conn.execute(
                        "INSERT INTO reference_nodes VALUES (?,?,?,?,?,?,?,?,?)",
                        (node.id, node.dimension, node.volume, node.chapter,
                         node.title, node.description,
                         json.dumps(node.payload, ensure_ascii=False),
                         node.created_at.isoformat(), node.created_by),
                    )
                except sqlite3.IntegrityError as e:
                    raise ValueError(f"storage integrity: {e}") from e
        # atomic_batch 自动 commit (0 exception) 或 rollback (exception)
```

- [ ] **Step 3.2**: Verify 2 storage atomicity tests PASS (GREEN)

```bash
pytest tests/cross_volume/test_storage.py::TestAppendNodesAtomic -v 2>&1 | tail -5
# 期望: 2 passed (RED → GREEN for storage atomicity tests)
```

- [ ] **Step 3.3**: Create `infra/cross_volume/backfill.py`

```python
# infra/cross_volume/backfill.py
"""Phase 9.11: Backfiller — 编排 4 Extractor + 跨章聚合 + 1 atomic_batch 写库.

复用 Phase 9.10 CrossVolumeReferenceGraph + RippleStorage.append_nodes_atomic (新增 additive).
"""
import dataclasses
import logging
import time
from dataclasses import dataclass
from pathlib import Path
from typing import Iterator

import yaml

from infra.cross_volume.extractors import (
    CharacterExtractor,
    ForeshadowExtractor,
    PlotPointExtractor,
    SettingExtractor,
)
from infra.cross_volume.reference_graph import (
    CrossVolumeReferenceGraph,
    DimensionT,
    ReferenceNode,
)

logger = logging.getLogger(__name__)


@dataclass(frozen=True)
class BackfillStats:
    character_count: int
    foreshadow_count: int
    setting_count: int
    plot_point_count: int
    total_count: int
    elapsed_s: float
    dry_run: bool

    def summary(self) -> str:
        return (
            f"[{'DRY-RUN' if self.dry_run else 'EXECUTED'}] "
            f"character={self.character_count} "
            f"foreshadow={self.foreshadow_count} "
            f"setting={self.setting_count} "
            f"plot_point={self.plot_point_count} "
            f"total={self.total_count} "
            f"elapsed={self.elapsed_s:.2f}s"
        )

    @classmethod
    def from_nodes(
        cls,
        all_nodes: dict[DimensionT, dict[str, ReferenceNode]],
        dry_run: bool,
        elapsed_s: float = 0.0,
    ) -> "BackfillStats":
        return cls(
            character_count=len(all_nodes["character"]),
            foreshadow_count=len(all_nodes["foreshadow"]),
            setting_count=len(all_nodes["setting"]),
            plot_point_count=len(all_nodes["plot_point"]),
            total_count=sum(len(b) for b in all_nodes.values()),
            elapsed_s=elapsed_s,
            dry_run=dry_run,
        )


class Backfiller:
    def __init__(
        self,
        rules_path: Path = Path("infra/cross_volume/extraction_rules.yaml"),
        corpus_root: Path = Path("03_内容仓库/04_正文"),
        graph: CrossVolumeReferenceGraph | None = None,
    ) -> None:
        self._rules = yaml.safe_load(rules_path.read_text(encoding="utf-8"))
        self._corpus_root = corpus_root
        self._graph = graph or CrossVolumeReferenceGraph(
            # 0 graph 注入时, 走默认 storage (Phase 9.10 pattern)
            storage=_default_storage()
        )
        # 4 Extractor 实例化 (Phase 9.11 lazy: 0 立即扫, 仅 init)
        self._extractors: dict[DimensionT, object] = {
            "character": CharacterExtractor(self._rules["character"], volume=1),
            "foreshadow": ForeshadowExtractor(self._rules["foreshadow"], volume=1),
            "setting": SettingExtractor(self._rules["setting"], volume=1),
            "plot_point": PlotPointExtractor(self._rules["plot_point"], volume=1),
        }

    def run(
        self,
        dry_run: bool = True,
        volume_filter: int | None = None,
    ) -> BackfillStats:
        start = time.perf_counter()
        all_nodes: dict[DimensionT, dict[str, ReferenceNode]] = {
            "character": {},
            "foreshadow": {},
            "setting": {},
            "plot_point": {},
        }
        for chapter_path in self._iter_chapters(volume_filter):
            chapter_num = self._parse_chapter_num(chapter_path)
            for dim, extractor in self._extractors.items():
                for node in extractor.extract(chapter_path):
                    self._merge_node(all_nodes[dim], node, chapter_num)
        elapsed = time.perf_counter() - start

        # --dry-run: 0 写库, 仅返 stats
        if dry_run:
            return BackfillStats.from_nodes(all_nodes, dry_run=True, elapsed_s=elapsed)

        # --execute: 1 atomic_batch wrap 全部 N inserts (additive append_nodes_atomic)
        all_node_list = [
            n for dim_nodes in all_nodes.values() for n in dim_nodes.values()
        ]
        self._graph._storage.append_nodes_atomic(all_node_list)
        logger.info("backfill done: %s", BackfillStats.from_nodes(all_nodes, dry_run=False, elapsed_s=elapsed).summary())
        return BackfillStats.from_nodes(all_nodes, dry_run=False, elapsed_s=elapsed)

    def _iter_chapters(self, volume_filter: int | None) -> Iterator[Path]:
        # 扫 ch{001..359}.md + ch{001..359}_大纲.md
        if not self._corpus_root.exists():
            return iter([])
        # vol 1-3 对应 ch001-ch120 / ch121-ch240 / ch241-ch359 (跟 Phase 9 spec 1:1)
        vol_ranges = {
            1: (1, 120),
            2: (121, 240),
            3: (241, 359),
        }
        if volume_filter is not None and volume_filter in vol_ranges:
            low, high = vol_ranges[volume_filter]
            chapter_nums = [n for n in range(low, high + 1)]
        else:
            chapter_nums = list(range(1, 360))

        for n in chapter_nums:
            stem = f"ch{n:03d}"
            for suffix in (".md", "_大纲.md"):
                p = self._corpus_root / f"{stem}{suffix}"
                if p.exists():
                    yield p

    def _merge_node(
        self,
        bucket: dict[str, ReferenceNode],
        new_node: ReferenceNode,
        chapter_num: int,
    ) -> None:
        # 跨章聚合: 同 id → 合并 payload.chapter_appearances
        if new_node.id in bucket:
            existing = bucket[new_node.id]
            merged_chapters = sorted(
                set(existing.payload.get("chapter_appearances", []) + [chapter_num])
            )
            bucket[new_node.id] = dataclasses.replace(
                existing,
                payload={**existing.payload, "chapter_appearances": merged_chapters},
            )
        else:
            bucket[new_node.id] = dataclasses.replace(
                new_node,
                payload={**new_node.payload, "chapter_appearances": [chapter_num]},
            )

    @staticmethod
    def _parse_chapter_num(path: Path) -> int:
        stem = path.stem.replace("_大纲", "")
        digits = "".join(c for c in stem if c.isdigit())
        return int(digits) if digits else 0


def _default_storage():
    """Phase 9.11: 默认 storage 走 infra/.state/ripple.db (跟 Phase 9.10 pattern)."""
    from infra.cross_volume.storage import RippleStorage
    return RippleStorage(db_path=Path("infra/.state/ripple.db"))
```

- [ ] **Step 3.4**: Verify 3 backfiller + 2 integration tests PASS (GREEN, skip 1 e2e 仍 RED)

```bash
pytest tests/cross_volume/test_backfill.py::TestBackfiller \
       tests/cross_volume/test_backfill.py::TestBackfillIntegration -v 2>&1 | tail -10
# 期望: 5 passed (3 backfiller + 2 integration, RED → GREEN)
# 注: 1 e2e test 仍 RED (SAMPLE_CORPUS_ROOT undefined, Task 5 修)
```

- [ ] **Step 3.5**: Commit (1 feat commit 含 backfill.py + storage.append_nodes_atomic)

```bash
git add infra/cross_volume/backfill.py infra/cross_volume/storage.py
git commit -m "feat(backfill): phase 9.11 — Backfiller + append_nodes_atomic additive

Phase 9.11 Backfiller + 1 additive RippleStorage method 实施 (跟 spec 1:1):

- infra/cross_volume/backfill.py (~120L):
  - Backfiller 编排: load YAML → 4 Extractor 扫 corpus → 跨章聚合 (id-based
    bucket, payload chapter_appearances 累积) → 1 commit 全部 N nodes
  - --dry-run (默认): 0 写库, 仅返 BackfillStats
  - --execute: 调 storage.append_nodes_atomic 1 次
  - _iter_chapters 走 vol 1-3 ranges (ch001-ch120 / ch121-ch240 / ch241-ch359)
  - _merge_node 跨章聚合 (id collision → dataclasses.replace 累积 chapters)

- infra/cross_volume/storage.py (+30L, additive):
  - 1 new method `append_nodes_atomic(nodes: list[ReferenceNode])` 复用
    Phase 9.10 atomic_batch context manager, 0 改现有 7 methods + 1 ctx mgr
  - IntegrityError → ValueError wrap (跟 Phase 9.10 append_node 1:1 模式)
  - 0 partial commit 保证 (atomic_batch 自动 commit/rollback)

GREEN: 5 tests (3 backfiller orchestration + 2 integration) + 2 storage atomicity
tests = 7 total pass RED→GREEN.
1 e2e test 仍 RED (SAMPLE_CORPUS_ROOT / SAMPLE_RULES_YAML undefined, Task 5 修).

baseline: 2278 passed, 0 failed, 27 skipped
target (post-Task 7): 2278 + 16 = 2294 passed

0 production code 改 (MasterController / dashboard / cost_tracker 0 touch)
0 Phase 9.10 现有 API 行为改 (append_nodes_atomic 纯 additive, 复用 atomic_batch)
0 LLM 成本 (纯规则)
0 Edge 创建 (留 Phase 9.12)
0 historical spec/plan doc 改 / 0 force-push / 0 amend / 0 --no-verify / 0 Co-Authored-By footer."
```

---

## Task 4: Implement `lingwen.py` CLI sub-command (GREEN — 0 new test in this task, Task 5 adds fixture + 1 e2e)

**Files:**
- Modify: `lingwen.py` (+30L, 1 new sub-command `ripple backfill`)

- [ ] **Step 4.1**: Read existing `lingwen.py` to find `ripple` group (Phase 9.10 留的)

```bash
grep -n "ripple\|@cli" lingwen.py | head -20
# 期望: 找到 Phase 9.10 @cli.group() def ripple(): block
```

- [ ] **Step 4.2**: Add `backfill` sub-command under `ripple` group

```python
# Modify lingwen.py: 在 Phase 9.10 留的 @ripple.command() block 后面加:

@ripple.command()
@click.option(
    "--dry-run/--execute",
    default=True,  # 主公决策 Q4: 默认 dry-run
    help="Default --dry-run (0 写库, 仅 print 统计); --execute 走真写",
)
@click.option(
    "--vol", type=int, default=None,
    help="Volume filter: 1/2/3 抽样式, 缺省全 359 章",
)
@click.option(
    "--rules", type=Path,
    default=Path("infra/cross_volume/extraction_rules.yaml"),
    help="Extraction rules YAML path",
)
def backfill(dry_run: bool, vol: int | None, rules: Path) -> None:
    """Backfill cross-volume ripple 4 维 ReferenceNode from 359 chapter corpus."""
    from infra.cross_volume.backfill import Backfiller
    backfiller = Backfiller(rules_path=rules)
    stats = backfiller.run(dry_run=dry_run, volume_filter=vol)
    click.echo(stats.summary())
    if dry_run:
        click.echo("(hint: 加 --execute 走真写)")
```

- [ ] **Step 4.3**: Verify CLI imports OK + 0 broken

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
python lingwen.py ripple backfill --help 2>&1 | tail -15
# 期望: Usage 显示 --dry-run/--execute, --vol, --rules 选项
```

- [ ] **Step 4.4**: Verify 0 baseline tests broken (CLI modify 不影响 pytest)

```bash
pytest -q 2>&1 | tail -3
# 期望: 2278 passed, 0 failed, 27 skipped (跟 Task 1 baseline 一致, 0 改)
```

- [ ] **Step 4.5**: Commit (跟 Phase 9.10 plan 1:1 模式, CLI 改 走 1 feat commit)

```bash
git add lingwen.py
git commit -m "feat(cli): phase 9.11 — lingwen.py ripple backfill sub-command

Phase 9.11 CLI 集成 (跟 spec Section 4 1:1):
- 1 new sub-command `lingwen.py ripple backfill` 复用 Phase 9.10 留的
  `ripple` group (0 重复 group)
- --dry-run/--execute: 默认 --dry-run (主公决策 Q4)
- --vol N: 1/2/3 抽样式, 缺省全 359 章
- --rules PATH: extraction_rules.yaml 路径 (default 走 repo 内置)
- 调 Backfiller.run(), print BackfillStats.summary()
- --dry-run 提示: '(hint: 加 --execute 走真写)'

baseline: 2278 passed, 0 failed, 27 skipped (跟 Task 1 baseline 一致, 0 改)
1 e2e test 仍 RED (SAMPLE_CORPUS_ROOT fixture, Task 5 修)

0 production code 改 (CLI 0 触 dashboard / cost_tracker / workflow)
0 Phase 9.10 4 source 改 (Backfiller 通过 add_node 间接复用)
0 LLM 成本 / 0 Edge 创建 (留 Phase 9.12)
0 historical spec/plan doc 改 / 0 force-push / 0 amend / 0 --no-verify / 0 Co-Authored-By footer."
```

---

## Task 5: Implement 10-chapter fixture + finalize E2E test (GREEN 1 e2e test)

**Files:**
- Create: `tests/cross_volume/fixtures/__init__.py` (~5L, exports SAMPLE_CORPUS_ROOT + SAMPLE_RULES_YAML)
- Create: `tests/cross_volume/fixtures/ch{001..010}.md` (10 章, ~3KB each, ~30KB total)
- Create: `tests/cross_volume/fixtures/ch{001..010}_大纲.md` (10 大纲, ~2KB each, ~20KB total)
- Create: `tests/cross_volume/fixtures/sample_corpus.py` (~40L, exports SAMPLE_CORPUS_ROOT + SAMPLE_RULES_YAML)

- [ ] **Step 5.1**: Create `tests/cross_volume/fixtures/__init__.py`

```python
# tests/cross_volume/fixtures/__init__.py
"""Phase 9.11: 10-chapter fixture for backfill E2E test."""
```

- [ ] **Step 5.2**: Create `tests/cross_volume/fixtures/sample_corpus.py` (跟 test_backfill.py E2E 引用的 SAMPLE_CORPUS_ROOT / SAMPLE_RULES_YAML 1:1)

```python
# tests/cross_volume/fixtures/sample_corpus.py
"""Phase 9.11: 10-chapter fixture exports for E2E test.

供 tests/cross_volume/test_backfill.py::TestBackfillE2E 引用:
- SAMPLE_CORPUS_ROOT: 10 章 .md (5 正文 + 5 大纲) sample corpus
- SAMPLE_RULES_YAML: 跟 corpus 匹配的最小 rules (含 character / setting / plot_point 各 ≥ 1)
"""
from pathlib import Path

SAMPLE_CORPUS_ROOT = Path(__file__).parent  # fixtures/ 目录含 10 章 .md

SAMPLE_RULES_YAML = """
character:
  name_pattern: "[李青云林凡]{2,3}"
  alias_map: {"凡": "林凡"}
  blacklist: []
  occurrence_threshold: 1
foreshadow:
  keywords: ["伏笔"]
  pattern: "(?P<content>[\\u4e00-\\u9fff]{6,30}?(?:伏笔))"
  chapter_window: 5
setting:
  name_pattern: "[\\u4e00-\\u9fff]{2,6}(?:学院|山门|大陆|帝国|秘境|宗)"
  blacklist: []
  occurrence_threshold: 1
plot_point:
  pattern: "^##\\s+(.+)$"
  min_length: 8
  max_length: 50
  blacklist: []
"""
```

- [ ] **Step 5.3**: Create 10 章 .md fixtures (5 章正文 + 5 章大纲, 满足 character/setting/plot_point 各 ≥ 1 触发)

```bash
# tests/cross_volume/fixtures/ch001.md (李青云 + 凌霄宗)
echo "李青云踏入凌霄宗山门, 拜见掌门." > tests/cross_volume/fixtures/ch001.md

# tests/cross_volume/fixtures/ch001_大纲.md (## 标题)
cat > tests/cross_volume/fixtures/ch001_大纲.md << 'EOF'
# 章节一

## 青云觉醒获得古剑碎片
## 林凡登场
EOF

# tests/cross_volume/fixtures/ch002.md (李青云 + 林凡 跨章聚合)
echo "李青云与林凡激战于凌霄宗, 古剑碎片是本章伏笔." > tests/cross_volume/fixtures/ch002.md

# tests/cross_volume/fixtures/ch002_大纲.md
cat > tests/cross_volume/fixtures/ch002_大纲.md << 'EOF'
# 章节二

## 凌霄宗大战林凡
EOF

# tests/cross_volume/fixtures/ch003.md (青云大陆 + 秘境)
echo "李青云游历青云大陆, 探访古幽秘境, 林凡同行." > tests/cross_volume/fixtures/ch003.md

# tests/cross_volume/fixtures/ch003_大纲.md
cat > tests/cross_volume/fixtures/ch003_大纲.md << 'EOF'
# 章节三

## 古幽秘境探险
EOF

# tests/cross_volume/fixtures/ch004.md
echo "林凡与李青云在青云大陆再战, 古剑碎片残留." > tests/cross_volume/fixtures/ch004.md

# tests/cross_volume/fixtures/ch004_大纲.md
cat > tests/cross_volume/fixtures/ch004_大纲.md << 'EOF'
# 章节四

## 古剑碎片显现
EOF

# tests/cross_volume/fixtures/ch005.md
echo "青云大陆最终决战, 凌霄宗获胜, 李青云成掌门." > tests/cross_volume/fixtures/ch005.md

# tests/cross_volume/fixtures/ch005_大纲.md
cat > tests/cross_volume/fixtures/ch005_大纲.md << 'EOF'
# 章节五

## 凌霄宗最终胜利
EOF
```

- [ ] **Step 5.4**: Verify E2E test PASS (GREEN, 16/16 total)

```bash
pytest tests/cross_volume/test_backfill.py -v 2>&1 | tail -20
# 期望: 15 passed (8 unit + 3 orchestration + 2 integration + 1 e2e + 1 storage atomicity)
```

- [ ] **Step 5.5**: Verify 2278 + 16 = 2294 baseline

```bash
pytest -q 2>&1 | tail -3
# 期望: 2294 passed, 0 failed, 27 skipped (2278 + 16 new)
```

- [ ] **Step 5.6**: Commit (1 chore commit 含 fixture + sample_corpus.py)

```bash
git add tests/cross_volume/fixtures/
git commit -m "chore(fixture): phase 9.11 — 10-chapter fixture for E2E test

Phase 9.11 E2E test fixture (跟 spec Section 5 1:1):
- tests/cross_volume/fixtures/__init__.py (5L, fixtures 入口)
- tests/cross_volume/fixtures/sample_corpus.py (~40L, exports SAMPLE_CORPUS_ROOT
  + SAMPLE_RULES_YAML 跟 test_backfill.py::TestBackfillE2E 引用 1:1)
- 10 章 .md fixture (5 章正文 + 5 章大纲, ~30KB total):
  - ch001-005.md 含 character (李青云, 林凡) + setting (凌霄宗, 青云大陆, 古幽秘境)
  - ch001-005_大纲.md 含 plot_point (## 标题, 触发 plot_point_extractor)
  - ch002 含 '伏笔' 触发 foreshadow_extractor (额外覆盖)
- 0 真实小说内容 (synthesized corpus, 0 IP 泄漏)

GREEN: 1 e2e test (TestBackfillE2E.test_backfill_runs_on_10_chapter_fixture)
pass RED→GREEN. 16/16 backfill+storage tests 全过.

baseline: 2278 passed, 0 failed, 27 skipped
target (post-Task 7): 2278 + 16 = 2294 passed

0 production code 改 (fixture 0 触 dashboard / cost_tracker)
0 Phase 9.10 4 source 改
0 LLM 成本 (fixture 是纯静态 .md, 0 token)
0 historical spec/plan doc 改 / 0 force-push / 0 amend / 0 --no-verify / 0 Co-Authored-By footer."
```

---

## Task 6: Full Verification (pytest + ruff + gitignore + 0 production 改 + dry-run 端到端)

**Step 6.1**: Full pytest run

```bash
pytest -q 2>&1 | tail -3
# 期望: 2294 passed, 0 failed, 27 skipped (2278 + 16 new)
```

**Step 6.2**: ruff lint

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
ruff check infra/cross_volume/ tests/cross_volume/ 2>&1 | tail -3
# 期望: All checks passed!
```

**Step 6.3**: Verify `.gitignore` 含 `ripple.db` (Phase 9.10 已加, Phase 9.11 0 改)

```bash
git check-ignore infra/.state/ripple.db
# 期望: infra/.state/ripple.db (gitignored)
```

**Step 6.4**: 0 production code 改 验证

```bash
git diff --stat 8b98d98..HEAD -- infra/ai_service/ infra/agent_system/ dashboard/
# 期望: empty (0 改)
```

**Step 6.5**: End-to-end dry-run 验证 (主公手动跑 1 次, 验证 CLI + 4 Extractor 跑通)

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
python lingwen.py ripple backfill --vol 1
# 期望输出:
# [DRY-RUN] character=N foreshadow=N setting=N plot_point=N total=N elapsed=Xs
# (hint: 加 --execute 走真写)
# 期望: N ≥ 10 (vol 1 有 120 章, 估 ≥ 10 nodes)
```

**Step 6.6**: Verify 0 Co-Authored-By trailers / 0 amend / 0 fixup-squash (5 commits verify)

```bash
echo "=== 5 commit SHA verify ===" && git log --oneline -5 && echo "=== 0 Co-Authored-By ===" && for sha in $(git log --format='%H' -5); do git show --format='%(trailers)' -s $sha | grep -E "Co-Authored-By" && echo "  ↑ FAIL" || echo "  $sha: clean"; done && echo "=== 0 fixup/squash ===" && git log --oneline -5 | grep -E "fixup|squash" || echo "  (0 fixup/squash)"
```

**Step 6.7**: 7 verify commit chain

```bash
echo "=== commit sequence ===" && git log --oneline 8b98d98..HEAD
# 期望: 5 commits (test RED + 4 feat commits + chore fixture = 5 commits, 跟 Phase 9.10 7 commits 不同但更紧凑)
```

---

## Task 7: Memory Sync + Final Push + DoD

**Step 7.1**: Update MEMORY.md

```bash
# ~/.claude/projects/.../memory/MEMORY.md (3 edits)

# Edit L12: 加 Phase 9.11 到 dashboard phase 链 (跟 Phase 9.10 1:1 模式)
# Old: - [Phase 8.16-8.45 + 9.10 dashboard polish + build tooling + test infra + UI completeness + cross-volume ripple data layer](phases-8-dashboard.md) — ... / Phase 9.10 cross-volume ripple data model + persistence (3 frozen dataclass + 1 CVG + 1 storage, 16 tests)
# New: - [Phase 8.16-8.45 + 9.10-9.11 dashboard polish + build tooling + test infra + UI completeness + cross-volume ripple data layer + backfill](phases-8-dashboard.md) — ... / Phase 9.10 data model + persistence (16 tests) / Phase 9.11 backfill 4 维 rule-based (1 YAML + 4 Extractor + 1 Backfiller + 1 storage additive + 1 CLI, 16 tests)

# Edit L26: bump v9.17 → v9.18
# Old: - **当前版本**: v9.17 (Phase 9.10 cross-volume ripple data model + persistence 已合并, 2026-06-09)
# New: - **当前版本**: v9.18 (Phase 9.11 CVG backfill 4 维 rule-based 已合并, 2026-06-09)

# Edit L29: update test count 2278 → 2294
# Old: - **测试覆盖**: 2278 passed, 0 failed, 27 skipped (2262 + 16 Phase 9.10, 详细 phase 增量见 [[phases]])
# New: - **测试覆盖**: 2294 passed, 0 failed, 27 skipped (2262 + 16 Phase 9.10 + 16 Phase 9.11, 详细 phase 增量见 [[phases]])
```

**Step 7.2**: Append Phase 9.11 entry to phases-8-dashboard.md

```bash
# ~/.claude/projects/.../memory/phases-8-dashboard.md
# Append after Phase 9.10 entry (跟 Phase 9.10 1:1 模式, ~1500 字符)
# Include: 5 commit SHAs (c421da1 spec + 4 impl + 1 chore + 1 feat cli = 6 commits)
# Wait — let me recount: spec c421da1 (Task 1 plan end) + test RED (Task 1) + extractor (Task 2) + backfill (Task 3) + cli (Task 4) + fixture (Task 5) = 6 commits (1 spec + 5 impl)
```

**Step 7.3**: Verify MEMORY.md < 200 lines

```bash
wc -l ~/.claude/projects/.../memory/MEMORY.md
# 期望: < 200 (跟 0 例外 hard rule, baseline 31L + 3 edits ≈ 33L)
```

**Step 7.4**: Verify memory files NOT in git commit (outside git repo)

```bash
git status --short
# 期望: clean (memory files 在 ~/.claude/projects/... 外, 0 git status)
```

**Step 7.5**: Push 6 commits to origin/master

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
git push origin master 2>&1 | tail -5
# 期望: 8b98d98..<new HEAD>  master -> master (6 commits ahead)
git rev-parse HEAD origin/master
# 期望: 2 行同 SHA (本地 == origin)
git rev-list --left-right --count origin/master...HEAD
# 期望: 0	0 (0 ahead, 0 behind)
```

**Step 7.6**: Final DoD check (9 gates 跟 Phase 9.10 1:1 模式)

```bash
# Gate 1: pytest 2294 passed, 0 failed, 27 skipped
pytest -q 2>&1 | tail -2

# Gate 2: ruff 0 issues
ruff check infra/cross_volume/ tests/cross_volume/ 2>&1 | tail -2

# Gate 3: 0 production code 改
git diff --stat 8b98d98..HEAD -- infra/ai_service/ infra/agent_system/ dashboard/

# Gate 4: 0 改 Phase 9.10 4 source 现有 API 行为 (additive 仅)
git diff --stat 8b98d98..HEAD -- infra/cross_volume/storage.py
# 期望: 仅 +30L (append_nodes_atomic 增量), 0 改 atomic_batch / append_node / load_all_nodes / _row_to_node

# Gate 5: 0 改 CLAUDE.md / pyproject.toml / pytest.ini
git diff --stat 8b98d98..HEAD -- CLAUDE.md pyproject.toml pytest.ini 2>&1

# Gate 6: 0 改 historical spec/plan doc
git diff --stat 8b98d98..HEAD -- docs/superpowers/

# Gate 7: 0 force-push / 0 amend / 0 --no-verify / 0 Co-Authored-By
for sha in $(git log --format='%H' 8b98d98..HEAD); do
  git show --format='%(trailers)' -s $sha | grep -E "Co-Authored-By" && echo "  ↑ FAIL" || echo "  $sha: clean"
done

# Gate 8: ripple.db gitignored
git check-ignore infra/.state/ripple.db
# 期望: infra/.state/ripple.db

# Gate 9: 6 commits 落 origin
git log --oneline origin/master -6
```

**Step 7.7**: Final 总结 (主公 review)

```markdown
## Phase 9.11 (2026-06-09)

CVG backfill (Phase 9 super-phase 2/5) — Phase 9 (跨卷涟漪) 5 super-phases 路线图 Phase 11 实施. 4 维 (character / foreshadow / setting / plot_point) 规则-based backfill 359 章 → ReferenceNode 写 ripple.db. **主公决策 (2026-06-09 AskUserQuestion, 7 Q's)**: (Q1) 规则-based (0 LLM); (Q2) 359 章正文 + 359 章大纲; (Q3) 0 Edge (留 Phase 9.12 LLM); (Q4) CLI --dry-run 默认; (Q5) 统一聚合 (跨章合 1 node); (Q6) 1 YAML 4 维全 + 4 Extractor class; (Q7) 1 atomic_batch 1 commit (实施发现 add_node 走 autocommit 跟 atomic_batch 0 串, 改 additive `append_nodes_atomic` 解决).

**架构选择**: 4 Extractor class 走 YAML 注入规则 → Backfiller 编排 (跨章聚合) → 1 additive RippleStorage method `append_nodes_atomic` (复用 atomic_batch) → CLI 默认 dry-run. 跟 Phase 9.10 纯数据层 1:1 模式, 0 LLM 成本.

**commit sequence** (6 commits 推 origin/master, 0 --no-verify, 0 amend, 0 force-push):
- c421da1 docs(spec) CVG backfill 4-dim rule-based extractor design (~624L)
- <test-RED> test(backfill) 16 failing tests (2 storage atomicity + 8 extractor unit + 3 backfiller orchestration + 2 integration + 1 e2e)
- <extractor-feat> feat(extractors) 4 Extractor + extraction_rules.yaml (1 config)
- <backfill-feat> feat(backfill) Backfiller + append_nodes_atomic additive
- <cli-feat> feat(cli) lingwen.py ripple backfill sub-command
- <fixture-chore> chore(fixture) 10-chapter fixture for E2E test

(主公 review 时补 5 impl commit SHAs)

**测试总** (post-push 验证):
- pytest 2294 passed 0 failed 27 skipped (2278 + 16 net new, baseline +0% failed 维持 0)
- ruff check 0 issues
- `infra/.state/ripple.db` 自动创建 0 git staged (gitignored)
- `git log --oneline origin/master -7` = 6 Phase 9.11 commits 落 origin
- 0 Co-Authored-By trailers (6 commits verify)
- 0 force-push / 0 amend / 0 --no-verify

**0 改**:
- backend production code (MasterController / got_bridge / dashboard / cost_tracker / cost_persistence / cost_router / workflow.db 全无 touch, 跟 Phase 9.10 baseline 一致)
- frontend (vitest / Vue / playwright / eslint 全无 touch, 跟 Phase 8.43+8.44+8.45+9.10 baseline 一致)
- CLAUDE.md / pyproject.toml / pytest.ini
- 0 改 historical spec/plan doc `:NNN` 行号 (Phase 9 spec + blueprint + Phase 9.10 spec/plan 全不动)
- Phase 9.10 4 source 现有 API 行为 (仅 additive `append_nodes_atomic`, 0 改 atomic_batch / append_node / load_all_nodes / _row_to_node)

**deviations**: (D1) Q7 plan 假设 Backfiller 走 `with atomic_batch() as conn: graph.add_node(...)` 1 commit, 实施发现 Phase 9.10 `add_node` 内部 `storage.append_node` 走 autocommit per call, 0 走 caller 的 `atomic_batch` context manager. 修: 1 additive `storage.append_nodes_atomic` 走 atomic_batch 1 commit 全部 N nodes, Backfiller 调 1 method 1 commit. 0 改 Phase 9.10 现有 API 行为. 1 new test (test_append_nodes_atomic_atomicity) 验证 0 partial commit.

**out of scope (有 followup)**:
- Phase 9.12 LLM scanner 4 prompts (character / foreshadow / setting / plot_point LLM 推断, 召回率提升 + Edge 推断 8 种 relationship_type)
- Phase 9.13 FastAPI `POST /api/ripple/backfill` + `GET /api/ripple/:id` + Vue 3 ReviewRipple.vue + WebSocket `/ws/ripple` cascade event
- Phase 9.14 audit_log + fsync tuning + real rollback logic
- Phase 9.15 real cascade BFS (替换 trigger_cascade stub) + cache layer (LRU 1000) + dual storage + perf benchmark
- Backfill 增量 (跟 workflow.db 集成, 章节完成时增量扫新章节)
- extraction_rules.yaml alias_map 人工 curation 跟主公 spec 0a8a900 角色表 cross-check (留 Phase 9.12 LLM 校准)
- corpus encoding 兼容性 (0 GBK 兼容, 留 Phase 9.12+ 跨编码)
- Per-chapter progress bar (YAGNI, 359 章估 < 30s, 0 progress UI)
- Backfill diff report (YAGNI, dry-run 看完 stats 决策 execute, 0 diff 报告)
- 5 input validation branches 加 unit test (跟 Phase 9.10 留 followup 1:1 模式)
- MEMORY.md 拆 42+ entries 到 phases.md topic (本 entry 后变 42+, 必拆 followup; phases-8-dashboard.md 现在约 100L, 4 era 文件 + index + MEMORY.md 6 文件总行数 soft cap 200 接近关注)
- MEMORY.md line 8 typo "8.16-8.24" → "8.16-8.45+9.10+9.11" (留 followup 跟 MEMORY.md 拆一起做)

后续: Phase 9.12 LLM scanner 4 prompts / Phase 9.13 FastAPI + Vue UI + WebSocket / Phase 9.14 audit + rollback / Phase 9.15 cascade BFS + cache / MEMORY.md 拆 42+ entries.
```

---

## Out of Scope (defer to Phase 9.12+)

- **Phase 9.12.1-9.12.4**: LLM scanner 4 prompts (character / foreshadow / setting / plot_point 各自 prompt template + LLM 推断 4 维)
- **Phase 9.12.5**: Edge 创建 (8 种 relationship_type LLM 推断, 跨章上下文语义)
- **Phase 9.12.6**: rule-based backfill → LLM-based 升级 (Hybrid 模式 9.11 规则 + 9.12 LLM 精筛)
- **Phase 9.13.1-9.13.2**: FastAPI `POST /api/ripple/backfill` + `GET /api/ripple/:id`
- **Phase 9.13.3**: Vue 3 `ReviewRipple.vue` (impact 列表 + diff preview)
- **Phase 9.13.4**: WebSocket `/ws/ripple` cascade event push
- **Phase 9.14.1**: `audit_log` table + trigger
- **Phase 9.14.2**: fsync tuning (`synchronous=EXTRA`)
- **Phase 9.14.3**: real rollback logic (savepoint per ripple)
- **Phase 9.14.4**: FK `ON DELETE CASCADE` 节点删除
- **Phase 9.15.1**: real BFS cascade impl (替换 `trigger_cascade` stub)
- **Phase 9.15.2**: cache layer (LRU 1000 nodes)
- **Phase 9.15.3**: dual storage (SQLite + JSON snapshot)
- **Phase 9.15.4**: perf benchmark + optimize
- **0 触 in Phase 9.11**: 12 SCENARIOS 路由, MasterController, decision 流程, dashboard UI, workflow.db, cost_tracker.db, Phase 9.10 现有 API 行为

---

## Risks + Mitigations

| 风险 | 缓解 |
|------|------|
| 2278 baseline tests 破 | 0 改 Phase 9.10 现有 API 行为, 仅 additive `append_nodes_atomic`; Backfiller 通过 `add_node` 间接复用 |
| Append-only invariant 被 backfill 误 mutate | 3 dataclass `frozen=True` + Backfiller 0 mutation API (仅 add_node) |
| FK violation 在 backfill 写库 | append_nodes_atomic 走 atomic_batch, 异常时 rollback (1 commit 全部 N inserts) |
| 并发 backfill 跑 2 次 | WAL mode 1-writer-N-reader; 主公走 sequential (`--vol N` 抽样式 + `--execute` 一次性) |
| DB 损坏 | `synchronous=FULL` fsync (Phase 9.10 已设), append_nodes_atomic 复用 |
| 规则误抽 (false positive 大量) | YAML `blacklist` 完善 + `occurrence_threshold` 过滤; 主公后续改 YAML 0 改 Python |
| 跨章聚合冲突 (同 name ≠ 同人) | YAML `alias_map` 人工 curation; Phase 9.12 LLM 精筛补救 |
| 359 章 × 4 维性能 | 4 Extractor 内部 `re.findall` 0 IO, 估 < 10s; atomic_batch 1 commit < 1s |
| .md 编码非 UTF-8 | 跟 Phase 9.10 一致, 全 UTF-8; 加载失败 raise; 0 兼容 GBK |
| dry-run / execute 状态混淆 | CLI 默认 dry-run, 显式 `--execute` 才写; 两者独立从 corpus 扫 (idempotent) |
| YAML 缺字段 / 错 regex | `KeyError` / `re.error` propagate, CLI exit 1; 0 silent fallback |
| 4 Extractor 4 文件 vs 1 文件 4 class | 4 文件隔离清晰, 跟 codebase pattern 一致 (e.g. agents/{content_writer,auditor,...}); 1 文件 4 class 跨 4 策略 type check 难 |
| 10 章 fixture 跟真实 359 章行为差 | E2E test 仅验证 "0 crash + 4 维 N nodes 范围合理"; 不验证具体 node id (359 章行为由主公 dry-run 验证) |
| atomic_batch rollback 失败 | Phase 9.10 留的 `BEGIN IMMEDIATE` + `commit/rollback` context manager 0 行为变 |
| `lingwen.py` CLI 改 跟 Phase 9.10 `ripple` group 冲突 | Backfill 复用 Phase 9.10 `ripple` group (0 重复 group), 同 pattern (跟 cost subcommand 一致) |
| MEMORY.md 追记 1 entry | 跟 Phase 9.10 1:1 pattern (MEMORY.md v9.17 → v9.18 + phases-8-dashboard.md 追 entry) |
| gitignore 漏 `ripple.db` | 跟 Phase 9.10 1:1, `.gitignore` L126 已 gitignored (Phase 9.10 加) |
| yaml.safe_load 安全 | 跟 codebase precedent 一致, 0 用 `yaml.load` (unsafe), 0 自定义 loader |
| Q7 plan bug (add_node vs atomic_batch 0 串) | spec self-review 修: 1 additive `append_nodes_atomic` 复用 atomic_batch, 0 改 Phase 9.10 现有 API |
| Task 3 测试 GREEN 需 fixture 引 SAMPLE_RULES_YAML | Task 5 才加 fixture, Task 3 测试用 inline rules 字典 (跟 test_backfill.py 1:1 模式), 0 依赖 fixture |

---

## Critical files

### Created
- `novel-factory/infra/cross_volume/extractors/__init__.py` (~10L) — 4 extractor import 入口
- `novel-factory/infra/cross_volume/extractors/character_extractor.py` (~80L) — character 维
- `novel-factory/infra/cross_volume/extractors/foreshadow_extractor.py` (~70L) — foreshadow 维
- `novel-factory/infra/cross_volume/extractors/setting_extractor.py` (~70L) — setting 维
- `novel-factory/infra/cross_volume/extractors/plot_point_extractor.py` (~80L) — plot_point 维
- `novel-factory/infra/cross_volume/extraction_rules.yaml` (~60L) — 4 维规则 config
- `novel-factory/infra/cross_volume/backfill.py` (~120L) — Backfiller 编排
- `novel-factory/tests/cross_volume/test_backfill.py` (~165L) — 15 tests
- `novel-factory/tests/cross_volume/fixtures/__init__.py` (~5L) — fixtures 入口
- `novel-factory/tests/cross_volume/fixtures/sample_corpus.py` (~40L) — SAMPLE_CORPUS_ROOT + SAMPLE_RULES_YAML
- `novel-factory/tests/cross_volume/fixtures/ch{001..010}_*.md` (~50KB) — 10 章 sample (5 正文 + 5 大纲)

### Modified (only 2 files additive, 0 production code behavior change)
- `novel-factory/infra/cross_volume/storage.py` (+30L, 1 new method `append_nodes_atomic`, 0 改 Phase 9.10 现有 API)
- `novel-factory/lingwen.py` (+30L, 1 new sub-command `ripple backfill`)
- `novel-factory/tests/cross_volume/test_storage.py` (+30L, 1 new test `TestAppendNodesAtomic`)
- `~/.claude/projects/.../memory/MEMORY.md` (~3 lines edit, 0 commit)
- `~/.claude/projects/.../memory/phases-8-dashboard.md` (~1.5K chars append, 0 commit)

### NOT modified (0 例外 hard rule)
- `novel-factory/infra/ai_service/cost_tracker.py` / `cost_tracker_db.py` (0 改)
- `novel-factory/infra/agent_system/master_controller.py` (0 改)
- `novel-factory/infra/agent_system/got_bridge.py` (0 改)
- `novel-factory/infra/agent_system/workflow_db.py` (0 改)
- `novel-factory/infra/cross_volume/reference_graph.py` (0 改, Phase 9.10 留)
- `novel-factory/infra/cross_volume/ripple.py` (0 改, Phase 9.10 留)
- `novel-factory/infra/cross_volume/__init__.py` (0 改, Phase 9.10 留)
- `novel-factory/dashboard/frontend/**` (0 改, Phase 9.11 纯 backend)
- `novel-factory/CLAUDE.md` (0 改)
- `novel-factory/pyproject.toml` (0 改)
- `novel-factory/pytest.ini` (0 改)
- `novel-factory/docs/superpowers/specs/2026-06-08-*` (Phase 9 spec 0 改, `:NNN` 行号不动)
- `novel-factory/docs/superpowers/plans/2026-06-08-*` (Phase 9 blueprint 0 改, `:NNN` 行号不动)
- `novel-factory/docs/superpowers/specs/2026-06-09-phase9.10-*` (Phase 9.10 spec 0 改, 留 commit ref)
- `novel-factory/docs/superpowers/plans/2026-06-09-phase9.10-*` (Phase 9.10 plan 0 改, 留 commit ref)
- 17 historical spec/plan docs (0 改, 跟 Phase 9.10 precedent 一致)

### Gitignored (NOT committed)
- `novel-factory/infra/.state/ripple.db` (Phase 9.10 加 gitignore L126, 0 改 .gitignore)
- `novel-factory/infra/.state/ripple.db-journal` (sqlite3 WAL)
- `novel-factory/infra/.state/ripple.db-wal` (sqlite3 WAL)
- `novel-factory/infra/.state/ripple.db-shm` (sqlite3 WAL)

---

## DoD (Definition of Done)

- [ ] `infra/cross_volume/extractors/{__init__,character,foreshadow,setting,plot_point}_extractor.py` 5 files 创建 (~310L)
- [ ] `infra/cross_volume/extraction_rules.yaml` 创建 (~60L, 4 维规则)
- [ ] `infra/cross_volume/backfill.py` 创建 (~120L, Backfiller 编排)
- [ ] `infra/cross_volume/storage.py` 加 1 additive method `append_nodes_atomic` (+30L, 0 改 Phase 9.10 现有 API)
- [ ] `lingwen.py` 加 `ripple backfill --dry-run/--execute --vol N` sub-command (+30L)
- [ ] `tests/cross_volume/test_backfill.py` 创建 (~165L, 15 tests)
- [ ] `tests/cross_volume/test_storage.py` 加 1 new test `TestAppendNodesAtomic` (+30L, 跟 Phase 9.10 7 tests 1:1 模式)
- [ ] `tests/cross_volume/fixtures/` 创建 (~95L code + ~50KB data, 10 章 fixture + sample_corpus.py)
- [ ] 6 atomic commits pushed to origin/master (1 spec + 1 test RED + 1 extractor feat + 1 backfill feat + 1 cli feat + 1 fixture chore)
- [ ] pytest 2294 passed, 0 failed, 27 skipped (2278 + 16 new)
- [ ] ruff 0 issues
- [ ] `git check-ignore infra/.state/ripple.db` → gitignored (Phase 9.10 已加, 0 改)
- [ ] 0 production code 改 (12 SCENARIOS, MasterController, dashboard, workflow.db, cost_tracker.db)
- [ ] 0 改 Phase 9.10 4 source 现有 API 行为 (仅 additive `append_nodes_atomic`, 0 改 atomic_batch / append_node / load_all_nodes / _row_to_node)
- [ ] 0 改 backend / frontend historical spec/plan doc (`:NNN` 行号不动)
- [ ] 0 改 CLAUDE.md / pyproject.toml / pytest.ini
- [ ] 0 API key 泄漏 / 0 .env 改
- [ ] 0 force-push, 0 --amend, 0 --no-verify, 0 Co-Authored-By footer
- [ ] MEMORY.md L12 + L26 + L29 同步 (v9.18, 2294 tests, Phase 9.11 entry 索引)
- [ ] phases-8-dashboard.md 追加 Phase 9.11 entry (含 6 commit SHAs)
- [ ] MEMORY.md line count < 200 (跟 0 例外 hard rule)
- [ ] `python lingwen.py ripple backfill --help` 显示 --dry-run/--execute, --vol, --rules 选项
- [ ] `python lingwen.py ripple backfill --vol 1` (主公手动) 输出 DRY-RUN stats ≥ 10 nodes

---

## 完成后下一步 (后续 phases)

- **Phase 9.12.1-9.12.4**: LLM scanner 4 prompts (character / foreshadow / setting / plot_point 各自 prompt template + LLM 推断 4 维, hybrid 模式 跟 9.11 规则互补)
- **Phase 9.12.5**: Edge 创建 (8 种 relationship_type LLM 推断, 跨章上下文语义)
- **Phase 9.13.1-9.13.2**: FastAPI `POST /api/ripple/backfill` + `GET /api/ripple/:id`
- **Phase 9.13.3**: Vue 3 `ReviewRipple.vue` (impact 列表 + diff preview)
- **Phase 9.13.4**: WebSocket `/ws/ripple` cascade event push
- **Phase 9.14.1**: `audit_log` table + trigger
- **Phase 9.14.2**: fsync tuning (`synchronous=EXTRA`)
- **Phase 9.14.3**: real rollback logic (savepoint per ripple)
- **Phase 9.15.1**: real BFS cascade impl (替换 `trigger_cascade` stub)
- **Phase 9.15.2**: cache layer (LRU 1000 nodes)
- **Phase 9.15.3**: dual storage (SQLite + JSON snapshot)
- **Phase 9.15.4**: perf benchmark + optimize
- **Phase 9 (5 super-phases) 进度**: 11/15 (73%) after Phase 9.11
- **MEMORY.md 拆 42+ entries** (defer, Phase 9.15 完后统一拆)
- **phases-8-dashboard.md 拆 2 sub-era** (defer, MEMORY.md 拆时一起做)
- **legacy ./memory/MEMORY.md v6.1 → v9.18 sync** (defer)
- **Coverage threshold branches 50→60** (defer, followup phase)
- **Playwright browser install + CI integration** (defer, followup phase)
- **5 input validation branches 加 unit test** (跟 Phase 9.10 留 followup 1:1 模式, 留 Phase 9.12)
- **ripple.py:13 `__import__("uuid")` 1-line cleanup** (跟 Phase 9.10 留 followup 1:1 模式)
- **Backfill 增量** (跟 workflow.db 集成, 章节完成时增量扫新章节, 留 Phase 9.13)
- **corpus encoding 兼容性** (0 GBK 兼容, 留 Phase 9.12+ 跨编码)
- **extraction_rules.yaml alias_map 跟主公 spec 0a8a900 角色表 cross-check** (留 Phase 9.12 LLM 校准)
