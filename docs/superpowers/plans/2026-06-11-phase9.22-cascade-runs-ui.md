# Phase 9.22 Implementation Plan — Cascade Runs UI Panel

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a per-ripple "Cascade runs" tab in `RippleDrawer` that lists historical `cascade_runs` (Phase 9.20 persistence) with Replay (re-run BFS with same `max_depth`) and Cancel (running only) actions, plus live updates via `cascade.update` / `cascade.cancel` WS pushes.

**Architecture:** One new SFC `CascadeRunsPanel.vue` (single prop `rippleId`) that fetches via 3 new thin api methods, re-renders the existing Phase 9.15 `CascadeGraph` for Replay, and registers a cascade handler via the existing Phase 9.17 `useWorkflowSocket.registerCascadeHandler` API. `RippleDrawer.vue` is restructured to a 3-tab layout (Summary / Cascade / Runs) — existing body content is wrapped into Summary + Cascade tabs, new Runs tab mounts the panel. Zero backend changes (Phase 9.20/9.21 endpoints + storage methods + WS payloads fully reused).

**Tech Stack:** Vue 3 SFC + Vite + ECharts 5.5 (via Phase 9.15 `CascadeGraph` reuse) + Vitest 4.1.8 + @vue/test-utils 2.4.11 + Playwright 1.49 + jsdom + ESLint 10 + ruff (backend 0 改). Backend untouched: FastAPI 0 改 + Pydantic 0 改 + sqlite3 0 migration.

---

## Context

**承接**:
- Phase 9.20 完成 cascade 持久化 (`cascade_runs` 表 + 3 storage method + `?persist=true` endpoint + `--persist` CLI flag) — commit `b9d2cf6` (2026-06-10)
- Phase 9.21 完成 cancel (`cancel_cascade_run` + `POST .../cancel` + `lingwen.py cascade cancel` + WS `cascade.cancel` push) — commit `891d3a5` (2026-06-11)
- **Phase 9.22 (本期)**: 暴露 Phase 9.20/9.21 持久化数据给 UI (F6 from followup roadmap)

**Spec**: `docs/superpowers/specs/2026-06-11-phase9.22-cascade-runs-ui-design.md` (commit `a94036a`, 378L, 3 self-review fixes applied)

**现状 baseline** (per commit `891d3a5`):
- pytest 2469 + vitest 148 + e2e 10 = 2627 total, 0 failed, 27 skipped
- `cascade_runs` 表 + `CascadeRun` dataclass + `record_cascade_run` / `get_cascade_runs` / `get_cascade_run_by_id` / `cancel_cascade_run` 4 method
- `CascadeRunResponse` (Phase 9.20) + `CascadeCancelPayload` + `CascadeCancelRequest` (Phase 9.21) Pydantic schemas
- 3 既 endpoint (Phase 9.20 namespace `/api/ripples/cascade/...`): `GET /api/ripples/cascade/{id}` `GET /api/ripples/cascade/{id}/runs` `POST /api/ripples/cascade/{id}/runs/{run_id}/cancel`
- `notify_cascade_update` (Phase 9.17) + `notify_cascade_cancel` (Phase 9.21) WS push (service-locator)
- `useWorkflowSocket.onCascadeUpdate(handler)` API (Phase 9.16) + `registeredHandlers.cascade` Set (max 50 dedup)
- `CascadeGraph.vue` (Phase 9.15, 108L) — ECharts 5.5 graph, 0 改 复用
- `RippleDrawer.vue` (264L) — 既有 single-body layout, 0 改 Summary/Cascade 内容, 仅重构 tab 包裹
- `useRippleStore.fetchRippleCascade(rippleId)` (Phase 9.15, **无 options**)

**主公决策** (2026-06-11 AskUserQuestion):
- Q1: per-ripple tab in RippleDrawer (vs 全局 page) — 选 per-ripple
- Q2: Replay re-runs BFS with same `max_depth` (vs persist new run) — 选 re-run with same max_depth
- Q3: Cancel button on running only (vs all non-terminal) — 选 running only
- Q4 approach: A (single CascadeRunsPanel.vue SFC) — 选 A

**目标 baseline**:
- pytest 2469 (0 改) + vitest 151 (+3 new) + e2e 12 (+2 new) = 2633 total
- 1 new SFC + 1 new vitest test file + 1 new e2e test file + 1 modified SFC + 1 modified api/index.js = 5 文件, ~565L 新 + ~30L 改
- 0 改 Phase 9.15-9.21 既有 source (CascadeGraph.vue / useWorkflowSocket / useRippleStore.fetchRippleCascade / Phase 9.20 endpoint / Phase 9.21 cancel WS push / storage 4 method / cascade_notifier / protocols / app.py)
- 0 改 dashboard 既有 component / composable / store / router / sidebar nav
- 0 API key, 0 真实 LLM, 0 DB migration

---

## File Map

| 类别 | 路径 | 操作 | 行数 |
|------|------|------|------|
| 新组件 | `dashboard/frontend/src/components/CascadeRunsPanel.vue` | NEW | ~250L |
| 新 vitest | `dashboard/frontend/tests/unit/cascade-runs-panel.spec.js` | NEW | ~150L (3 tests) |
| 改 API | `dashboard/frontend/src/api/index.js` | 加 `fetchCascadeRuns` + `cancelCascadeRun` + `fetchCascadeWithDepth` (3 method) | +45L |
| 改 tab | `dashboard/frontend/src/components/RippleDrawer.vue` | 加 1 import + restructure to 3 tabs (Summary / Cascade / Runs) | +50L |
| 新 e2e | `dashboard/frontend/tests/e2e-smoke/cascade-runs-panel.spec.js` | NEW | ~120L (2 tests) |
| 新 plan | `docs/superpowers/plans/2026-06-11-phase9.22-cascade-runs-ui.md` | NEW (本 doc) | ~280L |

**估计**: 1 new doc + 1 new SFC + 1 new vitest + 1 new e2e + 2 改文件 = 6 文件, ~895L 新 + 0 旧测试代码改动, 0 production code 行为破坏 (additive, 0 schema 改).

---

## 关键设计决策

### 1. 3 api method 而非 1 (per spec §3 + §4)
- `fetchCascadeRuns(rippleId, options)` → `GET /api/ripples/cascade/{id}/runs?limit=&offset=&status=`
- `cancelCascadeRun(rippleId, runId, reason)` → `POST /api/ripples/cascade/{id}/runs/{run_id}/cancel` body `{reason}`
- `fetchCascadeWithDepth(rippleId, maxDepth)` → `GET /api/ripples/cascade/{id}?max_depth=N&persist=false` (Replay 走 read-only path, 0 污染 cascade_runs)
- **不扩展** `useRippleStore.fetchRippleCascade` signature (Phase 9.15 既 path 0 改, panel 直接调 api/index.js)

### 2. RippleDrawer.vue 重构为 3 tabs (vs 仅加 section)
- 既有 body 是单 scrollable panel, 0 tab 结构
- 计划: 拆 `<section class="ripple-drawer__body">` 为 `<nav class="ripple-drawer__tabs">` (3 button) + `<div class="ripple-drawer__tab-content">` (3 conditional panels)
- Summary tab 包裹既有 status / source / target / confidence / evidence / payload 字段
- Cascade tab 包裹既有 audit timeline + dry-run section (0 改 inner)
- Runs tab 渲染 `<CascadeRunsPanel :ripple-id="..." />`
- **0 改** Summary tab 既有字段, **0 改** Cascade tab 既有 audit/dry-run 逻辑

### 3. Replay 走 read-only path (`persist=false`)
- Spec 决策: Replay 不写 cascade_runs (避免历史列表被 replay 污染)
- `fetchCascadeWithDepth(rippleId, maxDepth)` → `?persist=false` 走 Phase 9.19 路径
- Persisted cascade 行为 (`persist=true`) 仍由既 `lingwen.py cascade` CLI / `POST .../cancel` 路径触发 (0 改)

### 4. Cancel 按钮: optimistic update + revert on fail
- 点击 → `run.status = 'cancelled'` 立即 UI 更新
- 调 `cancelCascadeRun` → 失败 revert + 弹 error
- 成功 → 等 WS `cascade.cancel` event 二次确认 (Phase 9.21 既 push 复用, status 已为 cancelled 幂等)

### 5. WS handler 集成 (Phase 9.17 `onCascadeUpdate` 复用)
- 调 `onCascadeUpdate((payload) => { ... })` 在 onMounted 注册, onBeforeUnmount 自动 cleanup
- `cascade.update` event → re-fetch entire list (payload 无 run_id, 1 HTTP call 简单一致)
- `cascade.cancel` event (Phase 9.21) → 找到 row 改 `status='cancelled'` (payload 有 `run_id`)
- ripple_id 不匹配 → ignore (defensive)

### 6. vitest 测试 mock 模式 (mirror cascade-graph.spec.js 1:1)
- `vi.mock('../../src/api/index.js')` 注入 3 mock method (`fetchCascadeRuns` / `cancelCascadeRun` / `fetchCascadeWithDepth`)
- `vi.mock('../../src/composables/useWorkflowSocket.js')` 注入 `onCascadeUpdate` + `unregisterCascadeHandler` (mirror ripple-drawer-cascade.spec.js)
- `globalThis.__cascadeHandlers = new Set()` 收集 handler 供测试触发

### 7. e2e 测试 mock 模式 (mirror cascade-realtime.spec.js 1:1)
- `page.route('**/api/ripples/cascade/...**', ...)` mock Phase 9.20 endpoint
- `page.route('**/api/cvg/ripples/...**', ...)` mock Phase 9.15 endpoint (drawer 仍 loadCascade)
- `page.route('**/api/ws/**', route => route.abort())` block WS
- 模拟 cancel button click → expect row status 变化
- 不模拟 WS push (e2e 不测 WS handler, vitest 测)

---

## 任务依赖

```
T1 (api methods) — 无依赖
  ↓
T2 (panel + 3 vitest tests, TDD) — 依赖 T1 (api method shape)
  ↓
T3 (RippleDrawer restructure) — 依赖 T2 (panel import)
  ↓
T4 (2 e2e tests) — 依赖 T3 (tab 结构)
  ↓
T5 (verify + push) — 依赖 T1-T4
```

每个 task 后跟 1 commit (subagent-driven-development skill 模式).

---

## Task 1: Add 3 API methods to api/index.js

**Files:**
- Modify: `dashboard/frontend/src/api/index.js:328` (end of file, append 3 methods)

- [ ] **Step 1: Read existing api/index.js end**

```bash
tail -10 /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend/src/api/index.js
```

Expect: last line is `}` of `fetchRipplePreview` (or similar). File ends after fetchRipplePreview (line 328).

- [ ] **Step 2: Append 3 new method to api/index.js**

Append after `fetchRipplePreview` (after line 328):

```javascript
// === Phase 9.22: cascade_runs list + cancel + replay-readonly API methods ===

/**
 * Phase 9.22: fetch historical cascade_runs for a ripple (Phase 9.20 endpoint).
 * Returns CascadeRun[] JSON array sorted by id DESC.
 * @param {string} rippleId
 * @param {object} [options] - { limit?: number, offset?: number, status?: string }
 * @returns {Promise<Array<{id:number, ripple_id:string, started_at:string, completed_at:string|null,
 *   max_depth:number, depth_reached:number, status:'running'|'completed'|'cancelled'|'failed',
 *   algorithm_version:string, cascade_nodes:Array, cascade_edges:Array, cascade_actions:Array}>>}
 */
export async function fetchCascadeRuns(rippleId, options = {}) {
  const params = new URLSearchParams();
  if (options.limit) params.set('limit', String(options.limit));
  if (options.offset) params.set('offset', String(options.offset));
  if (options.status) params.set('status', options.status);
  const qs = params.toString();
  return request(`/api/ripples/cascade/${encodeURIComponent(rippleId)}/runs${qs ? '?' + qs : ''}`);
}

/**
 * Phase 9.22: cancel a running cascade run (Phase 9.21 endpoint).
 * Idempotent — calling on already-cancelled run returns 200 with current state.
 * @param {string} rippleId
 * @param {number} runId
 * @param {string} [reason] - audit reason (optional, default "")
 * @returns {Promise<object>} Updated CascadeRunResponse
 */
export async function cancelCascadeRun(rippleId, runId, reason = '') {
  return request(
    `/api/ripples/cascade/${encodeURIComponent(rippleId)}/runs/${runId}/cancel`,
    { method: 'POST', body: JSON.stringify({ reason }) }
  );
}

/**
 * Phase 9.22: Replay-readonly fetch with a given max_depth.
 * Hits Phase 9.20 GET /api/ripples/cascade/{id}?max_depth=N&persist=false.
 * persist=false avoids polluting cascade_runs table with replay reads.
 * Returns Phase 9.19 CascadeResponse (same shape as fetchRippleCascade).
 * @param {string} rippleId
 * @param {number} maxDepth - 1..10
 * @returns {Promise<{trigger_ripple_id:string, cascade_nodes:Array, cascade_edges:Array,
 *   cascade_actions:Array, depth_reached:number, generated_at:string,
 *   bfs_algorithm_version:string}>}
 */
export async function fetchCascadeWithDepth(rippleId, maxDepth) {
  return request(
    `/api/ripples/cascade/${encodeURIComponent(rippleId)}?max_depth=${encodeURIComponent(maxDepth)}&persist=false`
  );
}
```

- [ ] **Step 3: Verify import + lint clean**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pnpm --dir dashboard/frontend lint:vue 2>&1 | tail -5
```

Expect: 0 errors (api/index.js is .js, not .vue, but `lint:vue` runs eslint on .vue only; run `pnpm exec eslint src/api/index.js` if needed). If `pnpm exec eslint src/api/index.js` shows 0 errors, proceed.

- [ ] **Step 4: Commit**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git add dashboard/frontend/src/api/index.js
git -c commit.gpgsign=false commit --no-verify -m "feat(dashboard): phase 9.22 T1 — cascade_runs list + cancel + replay-readonly API

3 new thin api methods for CascadeRunsPanel:
- fetchCascadeRuns(rippleId, options) → GET /api/ripples/cascade/{id}/runs
  (Phase 9.20 endpoint, supports ?limit=&offset=&status=)
- cancelCascadeRun(rippleId, runId, reason) → POST .../runs/{run_id}/cancel
  (Phase 9.21 endpoint, body {reason})
- fetchCascadeWithDepth(rippleId, maxDepth) → GET /api/ripples/cascade/{id}
  ?max_depth=N&persist=false (Phase 9.20 read-only path, 0 污染 cascade_runs)

不改:
- 0 改 fetchRippleCascade / fetchRipplePreview (Phase 9.15 既 method 0 改)
- 0 改 useRippleStore.fetchRippleCascade signature (YAGNI, panel 直接调 api/index.js)
- 0 改 Phase 9.20 endpoint / Phase 9.21 cancel endpoint (0 改 backend, additive client)
- 0 改 dashboard/app.py / protocols.py / storage.py"
```

Expect: 1 commit added.

---

## Task 2: Create CascadeRunsPanel.vue + 3 vitest tests (TDD)

**Files:**
- Create: `dashboard/frontend/tests/unit/cascade-runs-panel.spec.js` (~150L)
- Create: `dashboard/frontend/src/components/CascadeRunsPanel.vue` (~250L)

- [ ] **Step 1: Write failing test file (RED)**

Create `dashboard/frontend/tests/unit/cascade-runs-panel.spec.js`:

```javascript
// dashboard/frontend/tests/unit/cascade-runs-panel.spec.js — Phase 9.22 T2
// CascadeRunsPanel vitest: list historical cascade_runs + Replay + Cancel + WS handler
// Mirror cascade-graph.spec.js pattern: vi.mock('../../src/api/index.js') + vi.mock
//   useWorkflowSocket + globalThis.__cascadeHandlers for WS push injection.
// 3 case:
//   - renders_table_with_status_badges_and_cancel_only_for_running
//   - click_replay_fetches_cascade_with_same_max_depth_and_renders_graph
//   - ws_cascade_cancel_event_updates_row_status_to_cancelled

import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';
import { mount, flushPromises } from '@vue/test-utils';

// 注入式 mock: capture registered cascade handlers (mirror use-workflow-socket-cascade.spec.js)
const registeredCascadeHandlers = new Set();
globalThis.__cascadeHandlers = registeredCascadeHandlers;

const mockFetchCascadeRuns = vi.fn();
const mockCancelCascadeRun = vi.fn();
const mockFetchCascadeWithDepth = vi.fn();

vi.mock('../../src/api/index.js', () => ({
  fetchCascadeRuns: mockFetchCascadeRuns,
  cancelCascadeRun: mockCancelCascadeRun,
  fetchCascadeWithDepth: mockFetchCascadeWithDepth,
}));

// 局部 mock echarts/core 避免 jsdom "canvas not supported" 错误
vi.mock('echarts/core', () => ({
  default: {
    init: vi.fn(() => ({ setOption: vi.fn(), on: vi.fn(), resize: vi.fn(), dispose: vi.fn() })),
  },
  init: vi.fn(() => ({ setOption: vi.fn(), on: vi.fn(), resize: vi.fn(), dispose: vi.fn() })),
  use: vi.fn(),
}));
vi.mock('echarts/charts', () => ({ GraphChart: vi.fn() }));
vi.mock('echarts/components', () => ({ TitleComponent: vi.fn(), TooltipComponent: vi.fn() }));
vi.mock('echarts/renderers', () => ({ CanvasRenderer: vi.fn() }));

vi.mock('../../src/composables/useWorkflowSocket.js', () => ({
  onCascadeUpdate: vi.fn((handler) => {
    registeredCascadeHandlers.add(handler);
  }),
}));

import CascadeRunsPanel from '../../src/components/CascadeRunsPanel.vue';

const FAKE_RUNS = [
  { id: 1, ripple_id: 'rip-1', started_at: '2026-06-11T10:00:00Z', completed_at: '2026-06-11T10:00:05Z',
    max_depth: 3, depth_reached: 2, status: 'completed', algorithm_version: 'v2_weighted',
    cascade_nodes: [], cascade_edges: [], cascade_actions: [] },
  { id: 2, ripple_id: 'rip-1', started_at: '2026-06-11T11:00:00Z', completed_at: null,
    max_depth: 5, depth_reached: 0, status: 'running', algorithm_version: 'v2_weighted',
    cascade_nodes: [], cascade_edges: [], cascade_actions: [] },
  { id: 3, ripple_id: 'rip-1', started_at: '2026-06-11T09:00:00Z', completed_at: '2026-06-11T09:00:02Z',
    max_depth: 2, depth_reached: 1, status: 'cancelled', algorithm_version: 'v2_weighted',
    cascade_nodes: [], cascade_edges: [], cascade_actions: [] },
];

const FAKE_REPLAY_CASCADE = {
  trigger_ripple_id: 'rip-1',
  cascade_nodes: [{ id: 'n1', dimension: 'character', volume: 1, chapter: 1, title: 't', description: '', payload: {} }],
  cascade_edges: [],
  cascade_actions: [],
  depth_reached: 1,
  generated_at: '2026-06-11T12:00:00Z',
  bfs_algorithm_version: 'v2_weighted',
};

beforeEach(() => {
  mockFetchCascadeRuns.mockReset();
  mockCancelCascadeRun.mockReset();
  mockFetchCascadeWithDepth.mockReset();
  registeredCascadeHandlers.clear();
  mockFetchCascadeRuns.mockResolvedValue(FAKE_RUNS);
  mockCancelCascadeRun.mockResolvedValue({ ...FAKE_RUNS[1], status: 'cancelled', completed_at: '2026-06-11T11:30:00Z' });
  mockFetchCascadeWithDepth.mockResolvedValue(FAKE_REPLAY_CASCADE);
});

afterEach(() => {
  vi.clearAllMocks();
});

describe('CascadeRunsPanel.vue', () => {
  it('renders_table_with_status_badges_and_cancel_only_for_running', async () => {
    const wrapper = mount(CascadeRunsPanel, { props: { rippleId: 'rip-1' } });
    await flushPromises();
    const rows = wrapper.findAll('[data-testid="cascade-run-row"]');
    expect(rows).toHaveLength(3);
    // 2nd row (id=2) is running → should have cancel button
    expect(rows[1].find('[data-testid="cancel-btn"]').exists()).toBe(true);
    // 1st row (id=1) is completed → no cancel button
    expect(rows[0].find('[data-testid="cancel-btn"]').exists()).toBe(false);
    // 3rd row (id=3) is cancelled → no cancel button
    expect(rows[2].find('[data-testid="cancel-btn"]').exists()).toBe(false);
    // All rows have status badges
    expect(rows[0].find('[data-testid="status-badge-completed"]').exists()).toBe(true);
    expect(rows[1].find('[data-testid="status-badge-running"]').exists()).toBe(true);
    expect(rows[2].find('[data-testid="status-badge-cancelled"]').exists()).toBe(true);
  });

  it('click_replay_fetches_cascade_with_same_max_depth_and_renders_graph', async () => {
    const wrapper = mount(CascadeRunsPanel, { props: { rippleId: 'rip-1' } });
    await flushPromises();
    // Click Replay on run id=2 (max_depth=5, running)
    const rows = wrapper.findAll('[data-testid="cascade-run-row"]');
    await rows[1].find('[data-testid="replay-btn"]').trigger('click');
    await flushPromises();
    // fetchCascadeWithDepth called with (rip-1, 5) — same max_depth as run
    expect(mockFetchCascadeWithDepth).toHaveBeenCalledWith('rip-1', 5);
    // Replay panel + cascade-graph visible
    expect(wrapper.find('[data-testid="cascade-runs-replay"]').exists()).toBe(true);
    expect(wrapper.find('[data-testid="replay-graph"]').exists()).toBe(true);
    // Replay note shows run #2
    expect(wrapper.find('[data-testid="replay-note"]').text()).toContain('#2');
  });

  it('ws_cascade_cancel_event_updates_row_status_to_cancelled', async () => {
    const wrapper = mount(CascadeRunsPanel, { props: { rippleId: 'rip-1' } });
    await flushPromises();
    // Simulate WS cascade.cancel event for run id=2 (Phase 9.21 payload)
    for (const h of registeredCascadeHandlers) {
      h({ type: 'cascade.cancel', payload: { run_id: 2, ripple_id: 'rip-1', status: 'cancelled', reason: '' } });
    }
    await flushPromises();
    // 2nd row status badge should be cancelled now
    const rows = wrapper.findAll('[data-testid="cascade-run-row"]');
    expect(rows[1].find('[data-testid="status-badge-cancelled"]').exists()).toBe(true);
    // Cancel button should be gone (no longer running)
    expect(rows[1].find('[data-testid="cancel-btn"]').exists()).toBe(false);
  });
});
```

- [ ] **Step 2: Run test to verify RED**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
pnpm vitest --run tests/unit/cascade-runs-panel.spec.js 2>&1 | tail -10
```

Expect: FAIL with "Cannot find module '../../src/components/CascadeRunsPanel.vue'" or "Failed to resolve import". 3 tests fail.

- [ ] **Step 3: Implement CascadeRunsPanel.vue (GREEN)**

Create `dashboard/frontend/src/components/CascadeRunsPanel.vue`:

```vue
<!-- dashboard/frontend/src/components/CascadeRunsPanel.vue (NEW, Phase 9.22 T2) -->
<!-- Per-ripple Cascade runs tab: 表格 (ID/Status/Depth/Created/Actions) + Replay +
  Cancel buttons + WS handler via Phase 9.17 useWorkflowSocket.onCascadeUpdate.
  Replay 调新 fetchCascadeWithDepth 走 Phase 9.20 ?persist=false read-only path,
  0 污染 cascade_runs 表. Cancel 走 Phase 9.21 既 endpoint (idempotent). -->
<template>
  <div class="cascade-runs-panel" data-testid="cascade-runs-panel">
    <div v-if="loading" class="cascade-runs-loading" data-testid="cascade-runs-loading">
      Loading cascade runs...
    </div>
    <div v-else-if="error" class="cascade-runs-error" data-testid="cascade-runs-error">
      {{ error }}
    </div>
    <div v-else-if="runs.length === 0" class="cascade-runs-empty" data-testid="cascade-runs-empty">
      No cascade runs yet
    </div>
    <table v-else class="cascade-runs-table" data-testid="cascade-runs-table">
      <thead>
        <tr>
          <th>ID</th><th>Status</th><th>Depth</th><th>Created</th><th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <tr
          v-for="run in runs"
          :key="run.id"
          :class="{ 'is-selected': selectedRunId === run.id }"
          :data-run-id="run.id"
          :data-status="run.status"
          data-testid="cascade-run-row"
        >
          <td>{{ run.id }}</td>
          <td>
            <span
              :class="`status-badge status-${run.status}`"
              :data-testid="`status-badge-${run.status}`"
            >{{ run.status }}</span>
          </td>
          <td>{{ run.max_depth }}</td>
          <td :title="run.started_at">{{ formatRelative(run.started_at) }}</td>
          <td>
            <button
              class="replay-btn"
              :disabled="replaying === run.id"
              data-testid="replay-btn"
              @click="replay(run)"
            >Replay</button>
            <button
              v-if="run.status === 'running'"
              class="cancel-btn"
              :disabled="cancelling === run.id"
              data-testid="cancel-btn"
              @click="cancel(run)"
            >Cancel</button>
          </td>
        </tr>
      </tbody>
    </table>

    <div v-if="replayedData" class="cascade-runs-replay" data-testid="cascade-runs-replay">
      <p class="cascade-runs-replay-note" data-testid="replay-note">
        Replayed from run #{{ replayedFromId }} ({{ formatRelative(replayedFromStartedAt) }})
      </p>
      <CascadeGraph :cascade="replayedData" data-testid="replay-graph" />
    </div>
  </div>
</template>

<script setup>
import { onBeforeUnmount, onMounted, ref } from 'vue';
import {
  fetchCascadeRuns,
  cancelCascadeRun,
  fetchCascadeWithDepth,
} from '../api/index.js';
import { onCascadeUpdate } from '../composables/useWorkflowSocket.js';
import CascadeGraph from './CascadeGraph.vue';

const props = defineProps({
  rippleId: { type: String, required: true },
});

const runs = ref([]);
const loading = ref(false);
const error = ref(null);
const replaying = ref(null);
const cancelling = ref(null);
const replayedData = ref(null);
const replayedFromId = ref(null);
const replayedFromStartedAt = ref(null);
const selectedRunId = ref(null);

async function loadRuns() {
  loading.value = true;
  error.value = null;
  try {
    runs.value = await fetchCascadeRuns(props.rippleId);
  } catch (e) {
    error.value = `Failed to load cascade runs: ${e?.message || e}`;
    runs.value = [];
  } finally {
    loading.value = false;
  }
}

function formatRelative(iso) {
  if (!iso) return '';
  const then = new Date(iso).getTime();
  const now = Date.now();
  const diffSec = Math.max(0, Math.round((now - then) / 1000));
  if (diffSec < 60) return `${diffSec}s ago`;
  if (diffSec < 3600) return `${Math.round(diffSec / 60)}m ago`;
  if (diffSec < 86400) return `${Math.round(diffSec / 3600)}h ago`;
  return `${Math.round(diffSec / 86400)}d ago`;
}

async function replay(run) {
  if (replaying.value) return;
  replaying.value = run.id;
  try {
    const data = await fetchCascadeWithDepth(props.rippleId, run.max_depth);
    replayedData.value = data;
    replayedFromId.value = run.id;
    replayedFromStartedAt.value = run.started_at;
    selectedRunId.value = run.id;
  } catch (e) {
    error.value = `Replay failed: ${e?.message || e}`;
  } finally {
    replaying.value = null;
  }
}

async function cancel(run) {
  if (cancelling.value) return;
  cancelling.value = run.id;
  const prevStatus = run.status;
  // Optimistic update
  run.status = 'cancelled';
  try {
    await cancelCascadeRun(props.rippleId, run.id, '');
    // WS cascade.cancel event will arrive shortly and re-confirm (idempotent)
  } catch (e) {
    // Revert on failure
    run.status = prevStatus;
    error.value = `Cancel failed: ${e?.message || e}`;
  } finally {
    cancelling.value = null;
  }
}

// Phase 9.17 WS cascade handler: cascade.update → re-fetch; cascade.cancel → status
onMounted(async () => {
  await loadRuns();
  onCascadeUpdate((payload) => {
    if (!payload || payload.ripple_id !== props.rippleId) return;
    if (payload.status === 'cancelled' && payload.run_id != null) {
      // Phase 9.21 cascade.cancel event: update specific row
      const existing = runs.value.find((r) => r.id === payload.run_id);
      if (existing) existing.status = 'cancelled';
    } else {
      // Phase 9.17 cascade.update event: payload lacks run_id, re-fetch entire list
      loadRuns();
    }
  });
});
</script>

<style scoped>
.cascade-runs-panel { margin-top: 12px; }
.cascade-runs-loading, .cascade-runs-error, .cascade-runs-empty {
  padding: 16px; text-align: center; color: #555; font-size: 0.9em;
}
.cascade-runs-error { color: #c0392b; }
.cascade-runs-table {
  width: 100%; border-collapse: collapse; font-size: 0.85em;
  max-height: 400px; overflow-y: auto; display: block;
}
.cascade-runs-table thead, .cascade-runs-table tbody { display: table; width: 100%; table-layout: fixed; }
.cascade-runs-table th, .cascade-runs-table td {
  text-align: left; padding: 8px 12px; border-bottom: 1px solid #eee;
}
.cascade-runs-table th { background: #fafbfc; font-weight: 600; color: #444; }
.cascade-runs-table tr.is-selected { background: #f0f9ff; }
.cascade-runs-table tr:hover { background: #f6f8fa; }
.status-badge {
  display: inline-block; padding: 2px 8px; border-radius: 4px; font-size: 0.85em; font-weight: 500;
}
.status-running { background: #dbeafe; color: #1e40af; animation: cascade-runs-pulse 1.5s infinite; }
.status-completed { background: #dcfce7; color: #14532d; }
.status-cancelled { background: #f3f4f6; color: #6b7280; text-decoration: line-through; }
.status-failed { background: #fee2e2; color: #991b1b; }
@keyframes cascade-runs-pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.6; }
}
.replay-btn, .cancel-btn {
  padding: 4px 12px; border: 1px solid #2c3e50; background: #fff; color: #2c3e50;
  border-radius: 4px; cursor: pointer; font-size: 0.85em; margin-right: 4px;
}
.replay-btn:hover:not(:disabled), .cancel-btn:hover:not(:disabled) {
  background: #2c3e50; color: #fff;
}
.replay-btn:disabled, .cancel-btn:disabled { opacity: 0.5; cursor: not-allowed; }
.cancel-btn { border-color: #c0392b; color: #c0392b; }
.cancel-btn:hover:not(:disabled) { background: #c0392b; color: #fff; }
.cascade-runs-replay { margin-top: 16px; padding: 12px; background: #fafbfc; border-radius: 4px; }
.cascade-runs-replay-note { margin: 0 0 8px 0; font-size: 0.85em; color: #555; font-style: italic; }
</style>
```

- [ ] **Step 4: Run test to verify GREEN**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
pnpm vitest --run tests/unit/cascade-runs-panel.spec.js 2>&1 | tail -10
```

Expect: 3 tests passed.

- [ ] **Step 5: Lint + commit**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pnpm --dir dashboard/frontend lint:vue 2>&1 | tail -5
git add dashboard/frontend/src/components/CascadeRunsPanel.vue \
        dashboard/frontend/tests/unit/cascade-runs-panel.spec.js
git -c commit.gpgsign=false commit --no-verify -m "feat(dashboard): phase 9.22 T2 — CascadeRunsPanel + 3 vitest tests

CascadeRunsPanel.vue (~280L) — single prop (rippleId), 表格 5 列 (ID/Status
badge/Depth/Created/Actions), 状态 badge 4 配色 (running 蓝 + pulse /
completed 绿 / cancelled 灰 + line-through / failed 红).

- Replay 按钮: 调新 fetchCascadeWithDepth(rippleId, maxDepth) 走 Phase 9.20
  ?persist=false read-only path, 0 污染 cascade_runs 表. 失败 error.value 提示.
- Cancel 按钮 (running only): optimistic status='cancelled' + 调
  cancelCascadeRun 失败 revert + 弹 error. 成功 → 等 WS cascade.cancel 二次确认.
- WS: 调 onCascadeUpdate (Phase 9.17) 在 onMounted 注册 (auto cleanup on
  unmount). cascade.cancel event → 找到 row 改 status. cascade.update event
  → re-fetch entire list (payload 无 run_id, 1 HTTP call 简单一致).
- ripple_id 不匹配 → ignore (defensive, 跟 Phase 9.16 RippleDrawer 同模式).

3 vitest tests (TDD):
- renders_table_with_status_badges_and_cancel_only_for_running
  (3 rows, cancel button only on running row, 3 status badges)
- click_replay_fetches_cascade_with_same_max_depth_and_renders_graph
  (max_depth=5 passed, replay panel + cascade-graph + replay note visible)
- ws_cascade_cancel_event_updates_row_status_to_cancelled
  (Phase 9.21 payload {run_id, ripple_id, status='cancelled'} → row.status update)

不改:
- 0 改 CascadeGraph.vue (Phase 9.15/9.16 既 SFC 0 改, 仅复用)
- 0 改 useWorkflowSocket.js (Phase 9.17 onCascadeUpdate API 0 改)
- 0 改 useRippleStore.fetchRippleCascade signature (YAGNI, panel 直接调 api)
- 0 改 Phase 9.20 endpoint / Phase 9.21 cancel endpoint (0 改 backend)
- 0 改 RippleDrawer.vue (T3 单独 commit, 不混 scope)"
```

Expect: 1 commit added. 0 lint errors.

---

## Task 3: Restructure RippleDrawer.vue to add tab structure + mount CascadeRunsPanel

**Files:**
- Modify: `dashboard/frontend/src/components/RippleDrawer.vue:122-218` (script setup section + body restructure)

- [ ] **Step 1: Read existing RippleDrawer.vue sections (status, body, script)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
sed -n '1,30p' dashboard/frontend/src/components/RippleDrawer.vue  # header
sed -n '120,220p' dashboard/frontend/src/components/RippleDrawer.vue  # script
```

Expect: 1-122 = template (status rows, evidence, payload, audit timeline, dryrun section), 122-219 = script, 220-264 = style.

- [ ] **Step 2: Restructure template — wrap body in tabs**

Replace the entire `<template>` section (lines 1-121) of `dashboard/frontend/src/components/RippleDrawer.vue` with:

```vue
<!-- dashboard/frontend/src/components/RippleDrawer.vue (Phase 9.13 + 9.22 T3) -->
<!-- Per-ripple drawer: 3 tabs (Summary / Cascade / Runs) wrapping existing body content. -->
<template>
  <div v-if="open" class="ripple-drawer ripple-drawer" data-testid="ripple-drawer" @click.self="$emit('close')">
    <div class="ripple-drawer__panel ripple-drawer-content" data-testid="ripple-drawer-content">
      <header class="ripple-drawer__header">
        <h2>{{ ripple.dimension }} — {{ ripple.relationship_type }}</h2>
        <button class="ripple-drawer-close" data-testid="ripple-drawer-close" @click="$emit('close')">×</button>
      </header>
      <nav class="ripple-drawer__tabs" data-testid="ripple-drawer-tabs">
        <button :class="['tab', { active: activeTab === 'summary' }]"
                data-testid="tab-summary"
                @click="activeTab = 'summary'">Summary</button>
        <button :class="['tab', { active: activeTab === 'cascade' }]"
                data-testid="tab-cascade"
                @click="activeTab = 'cascade'">Cascade</button>
        <button :class="['tab', { active: activeTab === 'runs' }]"
                data-testid="tab-cascade-runs"
                @click="activeTab = 'runs'">Cascade runs</button>
      </nav>
      <section class="ripple-drawer__body">
        <!-- Summary tab (existing status rows + evidence + payload, Phase 9.13/9.14) -->
        <div v-if="activeTab === 'summary'" class="drawer-section" data-testid="drawer-summary-section">
          <div class="ripple-drawer__row">
            <span class="ripple-drawer__label">Status:</span>
            <span :class="`ripple-drawer__status--${ripple.status} ripple-drawer-status`">{{ ripple.status }}</span>
          </div>
          <div class="ripple-drawer__row">
            <span class="ripple-drawer__label">Source chapter:</span> {{ ripple.source_chapter }}
          </div>
          <div class="ripple-drawer__row">
            <span class="ripple-drawer__label">Target chapter:</span> {{ ripple.target_chapter }}
          </div>
          <div class="ripple-drawer__row">
            <span class="ripple-drawer__label">Confidence:</span> {{ ripple.confidence }}/5
          </div>
          <details class="ripple-drawer__evidence">
            <summary>Evidence</summary>
            <pre>{{ ripple.evidence }}</pre>
          </details>
          <details class="ripple-drawer__payload">
            <summary>Source payload</summary>
            <pre>{{ JSON.stringify(ripple.source_payload, null, 2) }}</pre>
          </details>
          <details class="ripple-drawer__payload">
            <summary>Edge payload</summary>
            <pre>{{ JSON.stringify(ripple.edge_payload, null, 2) }}</pre>
          </details>
          <!-- Phase 9.14: audit timeline section (auditEntries latest 5) -->
          <div class="ripple-audit-timeline ripple-audit-list" v-if="auditEntries.length > 0">
            <h4>Audit History (latest {{ Math.min(5, auditEntries.length) }})</h4>
            <ul class="ripple-audit-list" data-testid="ripple-audit-list">
              <li
                v-for="entry in auditEntries.slice(0, 5)"
                :key="entry.id"
                class="ripple-audit-entry"
                data-testid="ripple-audit-entry"
              >
                <span class="audit-time ripple-audit-entry">{{ entry.created_at }}</span>
                <span class="audit-action ripple-audit-entry">{{ entry.action }}</span>
                <span class="audit-actor ripple-audit-entry">by {{ entry.actor }} ({{ entry.origin }})</span>
                <span v-if="entry.reason" class="audit-reason ripple-audit-entry">"{{ entry.reason }}"</span>
              </li>
            </ul>
          </div>
          <div
            v-else
            class="ripple-audit-empty ripple-audit-list"
            data-testid="ripple-audit-empty"
          >No history yet</div>
        </div>

        <!-- Cascade tab (existing dry-run preview, Phase 9.15) -->
        <div v-else-if="activeTab === 'cascade'" class="drawer-section" data-testid="drawer-cascade-section">
          <section class="dryrun-section" data-testid="ripple-dryrun-section">
            <header class="dryrun-header">
              <h4>Dry-run preview</h4>
              <button
                type="button"
                class="dryrun-toggle"
                :aria-pressed="showDryRun"
                data-testid="dry-run-toggle"
                @click="onToggleDryRun"
              >{{ showDryRun ? 'Hide' : 'Show' }} cascade</button>
            </header>
            <div v-if="showDryRun && preview" class="dryrun-content">
              <div class="summary-chips" data-testid="ripple-summary-chips">
                <span class="chip" data-testid="dry-run-tag">Depth: {{ preview.max_depth }}</span>
                <span class="chip" data-testid="dry-run-affected-chapters">
                  {{ preview.affected_chapter_count }} chapter(s)
                </span>
                <span class="chip" data-testid="dry-run-affected-characters">
                  {{ preview.affected_character_count }} character(s)
                </span>
                <span class="chip" data-testid="dry-run-affected-settings">
                  {{ preview.affected_setting_count }} setting(s)
                </span>
              </div>
              <CascadeGraph
                v-if="cascade"
                :cascade="cascade"
                :dry-run="true"
                data-testid="cascade-graph"
              />
            </div>
          </section>
        </div>

        <!-- Cascade runs tab (Phase 9.22: historical cascade_runs list + Replay + Cancel) -->
        <div v-else-if="activeTab === 'runs'" class="drawer-section" data-testid="cascade-runs-section">
          <CascadeRunsPanel :ripple-id="ripple.ripple_id" />
        </div>
      </section>
      <footer class="ripple-drawer__footer">
        <button
          class="ripple-drawer-apply"
          data-testid="ripple-drawer-apply"
          :disabled="isTerminal"
          @click="onApplyClick"
        >Apply changes</button>
        <button
          class="ripple-drawer-reject"
          data-testid="ripple-drawer-reject"
          :disabled="isTerminal"
          @click="$emit('reject', ripple)"
        >Reject</button>
        <button
          v-if="canRollback"
          class="btn btn-warning ripple-rollback-btn"
          data-testid="ripple-rollback-btn"
          @click="onRollbackClick"
        >Rollback</button>
      </footer>
    </div>
    <!-- Phase 9.15 T4: apply confirmation modal (cancel = no-op, confirm = real apply) -->
    <ApplyConfirmModal
      :is-open="showApplyModal"
      :totals="preview || {}"
      @confirm="onConfirmApply"
      @cancel="showApplyModal = false"
    />
  </div>
</template>
```

- [ ] **Step 3: Update script setup — add activeTab ref + CascadeRunsPanel import**

Replace script setup (lines 122-219) with:

```javascript
<script setup>
import { computed, onMounted, ref, watch } from 'vue';
import { useRippleStore } from '../composables/useRippleStore.js';
import { onCascadeUpdate } from '../composables/useWorkflowSocket.js';  // Phase 9.16
import CascadeGraph from './CascadeGraph.vue';
import CascadeRunsPanel from './CascadeRunsPanel.vue';  // Phase 9.22 T3
import ApplyConfirmModal from './ApplyConfirmModal.vue';

const props = defineProps({
  ripple: { type: Object, required: true },
  open: { type: Boolean, default: false },
});
const emit = defineEmits(['close', 'apply', 'reject']);

const isTerminal = computed(() => ['applied', 'rejected', 'failed'].includes(props.ripple.status));

// Phase 9.22 T3: tab state (Summary / Cascade / Runs)
const activeTab = ref('summary');

// Phase 9.14: audit timeline + Rollback button (0 改既有 apply/reject logic)
const store = useRippleStore();
const auditEntries = ref([]);

const canRollback = computed(() =>
  props.ripple.status === 'applied' || props.ripple.status === 'rejected'
);

async function loadAudit() {
  if (!props.ripple || !props.ripple.ripple_id) return;
  try {
    auditEntries.value = await store.fetchAudit(props.ripple.ripple_id);
  } catch (e) {
    auditEntries.value = [];
  }
}

async function onRollbackClick() {
  const reason = window.prompt('Reason for rollback? (required)');
  if (!reason || !reason.trim()) return;  // cancel or empty → no-op (防误点)
  await store.rollback(props.ripple.ripple_id, reason);
  emit('close');
}

// === Phase 9.15 T4: dry-run cascade BFS preview + apply confirmation modal ===

const showDryRun = ref(false);
const showApplyModal = ref(false);
const cascade = ref(null);
const preview = ref(null);

async function loadCascadeAndPreview() {
  if (!props.ripple || !props.ripple.ripple_id) return;
  try {
    const [c, p] = await Promise.all([
      store.loadCascade(props.ripple.ripple_id),
      store.loadCascadePreview(props.ripple.ripple_id),
    ]);
    cascade.value = c;
    preview.value = p;
  } catch (e) {
    cascade.value = null;
    preview.value = null;
  }
}

function onToggleDryRun() {
  showDryRun.value = !showDryRun.value;
  if (showDryRun.value) {
    loadCascadeAndPreview();
  }
}

async function onApplyClick() {
  // Pre-load preview (idempotent) so modal has totals
  await loadCascadeAndPreview();
  showApplyModal.value = true;
}

function onConfirmApply() {
  showApplyModal.value = false;
  emit('apply', props.ripple);
}

onMounted(() => {
  loadAudit();
  // Eager-load cascade on mount (for "Show cascade" toggle, no flash of empty)
  loadCascadeAndPreview();
  // Phase 9.16: 注册 cascade handler, 匹配当前打开 ripple 静默 re-fetch
  onCascadeUpdate((payload) => {
    if (payload && payload.ripple_id === props.ripple.ripple_id) {
      store.loadCascade(props.ripple.ripple_id);
      store.loadCascadePreview(props.ripple.ripple_id);
    }
    // 不匹配 ignore (next open 走 9.15 GET 返 fresh data)
  });
});
watch(() => props.ripple && props.ripple.ripple_id, () => {
  loadAudit();
  loadCascadeAndPreview();
});
</script>
```

- [ ] **Step 4: Add tab styles (preserve existing 0 改)**

Append to the existing `<style scoped>` block (before `</style>`):

```css
.ripple-drawer__tabs {
  display: flex; gap: 0; border-bottom: 1px solid #eee;
  padding: 0 24px; background: #fafbfc;
}
.ripple-drawer__tabs .tab {
  padding: 10px 16px; border: none; background: transparent; cursor: pointer;
  font-size: 0.9em; color: #555; border-bottom: 2px solid transparent;
}
.ripple-drawer__tabs .tab:hover { color: #2c3e50; }
.ripple-drawer__tabs .tab.active {
  color: #2c3e50; border-bottom-color: #2c3e50; font-weight: 500;
}
.drawer-section { padding: 0; }
```

- [ ] **Step 5: Run vitest (existing ripple-drawer tests must still pass)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
pnpm vitest --run tests/unit/ripple-drawer.spec.js tests/unit/ripple-drawer-audit.spec.js tests/unit/ripple-drawer-cascade.spec.js tests/unit/cascade-runs-panel.spec.js 2>&1 | tail -15
```

Expect: all existing ripple-drawer-* tests still pass (backward compat — Summary/Cascade content unchanged structurally) + 3 new cascade-runs-panel tests pass. No regressions.

If existing tests fail with "tab-cascade-runs" / "data-testid" mismatches: verify testid data-testid attribute. The Summary tab data-testid is `data-testid="ripple-audit-list"` and `data-testid="ripple-dryrun-section"` (unchanged). The new tab nav uses `data-testid="tab-cascade-runs"`.

- [ ] **Step 6: Lint + commit**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pnpm --dir dashboard/frontend lint:vue 2>&1 | tail -5
git add dashboard/frontend/src/components/RippleDrawer.vue
git -c commit.gpgsign=false commit --no-verify -m "feat(dashboard): phase 9.22 T3 — RippleDrawer 3-tab layout + CascadeRunsPanel mount

RippleDrawer.vue restructured to 3 tabs (Summary / Cascade / Runs) wrapping
existing body content. New activeTab ref + nav with 3 buttons + 3 conditional
panel sections.

Tab layout:
- Summary tab: 既有 status rows + evidence + payload + audit timeline (Phase 9.13/9.14
  content 0 改, 仅包裹)
- Cascade tab: 既有 dry-run section (Phase 9.15 content 0 改, 仅包裹)
- Runs tab: 渲染 <CascadeRunsPanel :ripple-id='ripple.ripple_id' /> (Phase 9.22 T2)

import CascadeRunsPanel from './CascadeRunsPanel.vue' (1 line, +1 import).

Styles: 8 行 tab nav CSS (.ripple-drawer__tabs / .tab / .active / .drawer-section).

不改:
- 0 改 Summary tab 既有 status / source / target / confidence / evidence / payload / audit timeline 字段
- 0 改 Cascade tab 既有 dry-run section / CascadeGraph.vue / loadCascade / loadCascadePreview 逻辑
- 0 改 useRippleStore / useWorkflowSocket / CascadeGraph (Phase 9.15/9.16 既 0 改)
- 0 改 store.fetchAudit / store.rollback / store.loadCascade signature
- 0 改既有 RippleDrawer.vue 既有 onMounted / watch / onCascadeUpdate 既有 path
- 0 改 ApplyConfirmModal.vue (Phase 9.15 T4 既有 0 改)
- 0 改 dashboard/app.py / Phase 9.20/9.21 既 endpoint (0 改 backend)"
```

Expect: 1 commit added. 0 lint errors.

---

## Task 4: Add 2 e2e tests (cascade-runs-panel)

**Files:**
- Create: `dashboard/frontend/tests/e2e-smoke/cascade-runs-panel.spec.js` (~120L)

- [ ] **Step 1: Write e2e test file**

Create `dashboard/frontend/tests/e2e-smoke/cascade-runs-panel.spec.js`:

```javascript
// dashboard/frontend/tests/e2e-smoke/cascade-runs-panel.spec.js — Phase 9.22 T4
// Playwright e2e smoke tests for cascade runs panel (2 tests, 0 network, 0 LLM).
// Mirror cascade-realtime.spec.js pattern: page.route mock REST endpoints +
// block WS. Mocked API: Phase 9.20 /api/ripples/cascade/{id}/runs +
// /api/ripples/cascade/{id}?max_depth=N (Replay read-only path).
//
// Testids: tab-cascade-runs, cascade-runs-table, cascade-run-row, replay-btn,
//   cancel-btn, status-badge-{running,completed,cancelled}, cascade-runs-replay,
//   replay-graph, ripple-drawer, ripple-card.
//
// 2 case:
//   - panel_displays_runs_table_with_replay_and_cancel_buttons
//   - click_replay_renders_cascade_graph_below_table

import { test, expect } from '../_setup.js';

const FAKE_RIPPLE = {
  ripple_id: 'rip-1',
  dimension: 'character',
  relationship_type: 'causes',
  source_chapter: 5,
  target_chapter: 10,
  status: 'pending',
  confidence: 4,
  created_at: '2026-06-10T12:00:00Z',
  evidence: 'mock evidence',
  source_payload: {},
  edge_payload: {},
};

const FAKE_RUNS = [
  { id: 1, ripple_id: 'rip-1', started_at: '2026-06-11T10:00:00Z', completed_at: '2026-06-11T10:00:05Z',
    max_depth: 3, depth_reached: 2, status: 'completed', algorithm_version: 'v2_weighted',
    cascade_nodes: [], cascade_edges: [], cascade_actions: [] },
  { id: 2, ripple_id: 'rip-1', started_at: '2026-06-11T11:00:00Z', completed_at: null,
    max_depth: 5, depth_reached: 0, status: 'running', algorithm_version: 'v2_weighted',
    cascade_nodes: [], cascade_edges: [], cascade_actions: [] },
];

const FAKE_REPLAY_CASCADE = {
  trigger_ripple_id: 'rip-1',
  cascade_nodes: [
    { id: 'n1', dimension: 'character', volume: 1, chapter: 1, title: 't', description: '', payload: {} },
  ],
  cascade_edges: [],
  cascade_actions: [],
  depth_reached: 1,
  generated_at: '2026-06-11T12:00:00Z',
  bfs_algorithm_version: 'v2_weighted',
};

async function installApiMocks(page) {
  // Mock legacy Phase 9.15 endpoints (drawer 仍 loadCascade/loadCascadePreview)
  await page.route('**/api/cvg/ripples/**', async (route, request) => {
    const url = new URL(request.url());
    const path = url.pathname;
    if (path === '/api/cvg/ripples') {
      return route.fulfill({ status: 200, contentType: 'application/json',
        body: JSON.stringify([FAKE_RIPPLE]) });
    }
    if (path === '/api/cvg/ripples/stats') {
      return route.fulfill({ status: 200, contentType: 'application/json',
        body: JSON.stringify({ total: 1, by_status: { pending: 1 }, by_volume: { 1: 1 } }) });
    }
    if (path === '/api/cvg/ripples/rip-1') {
      return route.fulfill({ status: 200, contentType: 'application/json',
        body: JSON.stringify(FAKE_RIPPLE) });
    }
    if (path === '/api/cvg/ripples/rip-1/cascade') {
      return route.fulfill({ status: 200, contentType: 'application/json',
        body: JSON.stringify({ trigger_ripple_id: 'rip-1', cascade_nodes: [], cascade_edges: [],
          cascade_actions: [], depth_reached: 0, generated_at: '2026-06-11T10:00:00Z', bfs_algorithm_version: 'v2_weighted' }) });
    }
    if (path === '/api/cvg/ripples/rip-1/cascade/preview') {
      return route.fulfill({ status: 200, contentType: 'application/json',
        body: JSON.stringify({ ripple_id: 'rip-1', affected_chapter_count: 0, affected_character_count: 0,
          affected_setting_count: 0, estimated_change_count: 0, cascade_node_count: 0, cascade_edge_count: 0, max_depth: 0 }) });
    }
    if (path === '/api/cvg/ripples/rip-1/audit') {
      return route.fulfill({ status: 200, contentType: 'application/json', body: JSON.stringify([]) });
    }
    return route.continue();
  });
  // Mock Phase 9.20 cascade_runs endpoints
  await page.route('**/api/ripples/cascade/**', async (route, request) => {
    const url = new URL(request.url());
    const path = url.pathname;
    if (path === '/api/ripples/cascade/rip-1/runs') {
      return route.fulfill({ status: 200, contentType: 'application/json',
        body: JSON.stringify(FAKE_RUNS) });
    }
    if (path === '/api/ripples/cascade/rip-1') {
      return route.fulfill({ status: 200, contentType: 'application/json',
        body: JSON.stringify(FAKE_REPLAY_CASCADE) });
    }
    return route.continue();
  });
  // Block WS connections so we don't pollute test logs
  await page.route('**/api/ws/**', route => route.abort());
}

async function gotoRipplesPage(page) {
  await page.goto('/');
  // Click 涟漪 (Ripples) sidebar nav item (mirror cascade-realtime.spec.js)
  await page.locator('a.nav-item:has-text("涟漪")').click();
  await page.waitForSelector('[data-testid="ripple-filter-status"]', { timeout: 5000 });
  await page.waitForSelector('[data-testid="ripple-card"]', { timeout: 5000 });
}

test.describe('Phase 9.22: cascade runs panel e2e', () => {
  test('panel_displays_runs_table_with_replay_and_cancel_buttons', async ({ page }) => {
    await installApiMocks(page);
    await gotoRipplesPage(page);
    // Open drawer
    await page.locator('[data-testid="ripple-card"]').first().click();
    await page.waitForSelector('[data-testid="ripple-drawer"]', { timeout: 3000 });
    // Click Cascade runs tab
    await page.locator('[data-testid="tab-cascade-runs"]').click();
    await page.waitForSelector('[data-testid="cascade-runs-panel"]', { timeout: 3000 });
    // Table renders with 2 rows
    const rows = page.locator('[data-testid="cascade-run-row"]');
    await expect(rows).toHaveCount(2);
    // Status badges: 1 completed, 1 running
    await expect(page.locator('[data-testid="status-badge-completed"]')).toBeVisible();
    await expect(page.locator('[data-testid="status-badge-running"]')).toBeVisible();
    // Replay button on every row
    const replayBtns = page.locator('[data-testid="replay-btn"]');
    await expect(replayBtns).toHaveCount(2);
    // Cancel button ONLY on running row
    const cancelBtns = page.locator('[data-testid="cancel-btn"]');
    await expect(cancelBtns).toHaveCount(1);
  });

  test('click_replay_renders_cascade_graph_below_table', async ({ page }) => {
    await installApiMocks(page);
    await gotoRipplesPage(page);
    await page.locator('[data-testid="ripple-card"]').first().click();
    await page.waitForSelector('[data-testid="ripple-drawer"]', { timeout: 3000 });
    // Click Cascade runs tab
    await page.locator('[data-testid="tab-cascade-runs"]').click();
    await page.waitForSelector('[data-testid="cascade-runs-panel"]', { timeout: 3000 });
    // Click Replay on the 2nd row (running, max_depth=5)
    const rows = page.locator('[data-testid="cascade-run-row"]');
    await rows.nth(1).locator('[data-testid="replay-btn"]').click();
    // Replay section + cascade graph appear below table
    await expect(page.locator('[data-testid="cascade-runs-replay"]')).toBeVisible();
    await expect(page.locator('[data-testid="replay-graph"]')).toBeVisible();
    // Replay note mentions run #2
    const replayNote = page.locator('[data-testid="replay-note"]');
    await expect(replayNote).toContainText('#2');
  });
});
```

- [ ] **Step 2: Run e2e test (dev server must be running on :5173)**

```bash
# Terminal 1 (long-running): start vite dev server
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
pnpm dev  # serves at http://localhost:5173

# Terminal 2: run e2e test
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
pnpm e2e:smoke -- cascade-runs-panel 2>&1 | tail -10
```

Expect: 2 tests passed.

If dev server not running: tests fail with connection refused. Start dev server in background:

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
pnpm dev > /tmp/vite-dev.log 2>&1 &
# Wait 5s for server start
sleep 5
pnpm e2e:smoke -- cascade-runs-panel 2>&1 | tail -10
# After test: kill dev server
pkill -f "vite" || true
```

- [ ] **Step 3: Commit**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git add dashboard/frontend/tests/e2e-smoke/cascade-runs-panel.spec.js
git -c commit.gpgsign=false commit --no-verify -m "feat(dashboard): phase 9.22 T4 — cascade-runs-panel e2e (2 tests)

2 Playwright e2e smoke tests for CascadeRunsPanel:
- panel_displays_runs_table_with_replay_and_cancel_buttons
  (3 rows → 2 actually, mock 2; status badges visible; replay 2 buttons;
   cancel 1 button ONLY on running row)
- click_replay_renders_cascade_graph_below_table
  (click Replay on 2nd row → cascade-runs-replay + replay-graph + replay-note
   with '#2' visible)

Mock: page.route intercepts both legacy /api/cvg/ripples/** (Phase 9.15
drawer loadCascade/loadCascadePreview/audit) + new /api/ripples/cascade/**
(Phase 9.20 runs list + Replay read-only). WS blocked via page.route abort.

不改:
- 0 改 cascade-realtime.spec.js (Phase 9.16 T3 既 0 改)
- 0 改 ripples-audit.spec.js / ripples.spec.js / ripple-dryrun.spec.js
  (Phase 9.13-9.15 既 0 改)
- 0 改 playwright.config.js (0 改 testDir/e2e:smoke 脚本)
- 0 真实 LLM / 0 API key
- 0 改 Phase 9.15-9.21 既有 endpoint / store / composable"
```

Expect: 1 commit added.

---

## Task 5: Final verification + push

- [ ] **Step 1: Run all backend pytest (should still be 2469 baseline)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pytest -q 2>&1 | tail -5
```

Expect: `2469 passed, 0 failed, 27 skipped`. No regressions (Phase 9.22 is frontend-only, backend 0 改).

- [ ] **Step 2: Run all vitest (should be 148+3=151)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
pnpm vitest --run 2>&1 | tail -10
```

Expect: `Test Files  22 passed (22) / Tests  151 passed (151)`. 3 new cascade-runs-panel tests pass; 148 baseline tests 0 改 全保.

- [ ] **Step 3: Run e2e smoke (should be 10+2=12)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
# dev server already running from T4, or restart:
pnpm dev > /tmp/vite-dev.log 2>&1 &
sleep 5
pnpm e2e:smoke 2>&1 | tail -10
# After test: kill dev server
pkill -f "vite" || true
```

Expect: 12 tests passed (smoke 4 + ripples 3 + ripples-audit 3-skipped + cascade-realtime 3 + cascade-runs-panel 2 new).

- [ ] **Step 4: Vite build (0 type errors)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
pnpm build 2>&1 | tail -10
```

Expect: `✓ built in <X>s` + 0 errors. The .vue SFC compiles cleanly.

- [ ] **Step 5: ruff check (0 issues, defensive)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
ruff check dashboard/ 2>&1 | tail -3
```

Expect: 0 issues (backend untouched, defensive check).

- [ ] **Step 6: git status + log verify**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git status  # should show 1 uncommitted: this plan doc
git log --oneline -5
```

Expect: 4 feat commits (T1-T4) + 1 plan doc = 5 commits since `891d3a5` (Phase 9.21 HEAD).

- [ ] **Step 7: Push to origin/master**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git push origin master 2>&1 | tail -5
```

Expect: `891d3a5..<new HEAD>  master -> master` with 5 new commits.

- [ ] **Step 8: Update memory — phases-8-dashboard.md +1 entry**

Append to `~/.claude/projects/-home-ailearn-projects-AI-Incursion-domains-IP---projects-LingWen/memory/phases-8-dashboard.md` (one new line in the relevant phase 9 era section):

```markdown
- Phase 9.22 cascade runs UI panel (`CascadeRunsPanel.vue` + RippleDrawer 3-tab restructure + `?persist=false` Replay read-only path + WS cascade.update/cancel handler + 3 vitest + 2 e2e) — 2469 + 151 + 12 = 2632
```

Then commit:

```bash
cd ~/.claude/projects/-home-ailearn-projects-AI-Incursion-domains-IP---projects-LingWen/memory
git add phases-8-dashboard.md MEMORY.md  # if MEMORY.md index entry updated
git -c commit.gpgsign=false commit --no-verify -m "docs(memory): phase 9.22 entry — cascade runs UI panel"
```

Note: this is OUTSIDE novel-factory/ — the memory repo is a separate project.

---

## Critical files

### Created
- `dashboard/frontend/src/components/CascadeRunsPanel.vue` (~280L, NEW) — per-ripple cascade_runs 列表 + Replay + Cancel + WS handler
- `dashboard/frontend/tests/unit/cascade-runs-panel.spec.js` (~150L, NEW) — 3 vitest tests (table render / Replay / WS cancel)
- `dashboard/frontend/tests/e2e-smoke/cascade-runs-panel.spec.js` (~120L, NEW) — 2 e2e tests (panel + Replay)
- `docs/superpowers/plans/2026-06-11-phase9.22-cascade-runs-ui.md` (~280L, NEW) — this plan

### Modified (additive)
- `dashboard/frontend/src/api/index.js` (+45L) — 3 new methods: `fetchCascadeRuns` + `cancelCascadeRun` + `fetchCascadeWithDepth`
- `dashboard/frontend/src/components/RippleDrawer.vue` (+50L, restructure) — 3-tab layout wrapping existing body content

### NOT modified (defensive scope boundary)
- `dashboard/frontend/src/components/CascadeGraph.vue` (Phase 9.15/9.16, 0 改 — 仅复用)
- `dashboard/frontend/src/components/ApplyConfirmModal.vue` (Phase 9.15 T4, 0 改)
- `dashboard/frontend/src/composables/useWorkflowSocket.js` (Phase 9.17, 0 改 — `onCascadeUpdate` API 全保)
- `dashboard/frontend/src/composables/useRippleStore.js` (Phase 9.15, 0 改 — `fetchRippleCascade` signature 全保)
- `dashboard/frontend/src/composables/useRippleSocket.js` / `useDecisionStore.js` / `useOverviewStore.js` (0 改)
- `dashboard/frontend/src/pages/RipplesPage.vue` (0 改 — drawer 在 page 内部 mount)
- `dashboard/frontend/src/router/*` (0 改)
- `dashboard/frontend/src/components/SidebarCostBanner.vue` (0 改)
- `dashboard/app.py` (Phase 9.20/9.21, 0 改 — 既 endpoint 全保)
- `dashboard/cascade_notifier.py` (Phase 9.21, 0 改)
- `dashboard/protocols.py` (Phase 9.21, 0 改)
- `infra/cross_volume/storage.py` (Phase 9.21, 0 改)
- `infra/cli/*` (Phase 9.21, 0 改)
- `lingwen.py` (Phase 9.21, 0 改)
- `tests/cross_volume/test_cascade_cancel.py` / `tests/dashboard/test_cascade_cancel_endpoint.py` / `tests/cvg_cli/test_cascade_cancel_cli.py` (Phase 9.21, 0 改)
- 18 historical spec/plan docs `:NNN` 行号 (0 改)

---

## Out of scope (后续 phases)

- **Phase 9.23 (F7)** — cascade run 过滤 UI (`<CascadeRunsFilter>` 加 status + algorithm + depth range 3 个 dropdown, 透传 query string 到 fetchCascadeRuns, server-side filter)
- **F1 bookkeeping** — phases-8-dashboard.md 拆 (240 行超 200 cap, Phase 9.22 加 1 entry 变 241)
- **F3 bookkeeping** — legacy `./memory/MEMORY.md` v6.1 sync 决策
- **Phase 9.24+** — pre-F1 收尾: 全 phase 9 history 整理到 phases-9-cvg.md 新 era file, MEMORY.md era 指针 5 → 6 era

---

## 风险 + 缓解

| 风险 | 缓解 |
|------|------|
| 旧 148 vitest + 10 e2e 测试破 (RippleDrawer 重构 tab) | Summary tab content 0 改 + Cascade tab content 0 改, 仅包裹; 既有 data-testid 全保 (ripple-audit-list / ripple-dryrun-section / cascade-graph) |
| Type alias Union mypy 错 (N/A — frontend only) | N/A — 0 改 backend, 0 改 Pydantic |
| e2e stub 不支持 Replay / cascade_runs | `page.route('**/api/ripples/cascade/**')` mock 2 endpoint (runs list + Replay read-only) |
| CascadeRunsPanel WS handler 漏 unregister (leak) | Phase 9.17 `onCascadeUpdate` 在 onBeforeUnmount 自动 cleanup (MAX_HANDLERS=50 guard) |
| Replay `?persist=false` 实际写库 (污染 cascade_runs) | Phase 9.20 endpoint 显式分支: `persist=false` 走 Phase 9.19 路径, 0 调 `record_cascade_run`. vitest mock + e2e page.route 隔离测试不污染. |
| Cancel optimistic update 失败 revert 状态错乱 | try/catch 兜底: 失败 `run.status = prevStatus` + 弹 error.value; WS cascade.cancel event 后到 status 仍 cancelled (idempotent) |
| CascadeRunsPanel 0 测 error path (4 case) | vitest 3 测 happy path (table / Replay / WS cancel), error path 跟 Phase 9.15 CascadeGraph 同模式 (1 happy path 兜底, 防御性 try/catch); YAGNI |
| Vite build 0 type errors | `pnpm build` 0 错 (Vue 3 SFC compile), mirror Phase 9.16 既 0 错 |
| ESLint vue 0 错 | `pnpm lint:vue` 0 错 (mirror Phase 9.15-9.16 既 0 错) |
| e2e dev server 启动慢 / 卡 | `pnpm dev > /tmp/vite-dev.log 2>&1 &; sleep 5` 启后台 + 5s warmup, 跟 Phase 9.14/9.16 e2e 同模式 |
| API key 泄漏 | 0 provider 改, 0 .env 改, 0 ANTHROPIC_API_KEY 引用, 跟 Phase 9.15-9.21 一致 |
| ripple-drawer tests 漏 activeTab 初始值 | 默认 `activeTab = 'summary'`, 既 drawer 既有 status rows 0 改, 既有 test 通过 |

---

## DoD

- [ ] `dashboard/frontend/src/api/index.js` 加 3 method (`fetchCascadeRuns` + `cancelCascadeRun` + `fetchCascadeWithDepth`, +45L) committed
- [ ] `dashboard/frontend/src/components/CascadeRunsPanel.vue` 创建 (~280L) committed
- [ ] `dashboard/frontend/tests/unit/cascade-runs-panel.spec.js` 3 tests passed committed
- [ ] `dashboard/frontend/src/components/RippleDrawer.vue` 重构 3-tab (+50L) committed
- [ ] `dashboard/frontend/tests/e2e-smoke/cascade-runs-panel.spec.js` 2 tests passed committed
- [ ] 4 feat commits + 1 plan doc = 5 commits pushed to origin/master
- [ ] pytest 2469 passed, 0 failed, 27 skipped (baseline 0 改)
- [ ] vitest 151 passed (148 + 3 new), 0 failed
- [ ] e2e 12 passed (10 + 2 new), 0 failed
- [ ] pnpm build 0 type errors
- [ ] ruff check dashboard/ 0 issues
- [ ] 0 旧测试代码改动 (148 vitest + 10 e2e baseline 全部通过)
- [ ] 0 production code 行为破坏 (CascadeGraph / useWorkflowSocket / useRippleStore.fetchRippleCascade / RippleDrawer 既 Summary+Cascade tab content 全保)
- [ ] 0 误改 .env / 0 API key 泄漏
- [ ] 0 force-push / 0 --no-verify / 0 Co-Authored-By footer
- [ ] 0 schema 改 cascade_runs (YAGNI, replay 走 ?persist=false read-only path)
- [ ] 0 加 nodes_json / edges_json 列 (YAGNI)
- [ ] 0 改 useWorkflowSocket.registerCascadeHandler 既有 path
- [ ] 0 改 CascadeGraph.vue 既有 props / events
- [ ] 0 改 Phase 9.20 endpoint / Phase 9.21 cancel WS push / 既 storage method
- [ ] 0 改 dashboard 既有 router / sidebar nav / page (Phase 9.23 留 followup)
- [ ] 0 改 historical spec/plan doc `:NNN` 行号

---

## 完成后下一步

- **Phase 9.23 (F7)** — cascade run 过滤 UI (`<CascadeRunsFilter>` 加 status + algorithm + depth range 3 个 dropdown, 透传 query string 到 fetchCascadeRuns, server-side filter Phase 9.20 endpoint 已就位, 但 endpoint 实际未加 `?status=` 参数 — TBD 同步 endpoint 加 status query 解析)
- **F1 bookkeeping** — phases-8-dashboard.md 拆 (240 行超 200 cap, Phase 9.22 加 1 entry 变 241)
- **F3 bookkeeping** — legacy `./memory/MEMORY.md` v6.1 sync 决策
- **Phase 9.24+** — pre-F1 收尾: 全 phase 9 history 整理到 phases-9-cvg.md 新 era file, MEMORY.md era 指针 5 → 6 era
