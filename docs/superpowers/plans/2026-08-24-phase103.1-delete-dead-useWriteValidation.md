# Phase 103.1 — Delete Dead `useWriteValidation.ts` Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Delete `src/composables/useCreatorWrite/useWriteValidation.ts` (139 lines, zero runtime consumers) and prune its 2 JSDoc reference lines so knip `Unused files` returns to 0.

**Architecture:** Surgical 3-file change — 1 `git rm` + 2 single-line JSDoc edits. Single atomic commit.

**Tech Stack:** Vue 3, TypeScript, JavaScript, knip 6.32.2, pnpm.

---

## File Structure

**Files deleted (1):**
- `apps/dashboard/src/composables/useCreatorWrite/useWriteValidation.ts`

**Files modified (2):**
- `apps/dashboard/src/composables/useCreatorWrite.js` — remove 1 line (line 9)
- `apps/dashboard/src/composables/useCreatorWrite/index.ts` — remove 1 line (line 6)

---

## Task 1: Pre-flight — verify file exists + capture baseline

**Files:**
- Read-only check.

- [ ] **Step 1.1: Confirm git state**

```bash
cd /home/ailearn/projects/LingWen && git log --oneline -3 && git status
```

Expected: HEAD on `acc3f14a docs(spec): Phase 103.1 — ...`. Working tree clean.

- [ ] **Step 1.2: Confirm useWriteValidation.ts exists + capture line count**

```bash
cd /home/ailearn/projects/LingWen && test -f apps/dashboard/src/composables/useCreatorWrite/useWriteValidation.ts && wc -l apps/dashboard/src/composables/useCreatorWrite/useWriteValidation.ts
```

Expected: line count (likely 139). If MISSING, STOP.

- [ ] **Step 1.3: View JSDoc lines to be edited**

```bash
cd /home/ailearn/projects/LingWen && sed -n '9p' apps/dashboard/src/composables/useCreatorWrite.js
```
Expected: ` * - useWriteValidation (写作验证：P0 复检/逻辑检查点击/偏离点击 — 类型骨架占位)`

```bash
cd /home/ailearn/projects/LingWen && sed -n '6p' apps/dashboard/src/composables/useCreatorWrite/index.ts
```
Expected: ` * - useWriteValidation (写作验证：P0 复检/逻辑检查点击/章节跳转/偏离点击)`

If exact text differs (e.g., line numbers shifted), read 5 lines around the expected lines and adapt the Edit's old_string accordingly.

- [ ] **Step 1.4: Confirm zero runtime consumers**

```bash
cd /home/ailearn/projects/LingWen && grep -rln "import.*useWriteValidation\|require.*useWriteValidation\|from.*useWriteValidation\|writeValidation\.ts" apps/dashboard/src apps/dashboard/tests --include="*.js" --include="*.ts" --include="*.vue" 2>/dev/null
```

Expected: zero matches (the 2 JSDoc references don't count as they don't import). If any match exists, STOP.

- [ ] **Step 1.5: Capture knip + test baseline**

```bash
cd /home/ailearn/projects/LingWen/apps/dashboard && pnpm exec knip --reporter=compact 2>&1 | grep "^Unused files"
```
Expected: `Unused files (1)` (just `useWriteValidation.ts`).

```bash
cd /home/ailearn/projects/LingWen/apps/dashboard && pnpm exec vitest run 2>&1 | tail -3
```
Expected: `1545 passed`. If red, STOP.

---

## Task 2: Delete `useWriteValidation.ts` + 2 JSDoc edits

**Files:**
- Modify: 1 file deleted, 2 files edited

- [ ] **Step 2.1: Delete the file via `git rm`**

```bash
cd /home/ailearn/projects/LingWen && git rm apps/dashboard/src/composables/useCreatorWrite/useWriteValidation.ts
```

Expected: `rm 'apps/dashboard/src/composables/useCreatorWrite/useWriteValidation.ts'`.

- [ ] **Step 2.2: Edit `useCreatorWrite.js` — remove JSDoc line 9**

Use Edit tool. The exact text to remove:

- **Find (old_string)** — full line including leading spaces (4 spaces of indent + content):
```
 * - useWriteValidation (写作验证：P0 复检/逻辑检查点击/偏离点击 — 类型骨架占位)
```

- **Replace (new_string)**:
```
```
```

(empty — deletes the line)

**IMPORTANT**: The Edit tool needs both old_string AND new_string. If new_string is empty, the line is removed. The surrounding lines stay.

If `old_string` doesn't match exactly (e.g., different text in the doc bullet), use `sed -n '9p'` to verify exact text first, then adapt.

- [ ] **Step 2.3: Edit `useCreatorWrite/index.ts` — remove JSDoc line 6**

Use Edit tool.

- **Find (old_string)** — exact text:
```
 * - useWriteValidation (写作验证：P0 复检/逻辑检查点击/章节跳转/偏离点击)
```

- **Replace (new_string)**:
```
```
```

(empty — deletes the line)

- [ ] **Step 2.4: Verify all 3 changes**

```bash
cd /home/ailearn/projects/LingWen && git status --short
```
Expected: 3 changes — 1 delete (`D useWriteValidation.ts`) + 2 modified JSDoc files.

```bash
cd /home/ailearn/projects/LingWen && grep -rn "useWriteValidation" apps/dashboard/src apps/dashboard/tests --include="*.js" --include="*.ts" --include="*.vue" 2>/dev/null
```
Expected: zero matches. If any remain, STOP.

---

## Task 3: Verify post-change state

**Files:**
- Read-only verification.

- [ ] **Step 3.1: knip `Unused files` is now 0**

```bash
cd /home/ailearn/projects/LingWen/apps/dashboard && pnpm exec knip --reporter=compact 2>&1 | grep "^Unused files"
```

Expected: `Unused files (0)`. If still showing, STOP.

- [ ] **Step 3.2: knip other categories unchanged**

```bash
cd /home/ailearn/projects/LingWen/apps/dashboard && pnpm exec knip --reporter=compact 2>&1 | grep -E "^(Unused|Unlisted|Duplicate)"
```

Expected: same counts as before this phase (no Unused exports, no Unlisted binaries regressions, etc.).

- [ ] **Step 3.3: Tests still pass**

```bash
cd /home/ailearn/projects/LingWen/apps/dashboard && pnpm exec vitest run 2>&1 | tail -3
```

Expected: `1545 passed`.

- [ ] **Step 3.4: Build succeeds**

```bash
cd /home/ailearn/projects/LingWen/apps/dashboard && pnpm run build 2>&1 | tail -3
```

Expected: build OK.

- [ ] **Step 3.5: vue-tsc clean**

```bash
cd /home/ailearn/projects/LingWen/apps/dashboard && pnpm exec vue-tsc --noEmit 2>&1 | tail -3
```

Expected: 0 errors.

- [ ] **Step 3.6: Lint clean**

```bash
cd /home/ailearn/projects/LingWen/apps/dashboard && pnpm run lint:all 2>&1 | tail -3
```

Expected: no errors.

- [ ] **Step 3.7: Diff stat — 3 file ops**

```bash
cd /home/ailearn/projects/LingWen && git diff --stat
```

Expected: 3 files (1 delete + 2 modified). Net ~-140 deletions (file delete is ~139 lines + 2 JSDoc line removals).

---

## Task 4: Commit + push

**Files:**
- Commit: 1 delete + 2 modifications.

- [ ] **Step 4.1: Stage all changes**

```bash
cd /home/ailearn/projects/LingWen && git add -A && git status
```

Expected: 3 files in staging (1 delete + 2 modifications). Nothing else.

- [ ] **Step 4.2: Commit with the spec-defined message**

```bash
cd /home/ailearn/projects/LingWen && git commit -m "chore(cleanup): delete dead useWriteValidation.ts (Phase 103.1)" -m "Phase 103.1 — resolve Phase 103 code-quality review LOW follow-up:

Phase 103 stripped re-exports of useWriteValidation from
useCreatorWrite/index.ts and composables/index.ts, leaving the source
file with zero runtime consumers. The only remaining refs were 2
JSDoc lines mentioning the function name.

- Delete apps/dashboard/src/composables/useCreatorWrite/useWriteValidation.ts
  (139 lines, exports useWriteValidation + WriteValidationDeps +
  WriteValidationReturn, zero runtime consumers)
- Remove JSDoc line 9 in useCreatorWrite.js (the
  'useWriteValidation (写作验证...)' doc bullet)
- Remove JSDoc line 6 in useCreatorWrite/index.ts (the
  'useWriteValidation (写作验证...)' doc bullet)

Effect: knip \`Unused files\` count drops 1 → 0.

测试基线不变: 1545 PASS, 0 type errors, 0 build errors." 2>&1 | tail -3
```

Expected: 1 commit created.

- [ ] **Step 4.3: Push to origin**

```bash
cd /home/ailearn/projects/LingWen && git push origin master 2>&1 | tail -3
```

Expected: push succeeds. `origin/master` advances by 1 commit.

- [ ] **Step 4.4: Final state check**

```bash
cd /home/ailearn/projects/LingWen && git log --oneline -3 && git status
```

Expected: 3 most recent commits include the Phase 103.1 commit. Working tree clean.

---

## Success Criteria

- [ ] `useWriteValidation.ts` deleted
- [ ] 2 JSDoc reference lines removed
- [ ] `pnpm exec knip` reports `Unused files (0)`
- [ ] 1545 tests pass
- [ ] Build OK
- [ ] vue-tsc clean
- [ ] Lint clean
- [ ] Single atomic commit on master
- [ ] Pushed to origin/master

---

## Rollback

If anything regresses:
```bash
cd /home/ailearn/projects/LingWen && git revert HEAD --no-edit && git push origin master
```

Reverts 1 commit. No data loss.

---

## Self-Review Notes

**Spec coverage**:
- §4.1 Change Set → Task 2 (delete + 2 JSDoc edits) ✅
- §4.3 Verification → Task 3 ✅
- §7 Commit Strategy → Task 4 ✅
- §9 Success Criteria → top-level checklist ✅

**Placeholder scan**: No "TBD"/"TODO" present.

**Type consistency**: No new types/functions. Only deletion + comment removal.

**Edge cases handled**:
- Task 1.1 git state — catch pre-existing breakage
- Task 1.2 file existence — catch accidental prior deletion
- Task 1.3 JSDoc exact text verification — catch text drift
- Task 1.4 zero runtime consumers verification — catch unexpected consumer
- Task 1.5 knip + tests baseline — catch pre-existing drift
- Task 2.4 grep verification after edits — catch missed JSDoc line
- Task 3.1 knip 0 confirmation — primary success criterion
- Rollback section