# Phase 15.0 Tasks — P3 一致性 (大重构)

> **目的**: 把 `plan.md` 的 5 个 refactor sub-tasks (T1-T5) + T6 verify/push 拆成可逐项追踪的 checklist。配套 `specs/` 设计 + `plans/-plan.md` 实施步骤。
> **进度总览**: 5 refactor (T1-T5) + T6 收口。Baseline 3023 pytest + 1029 vitest → Target 3023+ pytest + 1029 vitest (本 phase 0 改 baseline 测试, 只新增 0-12 API 验证)。

---

## 总览看板

| Task | 主题 | Status | Owner | 新增 pytest | 新增 vitest | 风险 |
|------|------|--------|-------|-------------|-------------|------|
| T1 | REFACTOR dashboard/app.py 拆分 (6265→多文件 <800) | pending | 主公 | 0-4 | 0 | R1 / R2 |
| T2 | REFACTOR SQLite 收敛 (15+ files → 1 套 singleton) | pending | 主公 | 0-2 | 0 | R3 / R4 |
| T3 | REFACTOR master_controller 拆分 (1420 → <600 + 5 ops) | pending | 主公 | 0 | 0 | R5 / R6 |
| T4 | REFACTOR CLI 收敛 (dual layout → single) | pending | 主公 | 0 | 0 | R7 |
| T5 | REFACTOR HTTPException 中央 handler (227 → APIError) | pending | 主公 | 0-6 | 0 | R8 / R9 |
| T6 | Verify + push + HANDOFF | pending | 主公 | 0 | 0 | R10 / R11 |
| **合计** | **5 项 P3 一致性** | — | — | **+0-12** | **0** | — |

---

## T1 — REFACTOR dashboard/app.py 拆分

**目标**: `dashboard/app.py` 6265 行 → `models/` (8) + `routes/` (9) + `helpers/` (6-8) + `errors.py` + `app.py` (<400)。

### 子任务清单

- [ ] **T1.1** 拆 Pydantic models → `dashboard/models/`
  - deps: 无
  - DoD: 8 个 model files 各 < 400 行 + `app.py` 行 85-1973 全删
  - 关键: 0 改 model 字段 / 验证
- [ ] **T1.2** 新 `dashboard/errors.py` (APIError 基类 + 5 子类)
  - deps: 无
  - DoD: 5 子类就绪, 不立即替换调用点
  - 关键: 子类硬编码 status_code + code
- [ ] **T1.3** 抽 helpers → `dashboard/helpers/` (6-8 modules)
  - deps: 无
  - DoD: `_ripple_to_list_item` 等 30+ helpers 搬到位
  - 关键: helpers 每 < 200 行
- [ ] **T1.4** 拆 routes → `dashboard/routes/` (9 files)
  - deps: T1.1 / T1.2 / T1.3
  - DoD: 9 个 route files 用 `APIRouter` 注册
  - 关键: 0 改 route path / response_model
- [ ] **T1.5** 收口 `app.py` 到 < 400 行
  - deps: T1.4
  - DoD: `create_app()` factory + middleware + lifespan + main entry + `app = create_app()` 顶层
  - 关键: `app` 同名 instance 供 TestClient 导入
- [ ] **T1.6** pytest dashboard 0 回归
  - deps: T1.5
  - DoD: `pytest tests/dashboard/ -q --timeout=120` (excluding creator_endpoints) → 239+ passed
- [ ] **T1.7** ruff + 3-4 commit
  - deps: T1.6
  - DoD: `ruff check dashboard/` 0 issues
  - commits:
    - `refactor(dashboard): phase 15.0 T1.1 — 拆 Pydantic models 到 dashboard/models/`
    - `refactor(dashboard): phase 15.0 T1.2-3 — errors.py + helpers/ 拆分`
    - `refactor(dashboard): phase 15.0 T1.4 — 拆 routes 到 dashboard/routes/`
    - `refactor(dashboard): phase 15.0 T1.5 — app.py 6265 → < 400 行`
  - 风险：R1 (routes import 错位) / R2 (model 循环 import)

### 验证命令

```bash
cd novel-factory
wc -l dashboard/app.py                              # < 800
ls dashboard/models/ dashboard/routes/ dashboard/helpers/
pytest tests/dashboard/ -q --timeout=120 --no-cov \
  --deselect tests/dashboard/test_creator_endpoints.py  # 0 回归
ruff check dashboard/                                # 0 issues
```

---

## T2 — REFACTOR SQLite 收敛

**目标**: 15+ files (5 套 backend) → 1 套 `infra/persistence/db.py` singleton + 5 schema SQL。db_path 保留原值, 只换 factory。

### 子任务清单

- [ ] **T2.1** 新 `infra/persistence/` 包 (db.py + schemas/)
  - deps: 无
  - DoD: `DatabasePool.get(path)` 工厂 + 5 个 schema SQL
  - 关键: 单 process 内 1 path = 1 connection
- [ ] **T2.2** schema 注册 + `init_db(path, schema_name)` 工厂
  - deps: T2.1
  - DoD: 启动时调一次, 单例负责迁移
- [ ] **T2.3a** state backend 切到 DatabasePool
  - deps: T2.2
  - DoD: `infra/state/database.py` + `state_manager.py` 用 `DatabasePool.get()`
  - 关键: `state.db` db_path 不变
- [ ] **T2.3b** cvg backend 切到 DatabasePool
  - deps: T2.3a
  - DoD: `infra/cross_volume/storage.py` 用 `DatabasePool.get()`
  - 关键: `ripple.db` db_path 不变
- [ ] **T2.3c** cost backend 切到 DatabasePool
  - deps: T2.3b
  - DoD: `infra/agent_system/cost_persistence.py` + `budget_persistence.py` 用 `DatabasePool.get()`
  - 关键: `cost_tracker.db` db_path 不变
- [ ] **T2.3d** workflow backend 切到 DatabasePool
  - deps: T2.3c
  - DoD: `infra/tools/workflow/lib/{db,state,tasks,migration,checkpoints}.py` 用 `DatabasePool.get()`
  - 关键: `workflow.db` db_path 不变
- [ ] **T2.3e** reading backend 切到 DatabasePool
  - deps: T2.3d
  - DoD: `infra/reading_power/db.py` 用 `DatabasePool.get()`
  - 关键: `reading_power.db` db_path 不变
- [ ] **T2.4** 旧 backend 入口加 DeprecationWarning
  - deps: T2.3e
  - DoD: `infra/state/backends/sqlite.py` + `infra/tools/migrate_to_sqlite.py` 加 warning, **不删**
- [ ] **T2.5** pytest 全集 0 回归
  - deps: T2.4
  - DoD: `pytest tests/ -q --timeout=120` (baseline exclusions) → 3023 passed
- [ ] **T2.6** ruff + 5-7 commit
  - deps: T2.5
  - DoD: `ruff check infra/persistence/ infra/state/ infra/cross_volume/ infra/agent_system/cost_persistence.py infra/tools/workflow/lib/ infra/reading_power/` 0 issues
  - 风险：R3 (db_path 冲突) / R4 (旧 caller 找不到)

### 验证命令

```bash
cd novel-factory
ls infra/persistence/ infra/persistence/schemas/
pytest tests/ -q --timeout=120 --no-cov \
  --ignore=tests/agent_system/test_e2e_workflow.py \
  --ignore=tests/tools/test_enhancement_tools.py \
  --ignore=tests/dashboard/test_creator_endpoints.py  # 3023 passed
ruff check infra/                                   # 0 issues
```

---

## T3 — REFACTOR master_controller 拆分

**目标**: `master_controller.py` 1420 行 → `< 600 主文件` + `operations/` (5 files) + `orchestrator.py` + `role_manager.py` + `decision_queue.py`。

### 子任务清单

- [ ] **T3.1** 新 `infra/agent_system/operations/` 包
  - deps: 无
  - DoD: 5 ops modules (`chapter.py` / `audit.py` / `polish.py` / `outline.py` + `__init__.py`)
  - 关键: `_impl_*` 用 `@staticmethod` 接受显式 ctx
- [ ] **T3.2** 新 `infra/agent_system/orchestrator.py`
  - deps: 无
  - DoD: `run_workflow` / `advance_step` / `dispatch_task` 抽离
- [ ] **T3.3** 新 `infra/agent_system/role_manager.py`
  - deps: 无
  - DoD: `switch_agent_role` / `get_agent_role_config` 抽离
- [ ] **T3.3.5** 新 `infra/agent_system/decision_queue.py`
  - deps: 无
  - DoD: `list_pending_decisions` / `resolve_decision` 抽离
- [ ] **T3.4** 收口 `master_controller.py` 到 < 600 行
  - deps: T3.1-T3.3.5
  - DoD: 公开 API 0 改 (1-line forward 委托)
  - 关键: `mc.write_chapter(...)` / `mc.run_workflow(...)` 签名不变
- [ ] **T3.5** pytest agent_system 0 回归
  - deps: T3.4
  - DoD: `pytest tests/agent_system/ -q --timeout=60` (excluding e2e_workflow) → 100+ passed
- [ ] **T3.6** ruff + 3-4 commit
  - deps: T3.5
  - DoD: `ruff check infra/agent_system/` 0 issues
  - 风险：R5 (委托链长) / R6 (LLM 路径搬家错位)

### 验证命令

```bash
cd novel-factory
wc -l infra/agent_system/master_controller.py      # < 600
ls infra/agent_system/operations/ infra/agent_system/{orchestrator,role_manager,decision_queue}.py
pytest tests/agent_system/ -q --timeout=60 --no-cov \
  --ignore=tests/agent_system/test_e2e_workflow.py  # 0 回归
ruff check infra/agent_system/                      # 0 issues
```

---

## T4 — REFACTOR CLI 收敛

**目标**: dual layout (`commands.py` + `commands/`) → single `commands/` + `lib/` 合并 helpers。

### 子任务清单

- [ ] **T4.1** 检查 `commands.py` 与 `commands/` 重复
  - deps: 无
  - DoD: 决策 (删 / 保留 deprecation warning)
- [ ] **T4.2** 合并 helpers → `infra/cli/lib/`
  - deps: 无
  - DoD: `parsers/` + `options.py` + `output.py` + `path_utils.py` + `project_range.py` + `range_parser.py` → `lib/`
- [ ] **T4.3** `infra/cli/__init__.py` 暴露 `register_commands(app)`
  - deps: T4.2
  - DoD: 单一入口注册 17 commands
- [ ] **T4.4** e2e CLI parity 验证 17 cmd `--help`
  - deps: T4.3
  - DoD: 17 cmd 全部能 import + register
- [ ] **T4.5** ruff + commit
  - deps: T4.4
  - DoD: `ruff check infra/cli/` 0 issues
  - 风险：R7 (`lingwen.py` / scripts 路径断)

### 验证命令

```bash
cd novel-factory
ls infra/cli/                                       # 无 commands.py, 有 lib/
for cmd in status check doctor verify repair polish cascade anti_trope backfill init_project \
           llm_analyze reading_power ripple_audit ripple_reset ripple_rollback ripple_scan \
           story_contract; do
  python lingwen.py $cmd --help 2>&1 | head -2       # 17 cmd 全部 OK
done
ruff check infra/cli/                                # 0 issues
```

---

## T5 — REFACTOR HTTPException 中央 handler

**目标**: 227 个 inline `raise HTTPException` → `APIError` 子类 + `@app.exception_handler(APIError)` 中央 handler + 保留 `detail` alias。

### 子任务清单

- [ ] **T5.1** 注册 `@app.exception_handler(APIError)`
  - deps: T1.2
  - DoD: 中央 handler 返回 `{code, message, detail}` 三字段 envelope
- [ ] **T5.2** 批量替换 227 个 `raise HTTPException`
  - deps: T5.1
  - DoD: 10 类 pattern 替换完 (404/400/409/401/429/其他 → 5+1 子类)
  - 关键: 0 改 status_code
- [ ] **T5.3** RateLimitExceeded handler 改用 envelope
  - deps: T5.2
  - DoD: 原 line 2562 handler 用 `RateLimitError`
- [ ] **T5.4** pytest dashboard 0 回归
  - deps: T5.3
  - DoD: `pytest tests/dashboard/ -q --timeout=120` → 239+ passed
- [ ] **T5.5** ruff + commit
  - deps: T5.4
  - DoD: `ruff check dashboard/` 0 issues
  - 风险：R8 (response body 改 → 7+ test 失败) / R9 (status_code 漂移)

### 验证命令

```bash
cd novel-factory
grep -rn "raise HTTPException" dashboard/ | wc -l   # 0 (除 RateLimitExceeded 1 处)
pytest tests/dashboard/ -q --timeout=120 --no-cov \
  --deselect tests/dashboard/test_creator_endpoints.py  # 239+ passed
ruff check dashboard/                                # 0 issues
```

---

## T6 — Verify + push + HANDOFF

### 子任务清单

- [ ] **T6.1** 写 `scripts/verify-p3-consistency.sh` (5 检查项)
  - deps: T1-T5
  - DoD: 5/5 PASS
- [ ] **T6.2** CI integration (`.github/workflows/test.yml`)
  - deps: T6.1
  - DoD: "Verify Phase 15.0 P3 consistency" 步加在 Phase 14.0 verify-p2 步下面
- [ ] **T6.3** ruff + commit
  - deps: T6.2
  - commit: `chore(ci): phase 15.0 T6 — wire P3 consistency verify`
- [ ] **T6.4** 全量 pytest 0 回归
  - deps: T6.3
  - DoD: 3023 passed
- [ ] **T6.5** vitest 1029 维持
  - deps: T6.4
  - DoD: 1029 passed
- [ ] **T6.6** e2e-live parity 95 维持
  - deps: T6.5
  - DoD: 95 passed (CI parity)
- [ ] **T6.7** push + HANDOFF §0.1 加 Phase 15.0 entry
  - deps: T6.6
  - commit: `docs(handoff): Phase 15.0 P3 一致性（T1-T5）条目`
  - DoD: 11-15 commits 总 push + HANDOFF 更新

### 验证命令

```bash
cd novel-factory
bash scripts/verify-p3-consistency.sh                # 5/5 PASS
pytest tests/ -q --timeout=120 --no-cov \
  --ignore=tests/agent_system/test_e2e_workflow.py \
  --ignore=tests/tools/test_enhancement_tools.py \
  --ignore=tests/dashboard/test_creator_endpoints.py  # 3023 passed
cd dashboard/frontend && pnpm vitest run              # 1029 passed
ruff check .                                          # All checks passed
bash scripts/verify-e2e-live-ci.sh 2>&1 | tail -3     # 95 passed
```

---

## 进度跟踪

| Phase | 描述 | Status |
|-------|------|--------|
| Phase 1 | 文档准备 (spec/plan/tasks) | ✅ spec ✅ plan ✅ tasks |
| Phase 2 | pytest baseline 锁定 | pending |
| Phase 3 | T1 dashboard/app.py 拆分 | pending |
| Phase 4 | T2 SQLite 收敛 | pending |
| Phase 5 | T3 master_controller 拆分 | pending |
| Phase 6 | T4 CLI 收敛 | pending |
| Phase 7 | T5 HTTPException handler | pending |
| Phase 8 | T6 verify + CI | pending |
| Phase 9 | Final + HANDOFF | pending |

---
*Status: DRAFT — implementation pending after 主公 approve spec + plan + tasks.*