# Phase 8.14 — hasCost Gate Tier 维度补全 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 `WorkflowStatus.vue` `hasCost` gate 1 行 OR, 让 backend 返 `cost_by_tier` 有值但 `cost_by_scenario` 空时整 cost section 仍 visible, 配套 1 ceremonial e2e 契约 spec.

**Architecture:** 单一 Vue 3 Composition API computed 改 additive OR (scenarioHas || tierHas), 0 改 backend / 0 改 CostBarChart (内部 hasData 已正确). Test 走 ceremonial Playwright contract pattern (跟 Phase 7.6/8.7/8.8/8.11/8.13/8.15 7 specs 同模式, runner 未装, 留作契约文档).

**Tech Stack:** Vue 3 (Composition API) + Playwright (ceremonial only) + plain JS (no TS, 跟项目 frontend 一致)

---

## File Map

| 类别 | 路径 | 操作 | 行数 |
|------|------|------|------|
| 改 component | `dashboard/frontend/src/components/WorkflowStatus.vue:129-133` | hasCost 改 additive OR (costByTier) | +5L |
| 新 e2e spec | `dashboard/frontend/tests/e2e/cost-section-tier-only.spec.js` (NEW) | 创建 ceremonial Playwright contract | ~80L |
| 改 memory | `~/.claude/projects/.../memory/phases.md` | 新增 Phase 8.14 entry (T2 同步) | +1 行 |

**估计**: 1 改 + 1 new + 1 chore = 3 改文件, 1 feat commit + 1 chore commit. 0 改 backend (Phase 8.13 cost_by_tier 已就位), 0 改 CostBarChart (内部 hasData 已正确), 0 改 8 historical spec/plan docs.

---

## Task 1: WorkflowStatus.vue hasCost OR 修复 (TDD-lite: RED via spec review → GREEN via Vue edit)

**Files:**
- Modify: `dashboard/frontend/src/components/WorkflowStatus.vue:129-133`
- Test: `dashboard/frontend/tests/e2e/cost-section-tier-only.spec.js` (T1.4 同步创建)

- [ ] **Step 1.1: 验证 spec 引用 WorkflowStatus.vue L129-133 当前内容**

Run (read only):
```bash
sed -n '129,133p' /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend/src/components/WorkflowStatus.vue
```

Expected output:
```js
const costByScenario = computed(() => props.status?.cost_by_scenario || {});
const hasCost = computed(() => {
  const data = costByScenario.value;
  return Object.keys(data).length > 0 && Object.values(data).some((v) => v > 0);
});
```

确认当前仅查 `costByScenario` — RED 状态确认 (tier-only 数据时 hasCost=false, cost section 隐藏).

- [ ] **Step 1.2: 改 `WorkflowStatus.vue:129-133` 为 additive OR (GREEN)**

用 Edit 工具替换以下 4 行 (L129-132 的 costByScenario + hasCost 块 + costByScenario 后加 1 行 costByTier):

`old_string`:
```js
const costByScenario = computed(() => props.status?.cost_by_scenario || {});
const hasCost = computed(() => {
  const data = costByScenario.value;
  return Object.keys(data).length > 0 && Object.values(data).some((v) => v > 0);
});
```

`new_string`:
```js
// Phase 8.14: hasCost OR check costByTier, 防 tier-only 数据时整 cost section 隐藏
// 跟 Phase 8.13 CostBarChart.hasData 对称 (任一 mode 有正值即 visible)
const costByScenario = computed(() => props.status?.cost_by_scenario || {});
const costByTier = computed(() => props.status?.cost_by_tier || {});
const hasCost = computed(() => {
  const scenarioHas = Object.keys(costByScenario.value).length > 0
    && Object.values(costByScenario.value).some((v) => v > 0);
  const tierHas = Object.keys(costByTier.value).length > 0
    && Object.values(costByTier.value).some((v) => v > 0);
  return scenarioHas || tierHas;
});
```

- [ ] **Step 1.3: 验证 WorkflowStatus.vue 改后内容**

Run:
```bash
sed -n '127,140p' /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend/src/components/WorkflowStatus.vue
```

Expected:
- `costByScenario` / `costByTier` 2 个 computed 各 1 行
- `hasCost` computed 拆 `scenarioHas` / `tierHas` 2 局部 + `return scenarioHas || tierHas`
- 注释 "Phase 8.14" 在块顶

- [ ] **Step 1.4: 验证 Vue SFC syntax 合法 (sanity)**

Run:
```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend && node -e "const fs=require('fs'); const c=fs.readFileSync('src/components/WorkflowStatus.vue','utf8'); const scriptMatch=c.match(/<script setup>([\s\S]*?)<\/script>/); if(!scriptMatch){console.error('NO SCRIPT BLOCK'); process.exit(1);} new Function(scriptMatch[1].replace(/import .* from .*;?\n?/g,'').replace(/defineProps|defineEmits/g,'var noop=()=>{}; var'))(); console.log('SCRIPT OK');"
```

Expected: `SCRIPT OK` (script setup 块能正常 parse, computed 三处引用 props.status 可访问).

若 `NO SCRIPT BLOCK` 或 parse 错 → revert, 调查 syntax.

- [ ] **Step 1.5: 暂存文件, 暂不 commit (等 T2 spec 一起 commit)**

Run:
```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory && git add dashboard/frontend/src/components/WorkflowStatus.vue && git status --short
```

Expected: `M  dashboard/frontend/src/components/WorkflowStatus.vue` (尚未 commit).

---

## Task 2: 创建 ceremonial e2e spec `cost-section-tier-only.spec.js`

**Files:**
- Create: `dashboard/frontend/tests/e2e/cost-section-tier-only.spec.js`

- [ ] **Step 2.1: 写 ceremonial Playwright contract spec (3 test cases)**

用 Write 工具创建 `dashboard/frontend/tests/e2e/cost-section-tier-only.spec.js` 内容:

```javascript
// Phase 8.14: hasCost gate tier 维度补全 ceremonial e2e spec
// 3 test cases: tier-only visible / neither hidden / both visible (sanity)
//
// 跟 Phase 7.6/8.7/8.8/8.11/8.13/8.15 ceremonial specs 同模式:
// Playwright runner 未装, 作为契约文档存在 (header note).
// Mock 走 page.route('**/api/workflows/active') 模式跟 7 specs 一致.

import { test, expect } from '@playwright/test'

const BASE_URL = process.env.BASE_URL || 'http://localhost:5173'

const ACTIVE_WORKFLOW = (cost_by_scenario, cost_by_tier) => ({
  workflow_name: 'novel_writing',
  is_active: true,
  paused: false,
  completed: 5,
  failed: 0,
  node_count: 7,
  steps: 12,
  cost_by_scenario,
  cost_by_tier,
  total_cost_usd: 0.05,
})

test.describe('Phase 8.14: WorkflowStatus hasCost gate (scenario + tier OR)', () => {
  test('shows_cost_section_when_only_tier_data_present', async ({ page }) => {
    // Phase 8.14 fix: tier-only 数据时 cost section 必须 visible
    // 旧 hasCost 仅查 costByScenario → RED 状态 (本测试断言)
    await page.route('**/api/workflows/active', (route) => {
      route.fulfill({
        status: 200,
        contentType: 'application/json',
        body: JSON.stringify(ACTIVE_WORKFLOW({}, { haiku: 0.05 })),
      })
    })
    await page.goto(`${BASE_URL}/workflows`)
    await expect(page.locator('.cost-section')).toBeVisible()
  })

  test('hides_cost_section_when_neither_present', async ({ page }) => {
    // 旧行为保: 双方都空 → cost section hidden
    await page.route('**/api/workflows/active', (route) => {
      route.fulfill({
        status: 200,
        contentType: 'application/json',
        body: JSON.stringify(ACTIVE_WORKFLOW({}, {})),
      })
    })
    await page.goto(`${BASE_URL}/workflows`)
    await expect(page.locator('.cost-section')).toBeHidden()
  })

  test('shows_cost_section_when_both_present', async ({ page }) => {
    // Sanity: 双方有值 → visible (Phase 8.13 path 不破)
    await page.route('**/api/workflows/active', (route) => {
      route.fulfill({
        status: 200,
        contentType: 'application/json',
        body: JSON.stringify(
          ACTIVE_WORKFLOW(
            { chapter_writing: 0.03 },
            { haiku: 0.01, sonnet: 0.02 },
          ),
        ),
      })
    })
    await page.goto(`${BASE_URL}/workflows`)
    await expect(page.locator('.cost-section')).toBeVisible()
  })
})
```

- [ ] **Step 2.2: 验证 spec 文件创建 + 格式正确**

Run:
```bash
wc -l /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend/tests/e2e/cost-section-tier-only.spec.js && echo "---" && head -10 /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend/tests/e2e/cost-section-tier-only.spec.js
```

Expected: 约 65-75 行 (含注释 70-80L 实际), 头部有 "Phase 8.14" + "Playwright runner 未装" 注释.

- [ ] **Step 2.3: 验证 JS 语法 (sanity)**

Run:
```bash
node --check /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend/tests/e2e/cost-section-tier-only.spec.js 2>&1 || true
```

Expected: 报 `Cannot use import statement outside a module` (ES module 在 node check 下正常 expected, 跟 Phase 7.6/8.7/8.8/8.11/8.13/8.15 6+1 specs 行为一致 — 它们都用 `import` 但通过 vitest/playwright runner 跑, node --check 不识别 import).
或: `SyntaxError: Cannot use import statement outside a module` (正常 expected)

若报真实 syntax error (e.g. `Unexpected token`) → fix spec, 重跑.

- [ ] **Step 2.4: 暂存 spec 文件**

Run:
```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory && git add dashboard/frontend/src/components/WorkflowStatus.vue dashboard/frontend/tests/e2e/cost-section-tier-only.spec.js && git status --short
```

Expected: 2 改文件 staged (1 modify + 1 new).

---

## Task 3: 验证 + 1 feat commit 推送

- [ ] **Step 3.1: 验证 pytest baseline 0 破 (0 改 backend)**

Run:
```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory && python -m pytest -q 2>&1 | tail -3
```

Expected: `2232 passed, 0 failed, 27 skipped` (不变, 0 改 backend).

若 0 != 2232 → revert T1+T2, 调查 frontend 改是否影响 backend test (理论上 0 影响).

- [ ] **Step 3.2: 验证 ruff 0 issue**

Run:
```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory && ruff check infra/ dashboard/ tests/agent_system/ tests/dashboard/ 2>&1 | tail -3
```

Expected: `All checks passed!` (Vue 不在 ruff 范围, frontend 不影响).

若报错 → revert, 调查 (Vue 0 改 backend 应 0 影响).

- [ ] **Step 3.3: 验证 vite build 0 error (frontend 编译)**

Run:
```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend && pnpm build 2>&1 | tail -10
```

Expected: `✓ built in <N>s` + 0 error (raw echarts, 跟 Phase 8.13/8.15 一致).

若报错 → revert T1+T2, 调查 WorkflowStatus.vue script setup syntax (Step 1.4 应已 catch).

- [ ] **Step 3.4: 1 feat commit + push**

Run:
```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory && git commit -m "feat(dashboard): phase 8.14 — hasCost gate OR tier 维度补全

Phase 8.13 Task 2 code review 留 pre-existing followup:
WorkflowStatus.vue:130-133 hasCost 仅查 costByScenario, 若 backend
返 cost_by_tier 有值但 cost_by_scenario 空则整 cost section 隐藏
(含 Phase 8.13 新加的 tier toggle).

修复: hasCost 改 additive OR (scenarioHas || tierHas), 加 costByTier
computed 跟 costByScenario 1:1 镜像 (props.status?.cost_by_tier || {}).
旧 scenario-only 行为 100% 保留 (tierHas=false 走旧 scenarioHas),
跟 Phase 8.13 CostBarChart.hasData 行为对称 (任一 mode 有正值即 visible).

新增 1 ceremonial e2e spec cost-section-tier-only.spec.js (3 test cases:
tier-only visible / neither hidden / both visible, 跟 Phase 7.6/8.7/8.8/
8.11/8.13/8.15 7 specs 同契约模式, Playwright runner 未装 留作契约文档).

不改: 0 backend (Phase 8.13 cost_by_tier 已就位) / 0 CostBarChart (内部
hasData 已正确) / 0 8 historical spec/plan docs (:NNN 行号不动) /
0 CLAUDE.md / 0 pyproject.toml / 0 pytest.ini / 0 .env / 0 API key /
0 .state/*.db 提交 (gitignored).

Baseline: pytest 2232 passed 0 failed 27 skipped (不变, 0 改 backend) /
ruff 0 / vite build 0 error. " -m "" 2>&1 | tail -3
```

Expected: `[master <hash>] feat(dashboard): phase 8.14 — hasCost gate OR tier 维度补全` + 2 files changed.

Run:
```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory && git push origin master 2>&1 | tail -3
```

Expected: `To github.com:XiaZiHunDun/LingWen.git` + `<old>..<new>  master -> master` (2 commits ahead: spec + feat).

若 push 失败 → git log --oneline -1 确认 commit 已在本地, retry push 或报告失败.

---

## Task 4: 1 chore commit 同步 phases.md entry (T2 memory sync)

**Files:**
- Modify: `~/.claude/projects/-home-ailearn-projects-AI-Incursion-domains-IP---projects-LingWen/memory/phases.md` (appended)

- [ ] **Step 4.1: 验证 phases.md 当前末尾**

Run:
```bash
tail -2 /home/ailearn/.claude/projects/-home-ailearn-projects-AI-Incursion-domains-IP---projects-LingWen/memory/phases.md
```

Expected: Phase 8.15 entry 末尾 (跟 T2 之前 sync 后的状态).

- [ ] **Step 4.2: Append Phase 8.14 entry via heredoc**

Run (heredoc 整段 append, 跟 Phase 8.15 entry 同 paragraph 密度):

```bash
cat >> /home/ailearn/.claude/projects/-home-ailearn-projects-AI-Incursion-domains-IP---projects-LingWen/memory/phases.md <<'PHASE_8_14_EOF'
## Phase 8.14 (2026-06-07)

Doc 4 §11 Phase 8.14 — hasCost Gate Tier 维度补全 (1 行 OR 修复 + 1 ceremonial e2e) — 收尾 Phase 8.13 Task 2 code review 留 pre-existing followup: `WorkflowStatus.vue:130-133` `hasCost` computed 仅查 `costByScenario`, 若 backend 返 `cost_by_tier` 有值但 `cost_by_scenario` 为空则整 cost section (含 Phase 8.13 新加的 tier toggle) 隐藏 — 触发条件: workflow 仅用单一模型 (e.g. 全 opus) 且 cost_records 表 scenario 字段未填时, tier 维度数据 visible, scenario 维度 hidden, 整 cost section 不可见; 主公决策 3 项: 选 Approach A OR 拼接 (最小 additive 改动) / 走 ceremonial e2e spec (Phase 8.13 模式, 0 改 test runner) / 0 改 backend (Phase 8.13 cost_by_tier 已就位); **实施路径**: spec (commit e514ea4, 115L) → plan (本 file) → subagent-driven-development 1 task: T1 `WorkflowStatus.vue:129-137` (+9L, additive 改 4 行 + 注释 1 行) — `costByTier` computed 跟 `costByScenario` 1:1 镜像 (`props.status?.cost_by_tier || {}` 兜底) + `hasCost` 拆 `scenarioHas` / `tierHas` 2 局部变量 + `return scenarioHas || tierHas` (OR 拼接, 旧 scenario-only 行为 100% 保留 tierHas=false 走旧 scenarioHas, 跟 `CostBarChart.hasData` 行为对称任一 mode 有正值即 visible) + 1 ceremonial e2e `cost-section-tier-only.spec.js` (NEW, ~70L, 3 test cases 跟 Phase 7.6/8.7/8.8/8.11/8.13/8.15 7 specs 同契约模式 Playwright runner 未装, header note 显式声明 `import { test, expect } from '@playwright/test'` + `page.route('**/api/workflows/active', ...)` mock pattern + ACTIVE_WORKFLOW 工厂函数 2 字段 cost_by_scenario/cost_by_tier + 3 cases: shows_cost_section_when_only_tier_data_present / hides_cost_section_when_neither_present / shows_cost_section_when_both_present); **additive backward compat** (0 破): hasCost 旧 scenario-only path 100% 保留 (tierHas=false 走旧 scenarioHas) + costByTier computed 跟 costByScenario 同 pattern `props.status?.cost_by_tier || {}` 兜底, Phase 8.13 旧 caller 0 改 + 0 改 CostBarChart (内部 hasData 走 displayData 已正确) + 0 改 backend (Phase 8.13 cost_by_tier 字段已就位, Phase 8.15 未触及此 path) + 0 改 8 historical spec/plan docs `:NNN` 行号不动 + 0 改 CLAUDE.md/pyproject.toml/pytest.ini/pnpm config/tsconfig + 0 改 .env + 0 API key 泄漏 + 0 .state/*.db 提交 (gitignored); **0 production code 行为破** (12 SCENARIOS 路由 0 改 / ComputeResult schema 0 改 / CostTrackerDB cost_records 表 0 改 / `_extract_cost_by_tier` helper 0 改 / `WorkflowStatusResponse.cost_by_tier` Field 0 改 / dashboard backend except frontend 0 改 / dashboard frontend except WorkflowStatus.vue 0 改 / Phase 8.5/8.7/8.8/8.10/8.11/8.12/8.13/8.15 既有 components 0 改); **TDD-lite 验证** (1 ceremonial e2e 不跑 runner, 契约文档模式): pytest 2232 passed 0 failed 27 skipped 维持 (不变, 0 改 backend) / ruff 0 / vite build 0 error (raw echarts); **git verify**: spec (commit e514ea4, 115L) + feat (commit pending) 推送 origin/master; **chore commit 同步 phases.md** (本次): append Phase 8.14 entry 描述完整 T1 + 1 ceremonial + 0 改 backend/frontend/backend 范围 + 维持 baseline + 后续 followups; **不动 MEMORY.md**: v9.14 / 2232 passed / Phase 1.1-8.15 已含 8.15 (本 patch 0 改 test count, 升 v9.14.1 YAGNI); **后续**: Phase 8.13 followup install vitest+@vue/test-utils+TS 把 7+1 Playwright contract specs 转真 e2e (独立 phase, 跨 7+1 specs 影响范围大) / Phase 8.13 followup data-testid convention 统一 (5 旧 specs 仍用 class selector) / Phase 8.16 dashboard 时间维度切分 (7d/30d/all) / Phase 8.17 SidebarCostBanner dominant tier 行 / Phase 8.18 per-tier 单独 budget alarm / Phase 9 跨卷涟漪 / 历史 cost 时间序列图 (需 budget DB + 时间维度) / WebSocket 断线 indicator (sidebar banner 加 ⚠️ 提示) / MEMORY.md 拆 20+ entries 到 phases.md topic (本 entry 后变 21 entries, 必拆 followup) / MEMORY.md line 12 typo "21 skipped" (历史留) / pre-existing infra/.state/workflow.db .gitignore (Phase 8.12 chore 已加, followup resolved)
PHASE_8_14_EOF
```

Expected: phases.md 从 174 → 175 行 (1 new heading + 1 paragraph, 1 block).

- [ ] **Step 4.3: 验证 append 成功**

Run:
```bash
tail -3 /home/ailearn/.claude/projects/-home-ailearn-projects-AI-Incursion-domains-IP---projects-LingWen/memory/phases.md && echo "---" && wc -l /home/ailearn/.claude/projects/-home-ailearn-projects-AI-Incursion-domains-IP---projects-LingWen/memory/phases.md
```

Expected: 末尾是 Phase 8.14 entry 描述 + 175 行总数.

- [ ] **Step 4.4: 1 chore commit 同步 phases.md (NOT in novel-factory repo, 单独 git repo)**

注意: phases.md 在 `~/.claude/projects/...` 不在 novel-factory git repo. 2 个选择:
- (A) 跳过 commit, 仅 append file (跟 Phase 8.15 时一致 — 我没看到 Phase 8.15 entry 单独 commit)
- (B) 单独 commit 进 phases.md 自己的 git history (若该 dir 是 git repo)

Run:
```bash
cd /home/ailearn/.claude/projects/-home-ailearn-projects-AI-Incursion-domains-IP---projects-LingWen && git status --short 2>&1 | head -3
```

- 若是 git repo 且有 changes → `git add memory/phases.md && git commit -m "chore(memory): phase 8.14 entry"`
- 若不是 git repo → 跳过 commit, file 留在 disk 即可 (跟 Phase 8.15 时一致)

Run (无论选择 A 或 B): `cat phases.md | tail -2 | head -1` 确认 Phase 8.14 heading present.

---

## Critical files

### Created
- `dashboard/frontend/tests/e2e/cost-section-tier-only.spec.js` (~70L) — 3 ceremonial Playwright contract test cases
- `docs/superpowers/specs/2026-06-07-phase8.14-hascost-gate-fix-design.md` (115L) — ✅ done in commit e514ea4
- `docs/superpowers/plans/2026-06-07-phase8.14-hascost-gate-fix.md` (本 file) — ✅ just written

### Modified
- `dashboard/frontend/src/components/WorkflowStatus.vue:129-133` (+9L) — hasCost 改 additive OR (scenarioHas || tierHas) + costByTier computed 1:1 mirror costByScenario
- `~/.claude/projects/.../memory/phases.md` (+1 entry) — Phase 8.14 entry 描述

### Referenced / 复用
- `dashboard/frontend/src/components/CostBarChart.vue:70-73` (`hasData` 走 `displayData` 已正确, 0 改)
- `dashboard/frontend/src/components/CostBarChart.vue:65-68` (`displayData` 走 `mode === 'tier' ? costByTier : costByScenario`, 0 改)
- `dashboard/protocols.py:_extract_cost_by_tier` (Phase 8.13 就位, 0 改)
- `dashboard/app.py:WorkflowStatusResponse.cost_by_tier` (Phase 8.13 Field default_factory=dict, 0 改)
- `dashboard/frontend/tests/e2e/cost-bar-chart-tier-mode.spec.js` (Phase 8.13 ceremonial, 0 改, 跟新 spec 同契约模式)

### NOT modified
- `dashboard/frontend/src/components/CostBarChart.vue` (内部 hasData 已正确)
- `dashboard/protocols.py` (Phase 8.13 _extract_cost_by_tier 已就位)
- `dashboard/app.py` (Phase 8.13 WorkflowStatusResponse.cost_by_tier Field 已就位)
- `dashboard/frontend/src/components/WorkflowStatus.vue` 除 L129-133 外 0 改
- 12 SCENARIOS 路由 / ComputeResult schema / CostTrackerDB cost_records 表
- 8 historical spec/plan docs (`:NNN` 行号不动)
- Phase 8.5/8.7/8.8/8.10/8.11/8.12/8.13/8.15 既有 components
- CLAUDE.md / pyproject.toml / pytest.ini / pnpm config / tsconfig
- 0 .env 改 / 0 API key 泄漏 / 0 .state/*.db 提交 (gitignored)

---

## Out of scope (后续 phases)

- **Phase 8.13 followup**: install vitest+@vue/test-utils+TS 把 7+1 Playwright contract specs 转真 e2e (独立 phase, 跨 7+1 specs 影响范围大)
- **Phase 8.13 followup**: data-testid convention 统一 (5 旧 specs 仍用 class selector, 选 (a) 改 5 旧 specs 一致 或 (b) 接受 split 留 spec/plan 注释)
- **Phase 8.16**: dashboard 时间维度切分 (7d/30d/all)
- **Phase 8.17**: SidebarCostBanner dominant tier 行
- **Phase 8.18**: per-tier 单独 budget alarm
- **Phase 9**: 跨卷涟漪
- **历史 cost 时间序列图** (需 budget DB + 时间维度)
- **WebSocket 断线 indicator** (sidebar banner 加 ⚠️ 提示)
- **MEMORY.md 拆 20+ entries 到 phases.md topic** (本 entry 后变 21 entries, 必拆)
- **MEMORY.md line 12 typo "21 skipped"** (Phase 8.2 后忘改, 历史留)
- **pre-existing infra/.state/workflow.db .gitignore** (Phase 8.12 chore 已加, followup resolved)
- **强制所有 spec 走 vitest** (YAGNI, 7+1 ceremonial specs 模式已 establish)
- **cost_by_run / cost_by_week 新维度** (YAGNI, 本 fix 触发条件未出现)

---

## 风险 + 缓解

| 风险 | 缓解 |
|------|------|
| 旧 caller 期望 `costByScenario` 单独驱动 hasCost 行为 | 0 改 — additive OR, scenario-only 行为不变 (tierHas=false, scenarioHas=true → true) |
| `costByTier` ref 引入后 `cost_by_tier = undefined` 旧 caller crash | `props.status?.cost_by_tier || {}` 兜底 (跟 costByScenario 同样 pattern) |
| `hasCost` 改 OR 后 stale tier data 仍 visible | `value > 0` 守卫保留, 0 cost 不显示, 跟 CostBarChart.hasData 行为对称 |
| Vue script setup parse 错 | Step 1.4 node -e sanity check 显式 parse, fail 则 revert |
| node --check ES module error | expected, 跟 Phase 7.6/8.7/8.8/8.11/8.13/8.15 7 specs 一致 (vitest/playwright runner 跑) |
| pytest baseline 0 != 2232 | Step 3.1 显式 verify, 0 != 2232 则 revert (理论上 0 影响) |
| vite build fail | Step 3.3 显式 verify, fail 则 revert (Step 1.4 应已 catch) |
| 1 ceremonial spec 漏约定模式 | 显式 header note + `page.route('**/api/workflows/active')` mock pattern 跟 6+1=7 specs 一致 |
| Phase 8.14 entry 进 novel-factory repo | spec/plan/phases.md 在不同路径, spec/plan 进 novel-factory, phases.md 在 `~/.claude/projects/...` 独立 (若该 dir 是 git repo 则独立 commit, 若不是则 file 留 disk) |

---

## 验证 (主公 review commit 后)

### Step 1: pytest baseline 0 破

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory && python -m pytest -q 2>&1 | tail -3
# 期望: 2232 passed, 0 failed, 27 skipped
```

### Step 2: ruff 0

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory && ruff check infra/ dashboard/ tests/agent_system/ tests/dashboard/ 2>&1 | tail -3
# 期望: All checks passed!
```

### Step 3: vite build 0 error

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend && pnpm build 2>&1 | tail -5
# 期望: ✓ built in <N>s
```

### Step 4: git status clean (after both commits)

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory && git status
# 期望: nothing to commit, working tree clean
```

### Step 5: git log verify

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory && git log --oneline origin/master | head -5
# 期望: feat(dashboard): phase 8.14 ... 在最顶 (紧随 spec commit)
```

### Step 6: ceremonial spec sanity

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory && head -8 dashboard/frontend/tests/e2e/cost-section-tier-only.spec.js
# 期望: 头部有 "Phase 8.14" + "Playwright runner 未装" + "import { test, expect } from '@playwright/test'"
```

### Step 7: WorkflowStatus.vue hasCost 改 verify

```bash
sed -n '127,140p' /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend/src/components/WorkflowStatus.vue
# 期望: costByScenario + costByTier 2 computed + hasCost 拆 scenarioHas/tierHas + OR 拼接
```

---

## 完成定义 (DoD)

- [ ] `WorkflowStatus.vue:129-133` 改 additive OR (+5L 实际, 9L 含注释 + 空行)
- [ ] `dashboard/frontend/tests/e2e/cost-section-tier-only.spec.js` ceremonial spec 创建 (~70L, 3 test cases)
- [ ] pytest 2232 passed 0 failed 27 skipped 维持
- [ ] ruff 0
- [ ] vite build 0 error
- [ ] 1 feat commit 推送 origin/master
- [ ] phases.md Phase 8.14 entry appended (T4, 1 段描述)
- [ ] MEMORY.md 不动 (v9.14 / 2232 passed / Phase 1.1-8.15 维持)
- [ ] 0 旧测试代码改动 (2232 baseline 维持)
- [ ] 0 production code 行为破 (backend 0 改 / CostBarChart 0 改 / 仅 WorkflowStatus.vue additive)
- [ ] 0 CLAUDE.md / pyproject.toml / pytest.ini 改动
- [ ] 0 误改 .env / 0 API key 泄漏
- [ ] 0 infra/.state/* 提交 (gitignored)
- [ ] 0 force-push, 0 --no-verify, 0 Co-Authored-By footer (跟项目 `~/.claude/settings.json` attribution-disabled 规则)
- [ ] 0 改 8 historical spec/plan docs (`:NNN` 行号不动)
- [ ] 0 改 12 SCENARIOS 路由
- [ ] 0 改 ComputeResult schema / 0 改 CostTrackerDB cost_records 表

---

## 完成后下一步 (后续 phases)

- **Phase 8.13 followup**: install vitest+@vue/test-utils+TS 把 7+1 Playwright contract specs 转真 e2e (独立 phase)
- **Phase 8.13 followup**: data-testid convention 统一 (Phase 8.14 spec 已 recorded)
- **Phase 8.16**: dashboard 时间维度切分 (7d/30d/all)
- **Phase 8.17**: SidebarCostBanner dominant tier 行
- **Phase 8.18**: per-tier 单独 budget alarm
- **Phase 9**: 跨卷涟漪 (独立大方向)
- **MEMORY.md 拆 21+ entries 到 phases.md topic** (本 entry 后变 21 entries, 必拆)
- **MEMORY.md line 12 typo "21 skipped"** (历史留)
