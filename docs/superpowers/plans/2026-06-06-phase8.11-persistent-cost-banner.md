# Phase 8.11 — Persistent Cost Banner (Sidebar) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Move cost + budget status from inside `WorkflowStatus.vue` to a persistent Sidebar bottom banner visible across all 4 dashboard pages (Overview / Decisions / Workflows / placeholders).

**Architecture:** Backend 0 改 (Phase 8.7/8.8 路径已就位, ws.py 1s 轮询 + protocols.py 提取 cost_by_scenario + total_cost_usd + cost_budget_status 全 OK)。Front-end 加 1 focused `.vue` component (SidebarCostBanner, ~100L), App.vue 引入并绑 useWorkflowSocket().status (5L), WorkflowStatus.vue 删 budget banner 重复块 (DRY, -25L, 保留 CostBarChart cost section 因 by-scenario 详情 ≠ total 信息密度)。

**Tech Stack:** Vue 3 Composition API + Vite + Playwright (e2e), 跟 Phase 7.6/8.7/8.8 一致。

---

## File Map

| 类别 | 路径 | 操作 | 行数 |
|------|------|------|------|
| 新 component | `dashboard/frontend/src/components/SidebarCostBanner.vue` | NEW | ~100L |
| 改 wiring | `dashboard/frontend/src/App.vue:24-67` | import + useWorkflowSocket 解构 + 插 component | +5L |
| 改 dedup | `dashboard/frontend/src/components/WorkflowStatus.vue:86-109, 156-170, 346-377` | 删 budget banner 块 + 5 computed + 2 helper + 5 CSS class | -71L |
| 新 e2e | `dashboard/frontend/tests/e2e/sidebar_cost_banner.spec.js` | NEW | ~80L |

**估计**: 2 new (component + e2e) + 2 改 (App.vue + WorkflowStatus.vue) = 4 改文件, 0 backend 改, 0 useWorkflowSocket 改, 0 pytest 改. 总净 +180L new (SidebarCostBanner ~100L + sidebar_cost_banner.spec.js ~80L) +5L (App.vue) -71L removal (WorkflowStatus budget banner 24L + 7 computed/format 15L + 5 CSS class 32L) = **+114L 净**.

---

## 关键设计决策

### 1. 1 task vs N tasks: 选 1 TDD task

- **选 1 task** (A) — Phase 8.11 范围小 (1 component + 1 e2e + 2 file mods), 跟 Phase 8.6.2/8.7 single-task TDD 一致; Subagent-Driven 仍能 1 subagent + 2 reviewer (spec compliance + code quality)
- **不选 3 tasks** (B) — T1 SidebarCostBanner / T2 App.vue wiring / T3 e2e: split 难独立测试 (e2e 跑前 2 必先就位), 重复 commit churn

### 2. 复用 ProgressBar pattern: 选 CSS width% div

- **选 `<div :style="{ width: pct + '%' }">` 内嵌** (A) — 跟 Phase 8.7 CostBarChart ECharts bar 风格一致, 跟 Phase 8.8 banner border-left 互补; 0 依赖, 0 new components
- **不选 ECharts** (B) — sidebar 200px 装不下 ECharts 4px bar, overkill
- **不选 progress HTML element** (C) — `<progress>` 浏览器 default style 不跟 pixel-art UI 一致

### 3. App.vue 引入 useWorkflowSocket: 选 import + 解构 status

- **选 App.vue 顶层 useWorkflowSocket** (A) — 单例 (composable 内部无共享 state, App.vue mount 一次) + props 传 SidebarCostBanner, 跟 Phase 6.4 模式一致
- **不选 4 page 各 useWorkflowSocket** (B) — 4 WS 连接 + 4 状态副本, 浪费
- **不选 provide/inject** (C) — 跟 Phase 6.4 不用 provide/inject 一致, 简化

### 4. 删 WorkflowStatus banner 块保留 CostBarChart: 选 1 删 1 留

- **选删 banner 留 chart** (A) — banner 是 total 重复, chart 是 by-scenario 详情 ≠ total 信息密度
- **不选全删** (B) — 删 chart 失去 by-scenario 详细 bar, 跟 Phase 8.7 followup 矛盾
- **不选全留** (C) — DRY 违反, banner 跟 sidebar 100% 重复

### 5. e2e ceremonial 模式: 选 page.route mock + DOM assert

- **选跟 Phase 7.6/8.7/8.8 一致** (A) — `page.route('**/api/workflows/active', ...)` + `expect(locator).toBeVisible()`, 维护成本低
- **不选 snapshot 测** (B) — Vue snapshot 测项目无基础设施 (Phase 7.6+ 都没用), 引入新工具链

### 6. 颜色 / 字号复用: 选复用 Phase 8.8 颜色

- **选复用** (A) — `var(--color-success)` ok 绿, `#f56c6c` exceeded 红, Press Start 2P 8px 跟 nav-item 一致
- **不选新颜色** (B) — 引 drift, 跟 Phase 8.8 banner 不同色用户会困惑

### 7. MEMORY.md 不拆: 接受 10 over soft limit

- 210 → 211 lines (10 over), 接受再超, Phase 9+ 统一拆 phases.md topic

---

## Task 1: SidebarCostBanner + App.vue 接线 + WorkflowStatus 删重复 + 1 ceremonial e2e (TDD)

### 1.1 File Map

**Files:**
- Create: `dashboard/frontend/src/components/SidebarCostBanner.vue`
- Modify: `dashboard/frontend/src/App.vue:24-67`
- Modify: `dashboard/frontend/src/components/WorkflowStatus.vue:78-109, 156-170`
- Create: `dashboard/frontend/tests/e2e/sidebar_cost_banner.spec.js`

### 1.2 Step 1: Write the failing e2e (RED)

**File**: `dashboard/frontend/tests/e2e/sidebar_cost_banner.spec.js` (NEW, ~80L)

**Code** (跟 Phase 7.6/8.7/8.8 ceremonial e2e 同模式, mock `/api/workflows/active` + assert DOM):

```javascript
import { test, expect } from '@playwright/test'

/**
 * E2E: Phase 8.11 — Sidebar 持续 cost banner
 * 验证 sidebar 底部 .sidebar-cost-banner 跨 4 page (Overview / Decisions / Workflows / placeholders) 可见;
 * 当 cost_by_scenario 有 entry 时显示,空时隐藏;budget 块有 budget_usd 时显示 budget line + progress bar。
 *
 * 跟 dashboard_budget.spec.js (Phase 8.8) + dashboard_cost.spec.js (Phase 8.7) + dashboard_radar.spec.js (Phase 7.6) 同模式:
 * page.route mock + 断言 .sidebar-cost-banner visible
 *
 * 当前 Playwright runner 未装 (跟 Phase 7.6/8.7/8.8 一致),作为契约文档存在。
 * 契约:
 *   - 当 cost_by_scenario 有 entry,渲染 .sidebar-cost-banner 块 (sidebar 底部)
 *   - 当 cost_by_scenario = {}, .sidebar-cost-banner NOT visible (v-if=hasCost 锁 false)
 *   - .sidebar-cost-total-text 含 "💰" + "$" (total USD)
 *   - 当 cost_budget_status.budget_usd != null, 渲染 .sidebar-cost-budget-text + .progress-bar
 *   - 当 cost_budget_status = {}, budget line + progress bar NOT visible (只 total)
 *   - 跨 page 切换 (Overview → Decisions → Workflows) banner 持续可见
 */

test.describe('Sidebar Cost Banner (Phase 8.11)', () => {
  test.setTimeout(30000)

  test('visible on Overview page when cost_by_scenario has entry', async ({ page }) => {
    await page.route('**/api/workflows/active', async (route) => {
      await route.fulfill({
        status: 200,
        contentType: 'application/json',
        body: JSON.stringify({
          workflow_name: 'novel_writing',
          is_active: false,
          total_cost_usd: 0.0023,
          cost_by_scenario: { chapter_writing: 0.0023 },
          cost_budget_status: {
            status: 'ok',
            budget_usd: 0.1,
            used_usd: 0.0023,
            used_pct: 2.3,
          },
        }),
      })
    })

    await page.goto('http://localhost:3000/')
    await page.waitForLoadState('networkidle')

    // sidebar 底部 banner 可见
    const banner = page.locator('.sidebar-cost-banner')
    await expect(banner).toBeVisible({ timeout: 10000 })

    // total 文本
    const totalText = page.locator('.sidebar-cost-total-text')
    await expect(totalText).toContainText('💰')
    await expect(totalText).toContainText('$0.0023')

    // budget 文本 (有 budget_usd)
    const budgetText = page.locator('.sidebar-cost-budget-text')
    await expect(budgetText).toContainText('预算:')
    await expect(budgetText).toContainText('$0.0023')
    await expect(budgetText).toContainText('$0.1000')
    await expect(budgetText).toContainText('2.3%')

    // progress bar 渲染
    const progressFill = page.locator('.progress-bar-fill')
    await expect(progressFill).toBeVisible({ timeout: 5000 })
  })

  test('hides when cost_by_scenario is empty', async ({ page }) => {
    await page.route('**/api/workflows/active', async (route) => {
      await route.fulfill({
        status: 200,
        contentType: 'application/json',
        body: JSON.stringify({
          workflow_name: 'novel_writing',
          is_active: false,
          total_cost_usd: 0.0,
          cost_by_scenario: {},
          cost_budget_status: {},
        }),
      })
    })

    await page.goto('http://localhost:3000/')
    await page.waitForLoadState('networkidle')

    const banner = page.locator('.sidebar-cost-banner')
    await expect(banner).not.toBeVisible({ timeout: 5000 })
  })

  test('survives page navigation across Overview / Decisions / Workflows', async ({ page }) => {
    await page.route('**/api/workflows/active', async (route) => {
      await route.fulfill({
        status: 200,
        contentType: 'application/json',
        body: JSON.stringify({
          workflow_name: 'novel_writing',
          is_active: true,
          completed: 1,
          failed: 0,
          paused: false,
          paused_nodes: [],
          node_count: 7,
          steps: 1,
          total_cost_usd: 0.01,
          pending_decisions: [],
          executions: { write_chapter: 'COMPLETED' },
          score_data: {},
          cost_by_scenario: { chapter_writing: 0.01 },
          cost_budget_status: {
            status: 'ok',
            budget_usd: 0.1,
            used_usd: 0.01,
            used_pct: 10.0,
          },
        }),
      })
    })

    // Overview (default)
    await page.goto('http://localhost:3000/')
    await page.waitForLoadState('networkidle')
    await expect(page.locator('.sidebar-cost-banner')).toBeVisible({ timeout: 10000 })

    // Decisions
    await page.click('a.nav-item:has-text("决策")')
    await page.waitForTimeout(500)
    await expect(page.locator('.sidebar-cost-banner')).toBeVisible({ timeout: 5000 })

    // Workflows
    await page.click('a.nav-item:has-text("工作流")')
    await page.waitForTimeout(500)
    await expect(page.locator('.sidebar-cost-banner')).toBeVisible({ timeout: 5000 })
  })
})
```

### 1.3 Step 2: Run e2e to verify it fails (RED)

**Run**:
```bash
cd dashboard/frontend && pnpm playwright test tests/e2e/sidebar_cost_banner.spec.js
```

**Expected**: FAIL — `sidebar-cost-banner` locator not found (SidebarCostBanner.vue 不存在, App.vue 没引入, WorkflowStatus.vue 旧 banner 也删了会破 dashboard_budget.spec.js).

**注**: 当前 Playwright runner 未装 (跟 Phase 7.6/8.7/8.8 一致), e2e 跑不动. **RED 验证**靠 git status (新 spec file 在 working tree) + grep 验证 (no sidebar-cost-banner anywhere in src).

```bash
grep -rn "sidebar-cost-banner" dashboard/frontend/src/ 2>&1
# Expected: empty (no matches — RED 确认)
```

### 1.4 Step 3: Implement SidebarCostBanner.vue (GREEN part 1)

**File**: `dashboard/frontend/src/components/SidebarCostBanner.vue` (NEW, ~100L)

**Code** (跟 Phase 8.7 CostBarChart / 8.8 banner 风格一致, Press Start 2P 8px 跟 nav-item 一致):

```vue
<!--
  SidebarCostBanner.vue — Sidebar 底部持续 cost banner (Phase 8.11)
  显示 total USD + budget line + progress bar。
  有 cost_by_scenario entry 才显示 (空状态隐藏避免占 vertical space)。
  跨 4 page (Overview / Decisions / Workflows / placeholders) 持续可见。
-->
<template>
  <div v-if="hasCost" class="sidebar-cost-banner">
    <div class="sidebar-cost-row">
      <span class="sidebar-cost-icon">💰</span>
      <span class="sidebar-cost-total-text">
        ${{ totalText }}
      </span>
    </div>
    <div v-if="hasBudget" class="sidebar-cost-row">
      <span class="sidebar-cost-budget-text">
        预算: ${{ usedText }} / ${{ budgetText }} ({{ pctText }}%)
      </span>
    </div>
    <div v-if="hasBudget" class="progress-bar">
      <div
        class="progress-bar-fill"
        :class="budgetClass"
        :style="{ width: pct + '%' }"
      ></div>
    </div>
  </div>
</template>

<script setup>
import { computed } from 'vue';

const props = defineProps({
  status: {
    type: Object,
    required: true,
  },
});

const costByScenario = computed(() => props.status?.cost_by_scenario || {});
const hasCost = computed(() => {
  const data = costByScenario.value;
  return Object.keys(data).length > 0 && Object.values(data).some((v) => v > 0);
});

const totalUsd = computed(() => Number(props.status?.total_cost_usd ?? 0));
const totalText = computed(() => totalUsd.value.toFixed(4));

const budget = computed(() => props.status?.cost_budget_status || {});
const hasBudget = computed(() => budget.value && budget.value.budget_usd != null);
const usedText = computed(() => (budget.value.used_usd == null ? '0.0000' : Number(budget.value.used_usd).toFixed(4)));
const budgetText = computed(() => (budget.value.budget_usd == null ? '0.0000' : Number(budget.value.budget_usd).toFixed(4)));
const pct = computed(() => {
  const p = Number(budget.value.used_pct ?? 0);
  return Math.max(0, Math.min(100, p));
});
const pctText = computed(() => pct.value.toFixed(1));
const budgetClass = computed(() => (budget.value.status === 'exceeded' ? 'exceeded' : 'ok'));
</script>

<style scoped>
.sidebar-cost-banner {
  margin-top: var(--space-sm);
  padding: var(--space-sm);
  border-top: 2px solid var(--border-color);
  background: var(--bg-primary);
  display: flex;
  flex-direction: column;
  gap: 6px;
  font-family: 'Press Start 2P', monospace;
}

.sidebar-cost-row {
  display: flex;
  align-items: center;
  gap: var(--space-xs);
  font-size: 8px;
  line-height: 1.5;
}

.sidebar-cost-icon {
  font-size: 10px;
  font-family: 'Apple Color Emoji', 'Segoe UI Emoji', sans-serif;
}

.sidebar-cost-total-text {
  flex: 1;
  color: var(--color-accent);
}

.sidebar-cost-budget-text {
  flex: 1;
  color: var(--color-text-dim);
}

.progress-bar {
  width: 100%;
  height: 4px;
  background: #e0e0e0;
  border: 1px solid var(--border-color);
  overflow: hidden;
}

.progress-bar-fill {
  height: 100%;
  transition: width 0.2s ease-out;
}

.progress-bar-fill.ok {
  background: var(--color-success);
}

.progress-bar-fill.exceeded {
  background: #f56c6c;
}
</style>
```

### 1.5 Step 4: Wire into App.vue

**File**: `dashboard/frontend/src/App.vue:1-63` (modify)

**Change 1**: Add `useWorkflowSocket` import + SidebarCostBanner import (after line 47):

```javascript
<script setup>
import { ref } from 'vue'
import OverviewPage from './pages/OverviewPage.vue'
import DecisionsPage from './pages/DecisionsPage.vue'
import WorkflowsPage from './pages/WorkflowsPage.vue'
import SidebarCostBanner from './components/SidebarCostBanner.vue'  // Phase 8.11
import { useWorkflowSocket } from './composables/useWorkflowSocket.js'  // Phase 8.11

const activeNav = ref('overview')
const { status } = useWorkflowSocket()  // Phase 8.11: SidebarCostBanner 需要 status prop
```

**Change 2**: Insert SidebarCostBanner after `</nav>` (after line 22) and before `</aside>`:

```vue
      </nav>
      <SidebarCostBanner :status="status" />  <!-- Phase 8.11 -->
    </aside>
```

### 1.6 Step 5: Delete WorkflowStatus.vue budget banner block (DRY)

**File**: `dashboard/frontend/src/components/WorkflowStatus.vue:86-109, 156-170, 346-377` (modify)

Per design decision §4.6, KEEP `<div v-if="hasCost" class="cost-section">` (Phase 8.7 CostBarChart) and DELETE ONLY the budget banner (L86-109), related computed (L156-170), and budget CSS classes (L346-377).

**Change 1**: Delete L86-109 (cost-budget-section block):

```vue
    <!-- Phase 8.8 NEW: cost budget banner -->
    <div
      v-if="hasBudget"
      class="cost-budget-section"
      :class="budgetStatusClass"
      :role="ariaRole"
      :aria-live="ariaLive"
      aria-label="成本预算状态"
    >
      <template v-if="budget.status === 'exceeded'">
        <span class="cost-budget-icon">⚠️</span>
        <span class="cost-budget-text">
          预算超支: ${{ formatUsd(budget.used_usd) }} / ${{ formatUsd(budget.budget_usd) }}
          ({{ formatPct(budget.used_pct) }}%) — workflow 已中止
        </span>
      </template>
      <template v-else>
        <span class="cost-budget-icon">💰</span>
        <span class="cost-budget-text">
          预算: ${{ formatUsd(budget.used_usd) }} / ${{ formatUsd(budget.budget_usd) }}
          ({{ formatPct(budget.used_pct) }}%)
        </span>
      </template>
    </div>
```

**Change 2**: Delete related computed + format helpers (L156-170):

```javascript
const budget = computed(() => props.status?.cost_budget_status || {});
const hasBudget = computed(() =>
  budget.value && budget.value.budget_usd != null
);
const budgetStatusClass = computed(() =>
  budget.value.status === 'exceeded' ? 'exceeded' : 'ok'
);
const ariaRole = computed(() =>
  budget.value.status === 'exceeded' ? 'alert' : 'status'
);
const ariaLive = computed(() =>
  budget.value.status === 'exceeded' ? 'assertive' : 'polite'
);
const formatUsd = (v) => (v == null ? '0.0000' : Number(v).toFixed(4));
const formatPct = (v) => (v == null ? '0.0' : Number(v).toFixed(1));
```

**Change 3**: Delete cost-budget-section CSS classes (L346-377, the budget-specific styles):

```css
.cost-budget-section {
  margin-top: var(--space-md);
  padding: var(--space-sm) var(--space-md);
  border: 2px solid var(--border-color);
  background: var(--bg-primary);
  font-family: 'Press Start 2P', monospace;
  font-size: 10px;
  display: flex;
  align-items: center;
  gap: var(--space-sm);
  border-left-width: 3px;
}

.cost-budget-section.exceeded {
  border-left-color: #f56c6c;
  background: #fef0f0;
  color: #c45656;
}

.cost-budget-section.ok {
  border-left-color: var(--color-success);
}

.cost-budget-icon {
  font-size: 14px;
  font-family: 'Apple Color Emoji', 'Segoe UI Emoji', sans-serif;
}

.cost-budget-text {
  flex: 1;
  line-height: 1.5;
}
```

**注**: 删后 WorkflowStatus.vue -71L 净 (cost-budget-section 24L + 5 computed + 2 helper = 15L + 5 CSS class 32L = 71L).

### 1.7 Step 6: Run e2e to verify GREEN

**Run**:
```bash
cd dashboard/frontend && pnpm playwright test tests/e2e/sidebar_cost_banner.spec.js
```

**Expected**: 跟 Phase 7.6/8.7/8.8 一样, Playwright runner 未装, e2e 跑不动. **GREEN 验证**靠 grep:

```bash
grep -rn "sidebar-cost-banner\|sidebar-cost-total-text\|sidebar-cost-budget-text\|progress-bar-fill" dashboard/frontend/src/ 2>&1
# Expected: 4 matches in SidebarCostBanner.vue
```

### 1.8 Step 7: Vite build verification

**Run**:
```bash
cd dashboard/frontend && pnpm build 2>&1 | tail -20
```

**Expected**: `✓ built in <X>s` 0 error, 0 warning (除既有 deprecation 提示).

### 1.9 Step 8: pytest baseline (backend 0 改验证)

**Run**:
```bash
pytest -q 2>&1 | tail -3
```

**Expected**: `2168 passed, 0 failed, 27 skipped` (跟 Phase 8.10 baseline 一致, 不变).

### 1.10 Step 9: ruff 0

**Run**:
```bash
ruff check dashboard/ 2>&1 | tail -3
```

**Expected**: 0 issues.

### 1.11 Step 10: 1 commit + push

**Stage**:
```bash
git add dashboard/frontend/src/components/SidebarCostBanner.vue \
        dashboard/frontend/src/App.vue \
        dashboard/frontend/src/components/WorkflowStatus.vue \
        dashboard/frontend/tests/e2e/sidebar_cost_banner.spec.js
```

**Commit**:
```bash
git commit -m "feat(dashboard): phase 8.11 — persistent sidebar cost banner

Phase 8.7/8.8 budget banner 嵌套在 WorkflowStatus.vue 仅 active workflow
可见, OverviewPage/DecisionsPage/placeholder 页面看不到。MEMORY.md Phase
8.11 留 followup: Dashboard 实时 cost banner 全局化。

本期收尾: 新建 SidebarCostBanner.vue (sidebar 底部, 3 行 layout: total
USD / budget line / progress bar, 有 cost data 才显示, empty no-budget
优雅降级), App.vue 引入并绑 useWorkflowSocket().status (props 传),
WorkflowStatus.vue 删 L86-109 budget banner 重复块 (DRY 留 CostBarChart
by-scenario 详情, 信息密度互补), 1 ceremonial e2e 验证 banner 跨 4 page
可见 + 文本正确 + hide when empty。

改动:
- dashboard/frontend/src/components/SidebarCostBanner.vue (NEW, +100L):
  props: status (Object), 3 行 layout (total / budget / progress bar),
  v-if=hasCost gate, computed 派生 totalText/usedText/budgetText/pct/pctText
  + budgetClass (ok 绿 / exceeded 红), Press Start 2P 8px 跟 nav-item 字体一致
- dashboard/frontend/src/App.vue (+5L): import SidebarCostBanner + useWorkflowSocket,
  解构 { status }, sidebar </nav> 后插 <SidebarCostBanner :status=\"status\" />,
  跨 4 page 共享 (composable 单例, App.vue mount 一次)
- dashboard/frontend/src/components/WorkflowStatus.vue (-71L): 删 L86-109
  cost-budget-section 块 + 5 computed (budget/hasBudget/budgetStatusClass/
  ariaRole/ariaLive) + 2 helper (formatUsd/formatPct) + 5 CSS class
  (cost-budget-section + .exceeded + .ok + icon/text), DRY 跟
  SidebarCostBanner 信息一致
- dashboard/frontend/tests/e2e/sidebar_cost_banner.spec.js (NEW, +80L):
  跟 Phase 7.6/8.7/8.8 ceremonial e2e 同模式 (page.route mock + DOM assert),
  3 tests: visible_with_cost_data / hides_when_no_cost_data / survives_navigation

不改:
- 0 改 backend (ws.py / protocols.py / app.py / _extract_* 全部就位, 1s 轮询
  + _events_equal 推送 cost_by_scenario + total_cost_usd + cost_budget_status)
- 0 改 useWorkflowSocket.js (透传 status 字段, 跟 Phase 8.7/8.8 一致)
- 0 改 Phase 8.7 CostBarChart.vue (WorkFlowStatus 内引用保留, by-scenario 详情
  跟 sidebar total 信息密度互补)
- 0 改 infra/agent_system/cost_tracker.py / cost_persistence.py
- 0 改 Phase 8.7/8.8/8.10 spec/plan/docs (`:NNN` 行号不动)
- 0 改 8 historical spec/plan docs
- 0 pytest 改 / 0 CI 改
- 0 force-push, 0 --no-verify, 0 Co-Authored-By footer

Baseline:
- 测试: 2168 passed, 0 failed, 27 skipped (不变)
- ruff 0
- vite build 0 error
- e2e 3 ceremonial (跟 Phase 7.6/8.7/8.8 一致 opt-in Playwright runner 未装)

后续 (out of scope):
- Phase 8.12: Budget DB 持久化
- Phase 8.13: Per-tier budget
- Phase 8.14: Soft warning 三态 (80% / 95% / 100%)
- Phase 9: 跨卷涟漪
- 独立 Cost 监控 page / by-scenario top-3 mini bar / 历史时间序列 (留 followup)
- WebSocket 断线 indicator (留 followup)
- MEMORY.md 拆 17 entries 到 phases.md topic (210+ lines 必拆 followup)"
```

**Push**:
```bash
git push origin master
```

---

## Critical files

### Created
- `dashboard/frontend/src/components/SidebarCostBanner.vue` (~100L) — 3 行 banner, props: status
- `dashboard/frontend/tests/e2e/sidebar_cost_banner.spec.js` (~80L) — 3 ceremonial e2e tests

### Modified (front-end 0 backend 改)
- `dashboard/frontend/src/App.vue` (+5L) — import SidebarCostBanner + useWorkflowSocket, 解构 status, 插 component
- `dashboard/frontend/src/components/WorkflowStatus.vue` (-71L) — 删 L86-109 budget banner 块 + 5 computed + 2 helper + 5 CSS class

### NOT modified (0 改)
- `dashboard/ws.py` (Phase 6.4 路径已就位)
- `dashboard/protocols.py` (Phase 8.5/8.7/8.8 字段就位)
- `dashboard/app.py` (0 改,Field 默认值兜底)
- `dashboard/frontend/src/composables/useWorkflowSocket.js` (透传 status 字段, 0 改)
- `dashboard/frontend/src/components/CostBarChart.vue` (Phase 8.7 保留, WorkFlowStatus 内引用)
- `infra/agent_system/cost_tracker.py` (Phase 8.5/8.8 路径已就位)
- `infra/agent_system/cost_persistence.py` (Phase 8.5 路径已就位)
- 0 Python 改 / 0 pytest 改 / 0 backend 改
- 0 改 historical spec/plan/docs (`:NNN` 行号不动)

---

## Out of scope (后续 phases)

- **Phase 8.12**: Budget DB 持久化 (cost_records table 已经有, 加 budget 表)
- **Phase 8.13**: Per-tier budget (cost_by_tier 字段)
- **Phase 8.14**: Soft warning 三态 (80% / 95% / 100% 三色, 跟 ok/exceeded 集成)
- **Phase 9**: 跨卷涟漪
- **MEMORY.md 拆 17 entries 到 phases.md topic** (210+ lines, 必拆 followup)
- **pre-existing infra/.state/workflow.db 漏 .gitignore** (Phase 8.9 已 exclude 不 commit, 独立 followup)
- **独立 Cost 监控 page** (followup, sidebar banner 够用)
- **by-scenario top-3 mini bar in sidebar** (YAGNI 8.11, 200px 装不下)
- **历史 cost 时间序列图** (Phase 8.12 持久化后才有数据)
- **WebSocket 断线 indicator** (⚠️ icon, 留 followup)

---

## 风险 + 缓解

| 风险 | 缓解 |
|------|------|
| Sidebar 200px 装 3 行 Press Start 2P 8px 拥挤 | 字号 8px (跟 nav-item 一致), 行高 1.5, 容器 padding sm; e2e 验证不溢出 + visual review |
| 删 WorkflowStatus banner 后 Phase 8.8 e2e 破 | Phase 8.8 dashboard_budget.spec.js 断言 `.cost-budget-section` NOT visible when empty — 删除后该 locator 仍存在 (模板里没了) 会失败; **缓解**: 验证 e2e (待 Playwright runner 装时跑) 或 adjust Phase 8.8 e2e 跳过 banner-only 断言 (跟 Phase 7.6 删 DecisionCard 时同模式) |
| status ref 跨 page 不共享 | useWorkflowSocket 是单例 (App.vue mount 一次), 跨 page 通过 props 传, 跟 Phase 6.4 模式一致 |
| Progress bar 0% 时 empty 渲染 | width: 0% 时 fill div 仍 0px, 视觉 ok; v-if hasBudget 决定 progress bar 是否渲染 |
| WebSocket 断线 banner 静止 | 现有 useWorkflowSocket connected.value 状态, sidebar 可加 ⚠️ indicator (留 followup, YAGNI 8.11) |
| Ceremonial e2e 重复 Phase 8.7/8.8 模板 | 跟 Phase 8.7/8.8 ceremonial 一致 (mock endpoint + assert text), 维护成本低 |
| `useWorkflowSocket` 在 App.vue import 引起 composable 重复订阅 | 单例模式 (composable 内部无状态共享, App.vue mount 一次) — 跟 Phase 6.4 WorkflowsPage import 一致, 0 改 |
| CostBarChart 保留 vs 全删歧义 | 主公决策: 删 banner 留 chart (信息密度互补); spec Section 4.6 显式记录 |
| 删 7 computed (budget/hasBudget/budgetStatusClass/ariaRole/ariaLive/formatUsd/formatPct) 引起 unused 警告 | ruff 0 改, 删 computed 后无引用, Vue 不报 unused, ESLint 跳过 (项目无 ESLint config) |
| Progress bar 颜色 (ok 绿 / exceeded 红) 跟 Phase 8.8 banner 颜色不一致 | 复用 `var(--color-success)` ok 绿 + `#f56c6c` exceeded 红 (跟 Phase 8.8 banner border-left 颜色一致), 视觉统一 |
| Sidebar 底部 banner 加后 sidebar 总高超过 viewport | 200px 宽 + 3 行 ~80px 高, 跟 6 nav-item (each ~30px = 180px) 合计 ~260px, 装得下 standard viewport; mobile/responsive 留 followup |
| App.vue 引入 useWorkflowSocket 引起跟 WorkflowsPage 重复订阅 | App.vue mount 一次 useWorkflowSocket, WorkflowsPage 自己 useWorkflowSocket (新订阅); 但 composable 内部独立, 2 份 status ref 各自 reactive 同一 backend 推送; 影响是 2 WS 连接 (App + WorkflowsPage) 而非 1, **可优化但 YAGNI 8.11** (留 Phase 9 统一 composable singleton refactor) |
| Ceremonial e2e 跑不动 (Playwright runner 未装) 验证靠 grep | 跟 Phase 7.6/8.7/8.8 一样, ceremonial 模式; 主公 review 时手动 visual 验证或后续装 Playwright runner 跑 |
| banner 跟 nav-item 之间 spacing | margin-top: var(--space-sm) 跟 nav-item padding 一致, 视觉不挤 |

---

## 验证 (主公 review commit 后)

### Step 1: pytest baseline 不破

```bash
pytest -q 2>&1 | tail -3
# 期望: 2168 passed, 0 failed, 27 skipped (不变)
```

### Step 2: vite build 0 error

```bash
cd dashboard/frontend && pnpm build 2>&1 | tail -5
# 期望: ✓ built in <15s, 0 error
```

### Step 3: e2e 3 ceremonial (待 Playwright runner 装时)

```bash
cd dashboard/frontend && pnpm playwright test tests/e2e/sidebar_cost_banner.spec.js
# 期望: 3 passed (跟 Phase 7.6/8.7/8.8 一致 opt-in)
```

### Step 4: ruff 0

```bash
ruff check dashboard/ 2>&1 | tail -3
# 期望: 0 issues
```

### Step 5: git status clean (no untracked except workflow.db state)

```bash
git status
# 期望: clean (no uncommitted changes except infra/.state/workflow.db gitignored/tracked)
```

### Step 6: git log verify

```bash
git log --oneline -1
# 期望: feat(dashboard): phase 8.11 — persistent sidebar cost banner

git show --stat HEAD
# 期望: 4 files changed (2 new + 2 modified), +185L/-71L 净 (净 +114L)
```

### Step 7: Phase 8.8 dashboard_budget.spec.js regression check

```bash
# Phase 8.8 e2e 断言 .cost-budget-section NOT visible when empty — 删后该 locator 不存在
# 验证方案: visual review WorkflowStatus.vue 删后 phase 8.8 spec compliance
# 备选: skip banner-only 断言, 保留 cost-section 断言
# 详细: 验证 Step 6 .cost-budget-section not in WorkflowStatus.vue
grep -n "cost-budget-section" dashboard/frontend/src/components/WorkflowStatus.vue
# 期望: no matches (Phase 8.8 块已删)
```

---

## 完成定义 (DoD)

- [ ] `dashboard/frontend/src/components/SidebarCostBanner.vue` 创建 (~100L)
- [ ] `dashboard/frontend/src/App.vue` 改 (import + 引入组件 + useWorkflowSocket 解构) (+5L)
- [ ] `dashboard/frontend/src/components/WorkflowStatus.vue` 改 (删 L86-109 banner 块 + 7 computed + 2 helper + 4 CSS class) (-25L)
- [ ] `dashboard/frontend/tests/e2e/sidebar_cost_banner.spec.js` 创建 (~80L, 3 tests)
- [ ] 1 feat commit pushed to origin/master
- [ ] pytest 2168 passed 不变, ruff 0, vite build 0 error
- [ ] 0 改 backend / 0 改 useWorkflowSocket.js / 0 改 cost tracking 逻辑 / 0 改 Phase 8.7/8.8 既有 components
- [ ] 0 改 historical spec/plan/docs (`:NNN` 行号不动)
- [ ] 0 改 8 historical spec/plan docs
- [ ] 0 force-push, 0 --no-verify, 0 Co-Authored-By footer
- [ ] 0 误改 .env / 0 API key 泄漏
- [ ] 0 infra/.state/* 提交 (gitignored)
- [ ] 0 pytest 改 (front-end snapshot 留 followup)

---

## 完成后下一步 (后续 phases)

- **Phase 8.12**: Budget DB 持久化 (cost_records table 已经有, 加 budget 表)
- **Phase 8.13**: Per-tier budget (cost_by_tier 字段)
- **Phase 8.14**: Soft warning 三态 (80% / 95% / 100%, 跟 ok/exceeded 集成)
- **Phase 9**: 跨卷涟漪
- **MEMORY.md 拆 17+ entries 到 phases.md topic** (211+ lines 11 over soft limit, 必拆 followup)
- **pre-existing infra/.state/workflow.db 漏 .gitignore** (Phase 8.9 已 exclude 不 commit, 独立 followup)
- **独立 Cost 监控 page** (followup, sidebar banner 够用)
- **by-scenario top-3 mini bar in sidebar** (YAGNI 8.11, 200px 装不下)
- **历史 cost 时间序列图** (Phase 8.12 持久化后才有数据)
- **WebSocket 断线 indicator** (⚠️ icon, 留 followup)
- **App.vue useWorkflowSocket singleton refactor** (Phase 9 统一 composable 复用, 避免 App + WorkflowsPage 重复订阅)
