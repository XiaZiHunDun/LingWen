# Phase 8.32 — DecisionCard/WorkflowGraph inner sub-element testid Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 DecisionCard (6) + WorkflowGraph (4) 加 10 个 inner sub-element `data-testid`,伴生 2 new vitest spec 100% 覆盖,跟 Phase 8.30b/8.31 vitest 模式 1:1 一致。

**Architecture:** 2 atomic commit (DecisionCard + spec 一组,WorkflowGraph + spec 一组),每组 TDD (spec 红 → testid 加 → spec 绿 → commit),最后 push + memory sync。Frontend-only,0 改 backend,0 改 33 现有 testid,0 改 14 现有 spec,0 改 setup.ts / vitest.config.ts。

**Tech Stack:** Vue 3 (`<script setup>` Composition API) + TypeScript spec (`vitest` + `@vue/test-utils` + `vi.mock` for `mermaid`) + jsdom。

**Spec:** `docs/superpowers/specs/2026-06-08-phase8.32-inner-testid-design.md` (333L, commit `ecfc958`)

---

## Task 1: DecisionCard 6 inner testid + decision-card.spec.ts (1 feat commit)

**Files:**
- Modify: `src/components/DecisionCard.vue:15/34/44/51/60/65` (6 attribute 注入)
- Create: `tests/unit/decision-card.spec.ts` (NEW, ~80L, 3 tests)

- [ ] **Step 1.1: 写 decision-card.spec.ts (RED 准备)**

创建 `tests/unit/decision-card.spec.ts`,内容如下 (跟 Phase 8.30b spec 同 Chinese header 注释 + TypeScript import pattern):

```ts
// tests/unit/decision-card.spec.ts — Phase 8.32
// DecisionCard 3 status 状态 (pending/resolved/cancelled) 互斥 inner testid 验证.
// 跟 Phase 6.6.B 注释设计一致: statusBadge map 只含 resolved/deferred/cancelled 3
// key, readonly-hint + meta-info 3 status 都会渲染. Spec 抓住这个语义边界.

import { describe, test, expect } from 'vitest'
import { mount } from '@vue/test-utils'
import DecisionCard from '../../src/components/DecisionCard.vue'

interface Decision {
  decision_id: string
  status: 'pending' | 'resolved' | 'deferred' | 'cancelled'
  kind?: string
  priority?: number
  prompt?: string
  options?: string[]
  context?: Record<string, unknown>
  resolved_by?: string
  resolution?: string
  resolved_at?: string
  reason?: string
}

describe('DecisionCard inner sub-element testid (Phase 8.32)', () => {
  test('pending decision renders options + defer + cancel, no status-badge, no readonly-hint, no meta-info', () => {
    const wrapper = mount(DecisionCard, {
      props: {
        decision: {
          decision_id: 'd1',
          status: 'pending',
          kind: 'outline_judgment',
          priority: 1,
          options: ['选项 A', '选项 B', '选项 C'],
          prompt: '请审核大纲',
          context: {},
        } as Decision,
      },
    })

    // pending: 3 个 option 按钮 + defer + cancel 显
    expect(wrapper.findAll('[data-testid="option-btn"]')).toHaveLength(3)
    expect(wrapper.find('[data-testid="defer-btn"]').exists()).toBe(true)
    expect(wrapper.find('[data-testid="cancel-btn"]').exists()).toBe(true)
    // pending: status-badge 0 (statusBadge map 不含 pending) + readonly-hint 0 + meta-info 0
    expect(wrapper.findAll('[data-testid="status-badge"]')).toHaveLength(0)
    expect(wrapper.findAll('[data-testid="readonly-hint"]')).toHaveLength(0)
    expect(wrapper.findAll('[data-testid="meta-info"]')).toHaveLength(0)
  })

  test('resolved decision renders status-badge (status-resolved) + meta-info, no buttons, no readonly-hint', () => {
    const wrapper = mount(DecisionCard, {
      props: {
        decision: {
          decision_id: 'd2',
          status: 'resolved',
          options: ['选项 A', '选项 B'],
          resolved_by: '主公',
          resolution: '选项 A',
          resolved_at: '2026-06-08T10:00:00',
          reason: 'OK',
        } as Decision,
      },
    })

    // resolved: 无 button (pending 才渲染)
    expect(wrapper.findAll('[data-testid="option-btn"]')).toHaveLength(0)
    expect(wrapper.findAll('[data-testid="defer-btn"]')).toHaveLength(0)
    expect(wrapper.findAll('[data-testid="cancel-btn"]')).toHaveLength(0)
    // resolved: status-badge 1 (含 status-resolved class) + readonly-hint 1
    // (走 v-else L59 + 三元 fallback 'resolved' ? '解决' : ...) + meta-info 1
    const statusBadge = wrapper.find('[data-testid="status-badge"]')
    expect(statusBadge.exists()).toBe(true)
    expect(statusBadge.classes()).toContain('status-resolved')
    const readonlyHint = wrapper.find('[data-testid="readonly-hint"]')
    expect(readonlyHint.exists()).toBe(true)
    expect(readonlyHint.text()).toContain('解决')
    const metaInfo = wrapper.find('[data-testid="meta-info"]')
    expect(metaInfo.exists()).toBe(true)
    expect(metaInfo.text()).toContain('解决人')
    expect(metaInfo.text()).toContain('选项 A')
    expect(metaInfo.text()).toContain('时间')
  })

  test('cancelled decision renders readonly-hint + meta-info, no buttons, no status-badge', () => {
    const wrapper = mount(DecisionCard, {
      props: {
        decision: {
          decision_id: 'd3',
          status: 'cancelled',
          resolved_by: '主公',
          resolved_at: '2026-06-08T11:00:00',
          reason: '实验失败',
        } as Decision,
      },
    })

    // cancelled: 无 button
    expect(wrapper.findAll('[data-testid="option-btn"]')).toHaveLength(0)
    expect(wrapper.findAll('[data-testid="defer-btn"]')).toHaveLength(0)
    expect(wrapper.findAll('[data-testid="cancel-btn"]')).toHaveLength(0)
    // cancelled: status-badge 1 (含 status-cancelled class, cancelled 在 map L110)
    // + readonly-hint 1 (走 v-else L59 + 三元 fallback 'cancelled' ? '取消' : '推迟')
    const statusBadge = wrapper.find('[data-testid="status-badge"]')
    expect(statusBadge.exists()).toBe(true)
    expect(statusBadge.classes()).toContain('status-cancelled')
    const readonlyHint = wrapper.find('[data-testid="readonly-hint"]')
    expect(readonlyHint.exists()).toBe(true)
    expect(readonlyHint.text()).toContain('取消')
    // cancelled: meta-info 走 cancelled 分支 ("取消原因" 而非 "推迟原因")
    const metaInfo = wrapper.find('[data-testid="meta-info"]')
    expect(metaInfo.exists()).toBe(true)
    expect(metaInfo.text()).toContain('取消原因: 实验失败')
  })
})
```

(3 tests 100% 走 inner testid,0 触发全局 event / window.prompt / window.confirm)

- [ ] **Step 1.2: 跑 spec,验证 RED (testid 缺 → 0 触发断言)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
npx vitest run tests/unit/decision-card.spec.ts
```

期望: 3 tests **全部 FAIL** (因为 DecisionCard 还没加 6 testid,`find('[data-testid="option-btn"]')` 返 0,`findAll` 返空数组,`toHaveLength(3)` 抛错)。

- [ ] **Step 1.3: 加 6 testid 到 DecisionCard.vue (GREEN)**

修改 `src/components/DecisionCard.vue`,6 处精确 attribute 注入,只加 `data-testid="..."` 不断其他:

| 行 | 改法 |
|---|---|
| L15 | `<span v-if="statusBadge" class="status-badge" :class="statusBadge.cls">` → 加 `data-testid="status-badge"` (3 attribute 形) |
| L34 | `<button v-for="opt in ..." class="option-btn pixel-border" :disabled="busy" @click="onResolve(opt)">` → 加 `data-testid="option-btn"` |
| L44 | `<button class="action-btn defer-btn pixel-border" :disabled="busy" @click="onDefer">` → 加 `data-testid="defer-btn"` |
| L51 | `<button class="action-btn cancel-btn pixel-border" :disabled="busy" @click="onCancel">` → 加 `data-testid="cancel-btn"` |
| L60 | `<span class="readonly-hint">🔒 此决策已...</span>` → 加 `data-testid="readonly-hint"` |
| L65 | `<div v-if="metaInfo" class="meta-info">` → 加 `data-testid="meta-info"` |

**示例 diff (status-badge L15, 6 处同形)**:
```vue
<!-- 前 -->
<span
  v-if="statusBadge"
  class="status-badge"
  :class="statusBadge.cls"
>{{ statusBadge.label }}</span>
<!-- 后 -->
<span
  v-if="statusBadge"
  class="status-badge"
  data-testid="status-badge"
  :class="statusBadge.cls"
>{{ statusBadge.label }}</span>
```

(0 改 class / 0 改 :class / 0 改 v-if / 0 改 text / 0 改其他 attribute。template attribute 顺序按 Phase 8.31 spec "stable 顺序" 原则: class / data-testid / 动态 attr / event)

- [ ] **Step 1.4: 跑 spec,验证 GREEN (3 tests 全过)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
npx vitest run tests/unit/decision-card.spec.ts
```

期望: `Tests  3 passed (3)`。如果仍 FAIL,检查:
- testid 拼写是否完全匹配 (`option-btn` not `option_btn`)
- 改的 attribute 行号是否对 (L15/L34/L44/L51/L60/L65,行号基于 Phase 8.31 后)
- Vue 编译是否报错 (`vue/compiler-sfc` v-if 三元在 readonly-hint 里的字符串拼接是否影响 testid)

- [ ] **Step 1.5: 跑全套 vitest,确认 0 改 14 现有 spec**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
npx vitest run
```

期望: `Test Files  15 passed (15)` (14 旧 + 1 new decision-card),`Tests  60 passed (60)` (57 baseline + 3 new)。如果 14 旧 spec 任何 1 个 FAIL,revert L15/L34/L44/L51/L60/L65 testid,调查根因。

- [ ] **Step 1.6: 跑 pytest,确认 baseline 不变**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pytest -q 2>&1 | tail -3
```

期望: `2262 passed, 0 failed, 27 skipped` (frontend-only 0 改 backend)。

- [ ] **Step 1.7: 1 feat commit**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
git add novel-factory/dashboard/frontend/src/components/DecisionCard.vue \
        novel-factory/dashboard/frontend/tests/unit/decision-card.spec.ts
git status --short
# 期望: A  2 files (DecisionCard.vue + decision-card.spec.ts)
git commit -m "feat(dashboard): phase 8.32 — DecisionCard 6 inner testid + 1 spec

Phase 8.31 收尾 followup: 严格遵守 'spec 需才加' 原则,伴生新 vitest spec 驱动
testid 范围,非纯加 attribute 0 spec 验证的 YAGNI 风险.

DecisionCard 6 inner testid (100% spec 需):
- option-btn (L34, v-for 多按钮 share)
- defer-btn (L44) / cancel-btn (L51) — pending action 按钮
- status-badge (L15) — resolved/deferred/cancelled 才显 (Phase 6.6.B map)
- readonly-hint (L60) — cancelled 走 fallback (Phase 6.6.B 设计)
- meta-info (L65) — 3 status 都会显 (resolved/cancelled 走 '解决人/选项/时间/原因',
  cancelled 走 '取消原因' 分支)

1 new spec (decision-card.spec.ts, 3 tests):
- pending: 3 option-btn + defer-btn + cancel-btn, 0 status-badge/0 readonly-hint/0 meta-info
- resolved: 0 buttons, 1 status-badge (含 status-resolved class), 1 meta-info (含 '解决人')
- cancelled: 0 buttons, 0 status-badge (cancelled 不在 map), 1 readonly-hint (含 '取消'),
  1 meta-info (含 '取消原因: 实验失败')

不改 (历史 lock):
- 0 改 33 现有 testid (Phase 8.13 + 8.31 累计)
- 0 改 14 现有 vitest spec
- 0 改 setup.ts / vitest.config.ts
- 0 改 backend / agent_system / composables / router
- 0 改 8 historical spec/plan docs \`:NNN\`
- 0 改 CLAUDE.md / pyproject.toml / pytest.ini / vitest.config.ts
- 0 改 .state/*.db (gitignored), 0 .env 改, 0 API key 泄漏
- 0 force-push, 0 --no-verify, 0 Co-Authored-By

Baseline:
- vitest: 14 spec / 57 tests → 15 spec / 60 tests (+1 spec +3 tests)
- pytest: 2262 / 27 skipped 维持 (frontend-only)

后续 (Phase 8.32 Task 2): WorkflowGraph 4 inner testid (zoom-in/out semantic
命名 Rule 2 例外) + workflow-graph.spec.ts (4 tests, mermaid vi.mock)"
```

---

## Task 2: WorkflowGraph 4 inner testid + workflow-graph.spec.ts (1 feat commit)

**Files:**
- Modify: `src/components/WorkflowGraph.vue:19/34/35/36` (4 attribute 注入)
- Create: `tests/unit/workflow-graph.spec.ts` (NEW, ~110L, 4 tests)

- [ ] **Step 2.1: 写 workflow-graph.spec.ts (RED 准备)**

创建 `tests/unit/workflow-graph.spec.ts`,内容如下 (mermaid mock 走 spec 内部 `vi.mock`,不污染全局 setup.ts):

```ts
// tests/unit/workflow-graph.spec.ts — Phase 8.32
// WorkflowGraph 4 inner testid 验证 (reset-zoom-btn / zoom-display /
// zoom-in-btn / zoom-out-btn). 4 状态 (loading/error/svg/empty) 留 followup
// (需 mermaid 异步 + visual regression / E2E 触发).
//
// 契约:
//   - vi.mock('mermaid') 局部 stub, render() 返 { svg: '<svg>fake</svg>' }
//     立即 resolve, 让 zoom-controls 容器 (v-if='svg') 渲染.
//   - Test 1: click zoom-in-btn 后 zoom-display 显示 "120%" (1.0 + 0.2 = 1.2).
//   - Test 2: click zoom-out-btn 后 zoom-display 显示 "80%" (1.0 - 0.2 = 0.8).
//   - Test 3: click zoom-in-btn 2 次 (140%) → click reset-zoom-btn → "100%".
//   - Test 4: click zoom-in-btn 6 次 (1.0→1.2→1.4→1.6→1.8→1.999...→2.0, Math.min(2, x)
//     限 2.0; 6 次而非 5 次因 JS float 1.0+0.2*5=1.9999999999999998 drift, 6 次才能让
//     Math.min(2, 1.999... + 0.2) = 2.0 精确 clamp) → zoom-display "200%" + zoom-in-btn disabled.

import { describe, test, expect, vi, beforeEach } from 'vitest'
import { mount, flushPromises } from '@vue/test-utils'
import WorkflowGraph from '../../src/components/WorkflowGraph.vue'

// Phase 8.32: 局部 vi.mock mermaid, render 立即返 fake svg 让 zoom-controls
// (v-if='svg') 渲染. 0 改 setup.ts, 0 jsdom canvas/svg 风险.
vi.mock('mermaid', () => ({
  default: {
    initialize: vi.fn(),
    render: vi.fn().mockResolvedValue({ svg: '<svg>fake</svg>' }),
  },
}))

describe('WorkflowGraph zoom controls inner sub-element testid (Phase 8.32)', () => {
  let wrapper: ReturnType<typeof mount>

  beforeEach(async () => {
    wrapper = mount(WorkflowGraph, {
      props: {
        mermaid: 'graph TD; A-->B',
        workflowName: 'test',
      },
    })
    // 等 mermaid.render() async resolve 让 svg.value 设定 + zoom-controls 渲染
    await flushPromises()
  })

  test('zoom-in-btn click increases zoom display to 120%', async () => {
    // 初始 zoom = 1.0
    expect(wrapper.find('[data-testid="zoom-display"]').text()).toBe('100%')
    await wrapper.find('[data-testid="zoom-in-btn"]').trigger('click')
    // zoomIn(): Math.min(2, 1.0 + 0.2) = 1.2 → "120%"
    expect(wrapper.find('[data-testid="zoom-display"]').text()).toBe('120%')
  })

  test('zoom-out-btn click decreases zoom display to 80%', async () => {
    expect(wrapper.find('[data-testid="zoom-display"]').text()).toBe('100%')
    await wrapper.find('[data-testid="zoom-out-btn"]').trigger('click')
    // zoomOut(): Math.max(0.4, 1.0 - 0.2) = 0.8 → "80%"
    expect(wrapper.find('[data-testid="zoom-display"]').text()).toBe('80%')
  })

  test('reset-zoom-btn returns to 100% after zoom in twice', async () => {
    // click 2 次: 1.0 → 1.2 → 1.4
    await wrapper.find('[data-testid="zoom-in-btn"]').trigger('click')
    await wrapper.find('[data-testid="zoom-in-btn"]').trigger('click')
    expect(wrapper.find('[data-testid="zoom-display"]').text()).toBe('140%')
    // click reset-zoom-btn → 1.0
    await wrapper.find('[data-testid="reset-zoom-btn"]').trigger('click')
    expect(wrapper.find('[data-testid="zoom-display"]').text()).toBe('100%')
  })

  test('zoom-in-btn disabled at max 2x (after 6 clicks — JS float drift requires 6 not 5)', async () => {
    // click 6 次: 1.0 → 1.2 → 1.4 → 1.6 → 1.8 → 1.999... → 2.0 (Math.min(2, x) 限 2.0)
    // 6 次而非 5 次: JS float 1.0 + 0.2*5 = 1.9999999999999998 (drift), 需 6 次
    // 才能让 Math.min(2, 1.999... + 0.2) = 2.0 精确 clamp.
    for (let i = 0; i < 6; i++) {
      await wrapper.find('[data-testid="zoom-in-btn"]').trigger('click')
    }
    // 第 6 次后 zoom = 2.0 (exact), display "200%"
    expect(wrapper.find('[data-testid="zoom-display"]').text()).toBe('200%')
    // :disabled="zoom >= 2" 触发后, button 显 disabled attribute
    const zoomInBtn = wrapper.find('[data-testid="zoom-in-btn"]')
    expect(zoomInBtn.attributes('disabled')).not.toBeUndefined()
  })
})
```

(4 tests 100% 走 inner testid,mermaid mock 局部化,0 触发 setup.ts 全局)

- [ ] **Step 2.2: 跑 spec,验证 RED (testid 缺 → 4 FAIL)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
npx vitest run tests/unit/workflow-graph.spec.ts
```

期望: 4 tests **全部 FAIL** (因为 WorkflowGraph 还没加 4 testid,`find('[data-testid="zoom-display"]')` 返 null,`.text()` 抛错 `Cannot read properties of null`)。

- [ ] **Step 2.3: 加 4 testid 到 WorkflowGraph.vue (GREEN)**

修改 `src/components/WorkflowGraph.vue`,4 处精确 attribute 注入:

| 行 | 改法 | 规则 |
|---|---|---|
| L19 | `<button class="reset-zoom-btn pixel-border" @click="resetZoom">⤢ 重置缩放</button>` → 加 `data-testid="reset-zoom-btn"` | Rule 1 (class 镜像) |
| L34 | `<button class="zoom-btn pixel-border" @click="zoomIn" :disabled="zoom >= 2">＋</button>` → 加 `data-testid="zoom-in-btn"` | **Rule 2** (semantic) |
| L35 | `<span class="zoom-display">{{ (zoom * 100).toFixed(0) }}%</span>` → 加 `data-testid="zoom-display"` | Rule 1 |
| L36 | `<button class="zoom-btn pixel-border" @click="zoomOut" :disabled="zoom <= 0.4">－</button>` → 加 `data-testid="zoom-out-btn"` | **Rule 2** (semantic) |

**示例 diff (zoom-in L34, semantic 命名例外)**:
```vue
<!-- 前 -->
<button class="zoom-btn pixel-border" @click="zoomIn" :disabled="zoom >= 2">＋</button>
<!-- 后 -->
<button class="zoom-btn pixel-border" data-testid="zoom-in-btn" @click="zoomIn" :disabled="zoom >= 2">＋</button>
```

(0 改 class / 0 改 @click / 0 改 :disabled / 0 改 inner text `＋` / 0 改 reset-zoom 跟 zoom-display)

**Rule 2 例外说明** (在 commit body 解释,跟 spec §1 一致): `zoom-btn` class 在 2 个 sibling button 共享 (zoomIn + zoomOut),无 parent wrapper 可走 scoped find,只能 semantic 命名区分。

- [ ] **Step 2.4: 跑 spec,验证 GREEN (4 tests 全过)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
npx vitest run tests/unit/workflow-graph.spec.ts
```

期望: `Tests  4 passed (4)`。如果仍 FAIL,检查:
- testid 拼写 (`zoom-in-btn` / `zoom-out-btn` / `reset-zoom-btn` / `zoom-display`,非 `zoomBtn` 等 camelCase)
- mermaid mock 是否在 file 顶部 (不在 `describe` 内)
- `flushPromises()` 位置 (`beforeEach` 末尾)
- `attributes('disabled')` 在 Vue 3 渲染 `:disabled="true"` 时返 `""` (truthy),`!== undefined` 成立

- [ ] **Step 2.5: 跑全套 vitest,确认 0 改 14 现有 spec**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
npx vitest run
```

期望: `Test Files  16 passed (16)` (15 旧 + 1 new workflow-graph, 加上 Task 1 的 decision-card),`Tests  64 passed (64)` (60 + 4 new)。如果任何 15 旧 spec FAIL,revert L19/L34/L35/L36 testid,调查根因。

- [ ] **Step 2.6: 跑 pytest,确认 baseline 不变**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pytest -q 2>&1 | tail -3
```

期望: `2262 passed, 0 failed, 27 skipped` (frontend-only)。

- [ ] **Step 2.7: grep 验证 10 new testid 在位 + 0 改 33 现有**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend

# 10 new testid (DecisionCard 6 + WorkflowGraph 4)
grep -rEn 'data-testid="(option-btn|defer-btn|cancel-btn|status-badge|readonly-hint|meta-info|reset-zoom-btn|zoom-in-btn|zoom-out-btn|zoom-display)"' src/ --include="*.vue" | wc -l
# 期望: 10

# 0 改 33 现有 testid (总 named testid 应 = 33 + 10 = 43)
grep -rEn 'data-testid="' src/ --include="*.vue" -h | grep -oE 'data-testid="[^"]+"' | sort -u | wc -l
# 期望: 43 (sort -u 去重 distinct testid 名)
```

- [ ] **Step 2.8: grep 验证 0 改 setup.ts / vitest.config.ts**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
git diff tests/unit/setup.ts vitest.config.ts
# 期望: 0 输出 (无 diff)
```

- [ ] **Step 2.9: 1 feat commit**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
git add novel-factory/dashboard/frontend/src/components/WorkflowGraph.vue \
        novel-factory/dashboard/frontend/tests/unit/workflow-graph.spec.ts
git status --short
# 期望: A  2 files (WorkflowGraph.vue + workflow-graph.spec.ts)
git commit -m "feat(dashboard): phase 8.32 — WorkflowGraph 4 inner testid + 1 spec

Phase 8.32 Task 2 (跟 Task 1 commit 平行): DecisionCard 6 testid + 1 spec 已在
前 commit 落地 (15 spec / 60 tests),本 commit 补 WorkflowGraph 4 testid + 1
spec (16 spec / 64 tests).

WorkflowGraph 4 inner testid (100% spec 需):
- reset-zoom-btn (L19, Rule 1 class 镜像)
- zoom-display (L35, Rule 1 class 镜像)
- zoom-in-btn (L34, **Rule 2 semantic 命名例外**)
- zoom-out-btn (L36, **Rule 2 semantic 命名例外**)

Rule 2 例外说明: zoom-btn class 在 2 个 sibling button 共享 (zoomIn + zoomOut),
无 parent wrapper 可走 scoped find (跟 Phase 8.31 sidebar-cost-tier-row 内层
span 不同,后者有 tier-row parent wrapper),只能 semantic 命名区分. 跟 spec §1
Rule 2 + zoom-in/out semantic 例外决策一致.

1 new spec (workflow-graph.spec.ts, 4 tests):
- zoom-in: 1.0 → 1.2 → display '120%'
- zoom-out: 1.0 → 0.8 → display '80%'
- reset-zoom: 2 次 zoom-in (140%) → reset → '100%'
- max 2x: 6 次 zoom-in (200%, JS float 1.0+0.2*5=1.999... drift → 需 6 次
  才能让 Math.min(2, ...) 精确 clamp 到 2.0) + zoom-in-btn disabled
  (zoom >= 2 触发 :disabled='zoom >= 2')

mermaid mock 局部化: vi.mock('mermaid') 在 spec file 顶部, render() 立即
.resolve { svg: '<svg>fake</svg>' } 让 zoom-controls (v-if='svg') 渲染.
**0 改 setup.ts** (跟 Phase 8.30b vi.mock 局部模式一致).

不改 (历史 lock):
- 0 改 33 + 6 (Task 1) = 39 现有 testid
- 0 改 15 现有 vitest spec (14 Phase 8.31 + 1 Task 1 decision-card)
- 0 改 setup.ts / vitest.config.ts
- 0 改 backend / agent_system / composables / router
- 0 改 8 historical spec/plan docs \`:NNN\`
- 0 改 CLAUDE.md / pyproject.toml / pytest.ini / vitest.config.ts
- 0 改 .state/*.db (gitignored), 0 .env 改, 0 API key 泄漏
- 0 force-push, 0 --no-verify, 0 Co-Authored-By

Baseline:
- vitest: 15 spec / 60 tests (Task 1) → 16 spec / 64 tests (本 commit, +1 spec +4 tests)
- pytest: 2262 / 27 skipped 维持 (frontend-only)

后续 (Phase 8.32 Task 3): push origin + phases-8-dashboard.md memory 同步."
```

---

## Task 3: Push origin + memory 同步 (1 chore)

**Files:**
- Modify: `~/.claude/projects/.../memory/phases-8-dashboard.md` (append Phase 8.32 entry ~20L, **不在项目 git 内**)

- [ ] **Step 3.1: 2 commit 推 origin**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
git push origin master
```

期望: `456360b..HEAD  master -> master` (N commits, 含 Phase 8.32 spec + Task 1 feat + Task 2 feat,共 3 commit 落 origin)。

如果 push fail,**STOP 报告错误 verbatim**,不 retry --force 或 workaround。

- [ ] **Step 3.2: 验证 push 落 origin**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen

# 5 new commits on origin (3 Phase 8.32 + 5 Phase 8.31, 共 8 ahead of 456360b)
git log origin/master --oneline -8

# local = origin (sync check)
git rev-parse HEAD origin/master
# 期望: 2 行相同 SHA
```

- [ ] **Step 3.3: 同步 phases-8-dashboard.md (in-place, 无 commit)**

修改 `/home/ailearn/.claude/projects/-home-ailearn-projects-AI-Incursion-domains-IP---projects-LingWen/memory/phases-8-dashboard.md`:
- H1 改 `Phase 8.16-8.31` → `Phase 8.16-8.32` (跟 Phase 8.31 一致 mode drift 处理)
- H2 改 `从 [[MEMORY]] 拆分过来的 Phase 8.16-8.31 详细条目` → `Phase 8.16-8.32 详细条目`
- 第 3 行末尾加 `+ DecisionCard/WorkflowGraph inner sub-element testid` (跟 Phase 8.30b/8.31 一致 narrative style)
- 文件末尾 append Phase 8.32 entry (跟 Phase 8.30b/8.31 同 density 风格,~20L):

```markdown
## Phase 8.32 (2026-06-08)

data-testid 统一 (10 inner sub-element + 2 new spec) — 收尾 Phase 8.31 留 followup: DecisionCard (11 候选) / WorkflowGraph (12 候选) inner sub-element 0 testid, 14 spec 0 触及 (旧 ceremonial Playwright spec 删后); 严格 "spec 需才加" 原则, 伴生 2 new vitest spec 驱动 testid 范围. **6 DecisionCard inner testid** (100% spec 需): option-btn (L34 v-for 多, spec findAll.length=3) / defer-btn (L44) / cancel-btn (L51) (pending 才显) / status-badge (L15, resolved 含 status-resolved class) / readonly-hint (L60, cancelled 走 fallback) / meta-info (L65, 3 status 都显, cancelled 走 "取消原因" 分支). **4 WorkflowGraph inner testid** (100% spec 需): reset-zoom-btn (L19, Rule 1 class 镜像) / zoom-display (L35) / **zoom-in-btn + zoom-out-btn (L34/L36, Rule 2 semantic 例外)** — zoom-btn class 在 2 sibling button 共享无 parent wrapper 走 scoped find, 只能 semantic 命名区分, 跟 spec §1 Rule 2 决策一致. **2 new vitest spec** (跟 Phase 8.30b 同 pattern, component-level mount + vi.mock 局部化): decision-card.spec.ts (3 tests, 3 status fixture × button/badge/hint/meta-info 4 维断言) + workflow-graph.spec.ts (4 tests, vi.mock('mermaid') 局部 stub render 返 fake svg 让 zoom-controls (v-if=svg) 渲染, 验 zoom 数学: 1.0→1.2→1.4→2.0 disable + reset 1.0 + 1.0→0.8 边界). **commit sequence**: ecfc958 docs(spec) / Task 1 feat / Task 2 feat 推送 origin/master. **测试总**: vitest 14 spec / 57 tests baseline → 16 spec / 64 tests (+2 spec +7 tests), pytest 2262 / 27 skipped 维持 (0 改 backend). **verification commands**: `grep -rEn 'data-testid="(option-btn|...|zoom-display)"' src/ --include="*.vue" | wc -l` = 10 / `grep -rEn 'data-testid="' src/ --include="*.vue" -h | grep -oE 'data-testid="[^"]+"' | sort -u | wc -l` = 43 (33+10 named) / `git diff tests/unit/setup.ts vitest.config.ts` = 0 改 / `git rev-parse HEAD origin/master` = 2 行同 SHA / `npx vitest run` = 16/64. **0 改**: 33 + 6 (Task 1) = 39 旧 testid (Phase 8.31 加的 8 个 + Task 1 加的 6 个 0 改) / 14 旧 spec (Phase 8.31 后 14 spec 0 改) / backend / composables / router / setup.ts / vitest.config.ts / CLAUDE.md / pyproject.toml / pytest.ini / .state/*.db / .env / 8 historical spec/plan docs `:NNN` 行号. **out of scope (有 followup)**: DecisionCard kind-badge / priority-badge / context / prompt / error-text / node-id testid (6 候选, 无 spec 需) / WorkflowGraph loading-state / error-state / graph-container / empty-state / svg-wrapper / error-title / error-detail / graph-title / graph-header testid (9 候选, 4 状态 spec 留 followup 需 mermaid 异步) / DecisionsPage/WorkflowsPage page-level spec (留 followup) / 7 kind spec (YAGNI) / data-testid lint rule (留 Phase 8.33) / store registry / byTestid helper / class 镜像清理 (YAGNI). 后续: Phase 8.33 data-testid lint rule (强制 root 必含, 43 testid 后能验证 lint 实际价值) / Phase 8.34 store registry (YAGNI) / Phase 8.35 byTestid helper (DRY 5+ 重复, 16 spec 不达阈值) / Phase 8.36 class 镜像层 (risk > value) / Phase 8.37 WorkflowGraph 4 状态单独 spec (mermaid 异步) / Phase 8.38 DecisionCard 7 kind spec (YAGNI) / Phase 8.39 DecisionsPage/WorkflowsPage page-level spec / Phase 9 跨卷涟漪 / MEMORY.md 拆 33+ entries 到 phases.md topic (本 entry 后变 33+ entries, 必拆 followup).
```

(0 改 MEMORY.md 本身, 跟 Phase 8.30b/8.31 一致; memory file 不进项目 git, 直接编辑更新即可)

- [ ] **Step 3.4: 验证 phases-8-dashboard.md 行数**

```bash
wc -l /home/ailearn/.claude/projects/-home-ailearn-projects-AI-Incursion-domains-IP---projects-LingWen/memory/phases-8-dashboard.md
# 期望: 54 + 5 = ~59L (Phase 8.31 entry ~6L, Phase 8.32 entry ~5L, 都按 ~5L/entry 计)
# 实际: 54 + ~20 = ~74L (Phase 8.32 entry 详细版, 但仍 < 100 cap)
```

- [ ] **Step 3.5: 最终 grep 验证**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend

# 10 new testid 在位
grep -rEn 'data-testid="(option-btn|defer-btn|cancel-btn|status-badge|readonly-hint|meta-info|reset-zoom-btn|zoom-in-btn|zoom-out-btn|zoom-display)"' src/ --include="*.vue" | wc -l
# 期望: 10

# 0 改 setup.ts / vitest.config.ts
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
git diff tests/unit/setup.ts vitest.config.ts
# 期望: 0 输出

# git status clean
git status
# 期望: nothing to commit, working tree clean
```

- [ ] **Step 3.6: 跑 ruff / lint 验证 (跟 Phase 8.30b/8.31 一致 0 触发)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen
ruff check novel-factory/ 2>&1 | tail -3
# 期望: 0 issues
```

(本 spec 全 frontend, ruff 应仍 0,因为后端 0 改)

---

## Critical files

### Created
- `novel-factory/docs/superpowers/plans/2026-06-08-phase8.32-inner-testid.md` (本文, ~600L)
- `novel-factory/dashboard/frontend/tests/unit/decision-card.spec.ts` (~80L, 3 tests)
- `novel-factory/dashboard/frontend/tests/unit/workflow-graph.spec.ts` (~110L, 4 tests + mermaid mock)

### Modified (注入点 only)
- `novel-factory/dashboard/frontend/src/components/DecisionCard.vue` (6 attribute: L15/L34/L44/L51/L60/L65)
- `novel-factory/dashboard/frontend/src/components/WorkflowGraph.vue` (4 attribute: L19/L34/L35/L36)
- `~/.claude/projects/.../memory/phases-8-dashboard.md` (append Phase 8.32 entry ~20L, 不进 git)

### Referenced / 复用
- Phase 8.31 spec `2026-06-08-phase8.31-data-testid-unification-design.md` (kebab-case 镜像 class 约定)
- Phase 8.30b spec 模式 (`vi.mock` 局部化 / component-level mount / Chinese header comment)
- `tests/unit/setup.ts` (echarts stub, mermaid mock 走 spec 内部不污染)
- `vitest.config.ts` (exclude `tests/e2e/**`, 本 phase 0 改)

### NOT modified
- `infra/agent_system/` 任何文件
- `infra/ai_service/cost_tracker.py` / `cost_persistence.py`
- `dashboard/protocols.py` / `dashboard/app.py` / FastAPI backend
- 16 现有 vitest spec (14 旧 + 1 Task 1 + 1 Task 2 自身 0 改)
- 33 + 6 (Task 1) = 39 现有 data-testid
- `tests/unit/setup.ts` / `vitest.config.ts` / `pyproject.toml` / `pytest.ini` / `CLAUDE.md`
- 8 historical spec/plan docs (`:NNN` 行号不动)
- `infra/.state/cost_tracker.db` (NOT committed, gitignored)
- `MEMORY.md` 本身 (跟 Phase 8.30b/8.31 一致)

---

## Out of scope (后续 phases)

- **Phase 8.33**: data-testid lint rule (强制 root 必含 testid) — 33+10=43 testid 后能验证 lint 实际价值
- **Phase 8.34**: pinia store testid registry (YAGNI)
- **Phase 8.35**: spec `byTestid()` helper (DRY 5+ 重复触发, 16 spec 不达阈值)
- **Phase 8.36**: remove `class=` mirror (反向问题)
- **Phase 8.37**: WorkflowGraph 4 状态 (loading/error/svg/empty) 单独 spec — 需 mermaid 异步 + visual regression / E2E 触发时再开
- **Phase 8.38**: DecisionCard 7 kind 单独 spec — YAGNI
- **Phase 8.39**: DecisionsPage / WorkflowsPage page-level spec — 留触发时
- **Phase 9**: 跨卷涟漪 (独立大方向)
- **MEMORY.md line 8 typo "27 skipped"**: 无 typo, 跟 Phase 8.30b/8.31 一致, 留 v9.16 修
- **CI filter 收紧**: Phase 8.3 LOW 标, 留 followup
- **dashboard.spec.ts L81 stale comment** ("StatCard 组件无 data-testid, 用 class 选"): 留 Phase 8.32 任何触碰 dashboard.spec.ts 的 commit 顺手清理 (但本 phase 0 触碰, 留 Phase 8.33+)

---

## 风险 + 缓解

| 风险 | 缓解 |
|---|---|
| 改坏 16 spec 已有 testid 断言 | 0 改 33+6=39 现有 testid, 10 个新 testid 镜像 stable class (Rule 1 8 处 + Rule 2 2 处), 跟现有命名 0 冲突 |
| mermaid mock 在 setup.ts 漏 import | 走 spec 内部 `vi.mock('mermaid', ...)`, **不**改 setup.ts, scope 局部化 |
| mermaid 真实 render 走 async, jsdom 缺 canvas / svg 注入可能 timeout | mock 立即 resolve `'fake svg'`, 0 走真实 mermaid.render, 0 jsdom SVG 风险 |
| `attributes('disabled')` API 在 Vue 3 / @vue/test-utils v2 变 | 跟 Phase 8.30b `ws-disconnected-indicator.spec.ts` pattern 一致, 先跑确认; mock mermaid 让 zoom-controls 渲染后, button `:disabled="zoom >= 2"` 触发后 HTML 渲染 `disabled=""` (空字符串 truthy) |
| Task 1 / Task 2 旧 spec regression | Task 1 / Task 2 各自 step 5 跑 `npx vitest run` 全套, 期望 16/64, 任何 FAIL 立即 revert 调查根因 |
| Phase 8.32 entry 让 MEMORY.md 超 200 行 | 接受再超 1-5 行 (33+ entries), 留 followup phase 拆 phases.md topic (跟 Phase 8.30b/8.31 一致) |
| 0 改 33 + 6 (Task 1) = 39 旧 testid 漏 0 改 | Task 1 step 1.5 + Task 2 step 2.5 跑全套 vitest 验证 baseline, 期望 16/64 (含 1 Task 1 + 1 Task 2 自身 0 改) |
| 后端 / agent_system 误改 | 0 改 backend (本 spec 全 frontend only), 16 spec 全 frontend |
| API key 泄漏 | 0 provider 改, 0 .env 改, 0 ANTHROPIC_API_KEY 引用, 跟 Phase 8.30b/8.31 一致 |

---

## 完成定义 (DoD)

- [ ] spec doc `docs/superpowers/specs/2026-06-08-phase8.32-inner-testid-design.md` 创建 + commit (`ecfc958`)
- [ ] plan doc `docs/superpowers/plans/2026-06-08-phase8.32-inner-testid.md` 创建 (本文, ~600L)
- [ ] Task 1: DecisionCard 6 inner testid + decision-card.spec.ts (3 tests) 落地 + 1 feat commit
- [ ] Task 2: WorkflowGraph 4 inner testid + workflow-graph.spec.ts (4 tests) 落地 + 1 feat commit
- [ ] Task 3: 3 commit 推 origin/master + phases-8-dashboard.md append Phase 8.32 entry
- [ ] vitest 16 spec files / 64 tests 全过 (14 baseline + 2 new + 7 tests)
- [ ] pytest 2262 / 27 skipped baseline 不变
- [ ] ruff 0
- [ ] grep 验证 10 new testid 在位
- [ ] grep 验证 0 改 33 现有 testid
- [ ] grep 验证 0 改 setup.ts / vitest.config.ts
- [ ] git log 3 commits (1 spec + 2 feat) 落 origin
- [ ] 0 production code 行为破 (纯 attribute 加, 0 logic 改)
- [ ] 0 改 backend / agent_system / composables / router
- [ ] 0 改 33 现有 data-testid
- [ ] 0 改 8 historical spec/plan docs `:NNN`
- [ ] 0 改 CLAUDE.md / pyproject.toml / pytest.ini / vitest.config.ts
- [ ] 0 改 .state/*.db (gitignored)
- [ ] 0 force-push, 0 --no-verify, 0 Co-Authored-By
- [ ] 0 .env 改, 0 API key 泄漏
- [ ] MEMORY.md 不动 (跟 Phase 8.30b/8.31 一致, phases-8-dashboard.md append 1 entry)
- [ ] DecisionCard kind-badge/priority-badge/context/prompt/error-text/node-id testid 留 followup
- [ ] WorkflowGraph 4 状态 + 7 kind + page-level spec 留 followup

---

## 完成后下一步 (后续 phases)

- **Phase 8.33**: data-testid lint rule (强制 src/components/*.vue root 必含 data-testid, 引 `@vue/test-utils/data-testid` rule 或 ESLint vue custom rule) — 33+10=43 testid 后能验证 lint 实际价值
- **Phase 8.34**: pinia store 中心化 testid registry (e.g. `TESTID.statCard` 走常量, IDE 跳转) — YAGNI, 43 testid 仍人类可读
- **Phase 8.35**: spec `byTestid()` helper (`byTestid('stat-card')` 替 `[data-testid="stat-card"]`) — DRY 触发要 5+ 重复, 16 spec 不达阈值
- **Phase 8.36**: remove `class=` mirror on testid element — risk > value, CSS 仍要 class, 反向问题
- **Phase 8.37**: WorkflowGraph 4 状态 (loading/error/svg/empty) 单独 spec + setup.ts mermaid stub
- **Phase 8.38**: DecisionCard 7 kind 单独 spec
- **Phase 8.39**: DecisionsPage / WorkflowsPage page-level spec
- **Phase 9**: 跨卷涟漪 (独立大方向)
- **MEMORY.md line 8 typo "27 skipped"**: 无 typo, 跟 Phase 8.30b/8.31 一致, 留 v9.16 修
- **CI filter 收紧**: Phase 8.3 LOW 标, 留 followup
