# Phase 8.45 UI Completeness Super-phase C — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 收尾 UI completeness 3 items (DecisionCard 3 status testid + OverviewPage e2e 加深 + Playwright runner install + 1 E2E smoke spec). 1:1 跟 Phase 8.43 (Super-phase A) + 8.44 (Super-phase B) 模式, **3 sub-phases / 3 commits 推 origin/master** (Task 1-3 各 1 commit, Task 4 = push + memory sync 0 commit because phases-8-dashboard.md 在 `~/.claude/...` 不在 git repo). 0 --no-verify (3 commit 全 hook ✅). **Plan shape**: 4 sub-tasks (8.45.1 DecisionCard / 8.45.2 OverviewPage / 8.45.3 Playwright runner + 1 e2e smoke / 8.45.4 push + memory sync 0 commit).

**Architecture:** 3 独立 sub-phase, 无相互依赖, 可独立 review + 独立 commit; 0 改 backend Python, 0 改 .env, 0 改 12 SCENARIOS 路由, 0 改 historical spec/plan docs `:NNN` 行号不动. 3 small additive .vue changes: 8.45.1 DecisionCard.vue (1 line + 1 computed) / 8.45.2 OverviewPage.vue (1 small empty fallback template) / 8.45.3 App.vue (1 small ws-status testid). 0 CI integration (Playwright 留 dev opt-in, vitest 仍 primary gate). 0 browser download in this phase.

**Tech Stack:** vitest 4.1.8 + @vue/test-utils 2.4.11 (Phase 8.43 baseline) + @playwright/test ^1.49.0 (NEW, 8.45.3) + jsdom 25 + ESLint 10 + pnpm 9.15.0 (corepack 走 Node 20.20 compat).

**Spec:** `docs/superpowers/specs/2026-06-09-phase8.45-ui-completeness-design.md` (commit `0a8a900`, 310L)

---

## Context

Phase 8.35-8.44 (4 super-phases) 已收尾前端大部分 polish + build tooling + test infra. `phases-8-dashboard.md` L78 out-of-scope 标了 Super-phase C 3 candidates, Phase 8.45 全部收尾.

**Verified baselines (2026-06-09, master @ `0a8a900` post-Phase 8.45 spec commit)**:
- `pnpm vitest run` → 23 spec files / 100 tests passed
- `pnpm vitest run --coverage` → EXIT=0 (Phase 8.44.1 已 enable, threshold 70/50/70/70)
- `pnpm build` → 0 error
- `pnpm lint:all` → EXIT=0
- `pytest -q` (backend) → 2262 passed, 0 failed, 27 skipped
- `ruff check .` → 0 issues
- `data-testid` 数量 → 47 (含 Phase 8.45.1 加的 3 status testid 后估 50)
- `DecisionCard.vue` L13-18 status badge → 1 generic `data-testid="status-badge"` (Phase 8.31)
- `DecisionCard.vue` L110-117 statusBadge computed → 3 entries (resolved/deferred/cancelled), pending = null
- `OverviewPage.vue` L4/L7/L15 testids → `page-title` / `refresh-btn` / `error-banner` (Phase 8.31 已就位)
- `OverviewPage.vue` chapters empty fallback → ❌ 0 显示 (spec 走 R4 verify, 缺则 add 1 small template change)
- `App.vue` ws-status testid → ❌ 0 存在 (spec 走 R5 verify, 缺则 add 1 small data-testid)
- `tests/e2e/*.spec.js` → 3 ceremonial specs (cost-bar-chart-tier-mode / cost-trend-chart-per-tier / sidebar_cost_banner), header 标 "Playwright runner 未装"
- `@playwright/test` devDep → ❌ NOT installed
- `playwright.config.js` → ❌ NOT exists
- `tests/e2e-smoke/smoke.spec.js` → ❌ NOT exists
- corepack `pnpm@9.15.0` → OK (workaround Phase 8.40 Node 20.20 兼容)
- 0 Co-Authored-By trailers in recent commits (跟 CLAUDE.md global config 一致)

---

## File Map

### Created (4)
- `dashboard/frontend/tests/unit/decision-card-status.spec.ts` (~80L, NEW, 8.45.1) — 4 tests 验证 3 status testid
- `dashboard/frontend/tests/unit/dashboard-error-loading.spec.ts` (~140L, NEW, 8.45.2) — 5 tests 覆盖 error/loading/empty/refresh-recovery/partial data
- `dashboard/frontend/playwright.config.js` (~30L, NEW, 8.45.3) — Playwright config
- `dashboard/frontend/tests/e2e-smoke/smoke.spec.js` (~60L, NEW, 8.45.3) — 4 E2E smoke tests
- `docs/superpowers/plans/2026-06-09-phase8.45-ui-completeness.md` (本 plan)

### Modified (8)
- `dashboard/frontend/src/components/DecisionCard.vue` (8.45.1: +1 line + 1 computed, 加 3 status testid)
- `dashboard/frontend/src/pages/OverviewPage.vue` (8.45.2: +1 small empty fallback template, R4 verify 后)
- `dashboard/frontend/src/App.vue` (8.45.3: +1 small ws-status testid, R5 verify 后)
- `dashboard/frontend/package.json` (8.45.3: +1 devDep `@playwright/test` + 1 script `e2e:smoke`)
- `dashboard/frontend/pnpm-lock.yaml` (8.45.3: auto by pnpm install)
- `dashboard/frontend/tests/e2e/cost-bar-chart-tier-mode.spec.js` (8.45.3: 1 line header comment update)
- `dashboard/frontend/tests/e2e/cost-trend-chart-per-tier.spec.js` (8.45.3: 1 line header comment update)
- `dashboard/frontend/tests/e2e/sidebar_cost_banner.spec.js` (8.45.3: 1 line header comment update)
- `~/.claude/projects/.../memory/phases-8-dashboard.md` (8.45.4: H1 + L3 + L4 edit + 8.45 entry append)

### NOT modified (~25)
- 14 .vue in `dashboard/frontend/src/` (除 3 small additive changes)
- 23 vitest spec in `dashboard/frontend/tests/unit/` (除 2 new)
- 5 composables / `api/index.js` / `eslint-rules/*` / `eslint.config.js` / `index.html` / `vite.config.js` / `vitest.config.js`
- `dashboard-frontend-ci.yml` / `test.yml` / `real-llm-tests.yml` (8.45 0 CI 集成, 留 followup)
- `.husky/pre-commit` / `.lintstagedrc.json` (Phase 8.44.2+8.44.3 已就位)
- backend Python (infra/agent_system / dashboard backend / cost_tracker / cost_persistence / cost_router)
- 9 historical spec/plan docs (`:NNN` 行号不动)
- 12 SCENARIOS 路由 (Python side)
- CLAUDE.md / pyproject.toml / pytest.ini
- `dashboard/frontend/.husky/_/h` (husky helper, 0 改)
- `.state/*.db` (gitignored) / `.env` (0 改 0 泄漏)
- `playwright.config.js` 0 改 webServer (dev 手动启 vite, smoke 走 http://localhost:5173)

---

## Pre-Execution Setup

每个 task 都用同 pattern:
1. 读 baseline state (确认 before snapshot)
2. 改文件 (specific code 在 step 里)
3. 跑 verification (commands + expected output)
4. Commit (`feat`/`chore`/`docs` conventional, 0 Co-Authored-By)

**TDD note**: 8.45.1 / 8.45.2 走 RED → GREEN 模式 (先写 failing test, 再改 .vue 加 testid, 验证 GREEN). 8.45.3 走 install + config + 1 spec 模式, 0 引新 frontend test, 走 Playwright runner install + 1 smoke spec.

**Critical safety** (跟 Phase 8.35-8.44 policy 一致):
- 0 force-push
- 0 amend (除 sub-phase fail 救火显式声明)
- 0 Co-Authored-By footer (跟 global CLAUDE.md config)
- 0 改 .env / 0 API key 泄漏
- 0 改 .state/*.db (gitignored)
- 0 改 historical spec/plan docs `:NNN` 行号
- 0 改 12 SCENARIOS 路由
- 0 改 CLAUDE.md / pyproject.toml / pytest.ini
- 0 改 23 vitest spec (除 2 new) / 0 改 14 .vue (除 3 small additive)
- 0 改 backend Python
- 0 改 `.github/workflows/real-llm-tests.yml`
- 0 改 `playwright.config.js` 之外的 .github/workflows
- 0 playwright browser download (Phase 8.45 0 CI 集成, 留 dev opt-in)

---

## Task 1: 8.45.1 — DecisionCard 3 status testid (low risk, hook ✅)

**Files:**
- Modify: `dashboard/frontend/src/components/DecisionCard.vue:13-18` (+1 line `:data-testid`)
- Modify: `dashboard/frontend/src/components/DecisionCard.vue:77-117` (+1 computed `statusKey`)
- Create: `dashboard/frontend/tests/unit/decision-card-status.spec.ts` (NEW, ~80L, 4 tests)

- [ ] **Step 1: 验证 pre-state**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
grep -n "status-badge" src/components/DecisionCard.vue
# 期望: L16 data-testid="status-badge" (1 line)
grep -n "statusBadge" src/components/DecisionCard.vue
# 期望: L110 const statusBadge = computed(...) + L116 statusBadge map
grep -c "data-testid" src/components/DecisionCard.vue
# 期望: 3 (decision-card + decision-kind-{kind} dynamic + status-badge) + 6 inner (option-btn × 2 selectors + defer-btn + cancel-btn + readonly-hint + meta-info + 2-3 spans)
ls tests/unit/decision-card-status.spec.ts 2>&1
# 期望: No such file
git status
# 期望: clean (working tree 0 uncommitted changes)
```

- [ ] **Step 2: Write the failing test (RED)**

Create `dashboard/frontend/tests/unit/decision-card-status.spec.ts`:

```ts
// tests/unit/decision-card-status.spec.ts — Phase 8.45.1
// DecisionCard 3 status testid: status-resolved / status-deferred / status-cancelled
// 跟 Phase 8.38 kind testid `decision-kind-${kindClass}` 1:1 mirror pattern.
// 跟 Phase 8.32 L16 generic `data-testid="status-badge"` 互不冲突 (specific +
// generic 2 个 testid attribute 同 span).
//
// Phase 8.32 已加 generic status-badge + 6 inner testid. Phase 8.45.1 收尾 specific
// 3 status testid (mirror 3 class names: status-resolved/-deferred/-cancelled), e2e
// 找特定 status 节点得 `.find('.status-resolved')` 脆 selector, 改 byTestid 稳定
// selector.

import { describe, test, expect } from 'vitest'
import { mount } from '@vue/test-utils'
import { byTestid } from '../helpers/by-testid'
import DecisionCard from '../../src/components/DecisionCard.vue'

describe('DecisionCard 3 status testid (Phase 8.45.1)', () => {
  test('resolved status → status-resolved testid renders + 1:1 镜像 class', () => {
    const wrapper = mount(DecisionCard, {
      props: {
        decision: {
          decision_id: 'd1',
          kind: 'outline_judgment',
          status: 'resolved',
          prompt: '...',
          options: [],
        },
      },
    })
    // resolved: status-resolved testid 在位 + 镜像 class
    const el = wrapper.find(byTestid('status-resolved'))
    expect(el.exists()).toBe(true)
    expect(el.classes()).toContain('status-resolved')
    // 其他 2 status testid 不在
    expect(wrapper.find(byTestid('status-deferred')).exists()).toBe(false)
    expect(wrapper.find(byTestid('status-cancelled')).exists()).toBe(false)
  })

  test('deferred status → status-deferred testid renders + 1:1 镜像 class', () => {
    const wrapper = mount(DecisionCard, {
      props: {
        decision: {
          decision_id: 'd2',
          kind: 'volume_judgment',
          status: 'deferred',
          prompt: '...',
          options: [],
        },
      },
    })
    const el = wrapper.find(byTestid('status-deferred'))
    expect(el.exists()).toBe(true)
    expect(el.classes()).toContain('status-deferred')
    expect(wrapper.find(byTestid('status-resolved')).exists()).toBe(false)
    expect(wrapper.find(byTestid('status-cancelled')).exists()).toBe(false)
  })

  test('cancelled status → status-cancelled testid renders + 1:1 镜像 class', () => {
    const wrapper = mount(DecisionCard, {
      props: {
        decision: {
          decision_id: 'd3',
          kind: 'chapter_iteration_judgment',
          status: 'cancelled',
          prompt: '...',
          options: [],
        },
      },
    })
    const el = wrapper.find(byTestid('status-cancelled'))
    expect(el.exists()).toBe(true)
    expect(el.classes()).toContain('status-cancelled')
    expect(wrapper.find(byTestid('status-resolved')).exists()).toBe(false)
    expect(wrapper.find(byTestid('status-deferred')).exists()).toBe(false)
  })

  test('pending status → 0 status testid (statusBadge map 不含 pending, v-if absent)', () => {
    const wrapper = mount(DecisionCard, {
      props: {
        decision: {
          decision_id: 'd4',
          kind: 'outline_judgment',
          status: 'pending',
          prompt: '...',
          options: ['A', 'B'],
        },
      },
    })
    // pending: 0 specific status testid + 0 generic status-badge (Phase 8.32 验证)
    expect(wrapper.find(byTestid('status-resolved')).exists()).toBe(false)
    expect(wrapper.find(byTestid('status-deferred')).exists()).toBe(false)
    expect(wrapper.find(byTestid('status-cancelled')).exists()).toBe(false)
    expect(wrapper.findAll(byTestid('status-badge'))).toHaveLength(0)
  })
})
```

- [ ] **Step 3: Run test, verify RED**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
corepack pnpm@9.15.0 vitest run decision-card-status 2>&1 | tail -25
# 期望: 4 failed (status-resolved/-deferred/-cancelled testid 0 存在, pending 0 testid 这条会 pass 因为我们检查 !exists)
# RED 确认
```

- [ ] **Step 4: Edit DecisionCard.vue — 加 :data-testid 动态 attribute**

Edit `dashboard/frontend/src/components/DecisionCard.vue`, modify L13-18 (status badge template, BEFORE):

```vue
      <span
        v-if="statusBadge"
        class="status-badge"
        data-testid="status-badge"
        :class="statusBadge.cls"
      >{{ statusBadge.label }}</span>
```

AFTER:

```vue
      <span
        v-if="statusBadge"
        class="status-badge"
        data-testid="status-badge"
        :data-testid="`status-${statusKey}`"
        :class="statusBadge.cls"
      >{{ statusBadge.label }}</span>
```

(同时支持 generic + specific 2 testid, 跟 Phase 8.32 L16 + Phase 8.45.1 收尾)

- [ ] **Step 5: Edit DecisionCard.vue — 加 statusKey computed**

Edit `dashboard/frontend/src/components/DecisionCard.vue`, 在 statusBadge computed (L110) **之前**加 statusKey computed:

```js
// Phase 8.45.1: status testid kebab-case key (mirror Phase 8.38 kindClass pattern)
// 实际 decision.status 已是 resolved/deferred/cancelled, 0 改 `_` chars, 但加
// kebab-case 转换防御未来 `in_progress` 之类多 token status.
const statusKey = computed(() => {
  return (props.decision.status || '').replace(/_/g, '-')
})
```

(脚本段 L77-117 `<script setup>` 内, 跟 statusBadge 邻近保 cohesion. 0 改 statusBadge computed / 0 改 kindClass computed / 0 改 props / 0 改 emit / 0 改 onResolve / onDefer / onCancel)

- [ ] **Step 6: Run test, verify GREEN**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
corepack pnpm@9.15.0 vitest run decision-card-status 2>&1 | tail -10
# 期望: Test Files 1 passed (1) / Tests 4 passed (4) / 0 failed
# GREEN 确认
```

- [ ] **Step 7: 验证 baseline 维持 (vitest 23 spec / lint)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
corepack pnpm@9.15.0 vitest run 2>&1 | tail -3
# 期望: Test Files 24 passed (24) / Tests 104 passed (104) / 0 failed (23 baseline + 1 new spec 4 tests)
corepack pnpm@9.15.0 lint:all 2>&1 | tail -5
# 期望: EXIT=0 (vue/attribute-hyphenation 0 errors 维持, Phase 8.43.6 baseline)
```

- [ ] **Step 8: 验证 data-testid 数量**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
grep -c "data-testid" src/components/DecisionCard.vue
# 期望: 4 (1 generic status-badge + 1 dynamic :data-testid + 2 existing decision-card/decision-kind-{kind} + inner 6)
# (具体 count 看 .vue 实际 attributes: decision-card + decision-kind-${kindClass} + option-btn + defer-btn + cancel-btn + readonly-hint + meta-info + status-badge + status-${statusKey} = 9 个 data-testid/attribute hits, 但 grep -c "data-testid" 算的是 line 数 = 9)
```

- [ ] **Step 9: Commit**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git add dashboard/frontend/src/components/DecisionCard.vue \
        dashboard/frontend/tests/unit/decision-card-status.spec.ts
git status
# 期望: 2 改文件 (1 .vue + 1 new spec)

git commit -m "feat(decision-card): phase 8.45.1 — 3 status testid (resolved/deferred/cancelled)

Phase 8.32 加 generic status-badge testid + 6 inner testid, Phase 8.45.1 收尾
specific 3 status testid (status-resolved/-deferred/-cancelled) mirror 3 existing
class names, 跟 Phase 8.38 kind testid decision-kind-\${kindClass} 1:1 mirror
pattern. e2e 找特定 status 节点得 .find('.status-resolved') 脆 selector, 改
byTestid 稳定 selector.

改动:
- DecisionCard.vue L13-18 (+1 line :data-testid) + L109 旁 (+1 statusKey computed
  kebab-case mirror, 防御未来 in_progress 之类多 token status)
- tests/unit/decision-card-status.spec.ts NEW (+80L, 4 tests): resolved /
  deferred / cancelled 3 status testid + 1:1 镜像 class + pending 0 testid
  (statusBadge map 不含 pending, v-if absent, 跟 Phase 8.32 验证一致)

不改:
- 0 改 statusBadge computed logic (3 entries: resolved/deferred/cancelled)
- 0 改 kindClass computed / 0 改 props / 0 改 emit / 0 改 onResolve/onDefer/onCancel
- 0 改 23 旧 vitest spec / 0 改 backend Python / 0 改 9 historical spec/plan docs
- 0 改 .vue except 1 line + 1 computed (additive)
- 0 force-push, 0 --no-verify, 0 Co-Authored-By

Baseline:
- vitest 23 spec + 1 new = 24 spec, 100 + 4 = 104 tests, 0 failed
- pnpm lint:all EXIT=0 维持 (vue/attribute-hyphenation 0 errors)"
```

---

## Task 2: 8.45.2 — OverviewPage e2e 加深 (5 tests, 0 .vue OR 1 small template change)

**Files:**
- Create: `dashboard/frontend/tests/unit/dashboard-error-loading.spec.ts` (NEW, ~140L, 5 tests)
- Optional Modify: `dashboard/frontend/src/pages/OverviewPage.vue` (R4 verify 后, 1 small empty fallback template)

- [ ] **Step 1: 验证 pre-state (R4 verify)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
grep -n "暂无" src/pages/OverviewPage.vue
# 期望: 0 matches (OverviewPage 当前 0 empty fallback 文案, R4 触发)
# 缺则 Task 2 加 1 small template change (Step 5)
ls tests/unit/dashboard-error-loading.spec.ts 2>&1
# 期望: No such file
git status
# 期望: clean
```

- [ ] **Step 2: Write the failing test (RED, 5 tests)**

Create `dashboard/frontend/tests/unit/dashboard-error-loading.spec.ts`:

```ts
// tests/unit/dashboard-error-loading.spec.ts — Phase 8.45.2
// OverviewPage e2e 加深: 5 tests 覆盖 error / loading / empty / refresh-recovery
// / partial data 5 态. 跟 Phase 8.30b dashboard.spec.ts 1:1 mirror, vi.mock
// api/index.js + vi.resetModules + dynamic import per test 保 singleton 隔离.
//
// Phase 8.30b 已覆盖 happy path 7 tests (page title / refresh / chapter table /
// 5 stat cards / hook-trend-chart / error banner not shown / StatCard testid).
// Phase 8.45.2 收尾 5 边界态: error / loading / empty / refresh-recovery / partial.

import { describe, test, expect, vi, beforeEach } from 'vitest'
import { mount, flushPromises } from '@vue/test-utils'
import { byTestid } from '../helpers/by-testid'

// 5 边界态 per-test mock (vi.mock hoisting 需 factory 返 0 参 fn, 用 vi.hoisted
// 把 mock fn 提到 hoist 时外层作用域)
const mocks = vi.hoisted(() => ({
  fetchOverview: vi.fn(),
  fetchChapters: vi.fn(),
}))

vi.mock('../../src/api/index.js', () => ({
  fetchOverview: mocks.fetchOverview,
  fetchChapters: mocks.fetchChapters,
}))

beforeEach(() => {
  mocks.fetchOverview.mockReset()
  mocks.fetchChapters.mockReset()
})

async function mountPage() {
  // 动态 import + vi.resetModules 保 test 间 module-level state 隔离
  vi.resetModules()
  const { default: OverviewPage } = await import('../../src/pages/OverviewPage.vue')
  const wrapper = mount(OverviewPage)
  await flushPromises()
  return wrapper
}

describe('OverviewPage e2e 加深 (Phase 8.45.2)', () => {
  test('error state: api throws → error-banner testid visible + page-title 仍 visible', async () => {
    mocks.fetchOverview.mockRejectedValue(new Error('API 500'))
    mocks.fetchChapters.mockResolvedValue({ chapters: [] })

    const wrapper = await mountPage()

    // error banner 显
    const errorBanner = wrapper.find(byTestid('error-banner'))
    expect(errorBanner.exists()).toBe(true)
    expect(errorBanner.text()).toContain('API 500')
    // page-title 仍 visible (不破 layout)
    expect(wrapper.find(byTestid('page-title')).exists()).toBe(true)
  })

  test('loading state: api slow → initial state (no error, refresh-btn enabled, 0 stat-card render)', async () => {
    // mock 永 not resolve → loading=true 持续
    mocks.fetchOverview.mockReturnValue(new Promise(() => {}))
    mocks.fetchChapters.mockReturnValue(new Promise(() => {}))

    const wrapper = await mountPage()

    // error banner 不显
    expect(wrapper.find(byTestid('error-banner')).exists()).toBe(false)
    // refresh-btn 显 + disabled (loading 中)
    const refreshBtn = wrapper.find(byTestid('refresh-btn'))
    expect(refreshBtn.exists()).toBe(true)
    expect(refreshBtn.attributes('disabled')).toBeDefined()
    // 0 stat-card render (overview 0 返)
    expect(wrapper.findAll(byTestid('stat-card'))).toHaveLength(0)
  })

  test('empty state: api returns 0 chapters + 0 overview → page-title visible + 0 stat-card', async () => {
    mocks.fetchOverview.mockResolvedValue({
      total_chapters: 0,
      total_hooks: 0,
      avg_hook_strength: null,
      total_coolpoints: 0,
      avg_coolpoint_density: null,
    })
    mocks.fetchChapters.mockResolvedValue({ chapters: [] })

    const wrapper = await mountPage()

    // page-title 仍 visible (layout 0 破)
    expect(wrapper.find(byTestid('page-title')).exists()).toBe(true)
    // 0 chapters → 0 stat-card (overview 0 字段, computed 走 0 fallback)
    expect(wrapper.findAll(byTestid('stat-card'))).toHaveLength(0)
    // error banner 不显 (api 0 throw)
    expect(wrapper.find(byTestid('error-banner')).exists()).toBe(false)
  })

  test('refresh error recovery: initial throw + click refresh + mock re-fetch happy → error-banner hide + stat-card 渲染', async () => {
    // 1st: throw; 2nd+: happy
    mocks.fetchOverview
      .mockRejectedValueOnce(new Error('网络错误'))
      .mockResolvedValueOnce({
        total_chapters: 359,
        total_hooks: 2845,
        avg_hook_strength: 0.78,
        total_coolpoints: 1823,
        avg_coolpoint_density: 5.1,
      })
    mocks.fetchChapters.mockResolvedValue({
      chapters: [
        { chapter: 1, hook_count: 8, hook_strength: 0.82, coolpoint_count: 5, coolpoint_density: 5.5 },
      ],
    })

    const wrapper = await mountPage()
    // initial: error banner 显
    expect(wrapper.find(byTestid('error-banner')).exists()).toBe(true)
    // click refresh
    await wrapper.find(byTestid('refresh-btn')).trigger('click')
    await flushPromises()
    // error banner 消失 + 5 stat-card 渲染
    expect(wrapper.find(byTestid('error-banner')).exists()).toBe(false)
    expect(wrapper.findAll(byTestid('stat-card'))).toHaveLength(5)
  })

  test('partial data: overview 有 + chapters 空 → 5 stat-card 渲染 + chapter table 空', async () => {
    mocks.fetchOverview.mockResolvedValue({
      total_chapters: 359,
      total_hooks: 2845,
      avg_hook_strength: 0.78,
      total_coolpoints: 1823,
      avg_coolpoint_density: 5.1,
    })
    mocks.fetchChapters.mockResolvedValue({ chapters: [] })

    const wrapper = await mountPage()

    // 5 stat-card 渲染 (overview 有)
    expect(wrapper.findAll(byTestid('stat-card'))).toHaveLength(5)
    // chapter table 仍显 (空 list)
    expect(wrapper.find(byTestid('chapter-table')).exists()).toBe(true)
    // 0 chapter-row
    expect(wrapper.findAll(byTestid('chapter-row'))).toHaveLength(0)
  })
})
```

- [ ] **Step 3: Run test, verify RED (假设未加 R4 fallback)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
corepack pnpm@9.15.0 vitest run dashboard-error-loading 2>&1 | tail -25
# 期望: 5 failed (loading + empty + refresh-recovery + partial 4 个因 overview 0 返 / refresh 不触发 等)
# 注意: error state 可能 pass 如果 error-banner testid 已显 (Phase 8.31 加的)
# RED 部分确认
```

(注: R4 验证 — 如果测试 5 都因 empty fallback 缺失 fail, Step 5 走 template change; 如果仅 1-3 fail, 仍走 Step 5 verify 是否需 fallback)

- [ ] **Step 4: R4 verify — 确定是否需 empty fallback template change**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
# 验证 0 "暂无" 文案
grep -n "暂无" src/pages/OverviewPage.vue
# 期望: 0 matches
# 验证 0 v-if="chapters.length === 0" 类 fallback
grep -n "chapters.length === 0" src/pages/OverviewPage.vue
# 期望: 0 matches
# 验证 0 empty 相关 v-if / v-else
grep -nE "v-if.*chapters\." src/pages/OverviewPage.vue
# 期望: 0 matches
```

(确认 0 empty fallback 后, 走 Step 5 加 1 small template change)

- [ ] **Step 5: Add 1 small empty fallback template (R4 mitigation)**

Edit `dashboard/frontend/src/pages/OverviewPage.vue`, 在 L37-39 `<section class="table-section">` 块内 **加** 1 个 sibling fallback 段 (BEFORE):

```vue
    <section class="table-section">
      <ChapterTable :chapters="chapters" />
    </section>
```

AFTER:

```vue
    <section class="table-section">
      <ChapterTable v-if="chapters.length > 0" :chapters="chapters" />
      <p v-else class="empty-fallback" data-testid="chapters-empty">暂无章节</p>
    </section>
```

(1 line additive template change. 0 改 chart / 0 改 stat card / 0 改 5 stat-cards section. ChapterTable 0 data-testid 加 — Phase 8.31 已加 `chapter-table` + `chapter-row` testid, 走 0 break)

- [ ] **Step 6: Run test, verify GREEN**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
corepack pnpm@9.15.0 vitest run dashboard-error-loading 2>&1 | tail -10
# 期望: Test Files 1 passed (1) / Tests 5 passed (5) / 0 failed
# GREEN 确认
```

(注: partial data test 走 chapters.length === 0 → 显 `chapters-empty` testid, 0 stat-card 渲染因 ChapterTable v-else 替换. 0 破 — `chapter-table` testid v-if absent 跟 partial test 期望一致)

- [ ] **Step 7: 验证 baseline 维持 (vitest 24 spec / lint / build)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
corepack pnpm@9.15.0 vitest run 2>&1 | tail -3
# 期望: Test Files 25 passed (25) / Tests 109 passed (109) / 0 failed (24 baseline + 1 new spec 5 tests)
corepack pnpm@9.15.0 lint:all 2>&1 | tail -3
# 期望: EXIT=0
corepack pnpm@9.15.0 build 2>&1 | tail -3
# 期望: 0 error
```

- [ ] **Step 8: 验证 dashboard.spec.ts 7 旧 test 不破 (R12 防御)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
corepack pnpm@9.15.0 vitest run dashboard.spec 2>&1 | tail -5
# 期望: Test Files 1 passed (1) / Tests 7 passed (7) / 0 failed
# 验证: chapter table 5 columns test 走 vi.mock happy fixture, chapters 非空, ChapterTable v-if 触发, 0 破
# 验证: 5 stat cards test 走 happy fixture, 0 break
```

- [ ] **Step 9: Commit**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git add dashboard/frontend/src/pages/OverviewPage.vue \
        dashboard/frontend/tests/unit/dashboard-error-loading.spec.ts
git status
# 期望: 2 改文件 (1 .vue 1 line template + 1 new spec)

git commit -m "feat(overview-page): phase 8.45.2 — e2e 加深 5 态 + empty fallback template

Phase 8.30b dashboard.spec.ts 7 tests 覆盖 happy path (page title / refresh /
chapter table / 5 stat cards / hook-trend-chart / error banner not shown /
StatCard testid). Phase 8.45.2 收尾 5 边界态: error / loading / empty / refresh
error recovery / partial data. 1 small additive OverviewPage.vue template
change: ChapterTable 走 v-if 包裹 + v-else empty fallback <p data-testid=
\"chapters-empty\">暂无章节</p>, 防御 R4 验证 (OverviewPage 当前 0 empty 文案,
spec 走 verify 后 add 1 line).

改动:
- OverviewPage.vue L37-39 (+1 line v-if 包裹 + 2 line v-else fallback) — 1 small
  additive template change, 0 改 stat-card / 0 改 chart / 0 改 stat-cards section
- tests/unit/dashboard-error-loading.spec.ts NEW (+140L, 5 tests):
  1. error state (api throws → error-banner 显 + page-title 仍 visible)
  2. loading state (api slow → refresh-btn disabled + 0 stat-card render)
  3. empty state (api 0 返 → 0 stat-card + 0 chapter-row, layout 0 破)
  4. refresh error recovery (initial throw + click refresh + happy re-fetch
     → error-banner hide + 5 stat-card 渲染)
  5. partial data (overview 有 + chapters 空 → 5 stat-card 渲染 + chapter
     table 显空)

不改:
- 0 改 5 stat cards computed (Phase 8.34 store registry 0 改)
- 0 改 chart section (HookTrendChart / CoolpointChart 0 改)
- 0 改 7 旧 dashboard.spec.ts tests (happy path 0 破, vi.mock 返 fixture 仍 happy)
- 0 改 api/index.js / 0 改 5 composables / 0 改 23 旧 vitest spec (除 1 new)
- 0 改 backend Python / 0 改 9 historical spec/plan docs
- 0 改 14 .vue except 1 small template change (additive)
- 0 force-push, 0 --no-verify, 0 Co-Authored-By

Baseline:
- vitest 24 spec + 1 new = 25 spec, 104 + 5 = 109 tests, 0 failed
- pnpm lint:all EXIT=0 维持
- pnpm build 0 error 维持"
```

---

## Task 3: 8.45.3 — Playwright runner install + 1 E2E smoke spec (medium risk, 0 hook E2E)

**Files:**
- Modify: `dashboard/frontend/package.json` (+1 devDep `@playwright/test` + 1 script `e2e:smoke`)
- Create: `dashboard/frontend/playwright.config.js` (NEW, ~30L)
- Create: `dashboard/frontend/tests/e2e-smoke/smoke.spec.js` (NEW, ~60L, 4 tests)
- Modify: `dashboard/frontend/src/App.vue` (R5 verify 后, +1 small `data-testid="ws-status"`)
- Modify: `dashboard/frontend/tests/e2e/cost-bar-chart-tier-mode.spec.js` (+1 line header comment)
- Modify: `dashboard/frontend/tests/e2e/cost-trend-chart-per-tier.spec.js` (+1 line header comment)
- Modify: `dashboard/frontend/tests/e2e/sidebar_cost_banner.spec.js` (+1 line header comment)

- [ ] **Step 1: 验证 pre-state (R5 verify)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
grep '"@playwright/test"' package.json
# 期望: 0 matches
grep '"e2e:smoke"' package.json
# 期望: 0 matches
ls playwright.config.js tests/e2e-smoke/ 2>&1
# 期望: No such file or directory
grep -n "ws-status" src/App.vue
# 期望: 0 matches (R5 触发, Task 3 加 1 small data-testid)
# 注: useWorkflowSocket composable 返 status ref, App.vue L57 const { status } = useWorkflowSocket()
# 触发 SidebarCostBanner 跟 status prop 显. ws-status data-testid 加在 SidebarCostBanner 元素上
git status
# 期望: clean
```

- [ ] **Step 2: Bump @playwright/test to ^1.49.0 + e2e:smoke script**

Edit `dashboard/frontend/package.json`, 在 devDependencies `vitest` **之前**加 (跟 alphabetical 顺序, `p` < `v`):

```diff
     "@vitest/coverage-v8": "^4.1.8",
+    "@playwright/test": "^1.49.0",
     "vite": "^6.0.0",
     "vitest": "^4.1.8",
```

(单独加 1 devDep, `p` 排序后 `@playwright/test` 在 `@types/node` 之后, 跟现有 alphabetical 不冲突 — 实际是 `@playwright/test` 跟 `@types/node` 都在 `@` prefix, alphabetic 内部 `p` < `t`, 所以放 `@types/node` 之前:

```diff
     "@types/node": "^25.9.2",
+    "@playwright/test": "^1.49.0",
     "@typescript-eslint/parser": "^8.0.0",
```

Edit `dashboard/frontend/package.json`, 在 scripts `build` 之后加 (跟现有 `dev` / `build` / `preview` / `lint:*` 排序, e2e 段独立):

```diff
     "preview": "vite preview",
+    "e2e:smoke": "playwright test",
     "lint:testid": "eslint --config eslint.config.js tests/unit/**/*.spec.ts tests/fixtures/lint-testid/*.spec.ts --no-error-on-unmatched-pattern",
```

- [ ] **Step 3: Install (跟 Phase 8.40 corepack pattern)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
corepack pnpm@9.15.0 install
# 期望: Done in N.Ns + 0 errors. pnpm-lock.yaml auto-updated.
# @playwright/test ~5MB download, 0 browser (browser opt-in via npx playwright install chromium)
```

- [ ] **Step 4: Create playwright.config.js**

Create `dashboard/frontend/playwright.config.js`:

```js
// playwright.config.js — Phase 8.45.3
// Playwright config (0 browser download, 0 CI integration).
// dev opt-in: npx playwright install chromium 装 browser 后跑 pnpm e2e:smoke
// vitest 走 dashboard-frontend-ci.yml 仍 vitest primary gate, Playwright 留
// manual + future CI integration (Phase 8.46+ followup).

import { defineConfig, devices } from '@playwright/test'

export default defineConfig({
  testDir: './tests/e2e-smoke',
  fullyParallel: false, // Phase 8.45.3 1 spec 单 thread 简化
  forbidOnly: !!process.env.CI,
  retries: 0, // 0 retry 简单 smoke
  workers: 1,
  reporter: 'list',
  use: {
    baseURL: 'http://localhost:5173', // 跟 vite dev server default port
    trace: 'retain-on-failure',
  },
  projects: [
    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
  ],
  // 0 webServer 配, dev 手动启 vite (`pnpm dev`), smoke 走 http://localhost:5173
})
```

- [ ] **Step 5: R5 verify — App.vue ws-status testid 缺失**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
grep -n "data-testid" src/App.vue
# 期望: 1 match (L2 data-testid="app-root", SidebarCostBanner 0 testid)
# R5 触发, Step 6 加 1 small data-testid
```

- [ ] **Step 6: Add 1 small ws-status data-testid (R5 mitigation)**

Edit `dashboard/frontend/src/App.vue`, modify L22 SidebarCostBanner (BEFORE):

```vue
      <SidebarCostBanner :status="status" /> <!-- Phase 8.11 -->
```

AFTER:

```vue
      <SidebarCostBanner :status="status" data-testid="ws-status" /> <!-- Phase 8.11 + 8.45.3 ws-status testid -->
```

(注: Vue 3.5 允许 inherit fallthrough attribute, 但 SidebarCostBanner 是 single root, `data-testid` 会自动 fallthrough 到根元素 — 跟 Phase 8.13+8.31 约定一致. 0 改 SidebarCostBanner.vue / 0 改 useWorkflowSocket composable / 0 改其他)

(备选方案: 如果 SidebarCostBanner 多 root / fragment, 改用 wrapper div 加 data-testid. Phase 8.45.3 spec 走 inherit fallthrough 简化)

- [ ] **Step 7: Create e2e-smoke/smoke.spec.js**

Create `dashboard/frontend/tests/e2e-smoke/smoke.spec.js`:

```js
// tests/e2e-smoke/smoke.spec.js — Phase 8.45.3
// E2E smoke (4 tests, 走真实 Chromium browser).
// 0 browser 装 = 0 跑 (header note: dev 需 npx playwright install chromium opt-in).
// 走 vitest 不跑 (vitest.config.js exclude 'tests/e2e/**', e2e-smoke/ 也加 exclude).
//
// Pre-conditions:
//   1. 启 vite dev server: pnpm dev (port 5173)
//   2. 装 browser: npx playwright install chromium (~500MB opt-in)
//   3. 跑 smoke: pnpm e2e:smoke
//
// 0 CI integration (Phase 8.45 留 followup, vitest 仍 primary gate)

import { test, expect } from '@playwright/test'

test.describe('Dashboard smoke (Phase 8.45.3)', () => {
  test('page title contains "Dashboard"', async ({ page }) => {
    await page.goto('/')
    await expect(page).toHaveTitle(/Dashboard/)
  })

  test('navigate to /workflows via nav click', async ({ page }) => {
    await page.goto('/')
    // 点击 "工作流" nav
    await page.click('a.nav-item:has-text("工作流")')
    await expect(page).toHaveURL(/#\/workflows|workflows/)
  })

  test('WS connect indicator visible (load complete)', async ({ page }) => {
    await page.goto('/')
    // Phase 8.45.3 加 data-testid="ws-status" 在 SidebarCostBanner 元素 (Vue 3.5
    // fallthrough to root). WS connect status = sidebar-cost-banner visible.
    await expect(page.locator('[data-testid="ws-status"]')).toBeVisible({ timeout: 5000 })
  })

  test('error banner on API failure', async ({ page }) => {
    // mock all api calls 失败, 验证 error-banner 显
    await page.route('**/api/**', route => route.abort())
    await page.goto('/')
    await expect(page.locator('[data-testid="error-banner"]')).toBeVisible({ timeout: 5000 })
  })
})
```

- [ ] **Step 8: 验证 e2e:smoke --list (skip browser install check)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
corepack pnpm@9.15.0 e2e:smoke --list 2>&1 | tail -10
# 期望: 4 tests listed (page title / navigate / ws connect / error banner)
# 注: --list 不启 browser, 0 actual run, 0 install browser required
```

- [ ] **Step 9: Edit vitest.config.js — exclude e2e-smoke/**

Edit `dashboard/frontend/vitest.config.js`, modify `exclude` 数组 (L20) 改成:

```js
    exclude: ['node_modules', 'dist', 'tests/e2e/**', 'tests/e2e-smoke/**', 'coverage/**'],
```

(避免 vitest 误把 Playwright spec 当 vitest spec 跑 — Playwright spec 用 `@playwright/test` import, vitest 0 识别会报错)

- [ ] **Step 10: 验证 baseline 维持 (vitest 25 spec / lint / build)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory/dashboard/frontend
corepack pnpm@9.15.0 vitest run 2>&1 | tail -3
# 期望: Test Files 25 passed (25) / Tests 109 passed (109) / 0 failed (e2e-smoke 走 exclude, 0 跑)
corepack pnpm@9.15.0 lint:all 2>&1 | tail -3
# 期望: EXIT=0
corepack pnpm@9.15.0 build 2>&1 | tail -3
# 期望: 0 error
```

- [ ] **Step 11: Update 3 ceremonial header comments (1 line each)**

Edit `dashboard/frontend/tests/e2e/cost-bar-chart-tier-mode.spec.js`, modify L11 (or current header) "Playwright runner 未装" 段 (BEFORE — find exact line via grep):

```js
 * 当前 Playwright runner 未装 (跟 Phase 7.6/8.7/8.8 一致),作为契约文档存在。
```

AFTER:

```js
 * Phase 8.45.3: Playwright runner 已装 (@playwright/test ^1.49.0, dev opt-in),
 * 走 pnpm e2e:smoke 跑 (需 npx playwright install chromium 先装 browser, ~500MB).
 * 0 CI integration (vitest 仍 primary gate, Playwright 留 manual + future Phase 8.46+).
```

(同样 pattern edit `cost-trend-chart-per-tier.spec.js` + `sidebar_cost_banner.spec.js` 2 files, 1 line each)

- [ ] **Step 12: Commit**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git add dashboard/frontend/package.json \
        dashboard/frontend/pnpm-lock.yaml \
        dashboard/frontend/playwright.config.js \
        dashboard/frontend/vitest.config.js \
        dashboard/frontend/src/App.vue \
        dashboard/frontend/tests/e2e-smoke/smoke.spec.js \
        dashboard/frontend/tests/e2e/cost-bar-chart-tier-mode.spec.js \
        dashboard/frontend/tests/e2e/cost-trend-chart-per-tier.spec.js \
        dashboard/frontend/tests/e2e/sidebar_cost_banner.spec.js
git status
# 期望: 9 改文件 (1 package.json + 1 pnpm-lock + 1 new playwright.config + 1 vitest.config 1 line + 1 App.vue 1 line + 1 new smoke spec + 3 ceremonial 1 line each)

git commit -m "chore(playwright): phase 8.45.3 — install @playwright/test + 1 E2E smoke spec

Phase 8.45 收尾 3 items 第 3: Playwright runner install + 1 new E2E smoke spec
(4 tests). 3 ceremonial specs (cost-bar-chart-tier-mode / cost-trend-chart-per-tier
/ sidebar_cost_banner) header comment 更新标 'runner 已装' (跟 Phase 8.30b
batch 5 删 11 + 留 3 残留 + 8.45.3 runner 就位 一致). 0 CI integration (vitest
仍 primary gate, Playwright 留 dev opt-in + manual + future Phase 8.46+ followup).
0 browser download (npx playwright install chromium 显式 opt-in, ~500MB).

改动:
- package.json (+1 devDep @playwright/test ^1.49.0 + 1 script e2e:smoke) —
  alphabetical 加在 @types/node 之后, e2e:smoke 段放 dev/build/preview 之后
- playwright.config.js NEW (30L): 1 chromium project, baseURL localhost:5173,
  fullyParallel false, workers 1, retries 0, trace retain-on-failure, 0 webServer
- vitest.config.js (+1 line exclude 'tests/e2e-smoke/**'): 防止 vitest 误把
  Playwright spec 当 vitest spec 跑
- App.vue L22 (+1 line data-testid='ws-status' fallback-through SidebarCostBanner):
  R5 verify 后加 1 small data-testid, 走 Vue 3.5 inherit fallthrough, 0 改
  SidebarCostBanner.vue / 0 改 useWorkflowSocket composable
- tests/e2e-smoke/smoke.spec.js NEW (60L, 4 tests):
  1. page title contains 'Dashboard'
  2. navigate to /workflows via nav click
  3. WS connect indicator visible (load complete, ws-status testid)
  4. error banner on API failure (mock all api abort → error-banner visible)
- tests/e2e/{cost-bar-chart-tier-mode,cost-trend-chart-per-tier,sidebar_cost_banner}
  .spec.js (3 files, +1 line header comment each): 'runner 未装' → 'runner 已装,
  0 CI integration' (跟 Phase 8.30b 留 3 残留 + 8.45.3 runner 就位 一致)

不改:
- 0 改 14 .vue except 1 small ws-status testid (additive)
- 0 改 25 旧 vitest spec (e2e-smoke/ 走 exclude, vitest 0 跑 Playwright spec)
- 0 改 5 composables / 0 改 api/index.js / 0 改 3 ceremonial spec logic (除 1 line header)
- 0 改 dashboard-frontend-ci.yml / 0 改 test.yml / 0 改 real-llm-tests.yml
  (Playwright 0 CI integration, vitest 仍 primary gate)
- 0 改 .eslintrc / 0 改 .lintstagedrc.json / 0 改 .husky/pre-commit
- 0 改 backend Python / 0 改 9 historical spec/plan docs
- 0 force-push, 0 --no-verify, 0 Co-Authored-By
- 0 改 MEMORY.md (8.45.4 push 阶段同步)

Baseline:
- vitest 25 spec / 109 tests / 0 failed (e2e-smoke 0 跑)
- pnpm e2e:smoke --list → 4 tests listed (browser 0 装, 0 actual run)
- pnpm lint:all EXIT=0 维持
- pnpm build 0 error 维持
- @playwright/test ~5MB install, 0 browser download

后续 (Phase 8.46+):
- Playwright CI integration (separate workflow + matrix + cache + browser install)
- 3 ceremonial vitest-化 (跟 Phase 8.30b pattern, 1:1 转换)"
```

---

## Task 4: 8.45.4 — Push 3 commits to origin/master + memory sync (0 commit)

- [ ] **Step 1: 验证 pre-state**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git log --oneline -5
# 期望: 3 Phase 8.45 commits (8.45.1 / 8.45.2 / 8.45.3) + Phase 8.44 + spec 0a8a900
git status
# 期望: clean (working tree 0 uncommitted changes)
git log -3 --format='%(trailers:key=Co-Authored-By,valueonly)'
# 期望: 3 行空 (0 Co-Authored-By, 跟 Phase 8.35-8.44 policy 一致)
```

- [ ] **Step 2: Push 3 commits to origin/master**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git push origin master
# 期望: 3 commits pushed (8.45.1 / 8.45.2 / 8.45.3)
# 注: spec 0a8a900 已在 origin/master (上轮 Phase 8.44.6 push 已含)
```

- [ ] **Step 3: 验证 push 成功 (HEAD = origin/master)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git rev-parse HEAD origin/master
# 期望: 2 行同 SHA (push 验证)

git log origin/master --oneline -5
# 期望: 3 Phase 8.45 commits 落 origin (8.45.1 / 8.45.2 / 8.45.3)
```

- [ ] **Step 4: 0 Co-Authored-By trailers 验证**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
git log -3 --format='%(trailers:key=Co-Authored-By,valueonly)'
# 期望: 3 行空 (0 Co-Authored-By, 跟 Phase 8.35-8.44 policy 一致)
```

- [ ] **Step 5: Final verification 全套 (跟 Phase 8.44 1:1 收尾)**

```bash
cd /home/ailearn/projects/AI-Incursion/domains/IP创作/projects/LingWen/novel-factory
pytest -q 2>&1 | tail -3
# 期望: 2262 passed, 0 failed, 27 skipped 维持 (0 改 backend)
ruff check . 2>&1 | tail -3
# 期望: All checks passed!

cd dashboard/frontend
corepack pnpm@9.15.0 vitest run 2>&1 | tail -3
# 期望: Test Files 25 passed (25) / Tests 109 passed (109) / 0 failed
corepack pnpm@9.15.0 lint:all 2>&1 | tail -3
# 期望: EXIT=0
corepack pnpm@9.15.0 build 2>&1 | tail -3
# 期望: 0 error
corepack pnpm@9.15.0 e2e:smoke --list 2>&1 | tail -5
# 期望: 4 tests listed (page title / navigate / ws connect / error banner)
```

- [ ] **Step 6: memory sync — phases-8-dashboard.md 同步**

Edit `~/.claude/projects/-home-ailearn-projects-AI-Incursion-domains-IP---projects-LingWen/memory/phases-8-dashboard.md`:

(a) H1 (L1) update — "Phase 8.16-8.44" → "Phase 8.16-8.45":

BEFORE:
```
# Phase 8.16-8.44 — Dashboard polish + build + test infra 收尾
```

AFTER:
```
# Phase 8.16-8.45 — Dashboard polish + build + test infra 收尾
```

(b) L3-4 核心 list 扩到 include 8.45 (跟 Phase 8.44 entry 对齐):

BEFORE (current 8.44 entry):
```
- Phase 8.44 (2026-06-09): vitest --coverage v8 + lint-staged 接入 + pre-commit pytest smoke + Codecov frontend + Codecov backend
```

AFTER (新增 8.45 entry):
```
- Phase 8.44 (2026-06-09): vitest --coverage v8 + lint-staged 接入 + pre-commit pytest smoke + Codecov frontend + Codecov backend
- Phase 8.45 (2026-06-09): DecisionCard 3 status testid + OverviewPage e2e 加深 5 态 + Playwright runner install + 1 E2E smoke spec
```

(c) Append 8.45 entry (跟 8.44 entry 模式 1:1, 在文件末尾):

```markdown

## Phase 8.45 (2026-06-09)

UI completeness Super-phase C 收尾, 3 sub-phases / 3 commits 推 origin/master.
1:1 跟 Phase 8.43 (A) + 8.44 (B) 模式, 0 --no-verify (3 commit 全 hook ✅).

Sub-phases:
- **8.45.1** DecisionCard 3 status testid: status-resolved / -deferred / -cancelled,
  跟 Phase 8.38 kind testid decision-kind-${kindClass} 1:1 mirror, generic
  status-badge 保留 (Phase 8.32 加的). 1 .vue 改 (+1 line :data-testid + 1 statusKey
  computed) + 1 new spec (4 tests).
- **8.45.2** OverviewPage e2e 加深: 5 tests 覆盖 error / loading / empty /
  refresh error recovery / partial data 5 态. 1 small .vue 改 (+1 line v-if
  包裹 + 2 line v-else empty fallback <p data-testid="chapters-empty">暂无章节</p>,
  R4 verify 后) + 1 new spec (5 tests). 7 旧 dashboard.spec.ts 0 破.
- **8.45.3** Playwright runner install: +1 devDep @playwright/test ^1.49.0 + 1
  script e2e:smoke + 1 new playwright.config.js + 1 new e2e-smoke/smoke.spec.js
  (4 tests) + 1 small .vue 改 (+1 line data-testid="ws-status" R5 verify 后) +
  3 ceremonial header 1-line update (cost-bar-chart-tier-mode / -trend-chart-per-
  tier / sidebar_cost_banner). 0 CI integration (vitest 仍 primary gate). 0
  browser download (npx playwright install chromium opt-in ~500MB).

Verifications:
- vitest 25 spec / 109 tests / 0 failed (23 baseline + 1 status spec 4 tests +
  1 error-loading spec 5 tests, e2e-smoke 走 exclude)
- pnpm lint:all EXIT=0 维持
- pnpm build 0 error 维持
- pnpm e2e:smoke --list → 4 tests listed
- pytest 2262 passed 0 failed 27 skipped 维持
- ruff 0 issues 维持
- 0 Co-Authored-By trailers 验证

Out of scope (后续 super-phase / phase):
- Phase 8.46+ Super-phase D (TS strict mode) 独立大方向
- Playwright CI integration (separate workflow + matrix + cache + browser install)
- 3 ceremonial vitest-化 (跟 Phase 8.30b pattern, 1:1 转换)
- DecisionCard meta-info sub-element testid (4 elements)
- WorkflowGraph 4 status testid (跟 DecisionCard 8.45.1 模式重新评估)
- More e2e specs (DecisionsPage / WorkflowsPage e2e)
- Browser download cache (CI opt-in npx playwright install --with-deps chromium)
- Visual regression specs (Playwright screenshot baseline)
- MEMORY.md 拆 phases-8-dashboard.md 39+ entries 到 phases.md topic (本 phase 后
  变 40+, 必拆 followup)
```

- [ ] **Step 7: Task 4 done (0 commit, push + memory sync 完)**

(注: phases-8-dashboard.md 在 `~/.claude/projects/.../memory/`, 不走 git. Task 4 done = 3 commits 推 origin + memory 同步 + 0 commit. 跟 Phase 8.44 Task 6 1:1 模式)

---

## Out of Scope (留后续 super-phase / phase)

- **Super-phase D (Phase 8.46+)**: TS strict mode (tsconfig.json + strict: true + 修 11 .ts) — 独立大方向
- **Playwright CI integration**: separate workflow + matrix + cache + browser install
- **3 ceremonial vitest-化**: 跟 Phase 8.30b pattern, 1:1 转换
- **Browser download cache**: CI opt-in `npx playwright install --with-deps chromium`
- **Visual regression specs**: Playwright screenshot baseline
- **More interaction e2e tests**: full chapter review flow / decision resolve flow
- **DecisionCard meta-info sub-element testid**: 4 elements (resolved_by / resolution / resolved_at / reason) — 留 followup
- **Page-level e2e for DecisionsPage / WorkflowsPage**: Phase 8.39 vitest spec 1:1 镜像
- **WorkflowGraph 4 status testid**: 跟 DecisionCard 8.45.1 模式重新评估
- **DashboardPage e2e**: OverviewPage 现有 25 tests baseline OK, 0 跟 e2e 交互
- **ESLint rule for Playwright spec style**: 0 加, YAGNI
- **0 Playwright test in dashboard-frontend-ci.yml**: 留 followup, vitest 仍 primary CI gate
- **MEMORY.md 拆 40+ entries 到 phases.md topic**: 本 phase 后变 40+, 必拆 followup
- **0 改 MEMORY.md L8 "Phase 8.16-8.24" pointer typo**: 留 followup 跟 MEMORY.md 拆一起做

---

## Risks + Mitigations

| # | Risk | Mitigation |
|---|------|-----------|
| R1 | Playwright 0 装 browser 装 dev 装 → 误以为可跑 | Header comment 显式 "需 npx playwright install chromium opt-in" |
| R2 | @playwright/test peer dep 跟 vitest / eslint conflict | pnpm install 验证 (通常无 conflict; playwright 1.49 standalone, ~5MB install) |
| R3 | 8.45.1 改 `:data-testid` 动态 attribute ESLint vue/attribute-hyphenation 误报 | 走 string template `` `:status-${statusKey}` `` OK; vue/attribute-hyphenation 走 Phase 8.43.6 baseline 0 errors 维持 |
| R4 | 8.45.2 empty state component 0 显 "暂无数据" 文案 | spec 走 grep verify, 缺则 add 1 small `<p v-if="chapters.length === 0">暂无章节</p>` template change (subordinate to 8.45.2 主 scope) |
| R5 | 8.45.3 ws-status data-testid App.vue 0 存在 | spec 走 grep verify, 缺则 add 1 small `data-testid="ws-status"` (Vue 3.5 inherit fallthrough to SidebarCostBanner root) |
| R6 | pnpm 11.5 / Node 20.20 compat | 跟 Phase 8.42 一样 corepack pnpm@9.15.0 workaround |
| R7 | 3 commits Co-Authored-By 风险 | 显式 0 footer (per global settings.json 兜底) |
| R8 | pre-commit hook pytest smoke 50s 触发 | 0 改 backend 0 risk; 8.45.1/8.45.2/8.45.3 都干净 |
| R9 | 0 --no-verify 需要 | 跟 8.44 1:1 全 hook ✅, 8.45.1/8.45.2/8.45.3 都干净 (frontend-only changes) |
| R10 | 8.45.1 改 DecisionCard.vue 0 behavior 改 | testid additive only, 0 改 statusBadge computed / 0 改 template logic / 0 改 props |
| R11 | 8.45.3 装 @playwright/test 磁盘空间 | 装 devDep ~5MB (no browser); 装 browser opt-in ~500MB 留 dev manual |
| R12 | 8.45.3 dev 跑 e2e:smoke 需启 vite | spec document dev workflow (manual vite + manual smoke); CI 0 集成 |
| R13 | 8.45.2 spec 改 OverviewPage.vue 1 small template (empty fallback) | 走 R4 verify 是否真需; 需则 1 line 改 (additive, 不破 7 旧 dashboard.spec.ts tests) |
| R14 | SidebarCostBanner 多 root / fragment → ws-status fallthrough 失败 | spec 走 Vue 3.5 inherit fallthrough; 失败则改 wrapper div 加 data-testid (1 line 备选) |
| R15 | 8.45.3 push 3 commits 失败 (网络) | 1 retry, 仍 fail → 排查 gh 鉴权, 0 force-push |
| R16 | playwright.config.js 错 → e2e:smoke 跑 0 → 0 smoke 信号 | 走 `pnpm e2e:smoke --list` dry-run 验证 (Task 3 step 8) |

---

## Critical Files

### Created (4)
- `dashboard/frontend/tests/unit/decision-card-status.spec.ts` (~80L, 8.45.1) — 4 tests 验证 3 status testid
- `dashboard/frontend/tests/unit/dashboard-error-loading.spec.ts` (~140L, 8.45.2) — 5 tests 5 边界态
- `dashboard/frontend/playwright.config.js` (~30L, 8.45.3) — Playwright config
- `dashboard/frontend/tests/e2e-smoke/smoke.spec.js` (~60L, 8.45.3) — 4 E2E smoke tests
- `docs/superpowers/plans/2026-06-09-phase8.45-ui-completeness.md` (本 plan)

### Modified (8)
- `dashboard/frontend/src/components/DecisionCard.vue` (8.45.1: +1 line + 1 computed, 加 3 status testid)
- `dashboard/frontend/src/pages/OverviewPage.vue` (8.45.2: +1 small empty fallback template, R4 verify 后)
- `dashboard/frontend/src/App.vue` (8.45.3: +1 small ws-status testid, R5 verify 后)
- `dashboard/frontend/package.json` (8.45.3: +1 devDep `@playwright/test` + 1 script `e2e:smoke`)
- `dashboard/frontend/pnpm-lock.yaml` (8.45.3: auto by pnpm install)
- `dashboard/frontend/vitest.config.js` (8.45.3: +1 line exclude `tests/e2e-smoke/**`)
- `dashboard/frontend/tests/e2e/cost-bar-chart-tier-mode.spec.js` (8.45.3: 1 line header comment update)
- `dashboard/frontend/tests/e2e/cost-trend-chart-per-tier.spec.js` (8.45.3: 1 line header comment update)
- `dashboard/frontend/tests/e2e/sidebar_cost_banner.spec.js` (8.45.3: 1 line header comment update)
- `~/.claude/projects/.../memory/phases-8-dashboard.md` (8.45.4: H1 + L3 + L4 edit + 8.45 entry append)

### NOT modified (~25)
- 14 .vue in `dashboard/frontend/src/` (除 3 small additive changes)
- 23 vitest spec in `dashboard/frontend/tests/unit/` (除 2 new)
- 5 composables / `api/index.js` / `eslint-rules/*` / `eslint.config.js` / `index.html` / `vite.config.js` (除 vitest.config.js 1 line)
- `dashboard-frontend-ci.yml` / `test.yml` / `real-llm-tests.yml` (8.45 0 CI 集成, 留 followup)
- `.husky/pre-commit` / `.lintstagedrc.json` (Phase 8.44.2+8.44.3 已就位)
- backend Python (infra/agent_system / dashboard backend / cost_tracker / cost_persistence / cost_router)
- 9 historical spec/plan docs (`:NNN` 行号不动)
- 12 SCENARIOS 路由 (Python side)
- CLAUDE.md / pyproject.toml / pytest.ini
- `dashboard/frontend/.husky/_/h` (husky helper, 0 改)
- `.state/*.db` (gitignored) / `.env` (0 改 0 泄漏)

---

## 完成定义 (DoD)

- [ ] `docs/superpowers/specs/2026-06-09-phase8.45-ui-completeness-design.md` 创建 (commit `0a8a900`)
- [ ] `docs/superpowers/plans/2026-06-09-phase8.45-ui-completeness.md` 创建 (本 plan)
- [ ] `dashboard/frontend/src/components/DecisionCard.vue` +1 line :data-testid + 1 statusKey computed (Task 1, 8.45.1)
- [ ] `dashboard/frontend/tests/unit/decision-card-status.spec.ts` 4 tests passed (Task 1, 8.45.1)
- [ ] `dashboard/frontend/src/pages/OverviewPage.vue` +1 small empty fallback template (Task 2, 8.45.2 R4 verify 后)
- [ ] `dashboard/frontend/tests/unit/dashboard-error-loading.spec.ts` 5 tests passed (Task 2, 8.45.2)
- [ ] `dashboard/frontend/src/App.vue` +1 small ws-status data-testid (Task 3, 8.45.3 R5 verify 后)
- [ ] `dashboard/frontend/package.json` +1 devDep @playwright/test + 1 script e2e:smoke (Task 3, 8.45.3)
- [ ] `dashboard/frontend/playwright.config.js` 创建 (Task 3, 8.45.3)
- [ ] `dashboard/frontend/tests/e2e-smoke/smoke.spec.js` 4 tests listed (Task 3, 8.45.3)
- [ ] `dashboard/frontend/vitest.config.js` +1 line exclude e2e-smoke (Task 3, 8.45.3)
- [ ] 3 ceremonial specs +1 line header comment each (Task 3, 8.45.3)
- [ ] 3 commits 推 origin/master (Task 1-3 + Task 4 push)
- [ ] 0 --no-verify (3 commit 全 hook ✅)
- [ ] vitest 25 spec / 109 tests passed 维持 (23 baseline + 2 new spec 9 tests)
- [ ] pytest 2262 passed 0 failed 27 skipped 维持
- [ ] ruff 0 issues 维持
- [ ] pnpm build 0 error 维持
- [ ] pnpm lint:all EXIT=0 维持
- [ ] pnpm e2e:smoke --list 4 tests listed
- [ ] 0 playwright browser download (npx install opt-in, 留 dev manual)
- [ ] git rev-parse HEAD origin/master 2 行同 SHA
- [ ] 0 改 23 旧 vitest spec / 0 改 14 .vue except 3 small additive / 0 改 9 historical spec/plan docs `:NNN` 行号 / 0 改 12 SCENARIOS 路由 / 0 改 backend Python / 0 改 CLAUDE.md / pyproject.toml / pytest.ini
- [ ] 0 force-push / 0 amend (除 sub-phase fail 救火显式声明) / 0 Co-Authored-By / 0 DANGEROUSLY_SKIP_PERMISSIONS / 0 误改 .env / 0 API key 泄漏
- [ ] memory sync `phases-8-dashboard.md` H1 + L3 + L4 edit + 8.45 entry append

---

## 完成后下一步 (后续 super-phase / phase)

- **Phase 8.46+ Super-phase D**: TS strict mode (tsconfig.json + strict: true + 修 11 .ts 文件) — 独立大方向
- **Phase 9-14 跨卷涟漪**: 5 super-phase 独立方向, 跟 frontend 收尾并行
- **Phase 8.45+ followups**: Playwright CI / 3 ceremonial vitest-化 / DecisionCard meta-info testid / WorkflowGraph 4 status testid / 更多 e2e specs / MEMORY.md 拆 entries
