# Plan: Phase 7.3 — novel_writing 加 polish 节点 + 清理 4 LOW

> Doc 4 §11 Phase 7.3
> Spec: `docs/superpowers/specs/2026-06-04-phase7.3-novel-writing-polish-node-design.md`
> 父 plan: `docs/superpowers/plans/2026-06-04-phase7.2-polisher-llm.md`
> Baseline: 2038 passed, 0 failed, 17 skipped (Phase 7.2 末)
> 目标 baseline: ~2043-2044 passed (+5-6 tests, 0 破坏)

---

## 实施原则 (沿用 Phase 7.2)

- **TDD 严格 RED → GREEN**: 每个 task 先写 failing test,再写最小实现
- **Subagent-driven**: 用 implementer + spec-reviewer + code-quality-reviewer 三阶段
- **测试不破坏 Phase 7.2**:`test_polisher_llm.py` 15 tests 必须仍全 pass
- **1 final commit**:`feat(workflow): phase 7.3 — add polish_chapter node + cleanup 4 LOW`
- **不 commit 状态文件**:`infra/.state/workflow.db` 等
- **MEMORY.md < 200 lines**: 写完 push 后追加 1 行 Phase 7.3 entry

---

## Task 1: RED — 加 5 个新测试覆盖 polish 节点 + 4 节点 → 5 节点契约

**Files**:
- Modify: `tests/agent_system/test_master_controller_stub_router_e2e.py` (line 216-227 `test_at_least_one_llm_call` + 加 ~5 tests)

**Step 1.1: 写 test_novel_writing_has_polish_node (failing)**

```python
# 加到 TestNovelWritingE2E 类
def test_novel_writing_has_polish_node(self, tmp_path: Path):
    """Phase 7.3: novel_writing.yaml 应含 polish_chapter 节点 (在 review 之后,emit 之前)"""
    router, _ = make_stub_router()
    result = self._run_novel_writing(tmp_path, router)

    executions = result["executions"]
    assert "polish_chapter" in executions, (
        f"expected polish_chapter in executions, got {list(executions.keys())}"
    )
    # 拓扑: review → polish → emit
    assert "review_chapter" in executions
    assert "emit_chapter" in executions
```

**Step 1.2: 写 test_polish_node_completed_in_production_path (failing)**

```python
def test_polish_node_completed_in_production_path(self, tmp_path: Path):
    """Phase 7.3: polish_chapter 节点应跑完,不是 SKIPPED/FAILED"""
    router, _ = make_stub_router()
    result = self._run_novel_writing(tmp_path, router)

    polish_exec = result["executions"]["polish_chapter"]
    assert polish_exec.status == NodeStatus.COMPLETED
    assert polish_exec.cost_tokens == 0  # token 估算在 TieredRouter 层
```

**Step 1.3: 写 test_polish_chapter_output_has_content (failing)**

```python
def test_polish_chapter_output_has_content(self, tmp_path: Path):
    """Phase 7.3: polish 节点 output 含非空 content 字段 (供 emit 落盘)"""
    router, _ = make_stub_router()
    result = self._run_novel_writing(tmp_path, router)

    polish_output = result["executions"]["polish_chapter"].output
    assert isinstance(polish_output, dict)
    assert "content" in polish_output
    content = polish_output["content"]
    assert isinstance(content, str)
    assert len(content) > 0, "polish_chapter output.content should be non-empty"
```

**Step 1.4: 写 test_emit_chapter_depends_on_polish_yaml_static (failing)**

```python
# 加到同一文件, 不在 _run_novel_writing 框架内
def test_emit_chapter_depends_on_polish_yaml_static(self):
    """Phase 7.3: YAML 静态检查 — emit_chapter.depends_on == [polish_chapter]"""
    import yaml
    from pathlib import Path

    yaml_path = Path(__file__).parent.parent.parent / "infra" / "got" / "workflows" / "novel_writing.yaml"
    assert yaml_path.exists(), f"workflow YAML not found: {yaml_path}"
    data = yaml.safe_load(yaml_path.read_text(encoding="utf-8"))

    nodes_by_id = {n["id"]: n for n in data["nodes"]}
    assert "polish_chapter" in nodes_by_id
    emit = nodes_by_id["emit_chapter"]
    assert "polish_chapter" in emit["depends_on"], (
        f"emit_chapter.depends_on should contain polish_chapter, got {emit['depends_on']}"
    )
```

**Step 1.5: 写 test_minimal_e2e_unchanged_no_polish (反向断言)**

```python
# 在 TestMinimalWorkflowE2E 类加
def test_minimal_e2e_unchanged_no_polish(self, tmp_path: Path):
    """Sanity: minimal_e2e.yaml 不应有 polish 节点 (防止误改)"""
    router, _ = make_stub_router()
    master = make_master_with_router(tmp_path, router)

    result = master.run_workflow(
        workflow_name="minimal_e2e",
        initial_inputs={
            "chapter_num": 1,
            "outline": self.MINIMAL_OUTLINE,
            "characters": [],
            "memory_context": {},
            "style_guide": {},
            "use_llm": True,
        },
    )
    assert "polish_chapter" not in result["executions"]
```

**Step 1.6: 收紧 test_at_least_one_llm_call 断言**

```python
# 在 TestNovelWritingE2E 类, 改 test_at_least_one_llm_call
def test_at_least_one_llm_call(self, tmp_path: Path):
    """Phase 7.3: novel_writing 5 节点 — write(1) + audit(1) + dialogue(1) + pacing(1) = 4 次 LLM
    read_snapshot/emit_chapter 旁路,polish_chapter 走 ai_trace_removal → master.polish_chapter
    → 2 LLM 路径 (Phase 7.2)"""
    router, providers = make_stub_router()
    self._run_novel_writing(tmp_path, router)

    total_calls = sum(len(p.calls) for p in providers.values())
    assert total_calls >= 4, (
        f"expected at least 4 LLM calls (write + audit + dialogue + pacing), "
        f"got {total_calls}. per-provider: {[(n, len(p.calls)) for n, p in providers.items()]}"
    )
```

**Step 1.7: 跑测试,验证 RED**

```bash
pytest tests/agent_system/test_master_controller_stub_router_e2e.py -q -k "test_novel_writing_has_polish_node or test_polish_node_completed or test_polish_chapter_output_has_content or test_emit_chapter_depends_on_polish_yaml_static or test_minimal_e2e_unchanged_no_polish or test_novel_writing and test_at_least"
```

**Expected**: 5 new tests FAIL (no polish node), 1 tightened test FAILS (`>= 4` vs current `>= 2`)

**Step 1.8: Commit (RED only)**

```bash
git add tests/agent_system/test_master_controller_stub_router_e2e.py
git commit -m "test(workflow): phase 7.3 RED — 5 polish node tests + tighten >= 2 to >= 4"
```

---

## Task 2: GREEN — 改 novel_writing.yaml 加 polish 节点

**Files**:
- Modify: `infra/got/workflows/novel_writing.yaml` (在 review_chapter 后插 polish_chapter + 改 emit_chapter.depends_on)

**Step 2.1: 在 review_chapter 后插 polish_chapter 节点**

```yaml
  # === Stage 3.5: LLM 增强润色 (Phase 7.3) ===
  - id: polish_chapter
    type: synthesis
    name: Polish Chapter
    description: LLM 增强润色 — 对话自然化 + 节奏调整 + AI 痕迹去除 (Phase 7.2 路径)
    depends_on: [review_chapter]
    prompt_scenario: ai_trace_removal
    output_schema: PolishedDraft
    token_budget: 6000
    timeout_s: 90
```

**Step 2.2: 改 emit_chapter.depends_on**

```yaml
  # === Stage 4: 落盘输出 (Phase 7.3: 改 depends_on) ===
  - id: emit_chapter
    type: output
    name: Emit Chapter
    description: 将润色后的章节写入 content_repo + 更新索引
    depends_on: [polish_chapter]    # ← 改: 从 [review_chapter] 改为 [polish_chapter]
    prompt_scenario: null
    output_schema: null
    token_budget: 0
    timeout_s: 30
```

**Step 2.3: 跑 Task 1 的 5 个新测试,验证 GREEN**

```bash
pytest tests/agent_system/test_master_controller_stub_router_e2e.py -q -k "test_novel_writing_has_polish_node or test_polish_node_completed or test_polish_chapter_output_has_content or test_emit_chapter_depends_on_polish_yaml_static or test_minimal_e2e_unchanged_no_polish"
```

**Expected**: 5 new tests PASS

**Step 2.4: 跑 test_at_least_one_llm_call (novel_writing), 验证 >= 4 PASS**

```bash
pytest tests/agent_system/test_master_controller_stub_router_e2e.py::TestNovelWritingE2E::test_at_least_one_llm_call -v
```

**Expected**: PASS (write 1 + audit 1 + dialogue 1 + pacing 1 = 4)

**Step 2.5: 跑 minimal_e2e test, 验证未破坏**

```bash
pytest tests/agent_system/test_master_controller_stub_router_e2e.py::TestMinimalWorkflowE2E -v
```

**Expected**: ALL PASS (minimal_e2e.yaml 2 节点无 polish, 不受影响)

**Step 2.6: 跑其他用 novel_writing 的测试, 验证未破坏**

```bash
pytest tests/agent_system/test_master_controller_workflow.py tests/agent_system/test_decision_integration.py tests/dashboard/test_decision_api.py -q
```

**Expected**: ALL PASS (这些测试用 mock 替换 master 方法 / 或只走 dashboard API, 不依赖 4 节点假设)

---

## Task 3: 清理 4 LOW (Phase 7.2 review 遗留)

**Files**:
- Modify: `infra/agent_system/agents/polisher/tools.py`
- Modify: `infra/agent_system/agents/polisher/prompts.py`

**Step 3.1: LOW-1 — 删 `self._variant_loader` dead 字段 + 守卫**

```python
# tools.py:31-35
def __init__(self, router=None):
    super().__init__(router)
    self._current_reader_id: Optional[str] = None
    if VARIANT_LOADER_AVAILABLE:
        self._variant_loader = get_variant_loader()
```

→

```python
def __init__(self, router=None):
    super().__init__(router)
    self._current_reader_id: Optional[str] = None
```

(注: variant_loader 是模块级单例 `get_variant_loader()`, 实例字段没意义)

**Step 3.2: LOW-2 — 抽 DEFAULT_MAX_TOKENS 常量**

```python
# tools.py 顶部 (imports 后)
DEFAULT_MAX_TOKENS = 3000


# 改 optimize_dialogue_llm (line 95)
return self.chat(prompt=prompt, system=system, temperature=0.5, max_tokens=DEFAULT_MAX_TOKENS)


# 改 adjust_pacing_llm (line 105)
return self.chat(prompt=prompt, system=system, temperature=0.4, max_tokens=DEFAULT_MAX_TOKENS)
```

**Step 3.3: LOW-3 — 删 tools.py 未用的 imports**

```python
# tools.py:12-19
from .prompts import (
    build_dialogue_prompt,
    build_pacing_prompt,
    build_polish_prompt,         # ← 删
    get_dialogue_system_prompt,
    get_pacing_system_prompt,
    get_polish_system_prompt,    # ← 删
)
```

→

```python
from .prompts import (
    build_dialogue_prompt,
    build_pacing_prompt,
    get_dialogue_system_prompt,
    get_pacing_system_prompt,
)
```

**Step 3.4: LOW-4 — 删 prompts.py 未用的 2 函数**

```python
# prompts.py:107-124 (build_polish_prompt + get_polish_system_prompt)
# ==================== Polish (主入口, 暂未使用, 保留) ====================

def build_polish_prompt(content: str, reader_id: str = "A") -> str:
    ...

def get_polish_system_prompt(reader_id: str = "A") -> str:
    ...
```

→ 整段删 (YAGNI — 留待 Phase 7.4+ 真正需要时再加)

**Step 3.5: 跑测试验证未破坏**

```bash
pytest tests/agent_system/test_polisher_llm.py -q
pytest tests/agent_system/test_agent_tools.py -q  # Phase 7.2 之前测试
```

**Expected**: ALL PASS (15 + 老的 polisher tests 都不依赖 build_polish_prompt)

**Step 3.6: ruff check**

```bash
ruff check infra/agent_system/agents/polisher/ tests/agent_system/test_master_controller_stub_router_e2e.py
```

**Expected**: 0 warnings

---

## Task 4: 全量回归 + MEMORY.md

**Step 4.1: 全量测试**

```bash
pytest tests/ -q --tb=line
```

**Expected**: 2038 + 5 new = 2043 passed, 0 failed, 17 skipped

**Step 4.2: ruff 全量**

```bash
ruff check .
```

**Expected**: 0 warnings

**Step 4.3: 追写 MEMORY.md (Phase 7.3 entry)**

在 MEMORY.md 已有 Phase 7.2 之后追写 1 行:

```markdown
- **Phase 7.3 (2026-06-04)**: Doc 4 §11 Phase 7.3 — novel_writing.yaml 4 → 5 节点, 在 review_chapter 后插 polish_chapter (SYNTHESIS, scenario=ai_trace_removal, 2 LLM 路径); emit_chapter.depends_on [review_chapter] → [polish_chapter]; 顺手清 Phase 7.2 review 4 LOW (删 variant_loader dead 字段 / 抽 DEFAULT_MAX_TOKENS=3000 常量 / 删 unused build_polish_prompt + get_polish_system_prompt); 收紧 TestNovelWritingE2E::test_at_least_one_llm_call >= 2 → >= 4 (write + audit + dialogue + pacing); 5 新测试 (test_novel_writing_has_polish_node / test_polish_node_completed / test_polish_chapter_output_has_content / test_emit_chapter_depends_on_polish_yaml_static / test_minimal_e2e_unchanged_no_polish); 2038→2043 passed (+5); ruff 0; 1 commit
```

**Step 4.4: 最终 commit + push**

```bash
git add infra/agent_system/agents/polisher/tools.py \
        infra/agent_system/agents/polisher/prompts.py \
        infra/got/workflows/novel_writing.yaml \
        tests/agent_system/test_master_controller_stub_router_e2e.py \
        /home/ailearn/.claude/projects/-home-ailearn-projects-AI-Incursion-domains-IP---projects-LingWen/memory/MEMORY.md

git commit -m "$(cat <<'EOF'
feat(workflow): phase 7.3 — add polish_chapter node + cleanup 4 LOW

novel_writing.yaml: 4 → 5 节点
- 插 polish_chapter (SYNTHESIS, scenario=ai_trace_removal, 2 LLM 路径)
- emit_chapter.depends_on [review_chapter] → [polish_chapter]
- 数据流: review.output.content (Phase 7.1 setdefault 透传) → _handler_polish → master.polish_chapter → emit

顺手清 Phase 7.2 review 4 LOW:
- LOW-1: 删 self._variant_loader dead 字段 (variant_loader 是模块级单例)
- LOW-2: 抽 DEFAULT_MAX_TOKENS=3000 常量 (2 处 max_tokens 引用)
- LOW-3: 删 tools.py unused imports (build_polish_prompt, get_polish_system_prompt)
- LOW-4: 删 prompts.py unused 2 函数 (YAGNI, 留待 Phase 7.4+ 真正需要时再加)

测试:
- 收紧 TestNovelWritingE2E::test_at_least_one_llm_call: >= 2 → >= 4 (write+audit+dialogue+pacing)
- 5 新测试: has_polish_node / node_completed / output_has_content / yaml_static_depends / minimal_e2e_unchanged
- 不破坏 Phase 7.2 (test_polisher_llm.py 15 tests) + minimal_e2e 路径

2038→2043 passed (+5), 0 failed, 17 skipped, ruff 0
EOF
)"

git push origin master
```

---

## Critical files

| 文件 | 改动 |
|------|------|
| `infra/got/workflows/novel_writing.yaml` | +14 行 (polish_chapter) + 1 行 (emit.depends_on 改) |
| `infra/agent_system/agents/polisher/tools.py` | -5 行 (LOW-1/3 删) + 2 行 (LOW-2 常量) |
| `infra/agent_system/agents/polisher/prompts.py` | -16 行 (LOW-4 删) |
| `tests/agent_system/test_master_controller_stub_router_e2e.py` | +~50 行 (5 tests) + -2 行 (断言收紧) |
| `MEMORY.md` | +1 行 Phase 7.3 entry |

**预计 1 final commit** (跟 spec commit 分开),2-3 个 commit 总数 (含 RED 单独 1 个可选)

---

## 风险 + 缓解

| 风险 | 缓解 |
|------|------|
| novel_writing.yaml 改错 (缩进/字段名) | `_parse_node` 字段验证立即 fail-fast, test_workflow_loader.py 兜底 |
| 加 polish 节点破坏 test_decision_integration.py / test_master_controller_workflow.py | 已用 mock 替换 master 方法,不读 workflow YAML;Task 2.6 跑全量验证 |
| 4 LOW 改错 (误删有用代码) | LOW-3/4 删 unused 函数 (grep 全工程 0 调用);LOW-1 删 dead 字段;LOW-2 只抽常量,行为不变 |
| Polish 节点 2 LLM 让 LLM stub 计数从 2 → 4,某些旧测试可能 fail | Task 1.6 已收紧断言;Task 2.5 验证 minimal_e2e 不受影响 |
| subagent 误改 test_polisher_llm.py 删测试 | subagent 指令明确"不破坏 Phase 7.2 15 tests";Task 3.5 显式跑回归 |

---

## TDD sub-phases

| 阶段 | 动作 | 累计测试 |
|------|------|---------|
| T1.1-1.7 | RED: 5 新测试 + 1 收紧断言 → FAIL | 2038 (但 6 new fail) |
| T2.1-2.6 | GREEN: novel_writing.yaml 加节点 → PASS | 2043 (5 new pass) |
| T3.1-3.6 | 4 LOW 清理 + ruff 0 | 2043 (不破坏) |
| T4.1-4.4 | 全量 + MEMORY.md + push | 2043 |

---

## 验证

### 集成验证 (Task 4.1)

```python
# 后端
from infra.agent_system.master_controller import MasterController
from infra.got.workflow_loader import load_workflow

# 1. 加载 workflow
graph = load_workflow("novel_writing")
nodes = {n.node_id: n for n in graph.nodes.values()}
assert "polish_chapter" in nodes
assert nodes["polish_chapter"].type.value == "synthesis"
assert nodes["polish_chapter"].prompt_scenario == "ai_trace_removal"
assert "polish_chapter" in nodes["emit_chapter"].depends_on

# 2. 跑端到端
from tests.agent_system._e2e_helpers import make_stub_router, make_master_with_router
import tempfile

with tempfile.TemporaryDirectory() as tmp:
    router, providers = make_stub_router()
    master = make_master_with_router(Path(tmp), router)
    result = master.run_workflow(
        workflow_name="novel_writing",
        initial_inputs={
            "chapter_num": 1, "outline": {"chapters": [{"num": 1, "title": "第1章",
                "events": ["e1"], "word_count_target": 2500}]},
            "characters": [], "memory_context": {}, "style_guide": {},
            "timeline": [], "use_llm": True,
        },
    )
    assert result["summary"].completed == 5
    assert result["summary"].failed == 0
    assert result["executions"]["polish_chapter"].status.value == "completed"
    total_calls = sum(len(p.calls) for p in providers.values())
    assert total_calls >= 4  # write + audit + dialogue + pacing
```

### 最终 baseline 目标
- 2038 → **2043 passed** (+5), 0 failed, 17 skipped
- ruff 0 warnings
- 1-2 commits pushed to origin/master
- MEMORY.md +1 行 Phase 7.3 entry (保持 < 200 lines)
