# Plan: Phase 6.6 — Dashboard UI 全面 polish (A+B+D)

> Doc 4 §10 Phase 6.6
> Spec: `docs/superpowers/specs/2026-06-04-phase6.6-dashboard-ui-polish-design.md`
> 父 plan: `docs/superpowers/plans/2026-06-04-phase7.3-novel-writing-polish-node.md`
> Baseline: 2043 passed, 0 failed, 17 skipped (Phase 7.3 末)
> 目标 baseline: 2044 passed (+1 后端 test), 0 failed, 17 skipped

---

## 实施原则

- **3 独立 commits** (A / B / D 互不重叠,可独立回滚)
- **TDD per commit**: D 有 1 后端 test (RED → GREEN),A/B 纯前端用浏览器手测
- **零 Phase 6.1-6.5 回归**: 所有现有 dashboard tests 仍全 PASS
- **ruff 0 / vite build 通过**
- **MEMORY.md +1 行 Phase 6.6 entry** (写完 push 后)

---

## Task 1 (6.6.A): DecisionsPage 3 tabs + 折叠

**Files**:
- Modify: `dashboard/frontend/src/pages/DecisionsPage.vue` (改, ~30 行)

**Step 1.1: 加 `resolved` / `closed` computed**

```js
// DecisionsPage.vue line 72-86
const pending = ref([]);
const all = ref([]);
const activeTab = ref('pending');
const expanded = ref({ resolved: false, closed: false });  // ← NEW

const resolved = computed(() =>
  all.value.filter(d => d.status === 'resolved')
    .sort((a, b) => (b.resolved_at || '').localeCompare(a.resolved_at || ''))
);
const closed = computed(() =>
  all.value.filter(d => d.status === 'deferred' || d.status === 'cancelled')
    .sort((a, b) => (b.resolved_at || a.created_at || '').localeCompare(a.resolved_at || a.created_at || ''))
);

const displayList = computed(() => {
  if (activeTab.value === 'pending') {
    return pending.value.filter(d => d.status === 'pending')
      .sort((a, b) => (a.priority ?? 999) - (b.priority ?? 999));
  }
  if (activeTab.value === 'resolved') {
    return expanded.value.resolved ? resolved.value : [];
  }
  if (activeTab.value === 'closed') {
    return expanded.value.closed ? closed.value : [];
  }
  return [];
});

const tabs = computed(() => [
  { id: 'pending', label: '待处理', count: pending.value.filter(d => d.status === 'pending').length },
  { id: 'resolved', label: '已完成', count: resolved.value.length },
  { id: 'closed', label: '已取消/推迟', count: closed.value.length },
]);
```

**Step 1.2: 改 template — 加折叠箭头 + 计数**

```html
<!-- 替换 line 21-31 的 tab-bar -->
<nav class="tab-bar">
  <button
    v-for="tab in tabs"
    :key="tab.id"
    class="tab-btn"
    :class="{ 'tab-btn--active': activeTab === tab.id }"
    @click="activeTab = tab.id"
  >
    {{ tab.label }} ({{ tab.count }})
  </button>
</nav>

<!-- 替换 line 41 (empty-state 之上) -->
<div
  v-if="(activeTab === 'resolved' || activeTab === 'closed') && displayList.length === 0"
  class="empty-state pixel-card"
>
  <p class="pixel-text">
    {{ activeTab === 'resolved' ? '📋 暂无已解决决策' : '🚫 暂无已取消/推迟决策' }}
  </p>
  <button
    class="expand-toggle pixel-border"
    @click="expanded[activeTab] = !expanded[activeTab]; activeTab = activeTab"
  >
    {{ expanded[activeTab] ? '▾ 折叠' : '▸ 展开' }}
  </button>
</div>
```

**Step 1.3: 改 empty-state 条件 — 新加的 tab 区分提示**

```html
<!-- 替换 line 42 的空状态文字 -->
<div v-else-if="!displayList.length" class="empty-state pixel-card">
  <p class="pixel-text">
    🎉 当前没有
    <template v-if="activeTab === 'pending'">待处理</template>
    <template v-else-if="activeTab === 'resolved'">已解决</template>
    <template v-else>已取消/推迟</template>
    决策
  </p>
  <p v-if="activeTab !== 'pending'" class="hint">点击 "展开" 折叠控件查看历史</p>
  <p v-else class="hint">启动一个工作流后,系统会扫描 DECISION 节点并创建待审核决策</p>
</div>
```

**Step 1.4: 加 CSS — 折叠按钮 + tab 颜色**

```css
/* DecisionsPage.vue <style scoped> 末尾追加 */
.expand-toggle {
  font-size: 9px;
  font-family: 'Press Start 2P', monospace;
  padding: 6px 10px;
  background: var(--bg-secondary);
  cursor: pointer;
  margin-top: var(--space-sm);
}

.tab-btn[data-tab="resolved"] { border-bottom: 2px solid var(--color-success); }
.tab-btn[data-tab="closed"]   { border-bottom: 2px solid var(--color-warning); }
```

**Step 1.5: 浏览器手测 (subagent 跑)**

1. 启动 dev server (`cd dashboard/frontend && npm run dev`)
2. 访问 `http://localhost:5173/decisions`
3. 验证 3 tabs 显示
4. 启动一个 novel_writing workflow,等产生决策
5. 解决一个,验证 resolved tab 出现"已完成 (1)",展开后看到卡片
6. 推迟一个,验证 closed tab 出现
7. 取消一个,验证 closed tab 计数 +1

**Step 1.6: 提交**

```bash
git add dashboard/frontend/src/pages/DecisionsPage.vue
git commit -m "$(cat <<'EOF'
feat(dashboard): phase 6.6.A — DecisionsPage 3 tabs + 折叠

- pending tab: status='pending', 按 priority 升序
- resolved tab: status='resolved', 默认折叠, 按 resolved_at 倒序
- closed tab: status in (deferred, cancelled), 默认折叠
- 加 expand toggle 控件 (像素风 ▸/▾ 箭头)
- tab 底部加颜色指示线 (resolved=绿, closed=黄, 沿用 CSS 变量)
- 0 后端改动, 0 新 test (纯 UI, 浏览器手测)
EOF
)"
```

---

## Task 2 (6.6.B): DecisionCard status badge + meta info

**Files**:
- Modify: `dashboard/frontend/src/components/DecisionCard.vue` (改, ~35 行)

**Step 2.1: 加 `statusBadge` computed**

```js
// DecisionCard.vue <script setup> 末尾追加
const statusBadge = computed(() => {
  const map = {
    resolved: { label: '✅ 已解决', cls: 'status-resolved' },
    deferred: { label: '⏸ 已推迟', cls: 'status-deferred' },
    cancelled: { label: '❌ 已取消', cls: 'status-cancelled' },
  };
  return map[props.decision.status] || null;
});

const metaInfo = computed(() => {
  const d = props.decision;
  if (d.status === 'pending') return null;
  const parts = [];
  if (d.resolved_by) parts.push(`解决人: ${d.resolved_by}`);
  if (d.resolution) parts.push(`选项: ${d.resolution}`);
  if (d.resolved_at) {
    const dt = new Date(d.resolved_at);
    parts.push(`时间: ${dt.toLocaleString('zh-CN', { hour12: false })}`);
  }
  if (d.reason) parts.push(`${d.status === 'cancelled' ? '取消' : '推迟'}原因: ${d.reason}`);
  return parts.join(' · ');
});
```

**Step 2.2: 改 template — card-header 加 status badge**

```html
<!-- 替换 line 7-13 的 card-header -->
<div class="card-header">
  <span class="kind-badge" :class="`kind-${kindClass}`">{{ kindLabel }}</span>
  <span class="priority-badge" v-if="decision.priority !== undefined">
    P{{ decision.priority }}
  </span>
  <span
    v-if="statusBadge"
    class="status-badge"
    :class="statusBadge.cls"
  >{{ statusBadge.label }}</span>
  <span class="node-id">{{ decision.node_id }}</span>
</div>
```

**Step 2.3: 加 meta info 区**

```html
<!-- 替换 line 53 (error 之前) -->
<div v-if="metaInfo" class="meta-info">
  {{ metaInfo }}
</div>

<div v-if="error" class="error-text">{{ error }}</div>
```

**Step 2.4: 改 resolve/defer/cancel 按钮守卫 — pending 才显示**

```html
<!-- 替换 line 24-51 (options + actions 区块) -->
<div v-if="decision.status === 'pending'" class="options">
  <button
    v-for="opt in decision.options || []"
    :key="opt"
    class="option-btn pixel-border"
    :disabled="busy"
    @click="onResolve(opt)"
  >{{ opt }}</button>
</div>

<div v-if="decision.status === 'pending'" class="actions">
  <button
    class="action-btn defer-btn pixel-border"
    :disabled="busy"
    @click="onDefer"
  >推迟</button>
  <button
    class="action-btn cancel-btn pixel-border"
    :disabled="busy"
    @click="onCancel"
  >取消</button>
</div>

<div v-else class="actions actions-readonly">
  <span class="readonly-hint">🔒 此决策已{{
    decision.status === 'resolved' ? '解决' : decision.status === 'cancelled' ? '取消' : '推迟'
  }}</span>
</div>
```

**Step 2.5: 加 CSS — 4 种 status 颜色**

```css
/* DecisionCard.vue <style scoped> 末尾追加 */
.status-badge {
  font-size: 8px;
  font-family: 'Press Start 2P', monospace;
  padding: 3px 6px;
  border: 2px solid;
  margin-left: var(--space-xs);
}
.status-resolved  { color: #2e7d32; border-color: #2e7d32; background: #c8e6c9; }
.status-deferred  { color: #f57c00; border-color: #f57c00; background: #ffe0b2; }
.status-cancelled { color: #616161; border-color: #616161; background: #e0e0e0; }

.meta-info {
  font-size: 9px;
  font-family: monospace;
  color: var(--color-text-dim);
  background: var(--bg-primary);
  border-left: 3px solid var(--color-accent);
  padding: var(--space-xs) var(--space-sm);
  margin-top: var(--space-sm);
  word-break: break-all;
}

.actions-readonly {
  opacity: 0.6;
}
.readonly-hint {
  font-size: 9px;
  font-family: 'Press Start 2P', monospace;
  color: var(--color-text-dim);
}
```

**Step 2.6: 浏览器手测 (subagent 跑)**

- 验证 pending 决策显示选项按钮 (无 status badge)
- 验证 resolved 决策显示绿色 badge + meta info + 无操作按钮 + 🔒 提示
- 验证 deferred 决策显示黄色 badge + 推迟原因
- 验证 cancelled 决策显示灰色 badge + 取消原因

**Step 2.7: 提交**

```bash
git add dashboard/frontend/src/components/DecisionCard.vue
git commit -m "$(cat <<'EOF'
feat(dashboard): phase 6.6.B — DecisionCard status badge + meta info

- 加 statusBadge computed: resolved/deferred/cancelled → ✅⏸❌ (绿/黄/灰)
- 加 metaInfo computed: 解决人 + 选项 + 时间 + (取消/推迟)原因
- resolve/defer/cancel 按钮守卫: status !== 'pending' 时隐藏,显示 🔒 提示
- 加 CSS: 4 种 status 颜色 + meta info 左 border 强调块
- 0 后端改动 (decision_queue.HumanDecision.to_dict() 字段已就位)
- 0 新 test (纯 UI, 浏览器手测)
EOF
)"
```

---

## Task 3 (6.6.D): Mermaid 节点状态染色 (后端 + 前端 + 1 test)

**Files**:
- Modify: `dashboard/protocols.py` (加 `executions` 字段)  — ~5 行
- Modify: `dashboard/app.py` (3 处: WorkflowStatusResponse + WorkflowMermaidResponse + /mermaid 端点)  — ~15 行
- Modify: `dashboard/frontend/src/api/index.js` (`fetchWorkflowGraph` 加 `includeStatus`)  — ~3 行
- Modify: `dashboard/frontend/src/pages/WorkflowsPage.vue` (`loadGraph` 传 `includeStatus: true`)  — 1 行
- New: `tests/dashboard/test_mermaid_status.py` (3 tests)  — ~80 行

### Step 3.1 (RED): 写 3 tests

**File**: `tests/dashboard/test_mermaid_status.py`

```python
"""Phase 6.6.D — /mermaid 端点 ?include_status=true 测试

- 默认 false → status_applied=False, node_statuses={}
- true + active workflow → status_applied=True, node_statuses 含各节点状态
- true + 无活跃工作流 → status_applied=False, node_statuses={} (不报错)
"""
from __future__ import annotations

from typing import Any
from pathlib import Path
import pytest
from fastapi.testclient import TestClient

from dashboard.app import create_app
from infra.agent_system.master_controller import MasterController


class _StubMaster:
    """满足 MasterControllerLike 协议 — 控制 get_active_workflow_status 返回"""
    def __init__(self, executions: dict[str, str] | None):
        self._executions = executions

    def get_active_workflow_status(self) -> dict[str, Any]:
        if self._executions is None:
            return {
                "is_active": False,
                "workflow_name": None,
                "pending_decisions": [],
            }
        return {
            "is_active": True,
            "workflow_name": "novel_writing",
            "completed": sum(1 for s in self._executions.values() if s == "completed"),
            "failed": sum(1 for s in self._executions.values() if s == "failed"),
            "paused": False,
            "paused_nodes": [],
            "node_count": len(self._executions),
            "steps": 1,
            "pending_decisions": [],
            "executions": self._executions,
        }


@pytest.fixture
def tmp_db(tmp_path: Path) -> Path:
    return tmp_path / "rp.db"


class TestMermaidStatusEndpoint:
    """Phase 6.6.D — Mermaid 节点状态染色"""

    def test_mermaid_endpoint_default_does_not_apply_status(self, tmp_db: Path):
        """?include_status 缺省 → status_applied=False, node_statuses={} (保后向兼容)"""
        stub = _StubMaster({"write_chapter": "completed", "review_chapter": "completed"})
        app = create_app(db_path=tmp_db, master_controller=stub)
        client = TestClient(app)

        response = client.get("/api/workflows/novel_writing/mermaid")
        assert response.status_code == 200
        data = response.json()
        assert data["status_applied"] is False
        assert data["node_statuses"] == {}

    def test_mermaid_endpoint_with_include_status_returns_node_statuses(self, tmp_db: Path):
        """?include_status=true + active workflow → status_applied=True + node_statuses 完整"""
        stub = _StubMaster({
            "read_snapshot": "completed",
            "write_chapter": "completed",
            "review_chapter": "running",
            "polish_chapter": "pending",
            "emit_chapter": "pending",
        })
        app = create_app(db_path=tmp_db, master_controller=stub)
        client = TestClient(app)

        response = client.get("/api/workflows/novel_writing/mermaid?include_status=true")
        assert response.status_code == 200
        data = response.json()
        assert data["status_applied"] is True
        assert data["node_statuses"] == {
            "read_snapshot": "completed",
            "write_chapter": "completed",
            "review_chapter": "running",
            "polish_chapter": "pending",
            "emit_chapter": "pending",
        }
        # mermaid 字符串应包含 status-based class 声明
        assert "class read_snapshot node-completed" in data["mermaid"]
        assert "class review_chapter node-running" in data["mermaid"]
        assert "class polish_chapter node-pending" in data["mermaid"]

    def test_mermaid_endpoint_with_no_active_workflow_returns_empty_statuses(self, tmp_db: Path):
        """?include_status=true + 无活跃工作流 → status_applied=False, node_statuses={} (不报错)"""
        stub = _StubMaster(None)  # is_active=False
        app = create_app(db_path=tmp_db, master_controller=stub)
        client = TestClient(app)

        response = client.get("/api/workflows/novel_writing/mermaid?include_status=true")
        assert response.status_code == 200
        data = response.json()
        assert data["status_applied"] is False
        assert data["node_statuses"] == {}
        # 仍返回 valid mermaid 字符串
        assert "graph TD" in data["mermaid"]
```

**Step 3.2: 跑测试,验证 RED**

```bash
pytest tests/dashboard/test_mermaid_status.py -q
```

**Expected**: 3 tests FAIL
- test 1: 缺 `status_applied` / `node_statuses` 字段 (Pydantic 422)
- test 2: 端点不接 `include_status` query param + 没 `executions` 字段
- test 3: 没 `include_status` 参数处理

**Step 3.3: GREEN — 改 `protocols.py`**

```python
# dashboard/protocols.py:243-253 末尾加 executions 字段
return {
    "is_active": True,
    "workflow_name": workflow_name,
    "completed": completed,
    "failed": failed,
    "paused": bool(paused_nodes),
    "paused_nodes": paused_nodes,
    "node_count": len(list(graph.node_ids())),
    "steps": steps,
    "pending_decisions": pending,
    "executions": {
        nid: ex.status.value  # "completed" | "running" | "waiting" | "pending" | "failed" | ...
        for nid, ex in executions.items()
    },
}
```

**Step 3.4: GREEN — 改 `app.py` WorkflowStatusResponse + WorkflowMermaidResponse**

```python
# dashboard/app.py:126 WorkflowStatusResponse 加字段
class WorkflowStatusResponse(BaseModel):
    """工作流状态响应"""
    workflow_name: Optional[str] = None
    is_active: bool = False
    completed: int = 0
    failed: int = 0
    paused: bool = False
    paused_nodes: list[str] = Field(default_factory=list)
    node_count: int = 0
    steps: int = 0
    pending_decisions: list[dict] = Field(default_factory=list)
    executions: dict[str, str] = Field(default_factory=dict)  # ← NEW (Phase 6.6.D)


# dashboard/app.py:WorkflowMermaidResponse 加 2 字段 (找现有定义附近)
class WorkflowMermaidResponse(BaseModel):
    workflow_name: str
    mermaid: str
    node_count: int
    has_decision_nodes: bool
    status_applied: bool = False           # ← NEW
    node_statuses: dict[str, str] = Field(default_factory=dict)  # ← NEW
```

**Step 3.5: GREEN — 改 `/mermaid` 端点**

```python
# dashboard/app.py:712 get_workflow_mermaid 改
@app.get(
    "/api/workflows/{workflow_name}/mermaid",
    response_model=WorkflowMermaidResponse,
)
def get_workflow_mermaid(
    workflow_name: str,
    include_status: bool = False,  # ← NEW query param
) -> WorkflowMermaidResponse:
    """渲染工作流 YAML 为 mermaid 字符串
    
    Query:
        include_status: true → 叠加当前活跃工作流的节点状态染色 (Phase 6.6.D)
    
    Raises:
        404: workflow YAML 不存在
        422: workflow 解析/验证失败
    """
    from infra.got.data_structures import NodeType
    from infra.got.visualizer import render_mermaid
    from infra.got.workflow_loader import (
        WorkflowError,
        WorkflowNotFoundError,
        load_workflow,
    )

    try:
        graph = load_workflow(workflow_name)
    except WorkflowNotFoundError:
        raise HTTPException(status_code=404, detail=f"workflow not found: {workflow_name}")
    except WorkflowError as e:
        raise HTTPException(status_code=422, detail=f"workflow load failed: {e}")

    # Phase 6.6.D: 叠加 status 染色
    status_applied = False
    node_statuses: dict[str, str] = {}
    executions: dict[str, Any] = {}
    if include_status:
        try:
            active = master_controller.get_active_workflow_status()
            if active.get("is_active"):
                # 转换 executions dict[node_id, status_str] 为 NodeExecution-like
                from infra.got.data_structures import NodeStatus
                for nid, st in active.get("executions", {}).items():
                    try:
                        # 构造 fake NodeExecution 只含 status
                        executions[nid] = type("FakeExec", (), {"status": NodeStatus(st)})()
                    except ValueError:
                        continue
                node_statuses = dict(active.get("executions", {}))
                status_applied = True
        except Exception:
            # 染色失败 → graceful degradation
            pass

    mermaid_str = render_mermaid(graph, executions=executions, include_classdef=True)
    has_decision = any(
        graph.get_node(nid).type == NodeType.DECISION
        for nid in graph.node_ids()
    )
    return WorkflowMermaidResponse(
        workflow_name=workflow_name,
        mermaid=mermaid_str,
        node_count=len(list(graph.node_ids())),
        has_decision_nodes=has_decision,
        status_applied=status_applied,
        node_statuses=node_statuses,
    )
```

**Step 3.6: 跑测试,验证 GREEN**

```bash
pytest tests/dashboard/test_mermaid_status.py -v
```

**Expected**: 3 tests PASS

**Step 3.7: 改前端 api + WorkflowsPage**

```js
// dashboard/frontend/src/api/index.js
export async function fetchWorkflowGraph(name, opts = {}) {
  const params = new URLSearchParams();
  if (opts.includeStatus) params.set('include_status', 'true');
  const qs = params.toString();
  return request(`/workflows/${name}/mermaid${qs ? `?${qs}` : ''}`);
}
```

```vue
<!-- dashboard/frontend/src/pages/WorkflowsPage.vue:228 -->
async function loadGraph() {
  if (!selected.value) return;
  graphLoading.value = true;
  error.value = null;
  try {
    graphData.value = await fetchWorkflowGraph(selected.value.name, { includeStatus: true });
  } catch (e) {
    error.value = `加载图失败: ${e?.message || e}`;
  } finally {
    graphLoading.value = false;
  }
}
```

**Step 3.8: 跑全量回归**

```bash
pytest tests/ -q --tb=line
```

**Expected**: 2044 passed (2043 + 1 new), 0 failed, 17 skipped

```bash
ruff check .
```

**Expected**: 0 warnings

**Step 3.9: 浏览器手测 (subagent 跑)**

1. 启动 dev server
2. 启动一个 novel_writing workflow
3. 等 review_chapter 完成、polish_chapter 暂停 (因 DECISION 节点)
4. 访问 `WorkflowsPage` → "📊 查看图"
5. 验证:
   - read_snapshot / write_chapter / review_chapter 节点是绿色 (completed)
   - polish_chapter 节点是黄色 (paused)
   - emit_chapter 节点是灰色 (pending)

**Step 3.10: 提交**

```bash
git add dashboard/protocols.py dashboard/app.py \
        dashboard/frontend/src/api/index.js \
        dashboard/frontend/src/pages/WorkflowsPage.vue \
        tests/dashboard/test_mermaid_status.py

git commit -m "$(cat <<'EOF'
feat(dashboard): phase 6.6.D — Mermaid 节点状态染色 (修正 Phase 6.3 启用缺失)

Phase 6.3 commit b21df02 的 render_mermaid(graph, executions={}) 传空 dict
导致所有节点 PENDING 黄色,染色功能压根没启用. 本 commit 修正:

- 后端 protocols.MasterControllerAdapter.get_active_workflow_status 加
  executions 字段 (dict[node_id, status_str]) — 现有 dashboard tests 走
  stub 走 is_active=False 路径不依赖此字段, 不破坏
- 后端 app.py WorkflowStatusResponse + WorkflowMermaidResponse 加新字段
  (status_applied / node_statuses) — Pydantic default {} 保后向兼容
- 后端 /mermaid 端点接受 ?include_status=true (默认 false):
  - 调 get_active_workflow_status() 拿 executions
  - 构造 fake NodeExecution 仅含 status 字段
  - 传给 render_mermaid(graph, executions=fake) 触发 class 染色
  - 失败/无活跃 → graceful degradation (status_applied=False, {})
- 前端 api/index.js fetchWorkflowGraph(name, { includeStatus: true })
- 前端 WorkflowsPage.vue loadGraph 传 includeStatus: true
- WorkflowGraph.vue 不动 (颜色烘进 mermaid class 声明)

测试:
- tests/dashboard/test_mermaid_status.py (NEW, 3 tests):
  - test_mermaid_endpoint_default_does_not_apply_status
  - test_mermaid_endpoint_with_include_status_returns_node_statuses
  - test_mermaid_endpoint_with_no_active_workflow_returns_empty_statuses
- 2043 → 2044 passed (+1), 0 failed, 17 skipped, ruff 0
EOF
)"
```

---

## Task 4: 全量回归 + MEMORY.md + push

**Step 4.1: 全量测试**

```bash
pytest tests/ -q --tb=line
```

**Expected**: 2044 passed, 0 failed, 17 skipped

**Step 4.2: ruff 全量**

```bash
ruff check .
```

**Expected**: 0 warnings

**Step 4.3: vite build (前端类型检查)**

```bash
cd dashboard/frontend && npm run build
```

**Expected**: build 通过 (如果有 TypeScript 错就修)

**Step 4.4: 追写 MEMORY.md (1 行 Phase 6.6 entry)**

在已有 Phase 6.5 之后追写 (line 13 后):

```markdown
- **Phase 6.6 (2026-06-04)**: Doc 4 §10 Phase 6.6 — dashboard UI 全面 polish (A+B+D) — A: DecisionsPage 2 tabs → 3 tabs (待处理/已完成折叠/已取消折叠) + 元信息排序 (priority 升序/resolved_at 倒序) [DecisionsPage.vue] — B: DecisionCard status badge (✅⏸❌ 绿/黄/灰) + meta info (解决人/选项/时间/原因) + resolve 守卫 (非 pending 隐藏操作按钮, 显示 🔒 提示) [DecisionCard.vue] — D: 修正 Phase 6.3 Mermaid 节点染色启用缺失 (原 render_mermaid 传空 dict 全 PENDING 黄) [protocols.py:209-253 加 executions 字段 + app.py WorkflowStatusResponse/WorkflowMermaidResponse 加 status_applied/node_statuses + /mermaid 端点接受 ?include_status=true query + api/index.js fetchWorkflowGraph 加 includeStatus 参数 + WorkflowsPage.vue loadGraph 传 includeStatus: true + WorkflowGraph.vue 0 改动 (颜色烘进 mermaid class)]; 1 新 test (test_mermaid_status.py 3 tests: default 不染色 / active workflow 染色 / 无活跃工作流 graceful degradation); 2043→2044 passed (+1), 0 failed, 17 skipped, ruff 0, vite build 通过; 3 commits 推送 origin/master
```

**Step 4.5: 最终 push (3 commits 已在 Task 1.6 / 2.7 / 3.10 推完, 此步 verify only)**

```bash
git log --oneline -5 origin/master
```

---

## Critical files

| 文件 | Task | 改动 | 行数 |
|------|------|------|------|
| `dashboard/frontend/src/pages/DecisionsPage.vue` | 1 | 3 tabs + 折叠 | +30 / -5 |
| `dashboard/frontend/src/components/DecisionCard.vue` | 2 | status badge + meta + 守卫 | +35 / -2 |
| `dashboard/protocols.py` | 3 | get_active_workflow_status 加 executions | +6 |
| `dashboard/app.py` | 3 | 2 Pydantic + 1 endpoint | +15 |
| `dashboard/frontend/src/api/index.js` | 3 | fetchWorkflowGraph 加 opts | +3 |
| `dashboard/frontend/src/pages/WorkflowsPage.vue` | 3 | loadGraph 传 includeStatus | +1 |
| `tests/dashboard/test_mermaid_status.py` (NEW) | 3 | 3 tests | +80 |

**预计 1 final commit (Task 4 验证) + 3 子 commits (Task 1/2/3 各一) = 4 commits 总**

---

## 风险 + 缓解

| 风险 | 缓解 |
|------|------|
| A/B 改 DecisionsPage/DecisionCard 视觉破坏现有布局 | 沿用 8-bit pixel 风格 + 复用 CSS 变量,只加元素不改网格 |
| D 后端加 `executions` 字段破坏旧 adapter 测试 | adapter 已有 stub 不返 `executions` → Pydantic 默认 `{}` → 不破坏 |
| D `?include_status=true` 但 controller 抛错 | 显式 try/except 调 controller,失败 graceful degradation (status_applied=False) |
| Mermaid 颜色可能跟 8-bit pixel 风格冲突 | 视觉上节点是 mermaid 默认白底,颜色是节点内部填色,不破坏页面整体风格 |
| Task 3 Step 3.5 的 fake NodeExecution 构造可能太 hacky | 改用 `from infra.got.data_structures import NodeExecution` 直接构造真实 dataclass (`NodeExecution(node_id=nid, status=NodeStatus(st), started_at=now)`) — 只需 .status 字段,其他可默认 |
| Task 3.1 的 _StubMaster 缺 MasterControllerLike 的其他方法 | 端点只需要 `get_active_workflow_status`,其他方法不会被调 (Mock 模式),不破坏 |

---

## TDD sub-phases

| 阶段 | 动作 | 累计测试 |
|------|------|---------|
| T1.1-1.6 | A: DecisionsPage 3 tabs (无 test, 浏览器手测) | 2043 |
| T2.1-2.7 | B: DecisionCard status badge (无 test, 浏览器手测) | 2043 |
| T3.1-3.2 | D RED: 3 tests FAIL | 2043 (3 fail) |
| T3.3-3.6 | D GREEN: protocols + app.py + 3 tests PASS | 2044 |
| T3.7 | D: 前端 2 文件 | 2044 |
| T3.8-3.10 | D: 全量回归 + 提交 | 2044 |
| T4.1-4.5 | 全量 + MEMORY.md + push | 2044 |

---

## 验证

### 集成验证 (Task 3.6)

```python
# 后端
from dashboard.app import create_app
from fastapi.testclient import TestClient
from pathlib import Path
import tempfile

class Stub:
    def get_active_workflow_status(self):
        return {
            "is_active": True,
            "workflow_name": "novel_writing",
            "completed": 2, "failed": 0, "paused": False, "paused_nodes": [],
            "node_count": 5, "steps": 1, "pending_decisions": [],
            "executions": {
                "read_snapshot": "completed",
                "write_chapter": "completed",
                "review_chapter": "running",
                "polish_chapter": "pending",
                "emit_chapter": "pending",
            },
        }

with tempfile.TemporaryDirectory() as tmp:
    app = create_app(db_path=Path(tmp) / "rp.db", master_controller=Stub())
    client = TestClient(app)
    resp = client.get("/api/workflows/novel_writing/mermaid?include_status=true")
    assert resp.status_code == 200
    data = resp.json()
    assert data["status_applied"] is True
    assert data["node_statuses"]["review_chapter"] == "running"
    assert "class review_chapter node-running" in data["mermaid"]
    assert "class polish_chapter node-pending" in data["mermaid"]
```

### 最终 baseline 目标
- 2043 → **2044 passed** (+1), 0 failed, 17 skipped
- ruff 0 warnings
- vite build 通过
- 3 commits pushed to origin/master (6.6.A / 6.6.B / 6.6.D)
- MEMORY.md +1 行 Phase 6.6 entry (保持 < 200 lines)
