# Phase 14.0 Tasks — P2 Performance

> **目的**: 把 `plan.md` 的 3 个 sub-tasks (T1-T3) + T6 verify/push 拆成可逐项追踪的 checklist，每项带 status / owner / deps / DoD。配套 `specs/` 设计 + `plans/ -plan.md` 实施步骤。
> **进度总览**: 3 sub-tasks (T1/T2/T3) + T6 收口。Baseline 3011 pytest + 1029 vitest → Target 3023+ pytest + 1029 vitest (本 phase 不触前端)。

---

## 总览看板

| Task | 主题 | Status | Owner | 新增 pytest | 新增 vitest | 新增 bench | 风险 |
|------|------|--------|-------|-------------|-------------|------------|------|
| T1 | H-PERF Cascade 邻接表 O(1) 查找 | pending | 主公 | +4 | 0 | +1 | R1 / R2 |
| T2 | H-PERF Qdrant async wrap (to_thread) | pending | 主公 | +3-4 | 0 | +1 | R3 / R4 |
| T3 | H-PERF _LRUCache TTL 防 stale | pending | 主公 | +3-4 | 0 | +1 | R5 / R6 |
| T6 | Verify + push + HANDOFF | pending | 主公 | 0 | 0 | +1 (aggregate) | R7 / R8 |
| **合计** | **3 项 P2 性能** | — | — | **+10-12** | **0** | **+4 (3 single + 1 agg)** | — |

---

## T1 — H-PERF Cascade 邻接表

**目标**: `reference_graph.py:trigger_cascade` 第 326-334 行 N+1 边查找 → O(1) via precompute `_adjacency_by_node`。

### 子任务清单

- [ ] **T1.1** 写 `novel-factory/tests/cross_volume/test_trigger_cascade_adjacency.py` 4 tests
  - deps: 无
  - owner: 主公
  - DoD: `pytest tests/cross_volume/test_trigger_cascade_adjacency.py` RED（缺新索引注入）
  - 覆盖：ingest_edge 索引建、双向索引、trigger_cascade 用新索引、helper 原子写双索引
- [ ] **T1.2** 改 `novel-factory/infra/cross_volume/reference_graph.py`
  - deps: T1.1
  - DoD: 4 tests GREEN
  - 改动: +25L / -8L
  - 关键点：
    - 新字段 `self._adjacency_by_node: dict[str, dict[str, str]]` 在 `__init__`
    - 抽 `_register_edge_in_indexes(edge)` helper（写 edges + set + adjacency 三处）
    - `ingest_edge` / `add_edge` 路由到 helper
    - `trigger_cascade` 用 `self._adjacency_by_node[current_id].get(neighbor.id)`
- [ ] **T1.3** 跑 pytest cross_volume 0 回归
  - deps: T1.2
  - DoD: `pytest tests/cross_volume/ -q --timeout=60` → ≥ 150 passed
- [ ] **T1.4** 写 `scripts/bench-cascade-adjacency.sh`
  - deps: T1.3
  - DoD: 跑 1000-node graph × 100 edges/node × 5 次，输出 `cascade adjacency: baseline Xms → new Yms (Zx speedup)` ≥ 5x
- [ ] **T1.5** ruff + commit
  - deps: T1.4
  - DoD: `ruff check infra/cross_volume/reference_graph.py` 0 issues
  - commit: `feat(cvg): phase 14.0 T1 — cascade 邻接表 O(1) 查找`
  - 风险：R1 (双索引同步) / R2 (callers 链断)

### 验证命令

```bash
cd novel-factory
pytest tests/cross_volume/test_trigger_cascade_adjacency.py  # RED → GREEN
pytest tests/cross_volume/ -q --timeout=60                     # 0 回归
bash scripts/bench-cascade-adjacency.sh                        # ≥ 5x speedup
ruff check infra/cross_volume/reference_graph.py               # 0 issues
```

---

## T2 — H-PERF Qdrant async wrap

**目标**: `qdrant_client.py` 加 `search_async` + `_raw_vector_search_async`, 用 `asyncio.to_thread()` 送 sync 主路径到 thread pool，释放 FastAPI event loop。

### 子任务清单

- [ ] **T2.1** 写 `novel-factory/tests/memory_system/test_qdrant_search_async.py` 3-4 tests
  - deps: 无
  - owner: 主公
  - DoD: `pytest tests/memory_system/test_qdrant_search_async.py` RED（缺 `search_async` 方法）
  - 覆盖：sync/async 返回等价、并发不阻塞 event loop、exception 透传
- [ ] **T2.2** 改 `novel-factory/infra/memory_system/vector/qdrant_client.py`
  - deps: T2.1
  - DoD: 3-4 tests GREEN
  - 改动: +30L / 0L
  - 关键点：
    - 新 `import asyncio`
    - `async def search_async(...)` 包 `await asyncio.to_thread(self.search, ...)`
    - `async def _raw_vector_search_async(...)` 同模式
    - 全部 sync 方法（`search` / `upsert` / `delete` / `batch_search` / `search_with_filter` 等）不动
- [ ] **T2.3** 跑 pytest memory_system 0 回归
  - deps: T2.2
  - DoD: `pytest tests/memory_system/ -q --timeout=120` → ≥ 40 passed（含 T2.1 新增）
  - 风险：R3（pytest-asyncio 兼容）/ R4（sync 主路径被误改）
- [ ] **T2.4** 写 `scripts/bench-qdrant-async.sh`
  - deps: T2.3
  - DoD: monkeypatched 200ms/次延迟，5 次并发 async vs 5 次串行 sync → ≥ 4x speedup
- [ ] **T2.5** ruff + commit
  - deps: T2.4
  - DoD: `ruff check infra/memory_system/vector/qdrant_client.py` 0 issues
  - commit: `feat(memory): phase 14.0 T2 — Qdrant async wrap (to_thread)`

### 验证命令

```bash
cd novel-factory
pytest tests/memory_system/test_qdrant_search_async.py            # RED → GREEN
pytest tests/memory_system/ -q --timeout=120                       # 0 回归
bash scripts/bench-qdrant-async.sh                                  # ≥ 4x speedup
ruff check infra/memory_system/vector/qdrant_client.py             # 0 issues
```

---

## T3 — H-PERF _LRUCache TTL

**目标**: `qdrant_client.py:_LRUCache` 加 `ttl_seconds` 字段，`get` 检查 stale 后移除；防 `embedded chapter新` 时旧 `vector search cache` 命中。

### 子任务清单

- [ ] **T3.1** 写 `novel-factory/tests/memory_system/test_qdrant_cache_ttl.py` 3-4 tests
  - deps: 无
  - owner: 主公
  - DoD: `pytest tests/memory_system/test_qdrant_cache_ttl.py` RED（缺 `ttl_seconds` 字段）
  - 覆盖：stale entry 被淘汰、新鲜 entry 返回、TTL=0 禁用、put 同 key 不刷 ts
- [ ] **T3.2** 改 `novel-factory/infra/memory_system/vector/qdrant_client.py` `_LRUCache` 类
  - deps: T3.1
  - DoD: 3-4 tests GREEN
  - 改动: +35L / -15L
  - 关键点：
    - `_LRUCache.__init__(max_size=1000, ttl_seconds=300.0)`
    - `_cache: dict[str, tuple[Any, float]]`
    - `get` 检查 `time.monotonic() - ts > ttl_seconds` 则 del + 返 None
    - `put(key, value, ts=None)` 同 key 只刷 value 保留 ts
    - `QdrantClientWrapper.__init__` 读 yaml `retrieval.cache_ttl_seconds` 默认 300
- [ ] **T3.3** 跑 pytest memory_system 0 回归
  - deps: T3.2
  - DoD: `pytest tests/memory_system/ -q --timeout=120` → ≥ 45 passed（含 T2.1 + T3.1 新增）
  - 风险：R5（TTL 太短命中率降）/ R6（monotonic 跨重启）
- [ ] **T3.4** 写 `scripts/bench-qdrant-cache-ttl.sh`
  - deps: T3.3
  - DoD: put 100 个 → 全部立刻命中；等 TTL×2 → 全部 stale miss；新 100 个 put → 全部命中 → `stale_hit_avoided=100/100 (100% effective)`
- [ ] **T3.5** ruff + commit
  - deps: T3.4
  - DoD: `ruff check infra/memory_system/vector/qdrant_client.py` 0 issues
  - commit: `feat(memory): phase 14.0 T3 — _LRUCache TTL 防 stale`

### 验证命令

```bash
cd novel-factory
pytest tests/memory_system/test_qdrant_cache_ttl.py              # RED → GREEN
pytest tests/memory_system/ -q --timeout=120                      # 0 回归
bash scripts/bench-qdrant-cache-ttl.sh                            # 100% effective
ruff check infra/memory_system/vector/qdrant_client.py            # 0 issues
```

---

## T6 — Verify + push + HANDOFF

**目标**: verify-p2 全 PASS + push 9 commits + HANDOFF §0.1 新增一行。

### 子任务清单

- [ ] **T6.1** 写 `scripts/verify-p2-performance.sh` 5 检查项
  - deps: T1.5 + T2.5 + T3.5
  - DoD: `bash scripts/verify-p2-performance.sh` → 5/5 PASS
- [ ] **T6.2** 写 `scripts/bench-p2.sh` 聚合 3 个 bench
  - deps: T6.1
  - DoD: 输出 markdown 表格，3/3 bench script PASS
- [ ] **T6.3** CI 接入 (`.github/workflows/test.yml`)
  - deps: T6.2
  - DoD: 新 step "Verify Phase 14.0 P2 performance"，working-dir ./，3.13 only，阻断
- [ ] **T6.4** ci/chore commit
  - deps: T6.3
  - DoD: `chore(ci): phase 14.0 T6 — wire P2 verify + bench aggregate`
- [ ] **T6.5** 全量 pytest 子集
  - deps: T6.4
  - DoD: `pytest tests/{cross_volume,memory_system,dashboard,cvg_cli,scripts}/ -q --timeout=180` → ≥ 3023 passed
- [ ] **T6.6** 全量 vitest + ruff + e2e-live
  - deps: T6.5
  - DoD: vitest 1029, ruff 0, e2e-live 95 passed
- [ ] **T6.7** docs(handoff) + git push
  - deps: T6.6
  - DoD: HANDOFF.md §0.1 新增 Phase 14.0 entry, `git push origin master`
  - commit: `docs(handoff): Phase 14.0 P2 性能（T1-T3）条目`
  - 风险：R7（e2e regression）/ R8（commit msg 风格）

### 验证命令

```bash
bash scripts/verify-p2-performance.sh   # 5/5 PASS
bash scripts/bench-p2.sh                # 3/3 PASS
pytest tests/cross_volume/ tests/memory_system/ tests/dashboard/ \
       tests/cvg_cli/ tests/scripts/ \
       -q --deselect tests/dashboard/test_creator_endpoints.py::test_creator_v41_logic_check_issues \
       --deselect tests/dashboard/test_creator_endpoints.py::test_creator_v42_logic_check_p0_only \
       --deselect tests/dashboard/test_creator_endpoints.py::test_creator_v43_logic_check_chapter \
       --deselect tests/dashboard/test_creator_endpoints.py::test_creator_v45_logic_check_paragraph \
       --timeout=180                                  # ≥ 3023 passed
cd dashboard/frontend && pnpm vitest run              # 1029 passed
cd ../.. && ruff check .                              # All checks passed
bash scripts/verify-e2e-live-ci.sh 2>&1 | tail -3      # 95 passed (CI parity OK)
git push origin master                                # 9 commits 总
```

---

## 进度跟踪

| 完成度 | 时间点 | 状态变化 |
|--------|--------|----------|
| 0% | T0 已审计 3 目标完成 | 进入 spec → plan → tasks |
| 25% | T1 GREEN | cascade 邻接表注入，pytest 150+ 0 回归 |
| 50% | T2 GREEN | Qdrant async 方法加，pytest 40+ 0 回归 |
| 75% | T3 GREEN | _LRUCache TTL 注入，pytest 45+ 0 回归 |
| 100% | T6 complete | verify-p2 5/5 + e2e 95 + push + HANDOFF |

---
*Last update: 2026-07-10 (Phase 14.0 kickoff)*
