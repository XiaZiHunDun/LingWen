# 反套路创意注入机制

> 版本：v1.0 | 日期：2026-05-31
> 用途：对抗AI内容套路化问题，注入新鲜感

---

## 一、核心概念

### 1.1 套路预警
AI写作容易陷入的常见套路：
- 废柴逆袭：主角从弱小到强大
- 退婚打脸：经典爽文模式
- 升级流：打怪升级换地图
- 暧昧套路：多角恋纠葛
- 机械降神：危机时神秘力量救场

### 1.2 反套路策略
| 套路类型 | 反套路手段 |
|---------|-----------|
| 废柴逆袭 | 让主角起点即高，但面临更高挑战 |
| 退婚打脸 | 取消打脸，让对方成为盟友 |
| 升级流 | 淡化等级，强调智慧/策略 |
| 暧昧套路 | 建立稳定关系，面对外部威胁 |
| 机械降神 | 让主角靠自身能力解决危机 |

---

## 二、创意注入工具

### 2.1 高概念创意库

**路径**：`01_灵感库/创意库/高概念创意.yaml`

```yaml
高概念创意:
  - id: "HC001"
    名称: "时间悖论循环"
    描述: "主角的行动导致了一个悖论，必须在未来阻止自己"
    适用场景: "高潮转折"
    反套路标签: ["非升级流", "智慧主导"]

  - id: "HC002"
    名称: "盟友即敌人"
    描述: "最信任的同伴是最终Boss的化身"
    适用场景: "信任危机"
    反套路标签: ["背叛循环", "无真正盟友"]

  - id: "HC003"
    名称: "力量代价"
    描述: "每次使用重大力量都会失去一段记忆"
    适用场景: "能力使用"
    反套路标签: ["成长非线性的", "记忆驱动"]

  - id: "HC004"
    名称: "沉默主角"
    描述: "主角几乎不说话，靠行动而非台词塑造"
    适用场景: "人物塑造"
    反套路标签: ["去话语中心", "行动驱动"]

  - id: "HC005"
    名称: "双赢结局"
    描述: "最终不是打败敌人，而是找到共存之道"
    适用场景: "结局设计"
    反套路标签: ["非零和", "和解导向"]
```

### 2.2 随机组合引擎

**路径**：`tools/anti_trope_generator.py`

```python
#!/usr/bin/env python3
"""
反套路创意生成器
随机组合不同元素创造新颖剧情

使用方法:
    python anti_trope_generator.py --mode random --count 3
    python anti_trope_generator.py --mode prompt "星际+克苏鲁"
"""

import random
import yaml
from pathlib import Path

PROJECT_ROOT = Path(__file__).parent.parent

class AntiTropeGenerator:
    """反套路创意生成器"""

    # 基础元素池
    ELEMENTS = {
        "setting": [
            "星际修真文明", "废土朋克世界", "赛博都市",
            "远古遗迹", "平行世界", "时间夹缝"
        ],
        "conflict": [
            "生存竞争", "理念分歧", "记忆争夺",
            "身份认同", "力量代价", "时间循环"
        ],
        "character": [
            "沉默型主角", "双面间谍", "失去过去的人",
            "不完美的英雄", "反英雄", "群体主角"
        ],
        "twist": [
            "盟友背叛", "敌人救主角", "主角是造物主",
            "时间倒流", "记忆错位", "虚假结局"
        ]
    }

    # 元素组合规则（避免套路组合）
    FORBIDDEN_COMBOS = [
        ("废土", "废柴逆袭"),  # 废土题材常见的废柴逆袭套路
        ("退婚", "打脸"),       # 退婚打脸套路
        ("升级", "换地图"),     # 无限换地图升级套路
    ]

    def __init__(self, concept_db_path: str = None):
        if concept_db_path is None:
            concept_db_path = PROJECT_ROOT / "01_灵感库" / "创意库" / "高概念创意.yaml"
        self.concept_db = self._load_concepts(concept_db_path)

    def _load_concepts(self, path: Path) -> list:
        """加载高概念创意库"""
        if path.exists():
            with open(path, 'r', encoding='utf-8') as f:
                data = yaml.safe_load(f)
                return data.get('高概念创意', [])
        return []

    def generate_random(self, count: int = 3) -> list:
        """
        随机生成反套路创意组合

        Args:
            count: 生成数量

        Returns:
            创意组合列表
        """
        results = []
        for _ in range(count):
            combo = {
                "setting": random.choice(self.ELEMENTS["setting"]),
                "conflict": random.choice(self.ELEMENTS["conflict"]),
                "character": random.choice(self.ELEMENTS["character"]),
                "twist": random.choice(self.ELEMENTS["twist"]),
            }

            # 检查是否有禁用组合
            if self._is_forbidden(combo):
                # 重新生成
                return self.generate_random(count)[0]

            results.append(combo)

        return results

    def _is_forbidden(self, combo: dict) -> bool:
        """检查是否有禁用组合"""
        for f_set, f_trope in self.FORBIDDEN_COMBOS:
            if f_set in combo["setting"] and f_trope in combo["conflict"]:
                return True
        return False

    def generate_from_prompt(self, prompt: str) -> list:
        """
        基于提示词生成创意（如"克苏鲁+职场"）

        Args:
            prompt: 用户输入的提示词

        Returns:
            创意组合列表
        """
        # 解析提示词中的元素
        elements = [e.strip() for e in prompt.split("+")]

        results = []
        for elem in elements:
            combo = {
                "setting": elem if "星际" in elem or "废土" in elem else random.choice(self.ELEMENTS["setting"]),
                "conflict": elem if "竞争" in elem or "分歧" in elem else random.choice(self.ELEMENTS["conflict"]),
                "character": elem if "主角" in elem else random.choice(self.ELEMENTS["character"]),
                "twist": random.choice(self.ELEMENTS["twist"]),
            }
            results.append(combo)

        return results

    def format_output(self, combos: list) -> str:
        """格式化输出创意组合"""
        lines = ["=" * 50, "反套路创意组合", "=" * 50]

        for i, combo in enumerate(combos, 1):
            lines.append(f"\n【组合 {i}】")
            lines.append(f"  世界观: {combo['setting']}")
            lines.append(f"  核心冲突: {combo['conflict']}")
            lines.append(f"  主角设定: {combo['character']}")
            lines.append(f"  转折设计: {combo['twist']}")

            # 检查是否符合高概念库
            matched = self._match_concepts(combo)
            if matched:
                lines.append(f"  匹配高概念: {matched['名称']}")

        return "\n".join(lines)

    def _match_concepts(self, combo: dict) -> dict | None:
        """匹配高概念库中的创意"""
        for concept in self.concept_db:
            tags = concept.get('反套路标签', [])
            for tag in tags:
                if tag in str(combo.values()):
                    return concept
        return None


def main():
    import argparse
    parser = argparse.ArgumentParser(description='反套路创意生成器')
    parser.add_argument('--mode', choices=['random', 'prompt'], default='random',
                        help='生成模式')
    parser.add_argument('--count', type=int, default=3,
                        help='生成数量')
    parser.add_argument('--prompt', type=str, default='',
                        help='提示词（用于prompt模式，如"克苏鲁+职场"）')

    args = parser.parse_args()

    generator = AntiTropeGenerator()

    if args.mode == 'random':
        combos = generator.generate_random(args.count)
    else:
        combos = generator.generate_from_prompt(args.prompt)

    print(generator.format_output(combos))


if __name__ == '__main__':
    main()
```

---

## 三、使用流程

### 3.1 注入时机

| 阶段 | 注入内容 | 执行者 |
|------|---------|--------|
| 项目立项 | 高概念创意选择 | 灵感部门 |
| 大纲设计 | 反套路元素融入 | outline_master |
| 章节写作 | 套路检测+替换 | content_writer |
| 审核期 | 反套路评估 | auditor |

### 3.2 注入标准

每20章必须注入一次"高概念创意"：
- 检查是否有套路化趋势
- 引入新的反套路元素
- 记录注入效果

### 3.3 套路检测报告

```yaml
套路检测报告:
  检测章节: ch080-ch100
  检测结果:
    升级流指数: 0.7  # 阈值0.8
    打脸场景数: 2    # 阈值1
    暧昧重复度: 0.3  # 正常
  建议注入:
    - HC002 "盟友即敌人"
    - HC004 "沉默主角"
  注入效果跟踪: 待评估
```

---

## 四、套路检测器集成

在现有 `repetitive_phrase_checker.py` 基础上扩展：

```python
# 新增套路类型检测
TROPE_PATTERNS = {
    "废柴逆袭": ["从弱小到强大", "被人看不起", "逆袭成功"],
    "退婚打脸": ["退婚", "打脸", "后悔"],
    "升级流": ["突破", "升级", "换地图"],
    "机械降神": ["突然出现", "神秘力量", "无条件帮助"],
}
```

---

*最后更新：2026-05-31*
*维护部门：灵感部门*